['text':'* Create a value of type `To` from the bits of `from`.
 *
 * similar to `std::bit_cast` but compatible with C++17,
 * should perform similar to `*reinterpret_cast<To*>(&from)`
 * or through punning without expecting any undefined behaviors.
 ','line_number':15,'multiline':True]
['text':' namespace np','line_number':49,'multiline':False]
['text':' NUMPY_CORE_SRC_COMMON_UTILS_HPP','line_number':50,'multiline':False]
