['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]
['text':'*
 * Runs immediately and whenever a transaction ends and an observed observable changed.
 * {@link fn} should start with a JS Doc using `@description` to name the autorun.
 ','line_number':11,'multiline':True]
['text':'*
 * Runs immediately and whenever a transaction ends and an observed observable changed.
 * {@link fn} should start with a JS Doc using `@description` to name the autorun.
 ','line_number':19,'multiline':True]
['text':'*
 * Runs immediately and whenever a transaction ends and an observed observable changed.
 * {@link fn} should start with a JS Doc using `@description` to name the autorun.
 *
 * Use `createEmptyChangeSummary` to create a "change summary" that can collect the changes.
 * Use `handleChange` to add a reported change to the change summary.
 * The run function is given the last change summary.
 * The change summary is discarded after the run function was called.
 *
 * @see autorun
 ','line_number':27,'multiline':True]
['text':'*
 * @see autorunHandleChanges (but with a disposable store that is cleared before the next run or on dispose)
 ','line_number':49,'multiline':True]
['text':'*
 * @see autorun (but with a disposable store that is cleared before the next run or on dispose)
 ','line_number':78,'multiline':True]
['text':'*
	 * A dependency could have changed.
	 * We need to explicitly ask them if at least one dependency changed.
	 ','line_number':99,'multiline':True]
['text':'*
	 * A dependency changed and we need to recompute.
	 ','line_number':105,'multiline':True]
['text':' We don't want our observed observables to think that they are (not even temporarily) not being observed.','line_number':180,'multiline':False]
['text':' Thus, we only unsubscribe from observables that are definitely not read anymore.','line_number':181,'multiline':False]
['text':' IObserver implementation','line_number':193,'multiline':False]
['text':' The other dependencies will refresh on demand','line_number':209,'multiline':False]
['text':' IReader implementation','line_number':242,'multiline':False]
['text':' In case the run action disposes the autorun','line_number':244,'multiline':False]
