['text':'
**********************************************************************
* Copyright (c) 2004-2016, International Business Machines
* Corporation and others.  All Rights Reserved.
**********************************************************************
* Author: Alan Liu
* Created: April 26, 2004
* Since: ICU 3.0
**********************************************************************
','line_number':1,'multiline':True]
['text':'*
 * \file 
 * \brief C++ API: A unit for measuring a quantity.
 ','line_number':20,'multiline':True]
['text':'*
 * A unit such as length, mass, volume, currency, etc.  A unit is
 * coupled with a numeric amount to produce a Measure.
 *
 * @author Alan Liu
 * @stable ICU 3.0
 ','line_number':29,'multiline':True]
['text':'*
     * Default constructor.
     * @stable ICU 3.0
     ','line_number':39,'multiline':True]
['text':'*
     * Copy constructor.
     * @stable ICU 3.0
     ','line_number':47,'multiline':True]
['text':'*
     * Assignment operator.
     * @stable ICU 3.0
     ','line_number':53,'multiline':True]
['text':'*
     * Returns a polymorphic clone of this object.  The result will
     * have the same class as returned by getDynamicClassID().
     * @stable ICU 3.0
     ','line_number':59,'multiline':True]
['text':'*
     * Destructor
     * @stable ICU 3.0
     ','line_number':66,'multiline':True]
['text':'*
     * Equality operator.  Return true if this object is equal
     * to the given object.
     * @stable ICU 3.0
     ','line_number':72,'multiline':True]
['text':'*
     * Inequality operator.  Return true if this object is not equal
     * to the given object.
     * @stable ICU 53
     ','line_number':79,'multiline':True]
['text':'*
     * Get the type.
     * @stable ICU 53
     ','line_number':88,'multiline':True]
['text':'*
     * Get the sub type.
     * @stable ICU 53
     ','line_number':94,'multiline':True]
['text':'*
     * getAvailable gets all of the available units.
     * If there are too many units to fit into destCapacity then the
     * error code is set to U_BUFFER_OVERFLOW_ERROR.
     *
     * @param destArray destination buffer.
     * @param destCapacity number of MeasureUnit instances available at dest.
     * @param errorCode ICU error code.
     * @return number of available units.
     * @stable ICU 53
     ','line_number':100,'multiline':True]
['text':'*
     * getAvailable gets all of the available units for a specific type.
     * If there are too many units to fit into destCapacity then the
     * error code is set to U_BUFFER_OVERFLOW_ERROR.
     *
     * @param type the type
     * @param destArray destination buffer.
     * @param destCapacity number of MeasureUnit instances available at dest.
     * @param errorCode ICU error code.
     * @return number of available units for type.
     * @stable ICU 53
     ','line_number':116,'multiline':True]
['text':'*
     * getAvailableTypes gets all of the available types. Caller owns the
     * returned StringEnumeration and must delete it when finished using it.
     *
     * @param errorCode ICU error code.
     * @return the types.
     * @stable ICU 53
     ','line_number':134,'multiline':True]
['text':'*
     * Return the class ID for this class. This is useful only for comparing to
     * a return value from getDynamicClassID(). For example:
     * <pre>
     * .   Base* polymorphic_pointer = createPolymorphicObject();
     * .   if (polymorphic_pointer->getDynamicClassID() ==
     * .       erived::getStaticClassID()) ...
     * </pre>
     * @return          The class ID for all objects of this class.
     * @stable ICU 53
     ','line_number':144,'multiline':True]
['text':'*
     * Returns a unique class ID POLYMORPHICALLY. Pure virtual override. This
     * method is to implement a simple version of RTTI, since not all C++
     * compilers support genuine RTTI. Polymorphic operator==() and clone()
     * methods call this method.
     *
     * @return          The class ID for this object. All objects of a
     *                  given class have the same class ID.  Objects of
     *                  other classes have different class IDs.
     * @stable ICU 53
     ','line_number':157,'multiline':True]
['text':'*
     * ICU use only.
     * Returns associated array index for this measure unit. Only valid for
     * non-currency measure units.
     * @internal
     ','line_number':171,'multiline':True]
['text':'*
     * ICU use only.
     * Returns maximum value from getIndex plus 1.
     * @internal
     ','line_number':179,'multiline':True]
['text':'*
     * ICU use only.
     * @return the unit.getIndex() of the unit which has this unit.getType() and unit.getSubtype(),
     *         or a negative value if there is no such unit
     * @internal
     ','line_number':186,'multiline':True]
['text':'*
     * ICU use only.
     * @internal
     ','line_number':194,'multiline':True]
['text':' U_HIDE_INTERNAL_API ','line_number':200,'multiline':True]
['text':' All code between the "Start generated createXXX methods" comment and','line_number':202,'multiline':False]
['text':' the "End generated createXXX methods" comment is auto generated code','line_number':203,'multiline':False]
['text':' and must not be edited manually. For instructions on how to correctly','line_number':204,'multiline':False]
['text':' update this code, refer to:','line_number':205,'multiline':False]
['text':' http://site.icu-project.org/design/formatting/measureformat/updating-measure-unit','line_number':206,'multiline':False]
['text':'','line_number':207,'multiline':False]
['text':' Start generated createXXX methods','line_number':208,'multiline':False]
['text':'*
     * Returns unit of acceleration: g-force.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':210,'multiline':True]
['text':'*
     * Returns unit of acceleration: meter-per-second-squared.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':218,'multiline':True]
['text':'*
     * Returns unit of angle: arc-minute.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':226,'multiline':True]
['text':'*
     * Returns unit of angle: arc-second.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':234,'multiline':True]
['text':'*
     * Returns unit of angle: degree.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':242,'multiline':True]
['text':'*
     * Returns unit of angle: radian.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':250,'multiline':True]
['text':'*
     * Returns unit of angle: revolution.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':259,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':266,'multiline':True]
['text':'*
     * Returns unit of area: acre.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':268,'multiline':True]
['text':'*
     * Returns unit of area: hectare.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':276,'multiline':True]
['text':'*
     * Returns unit of area: square-centimeter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':284,'multiline':True]
['text':'*
     * Returns unit of area: square-foot.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':292,'multiline':True]
['text':'*
     * Returns unit of area: square-inch.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':300,'multiline':True]
['text':'*
     * Returns unit of area: square-kilometer.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':308,'multiline':True]
['text':'*
     * Returns unit of area: square-meter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':316,'multiline':True]
['text':'*
     * Returns unit of area: square-mile.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':324,'multiline':True]
['text':'*
     * Returns unit of area: square-yard.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':332,'multiline':True]
['text':'*
     * Returns unit of concentr: karat.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':340,'multiline':True]
['text':'*
     * Returns unit of concentr: milligram-per-deciliter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 57
     ','line_number':349,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':356,'multiline':True]
['text':'*
     * Returns unit of concentr: millimole-per-liter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 57
     ','line_number':359,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':366,'multiline':True]
['text':'*
     * Returns unit of concentr: part-per-million.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 57
     ','line_number':369,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':376,'multiline':True]
['text':'*
     * Returns unit of consumption: liter-per-100kilometers.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':379,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':386,'multiline':True]
['text':'*
     * Returns unit of consumption: liter-per-kilometer.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':388,'multiline':True]
['text':'*
     * Returns unit of consumption: mile-per-gallon.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':396,'multiline':True]
['text':'*
     * Returns unit of consumption: mile-per-gallon-imperial.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 57
     ','line_number':405,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':412,'multiline':True]
['text':'*
     * Returns unit of digital: bit.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':414,'multiline':True]
['text':'*
     * Returns unit of digital: byte.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':422,'multiline':True]
['text':'*
     * Returns unit of digital: gigabit.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':430,'multiline':True]
['text':'*
     * Returns unit of digital: gigabyte.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':438,'multiline':True]
['text':'*
     * Returns unit of digital: kilobit.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':446,'multiline':True]
['text':'*
     * Returns unit of digital: kilobyte.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':454,'multiline':True]
['text':'*
     * Returns unit of digital: megabit.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':462,'multiline':True]
['text':'*
     * Returns unit of digital: megabyte.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':470,'multiline':True]
['text':'*
     * Returns unit of digital: terabit.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':478,'multiline':True]
['text':'*
     * Returns unit of digital: terabyte.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':486,'multiline':True]
['text':'*
     * Returns unit of duration: century.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':495,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':502,'multiline':True]
['text':'*
     * Returns unit of duration: day.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':504,'multiline':True]
['text':'*
     * Returns unit of duration: hour.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':512,'multiline':True]
['text':'*
     * Returns unit of duration: microsecond.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':520,'multiline':True]
['text':'*
     * Returns unit of duration: millisecond.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':528,'multiline':True]
['text':'*
     * Returns unit of duration: minute.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':536,'multiline':True]
['text':'*
     * Returns unit of duration: month.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':544,'multiline':True]
['text':'*
     * Returns unit of duration: nanosecond.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':552,'multiline':True]
['text':'*
     * Returns unit of duration: second.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':560,'multiline':True]
['text':'*
     * Returns unit of duration: week.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':568,'multiline':True]
['text':'*
     * Returns unit of duration: year.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':576,'multiline':True]
['text':'*
     * Returns unit of electric: ampere.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':584,'multiline':True]
['text':'*
     * Returns unit of electric: milliampere.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':592,'multiline':True]
['text':'*
     * Returns unit of electric: ohm.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':600,'multiline':True]
['text':'*
     * Returns unit of electric: volt.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':608,'multiline':True]
['text':'*
     * Returns unit of energy: calorie.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':616,'multiline':True]
['text':'*
     * Returns unit of energy: foodcalorie.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':624,'multiline':True]
['text':'*
     * Returns unit of energy: joule.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':632,'multiline':True]
['text':'*
     * Returns unit of energy: kilocalorie.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':640,'multiline':True]
['text':'*
     * Returns unit of energy: kilojoule.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':648,'multiline':True]
['text':'*
     * Returns unit of energy: kilowatt-hour.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':656,'multiline':True]
['text':'*
     * Returns unit of frequency: gigahertz.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':664,'multiline':True]
['text':'*
     * Returns unit of frequency: hertz.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':672,'multiline':True]
['text':'*
     * Returns unit of frequency: kilohertz.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':680,'multiline':True]
['text':'*
     * Returns unit of frequency: megahertz.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':688,'multiline':True]
['text':'*
     * Returns unit of length: astronomical-unit.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':696,'multiline':True]
['text':'*
     * Returns unit of length: centimeter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':704,'multiline':True]
['text':'*
     * Returns unit of length: decimeter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':712,'multiline':True]
['text':'*
     * Returns unit of length: fathom.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':720,'multiline':True]
['text':'*
     * Returns unit of length: foot.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':728,'multiline':True]
['text':'*
     * Returns unit of length: furlong.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':736,'multiline':True]
['text':'*
     * Returns unit of length: inch.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':744,'multiline':True]
['text':'*
     * Returns unit of length: kilometer.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':752,'multiline':True]
['text':'*
     * Returns unit of length: light-year.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':760,'multiline':True]
['text':'*
     * Returns unit of length: meter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':768,'multiline':True]
['text':'*
     * Returns unit of length: micrometer.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':776,'multiline':True]
['text':'*
     * Returns unit of length: mile.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':784,'multiline':True]
['text':'*
     * Returns unit of length: mile-scandinavian.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':793,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':800,'multiline':True]
['text':'*
     * Returns unit of length: millimeter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':802,'multiline':True]
['text':'*
     * Returns unit of length: nanometer.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':810,'multiline':True]
['text':'*
     * Returns unit of length: nautical-mile.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':818,'multiline':True]
['text':'*
     * Returns unit of length: parsec.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':826,'multiline':True]
['text':'*
     * Returns unit of length: picometer.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':834,'multiline':True]
['text':'*
     * Returns unit of length: yard.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':842,'multiline':True]
['text':'*
     * Returns unit of light: lux.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':850,'multiline':True]
['text':'*
     * Returns unit of mass: carat.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':858,'multiline':True]
['text':'*
     * Returns unit of mass: gram.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':866,'multiline':True]
['text':'*
     * Returns unit of mass: kilogram.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':874,'multiline':True]
['text':'*
     * Returns unit of mass: metric-ton.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':882,'multiline':True]
['text':'*
     * Returns unit of mass: microgram.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':890,'multiline':True]
['text':'*
     * Returns unit of mass: milligram.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':898,'multiline':True]
['text':'*
     * Returns unit of mass: ounce.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':906,'multiline':True]
['text':'*
     * Returns unit of mass: ounce-troy.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':914,'multiline':True]
['text':'*
     * Returns unit of mass: pound.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':922,'multiline':True]
['text':'*
     * Returns unit of mass: stone.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':930,'multiline':True]
['text':'*
     * Returns unit of mass: ton.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':938,'multiline':True]
['text':'*
     * Returns unit of power: gigawatt.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':946,'multiline':True]
['text':'*
     * Returns unit of power: horsepower.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':954,'multiline':True]
['text':'*
     * Returns unit of power: kilowatt.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':962,'multiline':True]
['text':'*
     * Returns unit of power: megawatt.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':970,'multiline':True]
['text':'*
     * Returns unit of power: milliwatt.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':978,'multiline':True]
['text':'*
     * Returns unit of power: watt.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':986,'multiline':True]
['text':'*
     * Returns unit of pressure: hectopascal.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':994,'multiline':True]
['text':'*
     * Returns unit of pressure: inch-hg.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1002,'multiline':True]
['text':'*
     * Returns unit of pressure: millibar.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1010,'multiline':True]
['text':'*
     * Returns unit of pressure: millimeter-of-mercury.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1018,'multiline':True]
['text':'*
     * Returns unit of pressure: pound-per-square-inch.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1026,'multiline':True]
['text':'*
     * Returns unit of speed: kilometer-per-hour.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1034,'multiline':True]
['text':'*
     * Returns unit of speed: knot.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':1043,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':1050,'multiline':True]
['text':'*
     * Returns unit of speed: meter-per-second.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1052,'multiline':True]
['text':'*
     * Returns unit of speed: mile-per-hour.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1060,'multiline':True]
['text':'*
     * Returns unit of temperature: celsius.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1068,'multiline':True]
['text':'*
     * Returns unit of temperature: fahrenheit.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1076,'multiline':True]
['text':'*
     * Returns unit of temperature: generic.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':1085,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':1092,'multiline':True]
['text':'*
     * Returns unit of temperature: kelvin.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1094,'multiline':True]
['text':'*
     * Returns unit of volume: acre-foot.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1102,'multiline':True]
['text':'*
     * Returns unit of volume: bushel.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1110,'multiline':True]
['text':'*
     * Returns unit of volume: centiliter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1118,'multiline':True]
['text':'*
     * Returns unit of volume: cubic-centimeter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1126,'multiline':True]
['text':'*
     * Returns unit of volume: cubic-foot.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1134,'multiline':True]
['text':'*
     * Returns unit of volume: cubic-inch.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1142,'multiline':True]
['text':'*
     * Returns unit of volume: cubic-kilometer.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1150,'multiline':True]
['text':'*
     * Returns unit of volume: cubic-meter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1158,'multiline':True]
['text':'*
     * Returns unit of volume: cubic-mile.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1166,'multiline':True]
['text':'*
     * Returns unit of volume: cubic-yard.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1174,'multiline':True]
['text':'*
     * Returns unit of volume: cup.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1182,'multiline':True]
['text':'*
     * Returns unit of volume: cup-metric.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':1191,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':1198,'multiline':True]
['text':'*
     * Returns unit of volume: deciliter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1200,'multiline':True]
['text':'*
     * Returns unit of volume: fluid-ounce.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1208,'multiline':True]
['text':'*
     * Returns unit of volume: gallon.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1216,'multiline':True]
['text':'*
     * Returns unit of volume: gallon-imperial.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 57
     ','line_number':1225,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':1232,'multiline':True]
['text':'*
     * Returns unit of volume: hectoliter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1234,'multiline':True]
['text':'*
     * Returns unit of volume: liter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 53
     ','line_number':1242,'multiline':True]
['text':'*
     * Returns unit of volume: megaliter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1250,'multiline':True]
['text':'*
     * Returns unit of volume: milliliter.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1258,'multiline':True]
['text':'*
     * Returns unit of volume: pint.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1266,'multiline':True]
['text':'*
     * Returns unit of volume: pint-metric.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @draft ICU 56
     ','line_number':1275,'multiline':True]
['text':' U_HIDE_DRAFT_API ','line_number':1282,'multiline':True]
['text':'*
     * Returns unit of volume: quart.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1284,'multiline':True]
['text':'*
     * Returns unit of volume: tablespoon.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1292,'multiline':True]
['text':'*
     * Returns unit of volume: teaspoon.
     * Caller owns returned value and must free it.
     * @param status ICU error code.
     * @stable ICU 54
     ','line_number':1300,'multiline':True]
['text':' End generated createXXX methods','line_number':1309,'multiline':False]
['text':'*
     * For ICU use only.
     * @internal
     ','line_number':1314,'multiline':True]
['text':'*
     * For ICU use only.
     * @internal
     ','line_number':1320,'multiline':True]
['text':' U_HIDE_INTERNAL_API ','line_number':1326,'multiline':True]
['text':' !UNCONFIG_NO_FORMATTING','line_number':1343,'multiline':False]
['text':' __MEASUREUNIT_H__','line_number':1344,'multiline':False]
