['text':' must start with string, i.e. call new_value','line_number':121,'multiline':False]
['text':' MONGODB MODIFICATION: The use of `BOOST_ASSERT(false)` above is probably a bug,','line_number':122,'multiline':False]
['text':' because falling through into the `key` case doesn't look like what we ought to','line_number':123,'multiline':False]
['text':' do, and that is what would happen if BOOST_ASSERT was disabled. However, we honor','line_number':124,'multiline':False]
['text':' the behavior of the code as written, and add a fallthrough annotation so we can','line_number':125,'multiline':False]
['text':' make use of it in our own code.','line_number':126,'multiline':False]
