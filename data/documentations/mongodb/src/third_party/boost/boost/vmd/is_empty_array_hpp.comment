['text':'  (C) Copyright Edward Diener 2011-2015','line_number':2,'multiline':False]
['text':'  Use, modification and distribution are subject to the Boost Software License,','line_number':3,'multiline':False]
['text':'  Version 1.0. (See accompanying file LICENSE_1_0.txt or copy at','line_number':4,'multiline':False]
['text':'  http://www.boost.org/LICENSE_1_0.txt).','line_number':5,'multiline':False]
['text':'

  The succeeding comments in this file are in doxygen format.

','line_number':19,'multiline':True]
['text':'* \file
','line_number':25,'multiline':True]
['text':'* \def BOOST_VMD_IS_EMPTY_ARRAY(sequence)

    \brief Tests whether a sequence is an empty Boost PP array.

    An empty Boost PP array is a two element tuple where the first
    size element is 0 and the second element is a tuple with a single 
    empty element, ie. '(0,())'.
    
    sequence = a possible empty array

    returns = 1 if the sequence is an empty Boost PP array,
              0 if it is not.
              
    The macro will generate a preprocessing error if the sequence
    is in the form of an array but its first tuple element, instead
    of being a number, is a preprocessor token which VMD cannot parse,
    as in the example '(&0,())' which is a valid tuple but an invalid
    array.
    
','line_number':28,'multiline':True]
['text':'','line_number':60,'multiline':True]
['text':'* \def BOOST_VMD_IS_EMPTY_ARRAY_D(d,sequence)

    \brief Tests whether a sequence is an empty Boost PP array. Re-entrant version.

    An empty Boost PP array is a two element tuple where the first
    size element is 0 and the second element is a tuple with a single 
    empty element, ie. '(0,())'.
    
    d        = The next available BOOST_PP_WHILE iteration. <br/>
    sequence = a possible empty array

    returns = 1 if the sequence is an empty Boost PP array,
              0 if it is not.
              
    The macro will generate a preprocessing error if the sequence
    is in the form of an array but its first tuple element, instead
    of being a number, is a preprocessor token which VMD cannot parse,
    as in the example '(&0,())' which is a valid tuple but an invalid
    array.
    
','line_number':62,'multiline':True]
['text':'','line_number':95,'multiline':True]
['text':' BOOST_PP_VARIADICS ','line_number':97,'multiline':True]
['text':' BOOST_VMD_IS_EMPTY_ARRAY_HPP ','line_number':98,'multiline':True]
