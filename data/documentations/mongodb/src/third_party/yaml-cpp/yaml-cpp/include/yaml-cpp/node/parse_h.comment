['text':' GCC supports "pragma once" correctly since 3.4','line_number':6,'multiline':False]
['text':'*
 * Loads the input string as a single YAML document.
 *
 * @throws {@link ParserException} if it is malformed.
 ','line_number':19,'multiline':True]
['text':'*
 * Loads the input string as a single YAML document.
 *
 * @throws {@link ParserException} if it is malformed.
 ','line_number':26,'multiline':True]
['text':'*
 * Loads the input stream as a single YAML document.
 *
 * @throws {@link ParserException} if it is malformed.
 ','line_number':33,'multiline':True]
['text':'*
 * Loads the input file as a single YAML document.
 *
 * @throws {@link ParserException} if it is malformed.
 * @throws {@link BadFile} if the file cannot be loaded.
 ','line_number':40,'multiline':True]
['text':'*
 * Loads the input string as a list of YAML documents.
 *
 * @throws {@link ParserException} if it is malformed.
 ','line_number':48,'multiline':True]
['text':'*
 * Loads the input string as a list of YAML documents.
 *
 * @throws {@link ParserException} if it is malformed.
 ','line_number':55,'multiline':True]
['text':'*
 * Loads the input stream as a list of YAML documents.
 *
 * @throws {@link ParserException} if it is malformed.
 ','line_number':62,'multiline':True]
['text':'*
 * Loads the input file as a list of YAML documents.
 *
 * @throws {@link ParserException} if it is malformed.
 * @throws {@link BadFile} if the file cannot be loaded.
 ','line_number':69,'multiline':True]
['text':' namespace YAML','line_number':76,'multiline':False]
['text':' VALUE_PARSE_H_62B23520_7C8E_11DE_8A39_0800200C9A66','line_number':78,'multiline':False]
