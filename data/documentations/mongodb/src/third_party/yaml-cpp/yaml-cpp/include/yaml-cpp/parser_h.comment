['text':' GCC supports "pragma once" correctly since 3.4','line_number':6,'multiline':False]
['text':'*
 * A parser turns a stream of bytes into one stream of "events" per YAML
 * document in the input stream.
 ','line_number':22,'multiline':True]
['text':'* Constructs an empty parser (with no input. ','line_number':28,'multiline':True]
['text':'*
   * Constructs a parser from the given input stream. The input stream must
   * live as long as the parser.
   ','line_number':36,'multiline':True]
['text':'* Evaluates to true if the parser has some valid input to be read. ','line_number':44,'multiline':True]
['text':'*
   * Resets the parser with the given input stream. Any existing state is
   * erased.
   ','line_number':47,'multiline':True]
['text':'*
   * Handles the next document by calling events on the {@code eventHandler}.
   *
   * @throw a ParserException on error.
   * @return false if there are no more documents
   ','line_number':53,'multiline':True]
['text':'*
   * Reads any directives that are next in the queue, setting the internal
   * {@code m_pDirectives} state.
   ','line_number':64,'multiline':True]
['text':'*
   * Handles a "YAML" directive, which should be of the form 'major.minor' (like
   * a version number).
   ','line_number':72,'multiline':True]
['text':'*
   * Handles a "TAG" directive, which should be of the form 'handle prefix',
   * where 'handle' is converted to 'prefix' in the file.
   ','line_number':78,'multiline':True]
['text':' namespace YAML','line_number':88,'multiline':False]
['text':' PARSER_H_62B23520_7C8E_11DE_8A39_0800200C9A66','line_number':90,'multiline':False]
