['text':'*
 * Tests that writes are disallowed while in kCommitted during unshard collection.
 *
 * @tags: [
 *   multiversion_incompatible,
 *   requires_fcv_72,
 *   featureFlagReshardingImprovements,
 *   featureFlagUnshardCollection,
 *   featureFlagTrackUnshardedCollectionsOnShardingCatalog,
 *   multiversion_incompatible
 * ]
 ','line_number':1,'multiline':True]
['text':' The collMod is serialized with the resharding command, so we explicitly add an','line_number':60,'multiline':False]
['text':' timeout to the command so that it doesn't get blocked and timeout the test.','line_number':61,'multiline':False]
