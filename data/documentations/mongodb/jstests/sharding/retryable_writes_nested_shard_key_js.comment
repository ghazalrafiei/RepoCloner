['text':'*
 * Tests retryable insert, update and delete operations on a sharded collection with a nested shard
 * key to ensure that each operation is not re-executed when run after chunk migration.
 ','line_number':1,'multiline':True]
['text':' Test that updateCmd is only executed a single time by verifying that counter has only been','line_number':40,'multiline':False]
['text':' incremented once.','line_number':41,'multiline':False]
['text':' Tests deleteCmd is only executed a single time by verifying that the command is able to','line_number':52,'multiline':False]
['text':' run a second time and that the response to the second command is equivalent to the first','line_number':53,'multiline':False]
['text':' Tests insertCmd is only executed a single time by verifying that the command is able to','line_number':65,'multiline':False]
['text':' run a second time and that the response to the second command is equivalent to the first.','line_number':66,'multiline':False]
['text':'   - If command were to execute a second time, we would receieve a duplicate key error','line_number':67,'multiline':False]
