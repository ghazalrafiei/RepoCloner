['text':'*
 * Tests that writes are disallowed while in kCommitted.
 *
 * @tags: [
 *   multiversion_incompatible,
 * ]
 ','line_number':1,'multiline':True]
['text':' The collMod is serialized with the resharding command, so we explicitly add an','line_number':74,'multiline':False]
['text':' timeout to the command so that it doesn't get blocked and timeout the test.','line_number':75,'multiline':False]
