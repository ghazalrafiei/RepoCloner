['text':'*
 * Tests that shard merge fails if the list of tenants passed to donorStartMigration is incomplete.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   requires_shard_merge,
 *   requires_fcv_71,
 * ]
 ','line_number':1,'multiline':True]
['text':' Set a shorter importQuorumTimeoutSeconds for automatic merge abort.','line_number':19,'multiline':False]
