['text':'*
 * Tests that writes that are blocked during a tenant migration will still be able to find out
 * migration outcome even if the migration's in memory state has been garbage collected.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_71,
 * ]
 ','line_number':1,'multiline':True]
['text':' Set the delay before a donor state doc is garbage collected to be short to speed up the test.','line_number':27,'multiline':False]
['text':' Set the TTL monitor to run at a smaller interval to speed up the test.','line_number':29,'multiline':False]
