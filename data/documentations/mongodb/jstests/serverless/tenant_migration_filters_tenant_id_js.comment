['text':'*
 * Tests that tenant migrations are correctly filtering DBs by tenantId.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_71,
 *   incompatible_with_shard_merge,
 * ]
 ','line_number':1,'multiline':True]
['text':' Run a migration with the base tenant ID.','line_number':43,'multiline':False]
['text':' verifyRecipientDB calls isNamespaceForTenant() to determine if the data should have been','line_number':50,'multiline':False]
['text':' migrated, so we can directly call it here.','line_number':51,'multiline':False]
