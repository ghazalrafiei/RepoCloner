['text':'*
 * Tests that large write error results from bulk write operations are within the BSON size limit.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_71,
 * ]
 ','line_number':1,'multiline':True]
['text':' num of writes to run in bulk.','line_number':53,'multiline':False]
['text':' Do a large unordered bulk insert that fails all inserts in order to generate a large write','line_number':57,'multiline':False]
['text':' result.','line_number':58,'multiline':False]
['text':' This assert is more or less a sanity check since jsThreads need to convert data it returns','line_number':85,'multiline':False]
['text':' into a BSON object. So if we have reached this assert, we already know that the write result','line_number':86,'multiline':False]
['text':' is within the BSON limits.','line_number':87,'multiline':False]
