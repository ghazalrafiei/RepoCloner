['text':'*
 * Tests that tenant migrations correctly clone 'system.views' collections that belong to the
 * tenant.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_71,
 * ]
 ','line_number':1,'multiline':True]
['text':' Create a view on the tenant DB and insert documents into the tenant collection.','line_number':41,'multiline':False]
['text':' Verify that the view was cloned correctly.','line_number':53,'multiline':False]
