['text':'*
 * Tests that the donor accepts writes after the shard split aborts.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_63
 * ]
 ','line_number':1,'multiline':True]
['text':'*
 * Tests that the donor does not reject writes after the migration aborts.
 ','line_number':49,'multiline':True]
['text':' Wait until the in-memory migration state is updated after the migration has majority','line_number':53,'multiline':False]
['text':' committed the abort decision. Otherwise, the command below is expected to block and then get','line_number':54,'multiline':False]
['text':' rejected.','line_number':55,'multiline':False]
['text':'*
 * run the setup for each cases before the migration starts
 ','line_number':70,'multiline':True]
['text':'*
 * Run the test cases after the migration has aborted.
 ','line_number':106,'multiline':True]
