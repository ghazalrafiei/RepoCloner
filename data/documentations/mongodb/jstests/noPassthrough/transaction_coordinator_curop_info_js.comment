['text':'*
 * Tests that the transaction items in the 'twoPhaseCommitCoordinator' object in currentOp() are
 * being tracked correctly.
 * @tags: [
 *   uses_prepare_transaction,
 *   uses_transactions,
 * ]
 ','line_number':1,'multiline':True]
['text':' Setup test','line_number':93,'multiline':False]
['text':' Manually abort the transaction on one of the participants, so that the participant fails to','line_number':185,'multiline':False]
['text':' prepare and failpoint is triggered on the coordinator.','line_number':186,'multiline':False]
