['text':'*
 * Tests that the writeConcern of a $out/$merge executed on a secondary is propagated to the primary
 * and is properly respected.
 *
 * @tags: [
 *   assumes_unsharded_collection,
 *   requires_replication,
 * ]
 ','line_number':1,'multiline':True]
['text':' The default WC is majority and stopServerReplication will prevent satisfying any majority writes.','line_number':21,'multiline':False]
['text':' Verify that the command sent to the primary has the expected w:2 writeConcern attached to it.','line_number':42,'multiline':False]
['text':' Stop the oplog fetcher on the secondary.','line_number':50,'multiline':False]
