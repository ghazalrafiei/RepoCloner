['text':'*
 * Verifies that a dropDatabase operation interrupted due to stepping down resets the drop pending
 * flag. Additionally, after the node steps down, we ensure it can drop the database as instructed
 * by the new primary.
 *
 * @tags: [
 *   requires_replication,
 * ]
 ','line_number':1,'multiline':True]
['text':' Have the new primary try to drop the database. The stepped down node must successfully replicate','line_number':63,'multiline':False]
['text':' this dropDatabase command.','line_number':64,'multiline':False]
