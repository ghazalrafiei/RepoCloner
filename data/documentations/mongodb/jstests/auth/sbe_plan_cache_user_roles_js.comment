['text':'*
 * Test $$USER_ROLES works correctly with the SBE plan cache. The same query should return the
 * updated user role info when a different user logs in.
 * @tags: [
 *   # Multiple servers can mess up the plan cache list.
 *   assumes_standalone_mongod,
 * ]
 ','line_number':1,'multiline':True]
['text':' Create two users, each with different roles.','line_number':17,'multiline':False]
['text':' While logged in as user1, we should see user1's roles.','line_number':36,'multiline':False]
['text':' It can take two executions of a query for a plan to get cached.','line_number':41,'multiline':False]
['text':' While logged in as user2, we should see user2's roles.','line_number':46,'multiline':False]
