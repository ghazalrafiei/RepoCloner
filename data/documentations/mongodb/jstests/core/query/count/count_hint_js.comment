['text':'*
 * Tests passing hint to the count command:
 *   - A hint should be respected even if it results in an incorrect count.
 *   - A bad argument to the hint() method should raise an error.
 *   - The hint() method should support both the name of the index, and the object spec of the
 *     index.
 *
 * @tags: [requires_fastcount]
 ','line_number':1,'multiline':True]
['text':' Create a sparse index which should have no entries.','line_number':29,'multiline':False]
['text':' A hint should be respected, even if it results in the wrong answer.','line_number':32,'multiline':False]
['text':' SERVER-14792: bad hints should cause the count to fail, even if there is no query predicate.','line_number':39,'multiline':False]
['text':' Test that a bad hint fails with the correct error code.','line_number':54,'multiline':False]
