['text':' Check that OCSP ignores statuses of irrelevant certificates in the response','line_number':1,'multiline':False]
['text':' @tags: [requires_http_client, requires_ocsp_stapling]','line_number':2,'multiline':False]
['text':'*
 * Tests OCSP status verification in the client-side ignores the statuses
 * of irrelevant certificates. No stapling is performed server-side.
 ','line_number':33,'multiline':True]
['text':' The mongoRunner spawns a new Mongo Object to validate the collections which races','line_number':78,'multiline':False]
['text':' with the shutdown logic of the mock_ocsp responder on some platforms. We need this','line_number':79,'multiline':False]
['text':' sleep to make sure that the threads don't interfere with each other.','line_number':80,'multiline':False]
['text':'*
 * Tests OCSP status verification in the server-side (for stapling) ignores
 * the statuses of irrelevant certificates. The server certificate must have
 * the status_request TLS Feature (aka MustStaple) extension for the assertions
 * in this test to work.
 ','line_number':85,'multiline':True]
['text':' The mongoRunner spawns a new Mongo Object to validate the collections which races','line_number':139,'multiline':False]
['text':' with the shutdown logic of the mock_ocsp responder on some platforms. We need this','line_number':140,'multiline':False]
['text':' sleep to make sure that the threads don't interfere with each other.','line_number':141,'multiline':False]
['text':' windows can't handle issuer names & keys hashed','line_number':148,'multiline':False]
['text':' using sha256, so this is only tested on openssl.','line_number':149,'multiline':False]
