['text':' Check that rotation works for the cluster certificate in a sharded cluster','line_number':1,'multiline':False]
['text':' server certificate is held constant so that shell can still connect','line_number':21,'multiline':False]
['text':' we start a cluster using the old certificates, then rotate one shard to use new certificates.','line_number':22,'multiline':False]
['text':' Make sure that mongos can communicate with every connected host EXCEPT that shard before a','line_number':23,'multiline':False]
['text':' rotate, and make sure it can communicate with ONLY that shard after a rotate.','line_number':24,'multiline':False]
['text':' Keep track of the hosts we hit in the initial ping multicast to compare against later multicasts','line_number':56,'multiline':False]
['text':' Swap out the certificate files and rotate the primary shard.','line_number':68,'multiline':False]
['text':' Make sure the primary is initially present','line_number':75,'multiline':False]
['text':' Drop connection to all hosts to see what we can reconnect to','line_number':78,'multiline':False]
['text':' multicast should fail, because the primary shard isn't hit','line_number':85,'multiline':False]
['text':' rotate, drop all connections, re-multicast and see what we now hit','line_number':108,'multiline':False]
['text':' Don't call st.stop() -- breaks because cluster is only partially rotated (this is hard to fix)','line_number':138,'multiline':False]
