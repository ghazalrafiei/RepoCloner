['text':'*
 * Tests that the validate command detects incorrect WT table logging settings.
 *
 * @tags: [
 *   requires_wiredtiger,
 * ]
 ','line_number':1,'multiline':True]
['text':' Create the collection and indexes as a standlone, which will cause the tables to be logged.','line_number':40,'multiline':False]
['text':' Skip the normal step of switching the logging setting on the tables.','line_number':47,'multiline':False]
['text':' Run validate as a replica set, which will expect the tables to not be logged.','line_number':58,'multiline':False]
['text':' Create the collection and indexes as a replica set, which will cause the tables to not be logged.','line_number':72,'multiline':False]
['text':' Run validate as a standalone, which will expect the tables to be logged.','line_number':79,'multiline':False]
