['text':'*
 * Test how $densify desugars.
 *
 * @tags: [
 *   # $mergeCursors was added to explain output in 5.3.
 *   requires_fcv_53,
 *   # We're testing the explain plan, not the query results, so the facet passthrough would fail.
 *   do_not_wrap_aggregations_in_facets,
 * ]
 ','line_number':1,'multiline':True]
['text':' Implicit partition fields and sort are generated.','line_number':16,'multiline':False]
['text':' PartitionByFields are prepended to the sortKey if "partition" is specified.','line_number':27,'multiline':False]
['text':' PartitionByFields are not prepended to the sortKey if "full" is specified.','line_number':41,'multiline':False]
['text':' PartitionByFields are prepended to the sortKey if numeric bounds are specified.','line_number':54,'multiline':False]
['text':' PartitionByFields are prepended to the sortKey if date bounds are specified.','line_number':67,'multiline':False]
