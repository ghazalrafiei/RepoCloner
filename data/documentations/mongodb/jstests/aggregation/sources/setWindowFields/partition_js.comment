['text':'
 * Test partitioning inside $setWindowFields.
 *
 * @tags: [
 *   # We assume the pipeline is not split into a shardsPart and mergerPart.
 *   assumes_unsharded_collection,
 *   # We're testing the explain plan, not the query results, so the facet passthrough would fail.
 *   do_not_wrap_aggregations_in_facets,
 * ]
 ','line_number':1,'multiline':True]
['text':' Test for runtime error when 'partitionBy' expression evaluates to an array','line_number':18,'multiline':False]
['text':' Test that a constant expression for 'partitionBy' is equivalent to no partitioning.','line_number':32,'multiline':False]
['text':' prevent stages from being absorbed into the .find() layer','line_number':36,'multiline':False]
['text':' Test that missing and null field are in the same partition.','line_number':57,'multiline':False]
['text':' Test that the compound key with the mix of missing and null field works correctly.','line_number':71,'multiline':False]
