['text':'*
 * map_reduce_inline.js
 *
 * Generates some random data and inserts it into a collection. Runs a
 * map-reduce command over the collection that computes the frequency
 * counts of the 'value' field in memory.
 *
 * Used as the base workload for the other map-reduce workloads.
 * @tags: [
 *   # mapReduce does not support afterClusterTime.
 *   does_not_support_causal_consistency,
 *   # Use mapReduce.
 *   requires_scripting,
 * ]
 ','line_number':1,'multiline':True]
['text':' no-op','line_number':48,'multiline':False]
['text':' other workloads that extend this workload use this method','line_number':49,'multiline':False]
['text':' TODO: this actually does assume that there are no unique indexes','line_number':67,'multiline':False]
