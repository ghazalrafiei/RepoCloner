['text':'*
 * timeseries_findAndModify_updates.js
 *
 * Extends timeseries_updates_and_inserts.js to test findAndModify with updates.
 *
 * @tags: [
 *   # We need a timeseries collection.
 *   requires_timeseries,
 *   # Time-series findAndModify does not support retryable writes.
 *   requires_non_retryable_writes,
 *   featureFlagTimeseriesUpdatesSupport,
 * ]
 ','line_number':1,'multiline':True]
['text':' Update a reading in each bucket.','line_number':22,'multiline':False]
