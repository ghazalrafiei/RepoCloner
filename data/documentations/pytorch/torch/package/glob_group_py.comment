['text':' to avoid corner cases for the first component, we prefix the candidate string','line_number':68,'multiline':False]
['text':' with '.' so `import torch` will regex against `.torch`, assuming '.' is the separator','line_number':69,'multiline':False]
