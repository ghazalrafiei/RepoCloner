['text':' as of 0.105.3, year, time, date and datetime filters include `is like` and `is not like` which are not practical','line_number':8,'multiline':False]['text':' `removeLikeAndNlikeFilters` in this upgrader is simply to remove them','line_number':9,'multiline':False]['text':' besides, `null` and `empty` will be migrated to `blank` in `migrateEmptyAndNullFilters`','line_number':11,'multiline':False]['text':' since the upcoming version will introduce a set of new filters for date / datetime with a new `comparison_sub_op`','line_number':13,'multiline':False]['text':' `eq` and `neq` would become `is` / `is not` (comparison_op) + `exact date` (comparison_sub_op)','line_number':14,'multiline':False]['text':' `migrateEqAndNeqFilters` in this upgrader is to add `exact date` in comparison_sub_op','line_number':15,'multiline':False]['text':' Change Summary:','line_number':17,'multiline':False]['text':' - Date / DateTime columns:','line_number':18,'multiline':False]['text':'   - remove `is like` and `is not like`','line_number':19,'multiline':False]['text':'   - migrate `null` or `empty` filters to `blank`','line_number':20,'multiline':False]['text':'   - add `exact date` in comparison_sub_op for existing filters `eq` and `neq`','line_number':21,'multiline':False]['text':' - Year / Time columns:','line_number':22,'multiline':False]['text':'   - remove `is like` and `is not like`','line_number':23,'multiline':False]['text':'   - migrate `null` or `empty` filters to `blank`','line_number':24,'multiline':False]['text':' remove `is like` and `is not like`','line_number':28,'multiline':False]['text':' remove `is like` and `is not like`','line_number':37,'multiline':False]['text':' remove `is like` and `is not like`','line_number':54,'multiline':False]['text':' migrate to blank','line_number':56,'multiline':False]['text':' migrate to not blank','line_number':67,'multiline':False]