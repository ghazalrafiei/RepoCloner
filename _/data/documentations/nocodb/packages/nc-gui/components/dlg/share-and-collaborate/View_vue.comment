['text':' const { inviteUser } = useManageUsers()','line_number':30,'multiline':False]['text':' const expandedSharedType = ref<'none' | 'base' | 'view'>('view')','line_number':32,'multiline':False]['text':'
const onShare = async () => {
  if (!invitationValid) return

  await inviteUser({
    email: invitationUsersData.value.emails!,
    roles: invitationUsersData.value.role!,
  })
}
','line_number':46,'multiline':True]