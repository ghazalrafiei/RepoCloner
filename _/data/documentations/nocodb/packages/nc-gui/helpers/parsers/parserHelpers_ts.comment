['text':' too many options are detected, convert the column to SingleLineText instead','line_number':71,'multiline':False]['text':' _disableSelect is used to disable the <a-select-option/> in TemplateEditor','line_number':73,'multiline':False]['text':' assume the column type is multiple select if there are repeated values','line_number':76,'multiline':False]['text':' set dtxp here so that users can have the options even they switch the type from other types to MultiSelect','line_number':80,'multiline':False]['text':' once it's set, dtxp needs to be reset if the final column type is not MultiSelect','line_number':81,'multiline':False]['text':' set dtxp here so that users can have the options even they switch the type from other types to SingleSelect','line_number':98,'multiline':False]['text':' once it's set, dtxp needs to be reset if the final column type is not SingleSelect','line_number':99,'multiline':False]