['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]['text':'*
 * This interface is intentionally not identical to node.js
 * process because it also works in sandboxed environments
 * where the process object is implemented differently. We
 * define the properties here that we need for `platform`
 * to work and nothing else.
 ','line_number':36,'multiline':True]['text':' Native environment (sandboxed)','line_number':61,'multiline':False]['text':' Native environment (non-sandboxed)','line_number':64,'multiline':False]['text':' Web environment','line_number':78,'multiline':False]['text':' This call _must_ be done in the file that calls `nls.getConfiguredDefaultLocale`','line_number':89,'multiline':False]['text':' to ensure that the NLS AMD Loader plugin has been loaded and configured.','line_number':90,'multiline':False]['text':' This is because the loader plugin decides what the default locale is based on','line_number':91,'multiline':False]['text':' how it's able to resolve the strings.','line_number':92,'multiline':False]['text':' Native environment','line_number':101,'multiline':False]['text':' VSCode's default language is 'en'','line_number':118,'multiline':False]['text':' Unknown environment','line_number':127,'multiline':False]['text':'*
 * Whether we run inside a CI environment, such as
 * GH actions or Azure Pipelines.
 ','line_number':169,'multiline':True]['text':'*
 * The language used for the user interface. The format of
 * the string is all lower case (e.g. zh-tw for Traditional
 * Chinese)
 ','line_number':177,'multiline':True]['text':'*
 * The OS locale or the locale specified by --locale. The format of
 * the string is all lower case (e.g. zh-tw for Traditional
 * Chinese). The UI is not necessarily shown in the provided locale.
 ','line_number':205,'multiline':True]['text':'*
 * This will always be set to the OS/browser's locale regardless of
 * what was specified by --locale. The format of the string is all
 * lower case (e.g. zh-tw for Traditional Chinese). The UI is not
 * necessarily shown in the provided locale.
 ','line_number':212,'multiline':True]['text':'*
 * The translations that are available through language packs.
 ','line_number':220,'multiline':True]['text':'*
 * See https://html.spec.whatwg.org/multipage/timers-and-user-prompts.html#:~:text=than%204%2C%20then-,set%20timeout%20to%204,-.
 *
 * Works similarly to `setTimeout(0)` but doesn't suffer from the 4ms artificial delay
 * that browsers set when the nesting level is > 5.
 ','line_number':227,'multiline':True]