['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]['text':'*
	 * Resolves the color of the given color identifier. If the theme does not
	 * specify the color, the default color is returned unless <code>useDefault</code> is set to false.
	 * @param color the id of the color
	 * @param useDefault specifies if the default color should be used. If not set, the default is used.
	 ','line_number':49,'multiline':True]['text':'*
	 * Returns whether the theme defines a value for the color. If not, that means the
	 * default color will be used.
	 ','line_number':57,'multiline':True]['text':'*
	 * Returns the token style for a given classification. The result uses the <code>MetadataConsts</code> format
	 ','line_number':63,'multiline':True]['text':'*
	 * List of all colors used with tokens. <code>getTokenStyleMetadata</code> references the colors by index into this list.
	 ','line_number':68,'multiline':True]['text':'*
	 * Defines whether semantic highlighting should be enabled for the theme.
	 ','line_number':73,'multiline':True]['text':'*
	 * Resolves the definition for the given icon as defined by the theme.
	 *
	 * @param iconContribution The icon
	 ','line_number':86,'multiline':True]['text':' static theming participant','line_number':120,'multiline':False]['text':'*
	 * Register a theming participant that is invoked on every theme change.
	 ','line_number':127,'multiline':True]['text':'*
 * Utility base class for all themable components.
 ','line_number':171,'multiline':True]['text':' Hook up to theme changes','line_number':184,'multiline':False]['text':' Subclasses to override','line_number':195,'multiline':False]