['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]['text':'*
 * Description of an action contribution
 ','line_number':43,'multiline':True]['text':'*
	 * An unique identifier of the contributed action.
	 ','line_number':47,'multiline':True]['text':'*
	 * A label of the action that will be presented to the user.
	 ','line_number':51,'multiline':True]['text':'*
	 * Precondition rule.
	 ','line_number':55,'multiline':True]['text':'*
	 * An array of keybindings for the action.
	 ','line_number':59,'multiline':True]['text':'*
	 * The keybinding rule (condition on top of precondition).
	 ','line_number':63,'multiline':True]['text':'*
	 * Control if the action should show up in the context menu and where.
	 * The context menu of the editor has these default:
	 *   navigation - The navigation group comes first in all cases.
	 *   1_modification - This group comes next and contains commands that modify your code.
	 *   9_cutcopypaste - The last default group with the basic editing commands.
	 * You can also create your own group.
	 * Defaults to null (don't show in context menu).
	 ','line_number':67,'multiline':True]['text':'*
	 * Control the order in the context menu group.
	 ','line_number':77,'multiline':True]['text':'*
	 * Method that will be executed when the action is triggered.
	 * @param editor The editor instance is passed in as a convenience
	 ','line_number':81,'multiline':True]['text':'*
 * Options which apply for all editors.
 ','line_number':88,'multiline':True]['text':'*
	 * The number of spaces a tab is equal to.
	 * This setting is overridden based on the file contents when `detectIndentation` is on.
	 * Defaults to 4.
	 ','line_number':92,'multiline':True]['text':'*
	 * Insert spaces when pressing `Tab`.
	 * This setting is overridden based on the file contents when `detectIndentation` is on.
	 * Defaults to true.
	 ','line_number':98,'multiline':True]['text':'*
	 * Controls whether `tabSize` and `insertSpaces` will be automatically detected when a file is opened based on the file contents.
	 * Defaults to true.
	 ','line_number':104,'multiline':True]['text':'*
	 * Remove trailing auto inserted whitespace.
	 * Defaults to true.
	 ','line_number':109,'multiline':True]['text':'*
	 * Special handling for large files to disable certain memory intensive features.
	 * Defaults to true.
	 ','line_number':114,'multiline':True]['text':'*
	 * Controls whether completions should be computed based on words in the document.
	 * Defaults to true.
	 ','line_number':119,'multiline':True]['text':'*
	 * Controls whether word based completions should be included from opened documents of the same language or any language.
	 ','line_number':124,'multiline':True]['text':'*
	 * Controls whether the semanticHighlighting is shown for the languages that support it.
	 * true: semanticHighlighting is enabled for all themes
	 * false: semanticHighlighting is disabled for all themes
	 * 'configuredByTheme': semanticHighlighting is controlled by the current color theme's semanticHighlighting setting.
	 * Defaults to 'byTheme'.
	 ','line_number':128,'multiline':True]['text':'*
	 * Keep peek editors open even when double-clicking their content or when hitting `Escape`.
	 * Defaults to false.
	 ','line_number':136,'multiline':True]['text':'*
	 * Lines above this length will not be tokenized for performance reasons.
	 * Defaults to 20000.
	 ','line_number':141,'multiline':True]['text':'*
	 * Theme to be used for rendering.
	 * The current out-of-the-box available themes are: 'vs' (default), 'vs-dark', 'hc-black', 'hc-light'.
	 * You can create custom themes via `monaco.editor.defineTheme`.
	 * To switch a theme, use `monaco.editor.setTheme`.
	 * **NOTE**: The theme might be overwritten if the OS is in high contrast mode, unless `autoDetectHighContrast` is set to false.
	 ','line_number':146,'multiline':True]['text':'*
	 * If enabled, will automatically change to high contrast theme if the OS is using a high contrast theme.
	 * Defaults to true.
	 ','line_number':154,'multiline':True]['text':'*
 * The options to create an editor.
 ','line_number':161,'multiline':True]['text':'*
	 * The initial model associated with this code editor.
	 ','line_number':165,'multiline':True]['text':'*
	 * The initial value of the auto created model in the editor.
	 * To not automatically create a model, use `model: null`.
	 ','line_number':169,'multiline':True]['text':'*
	 * The initial language of the auto created model in the editor.
	 * To not automatically create a model, use `model: null`.
	 ','line_number':174,'multiline':True]['text':'*
	 * Initial theme to be used for rendering.
	 * The current out-of-the-box available themes are: 'vs' (default), 'vs-dark', 'hc-black', 'hc-light.
	 * You can create custom themes via `monaco.editor.defineTheme`.
	 * To switch a theme, use `monaco.editor.setTheme`.
	 * **NOTE**: The theme might be overwritten if the OS is in high contrast mode, unless `autoDetectHighContrast` is set to false.
	 ','line_number':179,'multiline':True]['text':'*
	 * If enabled, will automatically change to high contrast theme if the OS is using a high contrast theme.
	 * Defaults to true.
	 ','line_number':187,'multiline':True]['text':'*
	 * An URL to open when Ctrl+H (Windows and Linux) or Cmd+H (OSX) is pressed in
	 * the accessibility help dialog in the editor.
	 *
	 * Defaults to "https://go.microsoft.com/fwlink/?linkid=852450"
	 ','line_number':192,'multiline':True]['text':'*
	 * Container element to use for ARIA messages.
	 * Defaults to document.body.
	 ','line_number':199,'multiline':True]['text':'*
 * The options to create a diff editor.
 ','line_number':206,'multiline':True]['text':'*
	 * Initial theme to be used for rendering.
	 * The current out-of-the-box available themes are: 'vs' (default), 'vs-dark', 'hc-black', 'hc-light.
	 * You can create custom themes via `monaco.editor.defineTheme`.
	 * To switch a theme, use `monaco.editor.setTheme`.
	 * **NOTE**: The theme might be overwritten if the OS is in high contrast mode, unless `autoDetectHighContrast` is set to false.
	 ','line_number':210,'multiline':True]['text':'*
	 * If enabled, will automatically change to high contrast theme if the OS is using a high contrast theme.
	 * Defaults to true.
	 ','line_number':218,'multiline':True]['text':'*
 * Create ARIA dom node inside parent,
 * or only for the first editor instantiation inside document.body.
 * @param parent container element for ARIA dom node
 ','line_number':244,'multiline':True]['text':'*
 * A code editor to be used both by the standalone editor and the standalone diff editor.
 ','line_number':259,'multiline':True]['text':' Read descriptor options','line_number':318,'multiline':False]['text':' Generate a unique id to allow the same descriptor.id across multiple editor instances','line_number':339,'multiline':False]['text':' Register the command','line_number':342,'multiline':False]['text':' Register the context menu item','line_number':345,'multiline':False]['text':' Register the keybindings','line_number':359,'multiline':False]['text':' Finally, register an internal editor action','line_number':366,'multiline':False]['text':' Store it under the original id, such that trigger with the original id will work','line_number':377,'multiline':False]['text':' Help commands find this editor as the active editor','line_number':388,'multiline':False]['text':'*
 * @internal
 ','line_number':571,'multiline':True]['text':'*
 * @internal
 ','line_number':587,'multiline':True]