['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]['text':'*
 * Create a new editor under `domElement`.
 * `domElement` should be empty (not contain other dom nodes).
 * The editor will read the size of `domElement`.
 ','line_number':44,'multiline':True]['text':'*
 * Emitted when an editor is created.
 * Creating a diff editor might cause this listener to be invoked with the two editors.
 * @event
 ','line_number':54,'multiline':True]['text':'*
 * Emitted when an diff editor is created.
 * @event
 ','line_number':66,'multiline':True]['text':'*
 * Get all the created editors.
 ','line_number':77,'multiline':True]['text':'*
 * Get all the created diff editors.
 ','line_number':85,'multiline':True]['text':'*
 * Create a new diff editor under `domElement`.
 * `domElement` should be empty (not contain other dom nodes).
 * The editor will read the size of `domElement`.
 ','line_number':93,'multiline':True]['text':'*
 * Description of a command contribution
 ','line_number':108,'multiline':True]['text':'*
	 * An unique identifier of the contributed command.
	 ','line_number':112,'multiline':True]['text':'*
	 * Callback that will be executed when the command is triggered.
	 ','line_number':116,'multiline':True]['text':'*
 * Add a command.
 ','line_number':122,'multiline':True]['text':'*
 * Add an action to all editors.
 ','line_number':132,'multiline':True]['text':' Register the command','line_number':147,'multiline':False]['text':' Register the context menu item','line_number':150,'multiline':False]['text':' Register the keybindings','line_number':164,'multiline':False]['text':'*
 * A keybinding rule.
 ','line_number':184,'multiline':True]['text':'*
 * Add a keybinding rule.
 ','line_number':194,'multiline':True]['text':'*
 * Add keybinding rules.
 ','line_number':201,'multiline':True]['text':'*
 * Create a new editor model.
 * You can specify the language that should be set for this model or let the language be inferred from the `uri`.
 ','line_number':221,'multiline':True]['text':'*
 * Change the language for a model.
 ','line_number':237,'multiline':True]['text':'*
 * Set the markers for a model.
 ','line_number':246,'multiline':True]['text':'*
 * Remove all markers of an owner.
 ','line_number':256,'multiline':True]['text':'*
 * Get markers for owner and/or resource
 *
 * @returns list of markers
 ','line_number':264,'multiline':True]['text':'*
 * Emitted when markers change for a model.
 * @event
 ','line_number':274,'multiline':True]['text':'*
 * Get the model that has `uri` if it exists.
 ','line_number':283,'multiline':True]['text':'*
 * Get all the created models.
 ','line_number':291,'multiline':True]['text':'*
 * Emitted when a model is created.
 * @event
 ','line_number':299,'multiline':True]['text':'*
 * Emitted right before a model is disposed.
 * @event
 ','line_number':308,'multiline':True]['text':'*
 * Emitted when a different language is set to a model.
 * @event
 ','line_number':317,'multiline':True]['text':'*
 * Create a new web worker that has model syncing capabilities built in.
 * Specify an AMD module to load that will `create` an object that will be proxied.
 ','line_number':331,'multiline':True]['text':'*
 * Colorize the contents of `domNode` using attribute `data-lang`.
 ','line_number':339,'multiline':True]['text':'*
 * Colorize `text` using language `languageId`.
 ','line_number':350,'multiline':True]['text':'*
 * Colorize a line in a model.
 ','line_number':360,'multiline':True]['text':'*
 * @internal
 ','line_number':369,'multiline':True]['text':'*
 * Tokenize `text` using language `languageId`
 ','line_number':383,'multiline':True]['text':' Needed in order to get the mode registered for subsequent look-ups','line_number':387,'multiline':False]['text':'*
 * Define a new theme or update an existing theme.
 ','line_number':404,'multiline':True]['text':'*
 * Switches to a theme.
 ','line_number':412,'multiline':True]['text':'*
 * Clears all cached font measurements and triggers re-measurement.
 ','line_number':420,'multiline':True]['text':'*
 * Register a command.
 ','line_number':427,'multiline':True]['text':'*
 * Registers a handler that is called when a link is opened in any editor. The handler callback should return `true` if the link was handled and `false` otherwise.
 * The handler that was registered last will be called first when a link is opened.
 *
 * Returns a disposable that can unregister the opener again.
 ','line_number':438,'multiline':True]['text':'*
 * Represents an object that can handle editor open operations (e.g. when "go to definition" is called
 * with a resource other than the current model).
 ','line_number':456,'multiline':True]['text':'*
	 * Callback that is invoked when a resource other than the current model should be opened (e.g. when "go to definition" is called).
	 * The callback should return `true` if the request was handled and `false` otherwise.
	 * @param source The code editor instance that initiated the request.
	 * @param resource The URI of the resource that should be opened.
	 * @param selectionOrPosition An optional position or selection inside the model corresponding to `resource` that can be used to set the cursor.
	 ','line_number':461,'multiline':True]['text':'*
 * Registers a handler that is called when a resource other than the current model should be opened in the editor (e.g. "go to definition").
 * The handler callback should return `true` if the request was handled and `false` otherwise.
 *
 * Returns a disposable that can unregister the opener again.
 *
 * If no handler is registered the default behavior is to do nothing for models other than the currently attached one.
 ','line_number':471,'multiline':True]['text':' return source editor to indicate that this handler has successfully handled the opening','line_number':493,'multiline':False]['text':' fallback to other registered handlers','line_number':495,'multiline':False]['text':'*
 * @internal
 ','line_number':499,'multiline':True]['text':' methods','line_number':504,'multiline':False]['text':' enums','line_number':543,'multiline':False]['text':' classes','line_number':569,'multiline':False]['text':' vars','line_number':580,'multiline':False]