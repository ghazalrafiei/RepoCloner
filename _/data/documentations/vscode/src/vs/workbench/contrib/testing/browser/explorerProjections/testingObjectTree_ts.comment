['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]['text':'*
	 * Gets a serialized view state for the tree, optimized for storage.
	 *
	 * @param updatePreviousState Optional previous state to mutate and update
	 * instead of creating a new one.
	 ','line_number':15,'multiline':True]['text':'*
		 * Recursive builder function. Returns whether the subtree has any non-default
		 * value. Adds itself to the parent children if it does.
		 ','line_number':24,'multiline':True]['text':' only saved collapsed state if it's not the default (not collapsed, or a root depth)','line_number':35,'multiline':False]['text':' Controller IDs are hidden if there's only a single test controller, but','line_number':57,'multiline':False]['text':' make sure they're added when the tree is built if this is the case, so','line_number':58,'multiline':False]['text':' that the later ID lookup works.','line_number':59,'multiline':False]