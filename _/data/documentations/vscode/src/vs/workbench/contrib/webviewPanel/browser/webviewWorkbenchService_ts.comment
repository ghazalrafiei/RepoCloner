['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]['text':'*
 * Service responsible for showing and managing webview editors in the workbench.
 ','line_number':33,'multiline':True]['text':'*
	 * Manages setting the icons show for a given webview.
	 ','line_number':39,'multiline':True]['text':'*
	 * Event fired when focus switches to a different webview editor.
	 *
	 * Fires `undefined` if focus switches to a non-webview editor.
	 ','line_number':44,'multiline':True]['text':'*
	 * Create a new webview editor and open it in the workbench.
	 ','line_number':51,'multiline':True]['text':'*
	 * Open a webview that is being restored from serialization.
	 ','line_number':61,'multiline':True]['text':'*
	 * Reveal an already opened webview editor in the workbench.
	 ','line_number':73,'multiline':True]['text':'*
	 * Register a new {@link WebviewResolver}.
	 *
	 * If there are any webviews awaiting revival that this resolver can handle, they will be resolved by it.
	 ','line_number':82,'multiline':True]['text':'*
	 * Check if a webview should be serialized across window reloads.
	 ','line_number':89,'multiline':True]['text':'*
	 * Try to resolve a webview. This will block until a resolver is registered for the webview.
	 ','line_number':94,'multiline':True]['text':'*
 * Handles filling in the content of webview before it can be shown to the user.
 ','line_number':100,'multiline':True]['text':'*
	 * Returns true if the resolver can resolve the given webview.
	 ','line_number':104,'multiline':True]['text':'*
	 * Resolves the webview.
	 ','line_number':109,'multiline':True]['text':' The user may have switched focus between two sides of a diff editor','line_number':234,'multiline':False]['text':' preserve pre 1.38 behaviour to not make group active when preserveFocus: true','line_number':288,'multiline':False]['text':' but make sure to restore the editor to fix https://github.com/microsoft/vscode/issues/79633','line_number':289,'multiline':False]['text':' preserve pre 1.38 behaviour to not make group active when preserveFocus: true','line_number':304,'multiline':False]['text':' but make sure to restore the editor to fix https://github.com/microsoft/vscode/issues/79633','line_number':305,'multiline':False]['text':' Revived webviews may not have an actively registered reviver but we still want to persist them','line_number':357,'multiline':False]['text':' since a reviver should exist when it is actually needed.','line_number':358,'multiline':False]['text':' A reviver may not be registered yet. Put into pool and resolve promise when we can revive','line_number':379,'multiline':False]