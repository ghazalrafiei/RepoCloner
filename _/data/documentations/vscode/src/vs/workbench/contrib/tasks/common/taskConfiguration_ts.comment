['text':'---------------------------------------------------------------------------------------------
 *  Copyright (c) Microsoft Corporation. All rights reserved.
 *  Licensed under the MIT License. See License.txt in the project root for license information.
 *--------------------------------------------------------------------------------------------','line_number':1,'multiline':True]['text':'*
	 * Default is character escaping.
	 ','line_number':30,'multiline':True]['text':'*
	 * Default is strong quoting
	 ','line_number':35,'multiline':True]['text':'*
	 * Default is weak quoting.
	 ','line_number':40,'multiline':True]['text':'*
	 * The character used to do character escaping.
	 ','line_number':47,'multiline':True]['text':'*
	 * The character used for string quoting.
	 ','line_number':55,'multiline':True]['text':'*
	 * The character used for weak quoting.
	 ','line_number':60,'multiline':True]['text':'*
	 * The current working directory of the executed program or shell.
	 * If omitted VSCode's current workspace root is used.
	 ','line_number':73,'multiline':True]['text':'*
	 * The additional environment of the executed program or shell. If omitted
	 * the parent process' environment is used.
	 ','line_number':79,'multiline':True]['text':'*
	 * The shell configuration;
	 ','line_number':85,'multiline':True]['text':'*
	 * Controls whether the terminal executing a task is brought to front or not.
	 * Defaults to `RevealKind.Always`.
	 ','line_number':92,'multiline':True]['text':'*
	 * Controls whether the problems panel is revealed when running this task or not.
	 * Defaults to `RevealKind.Never`.
	 ','line_number':98,'multiline':True]['text':'*
	 * Controls whether the executed command is printed to the output window or terminal as well.
	 ','line_number':104,'multiline':True]['text':'*
	 * Controls whether the terminal is focus when this task is executed
	 ','line_number':109,'multiline':True]['text':'*
	 * Controls whether the task runs in a new terminal
	 ','line_number':114,'multiline':True]['text':'*
	 * Controls whether to show the "Terminal will be reused by tasks, press any key to close it" message.
	 ','line_number':119,'multiline':True]['text':'*
	 * Controls whether the terminal should be cleared before running the task.
	 ','line_number':124,'multiline':True]['text':'*
	 * Controls whether the task is executed in a specific terminal group using split panes.
	 ','line_number':129,'multiline':True]['text':'*
	 * Controls whether the terminal that the task runs in is closed when the task completes.
	 ','line_number':134,'multiline':True]['text':'*
	 * @deprecated Use `isBackground` instead.
	 * Whether the executed command is kept alive and is watching the file system.
	 ','line_number':159,'multiline':True]['text':'*
	 * @deprecated Use `group` instead.
	 * Whether this task maps to the default build command.
	 ','line_number':165,'multiline':True]['text':'*
	 * @deprecated Use `group` instead.
	 * Whether this task maps to the default test command.
	 ','line_number':171,'multiline':True]['text':'*
	 * Whether this is a shell or process
	 ','line_number':180,'multiline':True]['text':'*
	 * @deprecated Use presentation options
	 * Controls whether the output view of the running tasks is brought to front or not.
	 * See BaseTaskRunnerConfiguration#showOutput for details.
	 ','line_number':185,'multiline':True]['text':'*
	 * @deprecated Use presentation options
	 * Controls whether the executed command is printed to the output windows as well.
	 ','line_number':192,'multiline':True]['text':'*
	 * @deprecated Use presentation instead
	 ','line_number':198,'multiline':True]['text':'*
	 * @deprecated Use inline commands.
	 * See BaseTaskRunnerConfiguration#suppressTaskName for details.
	 ','line_number':203,'multiline':True]['text':'*
	 * Some commands require that the task argument is highlighted with a special
	 * prefix (e.g. /t: for msbuild). This property can be used to control such
	 * a prefix.
	 ','line_number':209,'multiline':True]['text':'*
	 * @deprecated use the task type instead.
	 * Specifies whether the command is a shell command and therefore must
	 * be executed in a shell interpreter (e.g. cmd.exe, bash, ...).
	 *
	 * Defaults to false if omitted.
	 ','line_number':216,'multiline':True]['text':'*
	 * The command to be executed. Can be an external program or a shell
	 * command.
	 ','line_number':246,'multiline':True]['text':'*
	 * The command options used when the command is executed. Can be omitted.
	 ','line_number':252,'multiline':True]['text':'*
	 * The arguments passed to the command or additional arguments passed to the
	 * command when using a global command.
	 ','line_number':257,'multiline':True]['text':'*
	 * Windows specific command properties
	 ','line_number':267,'multiline':True]['text':'*
	 * OSX specific command properties
	 ','line_number':272,'multiline':True]['text':'*
	 * linux specific command properties
	 ','line_number':277,'multiline':True]['text':'*
	 * The task's name
	 ','line_number':289,'multiline':True]['text':'*
	 * The UI label used for the task.
	 ','line_number':294,'multiline':True]['text':'*
	 * An optional identifier which can be used to reference a task
	 * in a dependsOn or other attributes.
	 ','line_number':299,'multiline':True]['text':'*
	 * Whether the executed command is kept alive and runs in the background.
	 ','line_number':305,'multiline':True]['text':'*
	 * Whether the task should prompt on close for confirmation if running.
	 ','line_number':310,'multiline':True]['text':'*
	 * Defines the group the task belongs too.
	 ','line_number':315,'multiline':True]['text':'*
	 * A description of the task.
	 ','line_number':320,'multiline':True]['text':'*
	 * The other tasks the task depend on
	 ','line_number':325,'multiline':True]['text':'*
	 * The order the dependsOn tasks should be executed in.
	 ','line_number':330,'multiline':True]['text':'*
	 * Controls the behavior of the used terminal
	 ','line_number':335,'multiline':True]['text':'*
	 * Controls shell options.
	 ','line_number':340,'multiline':True]['text':'*
	 * The problem matcher(s) to use to capture problems in the tasks
	 * output.
	 ','line_number':345,'multiline':True]['text':'*
	 * Task run options. Control run related properties.
	 ','line_number':351,'multiline':True]['text':'*
	 * The icon for this task in the terminal tabs list
	 ','line_number':356,'multiline':True]['text':'*
	 * The icon's color in the terminal tabs list
	 ','line_number':361,'multiline':True]['text':'*
	 * Do not show this task in the run task quickpick
	 ','line_number':366,'multiline':True]['text':'*
	 * Custom tasks have the type CUSTOMIZED_TASK_TYPE
	 ','line_number':373,'multiline':True]['text':'*
	 * The contributed type of the task
	 ','line_number':381,'multiline':True]['text':'*
 * The base task runner configuration
 ','line_number':387,'multiline':True]['text':'*
	 * The command to be executed. Can be an external program or a shell
	 * command.
	 ','line_number':392,'multiline':True]['text':'*
	 * @deprecated Use type instead
	 *
	 * Specifies whether the command is a shell command and therefore must
	 * be executed in a shell interpreter (e.g. cmd.exe, bash, ...).
	 *
	 * Defaults to false if omitted.
	 ','line_number':398,'multiline':True]['text':'*
	 * The task type
	 ','line_number':408,'multiline':True]['text':'*
	 * The command options used when the command is executed. Can be omitted.
	 ','line_number':413,'multiline':True]['text':'*
	 * The arguments passed to the command. Can be omitted.
	 ','line_number':418,'multiline':True]['text':'*
	 * Controls whether the output view of the running tasks is brought to front or not.
	 * Valid values are:
	 *   "always": bring the output window always to front when a task is executed.
	 *   "silent": only bring it to front if no problem matcher is defined for the task executed.
	 *   "never": never bring the output window to front.
	 *
	 * If omitted "always" is used.
	 ','line_number':423,'multiline':True]['text':'*
	 * Controls whether the executed command is printed to the output windows as well.
	 ','line_number':434,'multiline':True]['text':'*
	 * The group
	 ','line_number':439,'multiline':True]['text':'*
	 * Controls the behavior of the used terminal
	 ','line_number':444,'multiline':True]['text':'*
	 * If set to false the task name is added as an additional argument to the
	 * command when executed. If set to true the task name is suppressed. If
	 * omitted false is used.
	 ','line_number':449,'multiline':True]['text':'*
	 * Some commands require that the task argument is highlighted with a special
	 * prefix (e.g. /t: for msbuild). This property can be used to control such
	 * a prefix.
	 ','line_number':456,'multiline':True]['text':'*
	 * The problem matcher(s) to used if a global command is executed (e.g. no tasks
	 * are defined). A tasks.json file can either contain a global problemMatcher
	 * property or a tasks property but not both.
	 ','line_number':463,'multiline':True]['text':'*
	 * @deprecated Use `isBackground` instead.
	 *
	 * Specifies whether a global command is a watching the filesystem. A task.json
	 * file can either contain a global isWatching property or a tasks property
	 * but not both.
	 ','line_number':470,'multiline':True]['text':'*
	 * Specifies whether a global command is a background task.
	 ','line_number':479,'multiline':True]['text':'*
	 * Whether the task should prompt on close for confirmation if running.
	 ','line_number':484,'multiline':True]['text':'*
	 * The configuration of the available tasks. A tasks.json file can either
	 * contain a global problemMatcher property or a tasks property but not both.
	 ','line_number':489,'multiline':True]['text':'*
	 * Problem matcher declarations.
	 ','line_number':495,'multiline':True]['text':'*
	 * Optional user input variables.
	 ','line_number':500,'multiline':True]['text':'*
 * A configuration of an external build system. BuildConfiguration.buildSystem
 * must be set to 'program'
 ','line_number':506,'multiline':True]['text':'*
	 * Determines the runner to use
	 ','line_number':514,'multiline':True]['text':'*
	 * The config's version number
	 ','line_number':519,'multiline':True]['text':'*
	 * Windows specific task configuration
	 ','line_number':524,'multiline':True]['text':'*
	 * Mac specific task configuration
	 ','line_number':529,'multiline':True]['text':'*
	 * Linux specific task configuration
	 ','line_number':534,'multiline':True]['text':' remove the name','line_number':1237,'multiline':False]['text':'context.schemaVersion === Tasks.JsonSchemaVersion.V2_0_0;','line_number':1577,'multiline':False]['text':' if the task has its own command then we suppress the','line_number':1596,'multiline':False]['text':' task name by default.','line_number':1597,'multiline':False]['text':' We only merge a command from a global definition if there is no dependsOn','line_number':1604,'multiline':False]['text':' or there is a dependsOn and a defined command.','line_number':1605,'multiline':False]['text':' promptOnClose is inferred from isBackground if available','line_number':1613,'multiline':False]['text':' There is some special logic for tasks with the labels "build" and "test".','line_number':1780,'multiline':False]['text':' Even if they are not marked as a task group Build or Test, we automagically group them as such.','line_number':1781,'multiline':False]['text':' However, if they are already grouped as Build or Test, we don't need to add this grouping.','line_number':1782,'multiline':False]['text':' Tasks are keyed by ID but we need to merge by name','line_number':1803,'multiline':False]