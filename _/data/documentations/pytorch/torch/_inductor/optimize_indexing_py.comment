['text':' bound within mantissa','line_number':22,'multiline':False]['text':' if a downstream use of a node explicitly converts to int32, or float16/float32/float64,','line_number':40,'multiline':False]['text':' then it's precision is set for that chain of uses, and we don't need to consider those','line_number':41,'multiline':False]['text':' dominated values','line_number':42,'multiline':False]['text':' TODO - there are dominated uses whose dtype does not depend on whether','line_number':50,'multiline':False]['text':' we reduce the precision here, e.g. add(int64, int64) one of the args can be reduced to','line_number':51,'multiline':False]['text':' int32 without changing the output precision of the node. this case hasn't shown up','line_number':52,'multiline':False]['text':' We check that we can compute all the indices it's involved in with int32','line_number':61,'multiline':False]['text':' all indices are integers, so make sure that we','line_number':69,'multiline':False]['text':' use the bounds of integers instead of floats.','line_number':70,'multiline':False]['text':' TODO - not sure if we should be doing int/float casts while tracing,','line_number':71,'multiline':False]['text':' might interfere with sympy.','line_number':72,'multiline':False]['text':' TODO - if dominated node of one to_dtype is not expressible in int32,','line_number':109,'multiline':False]['text':' we should short circuit another to_dtype node if that node also dominates','line_number':110,'multiline':False]