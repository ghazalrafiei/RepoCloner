['text':'
 * Copyright (c) Facebook, Inc. and its affiliates.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree.
 ','line_number':1,'multiline':True]['text':' Messages up to this size are formatted entirely on-stack, and don't allocate
 * heap memory ','line_number':32,'multiline':True]['text':' The first call to vsnprintf will clobber args, thus need a copy in case a
   * second vsnprintf call is needed ','line_number':61,'multiline':True]['text':' Format error in prefix (possible if prefix is modified): skip prefix
       * and continue as if nothing happened. ','line_number':73,'multiline':True]['text':'
     * Prefix + suffix alone would overflow the on-stack buffer, thus need to
     * use on-heap buffer. Do not even try to format the string into on-stack
     * buffer.
     ','line_number':81,'multiline':True]['text':' Format error in the message: silently ignore this particular message. ','line_number':95,'multiline':True]['text':' Allocate a buffer on heap, and vsnprintf to this buffer ','line_number':100,'multiline':True]['text':' Prefix didn't fit into on-stack buffer, re-format it again to on-heap
       * buffer ','line_number':107,'multiline':True]['text':' for '\0'-terminator ','line_number':111,'multiline':True]['text':' Copy pre-formatted prefix from on-stack buffer to on-heap buffer ','line_number':115,'multiline':True]['text':' The first call to vsnprintf will clobber args, thus need a copy in case a
   * second vsnprintf call is needed ','line_number':155,'multiline':True]['text':' Format error in prefix (possible if prefix is modified): skip prefix
       * and continue as if nothing happened. ','line_number':167,'multiline':True]['text':'
     * Prefix + suffix alone would overflow the on-stack buffer, thus need to
     * use on-heap buffer. Do not even try to format the string into on-stack
     * buffer.
     ','line_number':175,'multiline':True]['text':' Format error in the message: silently ignore this particular message. ','line_number':189,'multiline':True]['text':' Allocate a buffer on heap, and vsnprintf to this buffer ','line_number':194,'multiline':True]['text':' Prefix didn't fit into on-stack buffer, re-format it again to on-heap
       * buffer ','line_number':201,'multiline':True]['text':' for '\0'-terminator ','line_number':205,'multiline':True]['text':' Copy pre-formatted prefix from on-stack buffer to on-heap buffer ','line_number':209,'multiline':True]['text':' The first call to vsnprintf will clobber args, thus need a copy in case a
   * second vsnprintf call is needed ','line_number':249,'multiline':True]['text':' Format error in prefix (possible if prefix is modified): skip prefix
       * and continue as if nothing happened. ','line_number':264,'multiline':True]['text':'
     * Prefix + suffix alone would overflow the on-stack buffer, thus need to
     * use on-heap buffer. Do not even try to format the string into on-stack
     * buffer.
     ','line_number':272,'multiline':True]['text':' Format error in the message: silently ignore this particular message. ','line_number':286,'multiline':True]['text':' Allocate a buffer on heap, and vsnprintf to this buffer ','line_number':291,'multiline':True]['text':' Prefix didn't fit into on-stack buffer, re-format it again to on-heap
       * buffer ','line_number':298,'multiline':True]['text':' for '\0'-terminator ','line_number':302,'multiline':True]['text':' Copy pre-formatted prefix from on-stack buffer to on-heap buffer ','line_number':306,'multiline':True]['text':' The first call to vsnprintf will clobber args, thus need a copy in case a
   * second vsnprintf call is needed ','line_number':346,'multiline':True]['text':' Format error in prefix (possible if prefix is modified): skip prefix
       * and continue as if nothing happened. ','line_number':358,'multiline':True]['text':'
     * Prefix + suffix alone would overflow the on-stack buffer, thus need to
     * use on-heap buffer. Do not even try to format the string into on-stack
     * buffer.
     ','line_number':366,'multiline':True]['text':' Format error in the message: silently ignore this particular message. ','line_number':380,'multiline':True]['text':' Allocate a buffer on heap, and vsnprintf to this buffer ','line_number':385,'multiline':True]['text':' Prefix didn't fit into on-stack buffer, re-format it again to on-heap
       * buffer ','line_number':392,'multiline':True]['text':' for '\0'-terminator ','line_number':396,'multiline':True]['text':' Copy pre-formatted prefix from on-stack buffer to on-heap buffer ','line_number':400,'multiline':True]['text':' The first call to vsnprintf will clobber args, thus need a copy in case a
   * second vsnprintf call is needed ','line_number':440,'multiline':True]['text':' Format error in prefix (possible if prefix is modified): skip prefix
       * and continue as if nothing happened. ','line_number':452,'multiline':True]['text':'
     * Prefix + suffix alone would overflow the on-stack buffer, thus need to
     * use on-heap buffer. Do not even try to format the string into on-stack
     * buffer.
     ','line_number':460,'multiline':True]['text':' Format error in the message: silently ignore this particular message. ','line_number':474,'multiline':True]['text':' Allocate a buffer on heap, and vsnprintf to this buffer ','line_number':479,'multiline':True]['text':' Prefix didn't fit into on-stack buffer, re-format it again to on-heap
       * buffer ','line_number':486,'multiline':True]['text':' for '\0'-terminator ','line_number':490,'multiline':True]['text':' Copy pre-formatted prefix from on-stack buffer to on-heap buffer ','line_number':494,'multiline':True]