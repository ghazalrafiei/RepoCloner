['text':'
 * Copyright (c) Facebook, Inc. and its affiliates.
 * All rights reserved.
 *
 * This source code is licensed under the BSD-style license found in the
 * LICENSE file in the root directory of this source tree.
 ','line_number':1,'multiline':True]['text':'
   * Test that requantization of numbers ((i - zero point) * 2**s) with
   * - scale = exp2(-s)
   * - zero point in [0, 255]
   * - no output clamping
   * produces exactly i, provided that ((i - zero point) * 2**s) does not
   * overflow.
   ','line_number':76,'multiline':True]['text':' Note: need s >= 1 to ensure scale = exp2(-s) < 1.0 ','line_number':88,'multiline':True]['text':'
   * Test that requantization of numbers (i * 2**s + sign(i - zero point) *
   * 2**(s-1)) with
   * - scale = exp2(-s)
   * - zero point in [1, 255]
   * - no output clamping
   * produces exactly i, provided that ((i - zero point) * 2**s) does not
   * overflow.
   ','line_number':119,'multiline':True]['text':' Note: need s >= 1 to ensure scale = exp2(-s) < 1.0 ','line_number':132,'multiline':True]['text':'
   * Test that requantization of numbers (i * 2**s + sign(i - zero point) *
   * 2**(s-1)) with
   * - scale = exp2(-s)
   * - zero point in [1, 255]
   * - no output clamping
   * produces exactly i, provided that ((i - zero point) * 2**s) does not
   * overflow.
   ','line_number':164,'multiline':True]['text':' Note: need s >= 1 to ensure scale = exp2(-s) < 1.0 ','line_number':177,'multiline':True]['text':' Note: need s >= 1 to ensure scale = exp2(-s) < 1.0 ','line_number':213,'multiline':True]['text':' scale ','line_number':261,'multiline':True]['text':' zero point ','line_number':262,'multiline':True]['text':' scale ','line_number':276,'multiline':True]['text':' zero point ','line_number':277,'multiline':True]['text':' Ensure that outputs are not all identical, as in this case test doesn't
       * validate much ','line_number':315,'multiline':True]['text':' Ensure that outputs are not all identical, as in this case test doesn't
       * validate much ','line_number':362,'multiline':True]['text':' Ensure that outputs are not all identical, as in this case test doesn't
       * validate much ','line_number':424,'multiline':True]