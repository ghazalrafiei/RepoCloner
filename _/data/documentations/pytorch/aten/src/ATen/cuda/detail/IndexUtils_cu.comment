['text':'
 A comparator that will sort SizeAndStride structs by stride,
 in ascending order.
 ','line_number':13,'multiline':True]['text':'
Returns false if there is no possibility that the tensor
has "overlapping" indices and true otherwise.
"Overlapping" indices are two+ valid indices that specify
the same offset within the tensor.
The function does this by checking for a sufficient but not
necessary condition of no overlap. In particular, that
that there exists an ordering of the tensor's dimensions
that is nicely "nested," with each dimension contained
within the next one.
','line_number':26,'multiline':True]['text':' Extract size/stride arrays; only consider size >1 dims. ','line_number':38,'multiline':True]['text':' Short-circuits if tensor is a single element.','line_number':56,'multiline':False]['text':' Ascending order (innermost dimension in sorted view is at [0]) ','line_number':61,'multiline':True]['text':' detail','line_number':73,'multiline':False]['text':' cuda','line_number':74,'multiline':False]['text':' at','line_number':75,'multiline':False]