['text':' to_string, stoi and stod implementation for Android related stuff.','line_number':9,'multiline':False]['text':' Note(jiayq): Do not use the CAFFE2_TESTONLY_FORCE_STD_STRING_TEST macro','line_number':10,'multiline':False]['text':' outside testing code that lives under common_test.cc','line_number':11,'multiline':False]['text':' To mimic `std::stoi` and to avoid including `Exception.h`, throw','line_number':27,'multiline':False]['text':' `std::invalid_argument`.','line_number':28,'multiline':False]['text':' We can't easily detect out-of-range, so we don't use `std::out_of_range`.','line_number':29,'multiline':False]['text':' To mimic `std::stoull` and to avoid including `Exception.h`, throw','line_number':48,'multiline':False]['text':' `std::invalid_argument`.','line_number':49,'multiline':False]['text':' We can't easily detect out-of-range, so we don't use `std::out_of_range`.','line_number':50,'multiline':False]['text':' To mimic `std::stod` and to avoid including `Exception.h`, throw','line_number':62,'multiline':False]['text':' `std::invalid_argument`.','line_number':63,'multiline':False]['text':' We can't easily detect out-of-range, so we don't use `std::out_of_range`.','line_number':64,'multiline':False]['text':' std::stoll doesn't exist in our Android environment, we need to implement','line_number':78,'multiline':False]['text':' it ourselves.','line_number':79,'multiline':False]['text':' To mimic `std::stoll` and to avoid including `Exception.h`, throw','line_number':85,'multiline':False]['text':' `std::invalid_argument`.','line_number':86,'multiline':False]['text':' We can't easily detect out-of-range, so we don't use `std::out_of_range`.','line_number':87,'multiline':False]['text':' std::stoll doesn't exist in our Android environment, we need to implement','line_number':101,'multiline':False]['text':' it ourselves.','line_number':102,'multiline':False]['text':' To mimic `std::stoll` and to avoid including `Exception.h`, throw','line_number':116,'multiline':False]['text':' `std::invalid_argument`.','line_number':117,'multiline':False]['text':' We can't easily detect out-of-range, so we don't use `std::out_of_range`.','line_number':118,'multiline':False]['text':' defined(__ANDROID__) || defined(CAFFE2_FORCE_STD_STRING_FALLBACK_TEST)','line_number':131,'multiline':False]['text':' namespace c10','line_number':133,'multiline':False]['text':' std::strtoll isn't available on Android NDK platform < 21 when building','line_number':137,'multiline':False]['text':' with libstdc++, so bring the global version into std.','line_number':138,'multiline':False]