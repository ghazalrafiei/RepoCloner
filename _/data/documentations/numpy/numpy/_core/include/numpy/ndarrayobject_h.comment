['text':'
 * DON'T INCLUDE THIS DIRECTLY.
 ','line_number':1,'multiline':True]['text':' Includes the "function" C-API -- these are all stored in a
   list of pointers --- one for each file
   The two lists are concatenated into one in multiarray.

   They are available as import_array()
','line_number':14,'multiline':True]['text':'
 * Include any defintions which are defined differently for 1.x and 2.x
 * (Symbols only available on 2.x are not there, but rather guarded.)
 ','line_number':23,'multiline':True]['text':' C-API that requires previous API to be defined ','line_number':29,'multiline':True]['text':' These might be faster without the dereferencing of obj
   going on inside -- of course an optimizing compiler should
   inline the constants inside a for loop making it a moot point
','line_number':132,'multiline':True]['text':' Copy should always return contiguous array ','line_number':176,'multiline':True]['text':'
   Check to see if this key in the dictionary is the "title"
   entry of the tuple (i.e. a duplicate dictionary entry in the fields
   dict).
','line_number':205,'multiline':True]['text':'
     * On PyPy, dictionary keys do not always preserve object identity.
     * Fall back to comparison by value.
     ','line_number':223,'multiline':True]['text':' Macro, for backward compat with "if NPY_TITLE_KEY(key, value) { ..." ','line_number':234,'multiline':True]['text':' NUMPY_CORE_INCLUDE_NUMPY_NDARRAYOBJECT_H_ ','line_number':245,'multiline':True]