['text':' Private options for NewFromDescriptor ','line_number':17,'multiline':True]['text':'
     * Indicate the array should be zeroed, we may use calloc to do so
     * (otherwise much like ).
     ','line_number':19,'multiline':True]['text':' Whether to allow empty strings (implied by ensure dtype identity) ','line_number':24,'multiline':True]['text':'
     * If we take a view into an existing array and use its dtype, then that
     * dtype must be preserved (for example for subarray and S0, but also
     * possibly for future dtypes that store more metadata).
     ','line_number':26,'multiline':True]['text':' TODO: Put the order parameter in PyArray_CopyAnyInto and remove this ','line_number':103,'multiline':True]['text':' FIXME: remove those from here ','line_number':108,'multiline':True]['text':'
 * Calls arr_of_subclass.__array_wrap__(towrap), in order to make 'towrap'
 * have the same ndarray subclass as 'arr_of_subclass'.
 ','line_number':127,'multiline':True]['text':' NUMPY_CORE_SRC_MULTIARRAY_CTORS_H_ ','line_number':142,'multiline':True]