['text':'
 * Check if a python type is a ctypes class.
 *
 * Works like the Py<type>_Check functions, returning true if the argument
 * looks like a ctypes object.
 *
 * This entire function is just a wrapper around the Python function of the
 * same name.
 ','line_number':8,'multiline':True]['text':' If the above fails, then we should just assume that the type is not from
     * ctypes
     ','line_number':43,'multiline':True]['text':' NUMPY_CORE_SRC_COMMON_NPY_CTYPES_H_ ','line_number':50,'multiline':True]