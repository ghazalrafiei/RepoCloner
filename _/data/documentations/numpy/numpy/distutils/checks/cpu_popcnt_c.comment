['text':'
     * Unlike GCC and CLANG, Intel Compiler exposes all supported intrinsics,
     * whether or not the build options for those features are specified.
     * Therefore, we must test #definitions of CPU features when option native/host
     * is enabled via `--cpu-baseline` or through env vr `CFLAGS` otherwise
     * the test will be broken and leads to enable all possible features.
     ','line_number':2,'multiline':True]['text':' To make sure popcnt instructions are generated','line_number':22,'multiline':False]['text':' and been tested against the assembler','line_number':23,'multiline':False]