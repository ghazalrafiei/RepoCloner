['text':'
 * NOTE: This is generated code. Look in numpy/linalg/lapack_lite for
 *       information on remaking this file.
 ','line_number':1,'multiline':True]['text':'
f2c knows the exact rules for precedence, and so omits parentheses where not
strictly necessary. Since this is generated code, we don't really care if
it's readable, and we know what is written is correct. So don't warn about
them.
','line_number':19,'multiline':True]['text':' Table of constant values ','line_number':30,'multiline':True]['text':' Subroutine ','line_number':55,'multiline':True]['text':' System generated locals ','line_number':59,'multiline':True]['text':' Local variables ','line_number':62,'multiline':True]['text':' Subroutine ','line_number':67,'multiline':True]['text':' Subroutine ','line_number':70,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGEBAK forms the right or left eigenvectors of a complex general
    matrix by backward transformation on the computed eigenvectors of the
    balanced matrix output by CGEBAL.

    Arguments
    =========

    JOB     (input) CHARACTER*1
            Specifies the type of backward transformation required:
            = 'N', do nothing, return immediately;
            = 'P', do backward transformation for permutation only;
            = 'S', do backward transformation for scaling only;
            = 'B', do backward transformations for both permutation and
                   scaling.
            JOB must be the same as the argument JOB supplied to CGEBAL.

    SIDE    (input) CHARACTER*1
            = 'R':  V contains right eigenvectors;
            = 'L':  V contains left eigenvectors.

    N       (input) INTEGER
            The number of rows of the matrix V.  N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            The integers ILO and IHI determined by CGEBAL.
            1 <= ILO <= IHI <= N, if N > 0; ILO=1 and IHI=0, if N=0.

    SCALE   (input) REAL array, dimension (N)
            Details of the permutation and scaling factors, as returned
            by CGEBAL.

    M       (input) INTEGER
            The number of columns of the matrix V.  M >= 0.

    V       (input/output) COMPLEX array, dimension (LDV,M)
            On entry, the matrix of right or left eigenvectors to be
            transformed, as returned by CHSEIN or CTREVC.
            On exit, V is overwritten by the transformed eigenvectors.

    LDV     (input) INTEGER
            The leading dimension of the array V. LDV >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    =====================================================================


       Decode and Test the input parameters
','line_number':75,'multiline':True]['text':' Parameter adjustments ','line_number':138,'multiline':True]['text':' Function Body ','line_number':144,'multiline':True]['text':'     Quick return if possible ','line_number':171,'multiline':True]['text':'     Backward balance ','line_number':187,'multiline':True]['text':' L10: ','line_number':196,'multiline':True]['text':' L20: ','line_number':205,'multiline':True]['text':'
       Backward permutation

       For  I = ILO-1 step -1 until 1,
                IHI+1 step 1 until N do --
','line_number':211,'multiline':True]['text':'     End of CGEBAK ','line_number':263,'multiline':True]['text':' cgebak_ ','line_number':265,'multiline':True]['text':' Subroutine ','line_number':267,'multiline':True]['text':' System generated locals ','line_number':270,'multiline':True]['text':' Local variables ','line_number':274,'multiline':True]['text':' Subroutine ','line_number':280,'multiline':True]['text':' Subroutine ','line_number':285,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    CGEBAL balances a general complex matrix A.  This involves, first,
    permuting A by a similarity transformation to isolate eigenvalues
    in the first 1 to ILO-1 and last IHI+1 to N elements on the
    diagonal; and second, applying a diagonal similarity transformation
    to rows and columns ILO to IHI to make the rows and columns as
    close in norm as possible.  Both steps are optional.

    Balancing may reduce the 1-norm of the matrix, and improve the
    accuracy of the computed eigenvalues and/or eigenvectors.

    Arguments
    =========

    JOB     (input) CHARACTER*1
            Specifies the operations to be performed on A:
            = 'N':  none:  simply set ILO = 1, IHI = N, SCALE(I) = 1.0
                    for i = 1,...,N;
            = 'P':  permute only;
            = 'S':  scale only;
            = 'B':  both permute and scale.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the input matrix A.
            On exit,  A is overwritten by the balanced matrix.
            If JOB = 'N', A is not referenced.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    ILO     (output) INTEGER
    IHI     (output) INTEGER
            ILO and IHI are set to integers such that on exit
            A(i,j) = 0 if i > j and j = 1,...,ILO-1 or I = IHI+1,...,N.
            If JOB = 'N' or 'S', ILO = 1 and IHI = N.

    SCALE   (output) REAL array, dimension (N)
            Details of the permutations and scaling factors applied to
            A.  If P(j) is the index of the row and column interchanged
            with row and column j and D(j) is the scaling factor
            applied to row and column j, then
            SCALE(j) = P(j)    for j = 1,...,ILO-1
                     = D(j)    for j = ILO,...,IHI
                     = P(j)    for j = IHI+1,...,N.
            The order in which the interchanges are made is N to IHI+1,
            then 1 to ILO-1.

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The permutations consist of row and column interchanges which put
    the matrix in the form

               ( T1   X   Y  )
       P A P = (  0   B   Z  )
               (  0   0   T2 )

    where T1 and T2 are upper triangular matrices whose eigenvalues lie
    along the diagonal.  The column indices ILO and IHI mark the starting
    and ending columns of the submatrix B. Balancing consists of applying
    a diagonal similarity transformation inv(D) * B * D to make the
    1-norms of each row of B and its corresponding column nearly equal.
    The output matrix is

       ( T1     X*D          Y    )
       (  0  inv(D)*B*D  inv(D)*Z ).
       (  0      0           T2   )

    Information about the permutations P and the diagonal matrix D is
    returned in the vector SCALE.

    This subroutine is based on the EISPACK routine CBAL.

    Modified by Tzu-Yi Chen, Computer Science Division, University of
      California at Berkeley, USA

    =====================================================================


       Test the input parameters
','line_number':291,'multiline':True]['text':' Parameter adjustments ','line_number':390,'multiline':True]['text':' Function Body ','line_number':396,'multiline':True]['text':' L10: ','line_number':423,'multiline':True]['text':'     Permutation to isolate eigenvalues if possible ','line_number':432,'multiline':True]['text':'     Row and column exchange. ','line_number':436,'multiline':True]['text':'     Search for rows isolating an eigenvalue and push them down. ','line_number':454,'multiline':True]['text':'     Search for columns isolating an eigenvalue and push them left. ','line_number':487,'multiline':True]['text':' L130: ','line_number':520,'multiline':True]['text':'
       Balance the submatrix in rows K to L.

       Iterative loop for norm reduction
','line_number':527,'multiline':True]['text':'        Guard against zero C or R due to underflow. ','line_number':565,'multiline':True]['text':' Computing MAX ','line_number':574,'multiline':True]['text':' Computing MIN ','line_number':576,'multiline':True]['text':'           Exit if NaN to avoid infinite loop ','line_number':584,'multiline':True]['text':' Computing MIN ','line_number':602,'multiline':True]['text':'        Now balance. ','line_number':615,'multiline':True]['text':'     End of CGEBAL ','line_number':653,'multiline':True]['text':' cgebal_ ','line_number':655,'multiline':True]['text':' Subroutine ','line_number':657,'multiline':True]['text':' System generated locals ','line_number':661,'multiline':True]['text':' Local variables ','line_number':665,'multiline':True]['text':' Subroutine ','line_number':668,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGEBD2 reduces a complex general m by n matrix A to upper or lower
    real bidiagonal form B by a unitary transformation: Q' * A * P = B.

    If m >= n, B is upper bidiagonal; if m < n, B is lower bidiagonal.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows in the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns in the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the m by n general matrix to be reduced.
            On exit,
            if m >= n, the diagonal and the first superdiagonal are
              overwritten with the upper bidiagonal matrix B; the
              elements below the diagonal, with the array TAUQ, represent
              the unitary matrix Q as a product of elementary
              reflectors, and the elements above the first superdiagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors;
            if m < n, the diagonal and the first subdiagonal are
              overwritten with the lower bidiagonal matrix B; the
              elements below the first subdiagonal, with the array TAUQ,
              represent the unitary matrix Q as a product of
              elementary reflectors, and the elements above the diagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    D       (output) REAL array, dimension (min(M,N))
            The diagonal elements of the bidiagonal matrix B:
            D(i) = A(i,i).

    E       (output) REAL array, dimension (min(M,N)-1)
            The off-diagonal elements of the bidiagonal matrix B:
            if m >= n, E(i) = A(i,i+1) for i = 1,2,...,n-1;
            if m < n, E(i) = A(i+1,i) for i = 1,2,...,m-1.

    TAUQ    (output) COMPLEX array dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix Q. See Further Details.

    TAUP    (output) COMPLEX array, dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix P. See Further Details.

    WORK    (workspace) COMPLEX array, dimension (max(M,N))

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrices Q and P are represented as products of elementary
    reflectors:

    If m >= n,

       Q = H(1) H(2) . . . H(n)  and  P = G(1) G(2) . . . G(n-1)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors; v(1:i-1) = 0, v(i) = 1, and v(i+1:m) is stored on exit in
    A(i+1:m,i); u(1:i) = 0, u(i+1) = 1, and u(i+2:n) is stored on exit in
    A(i,i+2:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    If m < n,

       Q = H(1) H(2) . . . H(m-1)  and  P = G(1) G(2) . . . G(m)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, v and u are complex vectors;
    v(1:i) = 0, v(i+1) = 1, and v(i+2:m) is stored on exit in A(i+2:m,i);
    u(1:i-1) = 0, u(i) = 1, and u(i+1:n) is stored on exit in A(i,i+1:n);
    tauq is stored in TAUQ(i) and taup in TAUP(i).

    The contents of A on exit are illustrated by the following examples:

    m = 6 and n = 5 (m > n):          m = 5 and n = 6 (m < n):

      (  d   e   u1  u1  u1 )           (  d   u1  u1  u1  u1  u1 )
      (  v1  d   e   u2  u2 )           (  e   d   u2  u2  u2  u2 )
      (  v1  v2  d   e   u3 )           (  v1  e   d   u3  u3  u3 )
      (  v1  v2  v3  d   e  )           (  v1  v2  e   d   u4  u4 )
      (  v1  v2  v3  v4  d  )           (  v1  v2  v3  e   d   u5 )
      (  v1  v2  v3  v4  v5 )

    where d and e denote diagonal and off-diagonal elements of B, vi
    denotes an element of the vector defining H(i), and ui an element of
    the vector defining G(i).

    =====================================================================


       Test the input parameters
','line_number':675,'multiline':True]['text':' Parameter adjustments ','line_number':797,'multiline':True]['text':' Function Body ','line_number':807,'multiline':True]['text':'        Reduce to upper bidiagonal form ','line_number':824,'multiline':True]['text':'           Generate elementary reflector H(i) to annihilate A(i+1:m,i) ','line_number':829,'multiline':True]['text':' Computing MIN ','line_number':834,'multiline':True]['text':'           Apply H(i)' to A(i:m,i+1:n) from the left ','line_number':843,'multiline':True]['text':'
                Generate elementary reflector G(i) to annihilate
                A(i,i+2:n)
','line_number':858,'multiline':True]['text':' Computing MIN ','line_number':868,'multiline':True]['text':'              Apply G(i) to A(i+1:m,i+1:n) from the right ','line_number':877,'multiline':True]['text':' L10: ','line_number':893,'multiline':True]['text':'        Reduce to lower bidiagonal form ','line_number':897,'multiline':True]['text':'           Generate elementary reflector G(i) to annihilate A(i,i+1:n) ','line_number':902,'multiline':True]['text':' Computing MIN ','line_number':909,'multiline':True]['text':'           Apply G(i) to A(i+1:m,i:n) from the right ','line_number':918,'multiline':True]['text':'
                Generate elementary reflector H(i) to annihilate
                A(i+2:m,i)
','line_number':934,'multiline':True]['text':' Computing MIN ','line_number':942,'multiline':True]['text':'              Apply H(i)' to A(i+1:m,i+1:n) from the left ','line_number':951,'multiline':True]['text':' L20: ','line_number':966,'multiline':True]['text':'     End of CGEBD2 ','line_number':971,'multiline':True]['text':' cgebd2_ ','line_number':973,'multiline':True]['text':' Subroutine ','line_number':975,'multiline':True]['text':' System generated locals ','line_number':979,'multiline':True]['text':' Local variables ','line_number':984,'multiline':True]['text':' Subroutine ','line_number':987,'multiline':True]['text':' Subroutine ','line_number':991,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGEBRD reduces a general complex M-by-N matrix A to upper or lower
    bidiagonal form B by a unitary transformation: Q**H * A * P = B.

    If m >= n, B is upper bidiagonal; if m < n, B is lower bidiagonal.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows in the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns in the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the M-by-N general matrix to be reduced.
            On exit,
            if m >= n, the diagonal and the first superdiagonal are
              overwritten with the upper bidiagonal matrix B; the
              elements below the diagonal, with the array TAUQ, represent
              the unitary matrix Q as a product of elementary
              reflectors, and the elements above the first superdiagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors;
            if m < n, the diagonal and the first subdiagonal are
              overwritten with the lower bidiagonal matrix B; the
              elements below the first subdiagonal, with the array TAUQ,
              represent the unitary matrix Q as a product of
              elementary reflectors, and the elements above the diagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    D       (output) REAL array, dimension (min(M,N))
            The diagonal elements of the bidiagonal matrix B:
            D(i) = A(i,i).

    E       (output) REAL array, dimension (min(M,N)-1)
            The off-diagonal elements of the bidiagonal matrix B:
            if m >= n, E(i) = A(i,i+1) for i = 1,2,...,n-1;
            if m < n, E(i) = A(i+1,i) for i = 1,2,...,m-1.

    TAUQ    (output) COMPLEX array dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix Q. See Further Details.

    TAUP    (output) COMPLEX array, dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix P. See Further Details.

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The length of the array WORK.  LWORK >= max(1,M,N).
            For optimum performance LWORK >= (M+N)*NB, where NB
            is the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrices Q and P are represented as products of elementary
    reflectors:

    If m >= n,

       Q = H(1) H(2) . . . H(n)  and  P = G(1) G(2) . . . G(n-1)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors; v(1:i-1) = 0, v(i) = 1, and v(i+1:m) is stored on exit in
    A(i+1:m,i); u(1:i) = 0, u(i+1) = 1, and u(i+2:n) is stored on exit in
    A(i,i+2:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    If m < n,

       Q = H(1) H(2) . . . H(m-1)  and  P = G(1) G(2) . . . G(m)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors; v(1:i) = 0, v(i+1) = 1, and v(i+2:m) is stored on exit in
    A(i+2:m,i); u(1:i-1) = 0, u(i) = 1, and u(i+1:n) is stored on exit in
    A(i,i+1:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    The contents of A on exit are illustrated by the following examples:

    m = 6 and n = 5 (m > n):          m = 5 and n = 6 (m < n):

      (  d   e   u1  u1  u1 )           (  d   u1  u1  u1  u1  u1 )
      (  v1  d   e   u2  u2 )           (  e   d   u2  u2  u2  u2 )
      (  v1  v2  d   e   u3 )           (  v1  e   d   u3  u3  u3 )
      (  v1  v2  v3  d   e  )           (  v1  v2  e   d   u4  u4 )
      (  v1  v2  v3  v4  d  )           (  v1  v2  v3  e   d   u5 )
      (  v1  v2  v3  v4  v5 )

    where d and e denote diagonal and off-diagonal elements of B, vi
    denotes an element of the vector defining H(i), and ui an element of
    the vector defining G(i).

    =====================================================================


       Test the input parameters
','line_number':1002,'multiline':True]['text':' Parameter adjustments ','line_number':1135,'multiline':True]['text':' Function Body ','line_number':1145,'multiline':True]['text':' Computing MAX ','line_number':1147,'multiline':True]['text':' if(complicated condition) ','line_number':1161,'multiline':True]['text':' Computing MAX ','line_number':1162,'multiline':True]['text':'     Quick return if possible ','line_number':1176,'multiline':True]['text':'
          Set the crossover point NX.

   Computing MAX
','line_number':1190,'multiline':True]['text':'        Determine when to switch from blocked to unblocked code. ','line_number':1199,'multiline':True]['text':'
                Not enough work space for the optimal NB, consider using
                a smaller block size.
','line_number':1205,'multiline':True]['text':'
          Reduce rows and columns i:i+ib-1 to bidiagonal form and return
          the matrices X and Y which are needed to update the unreduced
          part of the matrix
','line_number':1228,'multiline':True]['text':'
          Update the trailing submatrix A(i+ib:m,i+ib:n), using
          an update of the form  A := A - V*Y' - X*U'
','line_number':1240,'multiline':True]['text':'        Copy diagonal and off-diagonal elements of B back into A ','line_number':1259,'multiline':True]['text':' L10: ','line_number':1270,'multiline':True]['text':' L20: ','line_number':1281,'multiline':True]['text':' L30: ','line_number':1284,'multiline':True]['text':'     Use unblocked code to reduce the remainder of the matrix ','line_number':1287,'multiline':True]['text':'     End of CGEBRD ','line_number':1296,'multiline':True]['text':' cgebrd_ ','line_number':1298,'multiline':True]['text':' Subroutine ','line_number':1300,'multiline':True]['text':' System generated locals ','line_number':1305,'multiline':True]['text':' Local variables ','line_number':1311,'multiline':True]['text':' Subroutine ','line_number':1321,'multiline':True]['text':' Subroutine ','line_number':1325,'multiline':True]['text':' Subroutine ','line_number':1333,'multiline':True]['text':' Subroutine ','line_number':1338,'multiline':True]['text':' Subroutine ','line_number':1346,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGEEV computes for an N-by-N complex nonsymmetric matrix A, the
    eigenvalues and, optionally, the left and/or right eigenvectors.

    The right eigenvector v(j) of A satisfies
                     A * v(j) = lambda(j) * v(j)
    where lambda(j) is its eigenvalue.
    The left eigenvector u(j) of A satisfies
                  u(j)**H * A = lambda(j) * u(j)**H
    where u(j)**H denotes the conjugate transpose of u(j).

    The computed eigenvectors are normalized to have Euclidean norm
    equal to 1 and largest component real.

    Arguments
    =========

    JOBVL   (input) CHARACTER*1
            = 'N': left eigenvectors of A are not computed;
            = 'V': left eigenvectors of are computed.

    JOBVR   (input) CHARACTER*1
            = 'N': right eigenvectors of A are not computed;
            = 'V': right eigenvectors of A are computed.

    N       (input) INTEGER
            The order of the matrix A. N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the N-by-N matrix A.
            On exit, A has been overwritten.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    W       (output) COMPLEX array, dimension (N)
            W contains the computed eigenvalues.

    VL      (output) COMPLEX array, dimension (LDVL,N)
            If JOBVL = 'V', the left eigenvectors u(j) are stored one
            after another in the columns of VL, in the same order
            as their eigenvalues.
            If JOBVL = 'N', VL is not referenced.
            u(j) = VL(:,j), the j-th column of VL.

    LDVL    (input) INTEGER
            The leading dimension of the array VL.  LDVL >= 1; if
            JOBVL = 'V', LDVL >= N.

    VR      (output) COMPLEX array, dimension (LDVR,N)
            If JOBVR = 'V', the right eigenvectors v(j) are stored one
            after another in the columns of VR, in the same order
            as their eigenvalues.
            If JOBVR = 'N', VR is not referenced.
            v(j) = VR(:,j), the j-th column of VR.

    LDVR    (input) INTEGER
            The leading dimension of the array VR.  LDVR >= 1; if
            JOBVR = 'V', LDVR >= N.

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= max(1,2*N).
            For good performance, LWORK must generally be larger.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    RWORK   (workspace) REAL array, dimension (2*N)

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  if INFO = i, the QR algorithm failed to compute all the
                  eigenvalues, and no eigenvectors have been computed;
                  elements and i+1:N of W contain eigenvalues which have
                  converged.

    =====================================================================


       Test the input arguments
','line_number':1361,'multiline':True]['text':' Parameter adjustments ','line_number':1458,'multiline':True]['text':' Function Body ','line_number':1472,'multiline':True]['text':'
       Compute workspace
        (Note: Comments in the code beginning "Workspace:" describe the
         minimal amount of workspace needed at that point in the code,
         as well as the preferred amount for good performance.
         CWorkspace refers to complex workspace, and RWorkspace to real
         workspace. NB refers to the optimal block size for the
         immediately following subroutine, as returned by ILAENV.
         HSWORK refers to the workspace preferred by CHSEQR, as
         calculated below. HSWORK is computed assuming ILO=1 and IHI=N,
         the worst case.)
','line_number':1491,'multiline':True]['text':' Computing MAX ','line_number':1513,'multiline':True]['text':' Computing MAX ','line_number':1520,'multiline':True]['text':' Computing MAX ','line_number':1531,'multiline':True]['text':'     Quick return if possible ','line_number':1550,'multiline':True]['text':'     Get machine constants ','line_number':1556,'multiline':True]['text':'     Scale A if max element outside range [SMLNUM,BIGNUM] ','line_number':1565,'multiline':True]['text':'
       Balance the matrix
       (CWorkspace: none)
       (RWorkspace: need N)
','line_number':1581,'multiline':True]['text':'
       Reduce to upper Hessenberg form
       (CWorkspace: need 2*N, prefer N+N*NB)
       (RWorkspace: none)
','line_number':1590,'multiline':True]['text':'
          Want left eigenvectors
          Copy Householder vectors to VL
','line_number':1604,'multiline':True]['text':'
          Generate unitary matrix in VL
          (CWorkspace: need 2*N-1, prefer N+(N-1)*NB)
          (RWorkspace: none)
','line_number':1613,'multiline':True]['text':'
          Perform QR iteration, accumulating Schur vectors in VL
          (CWorkspace: need 1, prefer HSWORK (see comments) )
          (RWorkspace: none)
','line_number':1623,'multiline':True]['text':'
             Want left and right eigenvectors
             Copy Schur vectors to VR
','line_number':1636,'multiline':True]['text':'
          Want right eigenvectors
          Copy Householder vectors to VR
','line_number':1647,'multiline':True]['text':'
          Generate unitary matrix in VR
          (CWorkspace: need 2*N-1, prefer N+(N-1)*NB)
          (RWorkspace: none)
','line_number':1656,'multiline':True]['text':'
          Perform QR iteration, accumulating Schur vectors in VR
          (CWorkspace: need 1, prefer HSWORK (see comments) )
          (RWorkspace: none)
','line_number':1666,'multiline':True]['text':'
          Compute eigenvalues only
          (CWorkspace: need 1, prefer HSWORK (see comments) )
          (RWorkspace: none)
','line_number':1679,'multiline':True]['text':'     If INFO > 0 from CHSEQR, then quit ','line_number':1691,'multiline':True]['text':'
          Compute left and/or right eigenvectors
          (CWorkspace: need 2*N)
          (RWorkspace: need 2*N)
','line_number':1699,'multiline':True]['text':'
          Undo balancing of left eigenvectors
          (CWorkspace: none)
          (RWorkspace: need N)
','line_number':1713,'multiline':True]['text':'        Normalize left eigenvectors and make largest component real ','line_number':1722,'multiline':True]['text':' Computing 2nd power ','line_number':1731,'multiline':True]['text':' Computing 2nd power ','line_number':1733,'multiline':True]['text':' L10: ','line_number':1736,'multiline':True]['text':' L20: ','line_number':1749,'multiline':True]['text':'
          Undo balancing of right eigenvectors
          (CWorkspace: none)
          (RWorkspace: need N)
','line_number':1755,'multiline':True]['text':'        Normalize right eigenvectors and make largest component real ','line_number':1764,'multiline':True]['text':' Computing 2nd power ','line_number':1773,'multiline':True]['text':' Computing 2nd power ','line_number':1775,'multiline':True]['text':' L30: ','line_number':1778,'multiline':True]['text':' L40: ','line_number':1791,'multiline':True]['text':'     Undo scaling if necessary ','line_number':1795,'multiline':True]['text':' Computing MAX ','line_number':1800,'multiline':True]['text':'     End of CGEEV ','line_number':1815,'multiline':True]['text':' cgeev_ ','line_number':1817,'multiline':True]['text':' Subroutine ','line_number':1819,'multiline':True]['text':' System generated locals ','line_number':1822,'multiline':True]['text':' Local variables ','line_number':1826,'multiline':True]['text':' Subroutine ','line_number':1829,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGEHD2 reduces a complex general matrix A to upper Hessenberg form H
    by a unitary similarity transformation:  Q' * A * Q = H .

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            It is assumed that A is already upper triangular in rows
            and columns 1:ILO-1 and IHI+1:N. ILO and IHI are normally
            set by a previous call to CGEBAL; otherwise they should be
            set to 1 and N respectively. See Further Details.
            1 <= ILO <= IHI <= max(1,N).

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the n by n general matrix to be reduced.
            On exit, the upper triangle and the first subdiagonal of A
            are overwritten with the upper Hessenberg matrix H, and the
            elements below the first subdiagonal, with the array TAU,
            represent the unitary matrix Q as a product of elementary
            reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    TAU     (output) COMPLEX array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace) COMPLEX array, dimension (N)

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrix Q is represented as a product of (ihi-ilo) elementary
    reflectors

       Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0, v(i+1) = 1 and v(ihi+1:n) = 0; v(i+2:ihi) is stored on
    exit in A(i+2:ihi,i), and tau in TAU(i).

    The contents of A are illustrated by the following example, with
    n = 7, ilo = 2 and ihi = 6:

    on entry,                        on exit,

    ( a   a   a   a   a   a   a )    (  a   a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      h   h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  v4  h   h   h )
    (                         a )    (                          a )

    where a denotes an element of the original matrix A, h denotes a
    modified element of the upper Hessenberg matrix H, and vi denotes an
    element of the vector defining H(i).

    =====================================================================


       Test the input parameters
','line_number':1835,'multiline':True]['text':' Parameter adjustments ','line_number':1922,'multiline':True]['text':' Function Body ','line_number':1929,'multiline':True]['text':'        Compute elementary reflector H(i) to annihilate A(i+2:ihi,i) ','line_number':1949,'multiline':True]['text':' Computing MIN ','line_number':1954,'multiline':True]['text':'        Apply H(i) to A(1:ihi,i+1:ihi) from the right ','line_number':1961,'multiline':True]['text':'        Apply H(i)' to A(i+1:ihi,i+1:n) from the left ','line_number':1967,'multiline':True]['text':' L10: ','line_number':1977,'multiline':True]['text':'     End of CGEHD2 ','line_number':1982,'multiline':True]['text':' cgehd2_ ','line_number':1984,'multiline':True]['text':' Subroutine ','line_number':1986,'multiline':True]['text':' System generated locals ','line_number':1990,'multiline':True]['text':' Local variables ','line_number':1994,'multiline':True]['text':' was [65][64] ','line_number':1996,'multiline':True]['text':' Subroutine ','line_number':2000,'multiline':True]['text':' Subroutine ','line_number':2004,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.1)                                  --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
    -- April 2009                                                      --


    Purpose
    =======

    CGEHRD reduces a complex general matrix A to upper Hessenberg form H by
    an unitary similarity transformation:  Q' * A * Q = H .

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            It is assumed that A is already upper triangular in rows
            and columns 1:ILO-1 and IHI+1:N. ILO and IHI are normally
            set by a previous call to CGEBAL; otherwise they should be
            set to 1 and N respectively. See Further Details.
            1 <= ILO <= IHI <= N, if N > 0; ILO=1 and IHI=0, if N=0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the N-by-N general matrix to be reduced.
            On exit, the upper triangle and the first subdiagonal of A
            are overwritten with the upper Hessenberg matrix H, and the
            elements below the first subdiagonal, with the array TAU,
            represent the unitary matrix Q as a product of elementary
            reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    TAU     (output) COMPLEX array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details). Elements 1:ILO-1 and IHI:N-1 of TAU are set to
            zero.

    WORK    (workspace/output) COMPLEX array, dimension (LWORK)
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The length of the array WORK.  LWORK >= max(1,N).
            For optimum performance LWORK >= N*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrix Q is represented as a product of (ihi-ilo) elementary
    reflectors

       Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0, v(i+1) = 1 and v(ihi+1:n) = 0; v(i+2:ihi) is stored on
    exit in A(i+2:ihi,i), and tau in TAU(i).

    The contents of A are illustrated by the following example, with
    n = 7, ilo = 2 and ihi = 6:

    on entry,                        on exit,

    ( a   a   a   a   a   a   a )    (  a   a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      h   h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  v4  h   h   h )
    (                         a )    (                          a )

    where a denotes an element of the original matrix A, h denotes a
    modified element of the upper Hessenberg matrix H, and vi denotes an
    element of the vector defining H(i).

    This file is a slight modification of LAPACK-3.0's DGEHRD
    subroutine incorporating improvements proposed by Quintana-Orti and
    Van de Geijn (2006). (See DLAHR2.)

    =====================================================================


       Test the input parameters
','line_number':2020,'multiline':True]['text':' Parameter adjustments ','line_number':2123,'multiline':True]['text':' Function Body ','line_number':2130,'multiline':True]['text':' Computing MIN ','line_number':2132,'multiline':True]['text':'     Set elements 1:ILO-1 and IHI:N-1 of TAU to zero ','line_number':2158,'multiline':True]['text':' L10: ','line_number':2164,'multiline':True]['text':' L20: ','line_number':2170,'multiline':True]['text':'     Quick return if possible ','line_number':2173,'multiline':True]['text':'
       Determine the block size

   Computing MIN
','line_number':2181,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code
          (last block is always handled by unblocked code)

   Computing MAX
','line_number':2193,'multiline':True]['text':'           Determine if workspace is large enough for blocked code ','line_number':2204,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  determine the
                minimum value of NB, and reduce NB or force use of
                unblocked code

   Computing MAX
','line_number':2209,'multiline':True]['text':'        Use unblocked code below ','line_number':2231,'multiline':True]['text':'        Use blocked code ','line_number':2237,'multiline':True]['text':' Computing MIN ','line_number':2242,'multiline':True]['text':'
             Reduce columns i:i+ib-1 to Hessenberg form, returning the
             matrices V and T of the block reflector H = I - V*T*V'
             which performs the reduction, and also the matrix Y = A*V*T
','line_number':2246,'multiline':True]['text':'
             Apply the block reflector H to A(1:ihi,i+ib:ihi) from the
             right, computing  A := A - Y * V'. V(i+ib,ib-1) must be set
             to 1
','line_number':2255,'multiline':True]['text':'
             Apply the block reflector H to A(1:i,i+1:i+ib-1) from the
             right
','line_number':2273,'multiline':True]['text':' L30: ','line_number':2287,'multiline':True]['text':'
             Apply the block reflector H to A(i+1:ihi,i+ib:n) from the
             left
','line_number':2290,'multiline':True]['text':' L40: ','line_number':2301,'multiline':True]['text':'     Use unblocked code to reduce the rest of the matrix ','line_number':2305,'multiline':True]['text':'     End of CGEHRD ','line_number':2312,'multiline':True]['text':' cgehrd_ ','line_number':2314,'multiline':True]['text':' Subroutine ','line_number':2316,'multiline':True]['text':' System generated locals ','line_number':2319,'multiline':True]['text':' Local variables ','line_number':2322,'multiline':True]['text':' Subroutine ','line_number':2325,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    CGELQ2 computes an LQ factorization of a complex m by n matrix A:
    A = L * Q.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the m by n matrix A.
            On exit, the elements on and below the diagonal of the array
            contain the m by min(m,n) lower trapezoidal matrix L (L is
            lower triangular if m <= n); the elements above the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of elementary reflectors (see Further Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace) COMPLEX array, dimension (M)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(k)' . . . H(2)' H(1)', where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; conjg(v(i+1:n)) is stored on exit in
    A(i,i+1:n), and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':2332,'multiline':True]['text':' Parameter adjustments ','line_number':2396,'multiline':True]['text':' Function Body ','line_number':2403,'multiline':True]['text':'        Generate elementary reflector H(i) to annihilate A(i,i+1:n) ','line_number':2423,'multiline':True]['text':' Computing MIN ','line_number':2430,'multiline':True]['text':'           Apply H(i) to A(i+1:m,i:n) from the right ','line_number':2436,'multiline':True]['text':' L10: ','line_number':2449,'multiline':True]['text':'     End of CGELQ2 ','line_number':2453,'multiline':True]['text':' cgelq2_ ','line_number':2455,'multiline':True]['text':' Subroutine ','line_number':2457,'multiline':True]['text':' System generated locals ','line_number':2460,'multiline':True]['text':' Local variables ','line_number':2463,'multiline':True]['text':' Subroutine ','line_number':2465,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGELQF computes an LQ factorization of a complex M-by-N matrix A:
    A = L * Q.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit, the elements on and below the diagonal of the array
            contain the m-by-min(m,n) lower trapezoidal matrix L (L is
            lower triangular if m <= n); the elements above the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of elementary reflectors (see Further Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= max(1,M).
            For optimum performance LWORK >= M*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(k)' . . . H(2)' H(1)', where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; conjg(v(i+1:n)) is stored on exit in
    A(i,i+1:n), and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':2478,'multiline':True]['text':' Parameter adjustments ','line_number':2553,'multiline':True]['text':' Function Body ','line_number':2560,'multiline':True]['text':'     Quick return if possible ','line_number':2584,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':2597,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':2607,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':2613,'multiline':True]['text':' Computing MAX ','line_number':2619,'multiline':True]['text':'        Use blocked code initially ','line_number':2629,'multiline':True]['text':' Computing MIN ','line_number':2634,'multiline':True]['text':'
             Compute the LQ factorization of the current block
             A(i:i+ib-1,i:n)
','line_number':2638,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':2648,'multiline':True]['text':'              Apply H to A(i+ib:m,i:n) from the right ','line_number':2657,'multiline':True]['text':' L10: ','line_number':2666,'multiline':True]['text':'     Use unblocked code to factor the last or only block. ','line_number':2672,'multiline':True]['text':'     End of CGELQF ','line_number':2684,'multiline':True]['text':' cgelqf_ ','line_number':2686,'multiline':True]['text':' Subroutine ','line_number':2688,'multiline':True]['text':' System generated locals ','line_number':2693,'multiline':True]['text':' Local variables ','line_number':2696,'multiline':True]['text':' Subroutine ','line_number':2702,'multiline':True]['text':' Subroutine ','line_number':2707,'multiline':True]['text':' Subroutine ','line_number':2715,'multiline':True]['text':' Subroutine ','line_number':2721,'multiline':True]['text':' Subroutine ','line_number':2729,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGELSD computes the minimum-norm solution to a real linear least
    squares problem:
        minimize 2-norm(| b - A*x |)
    using the singular value decomposition (SVD) of A. A is an M-by-N
    matrix which may be rank-deficient.

    Several right hand side vectors b and solution vectors x can be
    handled in a single call; they are stored as the columns of the
    M-by-NRHS right hand side matrix B and the N-by-NRHS solution
    matrix X.

    The problem is solved in three steps:
    (1) Reduce the coefficient matrix A to bidiagonal form with
        Householder tranformations, reducing the original problem
        into a "bidiagonal least squares problem" (BLS)
    (2) Solve the BLS using a divide and conquer approach.
    (3) Apply back all the Householder tranformations to solve
        the original least squares problem.

    The effective rank of A is determined by treating as zero those
    singular values which are less than RCOND times the largest singular
    value.

    The divide and conquer algorithm makes very mild assumptions about
    floating point arithmetic. It will work on machines with a guard
    digit in add/subtract, or on those binary machines without guard
    digits which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or
    Cray-2. It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A. N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrices B and X. NRHS >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit, A has been destroyed.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,M).

    B       (input/output) COMPLEX array, dimension (LDB,NRHS)
            On entry, the M-by-NRHS right hand side matrix B.
            On exit, B is overwritten by the N-by-NRHS solution matrix X.
            If m >= n and RANK = n, the residual sum-of-squares for
            the solution in the i-th column is given by the sum of
            squares of the modulus of elements n+1:m in that column.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,M,N).

    S       (output) REAL array, dimension (min(M,N))
            The singular values of A in decreasing order.
            The condition number of A in the 2-norm = S(1)/S(min(m,n)).

    RCOND   (input) REAL
            RCOND is used to determine the effective rank of A.
            Singular values S(i) <= RCOND*S(1) are treated as zero.
            If RCOND < 0, machine precision is used instead.

    RANK    (output) INTEGER
            The effective rank of A, i.e., the number of singular values
            which are greater than RCOND*S(1).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK must be at least 1.
            The exact minimum amount of workspace needed depends on M,
            N and NRHS. As long as LWORK is at least
                2 * N + N * NRHS
            if M is greater than or equal to N or
                2 * M + M * NRHS
            if M is less than N, the code will execute correctly.
            For good performance, LWORK should generally be larger.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the array WORK and the
            minimum sizes of the arrays RWORK and IWORK, and returns
            these values as the first entries of the WORK, RWORK and
            IWORK arrays, and no error message related to LWORK is issued
            by XERBLA.

    RWORK   (workspace) REAL array, dimension (MAX(1,LRWORK))
            LRWORK >=
               10*N + 2*N*SMLSIZ + 8*N*NLVL + 3*SMLSIZ*NRHS +
               MAX( (SMLSIZ+1)**2, N*(1+NRHS) + 2*NRHS )
            if M is greater than or equal to N or
               10*M + 2*M*SMLSIZ + 8*M*NLVL + 3*SMLSIZ*NRHS +
               MAX( (SMLSIZ+1)**2, N*(1+NRHS) + 2*NRHS )
            if M is less than N, the code will execute correctly.
            SMLSIZ is returned by ILAENV and is equal to the maximum
            size of the subproblems at the bottom of the computation
            tree (usually about 25), and
               NLVL = MAX( 0, INT( LOG_2( MIN( M,N )/(SMLSIZ+1) ) ) + 1 )
            On exit, if INFO = 0, RWORK(1) returns the minimum LRWORK.

    IWORK   (workspace) INTEGER array, dimension (MAX(1,LIWORK))
            LIWORK >= max(1, 3*MINMN*NLVL + 11*MINMN),
            where MINMN = MIN( M,N ).
            On exit, if INFO = 0, IWORK(1) returns the minimum LIWORK.

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value.
            > 0:  the algorithm for computing the SVD failed to converge;
                  if INFO = i, i off-diagonal elements of an intermediate
                  bidiagonal form did not converge to zero.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input arguments.
','line_number':2739,'multiline':True]['text':' Parameter adjustments ','line_number':2882,'multiline':True]['text':' Function Body ','line_number':2894,'multiline':True]['text':'
       Compute workspace.
       (Note: Comments in the code beginning "Workspace:" describe the
       minimal amount of workspace needed at that point in the code,
       as well as the preferred amount for good performance.
       NB refers to the optimal block size for the immediately
       following subroutine, as returned by ILAENV.)
','line_number':2911,'multiline':True]['text':' Computing MAX ','line_number':2930,'multiline':True]['text':'
                Path 1a - overdetermined, with many more rows than
                          columns.
','line_number':2938,'multiline':True]['text':' Computing MAX ','line_number':2944,'multiline':True]['text':' Computing MAX ','line_number':2948,'multiline':True]['text':'
                Path 1 - overdetermined or exactly determined.

   Computing MAX
   Computing 2nd power
','line_number':2955,'multiline':True]['text':' Computing MAX ','line_number':2965,'multiline':True]['text':' Computing MAX ','line_number':2970,'multiline':True]['text':' Computing MAX ','line_number':2975,'multiline':True]['text':' Computing MAX ','line_number':2980,'multiline':True]['text':' Computing MAX ','line_number':2983,'multiline':True]['text':'
   Computing MAX
   Computing 2nd power
','line_number':2988,'multiline':True]['text':'
                   Path 2a - underdetermined, with many more columns
                             than rows.
','line_number':2998,'multiline':True]['text':' Computing MAX ','line_number':3005,'multiline':True]['text':' Computing MAX ','line_number':3010,'multiline':True]['text':' Computing MAX ','line_number':3015,'multiline':True]['text':' Computing MAX ','line_number':3021,'multiline':True]['text':' Computing MAX ','line_number':3025,'multiline':True]['text':' Computing MAX ','line_number':3029,'multiline':True]['text':'
       XXX: Ensure the Path 2a case below is triggered.  The workspace
       calculation should use queries for all routines eventually.
   Computing MAX
   Computing MAX
','line_number':3032,'multiline':True]['text':'                 Path 2 - underdetermined. ','line_number':3045,'multiline':True]['text':' Computing MAX ','line_number':3049,'multiline':True]['text':' Computing MAX ','line_number':3054,'multiline':True]['text':' Computing MAX ','line_number':3059,'multiline':True]['text':' Computing MAX ','line_number':3063,'multiline':True]['text':'     Quick return if possible. ','line_number':3086,'multiline':True]['text':'     Get machine parameters. ','line_number':3093,'multiline':True]['text':'     Scale A if max entry outside range [SMLNUM,BIGNUM]. ','line_number':3101,'multiline':True]['text':'        Scale matrix norm up to SMLNUM ','line_number':3107,'multiline':True]['text':'        Scale matrix norm down to BIGNUM. ','line_number':3114,'multiline':True]['text':'        Matrix all zero. Return zero solution. ','line_number':3121,'multiline':True]['text':'     Scale B if max entry outside range [SMLNUM,BIGNUM]. ','line_number':3131,'multiline':True]['text':'        Scale matrix norm up to SMLNUM. ','line_number':3137,'multiline':True]['text':'        Scale matrix norm down to BIGNUM. ','line_number':3144,'multiline':True]['text':'     If M < N make sure B(M+1:N,:) = 0 ','line_number':3151,'multiline':True]['text':'     Overdetermined case. ','line_number':3158,'multiline':True]['text':'        Path 1 - overdetermined or exactly determined. ','line_number':3162,'multiline':True]['text':'           Path 1a - overdetermined, with many more rows than columns ','line_number':3167,'multiline':True]['text':'
             Compute A=Q*R.
             (RWorkspace: need N)
             (CWorkspace: need N, prefer N*NB)
','line_number':3173,'multiline':True]['text':'
             Multiply B by transpose(Q).
             (RWorkspace: need N)
             (CWorkspace: need NRHS, prefer NRHS*NB)
','line_number':3183,'multiline':True]['text':'           Zero out below R. ','line_number':3193,'multiline':True]['text':'
          Bidiagonalize R in A.
          (RWorkspace: need N)
          (CWorkspace: need 2*N+MM, prefer 2*N+(MM+N)*NB)
','line_number':3209,'multiline':True]['text':'
          Multiply B by transpose of left bidiagonalizing vectors of R.
          (CWorkspace: need 2*N+NRHS, prefer 2*N+NRHS*NB)
','line_number':3219,'multiline':True]['text':'        Solve the bidiagonal least squares problem. ','line_number':3228,'multiline':True]['text':'        Multiply B by right bidiagonalizing vectors of R. ','line_number':3236,'multiline':True]['text':' if(complicated condition) ','line_number':3242,'multiline':True]['text':' Computing MAX ','line_number':3243,'multiline':True]['text':'
          Path 2a - underdetermined, with many more columns than rows
          and sufficient workspace for an efficient algorithm.
','line_number':3248,'multiline':True]['text':'
   Computing MAX
   Computing MAX
','line_number':3254,'multiline':True]['text':'
          Compute A=L*Q.
          (CWorkspace: need 2*M, prefer M+M*NB)
','line_number':3268,'multiline':True]['text':'        Copy L to WORK(IL), zeroing out above its diagonal. ','line_number':3278,'multiline':True]['text':'
          Bidiagonalize L in WORK(IL).
          (RWorkspace: need M)
          (CWorkspace: need M*M+4*M, prefer M*M+4*M+2*M*NB)
','line_number':3291,'multiline':True]['text':'
          Multiply B by transpose of left bidiagonalizing vectors of L.
          (CWorkspace: need M*M+4*M+NRHS, prefer M*M+4*M+NRHS*NB)
','line_number':3301,'multiline':True]['text':'        Solve the bidiagonal least squares problem. ','line_number':3310,'multiline':True]['text':'        Multiply B by right bidiagonalizing vectors of L. ','line_number':3319,'multiline':True]['text':'        Zero out below first M rows of B. ','line_number':3325,'multiline':True]['text':'
          Multiply transpose(Q) by B.
          (CWorkspace: need NRHS, prefer NRHS*NB)
','line_number':3332,'multiline':True]['text':'        Path 2 - remaining underdetermined cases. ','line_number':3343,'multiline':True]['text':'
          Bidiagonalize A.
          (RWorkspace: need M)
          (CWorkspace: need 2*M+N, prefer 2*M+(M+N)*NB)
','line_number':3351,'multiline':True]['text':'
          Multiply B by transpose of left bidiagonalizing vectors.
          (CWorkspace: need 2*M+NRHS, prefer 2*M+NRHS*NB)
','line_number':3361,'multiline':True]['text':'        Solve the bidiagonal least squares problem. ','line_number':3370,'multiline':True]['text':'        Multiply B by right bidiagonalizing vectors of A. ','line_number':3379,'multiline':True]['text':'     Undo scaling. ','line_number':3388,'multiline':True]['text':'     End of CGELSD ','line_number':3415,'multiline':True]['text':' cgelsd_ ','line_number':3417,'multiline':True]['text':' Subroutine ','line_number':3419,'multiline':True]['text':' System generated locals ','line_number':3422,'multiline':True]['text':' Local variables ','line_number':3426,'multiline':True]['text':' Subroutine ','line_number':3429,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    CGEQR2 computes a QR factorization of a complex m by n matrix A:
    A = Q * R.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the m by n matrix A.
            On exit, the elements on and above the diagonal of the array
            contain the min(m,n) by n upper trapezoidal matrix R (R is
            upper triangular if m >= n); the elements below the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of elementary reflectors (see Further Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace) COMPLEX array, dimension (N)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(1) H(2) . . . H(k), where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; v(i+1:m) is stored on exit in A(i+1:m,i),
    and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':3435,'multiline':True]['text':' Parameter adjustments ','line_number':3499,'multiline':True]['text':' Function Body ','line_number':3506,'multiline':True]['text':'        Generate elementary reflector H(i) to annihilate A(i+1:m,i) ','line_number':3526,'multiline':True]['text':' Computing MIN ','line_number':3529,'multiline':True]['text':'           Apply H(i)' to A(i:m,i+1:n) from the left ','line_number':3535,'multiline':True]['text':' L10: ','line_number':3549,'multiline':True]['text':'     End of CGEQR2 ','line_number':3553,'multiline':True]['text':' cgeqr2_ ','line_number':3555,'multiline':True]['text':' Subroutine ','line_number':3557,'multiline':True]['text':' System generated locals ','line_number':3560,'multiline':True]['text':' Local variables ','line_number':3563,'multiline':True]['text':' Subroutine ','line_number':3565,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGEQRF computes a QR factorization of a complex M-by-N matrix A:
    A = Q * R.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit, the elements on and above the diagonal of the array
            contain the min(M,N)-by-N upper trapezoidal matrix R (R is
            upper triangular if m >= n); the elements below the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of min(m,n) elementary reflectors (see Further
            Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= max(1,N).
            For optimum performance LWORK >= N*NB, where NB is
            the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(1) H(2) . . . H(k), where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; v(i+1:m) is stored on exit in A(i+1:m,i),
    and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':3578,'multiline':True]['text':' Parameter adjustments ','line_number':3654,'multiline':True]['text':' Function Body ','line_number':3661,'multiline':True]['text':'     Quick return if possible ','line_number':3685,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':3698,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':3708,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':3714,'multiline':True]['text':' Computing MAX ','line_number':3720,'multiline':True]['text':'        Use blocked code initially ','line_number':3730,'multiline':True]['text':' Computing MIN ','line_number':3735,'multiline':True]['text':'
             Compute the QR factorization of the current block
             A(i:m,i:i+ib-1)
','line_number':3739,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':3749,'multiline':True]['text':'              Apply H' to A(i:m,i+ib:n) from the left ','line_number':3758,'multiline':True]['text':' L10: ','line_number':3767,'multiline':True]['text':'     Use unblocked code to factor the last or only block. ','line_number':3773,'multiline':True]['text':'     End of CGEQRF ','line_number':3785,'multiline':True]['text':' cgeqrf_ ','line_number':3787,'multiline':True]['text':' Subroutine ','line_number':3789,'multiline':True]['text':' System generated locals ','line_number':3794,'multiline':True]['text':' Local variables ','line_number':3798,'multiline':True]['text':' Subroutine ','line_number':3804,'multiline':True]['text':' Subroutine ','line_number':3811,'multiline':True]['text':' Subroutine ','line_number':3815,'multiline':True]['text':' Subroutine ','line_number':3820,'multiline':True]['text':' Subroutine ','line_number':3831,'multiline':True]['text':' Subroutine ','line_number':3836,'multiline':True]['text':' Subroutine ','line_number':3840,'multiline':True]['text':' Subroutine ','line_number':3847,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010
       8-15-00:  Improve consistency of WS calculations (eca)


    Purpose
    =======

    CGESDD computes the singular value decomposition (SVD) of a complex
    M-by-N matrix A, optionally computing the left and/or right singular
    vectors, by using divide-and-conquer method. The SVD is written

         A = U * SIGMA * conjugate-transpose(V)

    where SIGMA is an M-by-N matrix which is zero except for its
    min(m,n) diagonal elements, U is an M-by-M unitary matrix, and
    V is an N-by-N unitary matrix.  The diagonal elements of SIGMA
    are the singular values of A; they are real and non-negative, and
    are returned in descending order.  The first min(m,n) columns of
    U and V are the left and right singular vectors of A.

    Note that the routine returns VT = V**H, not V.

    The divide and conquer algorithm makes very mild assumptions about
    floating point arithmetic. It will work on machines with a guard
    digit in add/subtract, or on those binary machines without guard
    digits which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or
    Cray-2. It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    JOBZ    (input) CHARACTER*1
            Specifies options for computing all or part of the matrix U:
            = 'A':  all M columns of U and all N rows of V**H are
                    returned in the arrays U and VT;
            = 'S':  the first min(M,N) columns of U and the first
                    min(M,N) rows of V**H are returned in the arrays U
                    and VT;
            = 'O':  If M >= N, the first N columns of U are overwritten
                    in the array A and all rows of V**H are returned in
                    the array VT;
                    otherwise, all columns of U are returned in the
                    array U and the first M rows of V**H are overwritten
                    in the array A;
            = 'N':  no columns of U or rows of V**H are computed.

    M       (input) INTEGER
            The number of rows of the input matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the input matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit,
            if JOBZ = 'O',  A is overwritten with the first N columns
                            of U (the left singular vectors, stored
                            columnwise) if M >= N;
                            A is overwritten with the first M rows
                            of V**H (the right singular vectors, stored
                            rowwise) otherwise.
            if JOBZ .ne. 'O', the contents of A are destroyed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    S       (output) REAL array, dimension (min(M,N))
            The singular values of A, sorted so that S(i) >= S(i+1).

    U       (output) COMPLEX array, dimension (LDU,UCOL)
            UCOL = M if JOBZ = 'A' or JOBZ = 'O' and M < N;
            UCOL = min(M,N) if JOBZ = 'S'.
            If JOBZ = 'A' or JOBZ = 'O' and M < N, U contains the M-by-M
            unitary matrix U;
            if JOBZ = 'S', U contains the first min(M,N) columns of U
            (the left singular vectors, stored columnwise);
            if JOBZ = 'O' and M >= N, or JOBZ = 'N', U is not referenced.

    LDU     (input) INTEGER
            The leading dimension of the array U.  LDU >= 1; if
            JOBZ = 'S' or 'A' or JOBZ = 'O' and M < N, LDU >= M.

    VT      (output) COMPLEX array, dimension (LDVT,N)
            If JOBZ = 'A' or JOBZ = 'O' and M >= N, VT contains the
            N-by-N unitary matrix V**H;
            if JOBZ = 'S', VT contains the first min(M,N) rows of
            V**H (the right singular vectors, stored rowwise);
            if JOBZ = 'O' and M < N, or JOBZ = 'N', VT is not referenced.

    LDVT    (input) INTEGER
            The leading dimension of the array VT.  LDVT >= 1; if
            JOBZ = 'A' or JOBZ = 'O' and M >= N, LDVT >= N;
            if JOBZ = 'S', LDVT >= min(M,N).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= 1.
            if JOBZ = 'N', LWORK >= 2*min(M,N)+max(M,N).
            if JOBZ = 'O',
                  LWORK >= 2*min(M,N)*min(M,N)+2*min(M,N)+max(M,N).
            if JOBZ = 'S' or 'A',
                  LWORK >= min(M,N)*min(M,N)+2*min(M,N)+max(M,N).
            For good performance, LWORK should generally be larger.

            If LWORK = -1, a workspace query is assumed.  The optimal
            size for the WORK array is calculated and stored in WORK(1),
            and no other work except argument checking is performed.

    RWORK   (workspace) REAL array, dimension (MAX(1,LRWORK))
            If JOBZ = 'N', LRWORK >= 5*min(M,N).
            Otherwise,
            LRWORK >= min(M,N)*max(5*min(M,N)+7,2*max(M,N)+2*min(M,N)+1)

    IWORK   (workspace) INTEGER array, dimension (8*min(M,N))

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  The updating process of SBDSDC did not converge.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Huan Ren, Computer Science Division, University of
       California at Berkeley, USA

    =====================================================================


       Test the input arguments
','line_number':3855,'multiline':True]['text':' Parameter adjustments ','line_number':3995,'multiline':True]['text':' Function Body ','line_number':4010,'multiline':True]['text':'
       Compute workspace
        (Note: Comments in the code beginning "Workspace:" describe the
         minimal amount of workspace needed at that point in the code,
         as well as the preferred amount for good performance.
         CWorkspace refers to complex workspace, and RWorkspace to
         real workspace. NB refers to the optimal block size for the
         immediately following subroutine, as returned by ILAENV.)
','line_number':4039,'multiline':True]['text':'
             There is no complex work space needed for bidiagonal SVD
             The real work space needed for bidiagonal SVD is BDSPAC
             for computing singular values and singular vectors; BDSPAN
             for computing singular values only.
             BDSPAC = 5*N*N + 7*N
             BDSPAN = MAX(7*N+4, 3*N+2+SMLSIZ*(SMLSIZ+8))
','line_number':4052,'multiline':True]['text':'                 Path 1 (M much larger than N, JOBZ='N') ','line_number':4064,'multiline':True]['text':' Computing MAX ','line_number':4068,'multiline':True]['text':'                 Path 2 (M much larger than N, JOBZ='O') ','line_number':4076,'multiline':True]['text':' Computing MAX ','line_number':4080,'multiline':True]['text':' Computing MAX ','line_number':4084,'multiline':True]['text':' Computing MAX ','line_number':4089,'multiline':True]['text':' Computing MAX ','line_number':4094,'multiline':True]['text':'                 Path 3 (M much larger than N, JOBZ='S') ','line_number':4103,'multiline':True]['text':' Computing MAX ','line_number':4107,'multiline':True]['text':' Computing MAX ','line_number':4111,'multiline':True]['text':' Computing MAX ','line_number':4116,'multiline':True]['text':' Computing MAX ','line_number':4121,'multiline':True]['text':'                 Path 4 (M much larger than N, JOBZ='A') ','line_number':4130,'multiline':True]['text':' Computing MAX ','line_number':4134,'multiline':True]['text':' Computing MAX ','line_number':4138,'multiline':True]['text':' Computing MAX ','line_number':4143,'multiline':True]['text':' Computing MAX ','line_number':4148,'multiline':True]['text':'              Path 5 (M much larger than N, but not as much as MNTHR1) ','line_number':4158,'multiline':True]['text':' Computing MAX ','line_number':4164,'multiline':True]['text':' Computing MAX ','line_number':4169,'multiline':True]['text':' Computing MAX ','line_number':4177,'multiline':True]['text':' Computing MAX ','line_number':4182,'multiline':True]['text':' Computing MAX ','line_number':4188,'multiline':True]['text':' Computing MAX ','line_number':4193,'multiline':True]['text':'              Path 6 (M at least N, but not much larger) ','line_number':4201,'multiline':True]['text':' Computing MAX ','line_number':4207,'multiline':True]['text':' Computing MAX ','line_number':4212,'multiline':True]['text':' Computing MAX ','line_number':4220,'multiline':True]['text':' Computing MAX ','line_number':4225,'multiline':True]['text':' Computing MAX ','line_number':4231,'multiline':True]['text':' Computing MAX ','line_number':4236,'multiline':True]['text':'
             There is no complex work space needed for bidiagonal SVD
             The real work space needed for bidiagonal SVD is BDSPAC
             for computing singular values and singular vectors; BDSPAN
             for computing singular values only.
             BDSPAC = 5*M*M + 7*M
             BDSPAN = MAX(7*M+4, 3*M+2+SMLSIZ*(SMLSIZ+8))
','line_number':4245,'multiline':True]['text':'                 Path 1t (N much larger than M, JOBZ='N') ','line_number':4257,'multiline':True]['text':' Computing MAX ','line_number':4261,'multiline':True]['text':'                 Path 2t (N much larger than M, JOBZ='O') ','line_number':4269,'multiline':True]['text':' Computing MAX ','line_number':4273,'multiline':True]['text':' Computing MAX ','line_number':4277,'multiline':True]['text':' Computing MAX ','line_number':4282,'multiline':True]['text':' Computing MAX ','line_number':4287,'multiline':True]['text':'                 Path 3t (N much larger than M, JOBZ='S') ','line_number':4296,'multiline':True]['text':' Computing MAX ','line_number':4300,'multiline':True]['text':' Computing MAX ','line_number':4304,'multiline':True]['text':' Computing MAX ','line_number':4309,'multiline':True]['text':' Computing MAX ','line_number':4314,'multiline':True]['text':'                 Path 4t (N much larger than M, JOBZ='A') ','line_number':4323,'multiline':True]['text':' Computing MAX ','line_number':4327,'multiline':True]['text':' Computing MAX ','line_number':4331,'multiline':True]['text':' Computing MAX ','line_number':4336,'multiline':True]['text':' Computing MAX ','line_number':4341,'multiline':True]['text':'              Path 5t (N much larger than M, but not as much as MNTHR1) ','line_number':4351,'multiline':True]['text':' Computing MAX ','line_number':4357,'multiline':True]['text':' Computing MAX ','line_number':4362,'multiline':True]['text':' Computing MAX ','line_number':4370,'multiline':True]['text':' Computing MAX ','line_number':4375,'multiline':True]['text':' Computing MAX ','line_number':4381,'multiline':True]['text':' Computing MAX ','line_number':4386,'multiline':True]['text':'              Path 6t (N greater than M, but not much larger) ','line_number':4394,'multiline':True]['text':' Computing MAX ','line_number':4400,'multiline':True]['text':' Computing MAX ','line_number':4405,'multiline':True]['text':' Computing MAX ','line_number':4413,'multiline':True]['text':' Computing MAX ','line_number':4418,'multiline':True]['text':' Computing MAX ','line_number':4424,'multiline':True]['text':' Computing MAX ','line_number':4429,'multiline':True]['text':'     Quick returns ','line_number':4446,'multiline':True]['text':'     Get machine constants ','line_number':4460,'multiline':True]['text':'     Scale A if max element outside range [SMLNUM,BIGNUM] ','line_number':4466,'multiline':True]['text':'
          A has at least as many rows as columns. If A has sufficiently
          more rows than columns, first reduce using the QR
          decomposition (if sufficient workspace available)
','line_number':4482,'multiline':True]['text':'
                Path 1 (M much larger than N, JOBZ='N')
                No singular vectors to be computed
','line_number':4492,'multiline':True]['text':'
                Compute A=Q*R
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: need 0)
','line_number':4500,'multiline':True]['text':'              Zero out below R ','line_number':4510,'multiline':True]['text':'
                Bidiagonalize R in A
                (CWorkspace: need 3*N, prefer 2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4521,'multiline':True]['text':'
                Perform bidiagonal SVD, compute singular values only
                (CWorkspace: 0)
                (RWorkspace: need BDSPAN)
','line_number':4532,'multiline':True]['text':'
                Path 2 (M much larger than N, JOBZ='O')
                N left singular vectors to be overwritten on A and
                N right singular vectors to be computed in VT
','line_number':4543,'multiline':True]['text':'              WORK(IU) is N by N ','line_number':4551,'multiline':True]['text':'                 WORK(IR) is M by N ','line_number':4557,'multiline':True]['text':'
                Compute A=Q*R
                (CWorkspace: need N*N+2*N, prefer M*N+N+N*NB)
                (RWorkspace: 0)
','line_number':4566,'multiline':True]['text':'              Copy R to WORK( IR ), zeroing out below it ','line_number':4576,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4584,'multiline':True]['text':'
                Bidiagonalize R in WORK(IR)
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4598,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of R in WORK(IRU) and computing right singular vectors
                of R in WORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':4608,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by the left singular vectors of R
                (CWorkspace: need 2*N*N+3*N, prefer M*N+N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4623,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by the right singular vectors of R
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4636,'multiline':True]['text':'
                Multiply Q in A by left singular vectors of R in
                WORK(IU), storing result in WORK(IR) and copying to A
                (CWorkspace: need 2*N*N, prefer N*N+M*N)
                (RWorkspace: 0)
','line_number':4649,'multiline':True]['text':' Computing MIN ','line_number':4660,'multiline':True]['text':' L10: ','line_number':4668,'multiline':True]['text':'
                Path 3 (M much larger than N, JOBZ='S')
                N left singular vectors to be computed in U and
                N right singular vectors to be computed in VT
','line_number':4673,'multiline':True]['text':'              WORK(IR) is N by N ','line_number':4681,'multiline':True]['text':'
                Compute A=Q*R
                (CWorkspace: need N*N+2*N, prefer N*N+N+N*NB)
                (RWorkspace: 0)
','line_number':4687,'multiline':True]['text':'              Copy R to WORK(IR), zeroing out below it ','line_number':4697,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4705,'multiline':True]['text':'
                Bidiagonalize R in WORK(IR)
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4719,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':4729,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of R
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4744,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of R
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4756,'multiline':True]['text':'
                Multiply Q in A by left singular vectors of R in
                WORK(IR), storing result in U
                (CWorkspace: need N*N)
                (RWorkspace: 0)
','line_number':4769,'multiline':True]['text':'
                Path 4 (M much larger than N, JOBZ='A')
                M left singular vectors to be computed in U and
                N right singular vectors to be computed in VT
','line_number':4782,'multiline':True]['text':'              WORK(IU) is N by N ','line_number':4790,'multiline':True]['text':'
                Compute A=Q*R, copying result to U
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4796,'multiline':True]['text':'
                Generate Q in U
                (CWorkspace: need N+M, prefer N+M*NB)
                (RWorkspace: 0)
','line_number':4807,'multiline':True]['text':'              Produce R in A, zeroing out below it ','line_number':4817,'multiline':True]['text':'
                Bidiagonalize R in A
                (CWorkspace: need 3*N, prefer 2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4828,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':4841,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by left singular vectors of R
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4853,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of R
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':4866,'multiline':True]['text':'
                Multiply Q in U by left singular vectors of R in
                WORK(IU), storing result in A
                (CWorkspace: need N*N)
                (RWorkspace: 0)
','line_number':4879,'multiline':True]['text':'              Copy left singular vectors of A from A to U ','line_number':4889,'multiline':True]['text':'
             MNTHR2 <= M < MNTHR1

             Path 5 (M much larger than N, but not as much as MNTHR1)
             Reduce to bidiagonal form without QR decomposition, use
             CUNGBR and matrix multiplication to compute singular vectors
','line_number':4897,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*N+M, prefer 2*N+(M+N)*NB)
             (RWorkspace: need N)
','line_number':4911,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':4922,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':4936,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4947,'multiline':True]['text':'                 WORK( IU ) is M by N ','line_number':4959,'multiline':True]['text':'                 WORK(IU) is LDWRKU by N ','line_number':4964,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':4970,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in WORK(IU), copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need 3*N*N)
','line_number':4982,'multiline':True]['text':'
                Multiply Q in A by real matrix RWORK(IRU), storing the
                result in WORK(IU), copying to A
                (CWorkspace: need N*N, prefer M*N)
                (Rworkspace: need 3*N*N, prefer N*N+2*M*N)
','line_number':4993,'multiline':True]['text':' Computing MIN ','line_number':5005,'multiline':True]['text':' L20: ','line_number':5012,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5017,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5028,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5039,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need 3*N*N)
','line_number':5054,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: need 0)
                (Rworkspace: need N*N+2*M*N)
','line_number':5065,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5078,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5089,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5100,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need 3*N*N)
','line_number':5115,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: 0)
                (Rworkspace: need 3*N*N)
','line_number':5126,'multiline':True]['text':'
             M .LT. MNTHR2

             Path 6 (M at least N, but not much larger)
             Reduce to bidiagonal form without QR decomposition
             Use CUNMBR to compute singular vectors
','line_number':5141,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*N+M, prefer 2*N+(M+N)*NB)
             (RWorkspace: need N)
','line_number':5155,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':5166,'multiline':True]['text':'                 WORK( IU ) is M by N ','line_number':5181,'multiline':True]['text':'                 WORK( IU ) is LDWRKU by N ','line_number':5186,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5192,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: need 0)
','line_number':5204,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by left singular vectors of A, copying
                to A
                (Cworkspace: need M*N+2*N, prefer M*N+N+N*NB)
                (Rworkspace: need 0)
','line_number':5219,'multiline':True]['text':'
                   Generate Q in A
                   (Cworkspace: need 2*N, prefer N+N*NB)
                   (Rworkspace: need 0)
','line_number':5236,'multiline':True]['text':'
                   Multiply Q in A by real matrix RWORK(IRU), storing the
                   result in WORK(IU), copying to A
                   (CWorkspace: need N*N, prefer M*N)
                   (Rworkspace: need 3*N*N, prefer N*N+2*M*N)
','line_number':5246,'multiline':True]['text':' Computing MIN ','line_number':5258,'multiline':True]['text':' L30: ','line_number':5265,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5271,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':5286,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':5299,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5313,'multiline':True]['text':'              Set the right corner of U to identity matrix ','line_number':5328,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 2*N+M, prefer 2*N+M*NB)
                (RWorkspace: 0)
','line_number':5338,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':5350,'multiline':True]['text':'
          A has more columns than rows. If A has sufficiently more
          columns than rows, first reduce using the LQ decomposition (if
          sufficient workspace available)
','line_number':5368,'multiline':True]['text':'
                Path 1t (N much larger than M, JOBZ='N')
                No singular vectors to be computed
','line_number':5378,'multiline':True]['text':'
                Compute A=L*Q
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5386,'multiline':True]['text':'              Zero out above L ','line_number':5396,'multiline':True]['text':'
                Bidiagonalize L in A
                (CWorkspace: need 3*M, prefer 2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5407,'multiline':True]['text':'
                Perform bidiagonal SVD, compute singular values only
                (CWorkspace: 0)
                (RWorkspace: need BDSPAN)
','line_number':5418,'multiline':True]['text':'
                Path 2t (N much larger than M, JOBZ='O')
                M right singular vectors to be overwritten on A and
                M left singular vectors to be computed in U
','line_number':5429,'multiline':True]['text':'              WORK(IVT) is M by M ','line_number':5438,'multiline':True]['text':'                 WORK(IL) M by N ','line_number':5443,'multiline':True]['text':'                 WORK(IL) is M by CHUNK ','line_number':5449,'multiline':True]['text':'
                Compute A=L*Q
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5457,'multiline':True]['text':'              Copy L to WORK(IL), zeroing about above it ','line_number':5467,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need M*M+2*M, prefer M*M+M+M*NB)
                (RWorkspace: 0)
','line_number':5475,'multiline':True]['text':'
                Bidiagonalize L in WORK(IL)
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5489,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5499,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by the left singular vectors of L
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':5514,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix WORK(IVT)
                Overwrite WORK(IVT) by the right singular vectors of L
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':5526,'multiline':True]['text':'
                Multiply right singular vectors of L in WORK(IL) by Q
                in A, storing result in WORK(IL) and copying to A
                (CWorkspace: need 2*M*M, prefer M*M+M*N))
                (RWorkspace: 0)
','line_number':5539,'multiline':True]['text':' Computing MIN ','line_number':5550,'multiline':True]['text':' L40: ','line_number':5558,'multiline':True]['text':'
               Path 3t (N much larger than M, JOBZ='S')
               M right singular vectors to be computed in VT and
               M left singular vectors to be computed in U
','line_number':5563,'multiline':True]['text':'              WORK(IL) is M by M ','line_number':5571,'multiline':True]['text':'
                Compute A=L*Q
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5577,'multiline':True]['text':'              Copy L to WORK(IL), zeroing out above it ','line_number':5587,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need M*M+2*M, prefer M*M+M+M*NB)
                (RWorkspace: 0)
','line_number':5595,'multiline':True]['text':'
                Bidiagonalize L in WORK(IL)
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5609,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5619,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of L
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+M*NB)
                (RWorkspace: 0)
','line_number':5634,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by left singular vectors of L
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+M*NB)
                (RWorkspace: 0)
','line_number':5646,'multiline':True]['text':'
                Copy VT to WORK(IL), multiply right singular vectors of L
                in WORK(IL) by Q in A, storing result in VT
                (CWorkspace: need M*M)
                (RWorkspace: 0)
','line_number':5659,'multiline':True]['text':'
                Path 9t (N much larger than M, JOBZ='A')
                N right singular vectors to be computed in VT and
                M left singular vectors to be computed in U
','line_number':5672,'multiline':True]['text':'              WORK(IVT) is M by M ','line_number':5680,'multiline':True]['text':'
                Compute A=L*Q, copying result to VT
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5686,'multiline':True]['text':'
                Generate Q in VT
                (CWorkspace: need M+N, prefer M+N*NB)
                (RWorkspace: 0)
','line_number':5697,'multiline':True]['text':'              Produce L in A, zeroing out above it ','line_number':5707,'multiline':True]['text':'
                Bidiagonalize L in A
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5718,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5728,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of L
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: 0)
','line_number':5743,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix WORK(IVT)
                Overwrite WORK(IVT) by right singular vectors of L
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+M*NB)
                (RWorkspace: 0)
','line_number':5755,'multiline':True]['text':'
                Multiply right singular vectors of L in WORK(IVT) by
                Q in VT, storing result in A
                (CWorkspace: need M*M)
                (RWorkspace: 0)
','line_number':5768,'multiline':True]['text':'              Copy right singular vectors of A from A to VT ','line_number':5778,'multiline':True]['text':'
             MNTHR2 <= N < MNTHR1

             Path 5t (N much larger than M, but not as much as MNTHR1)
             Reduce to bidiagonal form without QR decomposition, use
             CUNGBR and matrix multiplication to compute singular vectors
','line_number':5786,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*M+N, prefer 2*M+(M+N)*NB)
             (RWorkspace: M)
','line_number':5801,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':5813,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5827,'multiline':True]['text':'
                Generate P**H in A
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5838,'multiline':True]['text':'                 WORK( IVT ) is M by N ','line_number':5851,'multiline':True]['text':'                 WORK( IVT ) is M by CHUNK ','line_number':5857,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5863,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRVT)
                storing the result in WORK(IVT), copying to U
                (Cworkspace: need 0)
                (Rworkspace: need 2*M*M)
','line_number':5875,'multiline':True]['text':'
                Multiply RWORK(IRVT) by P**H in A, storing the
                result in WORK(IVT), copying to A
                (CWorkspace: need M*M, prefer M*N)
                (Rworkspace: need 2*M*M, prefer 2*M*N)
','line_number':5886,'multiline':True]['text':' Computing MIN ','line_number':5898,'multiline':True]['text':' L50: ','line_number':5905,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5909,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5920,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5931,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: need 0)
                (Rworkspace: need 3*M*M)
','line_number':5946,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need M*M+2*M*N)
','line_number':5957,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5970,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5981,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5992,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: need 0)
                (Rworkspace: need 3*M*M)
','line_number':6007,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need M*M+2*M*N)
','line_number':6018,'multiline':True]['text':'
             N .LT. MNTHR2

             Path 6t (N greater than M, but not much larger)
             Reduce to bidiagonal form without LQ decomposition
             Use CUNMBR to compute singular vectors
','line_number':6032,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*M+N, prefer 2*M+(M+N)*NB)
             (RWorkspace: M)
','line_number':6046,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':6057,'multiline':True]['text':'                 WORK( IVT ) is M by N ','line_number':6070,'multiline':True]['text':'                 WORK( IVT ) is M by CHUNK ','line_number':6076,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':6082,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: need 0)
','line_number':6097,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix WORK(IVT)
                Overwrite WORK(IVT) by right singular vectors of A,
                copying to A
                (Cworkspace: need M*N+2*M, prefer M*N+M+M*NB)
                (Rworkspace: need 0)
','line_number':6111,'multiline':True]['text':'
                   Generate P**H in A
                   (Cworkspace: need 2*M, prefer M+M*NB)
                   (Rworkspace: need 0)
','line_number':6127,'multiline':True]['text':'
                   Multiply Q in A by real matrix RWORK(IRU), storing the
                   result in WORK(IU), copying to A
                   (CWorkspace: need M*M, prefer M*N)
                   (Rworkspace: need 3*M*M, prefer M*M+2*M*N)
','line_number':6137,'multiline':True]['text':' Computing MIN ','line_number':6149,'multiline':True]['text':' L60: ','line_number':6156,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':6161,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: M*M)
','line_number':6176,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: M*M)
','line_number':6188,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':6203,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: M*M)
','line_number':6219,'multiline':True]['text':'              Set all of VT to identity matrix ','line_number':6231,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 2*M+N, prefer 2*M+N*NB)
                (RWorkspace: M*M)
','line_number':6235,'multiline':True]['text':'     Undo scaling if necessary ','line_number':6253,'multiline':True]['text':'     Return optimal workspace in WORK(1) ','line_number':6276,'multiline':True]['text':'     End of CGESDD ','line_number':6282,'multiline':True]['text':' cgesdd_ ','line_number':6284,'multiline':True]['text':' Subroutine ','line_number':6286,'multiline':True]['text':' System generated locals ','line_number':6289,'multiline':True]['text':' Local variables ','line_number':6292,'multiline':True]['text':' Subroutine ','line_number':6293,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGESV computes the solution to a complex system of linear equations
       A * X = B,
    where A is an N-by-N matrix and X and B are N-by-NRHS matrices.

    The LU decomposition with partial pivoting and row interchanges is
    used to factor A as
       A = P * L * U,
    where P is a permutation matrix, L is unit lower triangular, and U is
    upper triangular.  The factored form of A is then used to solve the
    system of equations A * X = B.

    Arguments
    =========

    N       (input) INTEGER
            The number of linear equations, i.e., the order of the
            matrix A.  N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrix B.  NRHS >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the N-by-N coefficient matrix A.
            On exit, the factors L and U from the factorization
            A = P*L*U; the unit diagonal elements of L are not stored.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    IPIV    (output) INTEGER array, dimension (N)
            The pivot indices that define the permutation matrix P;
            row i of the matrix was interchanged with row IPIV(i).

    B       (input/output) COMPLEX array, dimension (LDB,NRHS)
            On entry, the N-by-NRHS matrix of right hand side matrix B.
            On exit, if INFO = 0, the N-by-NRHS solution matrix X.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, U(i,i) is exactly zero.  The factorization
                  has been completed, but the factor U is exactly
                  singular, so the solution could not be computed.

    =====================================================================


       Test the input parameters.
','line_number':6298,'multiline':True]['text':' Parameter adjustments ','line_number':6362,'multiline':True]['text':' Function Body ','line_number':6371,'multiline':True]['text':'     Compute the LU factorization of A. ','line_number':6388,'multiline':True]['text':'        Solve the system A*X = B, overwriting B with X. ','line_number':6393,'multiline':True]['text':'     End of CGESV ','line_number':6400,'multiline':True]['text':' cgesv_ ','line_number':6402,'multiline':True]['text':' Subroutine ','line_number':6404,'multiline':True]['text':' System generated locals ','line_number':6407,'multiline':True]['text':' Local variables ','line_number':6411,'multiline':True]['text':' Subroutine ','line_number':6413,'multiline':True]['text':' Subroutine ','line_number':6417,'multiline':True]['text':' Subroutine ','line_number':6421,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGETF2 computes an LU factorization of a general m-by-n matrix A
    using partial pivoting with row interchanges.

    The factorization has the form
       A = P * L * U
    where P is a permutation matrix, L is lower triangular with unit
    diagonal elements (lower trapezoidal if m > n), and U is upper
    triangular (upper trapezoidal if m < n).

    This is the right-looking Level 2 BLAS version of the algorithm.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the m by n matrix to be factored.
            On exit, the factors L and U from the factorization
            A = P*L*U; the unit diagonal elements of L are not stored.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    IPIV    (output) INTEGER array, dimension (min(M,N))
            The pivot indices; for 1 <= i <= min(M,N), row i of the
            matrix was interchanged with row IPIV(i).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value
            > 0: if INFO = k, U(k,k) is exactly zero. The factorization
                 has been completed, but the factor U is exactly
                 singular, and division by zero will occur if it is used
                 to solve a system of equations.

    =====================================================================


       Test the input parameters.
','line_number':6424,'multiline':True]['text':' Parameter adjustments ','line_number':6480,'multiline':True]['text':' Function Body ','line_number':6486,'multiline':True]['text':'     Quick return if possible ','line_number':6501,'multiline':True]['text':'     Compute machine safe minimum ','line_number':6507,'multiline':True]['text':'        Find pivot and test for singularity. ','line_number':6514,'multiline':True]['text':'           Apply the interchange to columns 1:N. ','line_number':6522,'multiline':True]['text':'           Compute elements J+1:M of J-th column. ','line_number':6528,'multiline':True]['text':' L20: ','line_number':6542,'multiline':True]['text':'           Update trailing submatrix. ','line_number':6554,'multiline':True]['text':' L10: ','line_number':6563,'multiline':True]['text':'     End of CGETF2 ','line_number':6567,'multiline':True]['text':' cgetf2_ ','line_number':6569,'multiline':True]['text':' Subroutine ','line_number':6571,'multiline':True]['text':' System generated locals ','line_number':6574,'multiline':True]['text':' Local variables ','line_number':6578,'multiline':True]['text':' Subroutine ','line_number':6580,'multiline':True]['text':' Subroutine ','line_number':6584,'multiline':True]['text':' Subroutine ','line_number':6591,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGETRF computes an LU factorization of a general M-by-N matrix A
    using partial pivoting with row interchanges.

    The factorization has the form
       A = P * L * U
    where P is a permutation matrix, L is lower triangular with unit
    diagonal elements (lower trapezoidal if m > n), and U is upper
    triangular (upper trapezoidal if m < n).

    This is the right-looking Level 3 BLAS version of the algorithm.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the M-by-N matrix to be factored.
            On exit, the factors L and U from the factorization
            A = P*L*U; the unit diagonal elements of L are not stored.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    IPIV    (output) INTEGER array, dimension (min(M,N))
            The pivot indices; for 1 <= i <= min(M,N), row i of the
            matrix was interchanged with row IPIV(i).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, U(i,i) is exactly zero. The factorization
                  has been completed, but the factor U is exactly
                  singular, and division by zero will occur if it is used
                  to solve a system of equations.

    =====================================================================


       Test the input parameters.
','line_number':6595,'multiline':True]['text':' Parameter adjustments ','line_number':6651,'multiline':True]['text':' Function Body ','line_number':6657,'multiline':True]['text':'     Quick return if possible ','line_number':6672,'multiline':True]['text':'     Determine the block size for this environment. ','line_number':6678,'multiline':True]['text':'        Use unblocked code. ','line_number':6684,'multiline':True]['text':'        Use blocked code. ','line_number':6689,'multiline':True]['text':' Computing MIN ','line_number':6694,'multiline':True]['text':'
             Factor diagonal and subdiagonal blocks and test for exact
             singularity.
','line_number':6698,'multiline':True]['text':'           Adjust INFO and the pivot indices. ','line_number':6706,'multiline':True]['text':' Computing MIN ','line_number':6711,'multiline':True]['text':' L10: ','line_number':6716,'multiline':True]['text':'           Apply interchanges to columns 1:J-1. ','line_number':6719,'multiline':True]['text':'              Apply interchanges to columns J+JB:N. ','line_number':6727,'multiline':True]['text':'              Compute block row of U. ','line_number':6734,'multiline':True]['text':'                 Update trailing submatrix. ','line_number':6742,'multiline':True]['text':' L20: ','line_number':6753,'multiline':True]['text':'     End of CGETRF ','line_number':6758,'multiline':True]['text':' cgetrf_ ','line_number':6760,'multiline':True]['text':' Subroutine ','line_number':6762,'multiline':True]['text':' System generated locals ','line_number':6766,'multiline':True]['text':' Local variables ','line_number':6769,'multiline':True]['text':' Subroutine ','line_number':6771,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CGETRS solves a system of linear equations
       A * X = B,  A**T * X = B,  or  A**H * X = B
    with a general N-by-N matrix A using the LU factorization computed
    by CGETRF.

    Arguments
    =========

    TRANS   (input) CHARACTER*1
            Specifies the form of the system of equations:
            = 'N':  A * X = B     (No transpose)
            = 'T':  A**T * X = B  (Transpose)
            = 'C':  A**H * X = B  (Conjugate transpose)

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrix B.  NRHS >= 0.

    A       (input) COMPLEX array, dimension (LDA,N)
            The factors L and U from the factorization A = P*L*U
            as computed by CGETRF.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    IPIV    (input) INTEGER array, dimension (N)
            The pivot indices from CGETRF; for 1<=i<=N, row i of the
            matrix was interchanged with row IPIV(i).

    B       (input/output) COMPLEX array, dimension (LDB,NRHS)
            On entry, the right hand side matrix B.
            On exit, the solution matrix X.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':6779,'multiline':True]['text':' Parameter adjustments ','line_number':6838,'multiline':True]['text':' Function Body ','line_number':6847,'multiline':True]['text':'     Quick return if possible ','line_number':6868,'multiline':True]['text':'
          Solve A * X = B.

          Apply row interchanges to the right hand sides.
','line_number':6876,'multiline':True]['text':'        Solve L*X = B, overwriting B with X. ','line_number':6884,'multiline':True]['text':'        Solve U*X = B, overwriting B with X. ','line_number':6889,'multiline':True]['text':'
          Solve A**T * X = B  or A**H * X = B.

          Solve U'*X = B, overwriting B with X.
','line_number':6895,'multiline':True]['text':'        Solve L'*X = B, overwriting B with X. ','line_number':6904,'multiline':True]['text':'        Apply row interchanges to the solution vectors. ','line_number':6909,'multiline':True]['text':'     End of CGETRS ','line_number':6916,'multiline':True]['text':' cgetrs_ ','line_number':6918,'multiline':True]['text':' Subroutine ','line_number':6920,'multiline':True]['text':' System generated locals ','line_number':6924,'multiline':True]['text':' Local variables ','line_number':6928,'multiline':True]['text':' Subroutine ','line_number':6938,'multiline':True]['text':' Subroutine ','line_number':6947,'multiline':True]['text':' Subroutine ','line_number':6952,'multiline':True]['text':' Subroutine ','line_number':6958,'multiline':True]['text':' Subroutine ','line_number':6961,'multiline':True]['text':' Subroutine ','line_number':6963,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CHEEVD computes all eigenvalues and, optionally, eigenvectors of a
    complex Hermitian matrix A.  If eigenvectors are desired, it uses a
    divide and conquer algorithm.

    The divide and conquer algorithm makes very mild assumptions about
    floating point arithmetic. It will work on machines with a guard
    digit in add/subtract, or on those binary machines without guard
    digits which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or
    Cray-2. It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    JOBZ    (input) CHARACTER*1
            = 'N':  Compute eigenvalues only;
            = 'V':  Compute eigenvalues and eigenvectors.

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA, N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the
            leading N-by-N upper triangular part of A contains the
            upper triangular part of the matrix A.  If UPLO = 'L',
            the leading N-by-N lower triangular part of A contains
            the lower triangular part of the matrix A.
            On exit, if JOBZ = 'V', then if INFO = 0, A contains the
            orthonormal eigenvectors of the matrix A.
            If JOBZ = 'N', then on exit the lower triangle (if UPLO='L')
            or the upper triangle (if UPLO='U') of A, including the
            diagonal, is destroyed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    W       (output) REAL array, dimension (N)
            If INFO = 0, the eigenvalues in ascending order.

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The length of the array WORK.
            If N <= 1,                LWORK must be at least 1.
            If JOBZ  = 'N' and N > 1, LWORK must be at least N + 1.
            If JOBZ  = 'V' and N > 1, LWORK must be at least 2*N + N**2.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal sizes of the WORK, RWORK and
            IWORK arrays, returns these values as the first entries of
            the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    RWORK   (workspace/output) REAL array,
                                           dimension (LRWORK)
            On exit, if INFO = 0, RWORK(1) returns the optimal LRWORK.

    LRWORK  (input) INTEGER
            The dimension of the array RWORK.
            If N <= 1,                LRWORK must be at least 1.
            If JOBZ  = 'N' and N > 1, LRWORK must be at least N.
            If JOBZ  = 'V' and N > 1, LRWORK must be at least
                           1 + 5*N + 2*N**2.

            If LRWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    IWORK   (workspace/output) INTEGER array, dimension (MAX(1,LIWORK))
            On exit, if INFO = 0, IWORK(1) returns the optimal LIWORK.

    LIWORK  (input) INTEGER
            The dimension of the array IWORK.
            If N <= 1,                LIWORK must be at least 1.
            If JOBZ  = 'N' and N > 1, LIWORK must be at least 1.
            If JOBZ  = 'V' and N > 1, LIWORK must be at least 3 + 5*N.

            If LIWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i and JOBZ = 'N', then the algorithm failed
                  to converge; i off-diagonal elements of an intermediate
                  tridiagonal form did not converge to zero;
                  if INFO = i and JOBZ = 'V', then the algorithm failed
                  to compute an eigenvalue while working on the submatrix
                  lying in rows and columns INFO/(N+1) through
                  mod(INFO,N+1).

    Further Details
    ===============

    Based on contributions by
       Jeff Rutter, Computer Science Division, University of California
       at Berkeley, USA

    Modified description of INFO. Sven, 16 Feb 05.
    =====================================================================


       Test the input parameters.
','line_number':6971,'multiline':True]['text':' Parameter adjustments ','line_number':7096,'multiline':True]['text':' Function Body ','line_number':7105,'multiline':True]['text':' Computing 2nd power ','line_number':7132,'multiline':True]['text':' Computing MAX ','line_number':7141,'multiline':True]['text':'     Quick return if possible ','line_number':7169,'multiline':True]['text':'     Get machine constants. ','line_number':7185,'multiline':True]['text':'     Scale matrix to allowable range, if necessary. ','line_number':7194,'multiline':True]['text':'     Call CHETRD to reduce Hermitian matrix to tridiagonal form. ','line_number':7210,'multiline':True]['text':'
       For eigenvalues only, call SSTERF.  For eigenvectors, first call
       CSTEDC to generate the eigenvector matrix, WORK(INDWRK), of the
       tridiagonal matrix, then call CUNMTR to multiply it to the
       Householder transformations represented as Householder vectors in
       A.
','line_number':7223,'multiline':True]['text':'     If matrix was scaled, then rescale eigenvalues appropriately. ','line_number':7241,'multiline':True]['text':'     End of CHEEVD ','line_number':7259,'multiline':True]['text':' cheevd_ ','line_number':7261,'multiline':True]['text':' Subroutine ','line_number':7263,'multiline':True]['text':' System generated locals ','line_number':7266,'multiline':True]['text':' Local variables ','line_number':7271,'multiline':True]['text':' Subroutine ','line_number':7274,'multiline':True]['text':' Complex ','line_number':7277,'multiline':True]['text':' Subroutine ','line_number':7280,'multiline':True]['text':' Subroutine ','line_number':7285,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CHETD2 reduces a complex Hermitian matrix A to real symmetric
    tridiagonal form T by a unitary similarity transformation:
    Q' * A * Q = T.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            Hermitian matrix A is stored:
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            n-by-n upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n-by-n lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.
            On exit, if UPLO = 'U', the diagonal and first superdiagonal
            of A are overwritten by the corresponding elements of the
            tridiagonal matrix T, and the elements above the first
            superdiagonal, with the array TAU, represent the unitary
            matrix Q as a product of elementary reflectors; if UPLO
            = 'L', the diagonal and first subdiagonal of A are over-
            written by the corresponding elements of the tridiagonal
            matrix T, and the elements below the first subdiagonal, with
            the array TAU, represent the unitary matrix Q as a product
            of elementary reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    D       (output) REAL array, dimension (N)
            The diagonal elements of the tridiagonal matrix T:
            D(i) = A(i,i).

    E       (output) REAL array, dimension (N-1)
            The off-diagonal elements of the tridiagonal matrix T:
            E(i) = A(i,i+1) if UPLO = 'U', E(i) = A(i+1,i) if UPLO = 'L'.

    TAU     (output) COMPLEX array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    If UPLO = 'U', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(n-1) . . . H(2) H(1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(i+1:n) = 0 and v(i) = 1; v(1:i-1) is stored on exit in
    A(1:i-1,i+1), and tau in TAU(i).

    If UPLO = 'L', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(1) H(2) . . . H(n-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0 and v(i+1) = 1; v(i+2:n) is stored on exit in A(i+2:n,i),
    and tau in TAU(i).

    The contents of A on exit are illustrated by the following examples
    with n = 5:

    if UPLO = 'U':                       if UPLO = 'L':

      (  d   e   v2  v3  v4 )              (  d                  )
      (      d   e   v3  v4 )              (  e   d              )
      (          d   e   v4 )              (  v1  e   d          )
      (              d   e  )              (  v1  v2  e   d      )
      (                  d  )              (  v1  v2  v3  e   d  )

    where d and e denote diagonal and off-diagonal elements of T, and vi
    denotes an element of the vector defining H(i).

    =====================================================================


       Test the input parameters
','line_number':7289,'multiline':True]['text':' Parameter adjustments ','line_number':7402,'multiline':True]['text':' Function Body ','line_number':7410,'multiline':True]['text':'     Quick return if possible ','line_number':7426,'multiline':True]['text':'        Reduce the upper triangle of A ','line_number':7434,'multiline':True]['text':'
             Generate elementary reflector H(i) = I - tau * v * v'
             to annihilate A(1:i-1,i+1)
','line_number':7442,'multiline':True]['text':'              Apply H(i) from both sides to A(1:i,1:i) ','line_number':7455,'multiline':True]['text':'              Compute  x := tau * A * v  storing x in TAU(1:i) ','line_number':7460,'multiline':True]['text':'              Compute  w := x - 1/2 * tau * (x'*v) * v ','line_number':7466,'multiline':True]['text':'
                Apply the transformation as a rank-2 update:
                   A := A - v * w' - w * v'
','line_number':7479,'multiline':True]['text':' L10: ','line_number':7502,'multiline':True]['text':'        Reduce the lower triangle of A ','line_number':7508,'multiline':True]['text':'
             Generate elementary reflector H(i) = I - tau * v * v'
             to annihilate A(i+2:n,i)
','line_number':7517,'multiline':True]['text':' Computing MIN ','line_number':7525,'multiline':True]['text':'              Apply H(i) from both sides to A(i+1:n,i+1:n) ','line_number':7534,'multiline':True]['text':'              Compute  x := tau * A * v  storing y in TAU(i:n-1) ','line_number':7539,'multiline':True]['text':'              Compute  w := x - 1/2 * tau * (x'*v) * v ','line_number':7546,'multiline':True]['text':'
                Apply the transformation as a rank-2 update:
                   A := A - v * w' - w * v'
','line_number':7561,'multiline':True]['text':' L20: ','line_number':7586,'multiline':True]['text':'     End of CHETD2 ','line_number':7595,'multiline':True]['text':' chetd2_ ','line_number':7597,'multiline':True]['text':' Subroutine ','line_number':7599,'multiline':True]['text':' System generated locals ','line_number':7603,'multiline':True]['text':' Local variables ','line_number':7607,'multiline':True]['text':' Subroutine ','line_number':7612,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CHETRD reduces a complex Hermitian matrix A to real symmetric
    tridiagonal form T by a unitary similarity transformation:
    Q**H * A * Q = T.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            N-by-N upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading N-by-N lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.
            On exit, if UPLO = 'U', the diagonal and first superdiagonal
            of A are overwritten by the corresponding elements of the
            tridiagonal matrix T, and the elements above the first
            superdiagonal, with the array TAU, represent the unitary
            matrix Q as a product of elementary reflectors; if UPLO
            = 'L', the diagonal and first subdiagonal of A are over-
            written by the corresponding elements of the tridiagonal
            matrix T, and the elements below the first subdiagonal, with
            the array TAU, represent the unitary matrix Q as a product
            of elementary reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    D       (output) REAL array, dimension (N)
            The diagonal elements of the tridiagonal matrix T:
            D(i) = A(i,i).

    E       (output) REAL array, dimension (N-1)
            The off-diagonal elements of the tridiagonal matrix T:
            E(i) = A(i,i+1) if UPLO = 'U', E(i) = A(i+1,i) if UPLO = 'L'.

    TAU     (output) COMPLEX array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= 1.
            For optimum performance LWORK >= N*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    If UPLO = 'U', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(n-1) . . . H(2) H(1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(i+1:n) = 0 and v(i) = 1; v(1:i-1) is stored on exit in
    A(1:i-1,i+1), and tau in TAU(i).

    If UPLO = 'L', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(1) H(2) . . . H(n-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0 and v(i+1) = 1; v(i+2:n) is stored on exit in A(i+2:n,i),
    and tau in TAU(i).

    The contents of A on exit are illustrated by the following examples
    with n = 5:

    if UPLO = 'U':                       if UPLO = 'L':

      (  d   e   v2  v3  v4 )              (  d                  )
      (      d   e   v3  v4 )              (  e   d              )
      (          d   e   v4 )              (  v1  e   d          )
      (              d   e  )              (  v1  v2  e   d      )
      (                  d  )              (  v1  v2  v3  e   d  )

    where d and e denote diagonal and off-diagonal elements of T, and vi
    denotes an element of the vector defining H(i).

    =====================================================================


       Test the input parameters
','line_number':7624,'multiline':True]['text':' Parameter adjustments ','line_number':7748,'multiline':True]['text':' Function Body ','line_number':7757,'multiline':True]['text':'        Determine the block size. ','line_number':7773,'multiline':True]['text':'     Quick return if possible ','line_number':7789,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code
          (last block is always handled by unblocked code).

   Computing MAX
','line_number':7800,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':7811,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  determine the
                minimum value of NB, and reduce NB or force use of
                unblocked code by setting NX = N.

   Computing MAX
','line_number':7817,'multiline':True]['text':'
          Reduce the upper triangle of A.
          Columns 1:kk are handled by the unblocked method.
','line_number':7841,'multiline':True]['text':'
             Reduce columns i:i+nb-1 to tridiagonal form and form the
             matrix W which is needed to update the unreduced part of
             the matrix
','line_number':7852,'multiline':True]['text':'
             Update the unreduced submatrix A(1:i-1,1:i-1), using an
             update of the form:  A := A - V*W' - W*V'
','line_number':7862,'multiline':True]['text':'
             Copy superdiagonal elements back into A, and diagonal
             elements into D
','line_number':7872,'multiline':True]['text':' L10: ','line_number':7885,'multiline':True]['text':' L20: ','line_number':7887,'multiline':True]['text':'        Use unblocked code to reduce the last or only block ','line_number':7890,'multiline':True]['text':'        Reduce the lower triangle of A ','line_number':7895,'multiline':True]['text':'
             Reduce columns i:i+nb-1 to tridiagonal form and form the
             matrix W which is needed to update the unreduced part of
             the matrix
','line_number':7901,'multiline':True]['text':'
             Update the unreduced submatrix A(i+nb:n,i+nb:n), using
             an update of the form:  A := A - V*W' - W*V'
','line_number':7911,'multiline':True]['text':'
             Copy subdiagonal elements back into A, and diagonal
             elements into D
','line_number':7922,'multiline':True]['text':' L30: ','line_number':7935,'multiline':True]['text':' L40: ','line_number':7937,'multiline':True]['text':'        Use unblocked code to reduce the last or only block ','line_number':7940,'multiline':True]['text':'     End of CHETRD ','line_number':7950,'multiline':True]['text':' chetrd_ ','line_number':7952,'multiline':True]['text':' Subroutine ','line_number':7954,'multiline':True]['text':' System generated locals ','line_number':7958,'multiline':True]['text':' Local variables ','line_number':7965,'multiline':True]['text':' was [49][49] ','line_number':7966,'multiline':True]['text':' Subroutine ','line_number':7969,'multiline':True]['text':' Subroutine ','line_number':7974,'multiline':True]['text':'
    -- LAPACK computational routine (version 3.2.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       June 2010

       Purpose
       =======

       CHSEQR computes the eigenvalues of a Hessenberg matrix H
       and, optionally, the matrices T and Z from the Schur decomposition
       H = Z T Z**H, where T is an upper triangular matrix (the
       Schur form), and Z is the unitary matrix of Schur vectors.

       Optionally Z may be postmultiplied into an input unitary
       matrix Q so that this routine can give the Schur factorization
       of a matrix A which has been reduced to the Hessenberg form H
       by the unitary matrix Q:  A = Q*H*Q**H = (QZ)*H*(QZ)**H.

       Arguments
       =========

       JOB   (input) CHARACTER*1
             = 'E':  compute eigenvalues only;
             = 'S':  compute eigenvalues and the Schur form T.

       COMPZ (input) CHARACTER*1
             = 'N':  no Schur vectors are computed;
             = 'I':  Z is initialized to the unit matrix and the matrix Z
                     of Schur vectors of H is returned;
             = 'V':  Z must contain an unitary matrix Q on entry, and
                     the product Q*Z is returned.

       N     (input) INTEGER
             The order of the matrix H.  N .GE. 0.

       ILO   (input) INTEGER
       IHI   (input) INTEGER
             It is assumed that H is already upper triangular in rows
             and columns 1:ILO-1 and IHI+1:N. ILO and IHI are normally
             set by a previous call to CGEBAL, and then passed to CGEHRD
             when the matrix output by CGEBAL is reduced to Hessenberg
             form. Otherwise ILO and IHI should be set to 1 and N
             respectively.  If N.GT.0, then 1.LE.ILO.LE.IHI.LE.N.
             If N = 0, then ILO = 1 and IHI = 0.

       H     (input/output) COMPLEX array, dimension (LDH,N)
             On entry, the upper Hessenberg matrix H.
             On exit, if INFO = 0 and JOB = 'S', H contains the upper
             triangular matrix T from the Schur decomposition (the
             Schur form). If INFO = 0 and JOB = 'E', the contents of
             H are unspecified on exit.  (The output value of H when
             INFO.GT.0 is given under the description of INFO below.)

             Unlike earlier versions of CHSEQR, this subroutine may
             explicitly H(i,j) = 0 for i.GT.j and j = 1, 2, ... ILO-1
             or j = IHI+1, IHI+2, ... N.

       LDH   (input) INTEGER
             The leading dimension of the array H. LDH .GE. max(1,N).

       W        (output) COMPLEX array, dimension (N)
             The computed eigenvalues. If JOB = 'S', the eigenvalues are
             stored in the same order as on the diagonal of the Schur
             form returned in H, with W(i) = H(i,i).

       Z     (input/output) COMPLEX array, dimension (LDZ,N)
             If COMPZ = 'N', Z is not referenced.
             If COMPZ = 'I', on entry Z need not be set and on exit,
             if INFO = 0, Z contains the unitary matrix Z of the Schur
             vectors of H.  If COMPZ = 'V', on entry Z must contain an
             N-by-N matrix Q, which is assumed to be equal to the unit
             matrix except for the submatrix Z(ILO:IHI,ILO:IHI). On exit,
             if INFO = 0, Z contains Q*Z.
             Normally Q is the unitary matrix generated by CUNGHR
             after the call to CGEHRD which formed the Hessenberg matrix
             H. (The output value of Z when INFO.GT.0 is given under
             the description of INFO below.)

       LDZ   (input) INTEGER
             The leading dimension of the array Z.  if COMPZ = 'I' or
             COMPZ = 'V', then LDZ.GE.MAX(1,N).  Otherwize, LDZ.GE.1.

       WORK  (workspace/output) COMPLEX array, dimension (LWORK)
             On exit, if INFO = 0, WORK(1) returns an estimate of
             the optimal value for LWORK.

       LWORK (input) INTEGER
             The dimension of the array WORK.  LWORK .GE. max(1,N)
             is sufficient and delivers very good and sometimes
             optimal performance.  However, LWORK as large as 11*N
             may be required for optimal performance.  A workspace
             query is recommended to determine the optimal workspace
             size.

             If LWORK = -1, then CHSEQR does a workspace query.
             In this case, CHSEQR checks the input parameters and
             estimates the optimal workspace size for the given
             values of N, ILO and IHI.  The estimate is returned
             in WORK(1).  No error message related to LWORK is
             issued by XERBLA.  Neither H nor Z are accessed.


       INFO  (output) INTEGER
               =  0:  successful exit
             .LT. 0:  if INFO = -i, the i-th argument had an illegal
                      value
             .GT. 0:  if INFO = i, CHSEQR failed to compute all of
                  the eigenvalues.  Elements 1:ilo-1 and i+1:n of WR
                  and WI contain those eigenvalues which have been
                  successfully computed.  (Failures are rare.)

                  If INFO .GT. 0 and JOB = 'E', then on exit, the
                  remaining unconverged eigenvalues are the eigen-
                  values of the upper Hessenberg matrix rows and
                  columns ILO through INFO of the final, output
                  value of H.

                  If INFO .GT. 0 and JOB   = 'S', then on exit

             (*)  (initial value of H)*U  = U*(final value of H)

                  where U is a unitary matrix.  The final
                  value of  H is upper Hessenberg and triangular in
                  rows and columns INFO+1 through IHI.

                  If INFO .GT. 0 and COMPZ = 'V', then on exit

                    (final value of Z)  =  (initial value of Z)*U

                  where U is the unitary matrix in (*) (regard-
                  less of the value of JOB.)

                  If INFO .GT. 0 and COMPZ = 'I', then on exit
                        (final value of Z)  = U
                  where U is the unitary matrix in (*) (regard-
                  less of the value of JOB.)

                  If INFO .GT. 0 and COMPZ = 'N', then Z is not
                  accessed.

       ================================================================
               Default values supplied by
               ILAENV(ISPEC,'CHSEQR',JOB(:1)//COMPZ(:1),N,ILO,IHI,LWORK).
               It is suggested that these defaults be adjusted in order
               to attain best performance in each particular
               computational environment.

              ISPEC=12: The CLAHQR vs CLAQR0 crossover point.
                        Default: 75. (Must be at least 11.)

              ISPEC=13: Recommended deflation window size.
                        This depends on ILO, IHI and NS.  NS is the
                        number of simultaneous shifts returned
                        by ILAENV(ISPEC=15).  (See ISPEC=15 below.)
                        The default for (IHI-ILO+1).LE.500 is NS.
                        The default for (IHI-ILO+1).GT.500 is 3*NS/2.

              ISPEC=14: Nibble crossover point. (See IPARMQ for
                        details.)  Default: 14% of deflation window
                        size.

              ISPEC=15: Number of simultaneous shifts in a multishift
                        QR iteration.

                        If IHI-ILO+1 is ...

                        greater than      ...but less    ... the
                        or equal to ...      than        default is

                             1               30          NS =   2(+)
                            30               60          NS =   4(+)
                            60              150          NS =  10(+)
                           150              590          NS =  **
                           590             3000          NS =  64
                          3000             6000          NS = 128
                          6000             infinity      NS = 256

                    (+)  By default some or all matrices of this order
                         are passed to the implicit double shift routine
                         CLAHQR and this parameter is ignored.  See
                         ISPEC=12 above and comments in IPARMQ for
                         details.

                   (**)  The asterisks (**) indicate an ad-hoc
                         function of N increasing from 10 to 64.

              ISPEC=16: Select structured matrix multiply.
                        If the number of simultaneous shifts (specified
                        by ISPEC=15) is less than 14, then the default
                        for ISPEC=16 is 0.  Otherwise the default for
                        ISPEC=16 is 2.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       References:
         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part I: Maintaining Well Focused Shifts, and Level 3
         Performance, SIAM Journal of Matrix Analysis, volume 23, pages
         929--947, 2002.

         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part II: Aggressive Early Deflation, SIAM Journal
         of Matrix Analysis, volume 23, pages 948--973, 2002.

       ================================================================

       ==== Matrices of order NTINY or smaller must be processed by
       .    CLAHQR because of insufficient subdiagonal scratch space.
       .    (This is a hard limit.) ====

       ==== NL allocates some local workspace to help small matrices
       .    through a rare CLAHQR failure.  NL .GT. NTINY = 11 is
       .    required and NL .LE. NMIN = ILAENV(ISPEC=12,...) is recom-
       .    mended.  (The default value of NMIN is 75.)  Using NL = 49
       .    allows up to six simultaneous shifts and a 16-by-16
       .    deflation window.  ====

       ==== Decode and check the input parameters. ====
','line_number':7988,'multiline':True]['text':' Parameter adjustments ','line_number':8212,'multiline':True]['text':' Function Body ','line_number':8222,'multiline':True]['text':'        ==== Quick return in case of invalid argument. ==== ','line_number':8252,'multiline':True]['text':'        ==== Quick return in case N = 0; nothing to do. ==== ','line_number':8260,'multiline':True]['text':'        ==== Quick return in case of a workspace query ==== ','line_number':8266,'multiline':True]['text':'
          ==== Ensure reported workspace size is backward-compatible with
          .    previous LAPACK versions. ====
   Computing MAX
','line_number':8270,'multiline':True]['text':'        ==== copy eigenvalues isolated by CGEBAL ==== ','line_number':8283,'multiline':True]['text':'        ==== Initialize Z, if requested ==== ','line_number':8297,'multiline':True]['text':'        ==== Quick return if possible ==== ','line_number':8304,'multiline':True]['text':'
          ==== CLAHQR/CLAQR0 crossover point ====

   Writing concatenation
','line_number':8313,'multiline':True]['text':'        ==== CLAQR0 for big matrices; CLAHQR for small ones ==== ','line_number':8325,'multiline':True]['text':'           ==== Small matrix ==== ','line_number':8332,'multiline':True]['text':'
                ==== A rare CLAHQR failure!  CLAQR0 sometimes succeeds
                .    when CLAHQR fails. ====
','line_number':8339,'multiline':True]['text':'
                   ==== Larger matrices have enough subdiagonal scratch
                   .    space to call CLAQR0 directly. ====
','line_number':8348,'multiline':True]['text':'
                   ==== Tiny matrices don't have enough subdiagonal
                   .    scratch space to benefit from CLAQR0.  Hence,
                   .    tiny matrices must be copied into a larger
                   .    array before calling CLAQR0. ====
','line_number':8359,'multiline':True]['text':'        ==== Clear out the trash, if necessary. ==== ','line_number':8382,'multiline':True]['text':'
          ==== Ensure reported workspace size is backward-compatible with
          .    previous LAPACK versions. ====

   Computing MAX
','line_number':8390,'multiline':True]['text':'     ==== End of CHSEQR ==== ','line_number':8402,'multiline':True]['text':' chseqr_ ','line_number':8405,'multiline':True]['text':' Subroutine ','line_number':8407,'multiline':True]['text':' System generated locals ','line_number':8411,'multiline':True]['text':' Local variables ','line_number':8416,'multiline':True]['text':' Subroutine ','line_number':8419,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLABRD reduces the first NB rows and columns of a complex general
    m by n matrix A to upper or lower real bidiagonal form by a unitary
    transformation Q' * A * P, and returns the matrices X and Y which
    are needed to apply the transformation to the unreduced part of A.

    If m >= n, A is reduced to upper bidiagonal form; if m < n, to lower
    bidiagonal form.

    This is an auxiliary routine called by CGEBRD

    Arguments
    =========

    M       (input) INTEGER
            The number of rows in the matrix A.

    N       (input) INTEGER
            The number of columns in the matrix A.

    NB      (input) INTEGER
            The number of leading rows and columns of A to be reduced.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the m by n general matrix to be reduced.
            On exit, the first NB rows and columns of the matrix are
            overwritten; the rest of the array is unchanged.
            If m >= n, elements on and below the diagonal in the first NB
              columns, with the array TAUQ, represent the unitary
              matrix Q as a product of elementary reflectors; and
              elements above the diagonal in the first NB rows, with the
              array TAUP, represent the unitary matrix P as a product
              of elementary reflectors.
            If m < n, elements below the diagonal in the first NB
              columns, with the array TAUQ, represent the unitary
              matrix Q as a product of elementary reflectors, and
              elements on and above the diagonal in the first NB rows,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    D       (output) REAL array, dimension (NB)
            The diagonal elements of the first NB rows and columns of
            the reduced matrix.  D(i) = A(i,i).

    E       (output) REAL array, dimension (NB)
            The off-diagonal elements of the first NB rows and columns of
            the reduced matrix.

    TAUQ    (output) COMPLEX array dimension (NB)
            The scalar factors of the elementary reflectors which
            represent the unitary matrix Q. See Further Details.

    TAUP    (output) COMPLEX array, dimension (NB)
            The scalar factors of the elementary reflectors which
            represent the unitary matrix P. See Further Details.

    X       (output) COMPLEX array, dimension (LDX,NB)
            The m-by-nb matrix X required to update the unreduced part
            of A.

    LDX     (input) INTEGER
            The leading dimension of the array X. LDX >= max(1,M).

    Y       (output) COMPLEX array, dimension (LDY,NB)
            The n-by-nb matrix Y required to update the unreduced part
            of A.

    LDY     (input) INTEGER
            The leading dimension of the array Y. LDY >= max(1,N).

    Further Details
    ===============

    The matrices Q and P are represented as products of elementary
    reflectors:

       Q = H(1) H(2) . . . H(nb)  and  P = G(1) G(2) . . . G(nb)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors.

    If m >= n, v(1:i-1) = 0, v(i) = 1, and v(i:m) is stored on exit in
    A(i:m,i); u(1:i) = 0, u(i+1) = 1, and u(i+1:n) is stored on exit in
    A(i,i+1:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    If m < n, v(1:i) = 0, v(i+1) = 1, and v(i+1:m) is stored on exit in
    A(i+2:m,i); u(1:i-1) = 0, u(i) = 1, and u(i:n) is stored on exit in
    A(i,i+1:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    The elements of the vectors v and u together form the m-by-nb matrix
    V and the nb-by-n matrix U' which are needed, with X and Y, to apply
    the transformation to the unreduced part of the matrix, using a block
    update of the form:  A := A - V*Y' - X*U'.

    The contents of A on exit are illustrated by the following examples
    with nb = 2:

    m = 6 and n = 5 (m > n):          m = 5 and n = 6 (m < n):

      (  1   1   u1  u1  u1 )           (  1   u1  u1  u1  u1  u1 )
      (  v1  1   1   u2  u2 )           (  1   1   u2  u2  u2  u2 )
      (  v1  v2  a   a   a  )           (  v1  1   a   a   a   a  )
      (  v1  v2  a   a   a  )           (  v1  v2  a   a   a   a  )
      (  v1  v2  a   a   a  )           (  v1  v2  a   a   a   a  )
      (  v1  v2  a   a   a  )

    where a denotes an element of the original matrix which is unchanged,
    vi denotes an element of the vector defining H(i), and ui an element
    of the vector defining G(i).

    =====================================================================


       Quick return if possible
','line_number':8426,'multiline':True]['text':' Parameter adjustments ','line_number':8559,'multiline':True]['text':' Function Body ','line_number':8574,'multiline':True]['text':'        Reduce to upper bidiagonal form ','line_number':8581,'multiline':True]['text':'           Update A(i:m,i) ','line_number':8586,'multiline':True]['text':'           Generate reflection Q(i) to annihilate A(i+1:m,i) ','line_number':8605,'multiline':True]['text':' Computing MIN ','line_number':8610,'multiline':True]['text':'              Compute Y(i+1:n,i) ','line_number':8620,'multiline':True]['text':'              Update A(i,i+1:n) ','line_number':8652,'multiline':True]['text':'              Generate reflection P(i) to annihilate A(i,i+2:n) ','line_number':8674,'multiline':True]['text':' Computing MIN ','line_number':8679,'multiline':True]['text':'              Compute X(i+1:m,i) ','line_number':8688,'multiline':True]['text':' L10: ','line_number':8720,'multiline':True]['text':'        Reduce to lower bidiagonal form ','line_number':8724,'multiline':True]['text':'           Update A(i,i:n) ','line_number':8729,'multiline':True]['text':'           Generate reflection P(i) to annihilate A(i,i+1:n) ','line_number':8754,'multiline':True]['text':' Computing MIN ','line_number':8759,'multiline':True]['text':'              Compute X(i+1:m,i) ','line_number':8769,'multiline':True]['text':'              Update A(i+1:m,i) ','line_number':8803,'multiline':True]['text':'              Generate reflection Q(i) to annihilate A(i+2:m,i) ','line_number':8821,'multiline':True]['text':' Computing MIN ','line_number':8826,'multiline':True]['text':'              Compute Y(i+1:n,i) ','line_number':8835,'multiline':True]['text':' L20: ','line_number':8869,'multiline':True]['text':'     End of CLABRD ','line_number':8874,'multiline':True]['text':' clabrd_ ','line_number':8876,'multiline':True]['text':' Subroutine ','line_number':8878,'multiline':True]['text':' System generated locals ','line_number':8880,'multiline':True]['text':' Local variables ','line_number':8884,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLACGV conjugates a complex vector of length N.

    Arguments
    =========

    N       (input) INTEGER
            The length of the vector X.  N >= 0.

    X       (input/output) COMPLEX array, dimension
                           (1+(N-1)*abs(INCX))
            On entry, the vector of length N to be conjugated.
            On exit, X is overwritten with conjg(X).

    INCX    (input) INTEGER
            The spacing between successive elements of X.

   =====================================================================
','line_number':8888,'multiline':True]['text':' Parameter adjustments ','line_number':8918,'multiline':True]['text':' Function Body ','line_number':8921,'multiline':True]['text':' L10: ','line_number':8928,'multiline':True]['text':' L20: ','line_number':8941,'multiline':True]['text':'     End of CLACGV ','line_number':8946,'multiline':True]['text':' clacgv_ ','line_number':8948,'multiline':True]['text':' Subroutine ','line_number':8950,'multiline':True]['text':' System generated locals ','line_number':8953,'multiline':True]['text':' Local variables ','line_number':8956,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLACP2 copies all or part of a real two-dimensional matrix A to a
    complex matrix B.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies the part of the matrix A to be copied to B.
            = 'U':      Upper triangular part
            = 'L':      Lower triangular part
            Otherwise:  All of the matrix A

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input) REAL array, dimension (LDA,N)
            The m by n matrix A.  If UPLO = 'U', only the upper trapezium
            is accessed; if UPLO = 'L', only the lower trapezium is
            accessed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    B       (output) COMPLEX array, dimension (LDB,N)
            On exit, B = A in the locations specified by UPLO.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,M).

    =====================================================================
','line_number':8961,'multiline':True]['text':' Parameter adjustments ','line_number':9007,'multiline':True]['text':' Function Body ','line_number':9015,'multiline':True]['text':' L10: ','line_number':9024,'multiline':True]['text':' L20: ','line_number':9026,'multiline':True]['text':' L30: ','line_number':9037,'multiline':True]['text':' L40: ','line_number':9039,'multiline':True]['text':' L50: ','line_number':9050,'multiline':True]['text':' L60: ','line_number':9052,'multiline':True]['text':'     End of CLACP2 ','line_number':9058,'multiline':True]['text':' clacp2_ ','line_number':9060,'multiline':True]['text':' Subroutine ','line_number':9062,'multiline':True]['text':' System generated locals ','line_number':9065,'multiline':True]['text':' Local variables ','line_number':9068,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLACPY copies all or part of a two-dimensional matrix A to another
    matrix B.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies the part of the matrix A to be copied to B.
            = 'U':      Upper triangular part
            = 'L':      Lower triangular part
            Otherwise:  All of the matrix A

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input) COMPLEX array, dimension (LDA,N)
            The m by n matrix A.  If UPLO = 'U', only the upper trapezium
            is accessed; if UPLO = 'L', only the lower trapezium is
            accessed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    B       (output) COMPLEX array, dimension (LDB,N)
            On exit, B = A in the locations specified by UPLO.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,M).

    =====================================================================
','line_number':9073,'multiline':True]['text':' Parameter adjustments ','line_number':9119,'multiline':True]['text':' Function Body ','line_number':9127,'multiline':True]['text':' L10: ','line_number':9136,'multiline':True]['text':' L20: ','line_number':9138,'multiline':True]['text':' L30: ','line_number':9149,'multiline':True]['text':' L40: ','line_number':9151,'multiline':True]['text':' L50: ','line_number':9162,'multiline':True]['text':' L60: ','line_number':9164,'multiline':True]['text':'     End of CLACPY ','line_number':9170,'multiline':True]['text':' clacpy_ ','line_number':9172,'multiline':True]['text':' Subroutine ','line_number':9174,'multiline':True]['text':' System generated locals ','line_number':9177,'multiline':True]['text':' Local variables ','line_number':9183,'multiline':True]['text':' Subroutine ','line_number':9185,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLACRM performs a very simple matrix-matrix multiplication:
             C := A * B,
    where A is M by N and complex; B is N by N and real;
    C is M by N and complex.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A and of the matrix C.
            M >= 0.

    N       (input) INTEGER
            The number of columns and rows of the matrix B and
            the number of columns of the matrix C.
            N >= 0.

    A       (input) COMPLEX array, dimension (LDA, N)
            A contains the M by N matrix A.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >=max(1,M).

    B       (input) REAL array, dimension (LDB, N)
            B contains the N by N matrix B.

    LDB     (input) INTEGER
            The leading dimension of the array B. LDB >=max(1,N).

    C       (input) COMPLEX array, dimension (LDC, N)
            C contains the M by N matrix C.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >=max(1,N).

    RWORK   (workspace) REAL array, dimension (2*M*N)

    =====================================================================


       Quick return if possible.
','line_number':9190,'multiline':True]['text':' Parameter adjustments ','line_number':9243,'multiline':True]['text':' Function Body ','line_number':9255,'multiline':True]['text':' L10: ','line_number':9266,'multiline':True]['text':' L20: ','line_number':9268,'multiline':True]['text':' L30: ','line_number':9281,'multiline':True]['text':' L40: ','line_number':9283,'multiline':True]['text':' L50: ','line_number':9291,'multiline':True]['text':' L60: ','line_number':9293,'multiline':True]['text':' L70: ','line_number':9307,'multiline':True]['text':' L80: ','line_number':9309,'multiline':True]['text':'     End of CLACRM ','line_number':9314,'multiline':True]['text':' clacrm_ ','line_number':9316,'multiline':True]['text':' Complex ','line_number':9318,'multiline':True]['text':' System generated locals ','line_number':9320,'multiline':True]['text':' Local variables ','line_number':9324,'multiline':True]['text':' Subroutine ','line_number':9326,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLADIV := X / Y, where X and Y are complex.  The computation of X / Y
    will not overflow on an intermediary step unless the results
    overflows.

    Arguments
    =========

    X       (input) COMPLEX
    Y       (input) COMPLEX
            The complex scalars X and Y.

    =====================================================================
','line_number':9330,'multiline':True]['text':'     End of CLADIV ','line_number':9365,'multiline':True]['text':' cladiv_ ','line_number':9367,'multiline':True]['text':' Subroutine ','line_number':9369,'multiline':True]['text':' System generated locals ','line_number':9373,'multiline':True]['text':' Local variables ','line_number':9377,'multiline':True]['text':' Subroutine ','line_number':9381,'multiline':True]['text':' Subroutine ','line_number':9384,'multiline':True]['text':' Subroutine ','line_number':9387,'multiline':True]['text':' Subroutine ','line_number':9393,'multiline':True]['text':' Subroutine ','line_number':9396,'multiline':True]['text':' Subroutine ','line_number':9401,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    Using the divide and conquer method, CLAED0 computes all eigenvalues
    of a symmetric tridiagonal matrix which is one diagonal block of
    those from reducing a dense or band Hermitian matrix and
    corresponding eigenvectors of the dense or band matrix.

    Arguments
    =========

    QSIZ   (input) INTEGER
           The dimension of the unitary matrix used to reduce
           the full matrix to tridiagonal form.  QSIZ >= N if ICOMPQ = 1.

    N      (input) INTEGER
           The dimension of the symmetric tridiagonal matrix.  N >= 0.

    D      (input/output) REAL array, dimension (N)
           On entry, the diagonal elements of the tridiagonal matrix.
           On exit, the eigenvalues in ascending order.

    E      (input/output) REAL array, dimension (N-1)
           On entry, the off-diagonal elements of the tridiagonal matrix.
           On exit, E has been destroyed.

    Q      (input/output) COMPLEX array, dimension (LDQ,N)
           On entry, Q must contain an QSIZ x N matrix whose columns
           unitarily orthonormal. It is a part of the unitary matrix
           that reduces the full dense Hermitian matrix to a
           (reducible) symmetric tridiagonal matrix.

    LDQ    (input) INTEGER
           The leading dimension of the array Q.  LDQ >= max(1,N).

    IWORK  (workspace) INTEGER array,
           the dimension of IWORK must be at least
                        6 + 6*N + 5*N*lg N
                        ( lg( N ) = smallest integer k
                                    such that 2^k >= N )

    RWORK  (workspace) REAL array,
                                 dimension (1 + 3*N + 2*N*lg N + 3*N**2)
                          ( lg( N ) = smallest integer k
                                      such that 2^k >= N )

    QSTORE (workspace) COMPLEX array, dimension (LDQS, N)
           Used to store parts of
           the eigenvector matrix when the updating matrix multiplies
           take place.

    LDQS   (input) INTEGER
           The leading dimension of the array QSTORE.
           LDQS >= max(1,N).

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  The algorithm failed to compute an eigenvalue while
                  working on the submatrix lying in rows and columns
                  INFO/(N+1) through mod(INFO,N+1).

    =====================================================================

    Warning:      N could be as big as QSIZ!


       Test the input parameters.
','line_number':9405,'multiline':True]['text':' Parameter adjustments ','line_number':9482,'multiline':True]['text':' Function Body ','line_number':9494,'multiline':True]['text':'
       IF( ICOMPQ .LT. 0 .OR. ICOMPQ .GT. 2 ) THEN
          INFO = -1
       ELSE IF( ( ICOMPQ .EQ. 1 ) .AND. ( QSIZ .LT. MAX( 0, N ) ) )
      $        THEN
','line_number':9497,'multiline':True]['text':'     Quick return if possible ','line_number':9518,'multiline':True]['text':'
       Determine the size and placement of the submatrices, and save in
       the leading elements of IWORK.
','line_number':9527,'multiline':True]['text':' L20: ','line_number':9540,'multiline':True]['text':' L30: ','line_number':9549,'multiline':True]['text':'
       Divide the matrix into SUBPBS submatrices of size at most SMLSIZ+1
       using rank-1 modifications (cuts).
','line_number':9552,'multiline':True]['text':' L40: ','line_number':9564,'multiline':True]['text':'
       Set up workspaces for eigenvalues only/accumulate new vectors
       routine
','line_number':9569,'multiline':True]['text':' Computing 2nd power ','line_number':9590,'multiline':True]['text':'     Initialize pointers ','line_number':9593,'multiline':True]['text':' L50: ','line_number':9598,'multiline':True]['text':'
       Solve each submatrix eigenproblem at the bottom of the divide and
       conquer tree.
','line_number':9602,'multiline':True]['text':' Computing 2nd power ','line_number':9623,'multiline':True]['text':' L60: ','line_number':9636,'multiline':True]['text':' L70: ','line_number':9638,'multiline':True]['text':'
       Successively merge eigensystems of adjacent submatrices
       into eigensystem for the corresponding larger matrix.

       while ( SUBPBS > 1 )
','line_number':9641,'multiline':True]['text':'
       Merge lower order eigensystems (of size MSD2 and MATSIZ - MSD2)
       into an eigensystem of size MATSIZ.  CLAED7 handles the case
       when the eigenvectors of a full or band Hermitian matrix (which
       was reduced to tridiagonal form) are desired.

       I am free to use Q as a valuable working space until Loop 150.
','line_number':9666,'multiline':True]['text':' L90: ','line_number':9686,'multiline':True]['text':'
       end while

       Re-merge the eigenvalues/vectors which were deflated at the final
       merge step.
','line_number':9693,'multiline':True]['text':' L100: ','line_number':9706,'multiline':True]['text':'     End of CLAED0 ','line_number':9712,'multiline':True]['text':' claed0_ ','line_number':9714,'multiline':True]['text':' Subroutine ','line_number':9716,'multiline':True]['text':' System generated locals ','line_number':9723,'multiline':True]['text':' Local variables ','line_number':9726,'multiline':True]['text':' Subroutine ','line_number':9728,'multiline':True]['text':' Subroutine ','line_number':9738,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLAED7 computes the updated eigensystem of a diagonal
    matrix after modification by a rank-one symmetric matrix. This
    routine is used only for the eigenproblem which requires all
    eigenvalues and optionally eigenvectors of a dense or banded
    Hermitian matrix that has been reduced to tridiagonal form.

      T = Q(in) ( D(in) + RHO * Z*Z' ) Q'(in) = Q(out) * D(out) * Q'(out)

      where Z = Q'u, u is a vector of length N with ones in the
      CUTPNT and CUTPNT + 1 th elements and zeros elsewhere.

       The eigenvectors of the original matrix are stored in Q, and the
       eigenvalues are in D.  The algorithm consists of three stages:

          The first stage consists of deflating the size of the problem
          when there are multiple eigenvalues or if there is a zero in
          the Z vector.  For each such occurence the dimension of the
          secular equation problem is reduced by one.  This stage is
          performed by the routine SLAED2.

          The second stage consists of calculating the updated
          eigenvalues. This is done by finding the roots of the secular
          equation via the routine SLAED4 (as called by SLAED3).
          This routine also calculates the eigenvectors of the current
          problem.

          The final stage consists of computing the updated eigenvectors
          directly using the updated eigenvalues.  The eigenvectors for
          the current problem are multiplied with the eigenvectors from
          the overall problem.

    Arguments
    =========

    N      (input) INTEGER
           The dimension of the symmetric tridiagonal matrix.  N >= 0.

    CUTPNT (input) INTEGER
           Contains the location of the last eigenvalue in the leading
           sub-matrix.  min(1,N) <= CUTPNT <= N.

    QSIZ   (input) INTEGER
           The dimension of the unitary matrix used to reduce
           the full matrix to tridiagonal form.  QSIZ >= N.

    TLVLS  (input) INTEGER
           The total number of merging levels in the overall divide and
           conquer tree.

    CURLVL (input) INTEGER
           The current level in the overall merge routine,
           0 <= curlvl <= tlvls.

    CURPBM (input) INTEGER
           The current problem in the current level in the overall
           merge routine (counting from upper left to lower right).

    D      (input/output) REAL array, dimension (N)
           On entry, the eigenvalues of the rank-1-perturbed matrix.
           On exit, the eigenvalues of the repaired matrix.

    Q      (input/output) COMPLEX array, dimension (LDQ,N)
           On entry, the eigenvectors of the rank-1-perturbed matrix.
           On exit, the eigenvectors of the repaired tridiagonal matrix.

    LDQ    (input) INTEGER
           The leading dimension of the array Q.  LDQ >= max(1,N).

    RHO    (input) REAL
           Contains the subdiagonal element used to create the rank-1
           modification.

    INDXQ  (output) INTEGER array, dimension (N)
           This contains the permutation which will reintegrate the
           subproblem just solved back into sorted order,
           ie. D( INDXQ( I = 1, N ) ) will be in ascending order.

    IWORK  (workspace) INTEGER array, dimension (4*N)

    RWORK  (workspace) REAL array,
                                   dimension (3*N+2*QSIZ*N)

    WORK   (workspace) COMPLEX array, dimension (QSIZ*N)

    QSTORE (input/output) REAL array, dimension (N**2+1)
           Stores eigenvectors of submatrices encountered during
           divide and conquer, packed together. QPTR points to
           beginning of the submatrices.

    QPTR   (input/output) INTEGER array, dimension (N+2)
           List of indices pointing to beginning of submatrices stored
           in QSTORE. The submatrices are numbered starting at the
           bottom left of the divide and conquer tree, from left to
           right and bottom to top.

    PRMPTR (input) INTEGER array, dimension (N lg N)
           Contains a list of pointers which indicate where in PERM a
           level's permutation is stored.  PRMPTR(i+1) - PRMPTR(i)
           indicates the size of the permutation and also the size of
           the full, non-deflated problem.

    PERM   (input) INTEGER array, dimension (N lg N)
           Contains the permutations (from deflation and sorting) to be
           applied to each eigenblock.

    GIVPTR (input) INTEGER array, dimension (N lg N)
           Contains a list of pointers which indicate where in GIVCOL a
           level's Givens rotations are stored.  GIVPTR(i+1) - GIVPTR(i)
           indicates the number of Givens rotations.

    GIVCOL (input) INTEGER array, dimension (2, N lg N)
           Each pair of numbers indicates a pair of columns to take place
           in a Givens rotation.

    GIVNUM (input) REAL array, dimension (2, N lg N)
           Each number indicates the S value to be used in the
           corresponding Givens rotation.

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  if INFO = 1, an eigenvalue did not converge

    =====================================================================


       Test the input parameters.
','line_number':9745,'multiline':True]['text':' Parameter adjustments ','line_number':9884,'multiline':True]['text':' Function Body ','line_number':9901,'multiline':True]['text':'
       IF( ICOMPQ.LT.0 .OR. ICOMPQ.GT.1 ) THEN
          INFO = -1
       ELSE IF( N.LT.0 ) THEN
','line_number':9904,'multiline':True]['text':'     Quick return if possible ','line_number':9924,'multiline':True]['text':'
       The following values are for bookkeeping purposes only.  They are
       integer pointers which indicate the portion of the workspace
       used by a particular array in SLAED2 and SLAED3.
','line_number':9930,'multiline':True]['text':'
       Form the z-vector which consists of the last row of Q_1 and the
       first row of Q_2.
','line_number':9946,'multiline':True]['text':' L10: ','line_number':9956,'multiline':True]['text':'
       When solving the final problem, we no longer need the stored data,
       so we will overwrite the data from this level onto the previously
       used storage space.
','line_number':9963,'multiline':True]['text':'     Sort and Deflate eigenvalues. ','line_number':9975,'multiline':True]['text':'     Solve Secular Equation. ','line_number':9984,'multiline':True]['text':' Computing 2nd power ','line_number':9991,'multiline':True]['text':'     Prepare the INDXQ sorting premutation. ','line_number':9998,'multiline':True]['text':' L20: ','line_number':10008,'multiline':True]['text':'     End of CLAED7 ','line_number':10014,'multiline':True]['text':' claed7_ ','line_number':10016,'multiline':True]['text':' Subroutine ','line_number':10018,'multiline':True]['text':' System generated locals ','line_number':10024,'multiline':True]['text':' Local variables ','line_number':10028,'multiline':True]['text':' Subroutine ','line_number':10035,'multiline':True]['text':' Subroutine ','line_number':10041,'multiline':True]['text':' Subroutine ','line_number':10045,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    CLAED8 merges the two sets of eigenvalues together into a single
    sorted set.  Then it tries to deflate the size of the problem.
    There are two ways in which deflation can occur:  when two or more
    eigenvalues are close together or if there is a tiny element in the
    Z vector.  For each such occurrence the order of the related secular
    equation problem is reduced by one.

    Arguments
    =========

    K      (output) INTEGER
           Contains the number of non-deflated eigenvalues.
           This is the order of the related secular equation.

    N      (input) INTEGER
           The dimension of the symmetric tridiagonal matrix.  N >= 0.

    QSIZ   (input) INTEGER
           The dimension of the unitary matrix used to reduce
           the dense or band matrix to tridiagonal form.
           QSIZ >= N if ICOMPQ = 1.

    Q      (input/output) COMPLEX array, dimension (LDQ,N)
           On entry, Q contains the eigenvectors of the partially solved
           system which has been previously updated in matrix
           multiplies with other partially solved eigensystems.
           On exit, Q contains the trailing (N-K) updated eigenvectors
           (those which were deflated) in its last N-K columns.

    LDQ    (input) INTEGER
           The leading dimension of the array Q.  LDQ >= max( 1, N ).

    D      (input/output) REAL array, dimension (N)
           On entry, D contains the eigenvalues of the two submatrices to
           be combined.  On exit, D contains the trailing (N-K) updated
           eigenvalues (those which were deflated) sorted into increasing
           order.

    RHO    (input/output) REAL
           Contains the off diagonal element associated with the rank-1
           cut which originally split the two submatrices which are now
           being recombined. RHO is modified during the computation to
           the value required by SLAED3.

    CUTPNT (input) INTEGER
           Contains the location of the last eigenvalue in the leading
           sub-matrix.  MIN(1,N) <= CUTPNT <= N.

    Z      (input) REAL array, dimension (N)
           On input this vector contains the updating vector (the last
           row of the first sub-eigenvector matrix and the first row of
           the second sub-eigenvector matrix).  The contents of Z are
           destroyed during the updating process.

    DLAMDA (output) REAL array, dimension (N)
           Contains a copy of the first K eigenvalues which will be used
           by SLAED3 to form the secular equation.

    Q2     (output) COMPLEX array, dimension (LDQ2,N)
           If ICOMPQ = 0, Q2 is not referenced.  Otherwise,
           Contains a copy of the first K eigenvectors which will be used
           by SLAED7 in a matrix multiply (SGEMM) to update the new
           eigenvectors.

    LDQ2   (input) INTEGER
           The leading dimension of the array Q2.  LDQ2 >= max( 1, N ).

    W      (output) REAL array, dimension (N)
           This will hold the first k values of the final
           deflation-altered z-vector and will be passed to SLAED3.

    INDXP  (workspace) INTEGER array, dimension (N)
           This will contain the permutation used to place deflated
           values of D at the end of the array. On output INDXP(1:K)
           points to the nondeflated D-values and INDXP(K+1:N)
           points to the deflated eigenvalues.

    INDX   (workspace) INTEGER array, dimension (N)
           This will contain the permutation used to sort the contents of
           D into ascending order.

    INDXQ  (input) INTEGER array, dimension (N)
           This contains the permutation which separately sorts the two
           sub-problems in D into ascending order.  Note that elements in
           the second half of this permutation must first have CUTPNT
           added to their values in order to be accurate.

    PERM   (output) INTEGER array, dimension (N)
           Contains the permutations (from deflation and sorting) to be
           applied to each eigenblock.

    GIVPTR (output) INTEGER
           Contains the number of Givens rotations which took place in
           this subproblem.

    GIVCOL (output) INTEGER array, dimension (2, N)
           Each pair of numbers indicates a pair of columns to take place
           in a Givens rotation.

    GIVNUM (output) REAL array, dimension (2, N)
           Each number indicates the S value to be used in the
           corresponding Givens rotation.

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    =====================================================================


       Test the input parameters.
','line_number':10049,'multiline':True]['text':' Parameter adjustments ','line_number':10172,'multiline':True]['text':' Function Body ','line_number':10190,'multiline':True]['text':'
       Need to initialize GIVPTR to O here in case of quick exit
       to prevent an unspecified code behavior (usually sigfault)
       when IWORK array on entry to *stedc is not zeroed
       (or at least some IWORK entries which used in *laed7 for GIVPTR).
','line_number':10210,'multiline':True]['text':'     Quick return if possible ','line_number':10219,'multiline':True]['text':'     Normalize z so that norm(z) = 1 ','line_number':10233,'multiline':True]['text':' L10: ','line_number':10239,'multiline':True]['text':'     Sort the eigenvalues into increasing order ','line_number':10244,'multiline':True]['text':' L20: ','line_number':10249,'multiline':True]['text':' L30: ','line_number':10255,'multiline':True]['text':' L40: ','line_number':10264,'multiline':True]['text':'     Calculate the allowable deflation tolerance ','line_number':10267,'multiline':True]['text':'
       If the rank-1 modifier is small enough, no more needs to be done
       -- except to reorganize Q so that its columns correspond with the
       elements in D.
','line_number':10274,'multiline':True]['text':' L50: ','line_number':10287,'multiline':True]['text':'
       If there are multiple eigenvalues then the problem deflates.  Here
       the number of equal eigenvalues are found.  As each equal
       eigenvalue is found, an elementary reflector is computed to rotate
       the corresponding eigensubspace so that the corresponding
       components of Z are zero in this new basis.
','line_number':10293,'multiline':True]['text':'           Deflate due to small z component. ','line_number':10307,'multiline':True]['text':' L60: ','line_number':10318,'multiline':True]['text':'        Deflate due to small z component. ','line_number':10327,'multiline':True]['text':'        Check if eigenvalues are close enough to allow deflation. ','line_number':10333,'multiline':True]['text':'
          Find sqrt(a**2+b**2) without overflow or
          destructive underflow.
','line_number':10338,'multiline':True]['text':'           Deflation is possible. ','line_number':10349,'multiline':True]['text':'           Record the appropriate Givens rotation ','line_number':10354,'multiline':True]['text':'     Record the last eigenvalue. ','line_number':10393,'multiline':True]['text':'
       Sort the eigenvalues and corresponding eigenvectors into DLAMDA
       and Q2 respectively.  The eigenvalues/vectors which were not
       deflated go into the first K slots of DLAMDA and Q2 respectively,
       while those which were deflated go into the last N - K slots.
','line_number':10402,'multiline':True]['text':' L110: ','line_number':10416,'multiline':True]['text':'
       The deflated eigenvalues and their corresponding vectors go back
       into the last N - K slots of D and Q respectively.
','line_number':10419,'multiline':True]['text':'     End of CLAED8 ','line_number':10434,'multiline':True]['text':' claed8_ ','line_number':10436,'multiline':True]['text':' Subroutine ','line_number':10438,'multiline':True]['text':' System generated locals ','line_number':10443,'multiline':True]['text':' Local variables ','line_number':10448,'multiline':True]['text':' Subroutine ','line_number':10469,'multiline':True]['text':' Subroutine ','line_number':10473,'multiline':True]['text':' Complex ','line_number':10475,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       Purpose
       =======

       CLAHQR is an auxiliary routine called by CHSEQR to update the
       eigenvalues and Schur decomposition already computed by CHSEQR, by
       dealing with the Hessenberg submatrix in rows and columns ILO to
       IHI.

       Arguments
       =========

       WANTT   (input) LOGICAL
            = .TRUE. : the full Schur form T is required;
            = .FALSE.: only eigenvalues are required.

       WANTZ   (input) LOGICAL
            = .TRUE. : the matrix of Schur vectors Z is required;
            = .FALSE.: Schur vectors are not required.

       N       (input) INTEGER
            The order of the matrix H.  N >= 0.

       ILO     (input) INTEGER
       IHI     (input) INTEGER
            It is assumed that H is already upper triangular in rows and
            columns IHI+1:N, and that H(ILO,ILO-1) = 0 (unless ILO = 1).
            CLAHQR works primarily with the Hessenberg submatrix in rows
            and columns ILO to IHI, but applies transformations to all of
            H if WANTT is .TRUE..
            1 <= ILO <= max(1,IHI); IHI <= N.

       H       (input/output) COMPLEX array, dimension (LDH,N)
            On entry, the upper Hessenberg matrix H.
            On exit, if INFO is zero and if WANTT is .TRUE., then H
            is upper triangular in rows and columns ILO:IHI.  If INFO
            is zero and if WANTT is .FALSE., then the contents of H
            are unspecified on exit.  The output state of H in case
            INF is positive is below under the description of INFO.

       LDH     (input) INTEGER
            The leading dimension of the array H. LDH >= max(1,N).

       W       (output) COMPLEX array, dimension (N)
            The computed eigenvalues ILO to IHI are stored in the
            corresponding elements of W. If WANTT is .TRUE., the
            eigenvalues are stored in the same order as on the diagonal
            of the Schur form returned in H, with W(i) = H(i,i).

       ILOZ    (input) INTEGER
       IHIZ    (input) INTEGER
            Specify the rows of Z to which transformations must be
            applied if WANTZ is .TRUE..
            1 <= ILOZ <= ILO; IHI <= IHIZ <= N.

       Z       (input/output) COMPLEX array, dimension (LDZ,N)
            If WANTZ is .TRUE., on entry Z must contain the current
            matrix Z of transformations accumulated by CHSEQR, and on
            exit Z has been updated; transformations are applied only to
            the submatrix Z(ILOZ:IHIZ,ILO:IHI).
            If WANTZ is .FALSE., Z is not referenced.

       LDZ     (input) INTEGER
            The leading dimension of the array Z. LDZ >= max(1,N).

       INFO    (output) INTEGER
             =   0: successful exit
            .GT. 0: if INFO = i, CLAHQR failed to compute all the
                    eigenvalues ILO to IHI in a total of 30 iterations
                    per eigenvalue; elements i+1:ihi of W contain
                    those eigenvalues which have been successfully
                    computed.

                    If INFO .GT. 0 and WANTT is .FALSE., then on exit,
                    the remaining unconverged eigenvalues are the
                    eigenvalues of the upper Hessenberg matrix
                    rows and columns ILO thorugh INFO of the final,
                    output value of H.

                    If INFO .GT. 0 and WANTT is .TRUE., then on exit
            (*)       (initial value of H)*U  = U*(final value of H)
                    where U is an orthognal matrix.    The final
                    value of H is upper Hessenberg and triangular in
                    rows and columns INFO+1 through IHI.

                    If INFO .GT. 0 and WANTZ is .TRUE., then on exit
                        (final value of Z)  = (initial value of Z)*U
                    where U is the orthogonal matrix in (*)
                    (regardless of the value of WANTT.)

       Further Details
       ===============

       02-96 Based on modifications by
       David Day, Sandia National Laboratory, USA

       12-04 Further modifications by
       Ralph Byers, University of Kansas, USA
       This is a modified version of CLAHQR from LAPACK version 3.0.
       It is (1) more robust against overflow and underflow and
       (2) adopts the more conservative Ahues & Tisseur stopping
       criterion (LAWN 122, 1997).

       =========================================================
','line_number':10480,'multiline':True]['text':' Parameter adjustments ','line_number':10592,'multiline':True]['text':' Function Body ','line_number':10601,'multiline':True]['text':'     Quick return if possible ','line_number':10604,'multiline':True]['text':'     ==== clear out the trash ==== ','line_number':10616,'multiline':True]['text':' L10: ','line_number':10623,'multiline':True]['text':'     ==== ensure that subdiagonal entries are real ==== ','line_number':10629,'multiline':True]['text':'
             ==== The following redundant normalization
             .    avoids problems with both gradual and
             .    sudden underflow in ABS(H(I,I-1)) ====
','line_number':10640,'multiline':True]['text':' Computing MIN ','line_number':10660,'multiline':True]['text':' L20: ','line_number':10671,'multiline':True]['text':'     Set machine-dependent constants for the stopping criterion. ','line_number':10677,'multiline':True]['text':'
       I1 and I2 are the indices of the first row and last column of H
       to which transformations must be applied. If eigenvalues only are
       being computed, I1 and I2 are set inside the main loop.
','line_number':10685,'multiline':True]['text':'
       The main loop begins here. I is the loop index and decreases from
       IHI to ILO in steps of 1. Each iteration of the loop works
       with the active submatrix in rows and columns L to I.
       Eigenvalues I+1 to IHI have already converged. Either L = ILO, or
       H(L,L-1) is negligible so that the matrix splits.
','line_number':10696,'multiline':True]['text':'
       Perform QR iterations on rows and columns ILO to I until a
       submatrix of order 1 splits off at the bottom because a
       subdiagonal element has become negligible.
','line_number':10710,'multiline':True]['text':'        Look for a single small subdiagonal element. ','line_number':10719,'multiline':True]['text':'
             ==== The following is a conservative small subdiagonal
             .    deflation criterion due to Ahues & Tisseur (LAWN 122,
             .    1997). It has better mathematical foundation and
             .    improves accuracy in some examples.  ====
','line_number':10744,'multiline':True]['text':' Computing MAX ','line_number':10752,'multiline':True]['text':' Computing MIN ','line_number':10760,'multiline':True]['text':' Computing MAX ','line_number':10773,'multiline':True]['text':' Computing MIN ','line_number':10784,'multiline':True]['text':' Computing MAX ','line_number':10791,'multiline':True]['text':' L40: ','line_number':10797,'multiline':True]['text':'           H(L,L-1) is negligible ','line_number':10803,'multiline':True]['text':'        Exit from loop if a submatrix of order 1 has split off. ','line_number':10809,'multiline':True]['text':'
          Now the active submatrix is in rows and columns L to I. If
          eigenvalues only are being computed, only the active submatrix
          need be transformed.
','line_number':10815,'multiline':True]['text':'           Exceptional shift. ','line_number':10828,'multiline':True]['text':'           Exceptional shift. ','line_number':10837,'multiline':True]['text':'           Wilkinson's shift. ','line_number':10846,'multiline':True]['text':' Computing MAX ','line_number':10863,'multiline':True]['text':'        Look for two consecutive small subdiagonal elements. ','line_number':10892,'multiline':True]['text':'
             Determine the effect of starting the single-shift QR
             iteration at row M, and see if this would make H(M,M-1)
             negligible.
','line_number':10897,'multiline':True]['text':' L60: ','line_number':10927,'multiline':True]['text':'        Single-shift QR step ','line_number':10946,'multiline':True]['text':'
             The first iteration of this loop determines a reflection G
             from the vector V and applies it from left and right to H,
             thus creating a nonzero bulge below the subdiagonal.

             Each subsequent iteration determines a reflection G to
             restore the Hessenberg form in the (K-1)th column, and thus
             chases the bulge one step toward the bottom of the active
             submatrix.

             V(2) is always real before the call to CLARFG, and hence
             after the call T2 ( = T1*V(2) ) is also real.
','line_number':10951,'multiline':True]['text':'
             Apply G from the left to transform the rows of the matrix
             in columns K to I2.
','line_number':10980,'multiline':True]['text':' L80: ','line_number':11005,'multiline':True]['text':'
             Apply G from the right to transform the columns of the
             matrix in rows I1 to min(K+2,I).

   Computing MIN
','line_number':11008,'multiline':True]['text':' L90: ','line_number':11035,'multiline':True]['text':'              Accumulate transformations in the matrix Z ','line_number':11040,'multiline':True]['text':' L100: ','line_number':11064,'multiline':True]['text':'
                If the QR step was started at row M > L because two
                consecutive small subdiagonals were found, then extra
                scaling must be performed to ensure that H(M,M-1) remains
                real.
','line_number':11070,'multiline':True]['text':' L110: ','line_number':11113,'multiline':True]['text':' L120: ','line_number':11116,'multiline':True]['text':'        Ensure that H(I,I-1) is real. ','line_number':11119,'multiline':True]['text':' L130: ','line_number':11141,'multiline':True]['text':'     Failure to converge in remaining number of iterations ','line_number':11144,'multiline':True]['text':'     H(I,I-1) is negligible: one eigenvalue has converged. ','line_number':11151,'multiline':True]['text':'     return to start of the main loop with new value of I. ','line_number':11157,'multiline':True]['text':'     End of CLAHQR ','line_number':11165,'multiline':True]['text':' clahqr_ ','line_number':11167,'multiline':True]['text':' Subroutine ','line_number':11169,'multiline':True]['text':' System generated locals ','line_number':11173,'multiline':True]['text':' Local variables ','line_number':11178,'multiline':True]['text':' Subroutine ','line_number':11181,'multiline':True]['text':'  -- LAPACK auxiliary routine (version 3.2.1)                        -- ','line_number':11198,'multiline':True]['text':'  -- LAPACK is a software package provided by Univ. of Tennessee,    --*  -- April 2009
                                 -- ','line_number':11199,'multiline':True]['text':'
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--


    Purpose
    =======

    CLAHR2 reduces the first NB columns of A complex general n-BY-(n-k+1)
    matrix A so that elements below the k-th subdiagonal are zero. The
    reduction is performed by an unitary similarity transformation
    Q' * A * Q. The routine returns the matrices V and T which determine
    Q as a block reflector I - V*T*V', and also the matrix Y = A * V * T.

    This is an auxiliary routine called by CGEHRD.

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix A.

    K       (input) INTEGER
            The offset for the reduction. Elements below the k-th
            subdiagonal in the first NB columns are reduced to zero.
            K < N.

    NB      (input) INTEGER
            The number of columns to be reduced.

    A       (input/output) COMPLEX array, dimension (LDA,N-K+1)
            On entry, the n-by-(n-k+1) general matrix A.
            On exit, the elements on and above the k-th subdiagonal in
            the first NB columns are overwritten with the corresponding
            elements of the reduced matrix; the elements below the k-th
            subdiagonal, with the array TAU, represent the matrix Q as a
            product of elementary reflectors. The other columns of A are
            unchanged. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    TAU     (output) COMPLEX array, dimension (NB)
            The scalar factors of the elementary reflectors. See Further
            Details.

    T       (output) COMPLEX array, dimension (LDT,NB)
            The upper triangular matrix T.

    LDT     (input) INTEGER
            The leading dimension of the array T.  LDT >= NB.

    Y       (output) COMPLEX array, dimension (LDY,NB)
            The n-by-nb matrix Y.

    LDY     (input) INTEGER
            The leading dimension of the array Y. LDY >= N.

    Further Details
    ===============

    The matrix Q is represented as a product of nb elementary reflectors

       Q = H(1) H(2) . . . H(nb).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i+k-1) = 0, v(i+k) = 1; v(i+k+1:n) is stored on exit in
    A(i+k+1:n,i), and tau in TAU(i).

    The elements of the vectors v together form the (n-k+1)-by-nb matrix
    V which is needed, with T and Y, to apply the transformation to the
    unreduced part of the matrix, using an update of the form:
    A := (I - V*T*V') * (A - Y*V').

    The contents of A on exit are illustrated by the following example
    with n = 7, k = 3 and nb = 2:

       ( a   a   a   a   a )
       ( a   a   a   a   a )
       ( a   a   a   a   a )
       ( h   h   a   a   a )
       ( v1  h   a   a   a )
       ( v1  v2  a   a   a )
       ( v1  v2  a   a   a )

    where a denotes an element of the original matrix A, h denotes a
    modified element of the upper Hessenberg matrix H, and vi denotes an
    element of the vector defining H(i).

    This subroutine is a slight modification of LAPACK-3.0's DLAHRD
    incorporating improvements proposed by Quintana-Orti and Van de
    Gejin. Note that the entries of A(1:K,2:NB) differ from those
    returned by the original LAPACK-3.0's DLAHRD routine. (This
    subroutine is not backward compatible with LAPACK-3.0's DLAHRD.)

    References
    ==========

    Gregorio Quintana-Orti and Robert van de Geijn, "Improving the
    performance of reduction to Hessenberg form," ACM Transactions on
    Mathematical Software, 32(2):180-194, June 2006.

    =====================================================================


       Quick return if possible
','line_number':11201,'multiline':True]['text':' Parameter adjustments ','line_number':11312,'multiline':True]['text':' Function Body ','line_number':11324,'multiline':True]['text':'
             Update A(K+1:N,I)

             Update I-th column of A - Y * V'
','line_number':11333,'multiline':True]['text':'
             Apply I - V * T' * V' to this column (call it b) from the
             left, using the last column of T as workspace

             Let  V = ( V1 )   and   b = ( b1 )   (first I-1 rows)
                      ( V2 )             ( b2 )

             where V1 is unit lower triangular

             w := V1' * b1
','line_number':11350,'multiline':True]['text':'           w := w + V2'*b2 ','line_number':11369,'multiline':True]['text':'           w := T'*w ','line_number':11377,'multiline':True]['text':'           b2 := b2 - V2*w ','line_number':11383,'multiline':True]['text':'           b1 := b1 - V1*w ','line_number':11392,'multiline':True]['text':'
          Generate the elementary reflector H(I) to annihilate
          A(K+I+1:N,I)
','line_number':11406,'multiline':True]['text':' Computing MIN ','line_number':11412,'multiline':True]['text':'        Compute  Y(K+1:N,I) ','line_number':11421,'multiline':True]['text':'        Compute T(1:I,I) ','line_number':11442,'multiline':True]['text':' L10: ','line_number':11456,'multiline':True]['text':'     Compute Y(1:K,1:NB) ','line_number':11461,'multiline':True]['text':'     End of CLAHR2 ','line_number':11477,'multiline':True]['text':' clahr2_ ','line_number':11479,'multiline':True]['text':' Subroutine ','line_number':11481,'multiline':True]['text':' System generated locals ','line_number':11488,'multiline':True]['text':' Local variables ','line_number':11495,'multiline':True]['text':' Subroutine ','line_number':11503,'multiline':True]['text':' Subroutine ','line_number':11508,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLALS0 applies back the multiplying factors of either the left or the
    right singular vector matrix of a diagonal matrix appended by a row
    to the right hand side matrix B in solving the least squares problem
    using the divide-and-conquer SVD approach.

    For the left singular vector matrix, three types of orthogonal
    matrices are involved:

    (1L) Givens rotations: the number of such rotations is GIVPTR; the
         pairs of columns/rows they were applied to are stored in GIVCOL;
         and the C- and S-values of these rotations are stored in GIVNUM.

    (2L) Permutation. The (NL+1)-st row of B is to be moved to the first
         row, and for J=2:N, PERM(J)-th row of B is to be moved to the
         J-th row.

    (3L) The left singular vector matrix of the remaining matrix.

    For the right singular vector matrix, four types of orthogonal
    matrices are involved:

    (1R) The right singular vector matrix of the remaining matrix.

    (2R) If SQRE = 1, one extra Givens rotation to generate the right
         null space.

    (3R) The inverse transformation of (2L).

    (4R) The inverse transformation of (1L).

    Arguments
    =========

    ICOMPQ (input) INTEGER
           Specifies whether singular vectors are to be computed in
           factored form:
           = 0: Left singular vector matrix.
           = 1: Right singular vector matrix.

    NL     (input) INTEGER
           The row dimension of the upper block. NL >= 1.

    NR     (input) INTEGER
           The row dimension of the lower block. NR >= 1.

    SQRE   (input) INTEGER
           = 0: the lower block is an NR-by-NR square matrix.
           = 1: the lower block is an NR-by-(NR+1) rectangular matrix.

           The bidiagonal matrix has row dimension N = NL + NR + 1,
           and column dimension M = N + SQRE.

    NRHS   (input) INTEGER
           The number of columns of B and BX. NRHS must be at least 1.

    B      (input/output) COMPLEX array, dimension ( LDB, NRHS )
           On input, B contains the right hand sides of the least
           squares problem in rows 1 through M. On output, B contains
           the solution X in rows 1 through N.

    LDB    (input) INTEGER
           The leading dimension of B. LDB must be at least
           max(1,MAX( M, N ) ).

    BX     (workspace) COMPLEX array, dimension ( LDBX, NRHS )

    LDBX   (input) INTEGER
           The leading dimension of BX.

    PERM   (input) INTEGER array, dimension ( N )
           The permutations (from deflation and sorting) applied
           to the two blocks.

    GIVPTR (input) INTEGER
           The number of Givens rotations which took place in this
           subproblem.

    GIVCOL (input) INTEGER array, dimension ( LDGCOL, 2 )
           Each pair of numbers indicates a pair of rows/columns
           involved in a Givens rotation.

    LDGCOL (input) INTEGER
           The leading dimension of GIVCOL, must be at least N.

    GIVNUM (input) REAL array, dimension ( LDGNUM, 2 )
           Each number indicates the C or S value used in the
           corresponding Givens rotation.

    LDGNUM (input) INTEGER
           The leading dimension of arrays DIFR, POLES and
           GIVNUM, must be at least K.

    POLES  (input) REAL array, dimension ( LDGNUM, 2 )
           On entry, POLES(1:K, 1) contains the new singular
           values obtained from solving the secular equation, and
           POLES(1:K, 2) is an array containing the poles in the secular
           equation.

    DIFL   (input) REAL array, dimension ( K ).
           On entry, DIFL(I) is the distance between I-th updated
           (undeflated) singular value and the I-th (undeflated) old
           singular value.

    DIFR   (input) REAL array, dimension ( LDGNUM, 2 ).
           On entry, DIFR(I, 1) contains the distances between I-th
           updated (undeflated) singular value and the I+1-th
           (undeflated) old singular value. And DIFR(I, 2) is the
           normalizing factor for the I-th right singular vector.

    Z      (input) REAL array, dimension ( K )
           Contain the components of the deflation-adjusted updating row
           vector.

    K      (input) INTEGER
           Contains the dimension of the non-deflated matrix,
           This is the order of the related secular equation. 1 <= K <=N.

    C      (input) REAL
           C contains garbage if SQRE =0 and the C-value of a Givens
           rotation related to the right null space if SQRE = 1.

    S      (input) REAL
           S contains garbage if SQRE =0 and the S-value of a Givens
           rotation related to the right null space if SQRE = 1.

    RWORK  (workspace) REAL array, dimension
           ( K*(1+NRHS) + 2*NRHS )

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input parameters.
','line_number':11515,'multiline':True]['text':' Parameter adjustments ','line_number':11671,'multiline':True]['text':' Function Body ','line_number':11695,'multiline':True]['text':'
          Apply back orthogonal transformations from the left.

          Step (1L): apply back the Givens rotations performed.
','line_number':11736,'multiline':True]['text':' L10: ','line_number':11747,'multiline':True]['text':'        Step (2L): permute rows of B. ','line_number':11750,'multiline':True]['text':' L20: ','line_number':11757,'multiline':True]['text':'
          Step (3L): apply the inverse of the left singular vector
          matrix to BX.
','line_number':11760,'multiline':True]['text':' L30: ','line_number':11797,'multiline':True]['text':' L40: ','line_number':11810,'multiline':True]['text':'
                Since B and BX are complex, the following call to SGEMV
                is performed in two steps (real and imaginary parts).

                CALL SGEMV( 'T', K, NRHS, ONE, BX, LDBX, WORK, 1, ZERO,
      $                     B( J, 1 ), LDB )
','line_number':11815,'multiline':True]['text':' L50: ','line_number':11831,'multiline':True]['text':' L60: ','line_number':11833,'multiline':True]['text':' L70: ','line_number':11844,'multiline':True]['text':' L80: ','line_number':11846,'multiline':True]['text':' L90: ','line_number':11858,'multiline':True]['text':' L100: ','line_number':11862,'multiline':True]['text':'        Move the deflated rows of BX to B also. ','line_number':11866,'multiline':True]['text':'
          Apply back the right orthogonal transformations.

          Step (1R): apply back the new right singular vector matrix
          to B.
','line_number':11875,'multiline':True]['text':' L110: ','line_number':11904,'multiline':True]['text':' L120: ','line_number':11916,'multiline':True]['text':'
                Since B and BX are complex, the following call to SGEMV
                is performed in two steps (real and imaginary parts).

                CALL SGEMV( 'T', K, NRHS, ONE, B, LDB, WORK, 1, ZERO,
      $                     BX( J, 1 ), LDBX )
','line_number':11919,'multiline':True]['text':' L130: ','line_number':11935,'multiline':True]['text':' L140: ','line_number':11937,'multiline':True]['text':' L150: ','line_number':11948,'multiline':True]['text':' L160: ','line_number':11950,'multiline':True]['text':' L170: ','line_number':11962,'multiline':True]['text':' L180: ','line_number':11964,'multiline':True]['text':'
          Step (2R): if SQRE = 1, apply back the rotation that is
          related to the right null space of the subproblem.
','line_number':11968,'multiline':True]['text':'        Step (3R): permute rows of B. ','line_number':11984,'multiline':True]['text':' L190: ','line_number':11994,'multiline':True]['text':'        Step (4R): apply back the Givens rotations performed. ','line_number':11997,'multiline':True]['text':' L200: ','line_number':12004,'multiline':True]['text':'     End of CLALS0 ','line_number':12010,'multiline':True]['text':' clals0_ ','line_number':12012,'multiline':True]['text':' Subroutine ','line_number':12014,'multiline':True]['text':' System generated locals ','line_number':12021,'multiline':True]['text':' Local variables ','line_number':12029,'multiline':True]['text':' Subroutine ','line_number':12033,'multiline':True]['text':' Subroutine ','line_number':12037,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLALSA is an itermediate step in solving the least squares problem
    by computing the SVD of the coefficient matrix in compact form (The
    singular vectors are computed as products of simple orthorgonal
    matrices.).

    If ICOMPQ = 0, CLALSA applies the inverse of the left singular vector
    matrix of an upper bidiagonal matrix to the right hand side; and if
    ICOMPQ = 1, CLALSA applies the right singular vector matrix to the
    right hand side. The singular vector matrices were generated in
    compact form by CLALSA.

    Arguments
    =========

    ICOMPQ (input) INTEGER
           Specifies whether the left or the right singular vector
           matrix is involved.
           = 0: Left singular vector matrix
           = 1: Right singular vector matrix

    SMLSIZ (input) INTEGER
           The maximum size of the subproblems at the bottom of the
           computation tree.

    N      (input) INTEGER
           The row and column dimensions of the upper bidiagonal matrix.

    NRHS   (input) INTEGER
           The number of columns of B and BX. NRHS must be at least 1.

    B      (input/output) COMPLEX array, dimension ( LDB, NRHS )
           On input, B contains the right hand sides of the least
           squares problem in rows 1 through M.
           On output, B contains the solution X in rows 1 through N.

    LDB    (input) INTEGER
           The leading dimension of B in the calling subprogram.
           LDB must be at least max(1,MAX( M, N ) ).

    BX     (output) COMPLEX array, dimension ( LDBX, NRHS )
           On exit, the result of applying the left or right singular
           vector matrix to B.

    LDBX   (input) INTEGER
           The leading dimension of BX.

    U      (input) REAL array, dimension ( LDU, SMLSIZ ).
           On entry, U contains the left singular vector matrices of all
           subproblems at the bottom level.

    LDU    (input) INTEGER, LDU = > N.
           The leading dimension of arrays U, VT, DIFL, DIFR,
           POLES, GIVNUM, and Z.

    VT     (input) REAL array, dimension ( LDU, SMLSIZ+1 ).
           On entry, VT' contains the right singular vector matrices of
           all subproblems at the bottom level.

    K      (input) INTEGER array, dimension ( N ).

    DIFL   (input) REAL array, dimension ( LDU, NLVL ).
           where NLVL = INT(log_2 (N/(SMLSIZ+1))) + 1.

    DIFR   (input) REAL array, dimension ( LDU, 2 * NLVL ).
           On entry, DIFL(*, I) and DIFR(*, 2 * I -1) record
           distances between singular values on the I-th level and
           singular values on the (I -1)-th level, and DIFR(*, 2 * I)
           record the normalizing factors of the right singular vectors
           matrices of subproblems on I-th level.

    Z      (input) REAL array, dimension ( LDU, NLVL ).
           On entry, Z(1, I) contains the components of the deflation-
           adjusted updating row vector for subproblems on the I-th
           level.

    POLES  (input) REAL array, dimension ( LDU, 2 * NLVL ).
           On entry, POLES(*, 2 * I -1: 2 * I) contains the new and old
           singular values involved in the secular equations on the I-th
           level.

    GIVPTR (input) INTEGER array, dimension ( N ).
           On entry, GIVPTR( I ) records the number of Givens
           rotations performed on the I-th problem on the computation
           tree.

    GIVCOL (input) INTEGER array, dimension ( LDGCOL, 2 * NLVL ).
           On entry, for each I, GIVCOL(*, 2 * I - 1: 2 * I) records the
           locations of Givens rotations performed on the I-th level on
           the computation tree.

    LDGCOL (input) INTEGER, LDGCOL = > N.
           The leading dimension of arrays GIVCOL and PERM.

    PERM   (input) INTEGER array, dimension ( LDGCOL, NLVL ).
           On entry, PERM(*, I) records permutations done on the I-th
           level of the computation tree.

    GIVNUM (input) REAL array, dimension ( LDU, 2 * NLVL ).
           On entry, GIVNUM(*, 2 *I -1 : 2 * I) records the C- and S-
           values of Givens rotations performed on the I-th level on the
           computation tree.

    C      (input) REAL array, dimension ( N ).
           On entry, if the I-th subproblem is not square,
           C( I ) contains the C-value of a Givens rotation related to
           the right null space of the I-th subproblem.

    S      (input) REAL array, dimension ( N ).
           On entry, if the I-th subproblem is not square,
           S( I ) contains the S-value of a Givens rotation related to
           the right null space of the I-th subproblem.

    RWORK  (workspace) REAL array, dimension at least
           MAX( (SMLSZ+1)*NRHS*3, N*(1+NRHS) + 2*NRHS ).

    IWORK  (workspace) INTEGER array.
           The dimension must be at least 3 * N

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input parameters.
','line_number':12047,'multiline':True]['text':' Parameter adjustments ','line_number':12193,'multiline':True]['text':' Function Body ','line_number':12234,'multiline':True]['text':'     Book-keeping and  setting up the computation tree. ','line_number':12260,'multiline':True]['text':'
       The following code applies back the left singular vector factors.
       For applying back the right singular vector factors, go to 170.
','line_number':12269,'multiline':True]['text':'
       The nodes on the bottom level of the tree were solved
       by SLASDQ. The corresponding left and right singular vector
       matrices are in explicit form. First apply back the left
       singular vector matrices.
','line_number':12278,'multiline':True]['text':'
          IC : center row of each node
          NL : number of rows of left  subproblem
          NR : number of rows of right subproblem
          NLF: starting row of the left   subproblem
          NRF: starting row of the right  subproblem
','line_number':12289,'multiline':True]['text':'
          Since B and BX are complex, the following call to SGEMM
          is performed in two steps (real and imaginary parts).

          CALL SGEMM( 'T', 'N', NL, NRHS, NL, ONE, U( NLF, 1 ), LDU,
       $               B( NLF, 1 ), LDB, ZERO, BX( NLF, 1 ), LDBX )
','line_number':12304,'multiline':True]['text':' L10: ','line_number':12320,'multiline':True]['text':' L20: ','line_number':12322,'multiline':True]['text':' L30: ','line_number':12333,'multiline':True]['text':' L40: ','line_number':12335,'multiline':True]['text':' L50: ','line_number':12353,'multiline':True]['text':' L60: ','line_number':12355,'multiline':True]['text':'
          Since B and BX are complex, the following call to SGEMM
          is performed in two steps (real and imaginary parts).

          CALL SGEMM( 'T', 'N', NR, NRHS, NR, ONE, U( NRF, 1 ), LDU,
      $               B( NRF, 1 ), LDB, ZERO, BX( NRF, 1 ), LDBX )
','line_number':12358,'multiline':True]['text':' L70: ','line_number':12374,'multiline':True]['text':' L80: ','line_number':12376,'multiline':True]['text':' L90: ','line_number':12387,'multiline':True]['text':' L100: ','line_number':12389,'multiline':True]['text':' L110: ','line_number':12407,'multiline':True]['text':' L120: ','line_number':12409,'multiline':True]['text':' L130: ','line_number':12412,'multiline':True]['text':'
       Next copy the rows of B that correspond to unchanged rows
       in the bidiagonal matrix to BX.
','line_number':12415,'multiline':True]['text':' L140: ','line_number':12424,'multiline':True]['text':'
       Finally go through the left singular vector matrices of all
       the other subproblems bottom-up on the tree.
','line_number':12427,'multiline':True]['text':'
          find the first node LF and last node LL on
          the current level LVL
','line_number':12438,'multiline':True]['text':' L150: ','line_number':12467,'multiline':True]['text':' L160: ','line_number':12469,'multiline':True]['text':'     ICOMPQ = 1: applying back the right singular vector factors. ','line_number':12473,'multiline':True]['text':'
       First now go through the right singular vector matrices of all
       the tree nodes top-down.
','line_number':12477,'multiline':True]['text':'
          Find the first node LF and last node LL on
          the current level LVL.
','line_number':12487,'multiline':True]['text':' L180: ','line_number':12521,'multiline':True]['text':' L190: ','line_number':12523,'multiline':True]['text':'
       The nodes on the bottom level of the tree were solved
       by SLASDQ. The corresponding right singular vector
       matrices are in explicit form. Apply them back.
','line_number':12526,'multiline':True]['text':'
          Since B and BX are complex, the following call to SGEMM is
          performed in two steps (real and imaginary parts).

          CALL SGEMM( 'T', 'N', NLP1, NRHS, NLP1, ONE, VT( NLF, 1 ), LDU,
      $               B( NLF, 1 ), LDB, ZERO, BX( NLF, 1 ), LDBX )
','line_number':12548,'multiline':True]['text':' L200: ','line_number':12564,'multiline':True]['text':' L210: ','line_number':12566,'multiline':True]['text':' L220: ','line_number':12578,'multiline':True]['text':' L230: ','line_number':12580,'multiline':True]['text':' L240: ','line_number':12598,'multiline':True]['text':' L250: ','line_number':12600,'multiline':True]['text':'
          Since B and BX are complex, the following call to SGEMM is
          performed in two steps (real and imaginary parts).

          CALL SGEMM( 'T', 'N', NRP1, NRHS, NRP1, ONE, VT( NRF, 1 ), LDU,
      $               B( NRF, 1 ), LDB, ZERO, BX( NRF, 1 ), LDBX )
','line_number':12603,'multiline':True]['text':' L260: ','line_number':12619,'multiline':True]['text':' L270: ','line_number':12621,'multiline':True]['text':' L280: ','line_number':12633,'multiline':True]['text':' L290: ','line_number':12635,'multiline':True]['text':' L300: ','line_number':12653,'multiline':True]['text':' L310: ','line_number':12655,'multiline':True]['text':' L320: ','line_number':12658,'multiline':True]['text':'     End of CLALSA ','line_number':12665,'multiline':True]['text':' clalsa_ ','line_number':12667,'multiline':True]['text':' Subroutine ','line_number':12669,'multiline':True]['text':' System generated locals ','line_number':12674,'multiline':True]['text':' Local variables ','line_number':12679,'multiline':True]['text':' Subroutine ','line_number':12694,'multiline':True]['text':' Subroutine ','line_number':12698,'multiline':True]['text':' Subroutine ','line_number':12701,'multiline':True]['text':' Subroutine ','line_number':12704,'multiline':True]['text':' Subroutine ','line_number':12712,'multiline':True]['text':' Subroutine ','line_number':12724,'multiline':True]['text':' Subroutine ','line_number':12733,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    CLALSD uses the singular value decomposition of A to solve the least
    squares problem of finding X to minimize the Euclidean norm of each
    column of A*X-B, where A is N-by-N upper bidiagonal, and X and B
    are N-by-NRHS. The solution X overwrites B.

    The singular values of A smaller than RCOND times the largest
    singular value are treated as zero in solving the least squares
    problem; in this case a minimum norm solution is returned.
    The actual singular values are returned in D in ascending order.

    This code makes very mild assumptions about floating point
    arithmetic. It will work on machines with a guard digit in
    add/subtract, or on those binary machines without guard digits
    which subtract like the Cray XMP, Cray YMP, Cray C 90, or Cray 2.
    It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    UPLO   (input) CHARACTER*1
           = 'U': D and E define an upper bidiagonal matrix.
           = 'L': D and E define a  lower bidiagonal matrix.

    SMLSIZ (input) INTEGER
           The maximum size of the subproblems at the bottom of the
           computation tree.

    N      (input) INTEGER
           The dimension of the  bidiagonal matrix.  N >= 0.

    NRHS   (input) INTEGER
           The number of columns of B. NRHS must be at least 1.

    D      (input/output) REAL array, dimension (N)
           On entry D contains the main diagonal of the bidiagonal
           matrix. On exit, if INFO = 0, D contains its singular values.

    E      (input/output) REAL array, dimension (N-1)
           Contains the super-diagonal entries of the bidiagonal matrix.
           On exit, E has been destroyed.

    B      (input/output) COMPLEX array, dimension (LDB,NRHS)
           On input, B contains the right hand sides of the least
           squares problem. On output, B contains the solution X.

    LDB    (input) INTEGER
           The leading dimension of B in the calling subprogram.
           LDB must be at least max(1,N).

    RCOND  (input) REAL
           The singular values of A less than or equal to RCOND times
           the largest singular value are treated as zero in solving
           the least squares problem. If RCOND is negative,
           machine precision is used instead.
           For example, if diag(S)*X=B were the least squares problem,
           where diag(S) is a diagonal matrix of singular values, the
           solution would be X(i) = B(i) / S(i) if S(i) is greater than
           RCOND*max(S), and X(i) = 0 if S(i) is less than or equal to
           RCOND*max(S).

    RANK   (output) INTEGER
           The number of singular values of A greater than RCOND times
           the largest singular value.

    WORK   (workspace) COMPLEX array, dimension (N * NRHS).

    RWORK  (workspace) REAL array, dimension at least
           (9*N + 2*N*SMLSIZ + 8*N*NLVL + 3*SMLSIZ*NRHS +
           MAX( (SMLSIZ+1)**2, N*(1+NRHS) + 2*NRHS ),
           where
           NLVL = MAX( 0, INT( LOG_2( MIN( M,N )/(SMLSIZ+1) ) ) + 1 )

    IWORK  (workspace) INTEGER array, dimension (3*N*NLVL + 11*N).

    INFO   (output) INTEGER
           = 0:  successful exit.
           < 0:  if INFO = -i, the i-th argument had an illegal value.
           > 0:  The algorithm failed to compute a singular value while
                 working on the submatrix lying in rows and columns
                 INFO/(N+1) through MOD(INFO,N+1).

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input parameters.
','line_number':12737,'multiline':True]['text':' Parameter adjustments ','line_number':12843,'multiline':True]['text':' Function Body ','line_number':12853,'multiline':True]['text':'     Set up the tolerance. ','line_number':12871,'multiline':True]['text':'     Quick return if possible. ','line_number':12881,'multiline':True]['text':'     Rotate the matrix if it is lower bidiagonal. ','line_number':12897,'multiline':True]['text':' L10: ','line_number':12913,'multiline':True]['text':' L20: ','line_number':12924,'multiline':True]['text':' L30: ','line_number':12926,'multiline':True]['text':'     Scale. ','line_number':12931,'multiline':True]['text':'
       If N is smaller than the minimum divide size SMLSIZ, then solve
       the problem with another solver.
','line_number':12944,'multiline':True]['text':'
          In the real version, B is passed to SLASDQ and multiplied
          internally by Q'. Here B is complex and that product is
          computed below in two steps (real and imaginary parts).
','line_number':12964,'multiline':True]['text':' L40: ','line_number':12978,'multiline':True]['text':' L50: ','line_number':12980,'multiline':True]['text':' L60: ','line_number':12991,'multiline':True]['text':' L70: ','line_number':12993,'multiline':True]['text':' L80: ','line_number':13010,'multiline':True]['text':' L90: ','line_number':13012,'multiline':True]['text':' L100: ','line_number':13026,'multiline':True]['text':'
          Since B is complex, the following call to SGEMM is performed
          in two steps (real and imaginary parts). That is for V * B
          (in the real version of the code V' is stored in WORK).

          CALL SGEMM( 'T', 'N', N, NRHS, N, ONE, WORK, N, B, LDB, ZERO,
      $               WORK( NWORK ), N )
','line_number':13029,'multiline':True]['text':' L110: ','line_number':13046,'multiline':True]['text':' L120: ','line_number':13048,'multiline':True]['text':' L130: ','line_number':13059,'multiline':True]['text':' L140: ','line_number':13061,'multiline':True]['text':' L150: ','line_number':13078,'multiline':True]['text':' L160: ','line_number':13080,'multiline':True]['text':'        Unscale. ','line_number':13083,'multiline':True]['text':'     Book-keeping and setting up some constants. ','line_number':13094,'multiline':True]['text':' L170: ','line_number':13134,'multiline':True]['text':'
             Subproblem found. First determine its size and then
             apply divide and conquer on it.
','line_number':13143,'multiline':True]['text':'              A subproblem with E(I) small for I < NM1. ','line_number':13150,'multiline':True]['text':'              A subproblem with E(NM1) not too small but I = NM1. ','line_number':13156,'multiline':True]['text':'
                A subproblem with E(NM1) small. This implies an
                1-by-1 subproblem at D(N), which is not solved
                explicitly.
','line_number':13162,'multiline':True]['text':'
                This is a 1-by-1 subproblem and is not solved
                explicitly.
','line_number':13178,'multiline':True]['text':'              This is a small subproblem and is solved by SLASDQ. ','line_number':13186,'multiline':True]['text':'
                In the real version, B is passed to SLASDQ and multiplied
                internally by Q'. Here B is complex and that product is
                computed below in two steps (real and imaginary parts).
','line_number':13200,'multiline':True]['text':' L180: ','line_number':13214,'multiline':True]['text':' L190: ','line_number':13216,'multiline':True]['text':' L200: ','line_number':13228,'multiline':True]['text':' L210: ','line_number':13230,'multiline':True]['text':' L220: ','line_number':13248,'multiline':True]['text':' L230: ','line_number':13250,'multiline':True]['text':'              A large problem. Solve it using divide and conquer. ','line_number':13257,'multiline':True]['text':' L240: ','line_number':13283,'multiline':True]['text':'     Apply the singular values and treat the tiny ones as zero. ','line_number':13286,'multiline':True]['text':'
          Some of the elements in D can be negative because 1-by-1
          subproblems were not solved explicitly.
','line_number':13293,'multiline':True]['text':' L250: ','line_number':13306,'multiline':True]['text':'     Now apply back the right singular vectors. ','line_number':13309,'multiline':True]['text':'
             Since B and BX are complex, the following call to SGEMM
             is performed in two steps (real and imaginary parts).

             CALL SGEMM( 'T', 'N', NSIZE, NRHS, NSIZE, ONE,
      $                  RWORK( VT+ST1 ), N, RWORK( BXST ), N, ZERO,
      $                  B( ST, 1 ), LDB )
','line_number':13322,'multiline':True]['text':' L260: ','line_number':13341,'multiline':True]['text':' L270: ','line_number':13343,'multiline':True]['text':' L280: ','line_number':13356,'multiline':True]['text':' L290: ','line_number':13358,'multiline':True]['text':' L300: ','line_number':13375,'multiline':True]['text':' L310: ','line_number':13377,'multiline':True]['text':' L320: ','line_number':13391,'multiline':True]['text':'     Unscale and sort the singular values. ','line_number':13394,'multiline':True]['text':'     End of CLALSD ','line_number':13403,'multiline':True]['text':' clalsd_ ','line_number':13405,'multiline':True]['text':' System generated locals ','line_number':13410,'multiline':True]['text':' Local variables ','line_number':13414,'multiline':True]['text':' Subroutine ','line_number':13419,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLANGE  returns the value of the one norm,  or the Frobenius norm, or
    the  infinity norm,  or the  element of  largest absolute value  of a
    singlecomplex matrix A.

    Description
    ===========

    CLANGE returns the value

       CLANGE = ( max(abs(A(i,j))), NORM = 'M' or 'm'
                (
                ( norm1(A),         NORM = '1', 'O' or 'o'
                (
                ( normI(A),         NORM = 'I' or 'i'
                (
                ( normF(A),         NORM = 'F', 'f', 'E' or 'e'

    where  norm1  denotes the  one norm of a matrix (maximum column sum),
    normI  denotes the  infinity norm  of a matrix  (maximum row sum) and
    normF  denotes the  Frobenius norm of a matrix (square root of sum of
    squares).  Note that  max(abs(A(i,j)))  is not a consistent matrix norm.

    Arguments
    =========

    NORM    (input) CHARACTER*1
            Specifies the value to be returned in CLANGE as described
            above.

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.  When M = 0,
            CLANGE is set to zero.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.  When N = 0,
            CLANGE is set to zero.

    A       (input) COMPLEX array, dimension (LDA,N)
            The m by n matrix A.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(M,1).

    WORK    (workspace) REAL array, dimension (MAX(1,LWORK)),
            where LWORK >= M when NORM = 'I'; otherwise, WORK is not
            referenced.

   =====================================================================
','line_number':13423,'multiline':True]['text':' Parameter adjustments ','line_number':13484,'multiline':True]['text':' Function Body ','line_number':13490,'multiline':True]['text':'        Find max(abs(A(i,j))). ','line_number':13495,'multiline':True]['text':' Computing MAX ','line_number':13502,'multiline':True]['text':' L10: ','line_number':13505,'multiline':True]['text':' L20: ','line_number':13507,'multiline':True]['text':'        Find norm1(A). ','line_number':13512,'multiline':True]['text':' L30: ','line_number':13521,'multiline':True]['text':' L40: ','line_number':13524,'multiline':True]['text':'        Find normI(A). ','line_number':13528,'multiline':True]['text':' L50: ','line_number':13533,'multiline':True]['text':' L60: ','line_number':13540,'multiline':True]['text':' L70: ','line_number':13542,'multiline':True]['text':' Computing MAX ','line_number':13547,'multiline':True]['text':' L80: ','line_number':13550,'multiline':True]['text':'        Find normF(A). ','line_number':13554,'multiline':True]['text':' L90: ','line_number':13561,'multiline':True]['text':'     End of CLANGE ','line_number':13569,'multiline':True]['text':' clange_ ','line_number':13571,'multiline':True]['text':' System generated locals ','line_number':13576,'multiline':True]['text':' Local variables ','line_number':13580,'multiline':True]['text':' Subroutine ','line_number':13585,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLANHE  returns the value of the one norm,  or the Frobenius norm, or
    the  infinity norm,  or the  element of  largest absolute value  of a
    singlecomplex hermitian matrix A.

    Description
    ===========

    CLANHE returns the value

       CLANHE = ( max(abs(A(i,j))), NORM = 'M' or 'm'
                (
                ( norm1(A),         NORM = '1', 'O' or 'o'
                (
                ( normI(A),         NORM = 'I' or 'i'
                (
                ( normF(A),         NORM = 'F', 'f', 'E' or 'e'

    where  norm1  denotes the  one norm of a matrix (maximum column sum),
    normI  denotes the  infinity norm  of a matrix  (maximum row sum) and
    normF  denotes the  Frobenius norm of a matrix (square root of sum of
    squares).  Note that  max(abs(A(i,j)))  is not a consistent matrix norm.

    Arguments
    =========

    NORM    (input) CHARACTER*1
            Specifies the value to be returned in CLANHE as described
            above.

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            hermitian matrix A is to be referenced.
            = 'U':  Upper triangular part of A is referenced
            = 'L':  Lower triangular part of A is referenced

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.  When N = 0, CLANHE is
            set to zero.

    A       (input) COMPLEX array, dimension (LDA,N)
            The hermitian matrix A.  If UPLO = 'U', the leading n by n
            upper triangular part of A contains the upper triangular part
            of the matrix A, and the strictly lower triangular part of A
            is not referenced.  If UPLO = 'L', the leading n by n lower
            triangular part of A contains the lower triangular part of
            the matrix A, and the strictly upper triangular part of A is
            not referenced. Note that the imaginary parts of the diagonal
            elements need not be set and are assumed to be zero.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(N,1).

    WORK    (workspace) REAL array, dimension (MAX(1,LWORK)),
            where LWORK >= N when NORM = 'I' or '1' or 'O'; otherwise,
            WORK is not referenced.

   =====================================================================
','line_number':13589,'multiline':True]['text':' Parameter adjustments ','line_number':13659,'multiline':True]['text':' Function Body ','line_number':13665,'multiline':True]['text':'        Find max(abs(A(i,j))). ','line_number':13670,'multiline':True]['text':' Computing MAX ','line_number':13678,'multiline':True]['text':' L10: ','line_number':13681,'multiline':True]['text':' Computing MAX ','line_number':13683,'multiline':True]['text':' L20: ','line_number':13687,'multiline':True]['text':' Computing MAX ','line_number':13692,'multiline':True]['text':' Computing MAX ','line_number':13698,'multiline':True]['text':' L30: ','line_number':13701,'multiline':True]['text':' L40: ','line_number':13703,'multiline':True]['text':'        Find normI(A) ( = norm1(A), since A is hermitian). ','line_number':13708,'multiline':True]['text':' L50: ','line_number':13720,'multiline':True]['text':' L60: ','line_number':13724,'multiline':True]['text':' Computing MAX ','line_number':13728,'multiline':True]['text':' L70: ','line_number':13731,'multiline':True]['text':' L80: ','line_number':13737,'multiline':True]['text':' L90: ','line_number':13748,'multiline':True]['text':' L100: ','line_number':13751,'multiline':True]['text':'        Find normF(A). ','line_number':13756,'multiline':True]['text':' L110: ','line_number':13765,'multiline':True]['text':' L120: ','line_number':13772,'multiline':True]['text':' Computing 2nd power ','line_number':13783,'multiline':True]['text':' Computing 2nd power ','line_number':13788,'multiline':True]['text':' L130: ','line_number':13793,'multiline':True]['text':'     End of CLANHE ','line_number':13801,'multiline':True]['text':' clanhe_ ','line_number':13803,'multiline':True]['text':' Subroutine ','line_number':13805,'multiline':True]['text':' System generated locals ','line_number':13810,'multiline':True]['text':' Local variables ','line_number':13815,'multiline':True]['text':' was [1][1] ','line_number':13825,'multiline':True]['text':' Subroutine ','line_number':13827,'multiline':True]['text':' Subroutine ','line_number':13840,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       Purpose
       =======

       CLAQR0 computes the eigenvalues of a Hessenberg matrix H
       and, optionally, the matrices T and Z from the Schur decomposition
       H = Z T Z**H, where T is an upper triangular matrix (the
       Schur form), and Z is the unitary matrix of Schur vectors.

       Optionally Z may be postmultiplied into an input unitary
       matrix Q so that this routine can give the Schur factorization
       of a matrix A which has been reduced to the Hessenberg form H
       by the unitary matrix Q:  A = Q*H*Q**H = (QZ)*H*(QZ)**H.

       Arguments
       =========

       WANTT   (input) LOGICAL
            = .TRUE. : the full Schur form T is required;
            = .FALSE.: only eigenvalues are required.

       WANTZ   (input) LOGICAL
            = .TRUE. : the matrix of Schur vectors Z is required;
            = .FALSE.: Schur vectors are not required.

       N     (input) INTEGER
             The order of the matrix H.  N .GE. 0.

       ILO   (input) INTEGER
       IHI   (input) INTEGER
             It is assumed that H is already upper triangular in rows
             and columns 1:ILO-1 and IHI+1:N and, if ILO.GT.1,
             H(ILO,ILO-1) is zero. ILO and IHI are normally set by a
             previous call to CGEBAL, and then passed to CGEHRD when the
             matrix output by CGEBAL is reduced to Hessenberg form.
             Otherwise, ILO and IHI should be set to 1 and N,
             respectively.  If N.GT.0, then 1.LE.ILO.LE.IHI.LE.N.
             If N = 0, then ILO = 1 and IHI = 0.

       H     (input/output) COMPLEX array, dimension (LDH,N)
             On entry, the upper Hessenberg matrix H.
             On exit, if INFO = 0 and WANTT is .TRUE., then H
             contains the upper triangular matrix T from the Schur
             decomposition (the Schur form). If INFO = 0 and WANT is
             .FALSE., then the contents of H are unspecified on exit.
             (The output value of H when INFO.GT.0 is given under the
             description of INFO below.)

             This subroutine may explicitly set H(i,j) = 0 for i.GT.j and
             j = 1, 2, ... ILO-1 or j = IHI+1, IHI+2, ... N.

       LDH   (input) INTEGER
             The leading dimension of the array H. LDH .GE. max(1,N).

       W        (output) COMPLEX array, dimension (N)
             The computed eigenvalues of H(ILO:IHI,ILO:IHI) are stored
             in W(ILO:IHI). If WANTT is .TRUE., then the eigenvalues are
             stored in the same order as on the diagonal of the Schur
             form returned in H, with W(i) = H(i,i).

       Z     (input/output) COMPLEX array, dimension (LDZ,IHI)
             If WANTZ is .FALSE., then Z is not referenced.
             If WANTZ is .TRUE., then Z(ILO:IHI,ILOZ:IHIZ) is
             replaced by Z(ILO:IHI,ILOZ:IHIZ)*U where U is the
             orthogonal Schur factor of H(ILO:IHI,ILO:IHI).
             (The output value of Z when INFO.GT.0 is given under
             the description of INFO below.)

       LDZ   (input) INTEGER
             The leading dimension of the array Z.  if WANTZ is .TRUE.
             then LDZ.GE.MAX(1,IHIZ).  Otherwize, LDZ.GE.1.

       WORK  (workspace/output) COMPLEX array, dimension LWORK
             On exit, if LWORK = -1, WORK(1) returns an estimate of
             the optimal value for LWORK.

       LWORK (input) INTEGER
             The dimension of the array WORK.  LWORK .GE. max(1,N)
             is sufficient, but LWORK typically as large as 6*N may
             be required for optimal performance.  A workspace query
             to determine the optimal workspace size is recommended.

             If LWORK = -1, then CLAQR0 does a workspace query.
             In this case, CLAQR0 checks the input parameters and
             estimates the optimal workspace size for the given
             values of N, ILO and IHI.  The estimate is returned
             in WORK(1).  No error message related to LWORK is
             issued by XERBLA.  Neither H nor Z are accessed.


       INFO  (output) INTEGER
               =  0:  successful exit
             .GT. 0:  if INFO = i, CLAQR0 failed to compute all of
                  the eigenvalues.  Elements 1:ilo-1 and i+1:n of WR
                  and WI contain those eigenvalues which have been
                  successfully computed.  (Failures are rare.)

                  If INFO .GT. 0 and WANT is .FALSE., then on exit,
                  the remaining unconverged eigenvalues are the eigen-
                  values of the upper Hessenberg matrix rows and
                  columns ILO through INFO of the final, output
                  value of H.

                  If INFO .GT. 0 and WANTT is .TRUE., then on exit

             (*)  (initial value of H)*U  = U*(final value of H)

                  where U is a unitary matrix.  The final
                  value of  H is upper Hessenberg and triangular in
                  rows and columns INFO+1 through IHI.

                  If INFO .GT. 0 and WANTZ is .TRUE., then on exit

                    (final value of Z(ILO:IHI,ILOZ:IHIZ)
                     =  (initial value of Z(ILO:IHI,ILOZ:IHIZ)*U

                  where U is the unitary matrix in (*) (regard-
                  less of the value of WANTT.)

                  If INFO .GT. 0 and WANTZ is .FALSE., then Z is not
                  accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       References:
         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part I: Maintaining Well Focused Shifts, and Level 3
         Performance, SIAM Journal of Matrix Analysis, volume 23, pages
         929--947, 2002.

         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part II: Aggressive Early Deflation, SIAM Journal
         of Matrix Analysis, volume 23, pages 948--973, 2002.

       ================================================================

       ==== Matrices of order NTINY or smaller must be processed by
       .    CLAHQR because of insufficient subdiagonal scratch space.
       .    (This is a hard limit.) ====

       ==== Exceptional deflation windows:  try to cure rare
       .    slow convergence by varying the size of the
       .    deflation window after KEXNW iterations. ====

       ==== Exceptional shifts: try to cure rare slow convergence
       .    with ad-hoc exceptional shifts every KEXSH iterations.
       .    ====

       ==== The constant WILK1 is used to form the exceptional
       .    shifts. ====
','line_number':13853,'multiline':True]['text':' Parameter adjustments ','line_number':14013,'multiline':True]['text':' Function Body ','line_number':14023,'multiline':True]['text':'     ==== Quick return for N = 0: nothing to do. ==== ','line_number':14026,'multiline':True]['text':'        ==== Tiny matrices must use CLAHQR. ==== ','line_number':14035,'multiline':True]['text':'
          ==== Use small bulge multi-shift QR with aggressive early
          .    deflation on larger-than-tiny matrices. ====

          ==== Hope for the best. ====
','line_number':14044,'multiline':True]['text':'        ==== Set up job flags for ILAENV. ==== ','line_number':14053,'multiline':True]['text':'
          ==== NWR = recommended deflation window size.  At this
          .    point,  N .GT. NTINY = 11, so there is enough
          .    subdiagonal workspace for NWR.GE.2 as required.
          .    (In fact, there is enough subdiagonal space for
          .    NWR.GE.3.) ====
','line_number':14066,'multiline':True]['text':' Computing MIN ','line_number':14077,'multiline':True]['text':'
          ==== NSR = recommended number of simultaneous shifts.
          .    At this point N .GT. NTINY = 11, so there is at
          .    enough subdiagonal workspace for NSR to be even
          .    and greater than or equal to two as required. ====
','line_number':14081,'multiline':True]['text':' Computing MIN ','line_number':14090,'multiline':True]['text':' Computing MAX ','line_number':14094,'multiline':True]['text':'
          ==== Estimate optimal workspace ====

          ==== Workspace query call to CLAQR3 ====
','line_number':14098,'multiline':True]['text':'
          ==== Optimal workspace = MAX(CLAQR5, CLAQR3) ====

   Computing MAX
','line_number':14110,'multiline':True]['text':'        ==== Quick return in case of workspace query. ==== ','line_number':14118,'multiline':True]['text':'        ==== CLAHQR/CLAQR0 crossover point ==== ','line_number':14127,'multiline':True]['text':'        ==== Nibble crossover point ==== ','line_number':14133,'multiline':True]['text':'
          ==== Accumulate reflections during ttswp?  Use block
          .    2-by-2 structure during matrix-matrix multiply? ====
','line_number':14139,'multiline':True]['text':'
          ==== NWMAX = the largest possible deflation window for
          .    which there is sufficient workspace. ====

   Computing MIN
','line_number':14149,'multiline':True]['text':'
          ==== NSMAX = the Largest number of simultaneous shifts
          .    for which there is sufficient workspace. ====

   Computing MIN
','line_number':14159,'multiline':True]['text':'        ==== NDFL: an iteration count restarted at deflation. ==== ','line_number':14169,'multiline':True]['text':'
          ==== ITMAX = iteration limit ====

   Computing MAX
','line_number':14173,'multiline':True]['text':'        ==== Last row and column in the active block ==== ','line_number':14181,'multiline':True]['text':'        ==== Main Loop ==== ','line_number':14185,'multiline':True]['text':'           ==== Done when KBOT falls below ILO ==== ','line_number':14190,'multiline':True]['text':'           ==== Locate active block ==== ','line_number':14196,'multiline':True]['text':' L10: ','line_number':14204,'multiline':True]['text':'
             ==== Select deflation window size:
             .    Typical Case:
             .      If possible and advisable, nibble the entire
             .      active block.  If not, use size MIN(NWR,NWMAX)
             .      or MIN(NWR+1,NWMAX) depending upon which has
             .      the smaller corresponding subdiagonal entry
             .      (a heuristic).
             .
             .    Exceptional Case:
             .      If there have been no deflations in KEXNW or
             .      more iterations, then vary the deflation window
             .      size.   At first, because, larger windows are,
             .      in general, more powerful than smaller ones,
             .      rapidly increase the window to the maximum possible.
             .      Then, gradually reduce the window size. ====
','line_number':14210,'multiline':True]['text':' Computing MIN ','line_number':14233,'multiline':True]['text':'
             ==== Aggressive early deflation:
             .    split workspace under the subdiagonal into
             .      - an nw-by-nw work array V in the lower
             .        left-hand-corner,
             .      - an NW-by-at-least-NW-but-more-is-better
             .        (NW-by-NHO) horizontal work array along
             .        the bottom edge,
             .      - an at-least-NW-but-more-is-better (NHV-by-NW)
             .        vertical work array along the left-hand-edge.
             .        ====
','line_number':14263,'multiline':True]['text':'           ==== Aggressive early deflation ==== ','line_number':14282,'multiline':True]['text':'           ==== Adjust KBOT accounting for new deflations. ==== ','line_number':14289,'multiline':True]['text':'           ==== KS points to the shifts. ==== ','line_number':14293,'multiline':True]['text':'
             ==== Skip an expensive QR sweep if there is a (partly
             .    heuristic) reason to expect that many eigenvalues
             .    will deflate without it.  Here, the QR sweep is
             .    skipped if many eigenvalues have just been deflated
             .    or if the remaining active block is small.
','line_number':14297,'multiline':True]['text':'
                ==== NS = nominal number of simultaneous shifts.
                .    This may be lowered (slightly) if CLAQR3
                .    did not provide that many shifts. ====

   Computing MIN
   Computing MAX
','line_number':14308,'multiline':True]['text':'
                ==== If there have been no deflations
                .    in a multiple of KEXSH iterations,
                .    then try exceptional shifts.
                .    Otherwise use shifts provided by
                .    CLAQR3 above or from the eigenvalues
                .    of a trailing principal submatrix. ====
','line_number':14321,'multiline':True]['text':' L30: ','line_number':14345,'multiline':True]['text':'
                   ==== Got NS/2 or fewer shifts? Use CLAQR4 or
                   .    CLAHQR on a trailing principal submatrix to
                   .    get more. (Since NS.LE.NSMAX.LE.(N+6)/9,
                   .    there is enough space below the subdiagonal
                   .    to fit an NS-by-NS scratch array.) ====
','line_number':14349,'multiline':True]['text':'
                      ==== In case of a rare QR failure use
                      .    eigenvalues of the trailing 2-by-2
                      .    principal submatrix.  Scale to avoid
                      .    overflows, underflows and subnormals.
                      .    (The scale factor S can not be zero,
                      .    because H(KBOT,KBOT-1) is nonzero.) ====
','line_number':14373,'multiline':True]['text':'                    ==== Sort the shifts (Helps a little) ==== ','line_number':14446,'multiline':True]['text':' L40: ','line_number':14473,'multiline':True]['text':' L50: ','line_number':14475,'multiline':True]['text':'
                ==== If there are only two shifts, then use
                .    only one.  ====
','line_number':14482,'multiline':True]['text':'
                ==== Use up to NS of the smallest magnatiude
                .    shifts.  If there aren't NS shifts available,
                .    then use them all, possibly dropping one to
                .    make the number of shifts even. ====

   Computing MIN
','line_number':14511,'multiline':True]['text':'
                ==== Small-bulge multi-shift QR sweep:
                .    split workspace under the subdiagonal into
                .    - a KDU-by-KDU work array U in the lower
                .      left-hand-corner,
                .    - a KDU-by-at-least-KDU-but-more-is-better
                .      (KDU-by-NHo) horizontal work array WH along
                .      the bottom edge,
                .    - and an at-least-KDU-but-more-is-better-by-KDU
                .      (NVE-by-KDU) vertical work WV arrow along
                .      the left-hand-edge. ====
','line_number':14524,'multiline':True]['text':'              ==== Small-bulge multi-shift QR sweep ==== ','line_number':14544,'multiline':True]['text':'           ==== Note progress (or the lack of it). ==== ','line_number':14553,'multiline':True]['text':'
             ==== End of main loop ====
   L70:
','line_number':14561,'multiline':True]['text':'
          ==== Iteration limit exceeded.  Set INFO to show where
          .    the problem occurred and exit. ====
','line_number':14567,'multiline':True]['text':'     ==== Return the optimal value of LWORK. ==== ','line_number':14577,'multiline':True]['text':'     ==== End of CLAQR0 ==== ','line_number':14583,'multiline':True]['text':' claqr0_ ','line_number':14586,'multiline':True]['text':' Subroutine ','line_number':14588,'multiline':True]['text':' System generated locals ','line_number':14591,'multiline':True]['text':' Local variables ','line_number':14596,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


         Given a 2-by-2 or 3-by-3 matrix H, CLAQR1 sets v to a
         scalar multiple of the first column of the product

         (*)  K = (H - s1*I)*(H - s2*I)

         scaling to avoid overflows and most underflows.

         This is useful for starting double implicit shift bulges
         in the QR algorithm.


         N      (input) integer
                Order of the matrix H. N must be either 2 or 3.

         H      (input) COMPLEX array of dimension (LDH,N)
                The 2-by-2 or 3-by-3 matrix H in (*).

         LDH    (input) integer
                The leading dimension of H as declared in
                the calling procedure.  LDH.GE.N

         S1     (input) COMPLEX
         S2     S1 and S2 are the shifts defining K in (*) above.

         V      (output) COMPLEX array of dimension N
                A scalar multiple of the first column of the
                matrix K in (*).

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
','line_number':14601,'multiline':True]['text':' Parameter adjustments ','line_number':14643,'multiline':True]['text':' Function Body ','line_number':14649,'multiline':True]['text':' claqr1_ ','line_number':14753,'multiline':True]['text':' Subroutine ','line_number':14755,'multiline':True]['text':' System generated locals ','line_number':14762,'multiline':True]['text':' Local variables ','line_number':14768,'multiline':True]['text':' Subroutine ','line_number':14780,'multiline':True]['text':' Subroutine ','line_number':14787,'multiline':True]['text':' Subroutine ','line_number':14792,'multiline':True]['text':' Subroutine ','line_number':14798,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2.1)                        --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
    -- April 2009                                                      --


       This subroutine is identical to CLAQR3 except that it avoids
       recursion by calling CLAHQR instead of CLAQR4.


       ******************************************************************
       Aggressive early deflation:

       This subroutine accepts as input an upper Hessenberg matrix
       H and performs an unitary similarity transformation
       designed to detect and deflate fully converged eigenvalues from
       a trailing principal submatrix.  On output H has been over-
       written by a new Hessenberg matrix that is a perturbation of
       an unitary similarity transformation of H.  It is to be
       hoped that the final version of H has many zero subdiagonal
       entries.

       ******************************************************************
       WANTT   (input) LOGICAL
            If .TRUE., then the Hessenberg matrix H is fully updated
            so that the triangular Schur factor may be
            computed (in cooperation with the calling subroutine).
            If .FALSE., then only enough of H is updated to preserve
            the eigenvalues.

       WANTZ   (input) LOGICAL
            If .TRUE., then the unitary matrix Z is updated so
            so that the unitary Schur factor may be computed
            (in cooperation with the calling subroutine).
            If .FALSE., then Z is not referenced.

       N       (input) INTEGER
            The order of the matrix H and (if WANTZ is .TRUE.) the
            order of the unitary matrix Z.

       KTOP    (input) INTEGER
            It is assumed that either KTOP = 1 or H(KTOP,KTOP-1)=0.
            KBOT and KTOP together determine an isolated block
            along the diagonal of the Hessenberg matrix.

       KBOT    (input) INTEGER
            It is assumed without a check that either
            KBOT = N or H(KBOT+1,KBOT)=0.  KBOT and KTOP together
            determine an isolated block along the diagonal of the
            Hessenberg matrix.

       NW      (input) INTEGER
            Deflation window size.  1 .LE. NW .LE. (KBOT-KTOP+1).

       H       (input/output) COMPLEX array, dimension (LDH,N)
            On input the initial N-by-N section of H stores the
            Hessenberg matrix undergoing aggressive early deflation.
            On output H has been transformed by a unitary
            similarity transformation, perturbed, and the returned
            to Hessenberg form that (it is to be hoped) has some
            zero subdiagonal entries.

       LDH     (input) integer
            Leading dimension of H just as declared in the calling
            subroutine.  N .LE. LDH

       ILOZ    (input) INTEGER
       IHIZ    (input) INTEGER
            Specify the rows of Z to which transformations must be
            applied if WANTZ is .TRUE.. 1 .LE. ILOZ .LE. IHIZ .LE. N.

       Z       (input/output) COMPLEX array, dimension (LDZ,N)
            IF WANTZ is .TRUE., then on output, the unitary
            similarity transformation mentioned above has been
            accumulated into Z(ILOZ:IHIZ,ILO:IHI) from the right.
            If WANTZ is .FALSE., then Z is unreferenced.

       LDZ     (input) integer
            The leading dimension of Z just as declared in the
            calling subroutine.  1 .LE. LDZ.

       NS      (output) integer
            The number of unconverged (ie approximate) eigenvalues
            returned in SR and SI that may be used as shifts by the
            calling subroutine.

       ND      (output) integer
            The number of converged eigenvalues uncovered by this
            subroutine.

       SH      (output) COMPLEX array, dimension KBOT
            On output, approximate eigenvalues that may
            be used for shifts are stored in SH(KBOT-ND-NS+1)
            through SR(KBOT-ND).  Converged eigenvalues are
            stored in SH(KBOT-ND+1) through SH(KBOT).

       V       (workspace) COMPLEX array, dimension (LDV,NW)
            An NW-by-NW work array.

       LDV     (input) integer scalar
            The leading dimension of V just as declared in the
            calling subroutine.  NW .LE. LDV

       NH      (input) integer scalar
            The number of columns of T.  NH.GE.NW.

       T       (workspace) COMPLEX array, dimension (LDT,NW)

       LDT     (input) integer
            The leading dimension of T just as declared in the
            calling subroutine.  NW .LE. LDT

       NV      (input) integer
            The number of rows of work array WV available for
            workspace.  NV.GE.NW.

       WV      (workspace) COMPLEX array, dimension (LDWV,NW)

       LDWV    (input) integer
            The leading dimension of W just as declared in the
            calling subroutine.  NW .LE. LDV

       WORK    (workspace) COMPLEX array, dimension LWORK.
            On exit, WORK(1) is set to an estimate of the optimal value
            of LWORK for the given values of N, NW, KTOP and KBOT.

       LWORK   (input) integer
            The dimension of the work array WORK.  LWORK = 2*NW
            suffices, but greater efficiency may result from larger
            values of LWORK.

            If LWORK = -1, then a workspace query is assumed; CLAQR2
            only estimates the optimal workspace size for the given
            values of N, NW, KTOP and KBOT.  The estimate is returned
            in WORK(1).  No error message related to LWORK is issued
            by XERBLA.  Neither H nor Z are accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================

       ==== Estimate optimal workspace. ====
','line_number':14807,'multiline':True]['text':' Parameter adjustments ','line_number':14954,'multiline':True]['text':' Function Body ','line_number':14973,'multiline':True]['text':' Computing MIN ','line_number':14974,'multiline':True]['text':'        ==== Workspace query call to CGEHRD ==== ','line_number':14981,'multiline':True]['text':'        ==== Workspace query call to CUNMHR ==== ','line_number':14988,'multiline':True]['text':'        ==== Optimal workspace ==== ','line_number':14995,'multiline':True]['text':'     ==== Quick return in case of workspace query. ==== ','line_number':15000,'multiline':True]['text':'
       ==== Nothing to do ...
       ... for an empty active block ... ====
','line_number':15009,'multiline':True]['text':'     ... nor for an empty deflation window. ==== ','line_number':15019,'multiline':True]['text':'     ==== Machine constants ==== ','line_number':15024,'multiline':True]['text':'
       ==== Setup deflation window ====

   Computing MIN
','line_number':15032,'multiline':True]['text':'        ==== 1-by-1 deflation window: not much to do ==== ','line_number':15049,'multiline':True]['text':' Computing MAX ','line_number':15056,'multiline':True]['text':'
       ==== Convert to spike-triangular form.  (In case of a
       .    rare QR failure, this routine continues to do
       .    aggressive early deflation using that part of
       .    the deflation window that converged using INFQR
       .    here and there to keep track.) ====
','line_number':15073,'multiline':True]['text':'     ==== Deflation detection loop ==== ','line_number':15093,'multiline':True]['text':'        ==== Small spike tip deflation test ==== ','line_number':15100,'multiline':True]['text':' Computing MAX ','line_number':15109,'multiline':True]['text':'           ==== One more converged eigenvalue ==== ','line_number':15115,'multiline':True]['text':'
             ==== One undeflatable eigenvalue.  Move it up out of the
             .    way.   (CTREXC can not fail in this case.) ====
','line_number':15120,'multiline':True]['text':' L10: ','line_number':15130,'multiline':True]['text':'        ==== Return to Hessenberg form ==== ','line_number':15133,'multiline':True]['text':'
          ==== sorting the diagonal of T improves accuracy for
          .    graded matrices.  ====
','line_number':15141,'multiline':True]['text':' L20: ','line_number':15159,'multiline':True]['text':' L30: ','line_number':15166,'multiline':True]['text':'     ==== Restore shift/eigenvalue array from T ==== ','line_number':15170,'multiline':True]['text':' L40: ','line_number':15177,'multiline':True]['text':'           ==== Reflect spike back into lower triangle ==== ','line_number':15184,'multiline':True]['text':' L50: ','line_number':15192,'multiline':True]['text':'        ==== Copy updated reduced window into place ==== ','line_number':15215,'multiline':True]['text':'
          ==== Accumulate orthogonal matrix in order update
          .    H and Z, if requested.  ====
','line_number':15232,'multiline':True]['text':'        ==== Update vertical slab in H ==== ','line_number':15243,'multiline':True]['text':' Computing MIN ','line_number':15254,'multiline':True]['text':' L60: ','line_number':15262,'multiline':True]['text':'        ==== Update horizontal slab in H ==== ','line_number':15265,'multiline':True]['text':' Computing MIN ','line_number':15272,'multiline':True]['text':' L70: ','line_number':15280,'multiline':True]['text':'        ==== Update vertical slab in Z ==== ','line_number':15284,'multiline':True]['text':' Computing MIN ','line_number':15291,'multiline':True]['text':' L80: ','line_number':15299,'multiline':True]['text':'     ==== Return the number of deflations ... ==== ','line_number':15304,'multiline':True]['text':'
       ==== ... and the number of shifts. (Subtracting
       .    INFQR from the spike length takes care
       .    of the case of a rare QR failure while
       .    calculating eigenvalues of the deflation
       .    window.)  ====
','line_number':15308,'multiline':True]['text':'      ==== Return optimal workspace. ==== ','line_number':15318,'multiline':True]['text':'     ==== End of CLAQR2 ==== ','line_number':15324,'multiline':True]['text':' claqr2_ ','line_number':15327,'multiline':True]['text':' Subroutine ','line_number':15329,'multiline':True]['text':' System generated locals ','line_number':15336,'multiline':True]['text':' Local variables ','line_number':15342,'multiline':True]['text':' Subroutine ','line_number':15354,'multiline':True]['text':' Subroutine ','line_number':15361,'multiline':True]['text':' Subroutine ','line_number':15368,'multiline':True]['text':' Subroutine ','line_number':15377,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2.1)                        --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
    -- April 2009                                                      --


       ******************************************************************
       Aggressive early deflation:

       This subroutine accepts as input an upper Hessenberg matrix
       H and performs an unitary similarity transformation
       designed to detect and deflate fully converged eigenvalues from
       a trailing principal submatrix.  On output H has been over-
       written by a new Hessenberg matrix that is a perturbation of
       an unitary similarity transformation of H.  It is to be
       hoped that the final version of H has many zero subdiagonal
       entries.

       ******************************************************************
       WANTT   (input) LOGICAL
            If .TRUE., then the Hessenberg matrix H is fully updated
            so that the triangular Schur factor may be
            computed (in cooperation with the calling subroutine).
            If .FALSE., then only enough of H is updated to preserve
            the eigenvalues.

       WANTZ   (input) LOGICAL
            If .TRUE., then the unitary matrix Z is updated so
            so that the unitary Schur factor may be computed
            (in cooperation with the calling subroutine).
            If .FALSE., then Z is not referenced.

       N       (input) INTEGER
            The order of the matrix H and (if WANTZ is .TRUE.) the
            order of the unitary matrix Z.

       KTOP    (input) INTEGER
            It is assumed that either KTOP = 1 or H(KTOP,KTOP-1)=0.
            KBOT and KTOP together determine an isolated block
            along the diagonal of the Hessenberg matrix.

       KBOT    (input) INTEGER
            It is assumed without a check that either
            KBOT = N or H(KBOT+1,KBOT)=0.  KBOT and KTOP together
            determine an isolated block along the diagonal of the
            Hessenberg matrix.

       NW      (input) INTEGER
            Deflation window size.  1 .LE. NW .LE. (KBOT-KTOP+1).

       H       (input/output) COMPLEX array, dimension (LDH,N)
            On input the initial N-by-N section of H stores the
            Hessenberg matrix undergoing aggressive early deflation.
            On output H has been transformed by a unitary
            similarity transformation, perturbed, and the returned
            to Hessenberg form that (it is to be hoped) has some
            zero subdiagonal entries.

       LDH     (input) integer
            Leading dimension of H just as declared in the calling
            subroutine.  N .LE. LDH

       ILOZ    (input) INTEGER
       IHIZ    (input) INTEGER
            Specify the rows of Z to which transformations must be
            applied if WANTZ is .TRUE.. 1 .LE. ILOZ .LE. IHIZ .LE. N.

       Z       (input/output) COMPLEX array, dimension (LDZ,N)
            IF WANTZ is .TRUE., then on output, the unitary
            similarity transformation mentioned above has been
            accumulated into Z(ILOZ:IHIZ,ILO:IHI) from the right.
            If WANTZ is .FALSE., then Z is unreferenced.

       LDZ     (input) integer
            The leading dimension of Z just as declared in the
            calling subroutine.  1 .LE. LDZ.

       NS      (output) integer
            The number of unconverged (ie approximate) eigenvalues
            returned in SR and SI that may be used as shifts by the
            calling subroutine.

       ND      (output) integer
            The number of converged eigenvalues uncovered by this
            subroutine.

       SH      (output) COMPLEX array, dimension KBOT
            On output, approximate eigenvalues that may
            be used for shifts are stored in SH(KBOT-ND-NS+1)
            through SR(KBOT-ND).  Converged eigenvalues are
            stored in SH(KBOT-ND+1) through SH(KBOT).

       V       (workspace) COMPLEX array, dimension (LDV,NW)
            An NW-by-NW work array.

       LDV     (input) integer scalar
            The leading dimension of V just as declared in the
            calling subroutine.  NW .LE. LDV

       NH      (input) integer scalar
            The number of columns of T.  NH.GE.NW.

       T       (workspace) COMPLEX array, dimension (LDT,NW)

       LDT     (input) integer
            The leading dimension of T just as declared in the
            calling subroutine.  NW .LE. LDT

       NV      (input) integer
            The number of rows of work array WV available for
            workspace.  NV.GE.NW.

       WV      (workspace) COMPLEX array, dimension (LDWV,NW)

       LDWV    (input) integer
            The leading dimension of W just as declared in the
            calling subroutine.  NW .LE. LDV

       WORK    (workspace) COMPLEX array, dimension LWORK.
            On exit, WORK(1) is set to an estimate of the optimal value
            of LWORK for the given values of N, NW, KTOP and KBOT.

       LWORK   (input) integer
            The dimension of the work array WORK.  LWORK = 2*NW
            suffices, but greater efficiency may result from larger
            values of LWORK.

            If LWORK = -1, then a workspace query is assumed; CLAQR3
            only estimates the optimal workspace size for the given
            values of N, NW, KTOP and KBOT.  The estimate is returned
            in WORK(1).  No error message related to LWORK is issued
            by XERBLA.  Neither H nor Z are accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================

       ==== Estimate optimal workspace. ====
','line_number':15386,'multiline':True]['text':' Parameter adjustments ','line_number':15529,'multiline':True]['text':' Function Body ','line_number':15548,'multiline':True]['text':' Computing MIN ','line_number':15549,'multiline':True]['text':'        ==== Workspace query call to CGEHRD ==== ','line_number':15556,'multiline':True]['text':'        ==== Workspace query call to CUNMHR ==== ','line_number':15563,'multiline':True]['text':'        ==== Workspace query call to CLAQR4 ==== ','line_number':15570,'multiline':True]['text':'
          ==== Optimal workspace ====

   Computing MAX
','line_number':15576,'multiline':True]['text':'     ==== Quick return in case of workspace query. ==== ','line_number':15585,'multiline':True]['text':'
       ==== Nothing to do ...
       ... for an empty active block ... ====
','line_number':15594,'multiline':True]['text':'     ... nor for an empty deflation window. ==== ','line_number':15604,'multiline':True]['text':'     ==== Machine constants ==== ','line_number':15609,'multiline':True]['text':'
       ==== Setup deflation window ====

   Computing MIN
','line_number':15617,'multiline':True]['text':'        ==== 1-by-1 deflation window: not much to do ==== ','line_number':15634,'multiline':True]['text':' Computing MAX ','line_number':15641,'multiline':True]['text':'
       ==== Convert to spike-triangular form.  (In case of a
       .    rare QR failure, this routine continues to do
       .    aggressive early deflation using that part of
       .    the deflation window that converged using INFQR
       .    here and there to keep track.) ====
','line_number':15658,'multiline':True]['text':'     ==== Deflation detection loop ==== ','line_number':15686,'multiline':True]['text':'        ==== Small spike tip deflation test ==== ','line_number':15693,'multiline':True]['text':' Computing MAX ','line_number':15702,'multiline':True]['text':'           ==== One more converged eigenvalue ==== ','line_number':15708,'multiline':True]['text':'
             ==== One undeflatable eigenvalue.  Move it up out of the
             .    way.   (CTREXC can not fail in this case.) ====
','line_number':15713,'multiline':True]['text':' L10: ','line_number':15723,'multiline':True]['text':'        ==== Return to Hessenberg form ==== ','line_number':15726,'multiline':True]['text':'
          ==== sorting the diagonal of T improves accuracy for
          .    graded matrices.  ====
','line_number':15734,'multiline':True]['text':' L20: ','line_number':15752,'multiline':True]['text':' L30: ','line_number':15759,'multiline':True]['text':'     ==== Restore shift/eigenvalue array from T ==== ','line_number':15763,'multiline':True]['text':' L40: ','line_number':15770,'multiline':True]['text':'           ==== Reflect spike back into lower triangle ==== ','line_number':15777,'multiline':True]['text':' L50: ','line_number':15785,'multiline':True]['text':'        ==== Copy updated reduced window into place ==== ','line_number':15808,'multiline':True]['text':'
          ==== Accumulate orthogonal matrix in order update
          .    H and Z, if requested.  ====
','line_number':15825,'multiline':True]['text':'        ==== Update vertical slab in H ==== ','line_number':15836,'multiline':True]['text':' Computing MIN ','line_number':15847,'multiline':True]['text':' L60: ','line_number':15855,'multiline':True]['text':'        ==== Update horizontal slab in H ==== ','line_number':15858,'multiline':True]['text':' Computing MIN ','line_number':15865,'multiline':True]['text':' L70: ','line_number':15873,'multiline':True]['text':'        ==== Update vertical slab in Z ==== ','line_number':15877,'multiline':True]['text':' Computing MIN ','line_number':15884,'multiline':True]['text':' L80: ','line_number':15892,'multiline':True]['text':'     ==== Return the number of deflations ... ==== ','line_number':15897,'multiline':True]['text':'
       ==== ... and the number of shifts. (Subtracting
       .    INFQR from the spike length takes care
       .    of the case of a rare QR failure while
       .    calculating eigenvalues of the deflation
       .    window.)  ====
','line_number':15901,'multiline':True]['text':'      ==== Return optimal workspace. ==== ','line_number':15911,'multiline':True]['text':'     ==== End of CLAQR3 ==== ','line_number':15917,'multiline':True]['text':' claqr3_ ','line_number':15920,'multiline':True]['text':' Subroutine ','line_number':15922,'multiline':True]['text':' System generated locals ','line_number':15927,'multiline':True]['text':' Local variables ','line_number':15932,'multiline':True]['text':' was [1][1] ','line_number':15942,'multiline':True]['text':' Subroutine ','line_number':15944,'multiline':True]['text':' Subroutine ','line_number':15954,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       This subroutine implements one level of recursion for CLAQR0.
       It is a complete implementation of the small bulge multi-shift
       QR algorithm.  It may be called by CLAQR0 and, for large enough
       deflation window size, it may be called by CLAQR3.  This
       subroutine is identical to CLAQR0 except that it calls CLAQR2
       instead of CLAQR3.

       Purpose
       =======

       CLAQR4 computes the eigenvalues of a Hessenberg matrix H
       and, optionally, the matrices T and Z from the Schur decomposition
       H = Z T Z**H, where T is an upper triangular matrix (the
       Schur form), and Z is the unitary matrix of Schur vectors.

       Optionally Z may be postmultiplied into an input unitary
       matrix Q so that this routine can give the Schur factorization
       of a matrix A which has been reduced to the Hessenberg form H
       by the unitary matrix Q:  A = Q*H*Q**H = (QZ)*H*(QZ)**H.

       Arguments
       =========

       WANTT   (input) LOGICAL
            = .TRUE. : the full Schur form T is required;
            = .FALSE.: only eigenvalues are required.

       WANTZ   (input) LOGICAL
            = .TRUE. : the matrix of Schur vectors Z is required;
            = .FALSE.: Schur vectors are not required.

       N     (input) INTEGER
             The order of the matrix H.  N .GE. 0.

       ILO   (input) INTEGER
       IHI   (input) INTEGER
             It is assumed that H is already upper triangular in rows
             and columns 1:ILO-1 and IHI+1:N and, if ILO.GT.1,
             H(ILO,ILO-1) is zero. ILO and IHI are normally set by a
             previous call to CGEBAL, and then passed to CGEHRD when the
             matrix output by CGEBAL is reduced to Hessenberg form.
             Otherwise, ILO and IHI should be set to 1 and N,
             respectively.  If N.GT.0, then 1.LE.ILO.LE.IHI.LE.N.
             If N = 0, then ILO = 1 and IHI = 0.

       H     (input/output) COMPLEX array, dimension (LDH,N)
             On entry, the upper Hessenberg matrix H.
             On exit, if INFO = 0 and WANTT is .TRUE., then H
             contains the upper triangular matrix T from the Schur
             decomposition (the Schur form). If INFO = 0 and WANT is
             .FALSE., then the contents of H are unspecified on exit.
             (The output value of H when INFO.GT.0 is given under the
             description of INFO below.)

             This subroutine may explicitly set H(i,j) = 0 for i.GT.j and
             j = 1, 2, ... ILO-1 or j = IHI+1, IHI+2, ... N.

       LDH   (input) INTEGER
             The leading dimension of the array H. LDH .GE. max(1,N).

       W        (output) COMPLEX array, dimension (N)
             The computed eigenvalues of H(ILO:IHI,ILO:IHI) are stored
             in W(ILO:IHI). If WANTT is .TRUE., then the eigenvalues are
             stored in the same order as on the diagonal of the Schur
             form returned in H, with W(i) = H(i,i).

       Z     (input/output) COMPLEX array, dimension (LDZ,IHI)
             If WANTZ is .FALSE., then Z is not referenced.
             If WANTZ is .TRUE., then Z(ILO:IHI,ILOZ:IHIZ) is
             replaced by Z(ILO:IHI,ILOZ:IHIZ)*U where U is the
             orthogonal Schur factor of H(ILO:IHI,ILO:IHI).
             (The output value of Z when INFO.GT.0 is given under
             the description of INFO below.)

       LDZ   (input) INTEGER
             The leading dimension of the array Z.  if WANTZ is .TRUE.
             then LDZ.GE.MAX(1,IHIZ).  Otherwize, LDZ.GE.1.

       WORK  (workspace/output) COMPLEX array, dimension LWORK
             On exit, if LWORK = -1, WORK(1) returns an estimate of
             the optimal value for LWORK.

       LWORK (input) INTEGER
             The dimension of the array WORK.  LWORK .GE. max(1,N)
             is sufficient, but LWORK typically as large as 6*N may
             be required for optimal performance.  A workspace query
             to determine the optimal workspace size is recommended.

             If LWORK = -1, then CLAQR4 does a workspace query.
             In this case, CLAQR4 checks the input parameters and
             estimates the optimal workspace size for the given
             values of N, ILO and IHI.  The estimate is returned
             in WORK(1).  No error message related to LWORK is
             issued by XERBLA.  Neither H nor Z are accessed.


       INFO  (output) INTEGER
               =  0:  successful exit
             .GT. 0:  if INFO = i, CLAQR4 failed to compute all of
                  the eigenvalues.  Elements 1:ilo-1 and i+1:n of WR
                  and WI contain those eigenvalues which have been
                  successfully computed.  (Failures are rare.)

                  If INFO .GT. 0 and WANT is .FALSE., then on exit,
                  the remaining unconverged eigenvalues are the eigen-
                  values of the upper Hessenberg matrix rows and
                  columns ILO through INFO of the final, output
                  value of H.

                  If INFO .GT. 0 and WANTT is .TRUE., then on exit

             (*)  (initial value of H)*U  = U*(final value of H)

                  where U is a unitary matrix.  The final
                  value of  H is upper Hessenberg and triangular in
                  rows and columns INFO+1 through IHI.

                  If INFO .GT. 0 and WANTZ is .TRUE., then on exit

                    (final value of Z(ILO:IHI,ILOZ:IHIZ)
                     =  (initial value of Z(ILO:IHI,ILOZ:IHIZ)*U

                  where U is the unitary matrix in (*) (regard-
                  less of the value of WANTT.)

                  If INFO .GT. 0 and WANTZ is .FALSE., then Z is not
                  accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       References:
         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part I: Maintaining Well Focused Shifts, and Level 3
         Performance, SIAM Journal of Matrix Analysis, volume 23, pages
         929--947, 2002.

         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part II: Aggressive Early Deflation, SIAM Journal
         of Matrix Analysis, volume 23, pages 948--973, 2002.

       ================================================================

       ==== Matrices of order NTINY or smaller must be processed by
       .    CLAHQR because of insufficient subdiagonal scratch space.
       .    (This is a hard limit.) ====

       ==== Exceptional deflation windows:  try to cure rare
       .    slow convergence by varying the size of the
       .    deflation window after KEXNW iterations. ====

       ==== Exceptional shifts: try to cure rare slow convergence
       .    with ad-hoc exceptional shifts every KEXSH iterations.
       .    ====

       ==== The constant WILK1 is used to form the exceptional
       .    shifts. ====
','line_number':15967,'multiline':True]['text':' Parameter adjustments ','line_number':16134,'multiline':True]['text':' Function Body ','line_number':16144,'multiline':True]['text':'     ==== Quick return for N = 0: nothing to do. ==== ','line_number':16147,'multiline':True]['text':'        ==== Tiny matrices must use CLAHQR. ==== ','line_number':16156,'multiline':True]['text':'
          ==== Use small bulge multi-shift QR with aggressive early
          .    deflation on larger-than-tiny matrices. ====

          ==== Hope for the best. ====
','line_number':16165,'multiline':True]['text':'        ==== Set up job flags for ILAENV. ==== ','line_number':16174,'multiline':True]['text':'
          ==== NWR = recommended deflation window size.  At this
          .    point,  N .GT. NTINY = 11, so there is enough
          .    subdiagonal workspace for NWR.GE.2 as required.
          .    (In fact, there is enough subdiagonal space for
          .    NWR.GE.3.) ====
','line_number':16187,'multiline':True]['text':' Computing MIN ','line_number':16198,'multiline':True]['text':'
          ==== NSR = recommended number of simultaneous shifts.
          .    At this point N .GT. NTINY = 11, so there is at
          .    enough subdiagonal workspace for NSR to be even
          .    and greater than or equal to two as required. ====
','line_number':16202,'multiline':True]['text':' Computing MIN ','line_number':16211,'multiline':True]['text':' Computing MAX ','line_number':16215,'multiline':True]['text':'
          ==== Estimate optimal workspace ====

          ==== Workspace query call to CLAQR2 ====
','line_number':16219,'multiline':True]['text':'
          ==== Optimal workspace = MAX(CLAQR5, CLAQR2) ====

   Computing MAX
','line_number':16231,'multiline':True]['text':'        ==== Quick return in case of workspace query. ==== ','line_number':16239,'multiline':True]['text':'        ==== CLAHQR/CLAQR0 crossover point ==== ','line_number':16248,'multiline':True]['text':'        ==== Nibble crossover point ==== ','line_number':16254,'multiline':True]['text':'
          ==== Accumulate reflections during ttswp?  Use block
          .    2-by-2 structure during matrix-matrix multiply? ====
','line_number':16260,'multiline':True]['text':'
          ==== NWMAX = the largest possible deflation window for
          .    which there is sufficient workspace. ====

   Computing MIN
','line_number':16270,'multiline':True]['text':'
          ==== NSMAX = the Largest number of simultaneous shifts
          .    for which there is sufficient workspace. ====

   Computing MIN
','line_number':16280,'multiline':True]['text':'        ==== NDFL: an iteration count restarted at deflation. ==== ','line_number':16290,'multiline':True]['text':'
          ==== ITMAX = iteration limit ====

   Computing MAX
','line_number':16294,'multiline':True]['text':'        ==== Last row and column in the active block ==== ','line_number':16302,'multiline':True]['text':'        ==== Main Loop ==== ','line_number':16306,'multiline':True]['text':'           ==== Done when KBOT falls below ILO ==== ','line_number':16311,'multiline':True]['text':'           ==== Locate active block ==== ','line_number':16317,'multiline':True]['text':' L10: ','line_number':16325,'multiline':True]['text':'
             ==== Select deflation window size:
             .    Typical Case:
             .      If possible and advisable, nibble the entire
             .      active block.  If not, use size MIN(NWR,NWMAX)
             .      or MIN(NWR+1,NWMAX) depending upon which has
             .      the smaller corresponding subdiagonal entry
             .      (a heuristic).
             .
             .    Exceptional Case:
             .      If there have been no deflations in KEXNW or
             .      more iterations, then vary the deflation window
             .      size.   At first, because, larger windows are,
             .      in general, more powerful than smaller ones,
             .      rapidly increase the window to the maximum possible.
             .      Then, gradually reduce the window size. ====
','line_number':16331,'multiline':True]['text':' Computing MIN ','line_number':16354,'multiline':True]['text':'
             ==== Aggressive early deflation:
             .    split workspace under the subdiagonal into
             .      - an nw-by-nw work array V in the lower
             .        left-hand-corner,
             .      - an NW-by-at-least-NW-but-more-is-better
             .        (NW-by-NHO) horizontal work array along
             .        the bottom edge,
             .      - an at-least-NW-but-more-is-better (NHV-by-NW)
             .        vertical work array along the left-hand-edge.
             .        ====
','line_number':16384,'multiline':True]['text':'           ==== Aggressive early deflation ==== ','line_number':16403,'multiline':True]['text':'           ==== Adjust KBOT accounting for new deflations. ==== ','line_number':16410,'multiline':True]['text':'           ==== KS points to the shifts. ==== ','line_number':16414,'multiline':True]['text':'
             ==== Skip an expensive QR sweep if there is a (partly
             .    heuristic) reason to expect that many eigenvalues
             .    will deflate without it.  Here, the QR sweep is
             .    skipped if many eigenvalues have just been deflated
             .    or if the remaining active block is small.
','line_number':16418,'multiline':True]['text':'
                ==== NS = nominal number of simultaneous shifts.
                .    This may be lowered (slightly) if CLAQR2
                .    did not provide that many shifts. ====

   Computing MIN
   Computing MAX
','line_number':16429,'multiline':True]['text':'
                ==== If there have been no deflations
                .    in a multiple of KEXSH iterations,
                .    then try exceptional shifts.
                .    Otherwise use shifts provided by
                .    CLAQR2 above or from the eigenvalues
                .    of a trailing principal submatrix. ====
','line_number':16442,'multiline':True]['text':' L30: ','line_number':16466,'multiline':True]['text':'
                   ==== Got NS/2 or fewer shifts? Use CLAHQR
                   .    on a trailing principal submatrix to
                   .    get more. (Since NS.LE.NSMAX.LE.(N+6)/9,
                   .    there is enough space below the subdiagonal
                   .    to fit an NS-by-NS scratch array.) ====
','line_number':16470,'multiline':True]['text':'
                      ==== In case of a rare QR failure use
                      .    eigenvalues of the trailing 2-by-2
                      .    principal submatrix.  Scale to avoid
                      .    overflows, underflows and subnormals.
                      .    (The scale factor S can not be zero,
                      .    because H(KBOT,KBOT-1) is nonzero.) ====
','line_number':16488,'multiline':True]['text':'                    ==== Sort the shifts (Helps a little) ==== ','line_number':16561,'multiline':True]['text':' L40: ','line_number':16588,'multiline':True]['text':' L50: ','line_number':16590,'multiline':True]['text':'
                ==== If there are only two shifts, then use
                .    only one.  ====
','line_number':16597,'multiline':True]['text':'
                ==== Use up to NS of the smallest magnatiude
                .    shifts.  If there aren't NS shifts available,
                .    then use them all, possibly dropping one to
                .    make the number of shifts even. ====

   Computing MIN
','line_number':16626,'multiline':True]['text':'
                ==== Small-bulge multi-shift QR sweep:
                .    split workspace under the subdiagonal into
                .    - a KDU-by-KDU work array U in the lower
                .      left-hand-corner,
                .    - a KDU-by-at-least-KDU-but-more-is-better
                .      (KDU-by-NHo) horizontal work array WH along
                .      the bottom edge,
                .    - and an at-least-KDU-but-more-is-better-by-KDU
                .      (NVE-by-KDU) vertical work WV arrow along
                .      the left-hand-edge. ====
','line_number':16639,'multiline':True]['text':'              ==== Small-bulge multi-shift QR sweep ==== ','line_number':16659,'multiline':True]['text':'           ==== Note progress (or the lack of it). ==== ','line_number':16668,'multiline':True]['text':'
             ==== End of main loop ====
   L70:
','line_number':16676,'multiline':True]['text':'
          ==== Iteration limit exceeded.  Set INFO to show where
          .    the problem occurred and exit. ====
','line_number':16682,'multiline':True]['text':'     ==== Return the optimal value of LWORK. ==== ','line_number':16692,'multiline':True]['text':'     ==== End of CLAQR4 ==== ','line_number':16698,'multiline':True]['text':' claqr4_ ','line_number':16701,'multiline':True]['text':' Subroutine ','line_number':16703,'multiline':True]['text':' System generated locals ','line_number':16710,'multiline':True]['text':' Local variables ','line_number':16717,'multiline':True]['text':' Subroutine ','line_number':16732,'multiline':True]['text':' Subroutine ','line_number':16736,'multiline':True]['text':' Subroutine ','line_number':16743,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       This auxiliary subroutine called by CLAQR0 performs a
       single small-bulge multi-shift QR sweep.

        WANTT  (input) logical scalar
               WANTT = .true. if the triangular Schur factor
               is being computed.  WANTT is set to .false. otherwise.

        WANTZ  (input) logical scalar
               WANTZ = .true. if the unitary Schur factor is being
               computed.  WANTZ is set to .false. otherwise.

        KACC22 (input) integer with value 0, 1, or 2.
               Specifies the computation mode of far-from-diagonal
               orthogonal updates.
          = 0: CLAQR5 does not accumulate reflections and does not
               use matrix-matrix multiply to update far-from-diagonal
               matrix entries.
          = 1: CLAQR5 accumulates reflections and uses matrix-matrix
               multiply to update the far-from-diagonal matrix entries.
          = 2: CLAQR5 accumulates reflections, uses matrix-matrix
               multiply to update the far-from-diagonal matrix entries,
               and takes advantage of 2-by-2 block structure during
               matrix multiplies.

        N      (input) integer scalar
               N is the order of the Hessenberg matrix H upon which this
               subroutine operates.

        KTOP   (input) integer scalar
        KBOT   (input) integer scalar
               These are the first and last rows and columns of an
               isolated diagonal block upon which the QR sweep is to be
               applied. It is assumed without a check that
                         either KTOP = 1  or   H(KTOP,KTOP-1) = 0
               and
                         either KBOT = N  or   H(KBOT+1,KBOT) = 0.

        NSHFTS (input) integer scalar
               NSHFTS gives the number of simultaneous shifts.  NSHFTS
               must be positive and even.

        S      (input/output) COMPLEX array of size (NSHFTS)
               S contains the shifts of origin that define the multi-
               shift QR sweep.  On output S may be reordered.

        H      (input/output) COMPLEX array of size (LDH,N)
               On input H contains a Hessenberg matrix.  On output a
               multi-shift QR sweep with shifts SR(J)+i*SI(J) is applied
               to the isolated diagonal block in rows and columns KTOP
               through KBOT.

        LDH    (input) integer scalar
               LDH is the leading dimension of H just as declared in the
               calling procedure.  LDH.GE.MAX(1,N).

        ILOZ   (input) INTEGER
        IHIZ   (input) INTEGER
               Specify the rows of Z to which transformations must be
               applied if WANTZ is .TRUE.. 1 .LE. ILOZ .LE. IHIZ .LE. N

        Z      (input/output) COMPLEX array of size (LDZ,IHI)
               If WANTZ = .TRUE., then the QR Sweep unitary
               similarity transformation is accumulated into
               Z(ILOZ:IHIZ,ILO:IHI) from the right.
               If WANTZ = .FALSE., then Z is unreferenced.

        LDZ    (input) integer scalar
               LDA is the leading dimension of Z just as declared in
               the calling procedure. LDZ.GE.N.

        V      (workspace) COMPLEX array of size (LDV,NSHFTS/2)

        LDV    (input) integer scalar
               LDV is the leading dimension of V as declared in the
               calling procedure.  LDV.GE.3.

        U      (workspace) COMPLEX array of size
               (LDU,3*NSHFTS-3)

        LDU    (input) integer scalar
               LDU is the leading dimension of U just as declared in the
               in the calling subroutine.  LDU.GE.3*NSHFTS-3.

        NH     (input) integer scalar
               NH is the number of columns in array WH available for
               workspace. NH.GE.1.

        WH     (workspace) COMPLEX array of size (LDWH,NH)

        LDWH   (input) integer scalar
               Leading dimension of WH just as declared in the
               calling procedure.  LDWH.GE.3*NSHFTS-3.

        NV     (input) integer scalar
               NV is the number of rows in WV agailable for workspace.
               NV.GE.1.

        WV     (workspace) COMPLEX array of size
               (LDWV,3*NSHFTS-3)

        LDWV   (input) integer scalar
               LDWV is the leading dimension of WV as declared in the
               in the calling subroutine.  LDWV.GE.NV.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       Reference:

       K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
       Algorithm Part I: Maintaining Well Focused Shifts, and
       Level 3 Performance, SIAM Journal of Matrix Analysis,
       volume 23, pages 929--947, 2002.

       ================================================================


       ==== If there are no shifts, then there is nothing to do. ====
','line_number':16752,'multiline':True]['text':' Parameter adjustments ','line_number':16881,'multiline':True]['text':' Function Body ','line_number':16902,'multiline':True]['text':'
       ==== If the active block is empty or 1-by-1, then there
       .    is nothing to do. ====
','line_number':16907,'multiline':True]['text':'
       ==== NSHFTS is supposed to be even, but if it is odd,
       .    then simply reduce it by one.  ====
','line_number':16916,'multiline':True]['text':'     ==== Machine constants for deflation ==== ','line_number':16923,'multiline':True]['text':'
       ==== Use accumulated reflections to update far-from-diagonal
       .    entries ? ====
','line_number':16931,'multiline':True]['text':'     ==== If so, exploit the 2-by-2 block structure? ==== ','line_number':16938,'multiline':True]['text':'     ==== clear trash ==== ','line_number':16942,'multiline':True]['text':'     ==== NBMPS = number of 2-shift bulges in the chain ==== ','line_number':16949,'multiline':True]['text':'     ==== KDU = width of slab ==== ','line_number':16953,'multiline':True]['text':'     ==== Create and chase chains of NBMPS bulges ==== ','line_number':16957,'multiline':True]['text':'
          ==== Near-the-diagonal bulge chase.  The following loop
          .    performs the near-the-diagonal part of a small bulge
          .    multi-shift QR sweep.  Each 6*NBMPS-2 column diagonal
          .    chunk extends from column INCOL to column NDCOL
          .    (including both column INCOL and column NDCOL). The
          .    following loop chases a 3*NBMPS column long chain of
          .    NBMPS bulges 3*NBMPS-2 columns to the right.  (INCOL
          .    may be less than KTOP and and NDCOL may be greater than
          .    KBOT indicating phantom columns from which to chase
          .    bulges before they are actually introduced or to which
          .    to chase bulges beyond column KBOT.)  ====

   Computing MIN
','line_number':16968,'multiline':True]['text':'
             ==== Bulges number MTOP to MBOT are active double implicit
             .    shift bulges.  There may or may not also be small
             .    2-by-2 bulge, if there is room.  The inactive bulges
             .    (if any) must wait until the active bulges have moved
             .    down the diagonal to make room.  The phantom matrix
             .    paradigm described above helps keep track.  ====

   Computing MAX
','line_number':16987,'multiline':True]['text':' Computing MIN ','line_number':16999,'multiline':True]['text':'
             ==== Generate reflections to chase the chain right
             .    one column.  (The minimum value of K is KTOP-1.) ====
','line_number':17005,'multiline':True]['text':'
                   ==== A Bulge may collapse because of vigilant
                   .    deflation or destructive underflow.  In the
                   .    underflow case, try the two-small-subdiagonals
                   .    trick to try to reinflate the bulge.  ====
','line_number':17032,'multiline':True]['text':'                    ==== Typical case: not collapsed (yet). ==== ','line_number':17046,'multiline':True]['text':'
                      ==== Atypical case: collapsed.  Attempt to
                      .    reintroduce ignoring H(K+1,K) and H(K+2,K).
                      .    If the fill resulting from the new
                      .    reflector is too large, then abandon it.
                      .    Otherwise, use the new one. ====
','line_number':17056,'multiline':True]['text':'
                         ==== Starting a new bulge here would
                         .    create non-negligible fill.  Use
                         .    the old one with trepidation. ====
','line_number':17106,'multiline':True]['text':'
                         ==== Stating a new bulge here would
                         .    create only negligible fill.
                         .    Replace the old reflector with
                         .    the new one. ====
','line_number':17120,'multiline':True]['text':' L10: ','line_number':17145,'multiline':True]['text':'           ==== Generate a 2-by-2 reflection, if needed. ==== ','line_number':17148,'multiline':True]['text':'           ==== Multiply H by reflections from the left ==== ','line_number':17175,'multiline':True]['text':' Computing MIN ','line_number':17186,'multiline':True]['text':' L20: ','line_number':17233,'multiline':True]['text':' L30: ','line_number':17235,'multiline':True]['text':' Computing MAX ','line_number':17239,'multiline':True]['text':' L40: ','line_number':17269,'multiline':True]['text':'
             ==== Multiply H by reflections from the right.
             .    Delay filling in the last row until the
             .    vigilant deflation check is complete. ====
','line_number':17273,'multiline':True]['text':' Computing MIN ','line_number':17291,'multiline':True]['text':' L50: ','line_number':17337,'multiline':True]['text':'
                      ==== Accumulate U. (If necessary, update Z later
                      .    with an efficient matrix-matrix
                      .    multiply.) ====
','line_number':17342,'multiline':True]['text':' Computing MAX ','line_number':17349,'multiline':True]['text':' L60: ','line_number':17396,'multiline':True]['text':'
                      ==== U is not accumulated, so update Z
                      .    now by multiplying by reflections
                      .    from the right. ====
','line_number':17400,'multiline':True]['text':' L70: ','line_number':17451,'multiline':True]['text':' L80: ','line_number':17455,'multiline':True]['text':'           ==== Special case: 2-by-2 reflection (if needed) ==== ','line_number':17458,'multiline':True]['text':' Computing MIN ','line_number':17463,'multiline':True]['text':' L90: ','line_number':17492,'multiline':True]['text':' Computing MAX ','line_number':17497,'multiline':True]['text':' L100: ','line_number':17528,'multiline':True]['text':' L110: ','line_number':17560,'multiline':True]['text':'           ==== Vigilant deflation check ==== ','line_number':17565,'multiline':True]['text':' Computing MIN ','line_number':17580,'multiline':True]['text':'
                ==== The following convergence test requires that
                .    the tradition small-compared-to-nearby-diagonals
                .    criterion and the Ahues & Tisseur (LAWN 122, 1997)
                .    criteria both be satisfied.  The latter improves
                .    accuracy in some examples. Falling back on an
                .    alternate convergence criterion when TST1 or TST2
                .    is zero (as done here) is traditional but probably
                .    unnecessary. ====
','line_number':17584,'multiline':True]['text':' Computing MAX ','line_number':17642,'multiline':True]['text':' Computing MAX ','line_number':17647,'multiline':True]['text':' Computing MIN ','line_number':17656,'multiline':True]['text':' Computing MAX ','line_number':17670,'multiline':True]['text':' Computing MIN ','line_number':17682,'multiline':True]['text':' Computing MAX ','line_number':17692,'multiline':True]['text':' L120: ','line_number':17701,'multiline':True]['text':'
             ==== Fill in the last row of each bulge. ====

   Computing MIN
','line_number':17704,'multiline':True]['text':' L130: ','line_number':17739,'multiline':True]['text':'
             ==== End of near-the-diagonal bulge chase. ====

   L140:
','line_number':17742,'multiline':True]['text':'
          ==== Use U (if accumulated) to update far-from-diagonal
          .    entries in H.  If required, use U to update Z as
          .    well. ====
','line_number':17749,'multiline':True]['text':'
                ==== Updates not exploiting the 2-by-2 block
                .    structure of U.  K1 and NU keep track of
                .    the location and size of U in the special
                .    cases of introducing bulges and chasing
                .    bulges off the bottom.  In these special
                .    cases and in case the number of shifts
                .    is NS = 2, there is no 2-by-2 block
                .    structure to exploit.  ====

   Computing MAX
','line_number':17765,'multiline':True]['text':' Computing MAX ','line_number':17779,'multiline':True]['text':'              ==== Horizontal Multiply ==== ','line_number':17783,'multiline':True]['text':' Computing MIN ','line_number':17789,'multiline':True]['text':' L150: ','line_number':17797,'multiline':True]['text':'              ==== Vertical multiply ==== ','line_number':17800,'multiline':True]['text':' Computing MIN ','line_number':17806,'multiline':True]['text':' L160: ','line_number':17814,'multiline':True]['text':'              ==== Z multiply (also vertical) ==== ','line_number':17817,'multiline':True]['text':' Computing MIN ','line_number':17824,'multiline':True]['text':' L170: ','line_number':17833,'multiline':True]['text':'
                ==== Updates exploiting U's 2-by-2 block structure.
                .    (I2, I4, J2, J4 are the last rows and columns
                .    of the blocks.) ====
','line_number':17838,'multiline':True]['text':'
                ==== KZS and KNZ deal with the band of zeros
                .    along the diagonal of one of the triangular
                .    blocks. ====
','line_number':17849,'multiline':True]['text':'              ==== Horizontal multiply ==== ','line_number':17858,'multiline':True]['text':' Computing MIN ','line_number':17864,'multiline':True]['text':'
                   ==== Copy bottom of H to top+KZS of scratch ====
                    (The first KZS rows get multiplied by zero.) ====
','line_number':17868,'multiline':True]['text':'                 ==== Multiply by U21' ==== ','line_number':17876,'multiline':True]['text':'                 ==== Multiply top of H by U11' ==== ','line_number':17884,'multiline':True]['text':'                 ==== Copy top of H to bottom of WH ==== ','line_number':17890,'multiline':True]['text':'                 ==== Multiply by U21' ==== ','line_number':17895,'multiline':True]['text':'                 ==== Multiply by U22 ==== ','line_number':17900,'multiline':True]['text':'                 ==== Copy it back ==== ','line_number':17909,'multiline':True]['text':' L180: ','line_number':17913,'multiline':True]['text':'              ==== Vertical multiply ==== ','line_number':17916,'multiline':True]['text':' Computing MIN ','line_number':17922,'multiline':True]['text':'
                   ==== Copy right of H to scratch (the first KZS
                   .    columns get multiplied by zero) ====
','line_number':17926,'multiline':True]['text':'                 ==== Multiply by U21 ==== ','line_number':17934,'multiline':True]['text':'                 ==== Multiply by U11 ==== ','line_number':17942,'multiline':True]['text':'                 ==== Copy left of H to right of scratch ==== ','line_number':17949,'multiline':True]['text':'                 ==== Multiply by U21 ==== ','line_number':17954,'multiline':True]['text':'                 ==== Multiply by U22 ==== ','line_number':17961,'multiline':True]['text':'                 ==== Copy it back ==== ','line_number':17970,'multiline':True]['text':' L190: ','line_number':17974,'multiline':True]['text':'              ==== Multiply Z (also vertical) ==== ','line_number':17977,'multiline':True]['text':' Computing MIN ','line_number':17984,'multiline':True]['text':'
                      ==== Copy right of Z to left of scratch (first
                      .     KZS columns get multiplied by zero) ====
','line_number':17988,'multiline':True]['text':'                    ==== Multiply by U12 ==== ','line_number':17997,'multiline':True]['text':'                    ==== Multiply by U11 ==== ','line_number':18005,'multiline':True]['text':'                    ==== Copy left of Z to right of scratch ==== ','line_number':18011,'multiline':True]['text':'                    ==== Multiply by U21 ==== ','line_number':18017,'multiline':True]['text':'                    ==== Multiply by U22 ==== ','line_number':18024,'multiline':True]['text':'                    ==== Copy the result back to Z ==== ','line_number':18033,'multiline':True]['text':' L200: ','line_number':18037,'multiline':True]['text':' L210: ','line_number':18042,'multiline':True]['text':'     ==== End of CLAQR5 ==== ','line_number':18045,'multiline':True]['text':' claqr5_ ','line_number':18048,'multiline':True]['text':' Subroutine ','line_number':18050,'multiline':True]['text':' System generated locals ','line_number':18053,'multiline':True]['text':' Local variables ','line_number':18059,'multiline':True]['text':' Subroutine ','line_number':18061,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLARCM performs a very simple matrix-matrix multiplication:
             C := A * B,
    where A is M by M and real; B is M by N and complex;
    C is M by N and complex.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A and of the matrix C.
            M >= 0.

    N       (input) INTEGER
            The number of columns and rows of the matrix B and
            the number of columns of the matrix C.
            N >= 0.

    A       (input) REAL array, dimension (LDA, M)
            A contains the M by M matrix A.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >=max(1,M).

    B       (input) REAL array, dimension (LDB, N)
            B contains the M by N matrix B.

    LDB     (input) INTEGER
            The leading dimension of the array B. LDB >=max(1,M).

    C       (input) COMPLEX array, dimension (LDC, N)
            C contains the M by N matrix C.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >=max(1,M).

    RWORK   (workspace) REAL array, dimension (2*M*N)

    =====================================================================


       Quick return if possible.
','line_number':18066,'multiline':True]['text':' Parameter adjustments ','line_number':18119,'multiline':True]['text':' Function Body ','line_number':18131,'multiline':True]['text':' L10: ','line_number':18142,'multiline':True]['text':' L20: ','line_number':18144,'multiline':True]['text':' L30: ','line_number':18157,'multiline':True]['text':' L40: ','line_number':18159,'multiline':True]['text':' L50: ','line_number':18167,'multiline':True]['text':' L60: ','line_number':18169,'multiline':True]['text':' L70: ','line_number':18183,'multiline':True]['text':' L80: ','line_number':18185,'multiline':True]['text':'     End of CLARCM ','line_number':18190,'multiline':True]['text':' clarcm_ ','line_number':18192,'multiline':True]['text':' Subroutine ','line_number':18194,'multiline':True]['text':' System generated locals ','line_number':18198,'multiline':True]['text':' Local variables ','line_number':18202,'multiline':True]['text':' Subroutine ','line_number':18205,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLARF applies a complex elementary reflector H to a complex M-by-N
    matrix C, from either the left or the right. H is represented in the
    form

          H = I - tau * v * v'

    where tau is a complex scalar and v is a complex vector.

    If tau = 0, then H is taken to be the unit matrix.

    To apply H' (the conjugate transpose of H), supply conjg(tau) instead
    tau.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': form  H * C
            = 'R': form  C * H

    M       (input) INTEGER
            The number of rows of the matrix C.

    N       (input) INTEGER
            The number of columns of the matrix C.

    V       (input) COMPLEX array, dimension
                       (1 + (M-1)*abs(INCV)) if SIDE = 'L'
                    or (1 + (N-1)*abs(INCV)) if SIDE = 'R'
            The vector v in the representation of H. V is not used if
            TAU = 0.

    INCV    (input) INTEGER
            The increment between elements of v. INCV <> 0.

    TAU     (input) COMPLEX
            The value tau in the representation of H.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by the matrix H * C if SIDE = 'L',
            or C * H if SIDE = 'R'.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX array, dimension
                           (N) if SIDE = 'L'
                        or (M) if SIDE = 'R'

    =====================================================================
','line_number':18215,'multiline':True]['text':' Parameter adjustments ','line_number':18279,'multiline':True]['text':' Function Body ','line_number':18286,'multiline':True]['text':'
       Set up variables for scanning V.  LASTV begins pointing to the end
       of V.
','line_number':18291,'multiline':True]['text':'     Look for the last non-zero row in V. ','line_number':18305,'multiline':True]['text':' while(complicated condition) ','line_number':18306,'multiline':True]['text':'     Scan for the last non-zero column in C(1:lastv,:). ','line_number':18314,'multiline':True]['text':'     Scan for the last non-zero row in C(:,1:lastv). ','line_number':18317,'multiline':True]['text':'
       Note that lastc.eq.0 renders the BLAS operations null; no special
       case is needed at this level.
','line_number':18321,'multiline':True]['text':'        Form  H * C ','line_number':18327,'multiline':True]['text':'           w(1:lastc,1) := C(1:lastv,1:lastc)' * v(1:lastv,1) ','line_number':18331,'multiline':True]['text':'           C(1:lastv,1:lastc) := C(...) - v(1:lastv,1) * w(1:lastc,1)' ','line_number':18336,'multiline':True]['text':'        Form  C * H ','line_number':18344,'multiline':True]['text':'           w(1:lastc,1) := C(1:lastc,1:lastv) * v(1:lastv,1) ','line_number':18348,'multiline':True]['text':'           C(1:lastc,1:lastv) := C(...) - w(1:lastc,1) * v(1:lastv,1)' ','line_number':18353,'multiline':True]['text':'     End of CLARF ','line_number':18362,'multiline':True]['text':' clarf_ ','line_number':18364,'multiline':True]['text':' Subroutine ','line_number':18366,'multiline':True]['text':' System generated locals ','line_number':18371,'multiline':True]['text':' Local variables ','line_number':18376,'multiline':True]['text':' Subroutine ','line_number':18378,'multiline':True]['text':' Subroutine ','line_number':18383,'multiline':True]['text':' Subroutine ','line_number':18389,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLARFB applies a complex block reflector H or its transpose H' to a
    complex M-by-N matrix C, from either the left or the right.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply H or H' from the Left
            = 'R': apply H or H' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply H (No transpose)
            = 'C': apply H' (Conjugate transpose)

    DIRECT  (input) CHARACTER*1
            Indicates how H is formed from a product of elementary
            reflectors
            = 'F': H = H(1) H(2) . . . H(k) (Forward)
            = 'B': H = H(k) . . . H(2) H(1) (Backward)

    STOREV  (input) CHARACTER*1
            Indicates how the vectors which define the elementary
            reflectors are stored:
            = 'C': Columnwise
            = 'R': Rowwise

    M       (input) INTEGER
            The number of rows of the matrix C.

    N       (input) INTEGER
            The number of columns of the matrix C.

    K       (input) INTEGER
            The order of the matrix T (= the number of elementary
            reflectors whose product defines the block reflector).

    V       (input) COMPLEX array, dimension
                                  (LDV,K) if STOREV = 'C'
                                  (LDV,M) if STOREV = 'R' and SIDE = 'L'
                                  (LDV,N) if STOREV = 'R' and SIDE = 'R'
            The matrix V. See further details.

    LDV     (input) INTEGER
            The leading dimension of the array V.
            If STOREV = 'C' and SIDE = 'L', LDV >= max(1,M);
            if STOREV = 'C' and SIDE = 'R', LDV >= max(1,N);
            if STOREV = 'R', LDV >= K.

    T       (input) COMPLEX array, dimension (LDT,K)
            The triangular K-by-K matrix T in the representation of the
            block reflector.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= K.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by H*C or H'*C or C*H or C*H'.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX array, dimension (LDWORK,K)

    LDWORK  (input) INTEGER
            The leading dimension of the array WORK.
            If SIDE = 'L', LDWORK >= max(1,N);
            if SIDE = 'R', LDWORK >= max(1,M).

    =====================================================================


       Quick return if possible
','line_number':18394,'multiline':True]['text':' Parameter adjustments ','line_number':18479,'multiline':True]['text':' Function Body ','line_number':18493,'multiline':True]['text':'
             Let  V =  ( V1 )    (first K rows)
                       ( V2 )
             where  V1  is unit lower triangular.
','line_number':18508,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':18516,'multiline':True]['text':'
                W := C' * V  =  (C1'*V1 + C2'*V2)  (stored in WORK)

                W := C1'
','line_number':18526,'multiline':True]['text':' L10: ','line_number':18537,'multiline':True]['text':'              W := W * V1 ','line_number':18540,'multiline':True]['text':'                 W := W + C2'*V2 ','line_number':18546,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':18555,'multiline':True]['text':'              C := C - V * W' ','line_number':18560,'multiline':True]['text':'                 C2 := C2 - V2 * W' ','line_number':18564,'multiline':True]['text':'              W := W * V1' ','line_number':18574,'multiline':True]['text':'              C1 := C1 - W' ','line_number':18580,'multiline':True]['text':' L20: ','line_number':18592,'multiline':True]['text':' L30: ','line_number':18594,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':18599,'multiline':True]['text':'
                W := C * V  =  (C1*V1 + C2*V2)  (stored in WORK)

                W := C1
','line_number':18608,'multiline':True]['text':' L40: ','line_number':18618,'multiline':True]['text':'              W := W * V1 ','line_number':18621,'multiline':True]['text':'                 W := W + C2 * V2 ','line_number':18627,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':18636,'multiline':True]['text':'              C := C - W * V' ','line_number':18641,'multiline':True]['text':'                 C2 := C2 - W * V2' ','line_number':18645,'multiline':True]['text':'              W := W * V1' ','line_number':18655,'multiline':True]['text':'              C1 := C1 - W ','line_number':18661,'multiline':True]['text':' L50: ','line_number':18673,'multiline':True]['text':' L60: ','line_number':18675,'multiline':True]['text':'
             Let  V =  ( V1 )
                       ( V2 )    (last K rows)
             where  V2  is unit upper triangular.
','line_number':18681,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':18689,'multiline':True]['text':'
                W := C' * V  =  (C1'*V1 + C2'*V2)  (stored in WORK)

                W := C2'
','line_number':18699,'multiline':True]['text':' L70: ','line_number':18710,'multiline':True]['text':'              W := W * V2 ','line_number':18713,'multiline':True]['text':'                 W := W + C1'*V1 ','line_number':18720,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':18728,'multiline':True]['text':'              C := C - V * W' ','line_number':18733,'multiline':True]['text':'                 C1 := C1 - V1 * W' ','line_number':18737,'multiline':True]['text':'              W := W * V2' ','line_number':18746,'multiline':True]['text':'              C2 := C2 - W' ','line_number':18752,'multiline':True]['text':' L80: ','line_number':18764,'multiline':True]['text':' L90: ','line_number':18766,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':18771,'multiline':True]['text':'
                W := C * V  =  (C1*V1 + C2*V2)  (stored in WORK)

                W := C2
','line_number':18780,'multiline':True]['text':' L100: ','line_number':18790,'multiline':True]['text':'              W := W * V2 ','line_number':18793,'multiline':True]['text':'                 W := W + C1 * V1 ','line_number':18800,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':18808,'multiline':True]['text':'              C := C - W * V' ','line_number':18813,'multiline':True]['text':'                 C1 := C1 - W * V1' ','line_number':18817,'multiline':True]['text':'              W := W * V2' ','line_number':18826,'multiline':True]['text':'              C2 := C2 - W ','line_number':18832,'multiline':True]['text':' L110: ','line_number':18844,'multiline':True]['text':' L120: ','line_number':18846,'multiline':True]['text':'
             Let  V =  ( V1  V2 )    (V1: first K columns)
             where  V1  is unit upper triangular.
','line_number':18855,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':18862,'multiline':True]['text':'
                W := C' * V'  =  (C1'*V1' + C2'*V2') (stored in WORK)

                W := C1'
','line_number':18872,'multiline':True]['text':' L130: ','line_number':18883,'multiline':True]['text':'              W := W * V1' ','line_number':18886,'multiline':True]['text':'                 W := W + C2'*V2' ','line_number':18893,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':18903,'multiline':True]['text':'              C := C - V' * W' ','line_number':18908,'multiline':True]['text':'                 C2 := C2 - V2' * W' ','line_number':18912,'multiline':True]['text':'              W := W * V1 ','line_number':18922,'multiline':True]['text':'              C1 := C1 - W' ','line_number':18927,'multiline':True]['text':' L140: ','line_number':18939,'multiline':True]['text':' L150: ','line_number':18941,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':18946,'multiline':True]['text':'
                W := C * V'  =  (C1*V1' + C2*V2')  (stored in WORK)

                W := C1
','line_number':18955,'multiline':True]['text':' L160: ','line_number':18965,'multiline':True]['text':'              W := W * V1' ','line_number':18968,'multiline':True]['text':'                 W := W + C2 * V2' ','line_number':18975,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':18984,'multiline':True]['text':'              C := C - W * V ','line_number':18989,'multiline':True]['text':'                 C2 := C2 - W * V2 ','line_number':18993,'multiline':True]['text':'              W := W * V1 ','line_number':19003,'multiline':True]['text':'              C1 := C1 - W ','line_number':19008,'multiline':True]['text':' L170: ','line_number':19020,'multiline':True]['text':' L180: ','line_number':19022,'multiline':True]['text':'
             Let  V =  ( V1  V2 )    (V2: last K columns)
             where  V2  is unit lower triangular.
','line_number':19029,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':19036,'multiline':True]['text':'
                W := C' * V'  =  (C1'*V1' + C2'*V2') (stored in WORK)

                W := C2'
','line_number':19046,'multiline':True]['text':' L190: ','line_number':19057,'multiline':True]['text':'              W := W * V2' ','line_number':19060,'multiline':True]['text':'                 W := W + C1'*V1' ','line_number':19067,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':19076,'multiline':True]['text':'              C := C - V' * W' ','line_number':19081,'multiline':True]['text':'                 C1 := C1 - V1' * W' ','line_number':19085,'multiline':True]['text':'              W := W * V2 ','line_number':19094,'multiline':True]['text':'              C2 := C2 - W' ','line_number':19100,'multiline':True]['text':' L200: ','line_number':19112,'multiline':True]['text':' L210: ','line_number':19114,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':19119,'multiline':True]['text':'
                W := C * V'  =  (C1*V1' + C2*V2')  (stored in WORK)

                W := C2
','line_number':19128,'multiline':True]['text':' L220: ','line_number':19138,'multiline':True]['text':'              W := W * V2' ','line_number':19141,'multiline':True]['text':'                 W := W + C1 * V1' ','line_number':19148,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':19156,'multiline':True]['text':'              C := C - W * V ','line_number':19161,'multiline':True]['text':'                 C1 := C1 - W * V1 ','line_number':19165,'multiline':True]['text':'              W := W * V2 ','line_number':19174,'multiline':True]['text':'              C1 := C1 - W ','line_number':19180,'multiline':True]['text':' L230: ','line_number':19192,'multiline':True]['text':' L240: ','line_number':19194,'multiline':True]['text':'     End of CLARFB ','line_number':19204,'multiline':True]['text':' clarfb_ ','line_number':19206,'multiline':True]['text':' Subroutine ','line_number':19208,'multiline':True]['text':' System generated locals ','line_number':19211,'multiline':True]['text':' Local variables ','line_number':19216,'multiline':True]['text':' Subroutine ','line_number':19219,'multiline':True]['text':' Complex ','line_number':19224,'multiline':True]['text':' Subroutine ','line_number':19226,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLARFG generates a complex elementary reflector H of order n, such
    that

          H' * ( alpha ) = ( beta ),   H' * H = I.
               (   x   )   (   0  )

    where alpha and beta are scalars, with beta real, and x is an
    (n-1)-element complex vector. H is represented in the form

          H = I - tau * ( 1 ) * ( 1 v' ) ,
                        ( v )

    where tau is a complex scalar and v is a complex (n-1)-element
    vector. Note that H is not hermitian.

    If the elements of x are all zero and alpha is real, then tau = 0
    and H is taken to be the unit matrix.

    Otherwise  1 <= real(tau) <= 2  and  abs(tau-1) <= 1 .

    Arguments
    =========

    N       (input) INTEGER
            The order of the elementary reflector.

    ALPHA   (input/output) COMPLEX
            On entry, the value alpha.
            On exit, it is overwritten with the value beta.

    X       (input/output) COMPLEX array, dimension
                           (1+(N-2)*abs(INCX))
            On entry, the vector x.
            On exit, it is overwritten with the vector v.

    INCX    (input) INTEGER
            The increment between elements of X. INCX > 0.

    TAU     (output) COMPLEX
            The value tau.

    =====================================================================
','line_number':19231,'multiline':True]['text':' Parameter adjustments ','line_number':19286,'multiline':True]['text':' Function Body ','line_number':19289,'multiline':True]['text':'        H  =  I ','line_number':19302,'multiline':True]['text':'        general case ','line_number':19307,'multiline':True]['text':'           XNORM, BETA may be inaccurate; scale X and recompute them ','line_number':19317,'multiline':True]['text':'           New BETA is at most 1, at least SAFMIN ','line_number':19330,'multiline':True]['text':'        If ALPHA is subnormal, it may lose relative accuracy ','line_number':19349,'multiline':True]['text':' L20: ','line_number':19354,'multiline':True]['text':'     End of CLARFG ','line_number':19361,'multiline':True]['text':' clarfg_ ','line_number':19363,'multiline':True]['text':' Subroutine ','line_number':19365,'multiline':True]['text':' System generated locals ','line_number':19368,'multiline':True]['text':' Local variables ','line_number':19372,'multiline':True]['text':' Subroutine ','line_number':19375,'multiline':True]['text':' Subroutine ','line_number':19380,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLARFT forms the triangular factor T of a complex block reflector H
    of order n, which is defined as a product of k elementary reflectors.

    If DIRECT = 'F', H = H(1) H(2) . . . H(k) and T is upper triangular;

    If DIRECT = 'B', H = H(k) . . . H(2) H(1) and T is lower triangular.

    If STOREV = 'C', the vector which defines the elementary reflector
    H(i) is stored in the i-th column of the array V, and

       H  =  I - V * T * V'

    If STOREV = 'R', the vector which defines the elementary reflector
    H(i) is stored in the i-th row of the array V, and

       H  =  I - V' * T * V

    Arguments
    =========

    DIRECT  (input) CHARACTER*1
            Specifies the order in which the elementary reflectors are
            multiplied to form the block reflector:
            = 'F': H = H(1) H(2) . . . H(k) (Forward)
            = 'B': H = H(k) . . . H(2) H(1) (Backward)

    STOREV  (input) CHARACTER*1
            Specifies how the vectors which define the elementary
            reflectors are stored (see also Further Details):
            = 'C': columnwise
            = 'R': rowwise

    N       (input) INTEGER
            The order of the block reflector H. N >= 0.

    K       (input) INTEGER
            The order of the triangular factor T (= the number of
            elementary reflectors). K >= 1.

    V       (input/output) COMPLEX array, dimension
                                 (LDV,K) if STOREV = 'C'
                                 (LDV,N) if STOREV = 'R'
            The matrix V. See further details.

    LDV     (input) INTEGER
            The leading dimension of the array V.
            If STOREV = 'C', LDV >= max(1,N); if STOREV = 'R', LDV >= K.

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i).

    T       (output) COMPLEX array, dimension (LDT,K)
            The k by k triangular factor T of the block reflector.
            If DIRECT = 'F', T is upper triangular; if DIRECT = 'B', T is
            lower triangular. The rest of the array is not used.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= K.

    Further Details
    ===============

    The shape of the matrix V and the storage of the vectors which define
    the H(i) is best illustrated by the following example with n = 5 and
    k = 3. The elements equal to 1 are not stored; the corresponding
    array elements are modified but restored on exit. The rest of the
    array is not used.

    DIRECT = 'F' and STOREV = 'C':         DIRECT = 'F' and STOREV = 'R':

                 V = (  1       )                 V = (  1 v1 v1 v1 v1 )
                     ( v1  1    )                     (     1 v2 v2 v2 )
                     ( v1 v2  1 )                     (        1 v3 v3 )
                     ( v1 v2 v3 )
                     ( v1 v2 v3 )

    DIRECT = 'B' and STOREV = 'C':         DIRECT = 'B' and STOREV = 'R':

                 V = ( v1 v2 v3 )                 V = ( v1 v1  1       )
                     ( v1 v2 v3 )                     ( v2 v2 v2  1    )
                     (  1 v2 v3 )                     ( v3 v3 v3 v3  1 )
                     (     1 v3 )
                     (        1 )

    =====================================================================


       Quick return if possible
','line_number':19384,'multiline':True]['text':' Parameter adjustments ','line_number':19485,'multiline':True]['text':' Function Body ','line_number':19494,'multiline':True]['text':'              H(i)  =  I ','line_number':19507,'multiline':True]['text':' L10: ','line_number':19513,'multiline':True]['text':'              general case ','line_number':19517,'multiline':True]['text':'                 Skip any trailing zeros. ','line_number':19524,'multiline':True]['text':'                 T(1:i-1,i) := - tau(i) * V(i:j,1:i-1)' * V(i:j,i) ','line_number':19535,'multiline':True]['text':'                 Skip any trailing zeros. ','line_number':19545,'multiline':True]['text':'                 T(1:i-1,i) := - tau(i) * V(1:i-1,i:j) * V(i,i:j)' ','line_number':19556,'multiline':True]['text':'              T(1:i-1,i) := T(1:i-1,1:i-1) * T(1:i-1,i) ','line_number':19577,'multiline':True]['text':' L20: ','line_number':19591,'multiline':True]['text':'              H(i)  =  I ','line_number':19599,'multiline':True]['text':' L30: ','line_number':19605,'multiline':True]['text':'              general case ','line_number':19609,'multiline':True]['text':'                    Skip any leading zeros. ','line_number':19617,'multiline':True]['text':'
                      T(i+1:k,i) :=
                              - tau(i) * V(j:n-k+i,i+1:k)' * V(j:n-k+i,i)
','line_number':19628,'multiline':True]['text':'                    Skip any leading zeros. ','line_number':19648,'multiline':True]['text':'
                      T(i+1:k,i) :=
                              - tau(i) * V(i+1:k,j:n-k+i) * V(i,j:n-k+i)'
','line_number':19659,'multiline':True]['text':'                 T(i+1:k,i) := T(i+1:k,i+1:k) * T(i+1:k,i) ','line_number':19680,'multiline':True]['text':' L40: ','line_number':19697,'multiline':True]['text':'     End of CLARFT ','line_number':19702,'multiline':True]['text':' clarft_ ','line_number':19704,'multiline':True]['text':' Subroutine ','line_number':19706,'multiline':True]['text':' System generated locals ','line_number':19709,'multiline':True]['text':' Local variables ','line_number':19714,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLARTG generates a plane rotation so that

       [  CS  SN  ]     [ F ]     [ R ]
       [  __      ]  .  [   ]  =  [   ]   where CS**2 + |SN|**2 = 1.
       [ -SN  CS  ]     [ G ]     [ 0 ]

    This is a faster version of the BLAS1 routine CROTG, except for
    the following differences:
       F and G are unchanged on return.
       If G=0, then CS=1 and SN=0.
       If F=0, then CS=0 and SN is chosen so that R is real.

    Arguments
    =========

    F       (input) COMPLEX
            The first component of vector to be rotated.

    G       (input) COMPLEX
            The second component of vector to be rotated.

    CS      (output) REAL
            The cosine of the rotation.

    SN      (output) COMPLEX
            The sine of the rotation.

    R       (output) COMPLEX
            The nonzero component of the rotated vector.

    Further Details
    ======= =======

    3-5-96 - Modified with a new algorithm by W. Kahan and J. Demmel

    This version has a few statements commented out for thread safety
    (machine parameters are computed on each entry). 10 feb 03, SJH.

    =====================================================================

       LOGICAL            FIRST
       SAVE               FIRST, SAFMX2, SAFMIN, SAFMN2
       DATA               FIRST / .TRUE. /

       IF( FIRST ) THEN
','line_number':19728,'multiline':True]['text':'
          FIRST = .FALSE.
       END IF
   Computing MAX
   Computing MAX
','line_number':19790,'multiline':True]['text':' Computing MAX ','line_number':19797,'multiline':True]['text':' Computing 2nd power ','line_number':19833,'multiline':True]['text':' Computing 2nd power ','line_number':19835,'multiline':True]['text':' Computing 2nd power ','line_number':19838,'multiline':True]['text':' Computing 2nd power ','line_number':19840,'multiline':True]['text':'        This is a rare case: F is very small. ','line_number':19845,'multiline':True]['text':'           Do complex/real division explicitly with two real divisions ','line_number':19853,'multiline':True]['text':'
          G2 and G2S are accurate
          G2 is at least SAFMIN, and G2S is at least SAFMN2
','line_number':19866,'multiline':True]['text':'
          Error in CS from underflow in F2S is at most
          UNFL / SAFMN2 .lt. sqrt(UNFL*EPS) .lt. EPS
          If MAX(G2,ONE)=G2, then F2 .lt. G2*SAFMIN,
          and so CS .lt. sqrt(SAFMIN)
          If MAX(G2,ONE)=ONE, then F2 .lt. SAFMIN
          and so CS .lt. sqrt(SAFMIN)/SAFMN2 = sqrt(EPS)
          Therefore, CS = F2S/G2S / sqrt( 1 + (F2S/G2S)**2 ) = F2S/G2S
','line_number':19871,'multiline':True]['text':'
          Make sure abs(FF) = 1
          Do complex/real division explicitly with 2 real divisions
   Computing MAX
','line_number':19881,'multiline':True]['text':'
          This is the most common case.
          Neither F2 nor F2/G2 are less than SAFMIN
          F2S cannot overflow, and it is accurate
','line_number':19918,'multiline':True]['text':'        Do the F2S(real)*FS(complex) multiply with two real multiplies ','line_number':19925,'multiline':True]['text':'        Do complex/real division explicitly with two real divisions ','line_number':19932,'multiline':True]['text':' L30: ','line_number':19947,'multiline':True]['text':' L40: ','line_number':19954,'multiline':True]['text':'     End of CLARTG ','line_number':19961,'multiline':True]['text':' clartg_ ','line_number':19963,'multiline':True]['text':' Subroutine ','line_number':19965,'multiline':True]['text':' System generated locals ','line_number':19969,'multiline':True]['text':' Local variables ','line_number':19973,'multiline':True]['text':' Subroutine ','line_number':19983,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLASCL multiplies the M by N complex matrix A by the real scalar
    CTO/CFROM.  This is done without over/underflow as long as the final
    result CTO*A(I,J)/CFROM does not over/underflow. TYPE specifies that
    A may be full, upper triangular, lower triangular, upper Hessenberg,
    or banded.

    Arguments
    =========

    TYPE    (input) CHARACTER*1
            TYPE indices the storage type of the input matrix.
            = 'G':  A is a full matrix.
            = 'L':  A is a lower triangular matrix.
            = 'U':  A is an upper triangular matrix.
            = 'H':  A is an upper Hessenberg matrix.
            = 'B':  A is a symmetric band matrix with lower bandwidth KL
                    and upper bandwidth KU and with the only the lower
                    half stored.
            = 'Q':  A is a symmetric band matrix with lower bandwidth KL
                    and upper bandwidth KU and with the only the upper
                    half stored.
            = 'Z':  A is a band matrix with lower bandwidth KL and upper
                    bandwidth KU.

    KL      (input) INTEGER
            The lower bandwidth of A.  Referenced only if TYPE = 'B',
            'Q' or 'Z'.

    KU      (input) INTEGER
            The upper bandwidth of A.  Referenced only if TYPE = 'B',
            'Q' or 'Z'.

    CFROM   (input) REAL
    CTO     (input) REAL
            The matrix A is multiplied by CTO/CFROM. A(I,J) is computed
            without over/underflow if the final result CTO*A(I,J)/CFROM
            can be represented without over/underflow.  CFROM must be
            nonzero.

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            The matrix to be multiplied by CTO/CFROM.  See TYPE for the
            storage type.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    INFO    (output) INTEGER
            0  - successful exit
            <0 - if INFO = -i, the i-th argument had an illegal value.

    =====================================================================


       Test the input arguments
','line_number':19989,'multiline':True]['text':' Parameter adjustments ','line_number':20061,'multiline':True]['text':' Function Body ','line_number':20066,'multiline':True]['text':' Computing MAX ','line_number':20100,'multiline':True]['text':' if(complicated condition) ','line_number':20104,'multiline':True]['text':' Computing MAX ','line_number':20105,'multiline':True]['text':'     Quick return if possible ','line_number':20123,'multiline':True]['text':'     Get machine parameters ','line_number':20129,'multiline':True]['text':'
          CFROMC is an inf.  Multiply by a correctly signed zero for
          finite CTOC, or a NaN if CTOC is infinite.
','line_number':20140,'multiline':True]['text':'
             CTOC is either 0 or an inf.  In both cases, CTOC itself
             serves as the correct multiplication factor.
','line_number':20150,'multiline':True]['text':'        Full matrix ','line_number':20173,'multiline':True]['text':' L20: ','line_number':20183,'multiline':True]['text':' L30: ','line_number':20185,'multiline':True]['text':'        Lower triangular matrix ','line_number':20190,'multiline':True]['text':' L40: ','line_number':20200,'multiline':True]['text':' L50: ','line_number':20202,'multiline':True]['text':'        Upper triangular matrix ','line_number':20207,'multiline':True]['text':' L60: ','line_number':20217,'multiline':True]['text':' L70: ','line_number':20219,'multiline':True]['text':'        Upper Hessenberg matrix ','line_number':20224,'multiline':True]['text':' Computing MIN ','line_number':20228,'multiline':True]['text':' L80: ','line_number':20236,'multiline':True]['text':' L90: ','line_number':20238,'multiline':True]['text':'        Lower half of a symmetric band matrix ','line_number':20243,'multiline':True]['text':' Computing MIN ','line_number':20249,'multiline':True]['text':' L100: ','line_number':20257,'multiline':True]['text':' L110: ','line_number':20259,'multiline':True]['text':'        Upper half of a symmetric band matrix ','line_number':20264,'multiline':True]['text':' Computing MAX ','line_number':20270,'multiline':True]['text':' L120: ','line_number':20278,'multiline':True]['text':' L130: ','line_number':20280,'multiline':True]['text':'        Band matrix ','line_number':20285,'multiline':True]['text':' Computing MAX ','line_number':20293,'multiline':True]['text':' Computing MIN ','line_number':20295,'multiline':True]['text':' L140: ','line_number':20303,'multiline':True]['text':' L150: ','line_number':20305,'multiline':True]['text':'     End of CLASCL ','line_number':20316,'multiline':True]['text':' clascl_ ','line_number':20318,'multiline':True]['text':' Subroutine ','line_number':20320,'multiline':True]['text':' System generated locals ','line_number':20323,'multiline':True]['text':' Local variables ','line_number':20326,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLASET initializes a 2-D array A to BETA on the diagonal and
    ALPHA on the offdiagonals.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies the part of the matrix A to be set.
            = 'U':      Upper triangular part is set. The lower triangle
                        is unchanged.
            = 'L':      Lower triangular part is set. The upper triangle
                        is unchanged.
            Otherwise:  All of the matrix A is set.

    M       (input) INTEGER
            On entry, M specifies the number of rows of A.

    N       (input) INTEGER
            On entry, N specifies the number of columns of A.

    ALPHA   (input) COMPLEX
            All the offdiagonal array elements are set to ALPHA.

    BETA    (input) COMPLEX
            All the diagonal array elements are set to BETA.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the m by n matrix A.
            On exit, A(i,j) = ALPHA, 1 <= i <= m, 1 <= j <= n, i.ne.j;
                     A(i,i) = BETA , 1 <= i <= min(m,n)

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    =====================================================================
','line_number':20331,'multiline':True]['text':' Parameter adjustments ','line_number':20379,'multiline':True]['text':' Function Body ','line_number':20384,'multiline':True]['text':'
          Set the diagonal to BETA and the strictly upper triangular
          part of the array to ALPHA.
','line_number':20387,'multiline':True]['text':' Computing MIN ','line_number':20394,'multiline':True]['text':' L10: ','line_number':20400,'multiline':True]['text':' L20: ','line_number':20402,'multiline':True]['text':' L30: ','line_number':20408,'multiline':True]['text':'
          Set the diagonal to BETA and the strictly lower triangular
          part of the array to ALPHA.
','line_number':20413,'multiline':True]['text':' L40: ','line_number':20424,'multiline':True]['text':' L50: ','line_number':20426,'multiline':True]['text':' L60: ','line_number':20432,'multiline':True]['text':'
          Set the array to BETA on the diagonal and ALPHA on the
          offdiagonal.
','line_number':20437,'multiline':True]['text':' L70: ','line_number':20448,'multiline':True]['text':' L80: ','line_number':20450,'multiline':True]['text':' L90: ','line_number':20456,'multiline':True]['text':'     End of CLASET ','line_number':20462,'multiline':True]['text':' claset_ ','line_number':20464,'multiline':True]['text':' Subroutine ','line_number':20466,'multiline':True]['text':' System generated locals ','line_number':20469,'multiline':True]['text':' Local variables ','line_number':20473,'multiline':True]['text':' Subroutine ','line_number':20478,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLASR applies a sequence of real plane rotations to a singlecomplex matrix
    A, from either the left or the right.

    When SIDE = 'L', the transformation takes the form

       A := P*A

    and when SIDE = 'R', the transformation takes the form

       A := A*P**T

    where P is an orthogonal matrix consisting of a sequence of z plane
    rotations, with z = M when SIDE = 'L' and z = N when SIDE = 'R',
    and P**T is the transpose of P.

    When DIRECT = 'F' (Forward sequence), then

       P = P(z-1) * ... * P(2) * P(1)

    and when DIRECT = 'B' (Backward sequence), then

       P = P(1) * P(2) * ... * P(z-1)

    where P(k) is a plane rotation matrix defined by the 2-by-2 rotation

       R(k) = (  c(k)  s(k) )
            = ( -s(k)  c(k) ).

    When PIVOT = 'V' (Variable pivot), the rotation is performed
    for the plane (k,k+1), i.e., P(k) has the form

       P(k) = (  1                                            )
              (       ...                                     )
              (              1                                )
              (                   c(k)  s(k)                  )
              (                  -s(k)  c(k)                  )
              (                                1              )
              (                                     ...       )
              (                                            1  )

    where R(k) appears as a rank-2 modification to the identity matrix in
    rows and columns k and k+1.

    When PIVOT = 'T' (Top pivot), the rotation is performed for the
    plane (1,k+1), so P(k) has the form

       P(k) = (  c(k)                    s(k)                 )
              (         1                                     )
              (              ...                              )
              (                     1                         )
              ( -s(k)                    c(k)                 )
              (                                 1             )
              (                                      ...      )
              (                                             1 )

    where R(k) appears in rows and columns 1 and k+1.

    Similarly, when PIVOT = 'B' (Bottom pivot), the rotation is
    performed for the plane (k,z), giving P(k) the form

       P(k) = ( 1                                             )
              (      ...                                      )
              (             1                                 )
              (                  c(k)                    s(k) )
              (                         1                     )
              (                              ...              )
              (                                     1         )
              (                 -s(k)                    c(k) )

    where R(k) appears in rows and columns k and z.  The rotations are
    performed without ever forming P(k) explicitly.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            Specifies whether the plane rotation matrix P is applied to
            A on the left or the right.
            = 'L':  Left, compute A := P*A
            = 'R':  Right, compute A:= A*P**T

    PIVOT   (input) CHARACTER*1
            Specifies the plane for which P(k) is a plane rotation
            matrix.
            = 'V':  Variable pivot, the plane (k,k+1)
            = 'T':  Top pivot, the plane (1,k+1)
            = 'B':  Bottom pivot, the plane (k,z)

    DIRECT  (input) CHARACTER*1
            Specifies whether P is a forward or backward sequence of
            plane rotations.
            = 'F':  Forward, P = P(z-1)*...*P(2)*P(1)
            = 'B':  Backward, P = P(1)*P(2)*...*P(z-1)

    M       (input) INTEGER
            The number of rows of the matrix A.  If m <= 1, an immediate
            return is effected.

    N       (input) INTEGER
            The number of columns of the matrix A.  If n <= 1, an
            immediate return is effected.

    C       (input) REAL array, dimension
                    (M-1) if SIDE = 'L'
                    (N-1) if SIDE = 'R'
            The cosines c(k) of the plane rotations.

    S       (input) REAL array, dimension
                    (M-1) if SIDE = 'L'
                    (N-1) if SIDE = 'R'
            The sines s(k) of the plane rotations.  The 2-by-2 plane
            rotation part of the matrix P(k), R(k), has the form
            R(k) = (  c(k)  s(k) )
                   ( -s(k)  c(k) ).

    A       (input/output) COMPLEX array, dimension (LDA,N)
            The M-by-N matrix A.  On exit, A is overwritten by P*A if
            SIDE = 'R' or by A*P**T if SIDE = 'L'.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    =====================================================================


       Test the input parameters
','line_number':20481,'multiline':True]['text':' Parameter adjustments ','line_number':20619,'multiline':True]['text':' Function Body ','line_number':20626,'multiline':True]['text':'     Quick return if possible ','line_number':20648,'multiline':True]['text':'        Form  P * A ','line_number':20655,'multiline':True]['text':' L10: ','line_number':20684,'multiline':True]['text':' L20: ','line_number':20687,'multiline':True]['text':' L30: ','line_number':20714,'multiline':True]['text':' L40: ','line_number':20717,'multiline':True]['text':' L50: ','line_number':20747,'multiline':True]['text':' L60: ','line_number':20750,'multiline':True]['text':' L70: ','line_number':20777,'multiline':True]['text':' L80: ','line_number':20780,'multiline':True]['text':' L90: ','line_number':20810,'multiline':True]['text':' L100: ','line_number':20813,'multiline':True]['text':' L110: ','line_number':20840,'multiline':True]['text':' L120: ','line_number':20843,'multiline':True]['text':'        Form A * P' ','line_number':20849,'multiline':True]['text':' L130: ','line_number':20878,'multiline':True]['text':' L140: ','line_number':20881,'multiline':True]['text':' L150: ','line_number':20908,'multiline':True]['text':' L160: ','line_number':20911,'multiline':True]['text':' L170: ','line_number':20941,'multiline':True]['text':' L180: ','line_number':20944,'multiline':True]['text':' L190: ','line_number':20971,'multiline':True]['text':' L200: ','line_number':20974,'multiline':True]['text':' L210: ','line_number':21004,'multiline':True]['text':' L220: ','line_number':21007,'multiline':True]['text':' L230: ','line_number':21034,'multiline':True]['text':' L240: ','line_number':21037,'multiline':True]['text':'     End of CLASR ','line_number':21045,'multiline':True]['text':' clasr_ ','line_number':21047,'multiline':True]['text':' Subroutine ','line_number':21049,'multiline':True]['text':' System generated locals ','line_number':21052,'multiline':True]['text':' Local variables ','line_number':21056,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLASSQ returns the values scl and ssq such that

       ( scl**2 )*ssq = x( 1 )**2 +...+ x( n )**2 + ( scale**2 )*sumsq,

    where x( i ) = abs( X( 1 + ( i - 1 )*INCX ) ). The value of sumsq is
    assumed to be at least unity and the value of ssq will then satisfy

       1.0 .le. ssq .le. ( sumsq + 2*n ).

    scale is assumed to be non-negative and scl returns the value

       scl = max( scale, abs( real( x( i ) ) ), abs( aimag( x( i ) ) ) ),
              i

    scale and sumsq must be supplied in SCALE and SUMSQ respectively.
    SCALE and SUMSQ are overwritten by scl and ssq respectively.

    The routine makes only one pass through the vector X.

    Arguments
    =========

    N       (input) INTEGER
            The number of elements to be used from the vector X.

    X       (input) COMPLEX array, dimension (N)
            The vector x as described above.
               x( i )  = X( 1 + ( i - 1 )*INCX ), 1 <= i <= n.

    INCX    (input) INTEGER
            The increment between successive values of the vector X.
            INCX > 0.

    SCALE   (input/output) REAL
            On entry, the value  scale  in the equation above.
            On exit, SCALE is overwritten with the value  scl .

    SUMSQ   (input/output) REAL
            On entry, the value  sumsq  in the equation above.
            On exit, SUMSQ is overwritten with the value  ssq .

   =====================================================================
','line_number':21061,'multiline':True]['text':' Parameter adjustments ','line_number':21116,'multiline':True]['text':' Function Body ','line_number':21119,'multiline':True]['text':' Computing 2nd power ','line_number':21129,'multiline':True]['text':' Computing 2nd power ','line_number':21134,'multiline':True]['text':' Computing 2nd power ','line_number':21142,'multiline':True]['text':' Computing 2nd power ','line_number':21147,'multiline':True]['text':' L10: ','line_number':21152,'multiline':True]['text':'     End of CLASSQ ','line_number':21158,'multiline':True]['text':' classq_ ','line_number':21160,'multiline':True]['text':' Subroutine ','line_number':21162,'multiline':True]['text':' System generated locals ','line_number':21165,'multiline':True]['text':' Local variables ','line_number':21168,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLASWP performs a series of row interchanges on the matrix A.
    One row interchange is initiated for each of rows K1 through K2 of A.

    Arguments
    =========

    N       (input) INTEGER
            The number of columns of the matrix A.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the matrix of column dimension N to which the row
            interchanges will be applied.
            On exit, the permuted matrix.

    LDA     (input) INTEGER
            The leading dimension of the array A.

    K1      (input) INTEGER
            The first element of IPIV for which a row interchange will
            be done.

    K2      (input) INTEGER
            The last element of IPIV for which a row interchange will
            be done.

    IPIV    (input) INTEGER array, dimension (K2*abs(INCX))
            The vector of pivot indices.  Only the elements in positions
            K1 through K2 of IPIV are accessed.
            IPIV(K) = L implies rows K and L are to be interchanged.

    INCX    (input) INTEGER
            The increment between successive values of IPIV.  If IPIV
            is negative, the pivots are applied in reverse order.

    Further Details
    ===============

    Modified by
     R. C. Whaley, Computer Science Dept., Univ. of Tenn., Knoxville, USA

   =====================================================================


       Interchange row I with row IPIV(I) for each of rows K1 through K2.
','line_number':21173,'multiline':True]['text':' Parameter adjustments ','line_number':21229,'multiline':True]['text':' Function Body ','line_number':21235,'multiline':True]['text':' L10: ','line_number':21270,'multiline':True]['text':' L20: ','line_number':21274,'multiline':True]['text':' L30: ','line_number':21276,'multiline':True]['text':' L40: ','line_number':21296,'multiline':True]['text':' L50: ','line_number':21300,'multiline':True]['text':'     End of CLASWP ','line_number':21306,'multiline':True]['text':' claswp_ ','line_number':21308,'multiline':True]['text':' Subroutine ','line_number':21310,'multiline':True]['text':' System generated locals ','line_number':21313,'multiline':True]['text':' Local variables ','line_number':21318,'multiline':True]['text':' Subroutine ','line_number':21321,'multiline':True]['text':' Complex ','line_number':21323,'multiline':True]['text':' Subroutine ','line_number':21325,'multiline':True]['text':' Subroutine ','line_number':21331,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLATRD reduces NB rows and columns of a complex Hermitian matrix A to
    Hermitian tridiagonal form by a unitary similarity
    transformation Q' * A * Q, and returns the matrices V and W which are
    needed to apply the transformation to the unreduced part of A.

    If UPLO = 'U', CLATRD reduces the last NB rows and columns of a
    matrix, of which the upper triangle is supplied;
    if UPLO = 'L', CLATRD reduces the first NB rows and columns of a
    matrix, of which the lower triangle is supplied.

    This is an auxiliary routine called by CHETRD.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            Hermitian matrix A is stored:
            = 'U': Upper triangular
            = 'L': Lower triangular

    N       (input) INTEGER
            The order of the matrix A.

    NB      (input) INTEGER
            The number of rows and columns to be reduced.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            n-by-n upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n-by-n lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.
            On exit:
            if UPLO = 'U', the last NB columns have been reduced to
              tridiagonal form, with the diagonal elements overwriting
              the diagonal elements of A; the elements above the diagonal
              with the array TAU, represent the unitary matrix Q as a
              product of elementary reflectors;
            if UPLO = 'L', the first NB columns have been reduced to
              tridiagonal form, with the diagonal elements overwriting
              the diagonal elements of A; the elements below the diagonal
              with the array TAU, represent the  unitary matrix Q as a
              product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    E       (output) REAL array, dimension (N-1)
            If UPLO = 'U', E(n-nb:n-1) contains the superdiagonal
            elements of the last NB columns of the reduced matrix;
            if UPLO = 'L', E(1:nb) contains the subdiagonal elements of
            the first NB columns of the reduced matrix.

    TAU     (output) COMPLEX array, dimension (N-1)
            The scalar factors of the elementary reflectors, stored in
            TAU(n-nb:n-1) if UPLO = 'U', and in TAU(1:nb) if UPLO = 'L'.
            See Further Details.

    W       (output) COMPLEX array, dimension (LDW,NB)
            The n-by-nb matrix W required to update the unreduced part
            of A.

    LDW     (input) INTEGER
            The leading dimension of the array W. LDW >= max(1,N).

    Further Details
    ===============

    If UPLO = 'U', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(n) H(n-1) . . . H(n-nb+1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(i:n) = 0 and v(i-1) = 1; v(1:i-1) is stored on exit in A(1:i-1,i),
    and tau in TAU(i-1).

    If UPLO = 'L', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(1) H(2) . . . H(nb).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0 and v(i+1) = 1; v(i+1:n) is stored on exit in A(i+1:n,i),
    and tau in TAU(i).

    The elements of the vectors v together form the n-by-nb matrix V
    which is needed, with W, to apply the transformation to the unreduced
    part of the matrix, using a Hermitian rank-2k update of the form:
    A := A - V*W' - W*V'.

    The contents of A on exit are illustrated by the following examples
    with n = 5 and nb = 2:

    if UPLO = 'U':                       if UPLO = 'L':

      (  a   a   a   v4  v5 )              (  d                  )
      (      a   a   v4  v5 )              (  1   d              )
      (          a   1   v5 )              (  v1  1   a          )
      (              d   1  )              (  v1  v2  a   a      )
      (                  d  )              (  v1  v2  a   a   a  )

    where d denotes a diagonal element of the reduced matrix, a denotes
    an element of the original matrix that is unchanged, and vi denotes
    an element of the vector defining H(i).

    =====================================================================


       Quick return if possible
','line_number':21337,'multiline':True]['text':' Parameter adjustments ','line_number':21471,'multiline':True]['text':' Function Body ','line_number':21481,'multiline':True]['text':'        Reduce last NB columns of upper triangle ','line_number':21488,'multiline':True]['text':'              Update A(1:i,i) ','line_number':21495,'multiline':True]['text':'
                Generate elementary reflector H(i) to annihilate
                A(1:i-2,i)
','line_number':21526,'multiline':True]['text':'              Compute W(1:i-1,i) ','line_number':21541,'multiline':True]['text':' L10: ','line_number':21588,'multiline':True]['text':'        Reduce first NB columns of lower triangle ','line_number':21592,'multiline':True]['text':'           Update A(i:n,i) ','line_number':21597,'multiline':True]['text':'
                Generate elementary reflector H(i) to annihilate
                A(i+2:n,i)
','line_number':21629,'multiline':True]['text':' Computing MIN ','line_number':21637,'multiline':True]['text':'              Compute W(i+1:n,i) ','line_number':21646,'multiline':True]['text':' L20: ','line_number':21691,'multiline':True]['text':'     End of CLATRD ','line_number':21697,'multiline':True]['text':' clatrd_ ','line_number':21699,'multiline':True]['text':' Subroutine ','line_number':21701,'multiline':True]['text':' System generated locals ','line_number':21705,'multiline':True]['text':' Local variables ','line_number':21710,'multiline':True]['text':' Complex ','line_number':21719,'multiline':True]['text':' Subroutine ','line_number':21722,'multiline':True]['text':' Complex ','line_number':21726,'multiline':True]['text':' Subroutine ','line_number':21729,'multiline':True]['text':' Subroutine ','line_number':21732,'multiline':True]['text':' Complex ','line_number':21735,'multiline':True]['text':' Subroutine ','line_number':21737,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLATRS solves one of the triangular systems

       A * x = s*b,  A**T * x = s*b,  or  A**H * x = s*b,

    with scaling to prevent overflow.  Here A is an upper or lower
    triangular matrix, A**T denotes the transpose of A, A**H denotes the
    conjugate transpose of A, x and b are n-element vectors, and s is a
    scaling factor, usually less than or equal to 1, chosen so that the
    components of x will be less than the overflow threshold.  If the
    unscaled problem will not cause overflow, the Level 2 BLAS routine
    CTRSV is called. If the matrix A is singular (A(j,j) = 0 for some j),
    then s is set to 0 and a non-trivial solution to A*x = 0 is returned.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the matrix A is upper or lower triangular.
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    TRANS   (input) CHARACTER*1
            Specifies the operation applied to A.
            = 'N':  Solve A * x = s*b     (No transpose)
            = 'T':  Solve A**T * x = s*b  (Transpose)
            = 'C':  Solve A**H * x = s*b  (Conjugate transpose)

    DIAG    (input) CHARACTER*1
            Specifies whether or not the matrix A is unit triangular.
            = 'N':  Non-unit triangular
            = 'U':  Unit triangular

    NORMIN  (input) CHARACTER*1
            Specifies whether CNORM has been set or not.
            = 'Y':  CNORM contains the column norms on entry
            = 'N':  CNORM is not set on entry.  On exit, the norms will
                    be computed and stored in CNORM.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input) COMPLEX array, dimension (LDA,N)
            The triangular matrix A.  If UPLO = 'U', the leading n by n
            upper triangular part of the array A contains the upper
            triangular matrix, and the strictly lower triangular part of
            A is not referenced.  If UPLO = 'L', the leading n by n lower
            triangular part of the array A contains the lower triangular
            matrix, and the strictly upper triangular part of A is not
            referenced.  If DIAG = 'U', the diagonal elements of A are
            also not referenced and are assumed to be 1.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max (1,N).

    X       (input/output) COMPLEX array, dimension (N)
            On entry, the right hand side b of the triangular system.
            On exit, X is overwritten by the solution vector x.

    SCALE   (output) REAL
            The scaling factor s for the triangular system
               A * x = s*b,  A**T * x = s*b,  or  A**H * x = s*b.
            If SCALE = 0, the matrix A is singular or badly scaled, and
            the vector x is an exact or approximate solution to A*x = 0.

    CNORM   (input or output) REAL array, dimension (N)

            If NORMIN = 'Y', CNORM is an input argument and CNORM(j)
            contains the norm of the off-diagonal part of the j-th column
            of A.  If TRANS = 'N', CNORM(j) must be greater than or equal
            to the infinity-norm, and if TRANS = 'T' or 'C', CNORM(j)
            must be greater than or equal to the 1-norm.

            If NORMIN = 'N', CNORM is an output argument and CNORM(j)
            returns the 1-norm of the offdiagonal part of the j-th column
            of A.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -k, the k-th argument had an illegal value

    Further Details
    ======= =======

    A rough bound on x is computed; if that is less than overflow, CTRSV
    is called, otherwise, specific code is used which checks for possible
    overflow or divide-by-zero at every operation.

    A columnwise scheme is used for solving A*x = b.  The basic algorithm
    if A is lower triangular is

         x[1:n] := b[1:n]
         for j = 1, ..., n
              x(j) := x(j) / A(j,j)
              x[j+1:n] := x[j+1:n] - x(j) * A[j+1:n,j]
         end

    Define bounds on the components of x after j iterations of the loop:
       M(j) = bound on x[1:j]
       G(j) = bound on x[j+1:n]
    Initially, let M(0) = 0 and G(0) = max{x(i), i=1,...,n}.

    Then for iteration j+1 we have
       M(j+1) <= G(j) / | A(j+1,j+1) |
       G(j+1) <= G(j) + M(j+1) * | A[j+2:n,j+1] |
              <= G(j) ( 1 + CNORM(j+1) / | A(j+1,j+1) | )

    where CNORM(j+1) is greater than or equal to the infinity-norm of
    column j+1 of A, not counting the diagonal.  Hence

       G(j) <= G(0) product ( 1 + CNORM(i) / | A(i,i) | )
                    1<=i<=j
    and

       |x(j)| <= ( G(0) / |A(j,j)| ) product ( 1 + CNORM(i) / |A(i,i)| )
                                     1<=i< j

    Since |x(j)| <= M(j), we use the Level 2 BLAS routine CTRSV if the
    reciprocal of the largest M(j), j=1,..,n, is larger than
    max(underflow, 1/overflow).

    The bound on x(j) is also used to determine when a step in the
    columnwise method can be performed without fear of overflow.  If
    the computed bound is greater than a large constant, x is scaled to
    prevent overflow, but if the bound overflows, x is set to 0, x(j) to
    1, and scale to 0, and a non-trivial solution to A*x = 0 is found.

    Similarly, a row-wise scheme is used to solve A**T *x = b  or
    A**H *x = b.  The basic algorithm for A upper triangular is

         for j = 1, ..., n
              x(j) := ( b(j) - A[1:j-1,j]' * x[1:j-1] ) / A(j,j)
         end

    We simultaneously compute two bounds
         G(j) = bound on ( b(i) - A[1:i-1,i]' * x[1:i-1] ), 1<=i<=j
         M(j) = bound on x(i), 1<=i<=j

    The initial values are G(0) = 0, M(0) = max{b(i), i=1,..,n}, and we
    add the constraint G(j) >= G(j-1) and M(j) >= M(j-1) for j >= 1.
    Then the bound on x(j) is

         M(j) <= M(j-1) * ( 1 + CNORM(j) ) / | A(j,j) |

              <= M(0) * product ( ( 1 + CNORM(i) ) / |A(i,i)| )
                        1<=i<=j

    and we can safely call CTRSV if 1/M(n) and 1/G(n) are both greater
    than max(underflow, 1/overflow).

    =====================================================================
','line_number':21748,'multiline':True]['text':' Parameter adjustments ','line_number':21911,'multiline':True]['text':' Function Body ','line_number':21918,'multiline':True]['text':'     Test the input parameters. ','line_number':21924,'multiline':True]['text':'     Quick return if possible ','line_number':21947,'multiline':True]['text':'     Determine machine dependent parameters to control overflow. ','line_number':21953,'multiline':True]['text':'        Compute the 1-norm of each column, not including the diagonal. ','line_number':21964,'multiline':True]['text':'           A is upper triangular. ','line_number':21968,'multiline':True]['text':' L10: ','line_number':21974,'multiline':True]['text':'           A is lower triangular. ','line_number':21978,'multiline':True]['text':' L20: ','line_number':21984,'multiline':True]['text':'
       Scale the column norms by TSCAL if the maximum element in CNORM is
       greater than BIGNUM/2.
','line_number':21990,'multiline':True]['text':'
       Compute a bound on the computed solution vector to see if the
       Level 2 BLAS routine CTRSV can be used.
','line_number':22004,'multiline':True]['text':' Computing MAX ','line_number':22012,'multiline':True]['text':' L30: ','line_number':22017,'multiline':True]['text':'        Compute the growth in A * x = b. ','line_number':22023,'multiline':True]['text':'
             A is non-unit triangular.

             Compute GROW = 1/G(j) and XBND = 1/M(j).
             Initially, G(0) = max{x(i), i=1,...,n}.
','line_number':22042,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22055,'multiline':True]['text':'
                   M(j) = G(j-1) / abs(A(j,j))

   Computing MIN
','line_number':22068,'multiline':True]['text':'                 M(j) could overflow, set XBND to 0. ','line_number':22077,'multiline':True]['text':'                 G(j) = G(j-1)*( 1 + CNORM(j) / abs(A(j,j)) ) ','line_number':22084,'multiline':True]['text':'                 G(j) could overflow, set GROW to 0. ','line_number':22089,'multiline':True]['text':' L40: ','line_number':22093,'multiline':True]['text':'
             A is unit triangular.

             Compute GROW = 1/G(j), where G(0) = max{x(i), i=1,...,n}.

   Computing MIN
','line_number':22098,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22111,'multiline':True]['text':'              G(j) = G(j-1)*( 1 + CNORM(j) ) ','line_number':22117,'multiline':True]['text':' L50: ','line_number':22120,'multiline':True]['text':'        Compute the growth in A**T * x = b  or  A**H * x = b. ','line_number':22128,'multiline':True]['text':'
             A is non-unit triangular.

             Compute GROW = 1/G(j) and XBND = 1/M(j).
             Initially, M(0) = max{x(i), i=1,...,n}.
','line_number':22147,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22160,'multiline':True]['text':'              G(j) = max( G(j-1), M(j-1)*( 1 + CNORM(j) ) ) ','line_number':22166,'multiline':True]['text':' Computing MIN ','line_number':22169,'multiline':True]['text':'                 M(j) = M(j-1)*( 1 + CNORM(j) ) / abs(A(j,j)) ','line_number':22180,'multiline':True]['text':'                 M(j) could overflow, set XBND to 0. ','line_number':22187,'multiline':True]['text':' L70: ','line_number':22191,'multiline':True]['text':'
             A is unit triangular.

             Compute GROW = 1/G(j), where G(0) = max{x(i), i=1,...,n}.

   Computing MIN
','line_number':22196,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22209,'multiline':True]['text':'              G(j) = ( 1 + CNORM(j) )*G(j-1) ','line_number':22215,'multiline':True]['text':' L80: ','line_number':22219,'multiline':True]['text':'
          Use the Level 2 BLAS solve if the reciprocal of the bound on
          elements of X is not too small.
','line_number':22228,'multiline':True]['text':'        Use a Level 1 BLAS solve, scaling intermediate results. ','line_number':22236,'multiline':True]['text':'
             Scale X so that its components are less than or equal to
             BIGNUM in absolute value.
','line_number':22240,'multiline':True]['text':'           Solve A * x = b ','line_number':22254,'multiline':True]['text':'              Compute x(j) = b(j) / A(j,j), scaling x if necessary. ','line_number':22260,'multiline':True]['text':'                    abs(A(j,j)) > SMLNUM: ','line_number':22279,'multiline':True]['text':'                          Scale x by 1/b(j). ','line_number':22284,'multiline':True]['text':'                    0 < abs(A(j,j)) <= SMLNUM: ','line_number':22300,'multiline':True]['text':'
                         Scale x by (1/abs(x(j)))*abs(A(j,j))*BIGNUM
                         to avoid overflow when dividing by A(j,j).
','line_number':22304,'multiline':True]['text':'
                            Scale by 1/CNORM(j) to avoid overflow when
                            multiplying x(j) times column j.
','line_number':22312,'multiline':True]['text':'
                      A(j,j) = 0:  Set x(1:n) = 0, x(j) = 1, and
                      scale = 0, and compute a solution to A*x = 0.
','line_number':22331,'multiline':True]['text':' L100: ','line_number':22340,'multiline':True]['text':'
                Scale x if necessary to avoid overflow when adding a
                multiple of column j of A.
','line_number':22350,'multiline':True]['text':'                    Scale x by 1/(2*abs(x(j))). ','line_number':22359,'multiline':True]['text':'                 Scale x by 1/2. ','line_number':22367,'multiline':True]['text':'
                      Compute the update
                         x(1:j-1) := x(1:j-1) - x(j) * A(1:j-1,j)
','line_number':22376,'multiline':True]['text':'
                      Compute the update
                         x(j+1:n) := x(j+1:n) - x(j) * A(j+1:n,j)
','line_number':22396,'multiline':True]['text':' L110: ','line_number':22414,'multiline':True]['text':'           Solve A**T * x = b ','line_number':22419,'multiline':True]['text':'
                Compute x(j) = b(j) - sum A(k,j)*x(k).
                                      k<>j
','line_number':22425,'multiline':True]['text':'                 If x(j) could overflow, scale x by 1/(2*XMAX). ','line_number':22437,'multiline':True]['text':'
                         Divide by A(j,j) when scaling x if A(j,j) > 1.

   Computing MIN
','line_number':22452,'multiline':True]['text':'
                   If the scaling needed for A in the dot product is 1,
                   call CDOTU to perform the dot product.
','line_number':22472,'multiline':True]['text':'                 Otherwise, use in-line code for the dot product. ','line_number':22490,'multiline':True]['text':' L120: ','line_number':22506,'multiline':True]['text':' L130: ','line_number':22522,'multiline':True]['text':'
                   Compute x(j) := ( x(j) - CSUMJ ) / A(j,j) if 1/A(j,j)
                   was not used to scale the dotproduct.
','line_number':22530,'multiline':True]['text':'                    Compute x(j) = x(j) / A(j,j), scaling if necessary. ','line_number':22555,'multiline':True]['text':'                       abs(A(j,j)) > SMLNUM: ','line_number':22561,'multiline':True]['text':'                             Scale X by 1/abs(x(j)). ','line_number':22566,'multiline':True]['text':'                       0 < abs(A(j,j)) <= SMLNUM: ','line_number':22579,'multiline':True]['text':'                          Scale x by (1/abs(x(j)))*abs(A(j,j))*BIGNUM. ','line_number':22583,'multiline':True]['text':'
                         A(j,j) = 0:  Set x(1:n) = 0, x(j) = 1, and
                         scale = 0 and compute a solution to A**T *x = 0.
','line_number':22595,'multiline':True]['text':' L140: ','line_number':22604,'multiline':True]['text':'
                   Compute x(j) := x(j) / A(j,j) - CSUMJ if the dot
                   product has already been divided by 1/A(j,j).
','line_number':22615,'multiline':True]['text':' Computing MAX ','line_number':22625,'multiline':True]['text':' L150: ','line_number':22630,'multiline':True]['text':'           Solve A**H * x = b ','line_number':22635,'multiline':True]['text':'
                Compute x(j) = b(j) - sum A(k,j)*x(k).
                                      k<>j
','line_number':22641,'multiline':True]['text':'                 If x(j) could overflow, scale x by 1/(2*XMAX). ','line_number':22653,'multiline':True]['text':'
                         Divide by A(j,j) when scaling x if A(j,j) > 1.

   Computing MIN
','line_number':22667,'multiline':True]['text':'
                   If the scaling needed for A in the dot product is 1,
                   call CDOTC to perform the dot product.
','line_number':22687,'multiline':True]['text':'                 Otherwise, use in-line code for the dot product. ','line_number':22705,'multiline':True]['text':' L160: ','line_number':22721,'multiline':True]['text':' L170: ','line_number':22737,'multiline':True]['text':'
                   Compute x(j) := ( x(j) - CSUMJ ) / A(j,j) if 1/A(j,j)
                   was not used to scale the dotproduct.
','line_number':22745,'multiline':True]['text':'                    Compute x(j) = x(j) / A(j,j), scaling if necessary. ','line_number':22769,'multiline':True]['text':'                       abs(A(j,j)) > SMLNUM: ','line_number':22775,'multiline':True]['text':'                             Scale X by 1/abs(x(j)). ','line_number':22780,'multiline':True]['text':'                       0 < abs(A(j,j)) <= SMLNUM: ','line_number':22793,'multiline':True]['text':'                          Scale x by (1/abs(x(j)))*abs(A(j,j))*BIGNUM. ','line_number':22797,'multiline':True]['text':'
                         A(j,j) = 0:  Set x(1:n) = 0, x(j) = 1, and
                         scale = 0 and compute a solution to A**H *x = 0.
','line_number':22809,'multiline':True]['text':' L180: ','line_number':22818,'multiline':True]['text':'
                   Compute x(j) := x(j) / A(j,j) - CSUMJ if the dot
                   product has already been divided by 1/A(j,j).
','line_number':22829,'multiline':True]['text':' Computing MAX ','line_number':22839,'multiline':True]['text':' L190: ','line_number':22844,'multiline':True]['text':'     Scale the column norms by 1/TSCAL for return. ','line_number':22850,'multiline':True]['text':'     End of CLATRS ','line_number':22859,'multiline':True]['text':' clatrs_ ','line_number':22861,'multiline':True]['text':' Subroutine ','line_number':22863,'multiline':True]['text':' System generated locals ','line_number':22866,'multiline':True]['text':' Local variables ','line_number':22871,'multiline':True]['text':' Complex ','line_number':22874,'multiline':True]['text':' Subroutine ','line_number':22877,'multiline':True]['text':' Subroutine ','line_number':22881,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLAUU2 computes the product U * U' or L' * L, where the triangular
    factor U or L is stored in the upper or lower triangular part of
    the array A.

    If UPLO = 'U' or 'u' then the upper triangle of the result is stored,
    overwriting the factor U in A.
    If UPLO = 'L' or 'l' then the lower triangle of the result is stored,
    overwriting the factor L in A.

    This is the unblocked form of the algorithm, calling Level 2 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the triangular factor stored in the array A
            is upper or lower triangular:
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the triangular factor U or L.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the triangular factor U or L.
            On exit, if UPLO = 'U', the upper triangle of A is
            overwritten with the upper triangle of the product U * U';
            if UPLO = 'L', the lower triangle of A is overwritten with
            the lower triangle of the product L' * L.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':22886,'multiline':True]['text':' Parameter adjustments ','line_number':22939,'multiline':True]['text':' Function Body ','line_number':22944,'multiline':True]['text':'     Quick return if possible ','line_number':22960,'multiline':True]['text':'        Compute the product U * U'. ','line_number':22968,'multiline':True]['text':' L10: ','line_number':22994,'multiline':True]['text':'        Compute the product L' * L. ','line_number':22999,'multiline':True]['text':' L20: ','line_number':23025,'multiline':True]['text':'     End of CLAUU2 ','line_number':23031,'multiline':True]['text':' clauu2_ ','line_number':23033,'multiline':True]['text':' Subroutine ','line_number':23035,'multiline':True]['text':' System generated locals ','line_number':23038,'multiline':True]['text':' Local variables ','line_number':23041,'multiline':True]['text':' Subroutine ','line_number':23043,'multiline':True]['text':' Subroutine ','line_number':23049,'multiline':True]['text':' Subroutine ','line_number':23053,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CLAUUM computes the product U * U' or L' * L, where the triangular
    factor U or L is stored in the upper or lower triangular part of
    the array A.

    If UPLO = 'U' or 'u' then the upper triangle of the result is stored,
    overwriting the factor U in A.
    If UPLO = 'L' or 'l' then the lower triangle of the result is stored,
    overwriting the factor L in A.

    This is the blocked form of the algorithm, calling Level 3 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the triangular factor stored in the array A
            is upper or lower triangular:
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the triangular factor U or L.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the triangular factor U or L.
            On exit, if UPLO = 'U', the upper triangle of A is
            overwritten with the upper triangle of the product U * U';
            if UPLO = 'L', the lower triangle of A is overwritten with
            the lower triangle of the product L' * L.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':23059,'multiline':True]['text':' Parameter adjustments ','line_number':23112,'multiline':True]['text':' Function Body ','line_number':23117,'multiline':True]['text':'     Quick return if possible ','line_number':23133,'multiline':True]['text':'     Determine the block size for this environment. ','line_number':23139,'multiline':True]['text':'        Use unblocked code ','line_number':23146,'multiline':True]['text':'        Use blocked code ','line_number':23151,'multiline':True]['text':'           Compute the product U * U'. ','line_number':23155,'multiline':True]['text':' Computing MIN ','line_number':23160,'multiline':True]['text':' L10: ','line_number':23180,'multiline':True]['text':'           Compute the product L' * L. ','line_number':23184,'multiline':True]['text':' Computing MIN ','line_number':23189,'multiline':True]['text':' L20: ','line_number':23209,'multiline':True]['text':'     End of CLAUUM ','line_number':23216,'multiline':True]['text':' clauum_ ','line_number':23218,'multiline':True]['text':' Subroutine ','line_number':23220,'multiline':True]['text':' System generated locals ','line_number':23223,'multiline':True]['text':' Local variables ','line_number':23228,'multiline':True]['text':' Complex ','line_number':23231,'multiline':True]['text':' Subroutine ','line_number':23234,'multiline':True]['text':' Subroutine ','line_number':23238,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CPOTF2 computes the Cholesky factorization of a complex Hermitian
    positive definite matrix A.

    The factorization has the form
       A = U' * U ,  if UPLO = 'U', or
       A = L  * L',  if UPLO = 'L',
    where U is an upper triangular matrix and L is lower triangular.

    This is the unblocked version of the algorithm, calling Level 2 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            Hermitian matrix A is stored.
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            n by n upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n by n lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.

            On exit, if INFO = 0, the factor U or L from the Cholesky
            factorization A = U'*U  or A = L*L'.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value
            > 0: if INFO = k, the leading minor of order k is not
                 positive definite, and the factorization could not be
                 completed.

    =====================================================================


       Test the input parameters.
','line_number':23244,'multiline':True]['text':' Parameter adjustments ','line_number':23304,'multiline':True]['text':' Function Body ','line_number':23309,'multiline':True]['text':'     Quick return if possible ','line_number':23325,'multiline':True]['text':'        Compute the Cholesky factorization A = U'*U. ','line_number':23333,'multiline':True]['text':'           Compute U(J,J) and test for non-positive-definiteness. ','line_number':23338,'multiline':True]['text':'           Compute elements J+1:N of row J. ','line_number':23356,'multiline':True]['text':' L10: ','line_number':23373,'multiline':True]['text':'        Compute the Cholesky factorization A = L*L'. ','line_number':23377,'multiline':True]['text':'           Compute L(J,J) and test for non-positive-definiteness. ','line_number':23382,'multiline':True]['text':'           Compute elements J+1:N of column J. ','line_number':23399,'multiline':True]['text':' L20: ','line_number':23416,'multiline':True]['text':'     End of CPOTF2 ','line_number':23427,'multiline':True]['text':' cpotf2_ ','line_number':23429,'multiline':True]['text':' Subroutine ','line_number':23431,'multiline':True]['text':' System generated locals ','line_number':23434,'multiline':True]['text':' Local variables ','line_number':23438,'multiline':True]['text':' Subroutine ','line_number':23440,'multiline':True]['text':' Subroutine ','line_number':23446,'multiline':True]['text':' Subroutine ','line_number':23450,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CPOTRF computes the Cholesky factorization of a complex Hermitian
    positive definite matrix A.

    The factorization has the form
       A = U**H * U,  if UPLO = 'U', or
       A = L  * L**H,  if UPLO = 'L',
    where U is an upper triangular matrix and L is lower triangular.

    This is the block version of the algorithm, calling Level 3 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            N-by-N upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading N-by-N lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.

            On exit, if INFO = 0, the factor U or L from the Cholesky
            factorization A = U**H*U or A = L*L**H.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, the leading minor of order i is not
                  positive definite, and the factorization could not be
                  completed.

    =====================================================================


       Test the input parameters.
','line_number':23456,'multiline':True]['text':' Parameter adjustments ','line_number':23514,'multiline':True]['text':' Function Body ','line_number':23519,'multiline':True]['text':'     Quick return if possible ','line_number':23535,'multiline':True]['text':'     Determine the block size for this environment. ','line_number':23541,'multiline':True]['text':'        Use unblocked code. ','line_number':23547,'multiline':True]['text':'        Use blocked code. ','line_number':23552,'multiline':True]['text':'           Compute the Cholesky factorization A = U'*U. ','line_number':23556,'multiline':True]['text':'
                Update and factorize the current diagonal block and test
                for non-positive-definiteness.

   Computing MIN
','line_number':23562,'multiline':True]['text':'                 Compute the current block row. ','line_number':23580,'multiline':True]['text':' L10: ','line_number':23594,'multiline':True]['text':'           Compute the Cholesky factorization A = L*L'. ','line_number':23599,'multiline':True]['text':'
                Update and factorize the current diagonal block and test
                for non-positive-definiteness.

   Computing MIN
','line_number':23605,'multiline':True]['text':'                 Compute the current block column. ','line_number':23622,'multiline':True]['text':' L20: ','line_number':23636,'multiline':True]['text':'     End of CPOTRF ','line_number':23648,'multiline':True]['text':' cpotrf_ ','line_number':23650,'multiline':True]['text':' Subroutine ','line_number':23652,'multiline':True]['text':' System generated locals ','line_number':23655,'multiline':True]['text':' Local variables ','line_number':23658,'multiline':True]['text':' Subroutine ','line_number':23660,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CPOTRI computes the inverse of a complex Hermitian positive definite
    matrix A using the Cholesky factorization A = U**H*U or A = L*L**H
    computed by CPOTRF.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the triangular factor U or L from the Cholesky
            factorization A = U**H*U or A = L*L**H, as computed by
            CPOTRF.
            On exit, the upper or lower triangle of the (Hermitian)
            inverse of A, overwriting the input factor U or L.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, the (i,i) element of the factor U or L is
                  zero, and the inverse could not be computed.

    =====================================================================


       Test the input parameters.
','line_number':23666,'multiline':True]['text':' Parameter adjustments ','line_number':23712,'multiline':True]['text':' Function Body ','line_number':23717,'multiline':True]['text':'     Quick return if possible ','line_number':23732,'multiline':True]['text':'     Invert the triangular Cholesky factor U or L. ','line_number':23738,'multiline':True]['text':'     Form inv(U)*inv(U)' or inv(L)'*inv(L). ','line_number':23745,'multiline':True]['text':'     End of CPOTRI ','line_number':23751,'multiline':True]['text':' cpotri_ ','line_number':23753,'multiline':True]['text':' Subroutine ','line_number':23755,'multiline':True]['text':' System generated locals ','line_number':23758,'multiline':True]['text':' Local variables ','line_number':23761,'multiline':True]['text':' Subroutine ','line_number':23763,'multiline':True]['text':' Subroutine ','line_number':23767,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CPOTRS solves a system of linear equations A*X = B with a Hermitian
    positive definite matrix A using the Cholesky factorization
    A = U**H*U or A = L*L**H computed by CPOTRF.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrix B.  NRHS >= 0.

    A       (input) COMPLEX array, dimension (LDA,N)
            The triangular factor U or L from the Cholesky factorization
            A = U**H*U or A = L*L**H, as computed by CPOTRF.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    B       (input/output) COMPLEX array, dimension (LDB,NRHS)
            On entry, the right hand side matrix B.
            On exit, the solution matrix X.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':23770,'multiline':True]['text':' Parameter adjustments ','line_number':23822,'multiline':True]['text':' Function Body ','line_number':23830,'multiline':True]['text':'     Quick return if possible ','line_number':23850,'multiline':True]['text':'
          Solve A*X = B where A = U'*U.

          Solve U'*X = B, overwriting B with X.
','line_number':23858,'multiline':True]['text':'        Solve U*X = B, overwriting B with X. ','line_number':23867,'multiline':True]['text':'
          Solve A*X = B where A = L*L'.

          Solve L*X = B, overwriting B with X.
','line_number':23873,'multiline':True]['text':'        Solve L'*X = B, overwriting B with X. ','line_number':23882,'multiline':True]['text':'     End of CPOTRS ','line_number':23890,'multiline':True]['text':' cpotrs_ ','line_number':23892,'multiline':True]['text':' Subroutine ','line_number':23894,'multiline':True]['text':' System generated locals ','line_number':23897,'multiline':True]['text':' Local variables ','line_number':23901,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CROT   applies a plane rotation, where the cos (C) is real and the
    sin (S) is complex, and the vectors CX and CY are complex.

    Arguments
    =========

    N       (input) INTEGER
            The number of elements in the vectors CX and CY.

    CX      (input/output) COMPLEX array, dimension (N)
            On input, the vector X.
            On output, CX is overwritten with C*X + S*Y.

    INCX    (input) INTEGER
            The increment between successive values of CY.  INCX <> 0.

    CY      (input/output) COMPLEX array, dimension (N)
            On input, the vector Y.
            On output, CY is overwritten with -CONJG(S)*X + C*Y.

    INCY    (input) INTEGER
            The increment between successive values of CY.  INCX <> 0.

    C       (input) REAL
    S       (input) COMPLEX
            C and S define a rotation
               [  C          S  ]
               [ -conjg(S)   C  ]
            where C*C + S*CONJG(S) = 1.0.

   =====================================================================
','line_number':23906,'multiline':True]['text':' Parameter adjustments ','line_number':23950,'multiline':True]['text':' Function Body ','line_number':23954,'multiline':True]['text':'     Code for unequal increments or equal increments not equal to 1 ','line_number':23962,'multiline':True]['text':' L10: ','line_number':23994,'multiline':True]['text':'     Code for both increments equal to 1 ','line_number':23998,'multiline':True]['text':' L30: ','line_number':24021,'multiline':True]['text':' crot_ ','line_number':24024,'multiline':True]['text':' Subroutine ','line_number':24026,'multiline':True]['text':' System generated locals ','line_number':24031,'multiline':True]['text':' Local variables ','line_number':24035,'multiline':True]['text':' Subroutine ','line_number':24041,'multiline':True]['text':' Subroutine ','line_number':24044,'multiline':True]['text':' Subroutine ','line_number':24048,'multiline':True]['text':' Subroutine ','line_number':24051,'multiline':True]['text':' Subroutine ','line_number':24057,'multiline':True]['text':' Subroutine ','line_number':24062,'multiline':True]['text':' Subroutine ','line_number':24066,'multiline':True]['text':' Subroutine ','line_number':24070,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CSTEDC computes all eigenvalues and, optionally, eigenvectors of a
    symmetric tridiagonal matrix using the divide and conquer method.
    The eigenvectors of a full or band complex Hermitian matrix can also
    be found if CHETRD or CHPTRD or CHBTRD has been used to reduce this
    matrix to tridiagonal form.

    This code makes very mild assumptions about floating point
    arithmetic. It will work on machines with a guard digit in
    add/subtract, or on those binary machines without guard digits
    which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or Cray-2.
    It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.  See SLAED3 for details.

    Arguments
    =========

    COMPZ   (input) CHARACTER*1
            = 'N':  Compute eigenvalues only.
            = 'I':  Compute eigenvectors of tridiagonal matrix also.
            = 'V':  Compute eigenvectors of original Hermitian matrix
                    also.  On entry, Z contains the unitary matrix used
                    to reduce the original matrix to tridiagonal form.

    N       (input) INTEGER
            The dimension of the symmetric tridiagonal matrix.  N >= 0.

    D       (input/output) REAL array, dimension (N)
            On entry, the diagonal elements of the tridiagonal matrix.
            On exit, if INFO = 0, the eigenvalues in ascending order.

    E       (input/output) REAL array, dimension (N-1)
            On entry, the subdiagonal elements of the tridiagonal matrix.
            On exit, E has been destroyed.

    Z       (input/output) COMPLEX array, dimension (LDZ,N)
            On entry, if COMPZ = 'V', then Z contains the unitary
            matrix used in the reduction to tridiagonal form.
            On exit, if INFO = 0, then if COMPZ = 'V', Z contains the
            orthonormal eigenvectors of the original Hermitian matrix,
            and if COMPZ = 'I', Z contains the orthonormal eigenvectors
            of the symmetric tridiagonal matrix.
            If  COMPZ = 'N', then Z is not referenced.

    LDZ     (input) INTEGER
            The leading dimension of the array Z.  LDZ >= 1.
            If eigenvectors are desired, then LDZ >= max(1,N).

    WORK    (workspace/output) COMPLEX    array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If COMPZ = 'N' or 'I', or N <= 1, LWORK must be at least 1.
            If COMPZ = 'V' and N > 1, LWORK must be at least N*N.
            Note that for COMPZ = 'V', then if N is less than or
            equal to the minimum divide size, usually 25, then LWORK need
            only be 1.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal sizes of the WORK, RWORK and
            IWORK arrays, returns these values as the first entries of
            the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    RWORK   (workspace/output) REAL array, dimension (MAX(1,LRWORK))
            On exit, if INFO = 0, RWORK(1) returns the optimal LRWORK.

    LRWORK  (input) INTEGER
            The dimension of the array RWORK.
            If COMPZ = 'N' or N <= 1, LRWORK must be at least 1.
            If COMPZ = 'V' and N > 1, LRWORK must be at least
                           1 + 3*N + 2*N*lg N + 3*N**2 ,
                           where lg( N ) = smallest integer k such
                           that 2**k >= N.
            If COMPZ = 'I' and N > 1, LRWORK must be at least
                           1 + 4*N + 2*N**2 .
            Note that for COMPZ = 'I' or 'V', then if N is less than or
            equal to the minimum divide size, usually 25, then LRWORK
            need only be max(1,2*(N-1)).

            If LRWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    IWORK   (workspace/output) INTEGER array, dimension (MAX(1,LIWORK))
            On exit, if INFO = 0, IWORK(1) returns the optimal LIWORK.

    LIWORK  (input) INTEGER
            The dimension of the array IWORK.
            If COMPZ = 'N' or N <= 1, LIWORK must be at least 1.
            If COMPZ = 'V' or N > 1,  LIWORK must be at least
                                      6 + 6*N + 5*N*lg N.
            If COMPZ = 'I' or N > 1,  LIWORK must be at least
                                      3 + 5*N .
            Note that for COMPZ = 'I' or 'V', then if N is less than or
            equal to the minimum divide size, usually 25, then LIWORK
            need only be 1.

            If LIWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  The algorithm failed to compute an eigenvalue while
                  working on the submatrix lying in rows and columns
                  INFO/(N+1) through mod(INFO,N+1).

    Further Details
    ===============

    Based on contributions by
       Jeff Rutter, Computer Science Division, University of California
       at Berkeley, USA

    =====================================================================


       Test the input parameters.
','line_number':24074,'multiline':True]['text':' Parameter adjustments ','line_number':24210,'multiline':True]['text':' Function Body ','line_number':24220,'multiline':True]['text':'        Compute the workspace requirements ','line_number':24243,'multiline':True]['text':' Computing 2nd power ','line_number':24264,'multiline':True]['text':' Computing 2nd power ','line_number':24270,'multiline':True]['text':'     Quick return if possible ','line_number':24296,'multiline':True]['text':'
       If the following conditional clause is removed, then the routine
       will use the Divide and Conquer routine to compute only the
       eigenvalues, which requires (3N + 3N**2) real workspace and
       (2 + 5N + 2N lg(N)) integer workspace.
       Since on many architectures SSTERF is much faster than any other
       algorithm for finding eigenvalues only, it is used here
       as the default. If the conditional clause is removed, then
       information on the size of workspace needs to be changed.

       If COMPZ = 'N', use SSTERF to compute the eigenvalues.
','line_number':24309,'multiline':True]['text':'
       If N is smaller than the minimum divide size (SMLSIZ+1), then
       solve the problem with another solver.
','line_number':24327,'multiline':True]['text':'        If COMPZ = 'I', we simply call SSTEDC instead. ','line_number':24339,'multiline':True]['text':' L10: ','line_number':24354,'multiline':True]['text':' L20: ','line_number':24356,'multiline':True]['text':'
          From now on, only option left to be handled is COMPZ = 'V',
          i.e. ICOMPZ = 1.

          Scale.
','line_number':24361,'multiline':True]['text':'        while ( START <= N ) ','line_number':24377,'multiline':True]['text':'
             Let FINISH be the position of the next subdiagonal entry
             such that E( FINISH ) <= TINY or FINISH = N if no such
             subdiagonal exists.  The matrix identified by the elements
             between START and FINISH constitutes an independent
             sub-problem.
','line_number':24382,'multiline':True]['text':'           (Sub) Problem determined.  Compute its size and solve it. ','line_number':24401,'multiline':True]['text':'              Scale. ','line_number':24406,'multiline':True]['text':'              Scale back. ','line_number':24424,'multiline':True]['text':'
          endwhile

          If the problem split any number of times, then the eigenvalues
          will not be properly ordered.  Here we permute the eigenvalues
          (and the associated eigenvectors) into ascending order.
','line_number':24446,'multiline':True]['text':'           Use Selection Sort to minimize swaps of eigenvectors ','line_number':24456,'multiline':True]['text':' L50: ','line_number':24469,'multiline':True]['text':' L60: ','line_number':24477,'multiline':True]['text':'     End of CSTEDC ','line_number':24489,'multiline':True]['text':' cstedc_ ','line_number':24491,'multiline':True]['text':' Subroutine ','line_number':24493,'multiline':True]['text':' System generated locals ','line_number':24496,'multiline':True]['text':' Local variables ','line_number':24500,'multiline':True]['text':' Subroutine ','line_number':24509,'multiline':True]['text':' Subroutine ','line_number':24512,'multiline':True]['text':' Subroutine ','line_number':24515,'multiline':True]['text':' Subroutine ','line_number':24518,'multiline':True]['text':' Subroutine ','line_number':24523,'multiline':True]['text':' Subroutine ','line_number':24526,'multiline':True]['text':' Subroutine ','line_number':24528,'multiline':True]['text':' Subroutine ','line_number':24531,'multiline':True]['text':' Subroutine ','line_number':24537,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CSTEQR computes all eigenvalues and, optionally, eigenvectors of a
    symmetric tridiagonal matrix using the implicit QL or QR method.
    The eigenvectors of a full or band complex Hermitian matrix can also
    be found if CHETRD or CHPTRD or CHBTRD has been used to reduce this
    matrix to tridiagonal form.

    Arguments
    =========

    COMPZ   (input) CHARACTER*1
            = 'N':  Compute eigenvalues only.
            = 'V':  Compute eigenvalues and eigenvectors of the original
                    Hermitian matrix.  On entry, Z must contain the
                    unitary matrix used to reduce the original matrix
                    to tridiagonal form.
            = 'I':  Compute eigenvalues and eigenvectors of the
                    tridiagonal matrix.  Z is initialized to the identity
                    matrix.

    N       (input) INTEGER
            The order of the matrix.  N >= 0.

    D       (input/output) REAL array, dimension (N)
            On entry, the diagonal elements of the tridiagonal matrix.
            On exit, if INFO = 0, the eigenvalues in ascending order.

    E       (input/output) REAL array, dimension (N-1)
            On entry, the (n-1) subdiagonal elements of the tridiagonal
            matrix.
            On exit, E has been destroyed.

    Z       (input/output) COMPLEX array, dimension (LDZ, N)
            On entry, if  COMPZ = 'V', then Z contains the unitary
            matrix used in the reduction to tridiagonal form.
            On exit, if INFO = 0, then if COMPZ = 'V', Z contains the
            orthonormal eigenvectors of the original Hermitian matrix,
            and if COMPZ = 'I', Z contains the orthonormal eigenvectors
            of the symmetric tridiagonal matrix.
            If COMPZ = 'N', then Z is not referenced.

    LDZ     (input) INTEGER
            The leading dimension of the array Z.  LDZ >= 1, and if
            eigenvectors are desired, then  LDZ >= max(1,N).

    WORK    (workspace) REAL array, dimension (max(1,2*N-2))
            If COMPZ = 'N', then WORK is not referenced.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  the algorithm has failed to find all the eigenvalues in
                  a total of 30*N iterations; if INFO = i, then i
                  elements of E have not converged to zero; on exit, D
                  and E contain the elements of a symmetric tridiagonal
                  matrix which is unitarily similar to the original
                  matrix.

    =====================================================================


       Test the input parameters.
','line_number':24540,'multiline':True]['text':' Parameter adjustments ','line_number':24613,'multiline':True]['text':' Function Body ','line_number':24621,'multiline':True]['text':'     Quick return if possible ','line_number':24646,'multiline':True]['text':'     Determine the unit roundoff and over/underflow thresholds. ','line_number':24660,'multiline':True]['text':' Computing 2nd power ','line_number':24663,'multiline':True]['text':'
       Compute the eigenvalues and eigenvectors of the tridiagonal
       matrix.
','line_number':24671,'multiline':True]['text':'
       Determine where the matrix splits and choose QL or QR iteration
       for each block, according to whether top or bottom diagonal
       element is smaller.
','line_number':24683,'multiline':True]['text':' L20: ','line_number':24711,'multiline':True]['text':'     Scale submatrix in rows and columns L to LEND ','line_number':24726,'multiline':True]['text':'     Choose between QL and QR iteration ','line_number':24752,'multiline':True]['text':'
          QL Iteration

          Look for small subdiagonal element.
','line_number':24761,'multiline':True]['text':' Computing 2nd power ','line_number':24772,'multiline':True]['text':' L50: ','line_number':24779,'multiline':True]['text':'
          If remaining matrix is 2-by-2, use SLAE2 or SLAEV2
          to compute its eigensystem.
','line_number':24794,'multiline':True]['text':'        Form shift. ','line_number':24824,'multiline':True]['text':'        Inner loop ','line_number':24834,'multiline':True]['text':'           If eigenvectors are desired, then save rotations. ','line_number':24851,'multiline':True]['text':' L70: ','line_number':24858,'multiline':True]['text':'        If eigenvectors are desired, then apply saved rotations. ','line_number':24861,'multiline':True]['text':'        Eigenvalue found. ','line_number':24873,'multiline':True]['text':'
          QR Iteration

          Look for small superdiagonal element.
','line_number':24886,'multiline':True]['text':' Computing 2nd power ','line_number':24897,'multiline':True]['text':' L100: ','line_number':24904,'multiline':True]['text':'
          If remaining matrix is 2-by-2, use SLAE2 or SLAEV2
          to compute its eigensystem.
','line_number':24919,'multiline':True]['text':'        Form shift. ','line_number':24950,'multiline':True]['text':'        Inner loop ','line_number':24960,'multiline':True]['text':'           If eigenvectors are desired, then save rotations. ','line_number':24977,'multiline':True]['text':' L120: ','line_number':24984,'multiline':True]['text':'        If eigenvectors are desired, then apply saved rotations. ','line_number':24987,'multiline':True]['text':'        Eigenvalue found. ','line_number':24999,'multiline':True]['text':'     Undo scaling if necessary ','line_number':25012,'multiline':True]['text':'
       Check for no convergence to an eigenvalue after a total
       of N*MAXIT iterations.
','line_number':25031,'multiline':True]['text':' L150: ','line_number':25042,'multiline':True]['text':'     Order eigenvalues and eigenvectors. ','line_number':25048,'multiline':True]['text':'        Use Quick Sort ','line_number':25053,'multiline':True]['text':'        Use Selection Sort to minimize swaps of eigenvectors ','line_number':25059,'multiline':True]['text':' L170: ','line_number':25072,'multiline':True]['text':' L180: ','line_number':25080,'multiline':True]['text':'     End of CSTEQR ','line_number':25085,'multiline':True]['text':' csteqr_ ','line_number':25087,'multiline':True]['text':' Subroutine ','line_number':25089,'multiline':True]['text':' System generated locals ','line_number':25094,'multiline':True]['text':' Local variables ','line_number':25100,'multiline':True]['text':' Subroutine ','line_number':25108,'multiline':True]['text':' Subroutine ','line_number':25112,'multiline':True]['text':' Subroutine ','line_number':25115,'multiline':True]['text':' Subroutine ','line_number':25118,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CTREVC computes some or all of the right and/or left eigenvectors of
    a complex upper triangular matrix T.
    Matrices of this type are produced by the Schur factorization of
    a complex general matrix:  A = Q*T*Q**H, as computed by CHSEQR.

    The right eigenvector x and the left eigenvector y of T corresponding
    to an eigenvalue w are defined by:

                 T*x = w*x,     (y**H)*T = w*(y**H)

    where y**H denotes the conjugate transpose of the vector y.
    The eigenvalues are not input to this routine, but are read directly
    from the diagonal of T.

    This routine returns the matrices X and/or Y of right and left
    eigenvectors of T, or the products Q*X and/or Q*Y, where Q is an
    input matrix.  If Q is the unitary factor that reduces a matrix A to
    Schur form T, then Q*X and Q*Y are the matrices of right and left
    eigenvectors of A.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'R':  compute right eigenvectors only;
            = 'L':  compute left eigenvectors only;
            = 'B':  compute both right and left eigenvectors.

    HOWMNY  (input) CHARACTER*1
            = 'A':  compute all right and/or left eigenvectors;
            = 'B':  compute all right and/or left eigenvectors,
                    backtransformed using the matrices supplied in
                    VR and/or VL;
            = 'S':  compute selected right and/or left eigenvectors,
                    as indicated by the logical array SELECT.

    SELECT  (input) LOGICAL array, dimension (N)
            If HOWMNY = 'S', SELECT specifies the eigenvectors to be
            computed.
            The eigenvector corresponding to the j-th eigenvalue is
            computed if SELECT(j) = .TRUE..
            Not referenced if HOWMNY = 'A' or 'B'.

    N       (input) INTEGER
            The order of the matrix T. N >= 0.

    T       (input/output) COMPLEX array, dimension (LDT,N)
            The upper triangular matrix T.  T is modified, but restored
            on exit.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= max(1,N).

    VL      (input/output) COMPLEX array, dimension (LDVL,MM)
            On entry, if SIDE = 'L' or 'B' and HOWMNY = 'B', VL must
            contain an N-by-N matrix Q (usually the unitary matrix Q of
            Schur vectors returned by CHSEQR).
            On exit, if SIDE = 'L' or 'B', VL contains:
            if HOWMNY = 'A', the matrix Y of left eigenvectors of T;
            if HOWMNY = 'B', the matrix Q*Y;
            if HOWMNY = 'S', the left eigenvectors of T specified by
                             SELECT, stored consecutively in the columns
                             of VL, in the same order as their
                             eigenvalues.
            Not referenced if SIDE = 'R'.

    LDVL    (input) INTEGER
            The leading dimension of the array VL.  LDVL >= 1, and if
            SIDE = 'L' or 'B', LDVL >= N.

    VR      (input/output) COMPLEX array, dimension (LDVR,MM)
            On entry, if SIDE = 'R' or 'B' and HOWMNY = 'B', VR must
            contain an N-by-N matrix Q (usually the unitary matrix Q of
            Schur vectors returned by CHSEQR).
            On exit, if SIDE = 'R' or 'B', VR contains:
            if HOWMNY = 'A', the matrix X of right eigenvectors of T;
            if HOWMNY = 'B', the matrix Q*X;
            if HOWMNY = 'S', the right eigenvectors of T specified by
                             SELECT, stored consecutively in the columns
                             of VR, in the same order as their
                             eigenvalues.
            Not referenced if SIDE = 'L'.

    LDVR    (input) INTEGER
            The leading dimension of the array VR.  LDVR >= 1, and if
            SIDE = 'R' or 'B'; LDVR >= N.

    MM      (input) INTEGER
            The number of columns in the arrays VL and/or VR. MM >= M.

    M       (output) INTEGER
            The number of columns in the arrays VL and/or VR actually
            used to store the eigenvectors.  If HOWMNY = 'A' or 'B', M
            is set to N.  Each selected eigenvector occupies one
            column.

    WORK    (workspace) COMPLEX array, dimension (2*N)

    RWORK   (workspace) REAL array, dimension (N)

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The algorithm used in this program is basically backward (forward)
    substitution, with scaling to make the code robust against
    possible overflow.

    Each eigenvector is normalized so that the element of largest
    magnitude has magnitude 1; here the magnitude of a complex number
    (x,y) is taken to be |x| + |y|.

    =====================================================================


       Decode and test the input parameters
','line_number':25127,'multiline':True]['text':' Parameter adjustments ','line_number':25258,'multiline':True]['text':' Function Body ','line_number':25272,'multiline':True]['text':'
       Set M to the number of columns required to store the selected
       eigenvectors.
','line_number':25281,'multiline':True]['text':' L10: ','line_number':25293,'multiline':True]['text':'     Quick return if possible. ','line_number':25321,'multiline':True]['text':'     Set the constants to control overflow. ','line_number':25327,'multiline':True]['text':'     Store the diagonal elements of T in working array WORK. ','line_number':25335,'multiline':True]['text':' L20: ','line_number':25342,'multiline':True]['text':'
       Compute 1-norm of each column of strictly upper triangular
       part of T to control overflow in triangular solver.
','line_number':25345,'multiline':True]['text':' L30: ','line_number':25355,'multiline':True]['text':'        Compute right eigenvectors. ','line_number':25360,'multiline':True]['text':' Computing MAX ','line_number':25370,'multiline':True]['text':'           Form right-hand side. ','line_number':25378,'multiline':True]['text':' L40: ','line_number':25386,'multiline':True]['text':'
             Solve the triangular system:
                (T(1:KI-1,1:KI-1) - T(KI,KI))*X = SCALE*WORK.
','line_number':25389,'multiline':True]['text':' L50: ','line_number':25408,'multiline':True]['text':'           Copy the vector x or Q*x to VR and normalize. ','line_number':25419,'multiline':True]['text':' L60: ','line_number':25434,'multiline':True]['text':'           Set back the original diagonal elements of T. ','line_number':25451,'multiline':True]['text':' L70: ','line_number':25458,'multiline':True]['text':'        Compute left eigenvectors. ','line_number':25469,'multiline':True]['text':' Computing MAX ','line_number':25480,'multiline':True]['text':'           Form right-hand side. ','line_number':25489,'multiline':True]['text':' L90: ','line_number':25497,'multiline':True]['text':'
             Solve the triangular system:
                (T(KI+1:N,KI+1:N) - T(KI,KI))'*X = SCALE*WORK.
','line_number':25500,'multiline':True]['text':' L100: ','line_number':25519,'multiline':True]['text':'           Copy the vector x or Q*x to VL and normalize. ','line_number':25531,'multiline':True]['text':' L110: ','line_number':25550,'multiline':True]['text':'           Set back the original diagonal elements of T. ','line_number':25568,'multiline':True]['text':' L120: ','line_number':25575,'multiline':True]['text':'     End of CTREVC ','line_number':25586,'multiline':True]['text':' ctrevc_ ','line_number':25588,'multiline':True]['text':' Subroutine ','line_number':25590,'multiline':True]['text':' System generated locals ','line_number':25594,'multiline':True]['text':' Local variables ','line_number':25598,'multiline':True]['text':' Subroutine ','line_number':25602,'multiline':True]['text':' Subroutine ','line_number':25606,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CTREXC reorders the Schur factorization of a complex matrix
    A = Q*T*Q**H, so that the diagonal element of T with row index IFST
    is moved to row ILST.

    The Schur form T is reordered by a unitary similarity transformation
    Z**H*T*Z, and optionally the matrix Q of Schur vectors is updated by
    postmultplying it with Z.

    Arguments
    =========

    COMPQ   (input) CHARACTER*1
            = 'V':  update the matrix Q of Schur vectors;
            = 'N':  do not update Q.

    N       (input) INTEGER
            The order of the matrix T. N >= 0.

    T       (input/output) COMPLEX array, dimension (LDT,N)
            On entry, the upper triangular matrix T.
            On exit, the reordered upper triangular matrix.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= max(1,N).

    Q       (input/output) COMPLEX array, dimension (LDQ,N)
            On entry, if COMPQ = 'V', the matrix Q of Schur vectors.
            On exit, if COMPQ = 'V', Q has been postmultiplied by the
            unitary transformation matrix Z which reorders T.
            If COMPQ = 'N', Q is not referenced.

    LDQ     (input) INTEGER
            The leading dimension of the array Q.  LDQ >= max(1,N).

    IFST    (input) INTEGER
    ILST    (input) INTEGER
            Specify the reordering of the diagonal elements of T:
            The element with row index IFST is moved to row ILST by a
            sequence of transpositions between adjacent elements.
            1 <= IFST <= N; 1 <= ILST <= N.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Decode and test the input parameters.
','line_number':25610,'multiline':True]['text':' Parameter adjustments ','line_number':25671,'multiline':True]['text':' Function Body ','line_number':25679,'multiline':True]['text':'     Quick return if possible ','line_number':25701,'multiline':True]['text':'        Move the IFST-th diagonal element forward down the diagonal. ','line_number':25709,'multiline':True]['text':'        Move the IFST-th diagonal element backward up the diagonal. ','line_number':25716,'multiline':True]['text':'        Interchange the k-th and (k+1)-th diagonal elements. ','line_number':25727,'multiline':True]['text':'        Determine the transformation to perform the interchange. ','line_number':25734,'multiline':True]['text':'        Apply transformation to the matrix T. ','line_number':25739,'multiline':True]['text':'           Accumulate transformation in the matrix Q. ','line_number':25758,'multiline':True]['text':' L10: ','line_number':25765,'multiline':True]['text':'     End of CTREXC ','line_number':25770,'multiline':True]['text':' ctrexc_ ','line_number':25772,'multiline':True]['text':' Subroutine ','line_number':25774,'multiline':True]['text':' System generated locals ','line_number':25777,'multiline':True]['text':' Local variables ','line_number':25781,'multiline':True]['text':' Subroutine ','line_number':25784,'multiline':True]['text':' Subroutine ','line_number':25788,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CTRTI2 computes the inverse of a singlecomplex upper or lower triangular
    matrix.

    This is the Level 2 BLAS version of the algorithm.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the matrix A is upper or lower triangular.
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    DIAG    (input) CHARACTER*1
            Specifies whether or not the matrix A is unit triangular.
            = 'N':  Non-unit triangular
            = 'U':  Unit triangular

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the triangular matrix A.  If UPLO = 'U', the
            leading n by n upper triangular part of the array A contains
            the upper triangular matrix, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n by n lower triangular part of the array A contains
            the lower triangular matrix, and the strictly upper
            triangular part of A is not referenced.  If DIAG = 'U', the
            diagonal elements of A are also not referenced and are
            assumed to be 1.

            On exit, the (triangular) inverse of the original matrix, in
            the same storage format.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':25793,'multiline':True]['text':' Parameter adjustments ','line_number':25851,'multiline':True]['text':' Function Body ','line_number':25856,'multiline':True]['text':'        Compute inverse of upper triangular matrix. ','line_number':25877,'multiline':True]['text':'           Compute elements 1:j-1 of j-th column. ','line_number':25893,'multiline':True]['text':' L10: ','line_number':25900,'multiline':True]['text':'        Compute inverse of lower triangular matrix. ','line_number':25904,'multiline':True]['text':'              Compute elements j+1:n of j-th column. ','line_number':25920,'multiline':True]['text':' L20: ','line_number':25928,'multiline':True]['text':'     End of CTRTI2 ','line_number':25934,'multiline':True]['text':' ctrti2_ ','line_number':25936,'multiline':True]['text':' Subroutine ','line_number':25938,'multiline':True]['text':' System generated locals ','line_number':25941,'multiline':True]['text':' Local variables ','line_number':25947,'multiline':True]['text':' Subroutine ','line_number':25950,'multiline':True]['text':' Subroutine ','line_number':25956,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CTRTRI computes the inverse of a complex upper or lower triangular
    matrix A.

    This is the Level 3 BLAS version of the algorithm.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  A is upper triangular;
            = 'L':  A is lower triangular.

    DIAG    (input) CHARACTER*1
            = 'N':  A is non-unit triangular;
            = 'U':  A is unit triangular.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the triangular matrix A.  If UPLO = 'U', the
            leading N-by-N upper triangular part of the array A contains
            the upper triangular matrix, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading N-by-N lower triangular part of the array A contains
            the lower triangular matrix, and the strictly upper
            triangular part of A is not referenced.  If DIAG = 'U', the
            diagonal elements of A are also not referenced and are
            assumed to be 1.
            On exit, the (triangular) inverse of the original matrix, in
            the same storage format.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value
            > 0: if INFO = i, A(i,i) is exactly zero.  The triangular
                 matrix is singular and its inverse can not be computed.

    =====================================================================


       Test the input parameters.
','line_number':25963,'multiline':True]['text':' Parameter adjustments ','line_number':26020,'multiline':True]['text':' Function Body ','line_number':26025,'multiline':True]['text':'     Quick return if possible ','line_number':26044,'multiline':True]['text':'     Check for singularity if non-unit. ','line_number':26050,'multiline':True]['text':' L10: ','line_number':26059,'multiline':True]['text':'
       Determine the block size for this environment.

   Writing concatenation
','line_number':26064,'multiline':True]['text':'        Use unblocked code ','line_number':26076,'multiline':True]['text':'        Use blocked code ','line_number':26081,'multiline':True]['text':'           Compute inverse of upper triangular matrix ','line_number':26085,'multiline':True]['text':' Computing MIN ','line_number':26090,'multiline':True]['text':'              Compute rows 1:j-1 of current block column ','line_number':26094,'multiline':True]['text':'              Compute inverse of current diagonal block ','line_number':26105,'multiline':True]['text':' L20: ','line_number':26108,'multiline':True]['text':'           Compute inverse of lower triangular matrix ','line_number':26112,'multiline':True]['text':' Computing MIN ','line_number':26117,'multiline':True]['text':'                 Compute rows j+jb:n of current block column ','line_number':26122,'multiline':True]['text':'              Compute inverse of current diagonal block ','line_number':26135,'multiline':True]['text':' L30: ','line_number':26138,'multiline':True]['text':'     End of CTRTRI ','line_number':26145,'multiline':True]['text':' ctrtri_ ','line_number':26147,'multiline':True]['text':' Subroutine ','line_number':26149,'multiline':True]['text':' System generated locals ','line_number':26152,'multiline':True]['text':' Local variables ','line_number':26156,'multiline':True]['text':' Subroutine ','line_number':26158,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNG2R generates an m by n complex matrix Q with orthonormal columns,
    which is defined as the first n columns of a product of k elementary
    reflectors of order m

          Q  =  H(1) H(2) . . . H(k)

    as returned by CGEQRF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. M >= N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. N >= K >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the i-th column must contain the vector which
            defines the elementary reflector H(i), for i = 1,2,...,k, as
            returned by CGEQRF in the first k columns of its array
            argument A.
            On exit, the m by n matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEQRF.

    WORK    (workspace) COMPLEX array, dimension (N)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':26164,'multiline':True]['text':' Parameter adjustments ','line_number':26221,'multiline':True]['text':' Function Body ','line_number':26228,'multiline':True]['text':'     Quick return if possible ','line_number':26245,'multiline':True]['text':'     Initialise columns k+1:n to columns of the unit matrix ','line_number':26251,'multiline':True]['text':' L10: ','line_number':26259,'multiline':True]['text':' L20: ','line_number':26263,'multiline':True]['text':'        Apply H(i) to A(i:m,i:n) from the left ','line_number':26268,'multiline':True]['text':'        Set A(1:i-1,i) to zero ','line_number':26289,'multiline':True]['text':' L30: ','line_number':26295,'multiline':True]['text':' L40: ','line_number':26297,'multiline':True]['text':'     End of CUNG2R ','line_number':26301,'multiline':True]['text':' cung2r_ ','line_number':26303,'multiline':True]['text':' Subroutine ','line_number':26305,'multiline':True]['text':' System generated locals ','line_number':26309,'multiline':True]['text':' Local variables ','line_number':26312,'multiline':True]['text':' Subroutine ','line_number':26317,'multiline':True]['text':' Subroutine ','line_number':26320,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNGBR generates one of the complex unitary matrices Q or P**H
    determined by CGEBRD when reducing a complex matrix A to bidiagonal
    form: A = Q * B * P**H.  Q and P**H are defined as products of
    elementary reflectors H(i) or G(i) respectively.

    If VECT = 'Q', A is assumed to have been an M-by-K matrix, and Q
    is of order M:
    if m >= k, Q = H(1) H(2) . . . H(k) and CUNGBR returns the first n
    columns of Q, where m >= n >= k;
    if m < k, Q = H(1) H(2) . . . H(m-1) and CUNGBR returns Q as an
    M-by-M matrix.

    If VECT = 'P', A is assumed to have been a K-by-N matrix, and P**H
    is of order N:
    if k < n, P**H = G(k) . . . G(2) G(1) and CUNGBR returns the first m
    rows of P**H, where n >= m >= k;
    if k >= n, P**H = G(n-1) . . . G(2) G(1) and CUNGBR returns P**H as
    an N-by-N matrix.

    Arguments
    =========

    VECT    (input) CHARACTER*1
            Specifies whether the matrix Q or the matrix P**H is
            required, as defined in the transformation applied by CGEBRD:
            = 'Q':  generate Q;
            = 'P':  generate P**H.

    M       (input) INTEGER
            The number of rows of the matrix Q or P**H to be returned.
            M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q or P**H to be returned.
            N >= 0.
            If VECT = 'Q', M >= N >= min(M,K);
            if VECT = 'P', N >= M >= min(N,K).

    K       (input) INTEGER
            If VECT = 'Q', the number of columns in the original M-by-K
            matrix reduced by CGEBRD.
            If VECT = 'P', the number of rows in the original K-by-N
            matrix reduced by CGEBRD.
            K >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the vectors which define the elementary reflectors,
            as returned by CGEBRD.
            On exit, the M-by-N matrix Q or P**H.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= M.

    TAU     (input) COMPLEX array, dimension
                                  (min(M,K)) if VECT = 'Q'
                                  (min(N,K)) if VECT = 'P'
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i) or G(i), which determines Q or P**H, as
            returned by CGEBRD in its array argument TAUQ or TAUP.

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= max(1,min(M,N)).
            For optimum performance LWORK >= min(M,N)*NB, where NB
            is the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':26328,'multiline':True]['text':' Parameter adjustments ','line_number':26421,'multiline':True]['text':' Function Body ','line_number':26428,'multiline':True]['text':'     Quick return if possible ','line_number':26468,'multiline':True]['text':'
          Form Q, determined by a call to CGEBRD to reduce an m-by-k
          matrix
','line_number':26477,'multiline':True]['text':'           If m >= k, assume m >= n >= k ','line_number':26484,'multiline':True]['text':'
             If m < k, assume m = n

             Shift the vectors which define the elementary reflectors one
             column to the right, and set the first row and column of Q
             to those of the unit matrix
','line_number':26491,'multiline':True]['text':' L10: ','line_number':26507,'multiline':True]['text':' L20: ','line_number':26509,'multiline':True]['text':' L30: ','line_number':26517,'multiline':True]['text':'              Form Q(2:m,2:m) ','line_number':26521,'multiline':True]['text':'
          Form P', determined by a call to CGEBRD to reduce a k-by-n
          matrix
','line_number':26532,'multiline':True]['text':'           If k < n, assume k <= m <= n ','line_number':26539,'multiline':True]['text':'
             If k >= n, assume m = n

             Shift the vectors which define the elementary reflectors one
             row downward, and set the first row and column of P' to
             those of the unit matrix
','line_number':26546,'multiline':True]['text':' L40: ','line_number':26560,'multiline':True]['text':' L50: ','line_number':26568,'multiline':True]['text':' L60: ','line_number':26572,'multiline':True]['text':'              Form P'(2:n,2:n) ','line_number':26576,'multiline':True]['text':'     End of CUNGBR ','line_number':26589,'multiline':True]['text':' cungbr_ ','line_number':26591,'multiline':True]['text':' Subroutine ','line_number':26593,'multiline':True]['text':' System generated locals ','line_number':26597,'multiline':True]['text':' Local variables ','line_number':26600,'multiline':True]['text':' Subroutine ','line_number':26602,'multiline':True]['text':' Subroutine ','line_number':26605,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNGHR generates a complex unitary matrix Q which is defined as the
    product of IHI-ILO elementary reflectors of order N, as returned by
    CGEHRD:

    Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix Q. N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            ILO and IHI must have the same values as in the previous call
            of CGEHRD. Q is equal to the unit matrix except in the
            submatrix Q(ilo+1:ihi,ilo+1:ihi).
            1 <= ILO <= IHI <= N, if N > 0; ILO=1 and IHI=0, if N=0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the vectors which define the elementary reflectors,
            as returned by CGEHRD.
            On exit, the N-by-N unitary matrix Q.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,N).

    TAU     (input) COMPLEX array, dimension (N-1)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEHRD.

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= IHI-ILO.
            For optimum performance LWORK >= (IHI-ILO)*NB, where NB is
            the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':26611,'multiline':True]['text':' Parameter adjustments ','line_number':26675,'multiline':True]['text':' Function Body ','line_number':26682,'multiline':True]['text':'     Quick return if possible ','line_number':26713,'multiline':True]['text':'
       Shift the vectors which define the elementary reflectors one
       column to the right, and set the first ilo and the last n-ihi
       rows and columns to those of the unit matrix
','line_number':26720,'multiline':True]['text':' L10: ','line_number':26732,'multiline':True]['text':' L20: ','line_number':26739,'multiline':True]['text':' L30: ','line_number':26745,'multiline':True]['text':' L40: ','line_number':26747,'multiline':True]['text':' L50: ','line_number':26755,'multiline':True]['text':' L60: ','line_number':26759,'multiline':True]['text':' L70: ','line_number':26767,'multiline':True]['text':' L80: ','line_number':26771,'multiline':True]['text':'        Generate Q(ilo+1:ihi,ilo+1:ihi) ','line_number':26776,'multiline':True]['text':'     End of CUNGHR ','line_number':26784,'multiline':True]['text':' cunghr_ ','line_number':26786,'multiline':True]['text':' Subroutine ','line_number':26788,'multiline':True]['text':' System generated locals ','line_number':26791,'multiline':True]['text':' Local variables ','line_number':26795,'multiline':True]['text':' Subroutine ','line_number':26797,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNGL2 generates an m-by-n complex matrix Q with orthonormal rows,
    which is defined as the first m rows of a product of k elementary
    reflectors of order n

          Q  =  H(k)' . . . H(2)' H(1)'

    as returned by CGELQF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. N >= M.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. M >= K >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the i-th row must contain the vector which defines
            the elementary reflector H(i), for i = 1,2,...,k, as returned
            by CGELQF in the first k rows of its array argument A.
            On exit, the m by n matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGELQF.

    WORK    (workspace) COMPLEX array, dimension (M)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':26804,'multiline':True]['text':' Parameter adjustments ','line_number':26860,'multiline':True]['text':' Function Body ','line_number':26867,'multiline':True]['text':'     Quick return if possible ','line_number':26884,'multiline':True]['text':'        Initialise rows k+1:m to rows of the unit matrix ','line_number':26892,'multiline':True]['text':' L10: ','line_number':26900,'multiline':True]['text':' L20: ','line_number':26906,'multiline':True]['text':'        Apply H(i)' to A(i:m,i:n) from the right ','line_number':26912,'multiline':True]['text':'        Set A(i,1:i-1,i) to zero ','line_number':26938,'multiline':True]['text':' L30: ','line_number':26944,'multiline':True]['text':' L40: ','line_number':26946,'multiline':True]['text':'     End of CUNGL2 ','line_number':26950,'multiline':True]['text':' cungl2_ ','line_number':26952,'multiline':True]['text':' Subroutine ','line_number':26954,'multiline':True]['text':' System generated locals ','line_number':26958,'multiline':True]['text':' Local variables ','line_number':26961,'multiline':True]['text':' Subroutine ','line_number':26963,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNGLQ generates an M-by-N complex matrix Q with orthonormal rows,
    which is defined as the first M rows of a product of K elementary
    reflectors of order N

          Q  =  H(k)' . . . H(2)' H(1)'

    as returned by CGELQF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. N >= M.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. M >= K >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the i-th row must contain the vector which defines
            the elementary reflector H(i), for i = 1,2,...,k, as returned
            by CGELQF in the first k rows of its array argument A.
            On exit, the M-by-N matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGELQF.

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= max(1,M).
            For optimum performance LWORK >= M*NB, where NB is
            the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit;
            < 0:  if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':26977,'multiline':True]['text':' Parameter adjustments ','line_number':27044,'multiline':True]['text':' Function Body ','line_number':27051,'multiline':True]['text':'     Quick return if possible ','line_number':27076,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':27088,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':27098,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':27104,'multiline':True]['text':' Computing MAX ','line_number':27110,'multiline':True]['text':'
          Use blocked code after the last block.
          The first kk rows are handled by the block method.
','line_number':27120,'multiline':True]['text':' Computing MIN ','line_number':27126,'multiline':True]['text':'        Set A(kk+1:m,1:kk) to zero. ','line_number':27130,'multiline':True]['text':' L10: ','line_number':27138,'multiline':True]['text':' L20: ','line_number':27140,'multiline':True]['text':'     Use unblocked code for the last or only block. ','line_number':27146,'multiline':True]['text':'        Use blocked code ','line_number':27158,'multiline':True]['text':' Computing MIN ','line_number':27162,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':27167,'multiline':True]['text':'              Apply H' to A(i+ib:m,i:n) from the right ','line_number':27176,'multiline':True]['text':'           Apply H' to columns i:n of current block ','line_number':27186,'multiline':True]['text':'           Set columns 1:i-1 of current block to zero ','line_number':27192,'multiline':True]['text':' L30: ','line_number':27200,'multiline':True]['text':' L40: ','line_number':27202,'multiline':True]['text':' L50: ','line_number':27204,'multiline':True]['text':'     End of CUNGLQ ','line_number':27211,'multiline':True]['text':' cunglq_ ','line_number':27213,'multiline':True]['text':' Subroutine ','line_number':27215,'multiline':True]['text':' System generated locals ','line_number':27219,'multiline':True]['text':' Local variables ','line_number':27222,'multiline':True]['text':' Subroutine ','line_number':27224,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNGQR generates an M-by-N complex matrix Q with orthonormal columns,
    which is defined as the first N columns of a product of K elementary
    reflectors of order M

          Q  =  H(1) H(2) . . . H(k)

    as returned by CGEQRF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. M >= N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. N >= K >= 0.

    A       (input/output) COMPLEX array, dimension (LDA,N)
            On entry, the i-th column must contain the vector which
            defines the elementary reflector H(i), for i = 1,2,...,k, as
            returned by CGEQRF in the first k columns of its array
            argument A.
            On exit, the M-by-N matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEQRF.

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= max(1,N).
            For optimum performance LWORK >= N*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':27238,'multiline':True]['text':' Parameter adjustments ','line_number':27306,'multiline':True]['text':' Function Body ','line_number':27313,'multiline':True]['text':'     Quick return if possible ','line_number':27338,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':27350,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':27360,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':27366,'multiline':True]['text':' Computing MAX ','line_number':27372,'multiline':True]['text':'
          Use blocked code after the last block.
          The first kk columns are handled by the block method.
','line_number':27382,'multiline':True]['text':' Computing MIN ','line_number':27388,'multiline':True]['text':'        Set A(1:kk,kk+1:n) to zero. ','line_number':27392,'multiline':True]['text':' L10: ','line_number':27400,'multiline':True]['text':' L20: ','line_number':27402,'multiline':True]['text':'     Use unblocked code for the last or only block. ','line_number':27408,'multiline':True]['text':'        Use blocked code ','line_number':27420,'multiline':True]['text':' Computing MIN ','line_number':27424,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':27429,'multiline':True]['text':'              Apply H to A(i:m,i+ib:n) from the left ','line_number':27438,'multiline':True]['text':'           Apply H to rows i:m of current block ','line_number':27448,'multiline':True]['text':'           Set rows 1:i-1 of current block to zero ','line_number':27454,'multiline':True]['text':' L30: ','line_number':27462,'multiline':True]['text':' L40: ','line_number':27464,'multiline':True]['text':' L50: ','line_number':27466,'multiline':True]['text':'     End of CUNGQR ','line_number':27473,'multiline':True]['text':' cungqr_ ','line_number':27475,'multiline':True]['text':' Subroutine ','line_number':27477,'multiline':True]['text':' System generated locals ','line_number':27481,'multiline':True]['text':' Local variables ','line_number':27485,'multiline':True]['text':' Subroutine ','line_number':27490,'multiline':True]['text':' Subroutine ','line_number':27493,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNM2L overwrites the general complex m-by-n matrix C with

          Q * C  if SIDE = 'L' and TRANS = 'N', or

          Q'* C  if SIDE = 'L' and TRANS = 'C', or

          C * Q  if SIDE = 'R' and TRANS = 'N', or

          C * Q' if SIDE = 'R' and TRANS = 'C',

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k) . . . H(2) H(1)

    as returned by CGEQLF. Q is of order m if SIDE = 'L' and of order n
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q' from the Left
            = 'R': apply Q or Q' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q' (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            CGEQLF in the last k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEQLF.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the m-by-n matrix C.
            On exit, C is overwritten by Q*C or Q'*C or C*Q' or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX array, dimension
                                     (N) if SIDE = 'L',
                                     (M) if SIDE = 'R'

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':27497,'multiline':True]['text':' Parameter adjustments ','line_number':27584,'multiline':True]['text':' Function Body ','line_number':27594,'multiline':True]['text':'     NQ is the order of Q ','line_number':27599,'multiline':True]['text':'     Quick return if possible ','line_number':27627,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m-k+i,1:n) ','line_number':27654,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m,1:n-k+i) ','line_number':27659,'multiline':True]['text':'        Apply H(i) or H(i)' ','line_number':27664,'multiline':True]['text':' L10: ','line_number':27681,'multiline':True]['text':'     End of CUNM2L ','line_number':27685,'multiline':True]['text':' cunm2l_ ','line_number':27687,'multiline':True]['text':' Subroutine ','line_number':27689,'multiline':True]['text':' System generated locals ','line_number':27693,'multiline':True]['text':' Local variables ','line_number':27697,'multiline':True]['text':' Subroutine ','line_number':27702,'multiline':True]['text':' Subroutine ','line_number':27705,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNM2R overwrites the general complex m-by-n matrix C with

          Q * C  if SIDE = 'L' and TRANS = 'N', or

          Q'* C  if SIDE = 'L' and TRANS = 'C', or

          C * Q  if SIDE = 'R' and TRANS = 'N', or

          C * Q' if SIDE = 'R' and TRANS = 'C',

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(1) H(2) . . . H(k)

    as returned by CGEQRF. Q is of order m if SIDE = 'L' and of order n
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q' from the Left
            = 'R': apply Q or Q' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q' (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            CGEQRF in the first k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEQRF.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the m-by-n matrix C.
            On exit, C is overwritten by Q*C or Q'*C or C*Q' or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX array, dimension
                                     (N) if SIDE = 'L',
                                     (M) if SIDE = 'R'

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':27709,'multiline':True]['text':' Parameter adjustments ','line_number':27796,'multiline':True]['text':' Function Body ','line_number':27806,'multiline':True]['text':'     NQ is the order of Q ','line_number':27811,'multiline':True]['text':'     Quick return if possible ','line_number':27839,'multiline':True]['text':'           H(i) or H(i)' is applied to C(i:m,1:n) ','line_number':27868,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m,i:n) ','line_number':27874,'multiline':True]['text':'        Apply H(i) or H(i)' ','line_number':27880,'multiline':True]['text':' L10: ','line_number':27897,'multiline':True]['text':'     End of CUNM2R ','line_number':27901,'multiline':True]['text':' cunm2r_ ','line_number':27903,'multiline':True]['text':' Subroutine ','line_number':27905,'multiline':True]['text':' System generated locals ','line_number':27910,'multiline':True]['text':' Local variables ','line_number':27915,'multiline':True]['text':' Subroutine ','line_number':27920,'multiline':True]['text':' Subroutine ','line_number':27923,'multiline':True]['text':' Subroutine ','line_number':27927,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    If VECT = 'Q', CUNMBR overwrites the general complex M-by-N matrix C
    with
                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    If VECT = 'P', CUNMBR overwrites the general complex M-by-N matrix C
    with
                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      P * C          C * P
    TRANS = 'C':      P**H * C       C * P**H

    Here Q and P**H are the unitary matrices determined by CGEBRD when
    reducing a complex matrix A to bidiagonal form: A = Q * B * P**H. Q
    and P**H are defined as products of elementary reflectors H(i) and
    G(i) respectively.

    Let nq = m if SIDE = 'L' and nq = n if SIDE = 'R'. Thus nq is the
    order of the unitary matrix Q or P**H that is applied.

    If VECT = 'Q', A is assumed to have been an NQ-by-K matrix:
    if nq >= k, Q = H(1) H(2) . . . H(k);
    if nq < k, Q = H(1) H(2) . . . H(nq-1).

    If VECT = 'P', A is assumed to have been a K-by-NQ matrix:
    if k < nq, P = G(1) G(2) . . . G(k);
    if k >= nq, P = G(1) G(2) . . . G(nq-1).

    Arguments
    =========

    VECT    (input) CHARACTER*1
            = 'Q': apply Q or Q**H;
            = 'P': apply P or P**H.

    SIDE    (input) CHARACTER*1
            = 'L': apply Q, Q**H, P or P**H from the Left;
            = 'R': apply Q, Q**H, P or P**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q or P;
            = 'C':  Conjugate transpose, apply Q**H or P**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            If VECT = 'Q', the number of columns in the original
            matrix reduced by CGEBRD.
            If VECT = 'P', the number of rows in the original
            matrix reduced by CGEBRD.
            K >= 0.

    A       (input) COMPLEX array, dimension
                                  (LDA,min(nq,K)) if VECT = 'Q'
                                  (LDA,nq)        if VECT = 'P'
            The vectors which define the elementary reflectors H(i) and
            G(i), whose products determine the matrices Q and P, as
            returned by CGEBRD.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If VECT = 'Q', LDA >= max(1,nq);
            if VECT = 'P', LDA >= max(1,min(nq,K)).

    TAU     (input) COMPLEX array, dimension (min(nq,K))
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i) or G(i) which determines Q or P, as returned
            by CGEBRD in the array argument TAUQ or TAUP.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q
            or P*C or P**H*C or C*P or C*P**H.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M);
            if N = 0 or M = 0, LWORK >= 1.
            For optimum performance LWORK >= max(1,N*NB) if SIDE = 'L',
            and LWORK >= max(1,M*NB) if SIDE = 'R', where NB is the
            optimal blocksize. (NB = 0 if M = 0 or N = 0.)

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':27936,'multiline':True]['text':' Parameter adjustments ','line_number':28054,'multiline':True]['text':' Function Body ','line_number':28064,'multiline':True]['text':'     NQ is the order of Q or P and NW is the minimum dimension of WORK ','line_number':28071,'multiline':True]['text':' if(complicated condition) ','line_number':28095,'multiline':True]['text':' Computing MAX ','line_number':28096,'multiline':True]['text':' Writing concatenation ','line_number':28111,'multiline':True]['text':' Writing concatenation ','line_number':28120,'multiline':True]['text':' Writing concatenation ','line_number':28131,'multiline':True]['text':' Writing concatenation ','line_number':28140,'multiline':True]['text':' Computing MAX ','line_number':28150,'multiline':True]['text':'     Quick return if possible ','line_number':28167,'multiline':True]['text':'        Apply Q ','line_number':28175,'multiline':True]['text':'           Q was determined by a call to CGEBRD with nq >= k ','line_number':28179,'multiline':True]['text':'           Q was determined by a call to CGEBRD with nq < k ','line_number':28185,'multiline':True]['text':'        Apply P ','line_number':28204,'multiline':True]['text':'           P was determined by a call to CGEBRD with nq > k ','line_number':28213,'multiline':True]['text':'           P was determined by a call to CGEBRD with nq <= k ','line_number':28219,'multiline':True]['text':'     End of CUNMBR ','line_number':28241,'multiline':True]['text':' cunmbr_ ','line_number':28243,'multiline':True]['text':' Subroutine ','line_number':28245,'multiline':True]['text':' System generated locals ','line_number':28250,'multiline':True]['text':' Local variables ','line_number':28255,'multiline':True]['text':' Subroutine ','line_number':28260,'multiline':True]['text':' Subroutine ','line_number':28263,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNMHR overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix of order nq, with nq = m if
    SIDE = 'L' and nq = n if SIDE = 'R'. Q is defined as the product of
    IHI-ILO elementary reflectors, as returned by CGEHRD:

    Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q**H (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            ILO and IHI must have the same values as in the previous call
            of CGEHRD. Q is equal to the unit matrix except in the
            submatrix Q(ilo+1:ihi,ilo+1:ihi).
            If SIDE = 'L', then 1 <= ILO <= IHI <= M, if M > 0, and
            ILO = 1 and IHI = 0, if M = 0;
            if SIDE = 'R', then 1 <= ILO <= IHI <= N, if N > 0, and
            ILO = 1 and IHI = 0, if N = 0.

    A       (input) COMPLEX array, dimension
                                 (LDA,M) if SIDE = 'L'
                                 (LDA,N) if SIDE = 'R'
            The vectors which define the elementary reflectors, as
            returned by CGEHRD.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            LDA >= max(1,M) if SIDE = 'L'; LDA >= max(1,N) if SIDE = 'R'.

    TAU     (input) COMPLEX array, dimension
                                 (M-1) if SIDE = 'L'
                                 (N-1) if SIDE = 'R'
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEHRD.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':28270,'multiline':True]['text':' Parameter adjustments ','line_number':28368,'multiline':True]['text':' Function Body ','line_number':28378,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':28384,'multiline':True]['text':' Writing concatenation ','line_number':28416,'multiline':True]['text':' Writing concatenation ','line_number':28423,'multiline':True]['text':'     Quick return if possible ','line_number':28442,'multiline':True]['text':'     End of CUNMHR ','line_number':28467,'multiline':True]['text':' cunmhr_ ','line_number':28469,'multiline':True]['text':' Subroutine ','line_number':28471,'multiline':True]['text':' System generated locals ','line_number':28475,'multiline':True]['text':' Local variables ','line_number':28479,'multiline':True]['text':' Subroutine ','line_number':28484,'multiline':True]['text':' Subroutine ','line_number':28487,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNML2 overwrites the general complex m-by-n matrix C with

          Q * C  if SIDE = 'L' and TRANS = 'N', or

          Q'* C  if SIDE = 'L' and TRANS = 'C', or

          C * Q  if SIDE = 'R' and TRANS = 'N', or

          C * Q' if SIDE = 'R' and TRANS = 'C',

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k)' . . . H(2)' H(1)'

    as returned by CGELQF. Q is of order m if SIDE = 'L' and of order n
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q' from the Left
            = 'R': apply Q or Q' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q' (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX array, dimension
                                 (LDA,M) if SIDE = 'L',
                                 (LDA,N) if SIDE = 'R'
            The i-th row must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            CGELQF in the first k rows of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,K).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGELQF.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the m-by-n matrix C.
            On exit, C is overwritten by Q*C or Q'*C or C*Q' or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX array, dimension
                                     (N) if SIDE = 'L',
                                     (M) if SIDE = 'R'

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':28492,'multiline':True]['text':' Parameter adjustments ','line_number':28579,'multiline':True]['text':' Function Body ','line_number':28589,'multiline':True]['text':'     NQ is the order of Q ','line_number':28594,'multiline':True]['text':'     Quick return if possible ','line_number':28622,'multiline':True]['text':'           H(i) or H(i)' is applied to C(i:m,1:n) ','line_number':28651,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m,i:n) ','line_number':28657,'multiline':True]['text':'        Apply H(i) or H(i)' ','line_number':28663,'multiline':True]['text':' L10: ','line_number':28688,'multiline':True]['text':'     End of CUNML2 ','line_number':28692,'multiline':True]['text':' cunml2_ ','line_number':28694,'multiline':True]['text':' Subroutine ','line_number':28696,'multiline':True]['text':' System generated locals ','line_number':28700,'multiline':True]['text':' Local variables ','line_number':28706,'multiline':True]['text':' was [65][64] ','line_number':28708,'multiline':True]['text':' Subroutine ','line_number':28713,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNMLQ overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k)' . . . H(2)' H(1)'

    as returned by CGELQF. Q is of order M if SIDE = 'L' and of order N
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Conjugate transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX array, dimension
                                 (LDA,M) if SIDE = 'L',
                                 (LDA,N) if SIDE = 'R'
            The i-th row must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            CGELQF in the first k rows of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,K).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGELQF.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':28730,'multiline':True]['text':' Parameter adjustments ','line_number':28825,'multiline':True]['text':' Function Body ','line_number':28835,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':28841,'multiline':True]['text':'
          Determine the block size.  NB may be at most NBMAX, where NBMAX
          is used to define the local array T.

   Computing MIN
   Writing concatenation
','line_number':28870,'multiline':True]['text':'     Quick return if possible ','line_number':28895,'multiline':True]['text':'
   Computing MAX
   Writing concatenation
','line_number':28908,'multiline':True]['text':'        Use unblocked code ','line_number':28925,'multiline':True]['text':'        Use blocked code ','line_number':28931,'multiline':True]['text':' Computing MIN ','line_number':28960,'multiline':True]['text':'
             Form the triangular factor of the block reflector
             H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':28964,'multiline':True]['text':'              H or H' is applied to C(i:m,1:n) ','line_number':28974,'multiline':True]['text':'              H or H' is applied to C(1:m,i:n) ','line_number':28980,'multiline':True]['text':'           Apply H or H' ','line_number':28986,'multiline':True]['text':' L10: ','line_number':28991,'multiline':True]['text':'     End of CUNMLQ ','line_number':28997,'multiline':True]['text':' cunmlq_ ','line_number':28999,'multiline':True]['text':' Subroutine ','line_number':29001,'multiline':True]['text':' System generated locals ','line_number':29005,'multiline':True]['text':' Local variables ','line_number':29011,'multiline':True]['text':' was [65][64] ','line_number':29013,'multiline':True]['text':' Subroutine ','line_number':29018,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNMQL overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k) . . . H(2) H(1)

    as returned by CGEQLF. Q is of order M if SIDE = 'L' and of order N
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            CGEQLF in the last k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEQLF.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':29033,'multiline':True]['text':' Parameter adjustments ','line_number':29128,'multiline':True]['text':' Function Body ','line_number':29138,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':29144,'multiline':True]['text':'
             Determine the block size.  NB may be at most NBMAX, where
             NBMAX is used to define the local array T.

   Computing MIN
   Writing concatenation
','line_number':29174,'multiline':True]['text':'     Quick return if possible ','line_number':29204,'multiline':True]['text':'
   Computing MAX
   Writing concatenation
','line_number':29216,'multiline':True]['text':'        Use unblocked code ','line_number':29233,'multiline':True]['text':'        Use blocked code ','line_number':29239,'multiline':True]['text':' Computing MIN ','line_number':29260,'multiline':True]['text':'
             Form the triangular factor of the block reflector
             H = H(i+ib-1) . . . H(i+1) H(i)
','line_number':29264,'multiline':True]['text':'              H or H' is applied to C(1:m-k+i+ib-1,1:n) ','line_number':29274,'multiline':True]['text':'              H or H' is applied to C(1:m,1:n-k+i+ib-1) ','line_number':29279,'multiline':True]['text':'           Apply H or H' ','line_number':29284,'multiline':True]['text':' L10: ','line_number':29289,'multiline':True]['text':'     End of CUNMQL ','line_number':29295,'multiline':True]['text':' cunmql_ ','line_number':29297,'multiline':True]['text':' Subroutine ','line_number':29299,'multiline':True]['text':' System generated locals ','line_number':29303,'multiline':True]['text':' Local variables ','line_number':29309,'multiline':True]['text':' was [65][64] ','line_number':29311,'multiline':True]['text':' Subroutine ','line_number':29316,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNMQR overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(1) H(2) . . . H(k)

    as returned by CGEQRF. Q is of order M if SIDE = 'L' and of order N
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Conjugate transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            CGEQRF in the first k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CGEQRF.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':29331,'multiline':True]['text':' Parameter adjustments ','line_number':29426,'multiline':True]['text':' Function Body ','line_number':29436,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':29442,'multiline':True]['text':'
          Determine the block size.  NB may be at most NBMAX, where NBMAX
          is used to define the local array T.

   Computing MIN
   Writing concatenation
','line_number':29471,'multiline':True]['text':'     Quick return if possible ','line_number':29496,'multiline':True]['text':'
   Computing MAX
   Writing concatenation
','line_number':29509,'multiline':True]['text':'        Use unblocked code ','line_number':29526,'multiline':True]['text':'        Use blocked code ','line_number':29532,'multiline':True]['text':' Computing MIN ','line_number':29555,'multiline':True]['text':'
             Form the triangular factor of the block reflector
             H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':29559,'multiline':True]['text':'              H or H' is applied to C(i:m,1:n) ','line_number':29570,'multiline':True]['text':'              H or H' is applied to C(1:m,i:n) ','line_number':29576,'multiline':True]['text':'           Apply H or H' ','line_number':29582,'multiline':True]['text':' L10: ','line_number':29587,'multiline':True]['text':'     End of CUNMQR ','line_number':29593,'multiline':True]['text':' cunmqr_ ','line_number':29595,'multiline':True]['text':' Subroutine ','line_number':29597,'multiline':True]['text':' System generated locals ','line_number':29601,'multiline':True]['text':' Local variables ','line_number':29606,'multiline':True]['text':' Subroutine ','line_number':29612,'multiline':True]['text':' Subroutine ','line_number':29615,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    CUNMTR overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix of order nq, with nq = m if
    SIDE = 'L' and nq = n if SIDE = 'R'. Q is defined as the product of
    nq-1 elementary reflectors, as returned by CHETRD:

    if UPLO = 'U', Q = H(nq-1) . . . H(2) H(1);

    if UPLO = 'L', Q = H(1) H(2) . . . H(nq-1).

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    UPLO    (input) CHARACTER*1
            = 'U': Upper triangle of A contains elementary reflectors
                   from CHETRD;
            = 'L': Lower triangle of A contains elementary reflectors
                   from CHETRD.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Conjugate transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    A       (input) COMPLEX array, dimension
                                 (LDA,M) if SIDE = 'L'
                                 (LDA,N) if SIDE = 'R'
            The vectors which define the elementary reflectors, as
            returned by CHETRD.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            LDA >= max(1,M) if SIDE = 'L'; LDA >= max(1,N) if SIDE = 'R'.

    TAU     (input) COMPLEX array, dimension
                                 (M-1) if SIDE = 'L'
                                 (N-1) if SIDE = 'R'
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by CHETRD.

    C       (input/output) COMPLEX array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >=M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':29624,'multiline':True]['text':' Parameter adjustments ','line_number':29720,'multiline':True]['text':' Function Body ','line_number':29730,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':29736,'multiline':True]['text':' Writing concatenation ','line_number':29767,'multiline':True]['text':' Writing concatenation ','line_number':29776,'multiline':True]['text':' Writing concatenation ','line_number':29787,'multiline':True]['text':' Writing concatenation ','line_number':29796,'multiline':True]['text':'     Quick return if possible ','line_number':29818,'multiline':True]['text':'        Q was determined by a call to CHETRD with UPLO = 'U' ','line_number':29835,'multiline':True]['text':'        Q was determined by a call to CHETRD with UPLO = 'L' ','line_number':29842,'multiline':True]['text':'     End of CUNMTR ','line_number':29858,'multiline':True]['text':' cunmtr_ ','line_number':29860,'multiline':True]