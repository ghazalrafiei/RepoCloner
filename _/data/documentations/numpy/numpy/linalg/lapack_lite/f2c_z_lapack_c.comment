['text':'
 * NOTE: This is generated code. Look in numpy/linalg/lapack_lite for
 *       information on remaking this file.
 ','line_number':1,'multiline':True]['text':'
f2c knows the exact rules for precedence, and so omits parentheses where not
strictly necessary. Since this is generated code, we don't really care if
it's readable, and we know what is written is correct. So don't warn about
them.
','line_number':19,'multiline':True]['text':' Table of constant values ','line_number':30,'multiline':True]['text':' Subroutine ','line_number':55,'multiline':True]['text':' System generated locals ','line_number':59,'multiline':True]['text':' Local variables ','line_number':62,'multiline':True]['text':' Subroutine ','line_number':68,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGEBAK forms the right or left eigenvectors of a complex general
    matrix by backward transformation on the computed eigenvectors of the
    balanced matrix output by ZGEBAL.

    Arguments
    =========

    JOB     (input) CHARACTER*1
            Specifies the type of backward transformation required:
            = 'N', do nothing, return immediately;
            = 'P', do backward transformation for permutation only;
            = 'S', do backward transformation for scaling only;
            = 'B', do backward transformations for both permutation and
                   scaling.
            JOB must be the same as the argument JOB supplied to ZGEBAL.

    SIDE    (input) CHARACTER*1
            = 'R':  V contains right eigenvectors;
            = 'L':  V contains left eigenvectors.

    N       (input) INTEGER
            The number of rows of the matrix V.  N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            The integers ILO and IHI determined by ZGEBAL.
            1 <= ILO <= IHI <= N, if N > 0; ILO=1 and IHI=0, if N=0.

    SCALE   (input) DOUBLE PRECISION array, dimension (N)
            Details of the permutation and scaling factors, as returned
            by ZGEBAL.

    M       (input) INTEGER
            The number of columns of the matrix V.  M >= 0.

    V       (input/output) COMPLEX*16 array, dimension (LDV,M)
            On entry, the matrix of right or left eigenvectors to be
            transformed, as returned by ZHSEIN or ZTREVC.
            On exit, V is overwritten by the transformed eigenvectors.

    LDV     (input) INTEGER
            The leading dimension of the array V. LDV >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    =====================================================================


       Decode and Test the input parameters
','line_number':74,'multiline':True]['text':' Parameter adjustments ','line_number':137,'multiline':True]['text':' Function Body ','line_number':143,'multiline':True]['text':'     Quick return if possible ','line_number':170,'multiline':True]['text':'     Backward balance ','line_number':186,'multiline':True]['text':' L10: ','line_number':195,'multiline':True]['text':' L20: ','line_number':204,'multiline':True]['text':'
       Backward permutation

       For  I = ILO-1 step -1 until 1,
                IHI+1 step 1 until N do --
','line_number':210,'multiline':True]['text':'     End of ZGEBAK ','line_number':262,'multiline':True]['text':' zgebak_ ','line_number':264,'multiline':True]['text':' Subroutine ','line_number':266,'multiline':True]['text':' System generated locals ','line_number':269,'multiline':True]['text':' Local variables ','line_number':273,'multiline':True]['text':' Subroutine ','line_number':279,'multiline':True]['text':' Subroutine ','line_number':284,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    ZGEBAL balances a general complex matrix A.  This involves, first,
    permuting A by a similarity transformation to isolate eigenvalues
    in the first 1 to ILO-1 and last IHI+1 to N elements on the
    diagonal; and second, applying a diagonal similarity transformation
    to rows and columns ILO to IHI to make the rows and columns as
    close in norm as possible.  Both steps are optional.

    Balancing may reduce the 1-norm of the matrix, and improve the
    accuracy of the computed eigenvalues and/or eigenvectors.

    Arguments
    =========

    JOB     (input) CHARACTER*1
            Specifies the operations to be performed on A:
            = 'N':  none:  simply set ILO = 1, IHI = N, SCALE(I) = 1.0
                    for i = 1,...,N;
            = 'P':  permute only;
            = 'S':  scale only;
            = 'B':  both permute and scale.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the input matrix A.
            On exit,  A is overwritten by the balanced matrix.
            If JOB = 'N', A is not referenced.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    ILO     (output) INTEGER
    IHI     (output) INTEGER
            ILO and IHI are set to integers such that on exit
            A(i,j) = 0 if i > j and j = 1,...,ILO-1 or I = IHI+1,...,N.
            If JOB = 'N' or 'S', ILO = 1 and IHI = N.

    SCALE   (output) DOUBLE PRECISION array, dimension (N)
            Details of the permutations and scaling factors applied to
            A.  If P(j) is the index of the row and column interchanged
            with row and column j and D(j) is the scaling factor
            applied to row and column j, then
            SCALE(j) = P(j)    for j = 1,...,ILO-1
                     = D(j)    for j = ILO,...,IHI
                     = P(j)    for j = IHI+1,...,N.
            The order in which the interchanges are made is N to IHI+1,
            then 1 to ILO-1.

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The permutations consist of row and column interchanges which put
    the matrix in the form

               ( T1   X   Y  )
       P A P = (  0   B   Z  )
               (  0   0   T2 )

    where T1 and T2 are upper triangular matrices whose eigenvalues lie
    along the diagonal.  The column indices ILO and IHI mark the starting
    and ending columns of the submatrix B. Balancing consists of applying
    a diagonal similarity transformation inv(D) * B * D to make the
    1-norms of each row of B and its corresponding column nearly equal.
    The output matrix is

       ( T1     X*D          Y    )
       (  0  inv(D)*B*D  inv(D)*Z ).
       (  0      0           T2   )

    Information about the permutations P and the diagonal matrix D is
    returned in the vector SCALE.

    This subroutine is based on the EISPACK routine CBAL.

    Modified by Tzu-Yi Chen, Computer Science Division, University of
      California at Berkeley, USA

    =====================================================================


       Test the input parameters
','line_number':290,'multiline':True]['text':' Parameter adjustments ','line_number':389,'multiline':True]['text':' Function Body ','line_number':395,'multiline':True]['text':' L10: ','line_number':422,'multiline':True]['text':'     Permutation to isolate eigenvalues if possible ','line_number':431,'multiline':True]['text':'     Row and column exchange. ','line_number':435,'multiline':True]['text':'     Search for rows isolating an eigenvalue and push them down. ','line_number':453,'multiline':True]['text':'     Search for columns isolating an eigenvalue and push them left. ','line_number':486,'multiline':True]['text':' L130: ','line_number':519,'multiline':True]['text':'
       Balance the submatrix in rows K to L.

       Iterative loop for norm reduction
','line_number':526,'multiline':True]['text':'        Guard against zero C or R due to underflow. ','line_number':564,'multiline':True]['text':' Computing MAX ','line_number':573,'multiline':True]['text':' Computing MIN ','line_number':575,'multiline':True]['text':'           Exit if NaN to avoid infinite loop ','line_number':583,'multiline':True]['text':' Computing MIN ','line_number':601,'multiline':True]['text':'        Now balance. ','line_number':614,'multiline':True]['text':'     End of ZGEBAL ','line_number':652,'multiline':True]['text':' zgebal_ ','line_number':654,'multiline':True]['text':' Subroutine ','line_number':656,'multiline':True]['text':' System generated locals ','line_number':660,'multiline':True]['text':' Local variables ','line_number':664,'multiline':True]['text':' Subroutine ','line_number':667,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGEBD2 reduces a complex general m by n matrix A to upper or lower
    real bidiagonal form B by a unitary transformation: Q' * A * P = B.

    If m >= n, B is upper bidiagonal; if m < n, B is lower bidiagonal.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows in the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns in the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the m by n general matrix to be reduced.
            On exit,
            if m >= n, the diagonal and the first superdiagonal are
              overwritten with the upper bidiagonal matrix B; the
              elements below the diagonal, with the array TAUQ, represent
              the unitary matrix Q as a product of elementary
              reflectors, and the elements above the first superdiagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors;
            if m < n, the diagonal and the first subdiagonal are
              overwritten with the lower bidiagonal matrix B; the
              elements below the first subdiagonal, with the array TAUQ,
              represent the unitary matrix Q as a product of
              elementary reflectors, and the elements above the diagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    D       (output) DOUBLE PRECISION array, dimension (min(M,N))
            The diagonal elements of the bidiagonal matrix B:
            D(i) = A(i,i).

    E       (output) DOUBLE PRECISION array, dimension (min(M,N)-1)
            The off-diagonal elements of the bidiagonal matrix B:
            if m >= n, E(i) = A(i,i+1) for i = 1,2,...,n-1;
            if m < n, E(i) = A(i+1,i) for i = 1,2,...,m-1.

    TAUQ    (output) COMPLEX*16 array dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix Q. See Further Details.

    TAUP    (output) COMPLEX*16 array, dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix P. See Further Details.

    WORK    (workspace) COMPLEX*16 array, dimension (max(M,N))

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrices Q and P are represented as products of elementary
    reflectors:

    If m >= n,

       Q = H(1) H(2) . . . H(n)  and  P = G(1) G(2) . . . G(n-1)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors; v(1:i-1) = 0, v(i) = 1, and v(i+1:m) is stored on exit in
    A(i+1:m,i); u(1:i) = 0, u(i+1) = 1, and u(i+2:n) is stored on exit in
    A(i,i+2:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    If m < n,

       Q = H(1) H(2) . . . H(m-1)  and  P = G(1) G(2) . . . G(m)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, v and u are complex vectors;
    v(1:i) = 0, v(i+1) = 1, and v(i+2:m) is stored on exit in A(i+2:m,i);
    u(1:i-1) = 0, u(i) = 1, and u(i+1:n) is stored on exit in A(i,i+1:n);
    tauq is stored in TAUQ(i) and taup in TAUP(i).

    The contents of A on exit are illustrated by the following examples:

    m = 6 and n = 5 (m > n):          m = 5 and n = 6 (m < n):

      (  d   e   u1  u1  u1 )           (  d   u1  u1  u1  u1  u1 )
      (  v1  d   e   u2  u2 )           (  e   d   u2  u2  u2  u2 )
      (  v1  v2  d   e   u3 )           (  v1  e   d   u3  u3  u3 )
      (  v1  v2  v3  d   e  )           (  v1  v2  e   d   u4  u4 )
      (  v1  v2  v3  v4  d  )           (  v1  v2  v3  e   d   u5 )
      (  v1  v2  v3  v4  v5 )

    where d and e denote diagonal and off-diagonal elements of B, vi
    denotes an element of the vector defining H(i), and ui an element of
    the vector defining G(i).

    =====================================================================


       Test the input parameters
','line_number':674,'multiline':True]['text':' Parameter adjustments ','line_number':796,'multiline':True]['text':' Function Body ','line_number':806,'multiline':True]['text':'        Reduce to upper bidiagonal form ','line_number':823,'multiline':True]['text':'           Generate elementary reflector H(i) to annihilate A(i+1:m,i) ','line_number':828,'multiline':True]['text':' Computing MIN ','line_number':833,'multiline':True]['text':'           Apply H(i)' to A(i:m,i+1:n) from the left ','line_number':842,'multiline':True]['text':'
                Generate elementary reflector G(i) to annihilate
                A(i,i+2:n)
','line_number':857,'multiline':True]['text':' Computing MIN ','line_number':867,'multiline':True]['text':'              Apply G(i) to A(i+1:m,i+1:n) from the right ','line_number':876,'multiline':True]['text':' L10: ','line_number':892,'multiline':True]['text':'        Reduce to lower bidiagonal form ','line_number':896,'multiline':True]['text':'           Generate elementary reflector G(i) to annihilate A(i,i+1:n) ','line_number':901,'multiline':True]['text':' Computing MIN ','line_number':908,'multiline':True]['text':'           Apply G(i) to A(i+1:m,i:n) from the right ','line_number':917,'multiline':True]['text':'
                Generate elementary reflector H(i) to annihilate
                A(i+2:m,i)
','line_number':933,'multiline':True]['text':' Computing MIN ','line_number':941,'multiline':True]['text':'              Apply H(i)' to A(i+1:m,i+1:n) from the left ','line_number':950,'multiline':True]['text':' L20: ','line_number':965,'multiline':True]['text':'     End of ZGEBD2 ','line_number':970,'multiline':True]['text':' zgebd2_ ','line_number':972,'multiline':True]['text':' Subroutine ','line_number':974,'multiline':True]['text':' System generated locals ','line_number':979,'multiline':True]['text':' Local variables ','line_number':984,'multiline':True]['text':' Subroutine ','line_number':988,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGEBRD reduces a general complex M-by-N matrix A to upper or lower
    bidiagonal form B by a unitary transformation: Q**H * A * P = B.

    If m >= n, B is upper bidiagonal; if m < n, B is lower bidiagonal.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows in the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns in the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the M-by-N general matrix to be reduced.
            On exit,
            if m >= n, the diagonal and the first superdiagonal are
              overwritten with the upper bidiagonal matrix B; the
              elements below the diagonal, with the array TAUQ, represent
              the unitary matrix Q as a product of elementary
              reflectors, and the elements above the first superdiagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors;
            if m < n, the diagonal and the first subdiagonal are
              overwritten with the lower bidiagonal matrix B; the
              elements below the first subdiagonal, with the array TAUQ,
              represent the unitary matrix Q as a product of
              elementary reflectors, and the elements above the diagonal,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    D       (output) DOUBLE PRECISION array, dimension (min(M,N))
            The diagonal elements of the bidiagonal matrix B:
            D(i) = A(i,i).

    E       (output) DOUBLE PRECISION array, dimension (min(M,N)-1)
            The off-diagonal elements of the bidiagonal matrix B:
            if m >= n, E(i) = A(i,i+1) for i = 1,2,...,n-1;
            if m < n, E(i) = A(i+1,i) for i = 1,2,...,m-1.

    TAUQ    (output) COMPLEX*16 array dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix Q. See Further Details.

    TAUP    (output) COMPLEX*16 array, dimension (min(M,N))
            The scalar factors of the elementary reflectors which
            represent the unitary matrix P. See Further Details.

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The length of the array WORK.  LWORK >= max(1,M,N).
            For optimum performance LWORK >= (M+N)*NB, where NB
            is the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrices Q and P are represented as products of elementary
    reflectors:

    If m >= n,

       Q = H(1) H(2) . . . H(n)  and  P = G(1) G(2) . . . G(n-1)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors; v(1:i-1) = 0, v(i) = 1, and v(i+1:m) is stored on exit in
    A(i+1:m,i); u(1:i) = 0, u(i+1) = 1, and u(i+2:n) is stored on exit in
    A(i,i+2:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    If m < n,

       Q = H(1) H(2) . . . H(m-1)  and  P = G(1) G(2) . . . G(m)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors; v(1:i) = 0, v(i+1) = 1, and v(i+2:m) is stored on exit in
    A(i+2:m,i); u(1:i-1) = 0, u(i) = 1, and u(i+1:n) is stored on exit in
    A(i,i+1:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    The contents of A on exit are illustrated by the following examples:

    m = 6 and n = 5 (m > n):          m = 5 and n = 6 (m < n):

      (  d   e   u1  u1  u1 )           (  d   u1  u1  u1  u1  u1 )
      (  v1  d   e   u2  u2 )           (  e   d   u2  u2  u2  u2 )
      (  v1  v2  d   e   u3 )           (  v1  e   d   u3  u3  u3 )
      (  v1  v2  v3  d   e  )           (  v1  v2  e   d   u4  u4 )
      (  v1  v2  v3  v4  d  )           (  v1  v2  v3  e   d   u5 )
      (  v1  v2  v3  v4  v5 )

    where d and e denote diagonal and off-diagonal elements of B, vi
    denotes an element of the vector defining H(i), and ui an element of
    the vector defining G(i).

    =====================================================================


       Test the input parameters
','line_number':1004,'multiline':True]['text':' Parameter adjustments ','line_number':1137,'multiline':True]['text':' Function Body ','line_number':1147,'multiline':True]['text':' Computing MAX ','line_number':1149,'multiline':True]['text':' if(complicated condition) ','line_number':1163,'multiline':True]['text':' Computing MAX ','line_number':1164,'multiline':True]['text':'     Quick return if possible ','line_number':1178,'multiline':True]['text':'
          Set the crossover point NX.

   Computing MAX
','line_number':1192,'multiline':True]['text':'        Determine when to switch from blocked to unblocked code. ','line_number':1201,'multiline':True]['text':'
                Not enough work space for the optimal NB, consider using
                a smaller block size.
','line_number':1207,'multiline':True]['text':'
          Reduce rows and columns i:i+ib-1 to bidiagonal form and return
          the matrices X and Y which are needed to update the unreduced
          part of the matrix
','line_number':1230,'multiline':True]['text':'
          Update the trailing submatrix A(i+ib:m,i+ib:n), using
          an update of the form  A := A - V*Y' - X*U'
','line_number':1242,'multiline':True]['text':'        Copy diagonal and off-diagonal elements of B back into A ','line_number':1261,'multiline':True]['text':' L10: ','line_number':1272,'multiline':True]['text':' L20: ','line_number':1283,'multiline':True]['text':' L30: ','line_number':1286,'multiline':True]['text':'     Use unblocked code to reduce the remainder of the matrix ','line_number':1289,'multiline':True]['text':'     End of ZGEBRD ','line_number':1298,'multiline':True]['text':' zgebrd_ ','line_number':1300,'multiline':True]['text':' Subroutine ','line_number':1302,'multiline':True]['text':' System generated locals ','line_number':1307,'multiline':True]['text':' Local variables ','line_number':1313,'multiline':True]['text':' Subroutine ','line_number':1324,'multiline':True]['text':' Subroutine ','line_number':1330,'multiline':True]['text':' Subroutine ','line_number':1336,'multiline':True]['text':' Subroutine ','line_number':1340,'multiline':True]['text':' Subroutine ','line_number':1345,'multiline':True]['text':' Subroutine ','line_number':1355,'multiline':True]['text':' Subroutine ','line_number':1362,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGEEV computes for an N-by-N complex nonsymmetric matrix A, the
    eigenvalues and, optionally, the left and/or right eigenvectors.

    The right eigenvector v(j) of A satisfies
                     A * v(j) = lambda(j) * v(j)
    where lambda(j) is its eigenvalue.
    The left eigenvector u(j) of A satisfies
                  u(j)**H * A = lambda(j) * u(j)**H
    where u(j)**H denotes the conjugate transpose of u(j).

    The computed eigenvectors are normalized to have Euclidean norm
    equal to 1 and largest component real.

    Arguments
    =========

    JOBVL   (input) CHARACTER*1
            = 'N': left eigenvectors of A are not computed;
            = 'V': left eigenvectors of are computed.

    JOBVR   (input) CHARACTER*1
            = 'N': right eigenvectors of A are not computed;
            = 'V': right eigenvectors of A are computed.

    N       (input) INTEGER
            The order of the matrix A. N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the N-by-N matrix A.
            On exit, A has been overwritten.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    W       (output) COMPLEX*16 array, dimension (N)
            W contains the computed eigenvalues.

    VL      (output) COMPLEX*16 array, dimension (LDVL,N)
            If JOBVL = 'V', the left eigenvectors u(j) are stored one
            after another in the columns of VL, in the same order
            as their eigenvalues.
            If JOBVL = 'N', VL is not referenced.
            u(j) = VL(:,j), the j-th column of VL.

    LDVL    (input) INTEGER
            The leading dimension of the array VL.  LDVL >= 1; if
            JOBVL = 'V', LDVL >= N.

    VR      (output) COMPLEX*16 array, dimension (LDVR,N)
            If JOBVR = 'V', the right eigenvectors v(j) are stored one
            after another in the columns of VR, in the same order
            as their eigenvalues.
            If JOBVR = 'N', VR is not referenced.
            v(j) = VR(:,j), the j-th column of VR.

    LDVR    (input) INTEGER
            The leading dimension of the array VR.  LDVR >= 1; if
            JOBVR = 'V', LDVR >= N.

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= max(1,2*N).
            For good performance, LWORK must generally be larger.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    RWORK   (workspace) DOUBLE PRECISION array, dimension (2*N)

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  if INFO = i, the QR algorithm failed to compute all the
                  eigenvalues, and no eigenvectors have been computed;
                  elements and i+1:N of W contain eigenvalues which have
                  converged.

    =====================================================================


       Test the input arguments
','line_number':1367,'multiline':True]['text':' Parameter adjustments ','line_number':1464,'multiline':True]['text':' Function Body ','line_number':1478,'multiline':True]['text':'
       Compute workspace
        (Note: Comments in the code beginning "Workspace:" describe the
         minimal amount of workspace needed at that point in the code,
         as well as the preferred amount for good performance.
         CWorkspace refers to complex workspace, and RWorkspace to real
         workspace. NB refers to the optimal block size for the
         immediately following subroutine, as returned by ILAENV.
         HSWORK refers to the workspace preferred by ZHSEQR, as
         calculated below. HSWORK is computed assuming ILO=1 and IHI=N,
         the worst case.)
','line_number':1497,'multiline':True]['text':' Computing MAX ','line_number':1519,'multiline':True]['text':' Computing MAX ','line_number':1526,'multiline':True]['text':' Computing MAX ','line_number':1537,'multiline':True]['text':'     Quick return if possible ','line_number':1556,'multiline':True]['text':'     Get machine constants ','line_number':1562,'multiline':True]['text':'     Scale A if max element outside range [SMLNUM,BIGNUM] ','line_number':1571,'multiline':True]['text':'
       Balance the matrix
       (CWorkspace: none)
       (RWorkspace: need N)
','line_number':1587,'multiline':True]['text':'
       Reduce to upper Hessenberg form
       (CWorkspace: need 2*N, prefer N+N*NB)
       (RWorkspace: none)
','line_number':1596,'multiline':True]['text':'
          Want left eigenvectors
          Copy Householder vectors to VL
','line_number':1610,'multiline':True]['text':'
          Generate unitary matrix in VL
          (CWorkspace: need 2*N-1, prefer N+(N-1)*NB)
          (RWorkspace: none)
','line_number':1619,'multiline':True]['text':'
          Perform QR iteration, accumulating Schur vectors in VL
          (CWorkspace: need 1, prefer HSWORK (see comments) )
          (RWorkspace: none)
','line_number':1629,'multiline':True]['text':'
             Want left and right eigenvectors
             Copy Schur vectors to VR
','line_number':1642,'multiline':True]['text':'
          Want right eigenvectors
          Copy Householder vectors to VR
','line_number':1653,'multiline':True]['text':'
          Generate unitary matrix in VR
          (CWorkspace: need 2*N-1, prefer N+(N-1)*NB)
          (RWorkspace: none)
','line_number':1662,'multiline':True]['text':'
          Perform QR iteration, accumulating Schur vectors in VR
          (CWorkspace: need 1, prefer HSWORK (see comments) )
          (RWorkspace: none)
','line_number':1672,'multiline':True]['text':'
          Compute eigenvalues only
          (CWorkspace: need 1, prefer HSWORK (see comments) )
          (RWorkspace: none)
','line_number':1685,'multiline':True]['text':'     If INFO > 0 from ZHSEQR, then quit ','line_number':1697,'multiline':True]['text':'
          Compute left and/or right eigenvectors
          (CWorkspace: need 2*N)
          (RWorkspace: need 2*N)
','line_number':1705,'multiline':True]['text':'
          Undo balancing of left eigenvectors
          (CWorkspace: none)
          (RWorkspace: need N)
','line_number':1719,'multiline':True]['text':'        Normalize left eigenvectors and make largest component real ','line_number':1728,'multiline':True]['text':' Computing 2nd power ','line_number':1737,'multiline':True]['text':' Computing 2nd power ','line_number':1739,'multiline':True]['text':' L10: ','line_number':1742,'multiline':True]['text':' L20: ','line_number':1755,'multiline':True]['text':'
          Undo balancing of right eigenvectors
          (CWorkspace: none)
          (RWorkspace: need N)
','line_number':1761,'multiline':True]['text':'        Normalize right eigenvectors and make largest component real ','line_number':1770,'multiline':True]['text':' Computing 2nd power ','line_number':1779,'multiline':True]['text':' Computing 2nd power ','line_number':1781,'multiline':True]['text':' L30: ','line_number':1784,'multiline':True]['text':' L40: ','line_number':1797,'multiline':True]['text':'     Undo scaling if necessary ','line_number':1801,'multiline':True]['text':' Computing MAX ','line_number':1806,'multiline':True]['text':'     End of ZGEEV ','line_number':1821,'multiline':True]['text':' zgeev_ ','line_number':1823,'multiline':True]['text':' Subroutine ','line_number':1825,'multiline':True]['text':' System generated locals ','line_number':1829,'multiline':True]['text':' Local variables ','line_number':1833,'multiline':True]['text':' Subroutine ','line_number':1836,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGEHD2 reduces a complex general matrix A to upper Hessenberg form H
    by a unitary similarity transformation:  Q' * A * Q = H .

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            It is assumed that A is already upper triangular in rows
            and columns 1:ILO-1 and IHI+1:N. ILO and IHI are normally
            set by a previous call to ZGEBAL; otherwise they should be
            set to 1 and N respectively. See Further Details.
            1 <= ILO <= IHI <= max(1,N).

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the n by n general matrix to be reduced.
            On exit, the upper triangle and the first subdiagonal of A
            are overwritten with the upper Hessenberg matrix H, and the
            elements below the first subdiagonal, with the array TAU,
            represent the unitary matrix Q as a product of elementary
            reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    TAU     (output) COMPLEX*16 array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace) COMPLEX*16 array, dimension (N)

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrix Q is represented as a product of (ihi-ilo) elementary
    reflectors

       Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0, v(i+1) = 1 and v(ihi+1:n) = 0; v(i+2:ihi) is stored on
    exit in A(i+2:ihi,i), and tau in TAU(i).

    The contents of A are illustrated by the following example, with
    n = 7, ilo = 2 and ihi = 6:

    on entry,                        on exit,

    ( a   a   a   a   a   a   a )    (  a   a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      h   h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  v4  h   h   h )
    (                         a )    (                          a )

    where a denotes an element of the original matrix A, h denotes a
    modified element of the upper Hessenberg matrix H, and vi denotes an
    element of the vector defining H(i).

    =====================================================================


       Test the input parameters
','line_number':1842,'multiline':True]['text':' Parameter adjustments ','line_number':1929,'multiline':True]['text':' Function Body ','line_number':1936,'multiline':True]['text':'        Compute elementary reflector H(i) to annihilate A(i+2:ihi,i) ','line_number':1956,'multiline':True]['text':' Computing MIN ','line_number':1961,'multiline':True]['text':'        Apply H(i) to A(1:ihi,i+1:ihi) from the right ','line_number':1968,'multiline':True]['text':'        Apply H(i)' to A(i+1:ihi,i+1:n) from the left ','line_number':1974,'multiline':True]['text':' L10: ','line_number':1984,'multiline':True]['text':'     End of ZGEHD2 ','line_number':1989,'multiline':True]['text':' zgehd2_ ','line_number':1991,'multiline':True]['text':' Subroutine ','line_number':1993,'multiline':True]['text':' System generated locals ','line_number':1997,'multiline':True]['text':' Local variables ','line_number':2001,'multiline':True]['text':' was [65][64] ','line_number':2003,'multiline':True]['text':' Subroutine ','line_number':2007,'multiline':True]['text':' Subroutine ','line_number':2022,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.1)                                  --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
    -- April 2009                                                      --


    Purpose
    =======

    ZGEHRD reduces a complex general matrix A to upper Hessenberg form H by
    an unitary similarity transformation:  Q' * A * Q = H .

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            It is assumed that A is already upper triangular in rows
            and columns 1:ILO-1 and IHI+1:N. ILO and IHI are normally
            set by a previous call to ZGEBAL; otherwise they should be
            set to 1 and N respectively. See Further Details.
            1 <= ILO <= IHI <= N, if N > 0; ILO=1 and IHI=0, if N=0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the N-by-N general matrix to be reduced.
            On exit, the upper triangle and the first subdiagonal of A
            are overwritten with the upper Hessenberg matrix H, and the
            elements below the first subdiagonal, with the array TAU,
            represent the unitary matrix Q as a product of elementary
            reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    TAU     (output) COMPLEX*16 array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details). Elements 1:ILO-1 and IHI:N-1 of TAU are set to
            zero.

    WORK    (workspace/output) COMPLEX*16 array, dimension (LWORK)
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The length of the array WORK.  LWORK >= max(1,N).
            For optimum performance LWORK >= N*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    The matrix Q is represented as a product of (ihi-ilo) elementary
    reflectors

       Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0, v(i+1) = 1 and v(ihi+1:n) = 0; v(i+2:ihi) is stored on
    exit in A(i+2:ihi,i), and tau in TAU(i).

    The contents of A are illustrated by the following example, with
    n = 7, ilo = 2 and ihi = 6:

    on entry,                        on exit,

    ( a   a   a   a   a   a   a )    (  a   a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      a   h   h   h   h   a )
    (     a   a   a   a   a   a )    (      h   h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  h   h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  h   h   h   h )
    (     a   a   a   a   a   a )    (      v2  v3  v4  h   h   h )
    (                         a )    (                          a )

    where a denotes an element of the original matrix A, h denotes a
    modified element of the upper Hessenberg matrix H, and vi denotes an
    element of the vector defining H(i).

    This file is a slight modification of LAPACK-3.0's DGEHRD
    subroutine incorporating improvements proposed by Quintana-Orti and
    Van de Geijn (2006). (See DLAHR2.)

    =====================================================================


       Test the input parameters
','line_number':2030,'multiline':True]['text':' Parameter adjustments ','line_number':2133,'multiline':True]['text':' Function Body ','line_number':2140,'multiline':True]['text':' Computing MIN ','line_number':2142,'multiline':True]['text':'     Set elements 1:ILO-1 and IHI:N-1 of TAU to zero ','line_number':2168,'multiline':True]['text':' L10: ','line_number':2174,'multiline':True]['text':' L20: ','line_number':2180,'multiline':True]['text':'     Quick return if possible ','line_number':2183,'multiline':True]['text':'
       Determine the block size

   Computing MIN
','line_number':2191,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code
          (last block is always handled by unblocked code)

   Computing MAX
','line_number':2203,'multiline':True]['text':'           Determine if workspace is large enough for blocked code ','line_number':2214,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  determine the
                minimum value of NB, and reduce NB or force use of
                unblocked code

   Computing MAX
','line_number':2219,'multiline':True]['text':'        Use unblocked code below ','line_number':2241,'multiline':True]['text':'        Use blocked code ','line_number':2247,'multiline':True]['text':' Computing MIN ','line_number':2252,'multiline':True]['text':'
             Reduce columns i:i+ib-1 to Hessenberg form, returning the
             matrices V and T of the block reflector H = I - V*T*V'
             which performs the reduction, and also the matrix Y = A*V*T
','line_number':2256,'multiline':True]['text':'
             Apply the block reflector H to A(1:ihi,i+ib:ihi) from the
             right, computing  A := A - Y * V'. V(i+ib,ib-1) must be set
             to 1
','line_number':2265,'multiline':True]['text':'
             Apply the block reflector H to A(1:i,i+1:i+ib-1) from the
             right
','line_number':2283,'multiline':True]['text':' L30: ','line_number':2297,'multiline':True]['text':'
             Apply the block reflector H to A(i+1:ihi,i+ib:n) from the
             left
','line_number':2300,'multiline':True]['text':' L40: ','line_number':2311,'multiline':True]['text':'     Use unblocked code to reduce the rest of the matrix ','line_number':2315,'multiline':True]['text':'     End of ZGEHRD ','line_number':2322,'multiline':True]['text':' zgehrd_ ','line_number':2324,'multiline':True]['text':' Subroutine ','line_number':2326,'multiline':True]['text':' System generated locals ','line_number':2329,'multiline':True]['text':' Local variables ','line_number':2332,'multiline':True]['text':' Subroutine ','line_number':2335,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    ZGELQ2 computes an LQ factorization of a complex m by n matrix A:
    A = L * Q.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the m by n matrix A.
            On exit, the elements on and below the diagonal of the array
            contain the m by min(m,n) lower trapezoidal matrix L (L is
            lower triangular if m <= n); the elements above the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of elementary reflectors (see Further Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX*16 array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace) COMPLEX*16 array, dimension (M)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(k)' . . . H(2)' H(1)', where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; conjg(v(i+1:n)) is stored on exit in
    A(i,i+1:n), and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':2342,'multiline':True]['text':' Parameter adjustments ','line_number':2406,'multiline':True]['text':' Function Body ','line_number':2413,'multiline':True]['text':'        Generate elementary reflector H(i) to annihilate A(i,i+1:n) ','line_number':2433,'multiline':True]['text':' Computing MIN ','line_number':2440,'multiline':True]['text':'           Apply H(i) to A(i+1:m,i:n) from the right ','line_number':2446,'multiline':True]['text':' L10: ','line_number':2459,'multiline':True]['text':'     End of ZGELQ2 ','line_number':2463,'multiline':True]['text':' zgelq2_ ','line_number':2465,'multiline':True]['text':' Subroutine ','line_number':2467,'multiline':True]['text':' System generated locals ','line_number':2471,'multiline':True]['text':' Local variables ','line_number':2474,'multiline':True]['text':' Subroutine ','line_number':2476,'multiline':True]['text':' Subroutine ','line_number':2481,'multiline':True]['text':' Subroutine ','line_number':2486,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGELQF computes an LQ factorization of a complex M-by-N matrix A:
    A = L * Q.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit, the elements on and below the diagonal of the array
            contain the m-by-min(m,n) lower trapezoidal matrix L (L is
            lower triangular if m <= n); the elements above the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of elementary reflectors (see Further Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX*16 array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= max(1,M).
            For optimum performance LWORK >= M*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(k)' . . . H(2)' H(1)', where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; conjg(v(i+1:n)) is stored on exit in
    A(i,i+1:n), and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':2493,'multiline':True]['text':' Parameter adjustments ','line_number':2568,'multiline':True]['text':' Function Body ','line_number':2575,'multiline':True]['text':'     Quick return if possible ','line_number':2599,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':2612,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':2622,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':2628,'multiline':True]['text':' Computing MAX ','line_number':2634,'multiline':True]['text':'        Use blocked code initially ','line_number':2644,'multiline':True]['text':' Computing MIN ','line_number':2649,'multiline':True]['text':'
             Compute the LQ factorization of the current block
             A(i:i+ib-1,i:n)
','line_number':2653,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':2663,'multiline':True]['text':'              Apply H to A(i+ib:m,i:n) from the right ','line_number':2672,'multiline':True]['text':' L10: ','line_number':2681,'multiline':True]['text':'     Use unblocked code to factor the last or only block. ','line_number':2687,'multiline':True]['text':'     End of ZGELQF ','line_number':2699,'multiline':True]['text':' zgelqf_ ','line_number':2701,'multiline':True]['text':' Subroutine ','line_number':2703,'multiline':True]['text':' System generated locals ','line_number':2708,'multiline':True]['text':' Local variables ','line_number':2711,'multiline':True]['text':' Subroutine ','line_number':2717,'multiline':True]['text':' Subroutine ','line_number':2719,'multiline':True]['text':' Subroutine ','line_number':2732,'multiline':True]['text':' Subroutine ','line_number':2741,'multiline':True]['text':' Subroutine ','line_number':2747,'multiline':True]['text':' Subroutine ','line_number':2754,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGELSD computes the minimum-norm solution to a real linear least
    squares problem:
        minimize 2-norm(| b - A*x |)
    using the singular value decomposition (SVD) of A. A is an M-by-N
    matrix which may be rank-deficient.

    Several right hand side vectors b and solution vectors x can be
    handled in a single call; they are stored as the columns of the
    M-by-NRHS right hand side matrix B and the N-by-NRHS solution
    matrix X.

    The problem is solved in three steps:
    (1) Reduce the coefficient matrix A to bidiagonal form with
        Householder tranformations, reducing the original problem
        into a "bidiagonal least squares problem" (BLS)
    (2) Solve the BLS using a divide and conquer approach.
    (3) Apply back all the Householder tranformations to solve
        the original least squares problem.

    The effective rank of A is determined by treating as zero those
    singular values which are less than RCOND times the largest singular
    value.

    The divide and conquer algorithm makes very mild assumptions about
    floating point arithmetic. It will work on machines with a guard
    digit in add/subtract, or on those binary machines without guard
    digits which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or
    Cray-2. It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A. N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrices B and X. NRHS >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit, A has been destroyed.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,M).

    B       (input/output) COMPLEX*16 array, dimension (LDB,NRHS)
            On entry, the M-by-NRHS right hand side matrix B.
            On exit, B is overwritten by the N-by-NRHS solution matrix X.
            If m >= n and RANK = n, the residual sum-of-squares for
            the solution in the i-th column is given by the sum of
            squares of the modulus of elements n+1:m in that column.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,M,N).

    S       (output) DOUBLE PRECISION array, dimension (min(M,N))
            The singular values of A in decreasing order.
            The condition number of A in the 2-norm = S(1)/S(min(m,n)).

    RCOND   (input) DOUBLE PRECISION
            RCOND is used to determine the effective rank of A.
            Singular values S(i) <= RCOND*S(1) are treated as zero.
            If RCOND < 0, machine precision is used instead.

    RANK    (output) INTEGER
            The effective rank of A, i.e., the number of singular values
            which are greater than RCOND*S(1).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK must be at least 1.
            The exact minimum amount of workspace needed depends on M,
            N and NRHS. As long as LWORK is at least
                2*N + N*NRHS
            if M is greater than or equal to N or
                2*M + M*NRHS
            if M is less than N, the code will execute correctly.
            For good performance, LWORK should generally be larger.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the array WORK and the
            minimum sizes of the arrays RWORK and IWORK, and returns
            these values as the first entries of the WORK, RWORK and
            IWORK arrays, and no error message related to LWORK is issued
            by XERBLA.

    RWORK   (workspace) DOUBLE PRECISION array, dimension (MAX(1,LRWORK))
            LRWORK >=
               10*N + 2*N*SMLSIZ + 8*N*NLVL + 3*SMLSIZ*NRHS +
               MAX( (SMLSIZ+1)**2, N*(1+NRHS) + 2*NRHS )
            if M is greater than or equal to N or
               10*M + 2*M*SMLSIZ + 8*M*NLVL + 3*SMLSIZ*NRHS +
               MAX( (SMLSIZ+1)**2, N*(1+NRHS) + 2*NRHS )
            if M is less than N, the code will execute correctly.
            SMLSIZ is returned by ILAENV and is equal to the maximum
            size of the subproblems at the bottom of the computation
            tree (usually about 25), and
               NLVL = MAX( 0, INT( LOG_2( MIN( M,N )/(SMLSIZ+1) ) ) + 1 )
            On exit, if INFO = 0, RWORK(1) returns the minimum LRWORK.

    IWORK   (workspace) INTEGER array, dimension (MAX(1,LIWORK))
            LIWORK >= max(1, 3*MINMN*NLVL + 11*MINMN),
            where MINMN = MIN( M,N ).
            On exit, if INFO = 0, IWORK(1) returns the minimum LIWORK.

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value.
            > 0:  the algorithm for computing the SVD failed to converge;
                  if INFO = i, i off-diagonal elements of an intermediate
                  bidiagonal form did not converge to zero.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input arguments.
','line_number':2761,'multiline':True]['text':' Parameter adjustments ','line_number':2904,'multiline':True]['text':' Function Body ','line_number':2916,'multiline':True]['text':'
       Compute workspace.
       (Note: Comments in the code beginning "Workspace:" describe the
       minimal amount of workspace needed at that point in the code,
       as well as the preferred amount for good performance.
       NB refers to the optimal block size for the immediately
       following subroutine, as returned by ILAENV.)
','line_number':2933,'multiline':True]['text':' Computing MAX ','line_number':2952,'multiline':True]['text':'
                Path 1a - overdetermined, with many more rows than
                          columns.
','line_number':2960,'multiline':True]['text':' Computing MAX ','line_number':2966,'multiline':True]['text':' Computing MAX ','line_number':2970,'multiline':True]['text':'
                Path 1 - overdetermined or exactly determined.

   Computing MAX
   Computing 2nd power
','line_number':2977,'multiline':True]['text':' Computing MAX ','line_number':2987,'multiline':True]['text':' Computing MAX ','line_number':2992,'multiline':True]['text':' Computing MAX ','line_number':2997,'multiline':True]['text':' Computing MAX ','line_number':3002,'multiline':True]['text':' Computing MAX ','line_number':3005,'multiline':True]['text':'
   Computing MAX
   Computing 2nd power
','line_number':3010,'multiline':True]['text':'
                   Path 2a - underdetermined, with many more columns
                             than rows.
','line_number':3020,'multiline':True]['text':' Computing MAX ','line_number':3027,'multiline':True]['text':' Computing MAX ','line_number':3032,'multiline':True]['text':' Computing MAX ','line_number':3037,'multiline':True]['text':' Computing MAX ','line_number':3043,'multiline':True]['text':' Computing MAX ','line_number':3047,'multiline':True]['text':' Computing MAX ','line_number':3051,'multiline':True]['text':'
       XXX: Ensure the Path 2a case below is triggered.  The workspace
       calculation should use queries for all routines eventually.
   Computing MAX
   Computing MAX
','line_number':3054,'multiline':True]['text':'                 Path 2 - underdetermined. ','line_number':3067,'multiline':True]['text':' Computing MAX ','line_number':3071,'multiline':True]['text':' Computing MAX ','line_number':3076,'multiline':True]['text':' Computing MAX ','line_number':3081,'multiline':True]['text':' Computing MAX ','line_number':3085,'multiline':True]['text':'     Quick return if possible. ','line_number':3108,'multiline':True]['text':'     Get machine parameters. ','line_number':3115,'multiline':True]['text':'     Scale A if max entry outside range [SMLNUM,BIGNUM]. ','line_number':3123,'multiline':True]['text':'        Scale matrix norm up to SMLNUM ','line_number':3129,'multiline':True]['text':'        Scale matrix norm down to BIGNUM. ','line_number':3136,'multiline':True]['text':'        Matrix all zero. Return zero solution. ','line_number':3143,'multiline':True]['text':'     Scale B if max entry outside range [SMLNUM,BIGNUM]. ','line_number':3153,'multiline':True]['text':'        Scale matrix norm up to SMLNUM. ','line_number':3159,'multiline':True]['text':'        Scale matrix norm down to BIGNUM. ','line_number':3166,'multiline':True]['text':'     If M < N make sure B(M+1:N,:) = 0 ','line_number':3173,'multiline':True]['text':'     Overdetermined case. ','line_number':3180,'multiline':True]['text':'        Path 1 - overdetermined or exactly determined. ','line_number':3184,'multiline':True]['text':'           Path 1a - overdetermined, with many more rows than columns ','line_number':3189,'multiline':True]['text':'
             Compute A=Q*R.
             (RWorkspace: need N)
             (CWorkspace: need N, prefer N*NB)
','line_number':3195,'multiline':True]['text':'
             Multiply B by transpose(Q).
             (RWorkspace: need N)
             (CWorkspace: need NRHS, prefer NRHS*NB)
','line_number':3205,'multiline':True]['text':'           Zero out below R. ','line_number':3215,'multiline':True]['text':'
          Bidiagonalize R in A.
          (RWorkspace: need N)
          (CWorkspace: need 2*N+MM, prefer 2*N+(MM+N)*NB)
','line_number':3231,'multiline':True]['text':'
          Multiply B by transpose of left bidiagonalizing vectors of R.
          (CWorkspace: need 2*N+NRHS, prefer 2*N+NRHS*NB)
','line_number':3241,'multiline':True]['text':'        Solve the bidiagonal least squares problem. ','line_number':3250,'multiline':True]['text':'        Multiply B by right bidiagonalizing vectors of R. ','line_number':3258,'multiline':True]['text':' if(complicated condition) ','line_number':3264,'multiline':True]['text':' Computing MAX ','line_number':3265,'multiline':True]['text':'
          Path 2a - underdetermined, with many more columns than rows
          and sufficient workspace for an efficient algorithm.
','line_number':3270,'multiline':True]['text':'
   Computing MAX
   Computing MAX
','line_number':3276,'multiline':True]['text':'
          Compute A=L*Q.
          (CWorkspace: need 2*M, prefer M+M*NB)
','line_number':3290,'multiline':True]['text':'        Copy L to WORK(IL), zeroing out above its diagonal. ','line_number':3300,'multiline':True]['text':'
          Bidiagonalize L in WORK(IL).
          (RWorkspace: need M)
          (CWorkspace: need M*M+4*M, prefer M*M+4*M+2*M*NB)
','line_number':3313,'multiline':True]['text':'
          Multiply B by transpose of left bidiagonalizing vectors of L.
          (CWorkspace: need M*M+4*M+NRHS, prefer M*M+4*M+NRHS*NB)
','line_number':3323,'multiline':True]['text':'        Solve the bidiagonal least squares problem. ','line_number':3332,'multiline':True]['text':'        Multiply B by right bidiagonalizing vectors of L. ','line_number':3341,'multiline':True]['text':'        Zero out below first M rows of B. ','line_number':3347,'multiline':True]['text':'
          Multiply transpose(Q) by B.
          (CWorkspace: need NRHS, prefer NRHS*NB)
','line_number':3354,'multiline':True]['text':'        Path 2 - remaining underdetermined cases. ','line_number':3365,'multiline':True]['text':'
          Bidiagonalize A.
          (RWorkspace: need M)
          (CWorkspace: need 2*M+N, prefer 2*M+(M+N)*NB)
','line_number':3373,'multiline':True]['text':'
          Multiply B by transpose of left bidiagonalizing vectors.
          (CWorkspace: need 2*M+NRHS, prefer 2*M+NRHS*NB)
','line_number':3383,'multiline':True]['text':'        Solve the bidiagonal least squares problem. ','line_number':3392,'multiline':True]['text':'        Multiply B by right bidiagonalizing vectors of A. ','line_number':3401,'multiline':True]['text':'     Undo scaling. ','line_number':3410,'multiline':True]['text':'     End of ZGELSD ','line_number':3437,'multiline':True]['text':' zgelsd_ ','line_number':3439,'multiline':True]['text':' Subroutine ','line_number':3441,'multiline':True]['text':' System generated locals ','line_number':3444,'multiline':True]['text':' Local variables ','line_number':3448,'multiline':True]['text':' Subroutine ','line_number':3451,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    ZGEQR2 computes a QR factorization of a complex m by n matrix A:
    A = Q * R.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the m by n matrix A.
            On exit, the elements on and above the diagonal of the array
            contain the min(m,n) by n upper trapezoidal matrix R (R is
            upper triangular if m >= n); the elements below the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of elementary reflectors (see Further Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX*16 array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace) COMPLEX*16 array, dimension (N)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(1) H(2) . . . H(k), where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; v(i+1:m) is stored on exit in A(i+1:m,i),
    and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':3457,'multiline':True]['text':' Parameter adjustments ','line_number':3521,'multiline':True]['text':' Function Body ','line_number':3528,'multiline':True]['text':'        Generate elementary reflector H(i) to annihilate A(i+1:m,i) ','line_number':3548,'multiline':True]['text':' Computing MIN ','line_number':3551,'multiline':True]['text':'           Apply H(i)' to A(i:m,i+1:n) from the left ','line_number':3557,'multiline':True]['text':' L10: ','line_number':3571,'multiline':True]['text':'     End of ZGEQR2 ','line_number':3575,'multiline':True]['text':' zgeqr2_ ','line_number':3577,'multiline':True]['text':' Subroutine ','line_number':3579,'multiline':True]['text':' System generated locals ','line_number':3583,'multiline':True]['text':' Local variables ','line_number':3586,'multiline':True]['text':' Subroutine ','line_number':3588,'multiline':True]['text':' Subroutine ','line_number':3593,'multiline':True]['text':' Subroutine ','line_number':3598,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGEQRF computes a QR factorization of a complex M-by-N matrix A:
    A = Q * R.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit, the elements on and above the diagonal of the array
            contain the min(M,N)-by-N upper trapezoidal matrix R (R is
            upper triangular if m >= n); the elements below the diagonal,
            with the array TAU, represent the unitary matrix Q as a
            product of min(m,n) elementary reflectors (see Further
            Details).

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    TAU     (output) COMPLEX*16 array, dimension (min(M,N))
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= max(1,N).
            For optimum performance LWORK >= N*NB, where NB is
            the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The matrix Q is represented as a product of elementary reflectors

       Q = H(1) H(2) . . . H(k), where k = min(m,n).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i-1) = 0 and v(i) = 1; v(i+1:m) is stored on exit in A(i+1:m,i),
    and tau in TAU(i).

    =====================================================================


       Test the input arguments
','line_number':3605,'multiline':True]['text':' Parameter adjustments ','line_number':3681,'multiline':True]['text':' Function Body ','line_number':3688,'multiline':True]['text':'     Quick return if possible ','line_number':3712,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':3725,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':3735,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':3741,'multiline':True]['text':' Computing MAX ','line_number':3747,'multiline':True]['text':'        Use blocked code initially ','line_number':3757,'multiline':True]['text':' Computing MIN ','line_number':3762,'multiline':True]['text':'
             Compute the QR factorization of the current block
             A(i:m,i:i+ib-1)
','line_number':3766,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':3776,'multiline':True]['text':'              Apply H' to A(i:m,i+ib:n) from the left ','line_number':3785,'multiline':True]['text':' L10: ','line_number':3794,'multiline':True]['text':'     Use unblocked code to factor the last or only block. ','line_number':3800,'multiline':True]['text':'     End of ZGEQRF ','line_number':3812,'multiline':True]['text':' zgeqrf_ ','line_number':3814,'multiline':True]['text':' Subroutine ','line_number':3816,'multiline':True]['text':' System generated locals ','line_number':3821,'multiline':True]['text':' Local variables ','line_number':3825,'multiline':True]['text':' Subroutine ','line_number':3833,'multiline':True]['text':' Subroutine ','line_number':3841,'multiline':True]['text':' Subroutine ','line_number':3844,'multiline':True]['text':' Subroutine ','line_number':3848,'multiline':True]['text':' Subroutine ','line_number':3859,'multiline':True]['text':' Subroutine ','line_number':3871,'multiline':True]['text':' Subroutine ','line_number':3876,'multiline':True]['text':' Subroutine ','line_number':3882,'multiline':True]['text':' Subroutine ','line_number':3889,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010
       8-15-00:  Improve consistency of WS calculations (eca)


    Purpose
    =======

    ZGESDD computes the singular value decomposition (SVD) of a complex
    M-by-N matrix A, optionally computing the left and/or right singular
    vectors, by using divide-and-conquer method. The SVD is written

         A = U * SIGMA * conjugate-transpose(V)

    where SIGMA is an M-by-N matrix which is zero except for its
    min(m,n) diagonal elements, U is an M-by-M unitary matrix, and
    V is an N-by-N unitary matrix.  The diagonal elements of SIGMA
    are the singular values of A; they are real and non-negative, and
    are returned in descending order.  The first min(m,n) columns of
    U and V are the left and right singular vectors of A.

    Note that the routine returns VT = V**H, not V.

    The divide and conquer algorithm makes very mild assumptions about
    floating point arithmetic. It will work on machines with a guard
    digit in add/subtract, or on those binary machines without guard
    digits which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or
    Cray-2. It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    JOBZ    (input) CHARACTER*1
            Specifies options for computing all or part of the matrix U:
            = 'A':  all M columns of U and all N rows of V**H are
                    returned in the arrays U and VT;
            = 'S':  the first min(M,N) columns of U and the first
                    min(M,N) rows of V**H are returned in the arrays U
                    and VT;
            = 'O':  If M >= N, the first N columns of U are overwritten
                    in the array A and all rows of V**H are returned in
                    the array VT;
                    otherwise, all columns of U are returned in the
                    array U and the first M rows of V**H are overwritten
                    in the array A;
            = 'N':  no columns of U or rows of V**H are computed.

    M       (input) INTEGER
            The number of rows of the input matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the input matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the M-by-N matrix A.
            On exit,
            if JOBZ = 'O',  A is overwritten with the first N columns
                            of U (the left singular vectors, stored
                            columnwise) if M >= N;
                            A is overwritten with the first M rows
                            of V**H (the right singular vectors, stored
                            rowwise) otherwise.
            if JOBZ .ne. 'O', the contents of A are destroyed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    S       (output) DOUBLE PRECISION array, dimension (min(M,N))
            The singular values of A, sorted so that S(i) >= S(i+1).

    U       (output) COMPLEX*16 array, dimension (LDU,UCOL)
            UCOL = M if JOBZ = 'A' or JOBZ = 'O' and M < N;
            UCOL = min(M,N) if JOBZ = 'S'.
            If JOBZ = 'A' or JOBZ = 'O' and M < N, U contains the M-by-M
            unitary matrix U;
            if JOBZ = 'S', U contains the first min(M,N) columns of U
            (the left singular vectors, stored columnwise);
            if JOBZ = 'O' and M >= N, or JOBZ = 'N', U is not referenced.

    LDU     (input) INTEGER
            The leading dimension of the array U.  LDU >= 1; if
            JOBZ = 'S' or 'A' or JOBZ = 'O' and M < N, LDU >= M.

    VT      (output) COMPLEX*16 array, dimension (LDVT,N)
            If JOBZ = 'A' or JOBZ = 'O' and M >= N, VT contains the
            N-by-N unitary matrix V**H;
            if JOBZ = 'S', VT contains the first min(M,N) rows of
            V**H (the right singular vectors, stored rowwise);
            if JOBZ = 'O' and M < N, or JOBZ = 'N', VT is not referenced.

    LDVT    (input) INTEGER
            The leading dimension of the array VT.  LDVT >= 1; if
            JOBZ = 'A' or JOBZ = 'O' and M >= N, LDVT >= N;
            if JOBZ = 'S', LDVT >= min(M,N).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= 1.
            if JOBZ = 'N', LWORK >= 2*min(M,N)+max(M,N).
            if JOBZ = 'O',
                  LWORK >= 2*min(M,N)*min(M,N)+2*min(M,N)+max(M,N).
            if JOBZ = 'S' or 'A',
                  LWORK >= min(M,N)*min(M,N)+2*min(M,N)+max(M,N).
            For good performance, LWORK should generally be larger.

            If LWORK = -1, a workspace query is assumed.  The optimal
            size for the WORK array is calculated and stored in WORK(1),
            and no other work except argument checking is performed.

    RWORK   (workspace) DOUBLE PRECISION array, dimension (MAX(1,LRWORK))
            If JOBZ = 'N', LRWORK >= 5*min(M,N).
            Otherwise,
            LRWORK >= min(M,N)*max(5*min(M,N)+7,2*max(M,N)+2*min(M,N)+1)

    IWORK   (workspace) INTEGER array, dimension (8*min(M,N))

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  The updating process of DBDSDC did not converge.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Huan Ren, Computer Science Division, University of
       California at Berkeley, USA

    =====================================================================


       Test the input arguments
','line_number':3894,'multiline':True]['text':' Parameter adjustments ','line_number':4034,'multiline':True]['text':' Function Body ','line_number':4049,'multiline':True]['text':'
       Compute workspace
        (Note: Comments in the code beginning "Workspace:" describe the
         minimal amount of workspace needed at that point in the code,
         as well as the preferred amount for good performance.
         CWorkspace refers to complex workspace, and RWorkspace to
         real workspace. NB refers to the optimal block size for the
         immediately following subroutine, as returned by ILAENV.)
','line_number':4078,'multiline':True]['text':'
             There is no complex work space needed for bidiagonal SVD
             The real work space needed for bidiagonal SVD is BDSPAC
             for computing singular values and singular vectors; BDSPAN
             for computing singular values only.
             BDSPAC = 5*N*N + 7*N
             BDSPAN = MAX(7*N+4, 3*N+2+SMLSIZ*(SMLSIZ+8))
','line_number':4091,'multiline':True]['text':'                 Path 1 (M much larger than N, JOBZ='N') ','line_number':4103,'multiline':True]['text':' Computing MAX ','line_number':4107,'multiline':True]['text':'                 Path 2 (M much larger than N, JOBZ='O') ','line_number':4115,'multiline':True]['text':' Computing MAX ','line_number':4119,'multiline':True]['text':' Computing MAX ','line_number':4123,'multiline':True]['text':' Computing MAX ','line_number':4128,'multiline':True]['text':' Computing MAX ','line_number':4133,'multiline':True]['text':'                 Path 3 (M much larger than N, JOBZ='S') ','line_number':4142,'multiline':True]['text':' Computing MAX ','line_number':4146,'multiline':True]['text':' Computing MAX ','line_number':4150,'multiline':True]['text':' Computing MAX ','line_number':4155,'multiline':True]['text':' Computing MAX ','line_number':4160,'multiline':True]['text':'                 Path 4 (M much larger than N, JOBZ='A') ','line_number':4169,'multiline':True]['text':' Computing MAX ','line_number':4173,'multiline':True]['text':' Computing MAX ','line_number':4177,'multiline':True]['text':' Computing MAX ','line_number':4182,'multiline':True]['text':' Computing MAX ','line_number':4187,'multiline':True]['text':'              Path 5 (M much larger than N, but not as much as MNTHR1) ','line_number':4197,'multiline':True]['text':' Computing MAX ','line_number':4203,'multiline':True]['text':' Computing MAX ','line_number':4208,'multiline':True]['text':' Computing MAX ','line_number':4216,'multiline':True]['text':' Computing MAX ','line_number':4221,'multiline':True]['text':' Computing MAX ','line_number':4227,'multiline':True]['text':' Computing MAX ','line_number':4232,'multiline':True]['text':'              Path 6 (M at least N, but not much larger) ','line_number':4240,'multiline':True]['text':' Computing MAX ','line_number':4246,'multiline':True]['text':' Computing MAX ','line_number':4251,'multiline':True]['text':' Computing MAX ','line_number':4259,'multiline':True]['text':' Computing MAX ','line_number':4264,'multiline':True]['text':' Computing MAX ','line_number':4270,'multiline':True]['text':' Computing MAX ','line_number':4275,'multiline':True]['text':'
             There is no complex work space needed for bidiagonal SVD
             The real work space needed for bidiagonal SVD is BDSPAC
             for computing singular values and singular vectors; BDSPAN
             for computing singular values only.
             BDSPAC = 5*M*M + 7*M
             BDSPAN = MAX(7*M+4, 3*M+2+SMLSIZ*(SMLSIZ+8))
','line_number':4284,'multiline':True]['text':'                 Path 1t (N much larger than M, JOBZ='N') ','line_number':4296,'multiline':True]['text':' Computing MAX ','line_number':4300,'multiline':True]['text':'                 Path 2t (N much larger than M, JOBZ='O') ','line_number':4308,'multiline':True]['text':' Computing MAX ','line_number':4312,'multiline':True]['text':' Computing MAX ','line_number':4316,'multiline':True]['text':' Computing MAX ','line_number':4321,'multiline':True]['text':' Computing MAX ','line_number':4326,'multiline':True]['text':'                 Path 3t (N much larger than M, JOBZ='S') ','line_number':4335,'multiline':True]['text':' Computing MAX ','line_number':4339,'multiline':True]['text':' Computing MAX ','line_number':4343,'multiline':True]['text':' Computing MAX ','line_number':4348,'multiline':True]['text':' Computing MAX ','line_number':4353,'multiline':True]['text':'                 Path 4t (N much larger than M, JOBZ='A') ','line_number':4362,'multiline':True]['text':' Computing MAX ','line_number':4366,'multiline':True]['text':' Computing MAX ','line_number':4370,'multiline':True]['text':' Computing MAX ','line_number':4375,'multiline':True]['text':' Computing MAX ','line_number':4380,'multiline':True]['text':'              Path 5t (N much larger than M, but not as much as MNTHR1) ','line_number':4390,'multiline':True]['text':' Computing MAX ','line_number':4396,'multiline':True]['text':' Computing MAX ','line_number':4401,'multiline':True]['text':' Computing MAX ','line_number':4409,'multiline':True]['text':' Computing MAX ','line_number':4414,'multiline':True]['text':' Computing MAX ','line_number':4420,'multiline':True]['text':' Computing MAX ','line_number':4425,'multiline':True]['text':'              Path 6t (N greater than M, but not much larger) ','line_number':4433,'multiline':True]['text':' Computing MAX ','line_number':4439,'multiline':True]['text':' Computing MAX ','line_number':4444,'multiline':True]['text':' Computing MAX ','line_number':4452,'multiline':True]['text':' Computing MAX ','line_number':4457,'multiline':True]['text':' Computing MAX ','line_number':4463,'multiline':True]['text':' Computing MAX ','line_number':4468,'multiline':True]['text':'     Quick returns ','line_number':4485,'multiline':True]['text':'     Get machine constants ','line_number':4499,'multiline':True]['text':'     Scale A if max element outside range [SMLNUM,BIGNUM] ','line_number':4505,'multiline':True]['text':'
          A has at least as many rows as columns. If A has sufficiently
          more rows than columns, first reduce using the QR
          decomposition (if sufficient workspace available)
','line_number':4521,'multiline':True]['text':'
                Path 1 (M much larger than N, JOBZ='N')
                No singular vectors to be computed
','line_number':4531,'multiline':True]['text':'
                Compute A=Q*R
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: need 0)
','line_number':4539,'multiline':True]['text':'              Zero out below R ','line_number':4549,'multiline':True]['text':'
                Bidiagonalize R in A
                (CWorkspace: need 3*N, prefer 2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4560,'multiline':True]['text':'
                Perform bidiagonal SVD, compute singular values only
                (CWorkspace: 0)
                (RWorkspace: need BDSPAN)
','line_number':4571,'multiline':True]['text':'
                Path 2 (M much larger than N, JOBZ='O')
                N left singular vectors to be overwritten on A and
                N right singular vectors to be computed in VT
','line_number':4582,'multiline':True]['text':'              WORK(IU) is N by N ','line_number':4590,'multiline':True]['text':'                 WORK(IR) is M by N ','line_number':4596,'multiline':True]['text':'
                Compute A=Q*R
                (CWorkspace: need N*N+2*N, prefer M*N+N+N*NB)
                (RWorkspace: 0)
','line_number':4605,'multiline':True]['text':'              Copy R to WORK( IR ), zeroing out below it ','line_number':4615,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4623,'multiline':True]['text':'
                Bidiagonalize R in WORK(IR)
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4637,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of R in WORK(IRU) and computing right singular vectors
                of R in WORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':4647,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by the left singular vectors of R
                (CWorkspace: need 2*N*N+3*N, prefer M*N+N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4662,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by the right singular vectors of R
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4675,'multiline':True]['text':'
                Multiply Q in A by left singular vectors of R in
                WORK(IU), storing result in WORK(IR) and copying to A
                (CWorkspace: need 2*N*N, prefer N*N+M*N)
                (RWorkspace: 0)
','line_number':4688,'multiline':True]['text':' Computing MIN ','line_number':4699,'multiline':True]['text':' L10: ','line_number':4707,'multiline':True]['text':'
                Path 3 (M much larger than N, JOBZ='S')
                N left singular vectors to be computed in U and
                N right singular vectors to be computed in VT
','line_number':4712,'multiline':True]['text':'              WORK(IR) is N by N ','line_number':4720,'multiline':True]['text':'
                Compute A=Q*R
                (CWorkspace: need N*N+2*N, prefer N*N+N+N*NB)
                (RWorkspace: 0)
','line_number':4726,'multiline':True]['text':'              Copy R to WORK(IR), zeroing out below it ','line_number':4736,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4744,'multiline':True]['text':'
                Bidiagonalize R in WORK(IR)
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4758,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':4768,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of R
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4783,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of R
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4795,'multiline':True]['text':'
                Multiply Q in A by left singular vectors of R in
                WORK(IR), storing result in U
                (CWorkspace: need N*N)
                (RWorkspace: 0)
','line_number':4808,'multiline':True]['text':'
                Path 4 (M much larger than N, JOBZ='A')
                M left singular vectors to be computed in U and
                N right singular vectors to be computed in VT
','line_number':4821,'multiline':True]['text':'              WORK(IU) is N by N ','line_number':4829,'multiline':True]['text':'
                Compute A=Q*R, copying result to U
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4835,'multiline':True]['text':'
                Generate Q in U
                (CWorkspace: need N+M, prefer N+M*NB)
                (RWorkspace: 0)
','line_number':4846,'multiline':True]['text':'              Produce R in A, zeroing out below it ','line_number':4856,'multiline':True]['text':'
                Bidiagonalize R in A
                (CWorkspace: need 3*N, prefer 2*N+2*N*NB)
                (RWorkspace: need N)
','line_number':4867,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':4880,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by left singular vectors of R
                (CWorkspace: need N*N+3*N, prefer N*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':4892,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of R
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':4905,'multiline':True]['text':'
                Multiply Q in U by left singular vectors of R in
                WORK(IU), storing result in A
                (CWorkspace: need N*N)
                (RWorkspace: 0)
','line_number':4918,'multiline':True]['text':'              Copy left singular vectors of A from A to U ','line_number':4928,'multiline':True]['text':'
             MNTHR2 <= M < MNTHR1

             Path 5 (M much larger than N, but not as much as MNTHR1)
             Reduce to bidiagonal form without QR decomposition, use
             ZUNGBR and matrix multiplication to compute singular vectors
','line_number':4936,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*N+M, prefer 2*N+(M+N)*NB)
             (RWorkspace: need N)
','line_number':4950,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':4961,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':4975,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need 2*N, prefer N+N*NB)
                (RWorkspace: 0)
','line_number':4986,'multiline':True]['text':'                 WORK( IU ) is M by N ','line_number':4998,'multiline':True]['text':'                 WORK(IU) is LDWRKU by N ','line_number':5003,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5009,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in WORK(IU), copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need 3*N*N)
','line_number':5021,'multiline':True]['text':'
                Multiply Q in A by real matrix RWORK(IRU), storing the
                result in WORK(IU), copying to A
                (CWorkspace: need N*N, prefer M*N)
                (Rworkspace: need 3*N*N, prefer N*N+2*M*N)
','line_number':5032,'multiline':True]['text':' Computing MIN ','line_number':5044,'multiline':True]['text':' L20: ','line_number':5051,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5056,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5067,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5078,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need 3*N*N)
','line_number':5093,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: need 0)
                (Rworkspace: need N*N+2*M*N)
','line_number':5104,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5117,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: 0)
','line_number':5128,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5139,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need 3*N*N)
','line_number':5154,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: 0)
                (Rworkspace: need 3*N*N)
','line_number':5165,'multiline':True]['text':'
             M .LT. MNTHR2

             Path 6 (M at least N, but not much larger)
             Reduce to bidiagonal form without QR decomposition
             Use ZUNMBR to compute singular vectors
','line_number':5180,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*N+M, prefer 2*N+(M+N)*NB)
             (RWorkspace: need N)
','line_number':5194,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':5205,'multiline':True]['text':'                 WORK( IU ) is M by N ','line_number':5220,'multiline':True]['text':'                 WORK( IU ) is LDWRKU by N ','line_number':5225,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5231,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (Cworkspace: need 2*N, prefer N+N*NB)
                (Rworkspace: need 0)
','line_number':5243,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by left singular vectors of A, copying
                to A
                (Cworkspace: need M*N+2*N, prefer M*N+N+N*NB)
                (Rworkspace: need 0)
','line_number':5258,'multiline':True]['text':'
                   Generate Q in A
                   (Cworkspace: need 2*N, prefer N+N*NB)
                   (Rworkspace: need 0)
','line_number':5275,'multiline':True]['text':'
                   Multiply Q in A by real matrix RWORK(IRU), storing the
                   result in WORK(IU), copying to A
                   (CWorkspace: need N*N, prefer M*N)
                   (Rworkspace: need 3*N*N, prefer N*N+2*M*N)
','line_number':5285,'multiline':True]['text':' Computing MIN ','line_number':5297,'multiline':True]['text':' L30: ','line_number':5304,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5310,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':5325,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':5338,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5352,'multiline':True]['text':'              Set the right corner of U to identity matrix ','line_number':5367,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 2*N+M, prefer 2*N+M*NB)
                (RWorkspace: 0)
','line_number':5377,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 3*N, prefer 2*N+N*NB)
                (RWorkspace: 0)
','line_number':5389,'multiline':True]['text':'
          A has more columns than rows. If A has sufficiently more
          columns than rows, first reduce using the LQ decomposition (if
          sufficient workspace available)
','line_number':5407,'multiline':True]['text':'
                Path 1t (N much larger than M, JOBZ='N')
                No singular vectors to be computed
','line_number':5417,'multiline':True]['text':'
                Compute A=L*Q
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5425,'multiline':True]['text':'              Zero out above L ','line_number':5435,'multiline':True]['text':'
                Bidiagonalize L in A
                (CWorkspace: need 3*M, prefer 2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5446,'multiline':True]['text':'
                Perform bidiagonal SVD, compute singular values only
                (CWorkspace: 0)
                (RWorkspace: need BDSPAN)
','line_number':5457,'multiline':True]['text':'
                Path 2t (N much larger than M, JOBZ='O')
                M right singular vectors to be overwritten on A and
                M left singular vectors to be computed in U
','line_number':5468,'multiline':True]['text':'              WORK(IVT) is M by M ','line_number':5477,'multiline':True]['text':'                 WORK(IL) M by N ','line_number':5482,'multiline':True]['text':'                 WORK(IL) is M by CHUNK ','line_number':5488,'multiline':True]['text':'
                Compute A=L*Q
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5496,'multiline':True]['text':'              Copy L to WORK(IL), zeroing about above it ','line_number':5506,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need M*M+2*M, prefer M*M+M+M*NB)
                (RWorkspace: 0)
','line_number':5514,'multiline':True]['text':'
                Bidiagonalize L in WORK(IL)
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5528,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5538,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix WORK(IU)
                Overwrite WORK(IU) by the left singular vectors of L
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':5553,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix WORK(IVT)
                Overwrite WORK(IVT) by the right singular vectors of L
                (CWorkspace: need N*N+3*N, prefer M*N+2*N+N*NB)
                (RWorkspace: 0)
','line_number':5565,'multiline':True]['text':'
                Multiply right singular vectors of L in WORK(IL) by Q
                in A, storing result in WORK(IL) and copying to A
                (CWorkspace: need 2*M*M, prefer M*M+M*N))
                (RWorkspace: 0)
','line_number':5578,'multiline':True]['text':' Computing MIN ','line_number':5589,'multiline':True]['text':' L40: ','line_number':5597,'multiline':True]['text':'
               Path 3t (N much larger than M, JOBZ='S')
               M right singular vectors to be computed in VT and
               M left singular vectors to be computed in U
','line_number':5602,'multiline':True]['text':'              WORK(IL) is M by M ','line_number':5610,'multiline':True]['text':'
                Compute A=L*Q
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5616,'multiline':True]['text':'              Copy L to WORK(IL), zeroing out above it ','line_number':5626,'multiline':True]['text':'
                Generate Q in A
                (CWorkspace: need M*M+2*M, prefer M*M+M+M*NB)
                (RWorkspace: 0)
','line_number':5634,'multiline':True]['text':'
                Bidiagonalize L in WORK(IL)
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5648,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5658,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of L
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+M*NB)
                (RWorkspace: 0)
','line_number':5673,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by left singular vectors of L
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+M*NB)
                (RWorkspace: 0)
','line_number':5685,'multiline':True]['text':'
                Copy VT to WORK(IL), multiply right singular vectors of L
                in WORK(IL) by Q in A, storing result in VT
                (CWorkspace: need M*M)
                (RWorkspace: 0)
','line_number':5698,'multiline':True]['text':'
                Path 9t (N much larger than M, JOBZ='A')
                N right singular vectors to be computed in VT and
                M left singular vectors to be computed in U
','line_number':5711,'multiline':True]['text':'              WORK(IVT) is M by M ','line_number':5719,'multiline':True]['text':'
                Compute A=L*Q, copying result to VT
                (CWorkspace: need 2*M, prefer M+M*NB)
                (RWorkspace: 0)
','line_number':5725,'multiline':True]['text':'
                Generate Q in VT
                (CWorkspace: need M+N, prefer M+N*NB)
                (RWorkspace: 0)
','line_number':5736,'multiline':True]['text':'              Produce L in A, zeroing out above it ','line_number':5746,'multiline':True]['text':'
                Bidiagonalize L in A
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+2*M*NB)
                (RWorkspace: need M)
','line_number':5757,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5767,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of L
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: 0)
','line_number':5782,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix WORK(IVT)
                Overwrite WORK(IVT) by right singular vectors of L
                (CWorkspace: need M*M+3*M, prefer M*M+2*M+M*NB)
                (RWorkspace: 0)
','line_number':5794,'multiline':True]['text':'
                Multiply right singular vectors of L in WORK(IVT) by
                Q in VT, storing result in A
                (CWorkspace: need M*M)
                (RWorkspace: 0)
','line_number':5807,'multiline':True]['text':'              Copy right singular vectors of A from A to VT ','line_number':5817,'multiline':True]['text':'
             MNTHR2 <= N < MNTHR1

             Path 5t (N much larger than M, but not as much as MNTHR1)
             Reduce to bidiagonal form without QR decomposition, use
             ZUNGBR and matrix multiplication to compute singular vectors
','line_number':5825,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*M+N, prefer 2*M+(M+N)*NB)
             (RWorkspace: M)
','line_number':5840,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':5852,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5866,'multiline':True]['text':'
                Generate P**H in A
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5877,'multiline':True]['text':'                 WORK( IVT ) is M by N ','line_number':5890,'multiline':True]['text':'                 WORK( IVT ) is M by CHUNK ','line_number':5896,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5902,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRVT)
                storing the result in WORK(IVT), copying to U
                (Cworkspace: need 0)
                (Rworkspace: need 2*M*M)
','line_number':5914,'multiline':True]['text':'
                Multiply RWORK(IRVT) by P**H in A, storing the
                result in WORK(IVT), copying to A
                (CWorkspace: need M*M, prefer M*N)
                (Rworkspace: need 2*M*M, prefer 2*M*N)
','line_number':5925,'multiline':True]['text':' Computing MIN ','line_number':5937,'multiline':True]['text':' L50: ','line_number':5944,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5948,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':5959,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':5970,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: need 0)
                (Rworkspace: need 3*M*M)
','line_number':5985,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need M*M+2*M*N)
','line_number':5996,'multiline':True]['text':'
                Copy A to U, generate Q
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':6009,'multiline':True]['text':'
                Copy A to VT, generate P**H
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: 0)
','line_number':6020,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':6031,'multiline':True]['text':'
                Multiply Q in U by real matrix RWORK(IRU), storing the
                result in A, copying to U
                (CWorkspace: need 0)
                (Rworkspace: need 3*M*M)
','line_number':6046,'multiline':True]['text':'
                Multiply real matrix RWORK(IRVT) by P**H in VT,
                storing the result in A, copying to VT
                (Cworkspace: need 0)
                (Rworkspace: need M*M+2*M*N)
','line_number':6057,'multiline':True]['text':'
             N .LT. MNTHR2

             Path 6t (N greater than M, but not much larger)
             Reduce to bidiagonal form without LQ decomposition
             Use ZUNMBR to compute singular vectors
','line_number':6071,'multiline':True]['text':'
             Bidiagonalize A
             (CWorkspace: need 2*M+N, prefer 2*M+(M+N)*NB)
             (RWorkspace: M)
','line_number':6085,'multiline':True]['text':'
                Compute singular values only
                (Cworkspace: 0)
                (Rworkspace: need BDSPAN)
','line_number':6096,'multiline':True]['text':'                 WORK( IVT ) is M by N ','line_number':6109,'multiline':True]['text':'                 WORK( IVT ) is M by CHUNK ','line_number':6115,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':6121,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (Cworkspace: need 2*M, prefer M+M*NB)
                (Rworkspace: need 0)
','line_number':6136,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix WORK(IVT)
                Overwrite WORK(IVT) by right singular vectors of A,
                copying to A
                (Cworkspace: need M*N+2*M, prefer M*N+M+M*NB)
                (Rworkspace: need 0)
','line_number':6150,'multiline':True]['text':'
                   Generate P**H in A
                   (Cworkspace: need 2*M, prefer M+M*NB)
                   (Rworkspace: need 0)
','line_number':6166,'multiline':True]['text':'
                   Multiply Q in A by real matrix RWORK(IRU), storing the
                   result in WORK(IU), copying to A
                   (CWorkspace: need M*M, prefer M*N)
                   (Rworkspace: need 3*M*M, prefer M*M+2*M*N)
','line_number':6176,'multiline':True]['text':' Computing MIN ','line_number':6188,'multiline':True]['text':' L60: ','line_number':6195,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':6200,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: M*M)
','line_number':6215,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: M*M)
','line_number':6227,'multiline':True]['text':'
                Perform bidiagonal SVD, computing left singular vectors
                of bidiagonal matrix in RWORK(IRU) and computing right
                singular vectors of bidiagonal matrix in RWORK(IRVT)
                (CWorkspace: need 0)
                (RWorkspace: need BDSPAC)
','line_number':6242,'multiline':True]['text':'
                Copy real matrix RWORK(IRU) to complex matrix U
                Overwrite U by left singular vectors of A
                (CWorkspace: need 3*M, prefer 2*M+M*NB)
                (RWorkspace: M*M)
','line_number':6258,'multiline':True]['text':'              Set all of VT to identity matrix ','line_number':6270,'multiline':True]['text':'
                Copy real matrix RWORK(IRVT) to complex matrix VT
                Overwrite VT by right singular vectors of A
                (CWorkspace: need 2*M+N, prefer 2*M+N*NB)
                (RWorkspace: M*M)
','line_number':6274,'multiline':True]['text':'     Undo scaling if necessary ','line_number':6292,'multiline':True]['text':'     Return optimal workspace in WORK(1) ','line_number':6315,'multiline':True]['text':'     End of ZGESDD ','line_number':6321,'multiline':True]['text':' zgesdd_ ','line_number':6323,'multiline':True]['text':' Subroutine ','line_number':6325,'multiline':True]['text':' System generated locals ','line_number':6329,'multiline':True]['text':' Local variables ','line_number':6332,'multiline':True]['text':' Subroutine ','line_number':6333,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGESV computes the solution to a complex system of linear equations
       A * X = B,
    where A is an N-by-N matrix and X and B are N-by-NRHS matrices.

    The LU decomposition with partial pivoting and row interchanges is
    used to factor A as
       A = P * L * U,
    where P is a permutation matrix, L is unit lower triangular, and U is
    upper triangular.  The factored form of A is then used to solve the
    system of equations A * X = B.

    Arguments
    =========

    N       (input) INTEGER
            The number of linear equations, i.e., the order of the
            matrix A.  N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrix B.  NRHS >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the N-by-N coefficient matrix A.
            On exit, the factors L and U from the factorization
            A = P*L*U; the unit diagonal elements of L are not stored.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    IPIV    (output) INTEGER array, dimension (N)
            The pivot indices that define the permutation matrix P;
            row i of the matrix was interchanged with row IPIV(i).

    B       (input/output) COMPLEX*16 array, dimension (LDB,NRHS)
            On entry, the N-by-NRHS matrix of right hand side matrix B.
            On exit, if INFO = 0, the N-by-NRHS solution matrix X.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, U(i,i) is exactly zero.  The factorization
                  has been completed, but the factor U is exactly
                  singular, so the solution could not be computed.

    =====================================================================


       Test the input parameters.
','line_number':6339,'multiline':True]['text':' Parameter adjustments ','line_number':6403,'multiline':True]['text':' Function Body ','line_number':6412,'multiline':True]['text':'     Compute the LU factorization of A. ','line_number':6429,'multiline':True]['text':'        Solve the system A*X = B, overwriting B with X. ','line_number':6434,'multiline':True]['text':'     End of ZGESV ','line_number':6441,'multiline':True]['text':' zgesv_ ','line_number':6443,'multiline':True]['text':' Subroutine ','line_number':6445,'multiline':True]['text':' System generated locals ','line_number':6448,'multiline':True]['text':' Local variables ','line_number':6452,'multiline':True]['text':' Subroutine ','line_number':6455,'multiline':True]['text':' Subroutine ','line_number':6461,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGETF2 computes an LU factorization of a general m-by-n matrix A
    using partial pivoting with row interchanges.

    The factorization has the form
       A = P * L * U
    where P is a permutation matrix, L is lower triangular with unit
    diagonal elements (lower trapezoidal if m > n), and U is upper
    triangular (upper trapezoidal if m < n).

    This is the right-looking Level 2 BLAS version of the algorithm.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the m by n matrix to be factored.
            On exit, the factors L and U from the factorization
            A = P*L*U; the unit diagonal elements of L are not stored.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    IPIV    (output) INTEGER array, dimension (min(M,N))
            The pivot indices; for 1 <= i <= min(M,N), row i of the
            matrix was interchanged with row IPIV(i).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value
            > 0: if INFO = k, U(k,k) is exactly zero. The factorization
                 has been completed, but the factor U is exactly
                 singular, and division by zero will occur if it is used
                 to solve a system of equations.

    =====================================================================


       Test the input parameters.
','line_number':6465,'multiline':True]['text':' Parameter adjustments ','line_number':6521,'multiline':True]['text':' Function Body ','line_number':6527,'multiline':True]['text':'     Quick return if possible ','line_number':6542,'multiline':True]['text':'     Compute machine safe minimum ','line_number':6548,'multiline':True]['text':'        Find pivot and test for singularity. ','line_number':6555,'multiline':True]['text':'           Apply the interchange to columns 1:N. ','line_number':6563,'multiline':True]['text':'           Compute elements J+1:M of J-th column. ','line_number':6569,'multiline':True]['text':' L20: ','line_number':6583,'multiline':True]['text':'           Update trailing submatrix. ','line_number':6595,'multiline':True]['text':' L10: ','line_number':6604,'multiline':True]['text':'     End of ZGETF2 ','line_number':6608,'multiline':True]['text':' zgetf2_ ','line_number':6610,'multiline':True]['text':' Subroutine ','line_number':6612,'multiline':True]['text':' System generated locals ','line_number':6615,'multiline':True]['text':' Local variables ','line_number':6619,'multiline':True]['text':' Subroutine ','line_number':6621,'multiline':True]['text':' Subroutine ','line_number':6631,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGETRF computes an LU factorization of a general M-by-N matrix A
    using partial pivoting with row interchanges.

    The factorization has the form
       A = P * L * U
    where P is a permutation matrix, L is lower triangular with unit
    diagonal elements (lower trapezoidal if m > n), and U is upper
    triangular (upper trapezoidal if m < n).

    This is the right-looking Level 3 BLAS version of the algorithm.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the M-by-N matrix to be factored.
            On exit, the factors L and U from the factorization
            A = P*L*U; the unit diagonal elements of L are not stored.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    IPIV    (output) INTEGER array, dimension (min(M,N))
            The pivot indices; for 1 <= i <= min(M,N), row i of the
            matrix was interchanged with row IPIV(i).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, U(i,i) is exactly zero. The factorization
                  has been completed, but the factor U is exactly
                  singular, and division by zero will occur if it is used
                  to solve a system of equations.

    =====================================================================


       Test the input parameters.
','line_number':6635,'multiline':True]['text':' Parameter adjustments ','line_number':6691,'multiline':True]['text':' Function Body ','line_number':6697,'multiline':True]['text':'     Quick return if possible ','line_number':6712,'multiline':True]['text':'     Determine the block size for this environment. ','line_number':6718,'multiline':True]['text':'        Use unblocked code. ','line_number':6724,'multiline':True]['text':'        Use blocked code. ','line_number':6729,'multiline':True]['text':' Computing MIN ','line_number':6734,'multiline':True]['text':'
             Factor diagonal and subdiagonal blocks and test for exact
             singularity.
','line_number':6738,'multiline':True]['text':'           Adjust INFO and the pivot indices. ','line_number':6746,'multiline':True]['text':' Computing MIN ','line_number':6751,'multiline':True]['text':' L10: ','line_number':6756,'multiline':True]['text':'           Apply interchanges to columns 1:J-1. ','line_number':6759,'multiline':True]['text':'              Apply interchanges to columns J+JB:N. ','line_number':6767,'multiline':True]['text':'              Compute block row of U. ','line_number':6774,'multiline':True]['text':'                 Update trailing submatrix. ','line_number':6782,'multiline':True]['text':' L20: ','line_number':6793,'multiline':True]['text':'     End of ZGETRF ','line_number':6798,'multiline':True]['text':' zgetrf_ ','line_number':6800,'multiline':True]['text':' Subroutine ','line_number':6802,'multiline':True]['text':' System generated locals ','line_number':6806,'multiline':True]['text':' Local variables ','line_number':6809,'multiline':True]['text':' Subroutine ','line_number':6811,'multiline':True]['text':' Subroutine ','line_number':6816,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZGETRS solves a system of linear equations
       A * X = B,  A**T * X = B,  or  A**H * X = B
    with a general N-by-N matrix A using the LU factorization computed
    by ZGETRF.

    Arguments
    =========

    TRANS   (input) CHARACTER*1
            Specifies the form of the system of equations:
            = 'N':  A * X = B     (No transpose)
            = 'T':  A**T * X = B  (Transpose)
            = 'C':  A**H * X = B  (Conjugate transpose)

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrix B.  NRHS >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,N)
            The factors L and U from the factorization A = P*L*U
            as computed by ZGETRF.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    IPIV    (input) INTEGER array, dimension (N)
            The pivot indices from ZGETRF; for 1<=i<=N, row i of the
            matrix was interchanged with row IPIV(i).

    B       (input/output) COMPLEX*16 array, dimension (LDB,NRHS)
            On entry, the right hand side matrix B.
            On exit, the solution matrix X.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':6820,'multiline':True]['text':' Parameter adjustments ','line_number':6879,'multiline':True]['text':' Function Body ','line_number':6888,'multiline':True]['text':'     Quick return if possible ','line_number':6909,'multiline':True]['text':'
          Solve A * X = B.

          Apply row interchanges to the right hand sides.
','line_number':6917,'multiline':True]['text':'        Solve L*X = B, overwriting B with X. ','line_number':6925,'multiline':True]['text':'        Solve U*X = B, overwriting B with X. ','line_number':6930,'multiline':True]['text':'
          Solve A**T * X = B  or A**H * X = B.

          Solve U'*X = B, overwriting B with X.
','line_number':6936,'multiline':True]['text':'        Solve L'*X = B, overwriting B with X. ','line_number':6945,'multiline':True]['text':'        Apply row interchanges to the solution vectors. ','line_number':6950,'multiline':True]['text':'     End of ZGETRS ','line_number':6957,'multiline':True]['text':' zgetrs_ ','line_number':6959,'multiline':True]['text':' Subroutine ','line_number':6961,'multiline':True]['text':' System generated locals ','line_number':6966,'multiline':True]['text':' Local variables ','line_number':6970,'multiline':True]['text':' Subroutine ','line_number':6977,'multiline':True]['text':' Subroutine ','line_number':6991,'multiline':True]['text':' Subroutine ','line_number':6996,'multiline':True]['text':' Subroutine ','line_number':7004,'multiline':True]['text':' Subroutine ','line_number':7012,'multiline':True]['text':'
    -- LAPACK driver routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZHEEVD computes all eigenvalues and, optionally, eigenvectors of a
    complex Hermitian matrix A.  If eigenvectors are desired, it uses a
    divide and conquer algorithm.

    The divide and conquer algorithm makes very mild assumptions about
    floating point arithmetic. It will work on machines with a guard
    digit in add/subtract, or on those binary machines without guard
    digits which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or
    Cray-2. It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    JOBZ    (input) CHARACTER*1
            = 'N':  Compute eigenvalues only;
            = 'V':  Compute eigenvalues and eigenvectors.

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA, N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the
            leading N-by-N upper triangular part of A contains the
            upper triangular part of the matrix A.  If UPLO = 'L',
            the leading N-by-N lower triangular part of A contains
            the lower triangular part of the matrix A.
            On exit, if JOBZ = 'V', then if INFO = 0, A contains the
            orthonormal eigenvectors of the matrix A.
            If JOBZ = 'N', then on exit the lower triangle (if UPLO='L')
            or the upper triangle (if UPLO='U') of A, including the
            diagonal, is destroyed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    W       (output) DOUBLE PRECISION array, dimension (N)
            If INFO = 0, the eigenvalues in ascending order.

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The length of the array WORK.
            If N <= 1,                LWORK must be at least 1.
            If JOBZ  = 'N' and N > 1, LWORK must be at least N + 1.
            If JOBZ  = 'V' and N > 1, LWORK must be at least 2*N + N**2.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal sizes of the WORK, RWORK and
            IWORK arrays, returns these values as the first entries of
            the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    RWORK   (workspace/output) DOUBLE PRECISION array,
                                           dimension (LRWORK)
            On exit, if INFO = 0, RWORK(1) returns the optimal LRWORK.

    LRWORK  (input) INTEGER
            The dimension of the array RWORK.
            If N <= 1,                LRWORK must be at least 1.
            If JOBZ  = 'N' and N > 1, LRWORK must be at least N.
            If JOBZ  = 'V' and N > 1, LRWORK must be at least
                           1 + 5*N + 2*N**2.

            If LRWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    IWORK   (workspace/output) INTEGER array, dimension (MAX(1,LIWORK))
            On exit, if INFO = 0, IWORK(1) returns the optimal LIWORK.

    LIWORK  (input) INTEGER
            The dimension of the array IWORK.
            If N <= 1,                LIWORK must be at least 1.
            If JOBZ  = 'N' and N > 1, LIWORK must be at least 1.
            If JOBZ  = 'V' and N > 1, LIWORK must be at least 3 + 5*N.

            If LIWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i and JOBZ = 'N', then the algorithm failed
                  to converge; i off-diagonal elements of an intermediate
                  tridiagonal form did not converge to zero;
                  if INFO = i and JOBZ = 'V', then the algorithm failed
                  to compute an eigenvalue while working on the submatrix
                  lying in rows and columns INFO/(N+1) through
                  mod(INFO,N+1).

    Further Details
    ===============

    Based on contributions by
       Jeff Rutter, Computer Science Division, University of California
       at Berkeley, USA

    Modified description of INFO. Sven, 16 Feb 05.
    =====================================================================


       Test the input parameters.
','line_number':7017,'multiline':True]['text':' Parameter adjustments ','line_number':7142,'multiline':True]['text':' Function Body ','line_number':7151,'multiline':True]['text':' Computing 2nd power ','line_number':7178,'multiline':True]['text':' Computing MAX ','line_number':7187,'multiline':True]['text':'     Quick return if possible ','line_number':7215,'multiline':True]['text':'     Get machine constants. ','line_number':7231,'multiline':True]['text':'     Scale matrix to allowable range, if necessary. ','line_number':7240,'multiline':True]['text':'     Call ZHETRD to reduce Hermitian matrix to tridiagonal form. ','line_number':7256,'multiline':True]['text':'
       For eigenvalues only, call DSTERF.  For eigenvectors, first call
       ZSTEDC to generate the eigenvector matrix, WORK(INDWRK), of the
       tridiagonal matrix, then call ZUNMTR to multiply it to the
       Householder transformations represented as Householder vectors in
       A.
','line_number':7269,'multiline':True]['text':'     If matrix was scaled, then rescale eigenvalues appropriately. ','line_number':7287,'multiline':True]['text':'     End of ZHEEVD ','line_number':7305,'multiline':True]['text':' zheevd_ ','line_number':7307,'multiline':True]['text':' Subroutine ','line_number':7309,'multiline':True]['text':' System generated locals ','line_number':7313,'multiline':True]['text':' Local variables ','line_number':7318,'multiline':True]['text':' Subroutine ','line_number':7321,'multiline':True]['text':' Double Complex ','line_number':7326,'multiline':True]['text':' Subroutine ','line_number':7328,'multiline':True]['text':' Subroutine ','line_number':7332,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZHETD2 reduces a complex Hermitian matrix A to real symmetric
    tridiagonal form T by a unitary similarity transformation:
    Q' * A * Q = T.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            Hermitian matrix A is stored:
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            n-by-n upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n-by-n lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.
            On exit, if UPLO = 'U', the diagonal and first superdiagonal
            of A are overwritten by the corresponding elements of the
            tridiagonal matrix T, and the elements above the first
            superdiagonal, with the array TAU, represent the unitary
            matrix Q as a product of elementary reflectors; if UPLO
            = 'L', the diagonal and first subdiagonal of A are over-
            written by the corresponding elements of the tridiagonal
            matrix T, and the elements below the first subdiagonal, with
            the array TAU, represent the unitary matrix Q as a product
            of elementary reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    D       (output) DOUBLE PRECISION array, dimension (N)
            The diagonal elements of the tridiagonal matrix T:
            D(i) = A(i,i).

    E       (output) DOUBLE PRECISION array, dimension (N-1)
            The off-diagonal elements of the tridiagonal matrix T:
            E(i) = A(i,i+1) if UPLO = 'U', E(i) = A(i+1,i) if UPLO = 'L'.

    TAU     (output) COMPLEX*16 array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    If UPLO = 'U', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(n-1) . . . H(2) H(1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(i+1:n) = 0 and v(i) = 1; v(1:i-1) is stored on exit in
    A(1:i-1,i+1), and tau in TAU(i).

    If UPLO = 'L', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(1) H(2) . . . H(n-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0 and v(i+1) = 1; v(i+2:n) is stored on exit in A(i+2:n,i),
    and tau in TAU(i).

    The contents of A on exit are illustrated by the following examples
    with n = 5:

    if UPLO = 'U':                       if UPLO = 'L':

      (  d   e   v2  v3  v4 )              (  d                  )
      (      d   e   v3  v4 )              (  e   d              )
      (          d   e   v4 )              (  v1  e   d          )
      (              d   e  )              (  v1  v2  e   d      )
      (                  d  )              (  v1  v2  v3  e   d  )

    where d and e denote diagonal and off-diagonal elements of T, and vi
    denotes an element of the vector defining H(i).

    =====================================================================


       Test the input parameters
','line_number':7338,'multiline':True]['text':' Parameter adjustments ','line_number':7451,'multiline':True]['text':' Function Body ','line_number':7459,'multiline':True]['text':'     Quick return if possible ','line_number':7475,'multiline':True]['text':'        Reduce the upper triangle of A ','line_number':7483,'multiline':True]['text':'
             Generate elementary reflector H(i) = I - tau * v * v'
             to annihilate A(1:i-1,i+1)
','line_number':7491,'multiline':True]['text':'              Apply H(i) from both sides to A(1:i,1:i) ','line_number':7504,'multiline':True]['text':'              Compute  x := tau * A * v  storing x in TAU(1:i) ','line_number':7509,'multiline':True]['text':'              Compute  w := x - 1/2 * tau * (x'*v) * v ','line_number':7515,'multiline':True]['text':'
                Apply the transformation as a rank-2 update:
                   A := A - v * w' - w * v'
','line_number':7528,'multiline':True]['text':' L10: ','line_number':7551,'multiline':True]['text':'        Reduce the lower triangle of A ','line_number':7557,'multiline':True]['text':'
             Generate elementary reflector H(i) = I - tau * v * v'
             to annihilate A(i+2:n,i)
','line_number':7566,'multiline':True]['text':' Computing MIN ','line_number':7574,'multiline':True]['text':'              Apply H(i) from both sides to A(i+1:n,i+1:n) ','line_number':7583,'multiline':True]['text':'              Compute  x := tau * A * v  storing y in TAU(i:n-1) ','line_number':7588,'multiline':True]['text':'              Compute  w := x - 1/2 * tau * (x'*v) * v ','line_number':7595,'multiline':True]['text':'
                Apply the transformation as a rank-2 update:
                   A := A - v * w' - w * v'
','line_number':7610,'multiline':True]['text':' L20: ','line_number':7635,'multiline':True]['text':'     End of ZHETD2 ','line_number':7644,'multiline':True]['text':' zhetd2_ ','line_number':7646,'multiline':True]['text':' Subroutine ','line_number':7648,'multiline':True]['text':' System generated locals ','line_number':7652,'multiline':True]['text':' Local variables ','line_number':7656,'multiline':True]['text':' Subroutine ','line_number':7661,'multiline':True]['text':' Subroutine ','line_number':7667,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZHETRD reduces a complex Hermitian matrix A to real symmetric
    tridiagonal form T by a unitary similarity transformation:
    Q**H * A * Q = T.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            N-by-N upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading N-by-N lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.
            On exit, if UPLO = 'U', the diagonal and first superdiagonal
            of A are overwritten by the corresponding elements of the
            tridiagonal matrix T, and the elements above the first
            superdiagonal, with the array TAU, represent the unitary
            matrix Q as a product of elementary reflectors; if UPLO
            = 'L', the diagonal and first subdiagonal of A are over-
            written by the corresponding elements of the tridiagonal
            matrix T, and the elements below the first subdiagonal, with
            the array TAU, represent the unitary matrix Q as a product
            of elementary reflectors. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    D       (output) DOUBLE PRECISION array, dimension (N)
            The diagonal elements of the tridiagonal matrix T:
            D(i) = A(i,i).

    E       (output) DOUBLE PRECISION array, dimension (N-1)
            The off-diagonal elements of the tridiagonal matrix T:
            E(i) = A(i,i+1) if UPLO = 'U', E(i) = A(i+1,i) if UPLO = 'L'.

    TAU     (output) COMPLEX*16 array, dimension (N-1)
            The scalar factors of the elementary reflectors (see Further
            Details).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.  LWORK >= 1.
            For optimum performance LWORK >= N*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    If UPLO = 'U', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(n-1) . . . H(2) H(1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(i+1:n) = 0 and v(i) = 1; v(1:i-1) is stored on exit in
    A(1:i-1,i+1), and tau in TAU(i).

    If UPLO = 'L', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(1) H(2) . . . H(n-1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0 and v(i+1) = 1; v(i+2:n) is stored on exit in A(i+2:n,i),
    and tau in TAU(i).

    The contents of A on exit are illustrated by the following examples
    with n = 5:

    if UPLO = 'U':                       if UPLO = 'L':

      (  d   e   v2  v3  v4 )              (  d                  )
      (      d   e   v3  v4 )              (  e   d              )
      (          d   e   v4 )              (  v1  e   d          )
      (              d   e  )              (  v1  v2  e   d      )
      (                  d  )              (  v1  v2  v3  e   d  )

    where d and e denote diagonal and off-diagonal elements of T, and vi
    denotes an element of the vector defining H(i).

    =====================================================================


       Test the input parameters
','line_number':7674,'multiline':True]['text':' Parameter adjustments ','line_number':7798,'multiline':True]['text':' Function Body ','line_number':7807,'multiline':True]['text':'        Determine the block size. ','line_number':7823,'multiline':True]['text':'     Quick return if possible ','line_number':7839,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code
          (last block is always handled by unblocked code).

   Computing MAX
','line_number':7850,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':7861,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  determine the
                minimum value of NB, and reduce NB or force use of
                unblocked code by setting NX = N.

   Computing MAX
','line_number':7867,'multiline':True]['text':'
          Reduce the upper triangle of A.
          Columns 1:kk are handled by the unblocked method.
','line_number':7891,'multiline':True]['text':'
             Reduce columns i:i+nb-1 to tridiagonal form and form the
             matrix W which is needed to update the unreduced part of
             the matrix
','line_number':7902,'multiline':True]['text':'
             Update the unreduced submatrix A(1:i-1,1:i-1), using an
             update of the form:  A := A - V*W' - W*V'
','line_number':7912,'multiline':True]['text':'
             Copy superdiagonal elements back into A, and diagonal
             elements into D
','line_number':7922,'multiline':True]['text':' L10: ','line_number':7935,'multiline':True]['text':' L20: ','line_number':7937,'multiline':True]['text':'        Use unblocked code to reduce the last or only block ','line_number':7940,'multiline':True]['text':'        Reduce the lower triangle of A ','line_number':7945,'multiline':True]['text':'
             Reduce columns i:i+nb-1 to tridiagonal form and form the
             matrix W which is needed to update the unreduced part of
             the matrix
','line_number':7951,'multiline':True]['text':'
             Update the unreduced submatrix A(i+nb:n,i+nb:n), using
             an update of the form:  A := A - V*W' - W*V'
','line_number':7961,'multiline':True]['text':'
             Copy subdiagonal elements back into A, and diagonal
             elements into D
','line_number':7972,'multiline':True]['text':' L30: ','line_number':7985,'multiline':True]['text':' L40: ','line_number':7987,'multiline':True]['text':'        Use unblocked code to reduce the last or only block ','line_number':7990,'multiline':True]['text':'     End of ZHETRD ','line_number':8000,'multiline':True]['text':' zhetrd_ ','line_number':8002,'multiline':True]['text':' Subroutine ','line_number':8004,'multiline':True]['text':' System generated locals ','line_number':8009,'multiline':True]['text':' Local variables ','line_number':8016,'multiline':True]['text':' was [49][49] ','line_number':8017,'multiline':True]['text':' Subroutine ','line_number':8023,'multiline':True]['text':' Subroutine ','line_number':8031,'multiline':True]['text':'
    -- LAPACK computational routine (version 3.2.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       June 2010

       Purpose
       =======

       ZHSEQR computes the eigenvalues of a Hessenberg matrix H
       and, optionally, the matrices T and Z from the Schur decomposition
       H = Z T Z**H, where T is an upper triangular matrix (the
       Schur form), and Z is the unitary matrix of Schur vectors.

       Optionally Z may be postmultiplied into an input unitary
       matrix Q so that this routine can give the Schur factorization
       of a matrix A which has been reduced to the Hessenberg form H
       by the unitary matrix Q:  A = Q*H*Q**H = (QZ)*H*(QZ)**H.

       Arguments
       =========

       JOB   (input) CHARACTER*1
             = 'E':  compute eigenvalues only;
             = 'S':  compute eigenvalues and the Schur form T.

       COMPZ (input) CHARACTER*1
             = 'N':  no Schur vectors are computed;
             = 'I':  Z is initialized to the unit matrix and the matrix Z
                     of Schur vectors of H is returned;
             = 'V':  Z must contain an unitary matrix Q on entry, and
                     the product Q*Z is returned.

       N     (input) INTEGER
             The order of the matrix H.  N .GE. 0.

       ILO   (input) INTEGER
       IHI   (input) INTEGER
             It is assumed that H is already upper triangular in rows
             and columns 1:ILO-1 and IHI+1:N. ILO and IHI are normally
             set by a previous call to ZGEBAL, and then passed to ZGEHRD
             when the matrix output by ZGEBAL is reduced to Hessenberg
             form. Otherwise ILO and IHI should be set to 1 and N
             respectively.  If N.GT.0, then 1.LE.ILO.LE.IHI.LE.N.
             If N = 0, then ILO = 1 and IHI = 0.

       H     (input/output) COMPLEX*16 array, dimension (LDH,N)
             On entry, the upper Hessenberg matrix H.
             On exit, if INFO = 0 and JOB = 'S', H contains the upper
             triangular matrix T from the Schur decomposition (the
             Schur form). If INFO = 0 and JOB = 'E', the contents of
             H are unspecified on exit.  (The output value of H when
             INFO.GT.0 is given under the description of INFO below.)

             Unlike earlier versions of ZHSEQR, this subroutine may
             explicitly H(i,j) = 0 for i.GT.j and j = 1, 2, ... ILO-1
             or j = IHI+1, IHI+2, ... N.

       LDH   (input) INTEGER
             The leading dimension of the array H. LDH .GE. max(1,N).

       W        (output) COMPLEX*16 array, dimension (N)
             The computed eigenvalues. If JOB = 'S', the eigenvalues are
             stored in the same order as on the diagonal of the Schur
             form returned in H, with W(i) = H(i,i).

       Z     (input/output) COMPLEX*16 array, dimension (LDZ,N)
             If COMPZ = 'N', Z is not referenced.
             If COMPZ = 'I', on entry Z need not be set and on exit,
             if INFO = 0, Z contains the unitary matrix Z of the Schur
             vectors of H.  If COMPZ = 'V', on entry Z must contain an
             N-by-N matrix Q, which is assumed to be equal to the unit
             matrix except for the submatrix Z(ILO:IHI,ILO:IHI). On exit,
             if INFO = 0, Z contains Q*Z.
             Normally Q is the unitary matrix generated by ZUNGHR
             after the call to ZGEHRD which formed the Hessenberg matrix
             H. (The output value of Z when INFO.GT.0 is given under
             the description of INFO below.)

       LDZ   (input) INTEGER
             The leading dimension of the array Z.  if COMPZ = 'I' or
             COMPZ = 'V', then LDZ.GE.MAX(1,N).  Otherwize, LDZ.GE.1.

       WORK  (workspace/output) COMPLEX*16 array, dimension (LWORK)
             On exit, if INFO = 0, WORK(1) returns an estimate of
             the optimal value for LWORK.

       LWORK (input) INTEGER
             The dimension of the array WORK.  LWORK .GE. max(1,N)
             is sufficient and delivers very good and sometimes
             optimal performance.  However, LWORK as large as 11*N
             may be required for optimal performance.  A workspace
             query is recommended to determine the optimal workspace
             size.

             If LWORK = -1, then ZHSEQR does a workspace query.
             In this case, ZHSEQR checks the input parameters and
             estimates the optimal workspace size for the given
             values of N, ILO and IHI.  The estimate is returned
             in WORK(1).  No error message related to LWORK is
             issued by XERBLA.  Neither H nor Z are accessed.


       INFO  (output) INTEGER
               =  0:  successful exit
             .LT. 0:  if INFO = -i, the i-th argument had an illegal
                      value
             .GT. 0:  if INFO = i, ZHSEQR failed to compute all of
                  the eigenvalues.  Elements 1:ilo-1 and i+1:n of WR
                  and WI contain those eigenvalues which have been
                  successfully computed.  (Failures are rare.)

                  If INFO .GT. 0 and JOB = 'E', then on exit, the
                  remaining unconverged eigenvalues are the eigen-
                  values of the upper Hessenberg matrix rows and
                  columns ILO through INFO of the final, output
                  value of H.

                  If INFO .GT. 0 and JOB   = 'S', then on exit

             (*)  (initial value of H)*U  = U*(final value of H)

                  where U is a unitary matrix.  The final
                  value of  H is upper Hessenberg and triangular in
                  rows and columns INFO+1 through IHI.

                  If INFO .GT. 0 and COMPZ = 'V', then on exit

                    (final value of Z)  =  (initial value of Z)*U

                  where U is the unitary matrix in (*) (regard-
                  less of the value of JOB.)

                  If INFO .GT. 0 and COMPZ = 'I', then on exit
                        (final value of Z)  = U
                  where U is the unitary matrix in (*) (regard-
                  less of the value of JOB.)

                  If INFO .GT. 0 and COMPZ = 'N', then Z is not
                  accessed.

       ================================================================
               Default values supplied by
               ILAENV(ISPEC,'ZHSEQR',JOB(:1)//COMPZ(:1),N,ILO,IHI,LWORK).
               It is suggested that these defaults be adjusted in order
               to attain best performance in each particular
               computational environment.

              ISPEC=12: The ZLAHQR vs ZLAQR0 crossover point.
                        Default: 75. (Must be at least 11.)

              ISPEC=13: Recommended deflation window size.
                        This depends on ILO, IHI and NS.  NS is the
                        number of simultaneous shifts returned
                        by ILAENV(ISPEC=15).  (See ISPEC=15 below.)
                        The default for (IHI-ILO+1).LE.500 is NS.
                        The default for (IHI-ILO+1).GT.500 is 3*NS/2.

              ISPEC=14: Nibble crossover point. (See IPARMQ for
                        details.)  Default: 14% of deflation window
                        size.

              ISPEC=15: Number of simultaneous shifts in a multishift
                        QR iteration.

                        If IHI-ILO+1 is ...

                        greater than      ...but less    ... the
                        or equal to ...      than        default is

                             1               30          NS =   2(+)
                            30               60          NS =   4(+)
                            60              150          NS =  10(+)
                           150              590          NS =  **
                           590             3000          NS =  64
                          3000             6000          NS = 128
                          6000             infinity      NS = 256

                    (+)  By default some or all matrices of this order
                         are passed to the implicit double shift routine
                         ZLAHQR and this parameter is ignored.  See
                         ISPEC=12 above and comments in IPARMQ for
                         details.

                   (**)  The asterisks (**) indicate an ad-hoc
                         function of N increasing from 10 to 64.

              ISPEC=16: Select structured matrix multiply.
                        If the number of simultaneous shifts (specified
                        by ISPEC=15) is less than 14, then the default
                        for ISPEC=16 is 0.  Otherwise the default for
                        ISPEC=16 is 2.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       References:
         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part I: Maintaining Well Focused Shifts, and Level 3
         Performance, SIAM Journal of Matrix Analysis, volume 23, pages
         929--947, 2002.

         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part II: Aggressive Early Deflation, SIAM Journal
         of Matrix Analysis, volume 23, pages 948--973, 2002.

       ================================================================

       ==== Matrices of order NTINY or smaller must be processed by
       .    ZLAHQR because of insufficient subdiagonal scratch space.
       .    (This is a hard limit.) ====

       ==== NL allocates some local workspace to help small matrices
       .    through a rare ZLAHQR failure.  NL .GT. NTINY = 11 is
       .    required and NL .LE. NMIN = ILAENV(ISPEC=12,...) is recom-
       .    mended.  (The default value of NMIN is 75.)  Using NL = 49
       .    allows up to six simultaneous shifts and a 16-by-16
       .    deflation window.  ====

       ==== Decode and check the input parameters. ====
','line_number':8041,'multiline':True]['text':' Parameter adjustments ','line_number':8265,'multiline':True]['text':' Function Body ','line_number':8275,'multiline':True]['text':'        ==== Quick return in case of invalid argument. ==== ','line_number':8305,'multiline':True]['text':'        ==== Quick return in case N = 0; nothing to do. ==== ','line_number':8313,'multiline':True]['text':'        ==== Quick return in case of a workspace query ==== ','line_number':8319,'multiline':True]['text':'
          ==== Ensure reported workspace size is backward-compatible with
          .    previous LAPACK versions. ====
   Computing MAX
','line_number':8323,'multiline':True]['text':'        ==== copy eigenvalues isolated by ZGEBAL ==== ','line_number':8336,'multiline':True]['text':'        ==== Initialize Z, if requested ==== ','line_number':8350,'multiline':True]['text':'        ==== Quick return if possible ==== ','line_number':8357,'multiline':True]['text':'
          ==== ZLAHQR/ZLAQR0 crossover point ====

   Writing concatenation
','line_number':8366,'multiline':True]['text':'        ==== ZLAQR0 for big matrices; ZLAHQR for small ones ==== ','line_number':8378,'multiline':True]['text':'           ==== Small matrix ==== ','line_number':8385,'multiline':True]['text':'
                ==== A rare ZLAHQR failure!  ZLAQR0 sometimes succeeds
                .    when ZLAHQR fails. ====
','line_number':8392,'multiline':True]['text':'
                   ==== Larger matrices have enough subdiagonal scratch
                   .    space to call ZLAQR0 directly. ====
','line_number':8401,'multiline':True]['text':'
                   ==== Tiny matrices don't have enough subdiagonal
                   .    scratch space to benefit from ZLAQR0.  Hence,
                   .    tiny matrices must be copied into a larger
                   .    array before calling ZLAQR0. ====
','line_number':8412,'multiline':True]['text':'        ==== Clear out the trash, if necessary. ==== ','line_number':8435,'multiline':True]['text':'
          ==== Ensure reported workspace size is backward-compatible with
          .    previous LAPACK versions. ====

   Computing MAX
','line_number':8443,'multiline':True]['text':'     ==== End of ZHSEQR ==== ','line_number':8455,'multiline':True]['text':' zhseqr_ ','line_number':8458,'multiline':True]['text':' Subroutine ','line_number':8460,'multiline':True]['text':' System generated locals ','line_number':8465,'multiline':True]['text':' Local variables ','line_number':8470,'multiline':True]['text':' Subroutine ','line_number':8473,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLABRD reduces the first NB rows and columns of a complex general
    m by n matrix A to upper or lower real bidiagonal form by a unitary
    transformation Q' * A * P, and returns the matrices X and Y which
    are needed to apply the transformation to the unreduced part of A.

    If m >= n, A is reduced to upper bidiagonal form; if m < n, to lower
    bidiagonal form.

    This is an auxiliary routine called by ZGEBRD

    Arguments
    =========

    M       (input) INTEGER
            The number of rows in the matrix A.

    N       (input) INTEGER
            The number of columns in the matrix A.

    NB      (input) INTEGER
            The number of leading rows and columns of A to be reduced.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the m by n general matrix to be reduced.
            On exit, the first NB rows and columns of the matrix are
            overwritten; the rest of the array is unchanged.
            If m >= n, elements on and below the diagonal in the first NB
              columns, with the array TAUQ, represent the unitary
              matrix Q as a product of elementary reflectors; and
              elements above the diagonal in the first NB rows, with the
              array TAUP, represent the unitary matrix P as a product
              of elementary reflectors.
            If m < n, elements below the diagonal in the first NB
              columns, with the array TAUQ, represent the unitary
              matrix Q as a product of elementary reflectors, and
              elements on and above the diagonal in the first NB rows,
              with the array TAUP, represent the unitary matrix P as
              a product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    D       (output) DOUBLE PRECISION array, dimension (NB)
            The diagonal elements of the first NB rows and columns of
            the reduced matrix.  D(i) = A(i,i).

    E       (output) DOUBLE PRECISION array, dimension (NB)
            The off-diagonal elements of the first NB rows and columns of
            the reduced matrix.

    TAUQ    (output) COMPLEX*16 array dimension (NB)
            The scalar factors of the elementary reflectors which
            represent the unitary matrix Q. See Further Details.

    TAUP    (output) COMPLEX*16 array, dimension (NB)
            The scalar factors of the elementary reflectors which
            represent the unitary matrix P. See Further Details.

    X       (output) COMPLEX*16 array, dimension (LDX,NB)
            The m-by-nb matrix X required to update the unreduced part
            of A.

    LDX     (input) INTEGER
            The leading dimension of the array X. LDX >= max(1,M).

    Y       (output) COMPLEX*16 array, dimension (LDY,NB)
            The n-by-nb matrix Y required to update the unreduced part
            of A.

    LDY     (input) INTEGER
            The leading dimension of the array Y. LDY >= max(1,N).

    Further Details
    ===============

    The matrices Q and P are represented as products of elementary
    reflectors:

       Q = H(1) H(2) . . . H(nb)  and  P = G(1) G(2) . . . G(nb)

    Each H(i) and G(i) has the form:

       H(i) = I - tauq * v * v'  and G(i) = I - taup * u * u'

    where tauq and taup are complex scalars, and v and u are complex
    vectors.

    If m >= n, v(1:i-1) = 0, v(i) = 1, and v(i:m) is stored on exit in
    A(i:m,i); u(1:i) = 0, u(i+1) = 1, and u(i+1:n) is stored on exit in
    A(i,i+1:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    If m < n, v(1:i) = 0, v(i+1) = 1, and v(i+1:m) is stored on exit in
    A(i+2:m,i); u(1:i-1) = 0, u(i) = 1, and u(i:n) is stored on exit in
    A(i,i+1:n); tauq is stored in TAUQ(i) and taup in TAUP(i).

    The elements of the vectors v and u together form the m-by-nb matrix
    V and the nb-by-n matrix U' which are needed, with X and Y, to apply
    the transformation to the unreduced part of the matrix, using a block
    update of the form:  A := A - V*Y' - X*U'.

    The contents of A on exit are illustrated by the following examples
    with nb = 2:

    m = 6 and n = 5 (m > n):          m = 5 and n = 6 (m < n):

      (  1   1   u1  u1  u1 )           (  1   u1  u1  u1  u1  u1 )
      (  v1  1   1   u2  u2 )           (  1   1   u2  u2  u2  u2 )
      (  v1  v2  a   a   a  )           (  v1  1   a   a   a   a  )
      (  v1  v2  a   a   a  )           (  v1  v2  a   a   a   a  )
      (  v1  v2  a   a   a  )           (  v1  v2  a   a   a   a  )
      (  v1  v2  a   a   a  )

    where a denotes an element of the original matrix which is unchanged,
    vi denotes an element of the vector defining H(i), and ui an element
    of the vector defining G(i).

    =====================================================================


       Quick return if possible
','line_number':8481,'multiline':True]['text':' Parameter adjustments ','line_number':8614,'multiline':True]['text':' Function Body ','line_number':8629,'multiline':True]['text':'        Reduce to upper bidiagonal form ','line_number':8636,'multiline':True]['text':'           Update A(i:m,i) ','line_number':8641,'multiline':True]['text':'           Generate reflection Q(i) to annihilate A(i+1:m,i) ','line_number':8660,'multiline':True]['text':' Computing MIN ','line_number':8665,'multiline':True]['text':'              Compute Y(i+1:n,i) ','line_number':8675,'multiline':True]['text':'              Update A(i,i+1:n) ','line_number':8707,'multiline':True]['text':'              Generate reflection P(i) to annihilate A(i,i+2:n) ','line_number':8729,'multiline':True]['text':' Computing MIN ','line_number':8734,'multiline':True]['text':'              Compute X(i+1:m,i) ','line_number':8743,'multiline':True]['text':' L10: ','line_number':8775,'multiline':True]['text':'        Reduce to lower bidiagonal form ','line_number':8779,'multiline':True]['text':'           Update A(i,i:n) ','line_number':8784,'multiline':True]['text':'           Generate reflection P(i) to annihilate A(i,i+1:n) ','line_number':8809,'multiline':True]['text':' Computing MIN ','line_number':8814,'multiline':True]['text':'              Compute X(i+1:m,i) ','line_number':8824,'multiline':True]['text':'              Update A(i+1:m,i) ','line_number':8858,'multiline':True]['text':'              Generate reflection Q(i) to annihilate A(i+2:m,i) ','line_number':8876,'multiline':True]['text':' Computing MIN ','line_number':8881,'multiline':True]['text':'              Compute Y(i+1:n,i) ','line_number':8890,'multiline':True]['text':' L20: ','line_number':8924,'multiline':True]['text':'     End of ZLABRD ','line_number':8929,'multiline':True]['text':' zlabrd_ ','line_number':8931,'multiline':True]['text':' Subroutine ','line_number':8933,'multiline':True]['text':' System generated locals ','line_number':8935,'multiline':True]['text':' Local variables ','line_number':8939,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLACGV conjugates a complex vector of length N.

    Arguments
    =========

    N       (input) INTEGER
            The length of the vector X.  N >= 0.

    X       (input/output) COMPLEX*16 array, dimension
                           (1+(N-1)*abs(INCX))
            On entry, the vector of length N to be conjugated.
            On exit, X is overwritten with conjg(X).

    INCX    (input) INTEGER
            The spacing between successive elements of X.

   =====================================================================
','line_number':8943,'multiline':True]['text':' Parameter adjustments ','line_number':8973,'multiline':True]['text':' Function Body ','line_number':8976,'multiline':True]['text':' L10: ','line_number':8983,'multiline':True]['text':' L20: ','line_number':8996,'multiline':True]['text':'     End of ZLACGV ','line_number':9001,'multiline':True]['text':' zlacgv_ ','line_number':9003,'multiline':True]['text':' Subroutine ','line_number':9005,'multiline':True]['text':' System generated locals ','line_number':9008,'multiline':True]['text':' Local variables ','line_number':9011,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLACP2 copies all or part of a real two-dimensional matrix A to a
    complex matrix B.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies the part of the matrix A to be copied to B.
            = 'U':      Upper triangular part
            = 'L':      Lower triangular part
            Otherwise:  All of the matrix A

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input) DOUBLE PRECISION array, dimension (LDA,N)
            The m by n matrix A.  If UPLO = 'U', only the upper trapezium
            is accessed; if UPLO = 'L', only the lower trapezium is
            accessed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    B       (output) COMPLEX*16 array, dimension (LDB,N)
            On exit, B = A in the locations specified by UPLO.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,M).

    =====================================================================
','line_number':9016,'multiline':True]['text':' Parameter adjustments ','line_number':9062,'multiline':True]['text':' Function Body ','line_number':9070,'multiline':True]['text':' L10: ','line_number':9079,'multiline':True]['text':' L20: ','line_number':9081,'multiline':True]['text':' L30: ','line_number':9092,'multiline':True]['text':' L40: ','line_number':9094,'multiline':True]['text':' L50: ','line_number':9105,'multiline':True]['text':' L60: ','line_number':9107,'multiline':True]['text':'     End of ZLACP2 ','line_number':9113,'multiline':True]['text':' zlacp2_ ','line_number':9115,'multiline':True]['text':' Subroutine ','line_number':9117,'multiline':True]['text':' System generated locals ','line_number':9120,'multiline':True]['text':' Local variables ','line_number':9123,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLACPY copies all or part of a two-dimensional matrix A to another
    matrix B.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies the part of the matrix A to be copied to B.
            = 'U':      Upper triangular part
            = 'L':      Lower triangular part
            Otherwise:  All of the matrix A

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,N)
            The m by n matrix A.  If UPLO = 'U', only the upper trapezium
            is accessed; if UPLO = 'L', only the lower trapezium is
            accessed.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    B       (output) COMPLEX*16 array, dimension (LDB,N)
            On exit, B = A in the locations specified by UPLO.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,M).

    =====================================================================
','line_number':9128,'multiline':True]['text':' Parameter adjustments ','line_number':9174,'multiline':True]['text':' Function Body ','line_number':9182,'multiline':True]['text':' L10: ','line_number':9191,'multiline':True]['text':' L20: ','line_number':9193,'multiline':True]['text':' L30: ','line_number':9204,'multiline':True]['text':' L40: ','line_number':9206,'multiline':True]['text':' L50: ','line_number':9217,'multiline':True]['text':' L60: ','line_number':9219,'multiline':True]['text':'     End of ZLACPY ','line_number':9225,'multiline':True]['text':' zlacpy_ ','line_number':9227,'multiline':True]['text':' Subroutine ','line_number':9229,'multiline':True]['text':' System generated locals ','line_number':9233,'multiline':True]['text':' Local variables ','line_number':9239,'multiline':True]['text':' Subroutine ','line_number':9241,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLACRM performs a very simple matrix-matrix multiplication:
             C := A * B,
    where A is M by N and complex; B is N by N and real;
    C is M by N and complex.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A and of the matrix C.
            M >= 0.

    N       (input) INTEGER
            The number of columns and rows of the matrix B and
            the number of columns of the matrix C.
            N >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA, N)
            A contains the M by N matrix A.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >=max(1,M).

    B       (input) DOUBLE PRECISION array, dimension (LDB, N)
            B contains the N by N matrix B.

    LDB     (input) INTEGER
            The leading dimension of the array B. LDB >=max(1,N).

    C       (input) COMPLEX*16 array, dimension (LDC, N)
            C contains the M by N matrix C.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >=max(1,N).

    RWORK   (workspace) DOUBLE PRECISION array, dimension (2*M*N)

    =====================================================================


       Quick return if possible.
','line_number':9246,'multiline':True]['text':' Parameter adjustments ','line_number':9299,'multiline':True]['text':' Function Body ','line_number':9311,'multiline':True]['text':' L10: ','line_number':9322,'multiline':True]['text':' L20: ','line_number':9324,'multiline':True]['text':' L30: ','line_number':9337,'multiline':True]['text':' L40: ','line_number':9339,'multiline':True]['text':' L50: ','line_number':9347,'multiline':True]['text':' L60: ','line_number':9349,'multiline':True]['text':' L70: ','line_number':9363,'multiline':True]['text':' L80: ','line_number':9365,'multiline':True]['text':'     End of ZLACRM ','line_number':9370,'multiline':True]['text':' zlacrm_ ','line_number':9372,'multiline':True]['text':' Double Complex ','line_number':9374,'multiline':True]['text':' System generated locals ','line_number':9377,'multiline':True]['text':' Local variables ','line_number':9381,'multiline':True]['text':' Subroutine ','line_number':9383,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLADIV := X / Y, where X and Y are complex.  The computation of X / Y
    will not overflow on an intermediary step unless the results
    overflows.

    Arguments
    =========

    X       (input) COMPLEX*16
    Y       (input) COMPLEX*16
            The complex scalars X and Y.

    =====================================================================
','line_number':9387,'multiline':True]['text':'     End of ZLADIV ','line_number':9422,'multiline':True]['text':' zladiv_ ','line_number':9424,'multiline':True]['text':' Subroutine ','line_number':9426,'multiline':True]['text':' System generated locals ','line_number':9430,'multiline':True]['text':' Local variables ','line_number':9434,'multiline':True]['text':' Subroutine ','line_number':9438,'multiline':True]['text':' Subroutine ','line_number':9441,'multiline':True]['text':' Subroutine ','line_number':9449,'multiline':True]['text':' Subroutine ','line_number':9452,'multiline':True]['text':' Subroutine ','line_number':9456,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    Using the divide and conquer method, ZLAED0 computes all eigenvalues
    of a symmetric tridiagonal matrix which is one diagonal block of
    those from reducing a dense or band Hermitian matrix and
    corresponding eigenvectors of the dense or band matrix.

    Arguments
    =========

    QSIZ   (input) INTEGER
           The dimension of the unitary matrix used to reduce
           the full matrix to tridiagonal form.  QSIZ >= N if ICOMPQ = 1.

    N      (input) INTEGER
           The dimension of the symmetric tridiagonal matrix.  N >= 0.

    D      (input/output) DOUBLE PRECISION array, dimension (N)
           On entry, the diagonal elements of the tridiagonal matrix.
           On exit, the eigenvalues in ascending order.

    E      (input/output) DOUBLE PRECISION array, dimension (N-1)
           On entry, the off-diagonal elements of the tridiagonal matrix.
           On exit, E has been destroyed.

    Q      (input/output) COMPLEX*16 array, dimension (LDQ,N)
           On entry, Q must contain an QSIZ x N matrix whose columns
           unitarily orthonormal. It is a part of the unitary matrix
           that reduces the full dense Hermitian matrix to a
           (reducible) symmetric tridiagonal matrix.

    LDQ    (input) INTEGER
           The leading dimension of the array Q.  LDQ >= max(1,N).

    IWORK  (workspace) INTEGER array,
           the dimension of IWORK must be at least
                        6 + 6*N + 5*N*lg N
                        ( lg( N ) = smallest integer k
                                    such that 2^k >= N )

    RWORK  (workspace) DOUBLE PRECISION array,
                                 dimension (1 + 3*N + 2*N*lg N + 3*N**2)
                          ( lg( N ) = smallest integer k
                                      such that 2^k >= N )

    QSTORE (workspace) COMPLEX*16 array, dimension (LDQS, N)
           Used to store parts of
           the eigenvector matrix when the updating matrix multiplies
           take place.

    LDQS   (input) INTEGER
           The leading dimension of the array QSTORE.
           LDQS >= max(1,N).

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  The algorithm failed to compute an eigenvalue while
                  working on the submatrix lying in rows and columns
                  INFO/(N+1) through mod(INFO,N+1).

    =====================================================================

    Warning:      N could be as big as QSIZ!


       Test the input parameters.
','line_number':9461,'multiline':True]['text':' Parameter adjustments ','line_number':9538,'multiline':True]['text':' Function Body ','line_number':9550,'multiline':True]['text':'
       IF( ICOMPQ .LT. 0 .OR. ICOMPQ .GT. 2 ) THEN
          INFO = -1
       ELSE IF( ( ICOMPQ .EQ. 1 ) .AND. ( QSIZ .LT. MAX( 0, N ) ) )
      $        THEN
','line_number':9553,'multiline':True]['text':'     Quick return if possible ','line_number':9574,'multiline':True]['text':'
       Determine the size and placement of the submatrices, and save in
       the leading elements of IWORK.
','line_number':9583,'multiline':True]['text':' L20: ','line_number':9596,'multiline':True]['text':' L30: ','line_number':9605,'multiline':True]['text':'
       Divide the matrix into SUBPBS submatrices of size at most SMLSIZ+1
       using rank-1 modifications (cuts).
','line_number':9608,'multiline':True]['text':' L40: ','line_number':9620,'multiline':True]['text':'
       Set up workspaces for eigenvalues only/accumulate new vectors
       routine
','line_number':9625,'multiline':True]['text':' Computing 2nd power ','line_number':9646,'multiline':True]['text':'     Initialize pointers ','line_number':9649,'multiline':True]['text':' L50: ','line_number':9654,'multiline':True]['text':'
       Solve each submatrix eigenproblem at the bottom of the divide and
       conquer tree.
','line_number':9658,'multiline':True]['text':' Computing 2nd power ','line_number':9679,'multiline':True]['text':' L60: ','line_number':9692,'multiline':True]['text':' L70: ','line_number':9694,'multiline':True]['text':'
       Successively merge eigensystems of adjacent submatrices
       into eigensystem for the corresponding larger matrix.

       while ( SUBPBS > 1 )
','line_number':9697,'multiline':True]['text':'
       Merge lower order eigensystems (of size MSD2 and MATSIZ - MSD2)
       into an eigensystem of size MATSIZ.  ZLAED7 handles the case
       when the eigenvectors of a full or band Hermitian matrix (which
       was reduced to tridiagonal form) are desired.

       I am free to use Q as a valuable working space until Loop 150.
','line_number':9722,'multiline':True]['text':' L90: ','line_number':9742,'multiline':True]['text':'
       end while

       Re-merge the eigenvalues/vectors which were deflated at the final
       merge step.
','line_number':9749,'multiline':True]['text':' L100: ','line_number':9762,'multiline':True]['text':'     End of ZLAED0 ','line_number':9768,'multiline':True]['text':' zlaed0_ ','line_number':9770,'multiline':True]['text':' Subroutine ','line_number':9772,'multiline':True]['text':' System generated locals ','line_number':9779,'multiline':True]['text':' Local variables ','line_number':9782,'multiline':True]['text':' Subroutine ','line_number':9784,'multiline':True]['text':' Subroutine ','line_number':9796,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLAED7 computes the updated eigensystem of a diagonal
    matrix after modification by a rank-one symmetric matrix. This
    routine is used only for the eigenproblem which requires all
    eigenvalues and optionally eigenvectors of a dense or banded
    Hermitian matrix that has been reduced to tridiagonal form.

      T = Q(in) ( D(in) + RHO * Z*Z' ) Q'(in) = Q(out) * D(out) * Q'(out)

      where Z = Q'u, u is a vector of length N with ones in the
      CUTPNT and CUTPNT + 1 th elements and zeros elsewhere.

       The eigenvectors of the original matrix are stored in Q, and the
       eigenvalues are in D.  The algorithm consists of three stages:

          The first stage consists of deflating the size of the problem
          when there are multiple eigenvalues or if there is a zero in
          the Z vector.  For each such occurence the dimension of the
          secular equation problem is reduced by one.  This stage is
          performed by the routine DLAED2.

          The second stage consists of calculating the updated
          eigenvalues. This is done by finding the roots of the secular
          equation via the routine DLAED4 (as called by SLAED3).
          This routine also calculates the eigenvectors of the current
          problem.

          The final stage consists of computing the updated eigenvectors
          directly using the updated eigenvalues.  The eigenvectors for
          the current problem are multiplied with the eigenvectors from
          the overall problem.

    Arguments
    =========

    N      (input) INTEGER
           The dimension of the symmetric tridiagonal matrix.  N >= 0.

    CUTPNT (input) INTEGER
           Contains the location of the last eigenvalue in the leading
           sub-matrix.  min(1,N) <= CUTPNT <= N.

    QSIZ   (input) INTEGER
           The dimension of the unitary matrix used to reduce
           the full matrix to tridiagonal form.  QSIZ >= N.

    TLVLS  (input) INTEGER
           The total number of merging levels in the overall divide and
           conquer tree.

    CURLVL (input) INTEGER
           The current level in the overall merge routine,
           0 <= curlvl <= tlvls.

    CURPBM (input) INTEGER
           The current problem in the current level in the overall
           merge routine (counting from upper left to lower right).

    D      (input/output) DOUBLE PRECISION array, dimension (N)
           On entry, the eigenvalues of the rank-1-perturbed matrix.
           On exit, the eigenvalues of the repaired matrix.

    Q      (input/output) COMPLEX*16 array, dimension (LDQ,N)
           On entry, the eigenvectors of the rank-1-perturbed matrix.
           On exit, the eigenvectors of the repaired tridiagonal matrix.

    LDQ    (input) INTEGER
           The leading dimension of the array Q.  LDQ >= max(1,N).

    RHO    (input) DOUBLE PRECISION
           Contains the subdiagonal element used to create the rank-1
           modification.

    INDXQ  (output) INTEGER array, dimension (N)
           This contains the permutation which will reintegrate the
           subproblem just solved back into sorted order,
           ie. D( INDXQ( I = 1, N ) ) will be in ascending order.

    IWORK  (workspace) INTEGER array, dimension (4*N)

    RWORK  (workspace) DOUBLE PRECISION array,
                                   dimension (3*N+2*QSIZ*N)

    WORK   (workspace) COMPLEX*16 array, dimension (QSIZ*N)

    QSTORE (input/output) DOUBLE PRECISION array, dimension (N**2+1)
           Stores eigenvectors of submatrices encountered during
           divide and conquer, packed together. QPTR points to
           beginning of the submatrices.

    QPTR   (input/output) INTEGER array, dimension (N+2)
           List of indices pointing to beginning of submatrices stored
           in QSTORE. The submatrices are numbered starting at the
           bottom left of the divide and conquer tree, from left to
           right and bottom to top.

    PRMPTR (input) INTEGER array, dimension (N lg N)
           Contains a list of pointers which indicate where in PERM a
           level's permutation is stored.  PRMPTR(i+1) - PRMPTR(i)
           indicates the size of the permutation and also the size of
           the full, non-deflated problem.

    PERM   (input) INTEGER array, dimension (N lg N)
           Contains the permutations (from deflation and sorting) to be
           applied to each eigenblock.

    GIVPTR (input) INTEGER array, dimension (N lg N)
           Contains a list of pointers which indicate where in GIVCOL a
           level's Givens rotations are stored.  GIVPTR(i+1) - GIVPTR(i)
           indicates the number of Givens rotations.

    GIVCOL (input) INTEGER array, dimension (2, N lg N)
           Each pair of numbers indicates a pair of columns to take place
           in a Givens rotation.

    GIVNUM (input) DOUBLE PRECISION array, dimension (2, N lg N)
           Each number indicates the S value to be used in the
           corresponding Givens rotation.

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  if INFO = 1, an eigenvalue did not converge

    =====================================================================


       Test the input parameters.
','line_number':9803,'multiline':True]['text':' Parameter adjustments ','line_number':9942,'multiline':True]['text':' Function Body ','line_number':9959,'multiline':True]['text':'
       IF( ICOMPQ.LT.0 .OR. ICOMPQ.GT.1 ) THEN
          INFO = -1
       ELSE IF( N.LT.0 ) THEN
','line_number':9962,'multiline':True]['text':'     Quick return if possible ','line_number':9982,'multiline':True]['text':'
       The following values are for bookkeeping purposes only.  They are
       integer pointers which indicate the portion of the workspace
       used by a particular array in DLAED2 and SLAED3.
','line_number':9988,'multiline':True]['text':'
       Form the z-vector which consists of the last row of Q_1 and the
       first row of Q_2.
','line_number':10004,'multiline':True]['text':' L10: ','line_number':10014,'multiline':True]['text':'
       When solving the final problem, we no longer need the stored data,
       so we will overwrite the data from this level onto the previously
       used storage space.
','line_number':10021,'multiline':True]['text':'     Sort and Deflate eigenvalues. ','line_number':10033,'multiline':True]['text':'     Solve Secular Equation. ','line_number':10042,'multiline':True]['text':' Computing 2nd power ','line_number':10049,'multiline':True]['text':'     Prepare the INDXQ sorting premutation. ','line_number':10056,'multiline':True]['text':' L20: ','line_number':10066,'multiline':True]['text':'     End of ZLAED7 ','line_number':10072,'multiline':True]['text':' zlaed7_ ','line_number':10074,'multiline':True]['text':' Subroutine ','line_number':10076,'multiline':True]['text':' System generated locals ','line_number':10083,'multiline':True]['text':' Local variables ','line_number':10087,'multiline':True]['text':' Subroutine ','line_number':10094,'multiline':True]['text':' Subroutine ','line_number':10102,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    ZLAED8 merges the two sets of eigenvalues together into a single
    sorted set.  Then it tries to deflate the size of the problem.
    There are two ways in which deflation can occur:  when two or more
    eigenvalues are close together or if there is a tiny element in the
    Z vector.  For each such occurrence the order of the related secular
    equation problem is reduced by one.

    Arguments
    =========

    K      (output) INTEGER
           Contains the number of non-deflated eigenvalues.
           This is the order of the related secular equation.

    N      (input) INTEGER
           The dimension of the symmetric tridiagonal matrix.  N >= 0.

    QSIZ   (input) INTEGER
           The dimension of the unitary matrix used to reduce
           the dense or band matrix to tridiagonal form.
           QSIZ >= N if ICOMPQ = 1.

    Q      (input/output) COMPLEX*16 array, dimension (LDQ,N)
           On entry, Q contains the eigenvectors of the partially solved
           system which has been previously updated in matrix
           multiplies with other partially solved eigensystems.
           On exit, Q contains the trailing (N-K) updated eigenvectors
           (those which were deflated) in its last N-K columns.

    LDQ    (input) INTEGER
           The leading dimension of the array Q.  LDQ >= max( 1, N ).

    D      (input/output) DOUBLE PRECISION array, dimension (N)
           On entry, D contains the eigenvalues of the two submatrices to
           be combined.  On exit, D contains the trailing (N-K) updated
           eigenvalues (those which were deflated) sorted into increasing
           order.

    RHO    (input/output) DOUBLE PRECISION
           Contains the off diagonal element associated with the rank-1
           cut which originally split the two submatrices which are now
           being recombined. RHO is modified during the computation to
           the value required by DLAED3.

    CUTPNT (input) INTEGER
           Contains the location of the last eigenvalue in the leading
           sub-matrix.  MIN(1,N) <= CUTPNT <= N.

    Z      (input) DOUBLE PRECISION array, dimension (N)
           On input this vector contains the updating vector (the last
           row of the first sub-eigenvector matrix and the first row of
           the second sub-eigenvector matrix).  The contents of Z are
           destroyed during the updating process.

    DLAMDA (output) DOUBLE PRECISION array, dimension (N)
           Contains a copy of the first K eigenvalues which will be used
           by DLAED3 to form the secular equation.

    Q2     (output) COMPLEX*16 array, dimension (LDQ2,N)
           If ICOMPQ = 0, Q2 is not referenced.  Otherwise,
           Contains a copy of the first K eigenvectors which will be used
           by DLAED7 in a matrix multiply (DGEMM) to update the new
           eigenvectors.

    LDQ2   (input) INTEGER
           The leading dimension of the array Q2.  LDQ2 >= max( 1, N ).

    W      (output) DOUBLE PRECISION array, dimension (N)
           This will hold the first k values of the final
           deflation-altered z-vector and will be passed to DLAED3.

    INDXP  (workspace) INTEGER array, dimension (N)
           This will contain the permutation used to place deflated
           values of D at the end of the array. On output INDXP(1:K)
           points to the nondeflated D-values and INDXP(K+1:N)
           points to the deflated eigenvalues.

    INDX   (workspace) INTEGER array, dimension (N)
           This will contain the permutation used to sort the contents of
           D into ascending order.

    INDXQ  (input) INTEGER array, dimension (N)
           This contains the permutation which separately sorts the two
           sub-problems in D into ascending order.  Note that elements in
           the second half of this permutation must first have CUTPNT
           added to their values in order to be accurate.

    PERM   (output) INTEGER array, dimension (N)
           Contains the permutations (from deflation and sorting) to be
           applied to each eigenblock.

    GIVPTR (output) INTEGER
           Contains the number of Givens rotations which took place in
           this subproblem.

    GIVCOL (output) INTEGER array, dimension (2, N)
           Each pair of numbers indicates a pair of columns to take place
           in a Givens rotation.

    GIVNUM (output) DOUBLE PRECISION array, dimension (2, N)
           Each number indicates the S value to be used in the
           corresponding Givens rotation.

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    =====================================================================


       Test the input parameters.
','line_number':10107,'multiline':True]['text':' Parameter adjustments ','line_number':10230,'multiline':True]['text':' Function Body ','line_number':10248,'multiline':True]['text':'
       Need to initialize GIVPTR to O here in case of quick exit
       to prevent an unspecified code behavior (usually sigfault)
       when IWORK array on entry to *stedc is not zeroed
       (or at least some IWORK entries which used in *laed7 for GIVPTR).
','line_number':10268,'multiline':True]['text':'     Quick return if possible ','line_number':10277,'multiline':True]['text':'     Normalize z so that norm(z) = 1 ','line_number':10291,'multiline':True]['text':' L10: ','line_number':10297,'multiline':True]['text':'     Sort the eigenvalues into increasing order ','line_number':10302,'multiline':True]['text':' L20: ','line_number':10307,'multiline':True]['text':' L30: ','line_number':10313,'multiline':True]['text':' L40: ','line_number':10322,'multiline':True]['text':'     Calculate the allowable deflation tolerance ','line_number':10325,'multiline':True]['text':'
       If the rank-1 modifier is small enough, no more needs to be done
       -- except to reorganize Q so that its columns correspond with the
       elements in D.
','line_number':10332,'multiline':True]['text':' L50: ','line_number':10345,'multiline':True]['text':'
       If there are multiple eigenvalues then the problem deflates.  Here
       the number of equal eigenvalues are found.  As each equal
       eigenvalue is found, an elementary reflector is computed to rotate
       the corresponding eigensubspace so that the corresponding
       components of Z are zero in this new basis.
','line_number':10351,'multiline':True]['text':'           Deflate due to small z component. ','line_number':10365,'multiline':True]['text':' L60: ','line_number':10376,'multiline':True]['text':'        Deflate due to small z component. ','line_number':10385,'multiline':True]['text':'        Check if eigenvalues are close enough to allow deflation. ','line_number':10391,'multiline':True]['text':'
          Find sqrt(a**2+b**2) without overflow or
          destructive underflow.
','line_number':10396,'multiline':True]['text':'           Deflation is possible. ','line_number':10407,'multiline':True]['text':'           Record the appropriate Givens rotation ','line_number':10412,'multiline':True]['text':'     Record the last eigenvalue. ','line_number':10451,'multiline':True]['text':'
       Sort the eigenvalues and corresponding eigenvectors into DLAMDA
       and Q2 respectively.  The eigenvalues/vectors which were not
       deflated go into the first K slots of DLAMDA and Q2 respectively,
       while those which were deflated go into the last N - K slots.
','line_number':10460,'multiline':True]['text':' L110: ','line_number':10474,'multiline':True]['text':'
       The deflated eigenvalues and their corresponding vectors go back
       into the last N - K slots of D and Q respectively.
','line_number':10477,'multiline':True]['text':'     End of ZLAED8 ','line_number':10492,'multiline':True]['text':' zlaed8_ ','line_number':10494,'multiline':True]['text':' Subroutine ','line_number':10496,'multiline':True]['text':' System generated locals ','line_number':10501,'multiline':True]['text':' Local variables ','line_number':10506,'multiline':True]['text':' Subroutine ','line_number':10527,'multiline':True]['text':' Subroutine ','line_number':10530,'multiline':True]['text':' Subroutine ','line_number':10534,'multiline':True]['text':' Double Complex ','line_number':10536,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       Purpose
       =======

       ZLAHQR is an auxiliary routine called by CHSEQR to update the
       eigenvalues and Schur decomposition already computed by CHSEQR, by
       dealing with the Hessenberg submatrix in rows and columns ILO to
       IHI.

       Arguments
       =========

       WANTT   (input) LOGICAL
            = .TRUE. : the full Schur form T is required;
            = .FALSE.: only eigenvalues are required.

       WANTZ   (input) LOGICAL
            = .TRUE. : the matrix of Schur vectors Z is required;
            = .FALSE.: Schur vectors are not required.

       N       (input) INTEGER
            The order of the matrix H.  N >= 0.

       ILO     (input) INTEGER
       IHI     (input) INTEGER
            It is assumed that H is already upper triangular in rows and
            columns IHI+1:N, and that H(ILO,ILO-1) = 0 (unless ILO = 1).
            ZLAHQR works primarily with the Hessenberg submatrix in rows
            and columns ILO to IHI, but applies transformations to all of
            H if WANTT is .TRUE..
            1 <= ILO <= max(1,IHI); IHI <= N.

       H       (input/output) COMPLEX*16 array, dimension (LDH,N)
            On entry, the upper Hessenberg matrix H.
            On exit, if INFO is zero and if WANTT is .TRUE., then H
            is upper triangular in rows and columns ILO:IHI.  If INFO
            is zero and if WANTT is .FALSE., then the contents of H
            are unspecified on exit.  The output state of H in case
            INF is positive is below under the description of INFO.

       LDH     (input) INTEGER
            The leading dimension of the array H. LDH >= max(1,N).

       W       (output) COMPLEX*16 array, dimension (N)
            The computed eigenvalues ILO to IHI are stored in the
            corresponding elements of W. If WANTT is .TRUE., the
            eigenvalues are stored in the same order as on the diagonal
            of the Schur form returned in H, with W(i) = H(i,i).

       ILOZ    (input) INTEGER
       IHIZ    (input) INTEGER
            Specify the rows of Z to which transformations must be
            applied if WANTZ is .TRUE..
            1 <= ILOZ <= ILO; IHI <= IHIZ <= N.

       Z       (input/output) COMPLEX*16 array, dimension (LDZ,N)
            If WANTZ is .TRUE., on entry Z must contain the current
            matrix Z of transformations accumulated by CHSEQR, and on
            exit Z has been updated; transformations are applied only to
            the submatrix Z(ILOZ:IHIZ,ILO:IHI).
            If WANTZ is .FALSE., Z is not referenced.

       LDZ     (input) INTEGER
            The leading dimension of the array Z. LDZ >= max(1,N).

       INFO    (output) INTEGER
             =   0: successful exit
            .GT. 0: if INFO = i, ZLAHQR failed to compute all the
                    eigenvalues ILO to IHI in a total of 30 iterations
                    per eigenvalue; elements i+1:ihi of W contain
                    those eigenvalues which have been successfully
                    computed.

                    If INFO .GT. 0 and WANTT is .FALSE., then on exit,
                    the remaining unconverged eigenvalues are the
                    eigenvalues of the upper Hessenberg matrix
                    rows and columns ILO thorugh INFO of the final,
                    output value of H.

                    If INFO .GT. 0 and WANTT is .TRUE., then on exit
            (*)       (initial value of H)*U  = U*(final value of H)
                    where U is an orthognal matrix.    The final
                    value of H is upper Hessenberg and triangular in
                    rows and columns INFO+1 through IHI.

                    If INFO .GT. 0 and WANTZ is .TRUE., then on exit
                        (final value of Z)  = (initial value of Z)*U
                    where U is the orthogonal matrix in (*)
                    (regardless of the value of WANTT.)

       Further Details
       ===============

       02-96 Based on modifications by
       David Day, Sandia National Laboratory, USA

       12-04 Further modifications by
       Ralph Byers, University of Kansas, USA
       This is a modified version of ZLAHQR from LAPACK version 3.0.
       It is (1) more robust against overflow and underflow and
       (2) adopts the more conservative Ahues & Tisseur stopping
       criterion (LAWN 122, 1997).

       =========================================================
','line_number':10541,'multiline':True]['text':' Parameter adjustments ','line_number':10653,'multiline':True]['text':' Function Body ','line_number':10662,'multiline':True]['text':'     Quick return if possible ','line_number':10665,'multiline':True]['text':'     ==== clear out the trash ==== ','line_number':10677,'multiline':True]['text':' L10: ','line_number':10684,'multiline':True]['text':'     ==== ensure that subdiagonal entries are real ==== ','line_number':10690,'multiline':True]['text':'
             ==== The following redundant normalization
             .    avoids problems with both gradual and
             .    sudden underflow in ABS(H(I,I-1)) ====
','line_number':10701,'multiline':True]['text':' Computing MIN ','line_number':10721,'multiline':True]['text':' L20: ','line_number':10732,'multiline':True]['text':'     Set machine-dependent constants for the stopping criterion. ','line_number':10738,'multiline':True]['text':'
       I1 and I2 are the indices of the first row and last column of H
       to which transformations must be applied. If eigenvalues only are
       being computed, I1 and I2 are set inside the main loop.
','line_number':10746,'multiline':True]['text':'
       The main loop begins here. I is the loop index and decreases from
       IHI to ILO in steps of 1. Each iteration of the loop works
       with the active submatrix in rows and columns L to I.
       Eigenvalues I+1 to IHI have already converged. Either L = ILO, or
       H(L,L-1) is negligible so that the matrix splits.
','line_number':10757,'multiline':True]['text':'
       Perform QR iterations on rows and columns ILO to I until a
       submatrix of order 1 splits off at the bottom because a
       subdiagonal element has become negligible.
','line_number':10771,'multiline':True]['text':'        Look for a single small subdiagonal element. ','line_number':10780,'multiline':True]['text':'
             ==== The following is a conservative small subdiagonal
             .    deflation criterion due to Ahues & Tisseur (LAWN 122,
             .    1997). It has better mathematical foundation and
             .    improves accuracy in some examples.  ====
','line_number':10805,'multiline':True]['text':' Computing MAX ','line_number':10813,'multiline':True]['text':' Computing MIN ','line_number':10821,'multiline':True]['text':' Computing MAX ','line_number':10834,'multiline':True]['text':' Computing MIN ','line_number':10845,'multiline':True]['text':' Computing MAX ','line_number':10852,'multiline':True]['text':' L40: ','line_number':10858,'multiline':True]['text':'           H(L,L-1) is negligible ','line_number':10864,'multiline':True]['text':'        Exit from loop if a submatrix of order 1 has split off. ','line_number':10870,'multiline':True]['text':'
          Now the active submatrix is in rows and columns L to I. If
          eigenvalues only are being computed, only the active submatrix
          need be transformed.
','line_number':10876,'multiline':True]['text':'           Exceptional shift. ','line_number':10889,'multiline':True]['text':'           Exceptional shift. ','line_number':10898,'multiline':True]['text':'           Wilkinson's shift. ','line_number':10907,'multiline':True]['text':' Computing MAX ','line_number':10923,'multiline':True]['text':'        Look for two consecutive small subdiagonal elements. ','line_number':10952,'multiline':True]['text':'
             Determine the effect of starting the single-shift QR
             iteration at row M, and see if this would make H(M,M-1)
             negligible.
','line_number':10957,'multiline':True]['text':' L60: ','line_number':10986,'multiline':True]['text':'        Single-shift QR step ','line_number':11005,'multiline':True]['text':'
             The first iteration of this loop determines a reflection G
             from the vector V and applies it from left and right to H,
             thus creating a nonzero bulge below the subdiagonal.

             Each subsequent iteration determines a reflection G to
             restore the Hessenberg form in the (K-1)th column, and thus
             chases the bulge one step toward the bottom of the active
             submatrix.

             V(2) is always real before the call to ZLARFG, and hence
             after the call T2 ( = T1*V(2) ) is also real.
','line_number':11010,'multiline':True]['text':'
             Apply G from the left to transform the rows of the matrix
             in columns K to I2.
','line_number':11039,'multiline':True]['text':' L80: ','line_number':11064,'multiline':True]['text':'
             Apply G from the right to transform the columns of the
             matrix in rows I1 to min(K+2,I).

   Computing MIN
','line_number':11067,'multiline':True]['text':' L90: ','line_number':11094,'multiline':True]['text':'              Accumulate transformations in the matrix Z ','line_number':11099,'multiline':True]['text':' L100: ','line_number':11123,'multiline':True]['text':'
                If the QR step was started at row M > L because two
                consecutive small subdiagonals were found, then extra
                scaling must be performed to ensure that H(M,M-1) remains
                real.
','line_number':11129,'multiline':True]['text':' L110: ','line_number':11172,'multiline':True]['text':' L120: ','line_number':11175,'multiline':True]['text':'        Ensure that H(I,I-1) is real. ','line_number':11178,'multiline':True]['text':' L130: ','line_number':11200,'multiline':True]['text':'     Failure to converge in remaining number of iterations ','line_number':11203,'multiline':True]['text':'     H(I,I-1) is negligible: one eigenvalue has converged. ','line_number':11210,'multiline':True]['text':'     return to start of the main loop with new value of I. ','line_number':11216,'multiline':True]['text':'     End of ZLAHQR ','line_number':11224,'multiline':True]['text':' zlahqr_ ','line_number':11226,'multiline':True]['text':' Subroutine ','line_number':11228,'multiline':True]['text':' System generated locals ','line_number':11232,'multiline':True]['text':' Local variables ','line_number':11237,'multiline':True]['text':' Subroutine ','line_number':11240,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2.1)                        --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
    -- April 2009                                                      --


    Purpose
    =======

    ZLAHR2 reduces the first NB columns of A complex general n-BY-(n-k+1)
    matrix A so that elements below the k-th subdiagonal are zero. The
    reduction is performed by an unitary similarity transformation
    Q' * A * Q. The routine returns the matrices V and T which determine
    Q as a block reflector I - V*T*V', and also the matrix Y = A * V * T.

    This is an auxiliary routine called by ZGEHRD.

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix A.

    K       (input) INTEGER
            The offset for the reduction. Elements below the k-th
            subdiagonal in the first NB columns are reduced to zero.
            K < N.

    NB      (input) INTEGER
            The number of columns to be reduced.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N-K+1)
            On entry, the n-by-(n-k+1) general matrix A.
            On exit, the elements on and above the k-th subdiagonal in
            the first NB columns are overwritten with the corresponding
            elements of the reduced matrix; the elements below the k-th
            subdiagonal, with the array TAU, represent the matrix Q as a
            product of elementary reflectors. The other columns of A are
            unchanged. See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    TAU     (output) COMPLEX*16 array, dimension (NB)
            The scalar factors of the elementary reflectors. See Further
            Details.

    T       (output) COMPLEX*16 array, dimension (LDT,NB)
            The upper triangular matrix T.

    LDT     (input) INTEGER
            The leading dimension of the array T.  LDT >= NB.

    Y       (output) COMPLEX*16 array, dimension (LDY,NB)
            The n-by-nb matrix Y.

    LDY     (input) INTEGER
            The leading dimension of the array Y. LDY >= N.

    Further Details
    ===============

    The matrix Q is represented as a product of nb elementary reflectors

       Q = H(1) H(2) . . . H(nb).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i+k-1) = 0, v(i+k) = 1; v(i+k+1:n) is stored on exit in
    A(i+k+1:n,i), and tau in TAU(i).

    The elements of the vectors v together form the (n-k+1)-by-nb matrix
    V which is needed, with T and Y, to apply the transformation to the
    unreduced part of the matrix, using an update of the form:
    A := (I - V*T*V') * (A - Y*V').

    The contents of A on exit are illustrated by the following example
    with n = 7, k = 3 and nb = 2:

       ( a   a   a   a   a )
       ( a   a   a   a   a )
       ( a   a   a   a   a )
       ( h   h   a   a   a )
       ( v1  h   a   a   a )
       ( v1  v2  a   a   a )
       ( v1  v2  a   a   a )

    where a denotes an element of the original matrix A, h denotes a
    modified element of the upper Hessenberg matrix H, and vi denotes an
    element of the vector defining H(i).

    This subroutine is a slight modification of LAPACK-3.0's DLAHRD
    incorporating improvements proposed by Quintana-Orti and Van de
    Gejin. Note that the entries of A(1:K,2:NB) differ from those
    returned by the original LAPACK-3.0's DLAHRD routine. (This
    subroutine is not backward compatible with LAPACK-3.0's DLAHRD.)

    References
    ==========

    Gregorio Quintana-Orti and Robert van de Geijn, "Improving the
    performance of reduction to Hessenberg form," ACM Transactions on
    Mathematical Software, 32(2):180-194, June 2006.

    =====================================================================


       Quick return if possible
','line_number':11259,'multiline':True]['text':' Parameter adjustments ','line_number':11373,'multiline':True]['text':' Function Body ','line_number':11385,'multiline':True]['text':'
             Update A(K+1:N,I)

             Update I-th column of A - Y * V'
','line_number':11394,'multiline':True]['text':'
             Apply I - V * T' * V' to this column (call it b) from the
             left, using the last column of T as workspace

             Let  V = ( V1 )   and   b = ( b1 )   (first I-1 rows)
                      ( V2 )             ( b2 )

             where V1 is unit lower triangular

             w := V1' * b1
','line_number':11411,'multiline':True]['text':'           w := w + V2'*b2 ','line_number':11430,'multiline':True]['text':'           w := T'*w ','line_number':11438,'multiline':True]['text':'           b2 := b2 - V2*w ','line_number':11444,'multiline':True]['text':'           b1 := b1 - V1*w ','line_number':11453,'multiline':True]['text':'
          Generate the elementary reflector H(I) to annihilate
          A(K+I+1:N,I)
','line_number':11467,'multiline':True]['text':' Computing MIN ','line_number':11473,'multiline':True]['text':'        Compute  Y(K+1:N,I) ','line_number':11482,'multiline':True]['text':'        Compute T(1:I,I) ','line_number':11503,'multiline':True]['text':' L10: ','line_number':11517,'multiline':True]['text':'     Compute Y(1:K,1:NB) ','line_number':11522,'multiline':True]['text':'     End of ZLAHR2 ','line_number':11538,'multiline':True]['text':' zlahr2_ ','line_number':11540,'multiline':True]['text':' Subroutine ','line_number':11542,'multiline':True]['text':' System generated locals ','line_number':11550,'multiline':True]['text':' Local variables ','line_number':11557,'multiline':True]['text':' Subroutine ','line_number':11565,'multiline':True]['text':' Subroutine ','line_number':11571,'multiline':True]['text':' Subroutine ','line_number':11574,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLALS0 applies back the multiplying factors of either the left or the
    right singular vector matrix of a diagonal matrix appended by a row
    to the right hand side matrix B in solving the least squares problem
    using the divide-and-conquer SVD approach.

    For the left singular vector matrix, three types of orthogonal
    matrices are involved:

    (1L) Givens rotations: the number of such rotations is GIVPTR; the
         pairs of columns/rows they were applied to are stored in GIVCOL;
         and the C- and S-values of these rotations are stored in GIVNUM.

    (2L) Permutation. The (NL+1)-st row of B is to be moved to the first
         row, and for J=2:N, PERM(J)-th row of B is to be moved to the
         J-th row.

    (3L) The left singular vector matrix of the remaining matrix.

    For the right singular vector matrix, four types of orthogonal
    matrices are involved:

    (1R) The right singular vector matrix of the remaining matrix.

    (2R) If SQRE = 1, one extra Givens rotation to generate the right
         null space.

    (3R) The inverse transformation of (2L).

    (4R) The inverse transformation of (1L).

    Arguments
    =========

    ICOMPQ (input) INTEGER
           Specifies whether singular vectors are to be computed in
           factored form:
           = 0: Left singular vector matrix.
           = 1: Right singular vector matrix.

    NL     (input) INTEGER
           The row dimension of the upper block. NL >= 1.

    NR     (input) INTEGER
           The row dimension of the lower block. NR >= 1.

    SQRE   (input) INTEGER
           = 0: the lower block is an NR-by-NR square matrix.
           = 1: the lower block is an NR-by-(NR+1) rectangular matrix.

           The bidiagonal matrix has row dimension N = NL + NR + 1,
           and column dimension M = N + SQRE.

    NRHS   (input) INTEGER
           The number of columns of B and BX. NRHS must be at least 1.

    B      (input/output) COMPLEX*16 array, dimension ( LDB, NRHS )
           On input, B contains the right hand sides of the least
           squares problem in rows 1 through M. On output, B contains
           the solution X in rows 1 through N.

    LDB    (input) INTEGER
           The leading dimension of B. LDB must be at least
           max(1,MAX( M, N ) ).

    BX     (workspace) COMPLEX*16 array, dimension ( LDBX, NRHS )

    LDBX   (input) INTEGER
           The leading dimension of BX.

    PERM   (input) INTEGER array, dimension ( N )
           The permutations (from deflation and sorting) applied
           to the two blocks.

    GIVPTR (input) INTEGER
           The number of Givens rotations which took place in this
           subproblem.

    GIVCOL (input) INTEGER array, dimension ( LDGCOL, 2 )
           Each pair of numbers indicates a pair of rows/columns
           involved in a Givens rotation.

    LDGCOL (input) INTEGER
           The leading dimension of GIVCOL, must be at least N.

    GIVNUM (input) DOUBLE PRECISION array, dimension ( LDGNUM, 2 )
           Each number indicates the C or S value used in the
           corresponding Givens rotation.

    LDGNUM (input) INTEGER
           The leading dimension of arrays DIFR, POLES and
           GIVNUM, must be at least K.

    POLES  (input) DOUBLE PRECISION array, dimension ( LDGNUM, 2 )
           On entry, POLES(1:K, 1) contains the new singular
           values obtained from solving the secular equation, and
           POLES(1:K, 2) is an array containing the poles in the secular
           equation.

    DIFL   (input) DOUBLE PRECISION array, dimension ( K ).
           On entry, DIFL(I) is the distance between I-th updated
           (undeflated) singular value and the I-th (undeflated) old
           singular value.

    DIFR   (input) DOUBLE PRECISION array, dimension ( LDGNUM, 2 ).
           On entry, DIFR(I, 1) contains the distances between I-th
           updated (undeflated) singular value and the I+1-th
           (undeflated) old singular value. And DIFR(I, 2) is the
           normalizing factor for the I-th right singular vector.

    Z      (input) DOUBLE PRECISION array, dimension ( K )
           Contain the components of the deflation-adjusted updating row
           vector.

    K      (input) INTEGER
           Contains the dimension of the non-deflated matrix,
           This is the order of the related secular equation. 1 <= K <=N.

    C      (input) DOUBLE PRECISION
           C contains garbage if SQRE =0 and the C-value of a Givens
           rotation related to the right null space if SQRE = 1.

    S      (input) DOUBLE PRECISION
           S contains garbage if SQRE =0 and the S-value of a Givens
           rotation related to the right null space if SQRE = 1.

    RWORK  (workspace) DOUBLE PRECISION array, dimension
           ( K*(1+NRHS) + 2*NRHS )

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input parameters.
','line_number':11581,'multiline':True]['text':' Parameter adjustments ','line_number':11737,'multiline':True]['text':' Function Body ','line_number':11761,'multiline':True]['text':'
          Apply back orthogonal transformations from the left.

          Step (1L): apply back the Givens rotations performed.
','line_number':11802,'multiline':True]['text':' L10: ','line_number':11813,'multiline':True]['text':'        Step (2L): permute rows of B. ','line_number':11816,'multiline':True]['text':' L20: ','line_number':11823,'multiline':True]['text':'
          Step (3L): apply the inverse of the left singular vector
          matrix to BX.
','line_number':11826,'multiline':True]['text':' L30: ','line_number':11863,'multiline':True]['text':' L40: ','line_number':11876,'multiline':True]['text':'
                Since B and BX are complex, the following call to DGEMV
                is performed in two steps (real and imaginary parts).

                CALL DGEMV( 'T', K, NRHS, ONE, BX, LDBX, WORK, 1, ZERO,
      $                     B( J, 1 ), LDB )
','line_number':11881,'multiline':True]['text':' L50: ','line_number':11897,'multiline':True]['text':' L60: ','line_number':11899,'multiline':True]['text':' L70: ','line_number':11910,'multiline':True]['text':' L80: ','line_number':11912,'multiline':True]['text':' L90: ','line_number':11924,'multiline':True]['text':' L100: ','line_number':11928,'multiline':True]['text':'        Move the deflated rows of BX to B also. ','line_number':11932,'multiline':True]['text':'
          Apply back the right orthogonal transformations.

          Step (1R): apply back the new right singular vector matrix
          to B.
','line_number':11941,'multiline':True]['text':' L110: ','line_number':11970,'multiline':True]['text':' L120: ','line_number':11982,'multiline':True]['text':'
                Since B and BX are complex, the following call to DGEMV
                is performed in two steps (real and imaginary parts).

                CALL DGEMV( 'T', K, NRHS, ONE, B, LDB, WORK, 1, ZERO,
      $                     BX( J, 1 ), LDBX )
','line_number':11985,'multiline':True]['text':' L130: ','line_number':12001,'multiline':True]['text':' L140: ','line_number':12003,'multiline':True]['text':' L150: ','line_number':12014,'multiline':True]['text':' L160: ','line_number':12016,'multiline':True]['text':' L170: ','line_number':12028,'multiline':True]['text':' L180: ','line_number':12030,'multiline':True]['text':'
          Step (2R): if SQRE = 1, apply back the rotation that is
          related to the right null space of the subproblem.
','line_number':12034,'multiline':True]['text':'        Step (3R): permute rows of B. ','line_number':12050,'multiline':True]['text':' L190: ','line_number':12060,'multiline':True]['text':'        Step (4R): apply back the Givens rotations performed. ','line_number':12063,'multiline':True]['text':' L200: ','line_number':12070,'multiline':True]['text':'     End of ZLALS0 ','line_number':12076,'multiline':True]['text':' zlals0_ ','line_number':12078,'multiline':True]['text':' Subroutine ','line_number':12080,'multiline':True]['text':' System generated locals ','line_number':12088,'multiline':True]['text':' Local variables ','line_number':12096,'multiline':True]['text':' Subroutine ','line_number':12099,'multiline':True]['text':' Subroutine ','line_number':12103,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLALSA is an itermediate step in solving the least squares problem
    by computing the SVD of the coefficient matrix in compact form (The
    singular vectors are computed as products of simple orthorgonal
    matrices.).

    If ICOMPQ = 0, ZLALSA applies the inverse of the left singular vector
    matrix of an upper bidiagonal matrix to the right hand side; and if
    ICOMPQ = 1, ZLALSA applies the right singular vector matrix to the
    right hand side. The singular vector matrices were generated in
    compact form by ZLALSA.

    Arguments
    =========

    ICOMPQ (input) INTEGER
           Specifies whether the left or the right singular vector
           matrix is involved.
           = 0: Left singular vector matrix
           = 1: Right singular vector matrix

    SMLSIZ (input) INTEGER
           The maximum size of the subproblems at the bottom of the
           computation tree.

    N      (input) INTEGER
           The row and column dimensions of the upper bidiagonal matrix.

    NRHS   (input) INTEGER
           The number of columns of B and BX. NRHS must be at least 1.

    B      (input/output) COMPLEX*16 array, dimension ( LDB, NRHS )
           On input, B contains the right hand sides of the least
           squares problem in rows 1 through M.
           On output, B contains the solution X in rows 1 through N.

    LDB    (input) INTEGER
           The leading dimension of B in the calling subprogram.
           LDB must be at least max(1,MAX( M, N ) ).

    BX     (output) COMPLEX*16 array, dimension ( LDBX, NRHS )
           On exit, the result of applying the left or right singular
           vector matrix to B.

    LDBX   (input) INTEGER
           The leading dimension of BX.

    U      (input) DOUBLE PRECISION array, dimension ( LDU, SMLSIZ ).
           On entry, U contains the left singular vector matrices of all
           subproblems at the bottom level.

    LDU    (input) INTEGER, LDU = > N.
           The leading dimension of arrays U, VT, DIFL, DIFR,
           POLES, GIVNUM, and Z.

    VT     (input) DOUBLE PRECISION array, dimension ( LDU, SMLSIZ+1 ).
           On entry, VT' contains the right singular vector matrices of
           all subproblems at the bottom level.

    K      (input) INTEGER array, dimension ( N ).

    DIFL   (input) DOUBLE PRECISION array, dimension ( LDU, NLVL ).
           where NLVL = INT(log_2 (N/(SMLSIZ+1))) + 1.

    DIFR   (input) DOUBLE PRECISION array, dimension ( LDU, 2 * NLVL ).
           On entry, DIFL(*, I) and DIFR(*, 2 * I -1) record
           distances between singular values on the I-th level and
           singular values on the (I -1)-th level, and DIFR(*, 2 * I)
           record the normalizing factors of the right singular vectors
           matrices of subproblems on I-th level.

    Z      (input) DOUBLE PRECISION array, dimension ( LDU, NLVL ).
           On entry, Z(1, I) contains the components of the deflation-
           adjusted updating row vector for subproblems on the I-th
           level.

    POLES  (input) DOUBLE PRECISION array, dimension ( LDU, 2 * NLVL ).
           On entry, POLES(*, 2 * I -1: 2 * I) contains the new and old
           singular values involved in the secular equations on the I-th
           level.

    GIVPTR (input) INTEGER array, dimension ( N ).
           On entry, GIVPTR( I ) records the number of Givens
           rotations performed on the I-th problem on the computation
           tree.

    GIVCOL (input) INTEGER array, dimension ( LDGCOL, 2 * NLVL ).
           On entry, for each I, GIVCOL(*, 2 * I - 1: 2 * I) records the
           locations of Givens rotations performed on the I-th level on
           the computation tree.

    LDGCOL (input) INTEGER, LDGCOL = > N.
           The leading dimension of arrays GIVCOL and PERM.

    PERM   (input) INTEGER array, dimension ( LDGCOL, NLVL ).
           On entry, PERM(*, I) records permutations done on the I-th
           level of the computation tree.

    GIVNUM (input) DOUBLE PRECISION array, dimension ( LDU, 2 * NLVL ).
           On entry, GIVNUM(*, 2 *I -1 : 2 * I) records the C- and S-
           values of Givens rotations performed on the I-th level on the
           computation tree.

    C      (input) DOUBLE PRECISION array, dimension ( N ).
           On entry, if the I-th subproblem is not square,
           C( I ) contains the C-value of a Givens rotation related to
           the right null space of the I-th subproblem.

    S      (input) DOUBLE PRECISION array, dimension ( N ).
           On entry, if the I-th subproblem is not square,
           S( I ) contains the S-value of a Givens rotation related to
           the right null space of the I-th subproblem.

    RWORK  (workspace) DOUBLE PRECISION array, dimension at least
           MAX( (SMLSZ+1)*NRHS*3, N*(1+NRHS) + 2*NRHS ).

    IWORK  (workspace) INTEGER array.
           The dimension must be at least 3 * N

    INFO   (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input parameters.
','line_number':12114,'multiline':True]['text':' Parameter adjustments ','line_number':12260,'multiline':True]['text':' Function Body ','line_number':12301,'multiline':True]['text':'     Book-keeping and  setting up the computation tree. ','line_number':12327,'multiline':True]['text':'
       The following code applies back the left singular vector factors.
       For applying back the right singular vector factors, go to 170.
','line_number':12336,'multiline':True]['text':'
       The nodes on the bottom level of the tree were solved
       by DLASDQ. The corresponding left and right singular vector
       matrices are in explicit form. First apply back the left
       singular vector matrices.
','line_number':12345,'multiline':True]['text':'
          IC : center row of each node
          NL : number of rows of left  subproblem
          NR : number of rows of right subproblem
          NLF: starting row of the left   subproblem
          NRF: starting row of the right  subproblem
','line_number':12356,'multiline':True]['text':'
          Since B and BX are complex, the following call to DGEMM
          is performed in two steps (real and imaginary parts).

          CALL DGEMM( 'T', 'N', NL, NRHS, NL, ONE, U( NLF, 1 ), LDU,
       $               B( NLF, 1 ), LDB, ZERO, BX( NLF, 1 ), LDBX )
','line_number':12371,'multiline':True]['text':' L10: ','line_number':12387,'multiline':True]['text':' L20: ','line_number':12389,'multiline':True]['text':' L30: ','line_number':12400,'multiline':True]['text':' L40: ','line_number':12402,'multiline':True]['text':' L50: ','line_number':12420,'multiline':True]['text':' L60: ','line_number':12422,'multiline':True]['text':'
          Since B and BX are complex, the following call to DGEMM
          is performed in two steps (real and imaginary parts).

          CALL DGEMM( 'T', 'N', NR, NRHS, NR, ONE, U( NRF, 1 ), LDU,
      $               B( NRF, 1 ), LDB, ZERO, BX( NRF, 1 ), LDBX )
','line_number':12425,'multiline':True]['text':' L70: ','line_number':12441,'multiline':True]['text':' L80: ','line_number':12443,'multiline':True]['text':' L90: ','line_number':12454,'multiline':True]['text':' L100: ','line_number':12456,'multiline':True]['text':' L110: ','line_number':12474,'multiline':True]['text':' L120: ','line_number':12476,'multiline':True]['text':' L130: ','line_number':12479,'multiline':True]['text':'
       Next copy the rows of B that correspond to unchanged rows
       in the bidiagonal matrix to BX.
','line_number':12482,'multiline':True]['text':' L140: ','line_number':12491,'multiline':True]['text':'
       Finally go through the left singular vector matrices of all
       the other subproblems bottom-up on the tree.
','line_number':12494,'multiline':True]['text':'
          find the first node LF and last node LL on
          the current level LVL
','line_number':12505,'multiline':True]['text':' L150: ','line_number':12534,'multiline':True]['text':' L160: ','line_number':12536,'multiline':True]['text':'     ICOMPQ = 1: applying back the right singular vector factors. ','line_number':12540,'multiline':True]['text':'
       First now go through the right singular vector matrices of all
       the tree nodes top-down.
','line_number':12544,'multiline':True]['text':'
          Find the first node LF and last node LL on
          the current level LVL.
','line_number':12554,'multiline':True]['text':' L180: ','line_number':12588,'multiline':True]['text':' L190: ','line_number':12590,'multiline':True]['text':'
       The nodes on the bottom level of the tree were solved
       by DLASDQ. The corresponding right singular vector
       matrices are in explicit form. Apply them back.
','line_number':12593,'multiline':True]['text':'
          Since B and BX are complex, the following call to DGEMM is
          performed in two steps (real and imaginary parts).

          CALL DGEMM( 'T', 'N', NLP1, NRHS, NLP1, ONE, VT( NLF, 1 ), LDU,
      $               B( NLF, 1 ), LDB, ZERO, BX( NLF, 1 ), LDBX )
','line_number':12615,'multiline':True]['text':' L200: ','line_number':12631,'multiline':True]['text':' L210: ','line_number':12633,'multiline':True]['text':' L220: ','line_number':12645,'multiline':True]['text':' L230: ','line_number':12647,'multiline':True]['text':' L240: ','line_number':12665,'multiline':True]['text':' L250: ','line_number':12667,'multiline':True]['text':'
          Since B and BX are complex, the following call to DGEMM is
          performed in two steps (real and imaginary parts).

          CALL DGEMM( 'T', 'N', NRP1, NRHS, NRP1, ONE, VT( NRF, 1 ), LDU,
      $               B( NRF, 1 ), LDB, ZERO, BX( NRF, 1 ), LDBX )
','line_number':12670,'multiline':True]['text':' L260: ','line_number':12686,'multiline':True]['text':' L270: ','line_number':12688,'multiline':True]['text':' L280: ','line_number':12700,'multiline':True]['text':' L290: ','line_number':12702,'multiline':True]['text':' L300: ','line_number':12720,'multiline':True]['text':' L310: ','line_number':12722,'multiline':True]['text':' L320: ','line_number':12725,'multiline':True]['text':'     End of ZLALSA ','line_number':12732,'multiline':True]['text':' zlalsa_ ','line_number':12734,'multiline':True]['text':' Subroutine ','line_number':12736,'multiline':True]['text':' System generated locals ','line_number':12741,'multiline':True]['text':' Local variables ','line_number':12746,'multiline':True]['text':' Subroutine ','line_number':12761,'multiline':True]['text':' Subroutine ','line_number':12765,'multiline':True]['text':' Subroutine ','line_number':12771,'multiline':True]['text':' Subroutine ','line_number':12780,'multiline':True]['text':' Subroutine ','line_number':12788,'multiline':True]['text':'
    -- LAPACK routine (version 3.2.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       June 2010


    Purpose
    =======

    ZLALSD uses the singular value decomposition of A to solve the least
    squares problem of finding X to minimize the Euclidean norm of each
    column of A*X-B, where A is N-by-N upper bidiagonal, and X and B
    are N-by-NRHS. The solution X overwrites B.

    The singular values of A smaller than RCOND times the largest
    singular value are treated as zero in solving the least squares
    problem; in this case a minimum norm solution is returned.
    The actual singular values are returned in D in ascending order.

    This code makes very mild assumptions about floating point
    arithmetic. It will work on machines with a guard digit in
    add/subtract, or on those binary machines without guard digits
    which subtract like the Cray XMP, Cray YMP, Cray C 90, or Cray 2.
    It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.

    Arguments
    =========

    UPLO   (input) CHARACTER*1
           = 'U': D and E define an upper bidiagonal matrix.
           = 'L': D and E define a  lower bidiagonal matrix.

    SMLSIZ (input) INTEGER
           The maximum size of the subproblems at the bottom of the
           computation tree.

    N      (input) INTEGER
           The dimension of the  bidiagonal matrix.  N >= 0.

    NRHS   (input) INTEGER
           The number of columns of B. NRHS must be at least 1.

    D      (input/output) DOUBLE PRECISION array, dimension (N)
           On entry D contains the main diagonal of the bidiagonal
           matrix. On exit, if INFO = 0, D contains its singular values.

    E      (input/output) DOUBLE PRECISION array, dimension (N-1)
           Contains the super-diagonal entries of the bidiagonal matrix.
           On exit, E has been destroyed.

    B      (input/output) COMPLEX*16 array, dimension (LDB,NRHS)
           On input, B contains the right hand sides of the least
           squares problem. On output, B contains the solution X.

    LDB    (input) INTEGER
           The leading dimension of B in the calling subprogram.
           LDB must be at least max(1,N).

    RCOND  (input) DOUBLE PRECISION
           The singular values of A less than or equal to RCOND times
           the largest singular value are treated as zero in solving
           the least squares problem. If RCOND is negative,
           machine precision is used instead.
           For example, if diag(S)*X=B were the least squares problem,
           where diag(S) is a diagonal matrix of singular values, the
           solution would be X(i) = B(i) / S(i) if S(i) is greater than
           RCOND*max(S), and X(i) = 0 if S(i) is less than or equal to
           RCOND*max(S).

    RANK   (output) INTEGER
           The number of singular values of A greater than RCOND times
           the largest singular value.

    WORK   (workspace) COMPLEX*16 array, dimension at least
           (N * NRHS).

    RWORK  (workspace) DOUBLE PRECISION array, dimension at least
           (9*N + 2*N*SMLSIZ + 8*N*NLVL + 3*SMLSIZ*NRHS +
           MAX( (SMLSIZ+1)**2, N*(1+NRHS) + 2*NRHS ),
           where
           NLVL = MAX( 0, INT( LOG_2( MIN( M,N )/(SMLSIZ+1) ) ) + 1 )

    IWORK  (workspace) INTEGER array, dimension at least
           (3*N*NLVL + 11*N).

    INFO   (output) INTEGER
           = 0:  successful exit.
           < 0:  if INFO = -i, the i-th argument had an illegal value.
           > 0:  The algorithm failed to compute a singular value while
                 working on the submatrix lying in rows and columns
                 INFO/(N+1) through MOD(INFO,N+1).

    Further Details
    ===============

    Based on contributions by
       Ming Gu and Ren-Cang Li, Computer Science Division, University of
         California at Berkeley, USA
       Osni Marques, LBNL/NERSC, USA

    =====================================================================


       Test the input parameters.
','line_number':12804,'multiline':True]['text':' Parameter adjustments ','line_number':12912,'multiline':True]['text':' Function Body ','line_number':12922,'multiline':True]['text':'     Set up the tolerance. ','line_number':12940,'multiline':True]['text':'     Quick return if possible. ','line_number':12950,'multiline':True]['text':'     Rotate the matrix if it is lower bidiagonal. ','line_number':12966,'multiline':True]['text':' L10: ','line_number':12982,'multiline':True]['text':' L20: ','line_number':12993,'multiline':True]['text':' L30: ','line_number':12995,'multiline':True]['text':'     Scale. ','line_number':13000,'multiline':True]['text':'
       If N is smaller than the minimum divide size SMLSIZ, then solve
       the problem with another solver.
','line_number':13013,'multiline':True]['text':'
          In the real version, B is passed to DLASDQ and multiplied
          internally by Q'. Here B is complex and that product is
          computed below in two steps (real and imaginary parts).
','line_number':13033,'multiline':True]['text':' L40: ','line_number':13047,'multiline':True]['text':' L50: ','line_number':13049,'multiline':True]['text':' L60: ','line_number':13060,'multiline':True]['text':' L70: ','line_number':13062,'multiline':True]['text':' L80: ','line_number':13079,'multiline':True]['text':' L90: ','line_number':13081,'multiline':True]['text':' L100: ','line_number':13095,'multiline':True]['text':'
          Since B is complex, the following call to DGEMM is performed
          in two steps (real and imaginary parts). That is for V * B
          (in the real version of the code V' is stored in WORK).

          CALL DGEMM( 'T', 'N', N, NRHS, N, ONE, WORK, N, B, LDB, ZERO,
      $               WORK( NWORK ), N )
','line_number':13098,'multiline':True]['text':' L110: ','line_number':13115,'multiline':True]['text':' L120: ','line_number':13117,'multiline':True]['text':' L130: ','line_number':13128,'multiline':True]['text':' L140: ','line_number':13130,'multiline':True]['text':' L150: ','line_number':13147,'multiline':True]['text':' L160: ','line_number':13149,'multiline':True]['text':'        Unscale. ','line_number':13152,'multiline':True]['text':'     Book-keeping and setting up some constants. ','line_number':13163,'multiline':True]['text':' L170: ','line_number':13204,'multiline':True]['text':'
             Subproblem found. First determine its size and then
             apply divide and conquer on it.
','line_number':13213,'multiline':True]['text':'              A subproblem with E(I) small for I < NM1. ','line_number':13220,'multiline':True]['text':'              A subproblem with E(NM1) not too small but I = NM1. ','line_number':13226,'multiline':True]['text':'
                A subproblem with E(NM1) small. This implies an
                1-by-1 subproblem at D(N), which is not solved
                explicitly.
','line_number':13232,'multiline':True]['text':'
                This is a 1-by-1 subproblem and is not solved
                explicitly.
','line_number':13248,'multiline':True]['text':'              This is a small subproblem and is solved by DLASDQ. ','line_number':13256,'multiline':True]['text':'
                In the real version, B is passed to DLASDQ and multiplied
                internally by Q'. Here B is complex and that product is
                computed below in two steps (real and imaginary parts).
','line_number':13270,'multiline':True]['text':' L180: ','line_number':13284,'multiline':True]['text':' L190: ','line_number':13286,'multiline':True]['text':' L200: ','line_number':13298,'multiline':True]['text':' L210: ','line_number':13300,'multiline':True]['text':' L220: ','line_number':13318,'multiline':True]['text':' L230: ','line_number':13320,'multiline':True]['text':'              A large problem. Solve it using divide and conquer. ','line_number':13327,'multiline':True]['text':' L240: ','line_number':13353,'multiline':True]['text':'     Apply the singular values and treat the tiny ones as zero. ','line_number':13356,'multiline':True]['text':'
          Some of the elements in D can be negative because 1-by-1
          subproblems were not solved explicitly.
','line_number':13363,'multiline':True]['text':' L250: ','line_number':13376,'multiline':True]['text':'     Now apply back the right singular vectors. ','line_number':13379,'multiline':True]['text':'
             Since B and BX are complex, the following call to DGEMM
             is performed in two steps (real and imaginary parts).

             CALL DGEMM( 'T', 'N', NSIZE, NRHS, NSIZE, ONE,
      $                  RWORK( VT+ST1 ), N, RWORK( BXST ), N, ZERO,
      $                  B( ST, 1 ), LDB )
','line_number':13392,'multiline':True]['text':' L260: ','line_number':13411,'multiline':True]['text':' L270: ','line_number':13413,'multiline':True]['text':' L280: ','line_number':13426,'multiline':True]['text':' L290: ','line_number':13428,'multiline':True]['text':' L300: ','line_number':13445,'multiline':True]['text':' L310: ','line_number':13447,'multiline':True]['text':' L320: ','line_number':13461,'multiline':True]['text':'     Unscale and sort the singular values. ','line_number':13464,'multiline':True]['text':'     End of ZLALSD ','line_number':13473,'multiline':True]['text':' zlalsd_ ','line_number':13475,'multiline':True]['text':' System generated locals ','line_number':13480,'multiline':True]['text':' Local variables ','line_number':13484,'multiline':True]['text':' Subroutine ','line_number':13489,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLANGE  returns the value of the one norm,  or the Frobenius norm, or
    the  infinity norm,  or the  element of  largest absolute value  of a
    complex matrix A.

    Description
    ===========

    ZLANGE returns the value

       ZLANGE = ( max(abs(A(i,j))), NORM = 'M' or 'm'
                (
                ( norm1(A),         NORM = '1', 'O' or 'o'
                (
                ( normI(A),         NORM = 'I' or 'i'
                (
                ( normF(A),         NORM = 'F', 'f', 'E' or 'e'

    where  norm1  denotes the  one norm of a matrix (maximum column sum),
    normI  denotes the  infinity norm  of a matrix  (maximum row sum) and
    normF  denotes the  Frobenius norm of a matrix (square root of sum of
    squares).  Note that  max(abs(A(i,j)))  is not a consistent matrix norm.

    Arguments
    =========

    NORM    (input) CHARACTER*1
            Specifies the value to be returned in ZLANGE as described
            above.

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.  When M = 0,
            ZLANGE is set to zero.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.  When N = 0,
            ZLANGE is set to zero.

    A       (input) COMPLEX*16 array, dimension (LDA,N)
            The m by n matrix A.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(M,1).

    WORK    (workspace) DOUBLE PRECISION array, dimension (MAX(1,LWORK)),
            where LWORK >= M when NORM = 'I'; otherwise, WORK is not
            referenced.

   =====================================================================
','line_number':13493,'multiline':True]['text':' Parameter adjustments ','line_number':13554,'multiline':True]['text':' Function Body ','line_number':13560,'multiline':True]['text':'        Find max(abs(A(i,j))). ','line_number':13565,'multiline':True]['text':' Computing MAX ','line_number':13572,'multiline':True]['text':' L10: ','line_number':13575,'multiline':True]['text':' L20: ','line_number':13577,'multiline':True]['text':'        Find norm1(A). ','line_number':13582,'multiline':True]['text':' L30: ','line_number':13591,'multiline':True]['text':' L40: ','line_number':13594,'multiline':True]['text':'        Find normI(A). ','line_number':13598,'multiline':True]['text':' L50: ','line_number':13603,'multiline':True]['text':' L60: ','line_number':13610,'multiline':True]['text':' L70: ','line_number':13612,'multiline':True]['text':' Computing MAX ','line_number':13617,'multiline':True]['text':' L80: ','line_number':13620,'multiline':True]['text':'        Find normF(A). ','line_number':13624,'multiline':True]['text':' L90: ','line_number':13631,'multiline':True]['text':'     End of ZLANGE ','line_number':13639,'multiline':True]['text':' zlange_ ','line_number':13641,'multiline':True]['text':' System generated locals ','line_number':13646,'multiline':True]['text':' Local variables ','line_number':13650,'multiline':True]['text':' Subroutine ','line_number':13655,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLANHE  returns the value of the one norm,  or the Frobenius norm, or
    the  infinity norm,  or the  element of  largest absolute value  of a
    complex hermitian matrix A.

    Description
    ===========

    ZLANHE returns the value

       ZLANHE = ( max(abs(A(i,j))), NORM = 'M' or 'm'
                (
                ( norm1(A),         NORM = '1', 'O' or 'o'
                (
                ( normI(A),         NORM = 'I' or 'i'
                (
                ( normF(A),         NORM = 'F', 'f', 'E' or 'e'

    where  norm1  denotes the  one norm of a matrix (maximum column sum),
    normI  denotes the  infinity norm  of a matrix  (maximum row sum) and
    normF  denotes the  Frobenius norm of a matrix (square root of sum of
    squares).  Note that  max(abs(A(i,j)))  is not a consistent matrix norm.

    Arguments
    =========

    NORM    (input) CHARACTER*1
            Specifies the value to be returned in ZLANHE as described
            above.

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            hermitian matrix A is to be referenced.
            = 'U':  Upper triangular part of A is referenced
            = 'L':  Lower triangular part of A is referenced

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.  When N = 0, ZLANHE is
            set to zero.

    A       (input) COMPLEX*16 array, dimension (LDA,N)
            The hermitian matrix A.  If UPLO = 'U', the leading n by n
            upper triangular part of A contains the upper triangular part
            of the matrix A, and the strictly lower triangular part of A
            is not referenced.  If UPLO = 'L', the leading n by n lower
            triangular part of A contains the lower triangular part of
            the matrix A, and the strictly upper triangular part of A is
            not referenced. Note that the imaginary parts of the diagonal
            elements need not be set and are assumed to be zero.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(N,1).

    WORK    (workspace) DOUBLE PRECISION array, dimension (MAX(1,LWORK)),
            where LWORK >= N when NORM = 'I' or '1' or 'O'; otherwise,
            WORK is not referenced.

   =====================================================================
','line_number':13659,'multiline':True]['text':' Parameter adjustments ','line_number':13729,'multiline':True]['text':' Function Body ','line_number':13735,'multiline':True]['text':'        Find max(abs(A(i,j))). ','line_number':13740,'multiline':True]['text':' Computing MAX ','line_number':13748,'multiline':True]['text':' L10: ','line_number':13751,'multiline':True]['text':' Computing MAX ','line_number':13753,'multiline':True]['text':' L20: ','line_number':13757,'multiline':True]['text':' Computing MAX ','line_number':13762,'multiline':True]['text':' Computing MAX ','line_number':13768,'multiline':True]['text':' L30: ','line_number':13771,'multiline':True]['text':' L40: ','line_number':13773,'multiline':True]['text':'        Find normI(A) ( = norm1(A), since A is hermitian). ','line_number':13778,'multiline':True]['text':' L50: ','line_number':13790,'multiline':True]['text':' L60: ','line_number':13794,'multiline':True]['text':' Computing MAX ','line_number':13798,'multiline':True]['text':' L70: ','line_number':13801,'multiline':True]['text':' L80: ','line_number':13807,'multiline':True]['text':' L90: ','line_number':13818,'multiline':True]['text':' L100: ','line_number':13821,'multiline':True]['text':'        Find normF(A). ','line_number':13826,'multiline':True]['text':' L110: ','line_number':13835,'multiline':True]['text':' L120: ','line_number':13842,'multiline':True]['text':' Computing 2nd power ','line_number':13853,'multiline':True]['text':' Computing 2nd power ','line_number':13858,'multiline':True]['text':' L130: ','line_number':13863,'multiline':True]['text':'     End of ZLANHE ','line_number':13871,'multiline':True]['text':' zlanhe_ ','line_number':13873,'multiline':True]['text':' Subroutine ','line_number':13875,'multiline':True]['text':' System generated locals ','line_number':13880,'multiline':True]['text':' Local variables ','line_number':13885,'multiline':True]['text':' was [1][1] ','line_number':13895,'multiline':True]['text':' Subroutine ','line_number':13897,'multiline':True]['text':' Subroutine ','line_number':13918,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       Purpose
       =======

       ZLAQR0 computes the eigenvalues of a Hessenberg matrix H
       and, optionally, the matrices T and Z from the Schur decomposition
       H = Z T Z**H, where T is an upper triangular matrix (the
       Schur form), and Z is the unitary matrix of Schur vectors.

       Optionally Z may be postmultiplied into an input unitary
       matrix Q so that this routine can give the Schur factorization
       of a matrix A which has been reduced to the Hessenberg form H
       by the unitary matrix Q:  A = Q*H*Q**H = (QZ)*H*(QZ)**H.

       Arguments
       =========

       WANTT   (input) LOGICAL
            = .TRUE. : the full Schur form T is required;
            = .FALSE.: only eigenvalues are required.

       WANTZ   (input) LOGICAL
            = .TRUE. : the matrix of Schur vectors Z is required;
            = .FALSE.: Schur vectors are not required.

       N     (input) INTEGER
             The order of the matrix H.  N .GE. 0.

       ILO   (input) INTEGER
       IHI   (input) INTEGER
             It is assumed that H is already upper triangular in rows
             and columns 1:ILO-1 and IHI+1:N and, if ILO.GT.1,
             H(ILO,ILO-1) is zero. ILO and IHI are normally set by a
             previous call to ZGEBAL, and then passed to ZGEHRD when the
             matrix output by ZGEBAL is reduced to Hessenberg form.
             Otherwise, ILO and IHI should be set to 1 and N,
             respectively.  If N.GT.0, then 1.LE.ILO.LE.IHI.LE.N.
             If N = 0, then ILO = 1 and IHI = 0.

       H     (input/output) COMPLEX*16 array, dimension (LDH,N)
             On entry, the upper Hessenberg matrix H.
             On exit, if INFO = 0 and WANTT is .TRUE., then H
             contains the upper triangular matrix T from the Schur
             decomposition (the Schur form). If INFO = 0 and WANT is
             .FALSE., then the contents of H are unspecified on exit.
             (The output value of H when INFO.GT.0 is given under the
             description of INFO below.)

             This subroutine may explicitly set H(i,j) = 0 for i.GT.j and
             j = 1, 2, ... ILO-1 or j = IHI+1, IHI+2, ... N.

       LDH   (input) INTEGER
             The leading dimension of the array H. LDH .GE. max(1,N).

       W        (output) COMPLEX*16 array, dimension (N)
             The computed eigenvalues of H(ILO:IHI,ILO:IHI) are stored
             in W(ILO:IHI). If WANTT is .TRUE., then the eigenvalues are
             stored in the same order as on the diagonal of the Schur
             form returned in H, with W(i) = H(i,i).

       Z     (input/output) COMPLEX*16 array, dimension (LDZ,IHI)
             If WANTZ is .FALSE., then Z is not referenced.
             If WANTZ is .TRUE., then Z(ILO:IHI,ILOZ:IHIZ) is
             replaced by Z(ILO:IHI,ILOZ:IHIZ)*U where U is the
             orthogonal Schur factor of H(ILO:IHI,ILO:IHI).
             (The output value of Z when INFO.GT.0 is given under
             the description of INFO below.)

       LDZ   (input) INTEGER
             The leading dimension of the array Z.  if WANTZ is .TRUE.
             then LDZ.GE.MAX(1,IHIZ).  Otherwize, LDZ.GE.1.

       WORK  (workspace/output) COMPLEX*16 array, dimension LWORK
             On exit, if LWORK = -1, WORK(1) returns an estimate of
             the optimal value for LWORK.

       LWORK (input) INTEGER
             The dimension of the array WORK.  LWORK .GE. max(1,N)
             is sufficient, but LWORK typically as large as 6*N may
             be required for optimal performance.  A workspace query
             to determine the optimal workspace size is recommended.

             If LWORK = -1, then ZLAQR0 does a workspace query.
             In this case, ZLAQR0 checks the input parameters and
             estimates the optimal workspace size for the given
             values of N, ILO and IHI.  The estimate is returned
             in WORK(1).  No error message related to LWORK is
             issued by XERBLA.  Neither H nor Z are accessed.


       INFO  (output) INTEGER
               =  0:  successful exit
             .GT. 0:  if INFO = i, ZLAQR0 failed to compute all of
                  the eigenvalues.  Elements 1:ilo-1 and i+1:n of WR
                  and WI contain those eigenvalues which have been
                  successfully computed.  (Failures are rare.)

                  If INFO .GT. 0 and WANT is .FALSE., then on exit,
                  the remaining unconverged eigenvalues are the eigen-
                  values of the upper Hessenberg matrix rows and
                  columns ILO through INFO of the final, output
                  value of H.

                  If INFO .GT. 0 and WANTT is .TRUE., then on exit

             (*)  (initial value of H)*U  = U*(final value of H)

                  where U is a unitary matrix.  The final
                  value of  H is upper Hessenberg and triangular in
                  rows and columns INFO+1 through IHI.

                  If INFO .GT. 0 and WANTZ is .TRUE., then on exit

                    (final value of Z(ILO:IHI,ILOZ:IHIZ)
                     =  (initial value of Z(ILO:IHI,ILOZ:IHIZ)*U

                  where U is the unitary matrix in (*) (regard-
                  less of the value of WANTT.)

                  If INFO .GT. 0 and WANTZ is .FALSE., then Z is not
                  accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       References:
         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part I: Maintaining Well Focused Shifts, and Level 3
         Performance, SIAM Journal of Matrix Analysis, volume 23, pages
         929--947, 2002.

         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part II: Aggressive Early Deflation, SIAM Journal
         of Matrix Analysis, volume 23, pages 948--973, 2002.

       ================================================================

       ==== Matrices of order NTINY or smaller must be processed by
       .    ZLAHQR because of insufficient subdiagonal scratch space.
       .    (This is a hard limit.) ====

       ==== Exceptional deflation windows:  try to cure rare
       .    slow convergence by varying the size of the
       .    deflation window after KEXNW iterations. ====

       ==== Exceptional shifts: try to cure rare slow convergence
       .    with ad-hoc exceptional shifts every KEXSH iterations.
       .    ====

       ==== The constant WILK1 is used to form the exceptional
       .    shifts. ====
','line_number':13926,'multiline':True]['text':' Parameter adjustments ','line_number':14086,'multiline':True]['text':' Function Body ','line_number':14096,'multiline':True]['text':'     ==== Quick return for N = 0: nothing to do. ==== ','line_number':14099,'multiline':True]['text':'        ==== Tiny matrices must use ZLAHQR. ==== ','line_number':14108,'multiline':True]['text':'
          ==== Use small bulge multi-shift QR with aggressive early
          .    deflation on larger-than-tiny matrices. ====

          ==== Hope for the best. ====
','line_number':14117,'multiline':True]['text':'        ==== Set up job flags for ILAENV. ==== ','line_number':14126,'multiline':True]['text':'
          ==== NWR = recommended deflation window size.  At this
          .    point,  N .GT. NTINY = 11, so there is enough
          .    subdiagonal workspace for NWR.GE.2 as required.
          .    (In fact, there is enough subdiagonal space for
          .    NWR.GE.3.) ====
','line_number':14139,'multiline':True]['text':' Computing MIN ','line_number':14150,'multiline':True]['text':'
          ==== NSR = recommended number of simultaneous shifts.
          .    At this point N .GT. NTINY = 11, so there is at
          .    enough subdiagonal workspace for NSR to be even
          .    and greater than or equal to two as required. ====
','line_number':14154,'multiline':True]['text':' Computing MIN ','line_number':14163,'multiline':True]['text':' Computing MAX ','line_number':14167,'multiline':True]['text':'
          ==== Estimate optimal workspace ====

          ==== Workspace query call to ZLAQR3 ====
','line_number':14171,'multiline':True]['text':'
          ==== Optimal workspace = MAX(ZLAQR5, ZLAQR3) ====

   Computing MAX
','line_number':14183,'multiline':True]['text':'        ==== Quick return in case of workspace query. ==== ','line_number':14191,'multiline':True]['text':'        ==== ZLAHQR/ZLAQR0 crossover point ==== ','line_number':14200,'multiline':True]['text':'        ==== Nibble crossover point ==== ','line_number':14206,'multiline':True]['text':'
          ==== Accumulate reflections during ttswp?  Use block
          .    2-by-2 structure during matrix-matrix multiply? ====
','line_number':14212,'multiline':True]['text':'
          ==== NWMAX = the largest possible deflation window for
          .    which there is sufficient workspace. ====

   Computing MIN
','line_number':14222,'multiline':True]['text':'
          ==== NSMAX = the Largest number of simultaneous shifts
          .    for which there is sufficient workspace. ====

   Computing MIN
','line_number':14232,'multiline':True]['text':'        ==== NDFL: an iteration count restarted at deflation. ==== ','line_number':14242,'multiline':True]['text':'
          ==== ITMAX = iteration limit ====

   Computing MAX
','line_number':14246,'multiline':True]['text':'        ==== Last row and column in the active block ==== ','line_number':14254,'multiline':True]['text':'        ==== Main Loop ==== ','line_number':14258,'multiline':True]['text':'           ==== Done when KBOT falls below ILO ==== ','line_number':14263,'multiline':True]['text':'           ==== Locate active block ==== ','line_number':14269,'multiline':True]['text':' L10: ','line_number':14277,'multiline':True]['text':'
             ==== Select deflation window size:
             .    Typical Case:
             .      If possible and advisable, nibble the entire
             .      active block.  If not, use size MIN(NWR,NWMAX)
             .      or MIN(NWR+1,NWMAX) depending upon which has
             .      the smaller corresponding subdiagonal entry
             .      (a heuristic).
             .
             .    Exceptional Case:
             .      If there have been no deflations in KEXNW or
             .      more iterations, then vary the deflation window
             .      size.   At first, because, larger windows are,
             .      in general, more powerful than smaller ones,
             .      rapidly increase the window to the maximum possible.
             .      Then, gradually reduce the window size. ====
','line_number':14283,'multiline':True]['text':' Computing MIN ','line_number':14306,'multiline':True]['text':'
             ==== Aggressive early deflation:
             .    split workspace under the subdiagonal into
             .      - an nw-by-nw work array V in the lower
             .        left-hand-corner,
             .      - an NW-by-at-least-NW-but-more-is-better
             .        (NW-by-NHO) horizontal work array along
             .        the bottom edge,
             .      - an at-least-NW-but-more-is-better (NHV-by-NW)
             .        vertical work array along the left-hand-edge.
             .        ====
','line_number':14336,'multiline':True]['text':'           ==== Aggressive early deflation ==== ','line_number':14355,'multiline':True]['text':'           ==== Adjust KBOT accounting for new deflations. ==== ','line_number':14362,'multiline':True]['text':'           ==== KS points to the shifts. ==== ','line_number':14366,'multiline':True]['text':'
             ==== Skip an expensive QR sweep if there is a (partly
             .    heuristic) reason to expect that many eigenvalues
             .    will deflate without it.  Here, the QR sweep is
             .    skipped if many eigenvalues have just been deflated
             .    or if the remaining active block is small.
','line_number':14370,'multiline':True]['text':'
                ==== NS = nominal number of simultaneous shifts.
                .    This may be lowered (slightly) if ZLAQR3
                .    did not provide that many shifts. ====

   Computing MIN
   Computing MAX
','line_number':14381,'multiline':True]['text':'
                ==== If there have been no deflations
                .    in a multiple of KEXSH iterations,
                .    then try exceptional shifts.
                .    Otherwise use shifts provided by
                .    ZLAQR3 above or from the eigenvalues
                .    of a trailing principal submatrix. ====
','line_number':14394,'multiline':True]['text':' L30: ','line_number':14418,'multiline':True]['text':'
                   ==== Got NS/2 or fewer shifts? Use ZLAQR4 or
                   .    ZLAHQR on a trailing principal submatrix to
                   .    get more. (Since NS.LE.NSMAX.LE.(N+6)/9,
                   .    there is enough space below the subdiagonal
                   .    to fit an NS-by-NS scratch array.) ====
','line_number':14422,'multiline':True]['text':'
                      ==== In case of a rare QR failure use
                      .    eigenvalues of the trailing 2-by-2
                      .    principal submatrix.  Scale to avoid
                      .    overflows, underflows and subnormals.
                      .    (The scale factor S can not be zero,
                      .    because H(KBOT,KBOT-1) is nonzero.) ====
','line_number':14446,'multiline':True]['text':'                    ==== Sort the shifts (Helps a little) ==== ','line_number':14519,'multiline':True]['text':' L40: ','line_number':14546,'multiline':True]['text':' L50: ','line_number':14548,'multiline':True]['text':'
                ==== If there are only two shifts, then use
                .    only one.  ====
','line_number':14555,'multiline':True]['text':'
                ==== Use up to NS of the smallest magnatiude
                .    shifts.  If there aren't NS shifts available,
                .    then use them all, possibly dropping one to
                .    make the number of shifts even. ====

   Computing MIN
','line_number':14584,'multiline':True]['text':'
                ==== Small-bulge multi-shift QR sweep:
                .    split workspace under the subdiagonal into
                .    - a KDU-by-KDU work array U in the lower
                .      left-hand-corner,
                .    - a KDU-by-at-least-KDU-but-more-is-better
                .      (KDU-by-NHo) horizontal work array WH along
                .      the bottom edge,
                .    - and an at-least-KDU-but-more-is-better-by-KDU
                .      (NVE-by-KDU) vertical work WV arrow along
                .      the left-hand-edge. ====
','line_number':14597,'multiline':True]['text':'              ==== Small-bulge multi-shift QR sweep ==== ','line_number':14617,'multiline':True]['text':'           ==== Note progress (or the lack of it). ==== ','line_number':14626,'multiline':True]['text':'
             ==== End of main loop ====
   L70:
','line_number':14634,'multiline':True]['text':'
          ==== Iteration limit exceeded.  Set INFO to show where
          .    the problem occurred and exit. ====
','line_number':14640,'multiline':True]['text':'     ==== Return the optimal value of LWORK. ==== ','line_number':14650,'multiline':True]['text':'     ==== End of ZLAQR0 ==== ','line_number':14656,'multiline':True]['text':' zlaqr0_ ','line_number':14659,'multiline':True]['text':' Subroutine ','line_number':14661,'multiline':True]['text':' System generated locals ','line_number':14664,'multiline':True]['text':' Local variables ','line_number':14669,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


         Given a 2-by-2 or 3-by-3 matrix H, ZLAQR1 sets v to a
         scalar multiple of the first column of the product

         (*)  K = (H - s1*I)*(H - s2*I)

         scaling to avoid overflows and most underflows.

         This is useful for starting double implicit shift bulges
         in the QR algorithm.


         N      (input) integer
                Order of the matrix H. N must be either 2 or 3.

         H      (input) COMPLEX*16 array of dimension (LDH,N)
                The 2-by-2 or 3-by-3 matrix H in (*).

         LDH    (input) integer
                The leading dimension of H as declared in
                the calling procedure.  LDH.GE.N

         S1     (input) COMPLEX*16
         S2     S1 and S2 are the shifts defining K in (*) above.

         V      (output) COMPLEX*16 array of dimension N
                A scalar multiple of the first column of the
                matrix K in (*).

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
','line_number':14674,'multiline':True]['text':' Parameter adjustments ','line_number':14716,'multiline':True]['text':' Function Body ','line_number':14722,'multiline':True]['text':' zlaqr1_ ','line_number':14826,'multiline':True]['text':' Subroutine ','line_number':14828,'multiline':True]['text':' System generated locals ','line_number':14836,'multiline':True]['text':' Local variables ','line_number':14842,'multiline':True]['text':' Subroutine ','line_number':14854,'multiline':True]['text':' Subroutine ','line_number':14858,'multiline':True]['text':' Subroutine ','line_number':14863,'multiline':True]['text':' Subroutine ','line_number':14867,'multiline':True]['text':' Subroutine ','line_number':14878,'multiline':True]['text':' Subroutine ','line_number':14882,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2.1)                        --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
    -- April 2009                                                      --


       This subroutine is identical to ZLAQR3 except that it avoids
       recursion by calling ZLAHQR instead of ZLAQR4.


       ******************************************************************
       Aggressive early deflation:

       This subroutine accepts as input an upper Hessenberg matrix
       H and performs an unitary similarity transformation
       designed to detect and deflate fully converged eigenvalues from
       a trailing principal submatrix.  On output H has been over-
       written by a new Hessenberg matrix that is a perturbation of
       an unitary similarity transformation of H.  It is to be
       hoped that the final version of H has many zero subdiagonal
       entries.

       ******************************************************************
       WANTT   (input) LOGICAL
            If .TRUE., then the Hessenberg matrix H is fully updated
            so that the triangular Schur factor may be
            computed (in cooperation with the calling subroutine).
            If .FALSE., then only enough of H is updated to preserve
            the eigenvalues.

       WANTZ   (input) LOGICAL
            If .TRUE., then the unitary matrix Z is updated so
            so that the unitary Schur factor may be computed
            (in cooperation with the calling subroutine).
            If .FALSE., then Z is not referenced.

       N       (input) INTEGER
            The order of the matrix H and (if WANTZ is .TRUE.) the
            order of the unitary matrix Z.

       KTOP    (input) INTEGER
            It is assumed that either KTOP = 1 or H(KTOP,KTOP-1)=0.
            KBOT and KTOP together determine an isolated block
            along the diagonal of the Hessenberg matrix.

       KBOT    (input) INTEGER
            It is assumed without a check that either
            KBOT = N or H(KBOT+1,KBOT)=0.  KBOT and KTOP together
            determine an isolated block along the diagonal of the
            Hessenberg matrix.

       NW      (input) INTEGER
            Deflation window size.  1 .LE. NW .LE. (KBOT-KTOP+1).

       H       (input/output) COMPLEX*16 array, dimension (LDH,N)
            On input the initial N-by-N section of H stores the
            Hessenberg matrix undergoing aggressive early deflation.
            On output H has been transformed by a unitary
            similarity transformation, perturbed, and the returned
            to Hessenberg form that (it is to be hoped) has some
            zero subdiagonal entries.

       LDH     (input) integer
            Leading dimension of H just as declared in the calling
            subroutine.  N .LE. LDH

       ILOZ    (input) INTEGER
       IHIZ    (input) INTEGER
            Specify the rows of Z to which transformations must be
            applied if WANTZ is .TRUE.. 1 .LE. ILOZ .LE. IHIZ .LE. N.

       Z       (input/output) COMPLEX*16 array, dimension (LDZ,N)
            IF WANTZ is .TRUE., then on output, the unitary
            similarity transformation mentioned above has been
            accumulated into Z(ILOZ:IHIZ,ILO:IHI) from the right.
            If WANTZ is .FALSE., then Z is unreferenced.

       LDZ     (input) integer
            The leading dimension of Z just as declared in the
            calling subroutine.  1 .LE. LDZ.

       NS      (output) integer
            The number of unconverged (ie approximate) eigenvalues
            returned in SR and SI that may be used as shifts by the
            calling subroutine.

       ND      (output) integer
            The number of converged eigenvalues uncovered by this
            subroutine.

       SH      (output) COMPLEX*16 array, dimension KBOT
            On output, approximate eigenvalues that may
            be used for shifts are stored in SH(KBOT-ND-NS+1)
            through SR(KBOT-ND).  Converged eigenvalues are
            stored in SH(KBOT-ND+1) through SH(KBOT).

       V       (workspace) COMPLEX*16 array, dimension (LDV,NW)
            An NW-by-NW work array.

       LDV     (input) integer scalar
            The leading dimension of V just as declared in the
            calling subroutine.  NW .LE. LDV

       NH      (input) integer scalar
            The number of columns of T.  NH.GE.NW.

       T       (workspace) COMPLEX*16 array, dimension (LDT,NW)

       LDT     (input) integer
            The leading dimension of T just as declared in the
            calling subroutine.  NW .LE. LDT

       NV      (input) integer
            The number of rows of work array WV available for
            workspace.  NV.GE.NW.

       WV      (workspace) COMPLEX*16 array, dimension (LDWV,NW)

       LDWV    (input) integer
            The leading dimension of W just as declared in the
            calling subroutine.  NW .LE. LDV

       WORK    (workspace) COMPLEX*16 array, dimension LWORK.
            On exit, WORK(1) is set to an estimate of the optimal value
            of LWORK for the given values of N, NW, KTOP and KBOT.

       LWORK   (input) integer
            The dimension of the work array WORK.  LWORK = 2*NW
            suffices, but greater efficiency may result from larger
            values of LWORK.

            If LWORK = -1, then a workspace query is assumed; ZLAQR2
            only estimates the optimal workspace size for the given
            values of N, NW, KTOP and KBOT.  The estimate is returned
            in WORK(1).  No error message related to LWORK is issued
            by XERBLA.  Neither H nor Z are accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================

       ==== Estimate optimal workspace. ====
','line_number':14888,'multiline':True]['text':' Parameter adjustments ','line_number':15035,'multiline':True]['text':' Function Body ','line_number':15054,'multiline':True]['text':' Computing MIN ','line_number':15055,'multiline':True]['text':'        ==== Workspace query call to ZGEHRD ==== ','line_number':15062,'multiline':True]['text':'        ==== Workspace query call to ZUNMHR ==== ','line_number':15069,'multiline':True]['text':'        ==== Optimal workspace ==== ','line_number':15076,'multiline':True]['text':'     ==== Quick return in case of workspace query. ==== ','line_number':15081,'multiline':True]['text':'
       ==== Nothing to do ...
       ... for an empty active block ... ====
','line_number':15090,'multiline':True]['text':'     ... nor for an empty deflation window. ==== ','line_number':15100,'multiline':True]['text':'     ==== Machine constants ==== ','line_number':15105,'multiline':True]['text':'
       ==== Setup deflation window ====

   Computing MIN
','line_number':15113,'multiline':True]['text':'        ==== 1-by-1 deflation window: not much to do ==== ','line_number':15130,'multiline':True]['text':' Computing MAX ','line_number':15137,'multiline':True]['text':'
       ==== Convert to spike-triangular form.  (In case of a
       .    rare QR failure, this routine continues to do
       .    aggressive early deflation using that part of
       .    the deflation window that converged using INFQR
       .    here and there to keep track.) ====
','line_number':15154,'multiline':True]['text':'     ==== Deflation detection loop ==== ','line_number':15174,'multiline':True]['text':'        ==== Small spike tip deflation test ==== ','line_number':15181,'multiline':True]['text':' Computing MAX ','line_number':15190,'multiline':True]['text':'           ==== One more converged eigenvalue ==== ','line_number':15196,'multiline':True]['text':'
             ==== One undeflatable eigenvalue.  Move it up out of the
             .    way.   (ZTREXC can not fail in this case.) ====
','line_number':15201,'multiline':True]['text':' L10: ','line_number':15211,'multiline':True]['text':'        ==== Return to Hessenberg form ==== ','line_number':15214,'multiline':True]['text':'
          ==== sorting the diagonal of T improves accuracy for
          .    graded matrices.  ====
','line_number':15222,'multiline':True]['text':' L20: ','line_number':15240,'multiline':True]['text':' L30: ','line_number':15247,'multiline':True]['text':'     ==== Restore shift/eigenvalue array from T ==== ','line_number':15251,'multiline':True]['text':' L40: ','line_number':15258,'multiline':True]['text':'           ==== Reflect spike back into lower triangle ==== ','line_number':15265,'multiline':True]['text':' L50: ','line_number':15273,'multiline':True]['text':'        ==== Copy updated reduced window into place ==== ','line_number':15296,'multiline':True]['text':'
          ==== Accumulate orthogonal matrix in order update
          .    H and Z, if requested.  ====
','line_number':15313,'multiline':True]['text':'        ==== Update vertical slab in H ==== ','line_number':15324,'multiline':True]['text':' Computing MIN ','line_number':15335,'multiline':True]['text':' L60: ','line_number':15343,'multiline':True]['text':'        ==== Update horizontal slab in H ==== ','line_number':15346,'multiline':True]['text':' Computing MIN ','line_number':15353,'multiline':True]['text':' L70: ','line_number':15361,'multiline':True]['text':'        ==== Update vertical slab in Z ==== ','line_number':15365,'multiline':True]['text':' Computing MIN ','line_number':15372,'multiline':True]['text':' L80: ','line_number':15380,'multiline':True]['text':'     ==== Return the number of deflations ... ==== ','line_number':15385,'multiline':True]['text':'
       ==== ... and the number of shifts. (Subtracting
       .    INFQR from the spike length takes care
       .    of the case of a rare QR failure while
       .    calculating eigenvalues of the deflation
       .    window.)  ====
','line_number':15389,'multiline':True]['text':'      ==== Return optimal workspace. ==== ','line_number':15399,'multiline':True]['text':'     ==== End of ZLAQR2 ==== ','line_number':15405,'multiline':True]['text':' zlaqr2_ ','line_number':15408,'multiline':True]['text':' Subroutine ','line_number':15410,'multiline':True]['text':' System generated locals ','line_number':15418,'multiline':True]['text':' Local variables ','line_number':15424,'multiline':True]['text':' Subroutine ','line_number':15436,'multiline':True]['text':' Subroutine ','line_number':15440,'multiline':True]['text':' Subroutine ','line_number':15445,'multiline':True]['text':' Subroutine ','line_number':15456,'multiline':True]['text':' Subroutine ','line_number':15467,'multiline':True]['text':' Subroutine ','line_number':15471,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2.1)                        --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
    -- April 2009                                                      --


       ******************************************************************
       Aggressive early deflation:

       This subroutine accepts as input an upper Hessenberg matrix
       H and performs an unitary similarity transformation
       designed to detect and deflate fully converged eigenvalues from
       a trailing principal submatrix.  On output H has been over-
       written by a new Hessenberg matrix that is a perturbation of
       an unitary similarity transformation of H.  It is to be
       hoped that the final version of H has many zero subdiagonal
       entries.

       ******************************************************************
       WANTT   (input) LOGICAL
            If .TRUE., then the Hessenberg matrix H is fully updated
            so that the triangular Schur factor may be
            computed (in cooperation with the calling subroutine).
            If .FALSE., then only enough of H is updated to preserve
            the eigenvalues.

       WANTZ   (input) LOGICAL
            If .TRUE., then the unitary matrix Z is updated so
            so that the unitary Schur factor may be computed
            (in cooperation with the calling subroutine).
            If .FALSE., then Z is not referenced.

       N       (input) INTEGER
            The order of the matrix H and (if WANTZ is .TRUE.) the
            order of the unitary matrix Z.

       KTOP    (input) INTEGER
            It is assumed that either KTOP = 1 or H(KTOP,KTOP-1)=0.
            KBOT and KTOP together determine an isolated block
            along the diagonal of the Hessenberg matrix.

       KBOT    (input) INTEGER
            It is assumed without a check that either
            KBOT = N or H(KBOT+1,KBOT)=0.  KBOT and KTOP together
            determine an isolated block along the diagonal of the
            Hessenberg matrix.

       NW      (input) INTEGER
            Deflation window size.  1 .LE. NW .LE. (KBOT-KTOP+1).

       H       (input/output) COMPLEX*16 array, dimension (LDH,N)
            On input the initial N-by-N section of H stores the
            Hessenberg matrix undergoing aggressive early deflation.
            On output H has been transformed by a unitary
            similarity transformation, perturbed, and the returned
            to Hessenberg form that (it is to be hoped) has some
            zero subdiagonal entries.

       LDH     (input) integer
            Leading dimension of H just as declared in the calling
            subroutine.  N .LE. LDH

       ILOZ    (input) INTEGER
       IHIZ    (input) INTEGER
            Specify the rows of Z to which transformations must be
            applied if WANTZ is .TRUE.. 1 .LE. ILOZ .LE. IHIZ .LE. N.

       Z       (input/output) COMPLEX*16 array, dimension (LDZ,N)
            IF WANTZ is .TRUE., then on output, the unitary
            similarity transformation mentioned above has been
            accumulated into Z(ILOZ:IHIZ,ILO:IHI) from the right.
            If WANTZ is .FALSE., then Z is unreferenced.

       LDZ     (input) integer
            The leading dimension of Z just as declared in the
            calling subroutine.  1 .LE. LDZ.

       NS      (output) integer
            The number of unconverged (ie approximate) eigenvalues
            returned in SR and SI that may be used as shifts by the
            calling subroutine.

       ND      (output) integer
            The number of converged eigenvalues uncovered by this
            subroutine.

       SH      (output) COMPLEX*16 array, dimension KBOT
            On output, approximate eigenvalues that may
            be used for shifts are stored in SH(KBOT-ND-NS+1)
            through SR(KBOT-ND).  Converged eigenvalues are
            stored in SH(KBOT-ND+1) through SH(KBOT).

       V       (workspace) COMPLEX*16 array, dimension (LDV,NW)
            An NW-by-NW work array.

       LDV     (input) integer scalar
            The leading dimension of V just as declared in the
            calling subroutine.  NW .LE. LDV

       NH      (input) integer scalar
            The number of columns of T.  NH.GE.NW.

       T       (workspace) COMPLEX*16 array, dimension (LDT,NW)

       LDT     (input) integer
            The leading dimension of T just as declared in the
            calling subroutine.  NW .LE. LDT

       NV      (input) integer
            The number of rows of work array WV available for
            workspace.  NV.GE.NW.

       WV      (workspace) COMPLEX*16 array, dimension (LDWV,NW)

       LDWV    (input) integer
            The leading dimension of W just as declared in the
            calling subroutine.  NW .LE. LDV

       WORK    (workspace) COMPLEX*16 array, dimension LWORK.
            On exit, WORK(1) is set to an estimate of the optimal value
            of LWORK for the given values of N, NW, KTOP and KBOT.

       LWORK   (input) integer
            The dimension of the work array WORK.  LWORK = 2*NW
            suffices, but greater efficiency may result from larger
            values of LWORK.

            If LWORK = -1, then a workspace query is assumed; ZLAQR3
            only estimates the optimal workspace size for the given
            values of N, NW, KTOP and KBOT.  The estimate is returned
            in WORK(1).  No error message related to LWORK is issued
            by XERBLA.  Neither H nor Z are accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================

       ==== Estimate optimal workspace. ====
','line_number':15477,'multiline':True]['text':' Parameter adjustments ','line_number':15620,'multiline':True]['text':' Function Body ','line_number':15639,'multiline':True]['text':' Computing MIN ','line_number':15640,'multiline':True]['text':'        ==== Workspace query call to ZGEHRD ==== ','line_number':15647,'multiline':True]['text':'        ==== Workspace query call to ZUNMHR ==== ','line_number':15654,'multiline':True]['text':'        ==== Workspace query call to ZLAQR4 ==== ','line_number':15661,'multiline':True]['text':'
          ==== Optimal workspace ====

   Computing MAX
','line_number':15667,'multiline':True]['text':'     ==== Quick return in case of workspace query. ==== ','line_number':15676,'multiline':True]['text':'
       ==== Nothing to do ...
       ... for an empty active block ... ====
','line_number':15685,'multiline':True]['text':'     ... nor for an empty deflation window. ==== ','line_number':15695,'multiline':True]['text':'     ==== Machine constants ==== ','line_number':15700,'multiline':True]['text':'
       ==== Setup deflation window ====

   Computing MIN
','line_number':15708,'multiline':True]['text':'        ==== 1-by-1 deflation window: not much to do ==== ','line_number':15725,'multiline':True]['text':' Computing MAX ','line_number':15732,'multiline':True]['text':'
       ==== Convert to spike-triangular form.  (In case of a
       .    rare QR failure, this routine continues to do
       .    aggressive early deflation using that part of
       .    the deflation window that converged using INFQR
       .    here and there to keep track.) ====
','line_number':15749,'multiline':True]['text':'     ==== Deflation detection loop ==== ','line_number':15777,'multiline':True]['text':'        ==== Small spike tip deflation test ==== ','line_number':15784,'multiline':True]['text':' Computing MAX ','line_number':15793,'multiline':True]['text':'           ==== One more converged eigenvalue ==== ','line_number':15799,'multiline':True]['text':'
             ==== One undeflatable eigenvalue.  Move it up out of the
             .    way.   (ZTREXC can not fail in this case.) ====
','line_number':15804,'multiline':True]['text':' L10: ','line_number':15814,'multiline':True]['text':'        ==== Return to Hessenberg form ==== ','line_number':15817,'multiline':True]['text':'
          ==== sorting the diagonal of T improves accuracy for
          .    graded matrices.  ====
','line_number':15825,'multiline':True]['text':' L20: ','line_number':15843,'multiline':True]['text':' L30: ','line_number':15850,'multiline':True]['text':'     ==== Restore shift/eigenvalue array from T ==== ','line_number':15854,'multiline':True]['text':' L40: ','line_number':15861,'multiline':True]['text':'           ==== Reflect spike back into lower triangle ==== ','line_number':15868,'multiline':True]['text':' L50: ','line_number':15876,'multiline':True]['text':'        ==== Copy updated reduced window into place ==== ','line_number':15899,'multiline':True]['text':'
          ==== Accumulate orthogonal matrix in order update
          .    H and Z, if requested.  ====
','line_number':15916,'multiline':True]['text':'        ==== Update vertical slab in H ==== ','line_number':15927,'multiline':True]['text':' Computing MIN ','line_number':15938,'multiline':True]['text':' L60: ','line_number':15946,'multiline':True]['text':'        ==== Update horizontal slab in H ==== ','line_number':15949,'multiline':True]['text':' Computing MIN ','line_number':15956,'multiline':True]['text':' L70: ','line_number':15964,'multiline':True]['text':'        ==== Update vertical slab in Z ==== ','line_number':15968,'multiline':True]['text':' Computing MIN ','line_number':15975,'multiline':True]['text':' L80: ','line_number':15983,'multiline':True]['text':'     ==== Return the number of deflations ... ==== ','line_number':15988,'multiline':True]['text':'
       ==== ... and the number of shifts. (Subtracting
       .    INFQR from the spike length takes care
       .    of the case of a rare QR failure while
       .    calculating eigenvalues of the deflation
       .    window.)  ====
','line_number':15992,'multiline':True]['text':'      ==== Return optimal workspace. ==== ','line_number':16002,'multiline':True]['text':'     ==== End of ZLAQR3 ==== ','line_number':16008,'multiline':True]['text':' zlaqr3_ ','line_number':16011,'multiline':True]['text':' Subroutine ','line_number':16013,'multiline':True]['text':' System generated locals ','line_number':16018,'multiline':True]['text':' Local variables ','line_number':16023,'multiline':True]['text':' was [1][1] ','line_number':16033,'multiline':True]['text':' Subroutine ','line_number':16035,'multiline':True]['text':' Subroutine ','line_number':16053,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       This subroutine implements one level of recursion for ZLAQR0.
       It is a complete implementation of the small bulge multi-shift
       QR algorithm.  It may be called by ZLAQR0 and, for large enough
       deflation window size, it may be called by ZLAQR3.  This
       subroutine is identical to ZLAQR0 except that it calls ZLAQR2
       instead of ZLAQR3.

       Purpose
       =======

       ZLAQR4 computes the eigenvalues of a Hessenberg matrix H
       and, optionally, the matrices T and Z from the Schur decomposition
       H = Z T Z**H, where T is an upper triangular matrix (the
       Schur form), and Z is the unitary matrix of Schur vectors.

       Optionally Z may be postmultiplied into an input unitary
       matrix Q so that this routine can give the Schur factorization
       of a matrix A which has been reduced to the Hessenberg form H
       by the unitary matrix Q:  A = Q*H*Q**H = (QZ)*H*(QZ)**H.

       Arguments
       =========

       WANTT   (input) LOGICAL
            = .TRUE. : the full Schur form T is required;
            = .FALSE.: only eigenvalues are required.

       WANTZ   (input) LOGICAL
            = .TRUE. : the matrix of Schur vectors Z is required;
            = .FALSE.: Schur vectors are not required.

       N     (input) INTEGER
             The order of the matrix H.  N .GE. 0.

       ILO   (input) INTEGER
       IHI   (input) INTEGER
             It is assumed that H is already upper triangular in rows
             and columns 1:ILO-1 and IHI+1:N and, if ILO.GT.1,
             H(ILO,ILO-1) is zero. ILO and IHI are normally set by a
             previous call to ZGEBAL, and then passed to ZGEHRD when the
             matrix output by ZGEBAL is reduced to Hessenberg form.
             Otherwise, ILO and IHI should be set to 1 and N,
             respectively.  If N.GT.0, then 1.LE.ILO.LE.IHI.LE.N.
             If N = 0, then ILO = 1 and IHI = 0.

       H     (input/output) COMPLEX*16 array, dimension (LDH,N)
             On entry, the upper Hessenberg matrix H.
             On exit, if INFO = 0 and WANTT is .TRUE., then H
             contains the upper triangular matrix T from the Schur
             decomposition (the Schur form). If INFO = 0 and WANT is
             .FALSE., then the contents of H are unspecified on exit.
             (The output value of H when INFO.GT.0 is given under the
             description of INFO below.)

             This subroutine may explicitly set H(i,j) = 0 for i.GT.j and
             j = 1, 2, ... ILO-1 or j = IHI+1, IHI+2, ... N.

       LDH   (input) INTEGER
             The leading dimension of the array H. LDH .GE. max(1,N).

       W        (output) COMPLEX*16 array, dimension (N)
             The computed eigenvalues of H(ILO:IHI,ILO:IHI) are stored
             in W(ILO:IHI). If WANTT is .TRUE., then the eigenvalues are
             stored in the same order as on the diagonal of the Schur
             form returned in H, with W(i) = H(i,i).

       Z     (input/output) COMPLEX*16 array, dimension (LDZ,IHI)
             If WANTZ is .FALSE., then Z is not referenced.
             If WANTZ is .TRUE., then Z(ILO:IHI,ILOZ:IHIZ) is
             replaced by Z(ILO:IHI,ILOZ:IHIZ)*U where U is the
             orthogonal Schur factor of H(ILO:IHI,ILO:IHI).
             (The output value of Z when INFO.GT.0 is given under
             the description of INFO below.)

       LDZ   (input) INTEGER
             The leading dimension of the array Z.  if WANTZ is .TRUE.
             then LDZ.GE.MAX(1,IHIZ).  Otherwize, LDZ.GE.1.

       WORK  (workspace/output) COMPLEX*16 array, dimension LWORK
             On exit, if LWORK = -1, WORK(1) returns an estimate of
             the optimal value for LWORK.

       LWORK (input) INTEGER
             The dimension of the array WORK.  LWORK .GE. max(1,N)
             is sufficient, but LWORK typically as large as 6*N may
             be required for optimal performance.  A workspace query
             to determine the optimal workspace size is recommended.

             If LWORK = -1, then ZLAQR4 does a workspace query.
             In this case, ZLAQR4 checks the input parameters and
             estimates the optimal workspace size for the given
             values of N, ILO and IHI.  The estimate is returned
             in WORK(1).  No error message related to LWORK is
             issued by XERBLA.  Neither H nor Z are accessed.


       INFO  (output) INTEGER
               =  0:  successful exit
             .GT. 0:  if INFO = i, ZLAQR4 failed to compute all of
                  the eigenvalues.  Elements 1:ilo-1 and i+1:n of WR
                  and WI contain those eigenvalues which have been
                  successfully computed.  (Failures are rare.)

                  If INFO .GT. 0 and WANT is .FALSE., then on exit,
                  the remaining unconverged eigenvalues are the eigen-
                  values of the upper Hessenberg matrix rows and
                  columns ILO through INFO of the final, output
                  value of H.

                  If INFO .GT. 0 and WANTT is .TRUE., then on exit

             (*)  (initial value of H)*U  = U*(final value of H)

                  where U is a unitary matrix.  The final
                  value of  H is upper Hessenberg and triangular in
                  rows and columns INFO+1 through IHI.

                  If INFO .GT. 0 and WANTZ is .TRUE., then on exit

                    (final value of Z(ILO:IHI,ILOZ:IHIZ)
                     =  (initial value of Z(ILO:IHI,ILOZ:IHIZ)*U

                  where U is the unitary matrix in (*) (regard-
                  less of the value of WANTT.)

                  If INFO .GT. 0 and WANTZ is .FALSE., then Z is not
                  accessed.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       References:
         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part I: Maintaining Well Focused Shifts, and Level 3
         Performance, SIAM Journal of Matrix Analysis, volume 23, pages
         929--947, 2002.

         K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
         Algorithm Part II: Aggressive Early Deflation, SIAM Journal
         of Matrix Analysis, volume 23, pages 948--973, 2002.

       ================================================================

       ==== Matrices of order NTINY or smaller must be processed by
       .    ZLAHQR because of insufficient subdiagonal scratch space.
       .    (This is a hard limit.) ====

       ==== Exceptional deflation windows:  try to cure rare
       .    slow convergence by varying the size of the
       .    deflation window after KEXNW iterations. ====

       ==== Exceptional shifts: try to cure rare slow convergence
       .    with ad-hoc exceptional shifts every KEXSH iterations.
       .    ====

       ==== The constant WILK1 is used to form the exceptional
       .    shifts. ====
','line_number':16061,'multiline':True]['text':' Parameter adjustments ','line_number':16228,'multiline':True]['text':' Function Body ','line_number':16238,'multiline':True]['text':'     ==== Quick return for N = 0: nothing to do. ==== ','line_number':16241,'multiline':True]['text':'        ==== Tiny matrices must use ZLAHQR. ==== ','line_number':16250,'multiline':True]['text':'
          ==== Use small bulge multi-shift QR with aggressive early
          .    deflation on larger-than-tiny matrices. ====

          ==== Hope for the best. ====
','line_number':16259,'multiline':True]['text':'        ==== Set up job flags for ILAENV. ==== ','line_number':16268,'multiline':True]['text':'
          ==== NWR = recommended deflation window size.  At this
          .    point,  N .GT. NTINY = 11, so there is enough
          .    subdiagonal workspace for NWR.GE.2 as required.
          .    (In fact, there is enough subdiagonal space for
          .    NWR.GE.3.) ====
','line_number':16281,'multiline':True]['text':' Computing MIN ','line_number':16292,'multiline':True]['text':'
          ==== NSR = recommended number of simultaneous shifts.
          .    At this point N .GT. NTINY = 11, so there is at
          .    enough subdiagonal workspace for NSR to be even
          .    and greater than or equal to two as required. ====
','line_number':16296,'multiline':True]['text':' Computing MIN ','line_number':16305,'multiline':True]['text':' Computing MAX ','line_number':16309,'multiline':True]['text':'
          ==== Estimate optimal workspace ====

          ==== Workspace query call to ZLAQR2 ====
','line_number':16313,'multiline':True]['text':'
          ==== Optimal workspace = MAX(ZLAQR5, ZLAQR2) ====

   Computing MAX
','line_number':16325,'multiline':True]['text':'        ==== Quick return in case of workspace query. ==== ','line_number':16333,'multiline':True]['text':'        ==== ZLAHQR/ZLAQR0 crossover point ==== ','line_number':16342,'multiline':True]['text':'        ==== Nibble crossover point ==== ','line_number':16348,'multiline':True]['text':'
          ==== Accumulate reflections during ttswp?  Use block
          .    2-by-2 structure during matrix-matrix multiply? ====
','line_number':16354,'multiline':True]['text':'
          ==== NWMAX = the largest possible deflation window for
          .    which there is sufficient workspace. ====

   Computing MIN
','line_number':16364,'multiline':True]['text':'
          ==== NSMAX = the Largest number of simultaneous shifts
          .    for which there is sufficient workspace. ====

   Computing MIN
','line_number':16374,'multiline':True]['text':'        ==== NDFL: an iteration count restarted at deflation. ==== ','line_number':16384,'multiline':True]['text':'
          ==== ITMAX = iteration limit ====

   Computing MAX
','line_number':16388,'multiline':True]['text':'        ==== Last row and column in the active block ==== ','line_number':16396,'multiline':True]['text':'        ==== Main Loop ==== ','line_number':16400,'multiline':True]['text':'           ==== Done when KBOT falls below ILO ==== ','line_number':16405,'multiline':True]['text':'           ==== Locate active block ==== ','line_number':16411,'multiline':True]['text':' L10: ','line_number':16419,'multiline':True]['text':'
             ==== Select deflation window size:
             .    Typical Case:
             .      If possible and advisable, nibble the entire
             .      active block.  If not, use size MIN(NWR,NWMAX)
             .      or MIN(NWR+1,NWMAX) depending upon which has
             .      the smaller corresponding subdiagonal entry
             .      (a heuristic).
             .
             .    Exceptional Case:
             .      If there have been no deflations in KEXNW or
             .      more iterations, then vary the deflation window
             .      size.   At first, because, larger windows are,
             .      in general, more powerful than smaller ones,
             .      rapidly increase the window to the maximum possible.
             .      Then, gradually reduce the window size. ====
','line_number':16425,'multiline':True]['text':' Computing MIN ','line_number':16448,'multiline':True]['text':'
             ==== Aggressive early deflation:
             .    split workspace under the subdiagonal into
             .      - an nw-by-nw work array V in the lower
             .        left-hand-corner,
             .      - an NW-by-at-least-NW-but-more-is-better
             .        (NW-by-NHO) horizontal work array along
             .        the bottom edge,
             .      - an at-least-NW-but-more-is-better (NHV-by-NW)
             .        vertical work array along the left-hand-edge.
             .        ====
','line_number':16478,'multiline':True]['text':'           ==== Aggressive early deflation ==== ','line_number':16497,'multiline':True]['text':'           ==== Adjust KBOT accounting for new deflations. ==== ','line_number':16504,'multiline':True]['text':'           ==== KS points to the shifts. ==== ','line_number':16508,'multiline':True]['text':'
             ==== Skip an expensive QR sweep if there is a (partly
             .    heuristic) reason to expect that many eigenvalues
             .    will deflate without it.  Here, the QR sweep is
             .    skipped if many eigenvalues have just been deflated
             .    or if the remaining active block is small.
','line_number':16512,'multiline':True]['text':'
                ==== NS = nominal number of simultaneous shifts.
                .    This may be lowered (slightly) if ZLAQR2
                .    did not provide that many shifts. ====

   Computing MIN
   Computing MAX
','line_number':16523,'multiline':True]['text':'
                ==== If there have been no deflations
                .    in a multiple of KEXSH iterations,
                .    then try exceptional shifts.
                .    Otherwise use shifts provided by
                .    ZLAQR2 above or from the eigenvalues
                .    of a trailing principal submatrix. ====
','line_number':16536,'multiline':True]['text':' L30: ','line_number':16560,'multiline':True]['text':'
                   ==== Got NS/2 or fewer shifts? Use ZLAHQR
                   .    on a trailing principal submatrix to
                   .    get more. (Since NS.LE.NSMAX.LE.(N+6)/9,
                   .    there is enough space below the subdiagonal
                   .    to fit an NS-by-NS scratch array.) ====
','line_number':16564,'multiline':True]['text':'
                      ==== In case of a rare QR failure use
                      .    eigenvalues of the trailing 2-by-2
                      .    principal submatrix.  Scale to avoid
                      .    overflows, underflows and subnormals.
                      .    (The scale factor S can not be zero,
                      .    because H(KBOT,KBOT-1) is nonzero.) ====
','line_number':16582,'multiline':True]['text':'                    ==== Sort the shifts (Helps a little) ==== ','line_number':16655,'multiline':True]['text':' L40: ','line_number':16682,'multiline':True]['text':' L50: ','line_number':16684,'multiline':True]['text':'
                ==== If there are only two shifts, then use
                .    only one.  ====
','line_number':16691,'multiline':True]['text':'
                ==== Use up to NS of the smallest magnatiude
                .    shifts.  If there aren't NS shifts available,
                .    then use them all, possibly dropping one to
                .    make the number of shifts even. ====

   Computing MIN
','line_number':16720,'multiline':True]['text':'
                ==== Small-bulge multi-shift QR sweep:
                .    split workspace under the subdiagonal into
                .    - a KDU-by-KDU work array U in the lower
                .      left-hand-corner,
                .    - a KDU-by-at-least-KDU-but-more-is-better
                .      (KDU-by-NHo) horizontal work array WH along
                .      the bottom edge,
                .    - and an at-least-KDU-but-more-is-better-by-KDU
                .      (NVE-by-KDU) vertical work WV arrow along
                .      the left-hand-edge. ====
','line_number':16733,'multiline':True]['text':'              ==== Small-bulge multi-shift QR sweep ==== ','line_number':16753,'multiline':True]['text':'           ==== Note progress (or the lack of it). ==== ','line_number':16762,'multiline':True]['text':'
             ==== End of main loop ====
   L70:
','line_number':16770,'multiline':True]['text':'
          ==== Iteration limit exceeded.  Set INFO to show where
          .    the problem occurred and exit. ====
','line_number':16776,'multiline':True]['text':'     ==== Return the optimal value of LWORK. ==== ','line_number':16786,'multiline':True]['text':'     ==== End of ZLAQR4 ==== ','line_number':16792,'multiline':True]['text':' zlaqr4_ ','line_number':16795,'multiline':True]['text':' Subroutine ','line_number':16797,'multiline':True]['text':' System generated locals ','line_number':16805,'multiline':True]['text':' Local variables ','line_number':16812,'multiline':True]['text':' Subroutine ','line_number':16828,'multiline':True]['text':' Subroutine ','line_number':16839,'multiline':True]['text':' Subroutine ','line_number':16842,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
       Univ. of Tennessee, Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..
       November 2006


       This auxiliary subroutine called by ZLAQR0 performs a
       single small-bulge multi-shift QR sweep.

        WANTT  (input) logical scalar
               WANTT = .true. if the triangular Schur factor
               is being computed.  WANTT is set to .false. otherwise.

        WANTZ  (input) logical scalar
               WANTZ = .true. if the unitary Schur factor is being
               computed.  WANTZ is set to .false. otherwise.

        KACC22 (input) integer with value 0, 1, or 2.
               Specifies the computation mode of far-from-diagonal
               orthogonal updates.
          = 0: ZLAQR5 does not accumulate reflections and does not
               use matrix-matrix multiply to update far-from-diagonal
               matrix entries.
          = 1: ZLAQR5 accumulates reflections and uses matrix-matrix
               multiply to update the far-from-diagonal matrix entries.
          = 2: ZLAQR5 accumulates reflections, uses matrix-matrix
               multiply to update the far-from-diagonal matrix entries,
               and takes advantage of 2-by-2 block structure during
               matrix multiplies.

        N      (input) integer scalar
               N is the order of the Hessenberg matrix H upon which this
               subroutine operates.

        KTOP   (input) integer scalar
        KBOT   (input) integer scalar
               These are the first and last rows and columns of an
               isolated diagonal block upon which the QR sweep is to be
               applied. It is assumed without a check that
                         either KTOP = 1  or   H(KTOP,KTOP-1) = 0
               and
                         either KBOT = N  or   H(KBOT+1,KBOT) = 0.

        NSHFTS (input) integer scalar
               NSHFTS gives the number of simultaneous shifts.  NSHFTS
               must be positive and even.

        S      (input/output) COMPLEX*16 array of size (NSHFTS)
               S contains the shifts of origin that define the multi-
               shift QR sweep.  On output S may be reordered.

        H      (input/output) COMPLEX*16 array of size (LDH,N)
               On input H contains a Hessenberg matrix.  On output a
               multi-shift QR sweep with shifts SR(J)+i*SI(J) is applied
               to the isolated diagonal block in rows and columns KTOP
               through KBOT.

        LDH    (input) integer scalar
               LDH is the leading dimension of H just as declared in the
               calling procedure.  LDH.GE.MAX(1,N).

        ILOZ   (input) INTEGER
        IHIZ   (input) INTEGER
               Specify the rows of Z to which transformations must be
               applied if WANTZ is .TRUE.. 1 .LE. ILOZ .LE. IHIZ .LE. N

        Z      (input/output) COMPLEX*16 array of size (LDZ,IHI)
               If WANTZ = .TRUE., then the QR Sweep unitary
               similarity transformation is accumulated into
               Z(ILOZ:IHIZ,ILO:IHI) from the right.
               If WANTZ = .FALSE., then Z is unreferenced.

        LDZ    (input) integer scalar
               LDA is the leading dimension of Z just as declared in
               the calling procedure. LDZ.GE.N.

        V      (workspace) COMPLEX*16 array of size (LDV,NSHFTS/2)

        LDV    (input) integer scalar
               LDV is the leading dimension of V as declared in the
               calling procedure.  LDV.GE.3.

        U      (workspace) COMPLEX*16 array of size
               (LDU,3*NSHFTS-3)

        LDU    (input) integer scalar
               LDU is the leading dimension of U just as declared in the
               in the calling subroutine.  LDU.GE.3*NSHFTS-3.

        NH     (input) integer scalar
               NH is the number of columns in array WH available for
               workspace. NH.GE.1.

        WH     (workspace) COMPLEX*16 array of size (LDWH,NH)

        LDWH   (input) integer scalar
               Leading dimension of WH just as declared in the
               calling procedure.  LDWH.GE.3*NSHFTS-3.

        NV     (input) integer scalar
               NV is the number of rows in WV agailable for workspace.
               NV.GE.1.

        WV     (workspace) COMPLEX*16 array of size
               (LDWV,3*NSHFTS-3)

        LDWV   (input) integer scalar
               LDWV is the leading dimension of WV as declared in the
               in the calling subroutine.  LDWV.GE.NV.

       ================================================================
       Based on contributions by
          Karen Braman and Ralph Byers, Department of Mathematics,
          University of Kansas, USA

       ================================================================
       Reference:

       K. Braman, R. Byers and R. Mathias, The Multi-Shift QR
       Algorithm Part I: Maintaining Well Focused Shifts, and
       Level 3 Performance, SIAM Journal of Matrix Analysis,
       volume 23, pages 929--947, 2002.

       ================================================================


       ==== If there are no shifts, then there is nothing to do. ====
','line_number':16850,'multiline':True]['text':' Parameter adjustments ','line_number':16979,'multiline':True]['text':' Function Body ','line_number':17000,'multiline':True]['text':'
       ==== If the active block is empty or 1-by-1, then there
       .    is nothing to do. ====
','line_number':17005,'multiline':True]['text':'
       ==== NSHFTS is supposed to be even, but if it is odd,
       .    then simply reduce it by one.  ====
','line_number':17014,'multiline':True]['text':'     ==== Machine constants for deflation ==== ','line_number':17021,'multiline':True]['text':'
       ==== Use accumulated reflections to update far-from-diagonal
       .    entries ? ====
','line_number':17029,'multiline':True]['text':'     ==== If so, exploit the 2-by-2 block structure? ==== ','line_number':17036,'multiline':True]['text':'     ==== clear trash ==== ','line_number':17040,'multiline':True]['text':'     ==== NBMPS = number of 2-shift bulges in the chain ==== ','line_number':17047,'multiline':True]['text':'     ==== KDU = width of slab ==== ','line_number':17051,'multiline':True]['text':'     ==== Create and chase chains of NBMPS bulges ==== ','line_number':17055,'multiline':True]['text':'
          ==== Near-the-diagonal bulge chase.  The following loop
          .    performs the near-the-diagonal part of a small bulge
          .    multi-shift QR sweep.  Each 6*NBMPS-2 column diagonal
          .    chunk extends from column INCOL to column NDCOL
          .    (including both column INCOL and column NDCOL). The
          .    following loop chases a 3*NBMPS column long chain of
          .    NBMPS bulges 3*NBMPS-2 columns to the right.  (INCOL
          .    may be less than KTOP and and NDCOL may be greater than
          .    KBOT indicating phantom columns from which to chase
          .    bulges before they are actually introduced or to which
          .    to chase bulges beyond column KBOT.)  ====

   Computing MIN
','line_number':17066,'multiline':True]['text':'
             ==== Bulges number MTOP to MBOT are active double implicit
             .    shift bulges.  There may or may not also be small
             .    2-by-2 bulge, if there is room.  The inactive bulges
             .    (if any) must wait until the active bulges have moved
             .    down the diagonal to make room.  The phantom matrix
             .    paradigm described above helps keep track.  ====

   Computing MAX
','line_number':17085,'multiline':True]['text':' Computing MIN ','line_number':17097,'multiline':True]['text':'
             ==== Generate reflections to chase the chain right
             .    one column.  (The minimum value of K is KTOP-1.) ====
','line_number':17103,'multiline':True]['text':'
                   ==== A Bulge may collapse because of vigilant
                   .    deflation or destructive underflow.  In the
                   .    underflow case, try the two-small-subdiagonals
                   .    trick to try to reinflate the bulge.  ====
','line_number':17130,'multiline':True]['text':'                    ==== Typical case: not collapsed (yet). ==== ','line_number':17144,'multiline':True]['text':'
                      ==== Atypical case: collapsed.  Attempt to
                      .    reintroduce ignoring H(K+1,K) and H(K+2,K).
                      .    If the fill resulting from the new
                      .    reflector is too large, then abandon it.
                      .    Otherwise, use the new one. ====
','line_number':17154,'multiline':True]['text':'
                         ==== Starting a new bulge here would
                         .    create non-negligible fill.  Use
                         .    the old one with trepidation. ====
','line_number':17204,'multiline':True]['text':'
                         ==== Stating a new bulge here would
                         .    create only negligible fill.
                         .    Replace the old reflector with
                         .    the new one. ====
','line_number':17218,'multiline':True]['text':' L10: ','line_number':17243,'multiline':True]['text':'           ==== Generate a 2-by-2 reflection, if needed. ==== ','line_number':17246,'multiline':True]['text':'           ==== Multiply H by reflections from the left ==== ','line_number':17273,'multiline':True]['text':' Computing MIN ','line_number':17284,'multiline':True]['text':' L20: ','line_number':17331,'multiline':True]['text':' L30: ','line_number':17333,'multiline':True]['text':' Computing MAX ','line_number':17337,'multiline':True]['text':' L40: ','line_number':17367,'multiline':True]['text':'
             ==== Multiply H by reflections from the right.
             .    Delay filling in the last row until the
             .    vigilant deflation check is complete. ====
','line_number':17371,'multiline':True]['text':' Computing MIN ','line_number':17389,'multiline':True]['text':' L50: ','line_number':17435,'multiline':True]['text':'
                      ==== Accumulate U. (If necessary, update Z later
                      .    with an efficient matrix-matrix
                      .    multiply.) ====
','line_number':17440,'multiline':True]['text':' Computing MAX ','line_number':17447,'multiline':True]['text':' L60: ','line_number':17494,'multiline':True]['text':'
                      ==== U is not accumulated, so update Z
                      .    now by multiplying by reflections
                      .    from the right. ====
','line_number':17498,'multiline':True]['text':' L70: ','line_number':17549,'multiline':True]['text':' L80: ','line_number':17553,'multiline':True]['text':'           ==== Special case: 2-by-2 reflection (if needed) ==== ','line_number':17556,'multiline':True]['text':' Computing MIN ','line_number':17561,'multiline':True]['text':' L90: ','line_number':17590,'multiline':True]['text':' Computing MAX ','line_number':17595,'multiline':True]['text':' L100: ','line_number':17626,'multiline':True]['text':' L110: ','line_number':17658,'multiline':True]['text':'           ==== Vigilant deflation check ==== ','line_number':17663,'multiline':True]['text':' Computing MIN ','line_number':17678,'multiline':True]['text':'
                ==== The following convergence test requires that
                .    the tradition small-compared-to-nearby-diagonals
                .    criterion and the Ahues & Tisseur (LAWN 122, 1997)
                .    criteria both be satisfied.  The latter improves
                .    accuracy in some examples. Falling back on an
                .    alternate convergence criterion when TST1 or TST2
                .    is zero (as done here) is traditional but probably
                .    unnecessary. ====
','line_number':17682,'multiline':True]['text':' Computing MAX ','line_number':17740,'multiline':True]['text':' Computing MAX ','line_number':17745,'multiline':True]['text':' Computing MIN ','line_number':17754,'multiline':True]['text':' Computing MAX ','line_number':17768,'multiline':True]['text':' Computing MIN ','line_number':17780,'multiline':True]['text':' Computing MAX ','line_number':17790,'multiline':True]['text':' L120: ','line_number':17799,'multiline':True]['text':'
             ==== Fill in the last row of each bulge. ====

   Computing MIN
','line_number':17802,'multiline':True]['text':' L130: ','line_number':17837,'multiline':True]['text':'
             ==== End of near-the-diagonal bulge chase. ====

   L140:
','line_number':17840,'multiline':True]['text':'
          ==== Use U (if accumulated) to update far-from-diagonal
          .    entries in H.  If required, use U to update Z as
          .    well. ====
','line_number':17847,'multiline':True]['text':'
                ==== Updates not exploiting the 2-by-2 block
                .    structure of U.  K1 and NU keep track of
                .    the location and size of U in the special
                .    cases of introducing bulges and chasing
                .    bulges off the bottom.  In these special
                .    cases and in case the number of shifts
                .    is NS = 2, there is no 2-by-2 block
                .    structure to exploit.  ====

   Computing MAX
','line_number':17863,'multiline':True]['text':' Computing MAX ','line_number':17877,'multiline':True]['text':'              ==== Horizontal Multiply ==== ','line_number':17881,'multiline':True]['text':' Computing MIN ','line_number':17887,'multiline':True]['text':' L150: ','line_number':17895,'multiline':True]['text':'              ==== Vertical multiply ==== ','line_number':17898,'multiline':True]['text':' Computing MIN ','line_number':17904,'multiline':True]['text':' L160: ','line_number':17912,'multiline':True]['text':'              ==== Z multiply (also vertical) ==== ','line_number':17915,'multiline':True]['text':' Computing MIN ','line_number':17922,'multiline':True]['text':' L170: ','line_number':17931,'multiline':True]['text':'
                ==== Updates exploiting U's 2-by-2 block structure.
                .    (I2, I4, J2, J4 are the last rows and columns
                .    of the blocks.) ====
','line_number':17936,'multiline':True]['text':'
                ==== KZS and KNZ deal with the band of zeros
                .    along the diagonal of one of the triangular
                .    blocks. ====
','line_number':17947,'multiline':True]['text':'              ==== Horizontal multiply ==== ','line_number':17956,'multiline':True]['text':' Computing MIN ','line_number':17962,'multiline':True]['text':'
                   ==== Copy bottom of H to top+KZS of scratch ====
                    (The first KZS rows get multiplied by zero.) ====
','line_number':17966,'multiline':True]['text':'                 ==== Multiply by U21' ==== ','line_number':17974,'multiline':True]['text':'                 ==== Multiply top of H by U11' ==== ','line_number':17982,'multiline':True]['text':'                 ==== Copy top of H to bottom of WH ==== ','line_number':17988,'multiline':True]['text':'                 ==== Multiply by U21' ==== ','line_number':17993,'multiline':True]['text':'                 ==== Multiply by U22 ==== ','line_number':17998,'multiline':True]['text':'                 ==== Copy it back ==== ','line_number':18007,'multiline':True]['text':' L180: ','line_number':18011,'multiline':True]['text':'              ==== Vertical multiply ==== ','line_number':18014,'multiline':True]['text':' Computing MIN ','line_number':18020,'multiline':True]['text':'
                   ==== Copy right of H to scratch (the first KZS
                   .    columns get multiplied by zero) ====
','line_number':18024,'multiline':True]['text':'                 ==== Multiply by U21 ==== ','line_number':18032,'multiline':True]['text':'                 ==== Multiply by U11 ==== ','line_number':18040,'multiline':True]['text':'                 ==== Copy left of H to right of scratch ==== ','line_number':18047,'multiline':True]['text':'                 ==== Multiply by U21 ==== ','line_number':18052,'multiline':True]['text':'                 ==== Multiply by U22 ==== ','line_number':18059,'multiline':True]['text':'                 ==== Copy it back ==== ','line_number':18068,'multiline':True]['text':' L190: ','line_number':18072,'multiline':True]['text':'              ==== Multiply Z (also vertical) ==== ','line_number':18075,'multiline':True]['text':' Computing MIN ','line_number':18082,'multiline':True]['text':'
                      ==== Copy right of Z to left of scratch (first
                      .     KZS columns get multiplied by zero) ====
','line_number':18086,'multiline':True]['text':'                    ==== Multiply by U12 ==== ','line_number':18095,'multiline':True]['text':'                    ==== Multiply by U11 ==== ','line_number':18103,'multiline':True]['text':'                    ==== Copy left of Z to right of scratch ==== ','line_number':18109,'multiline':True]['text':'                    ==== Multiply by U21 ==== ','line_number':18115,'multiline':True]['text':'                    ==== Multiply by U22 ==== ','line_number':18122,'multiline':True]['text':'                    ==== Copy the result back to Z ==== ','line_number':18131,'multiline':True]['text':' L200: ','line_number':18135,'multiline':True]['text':' L210: ','line_number':18140,'multiline':True]['text':'     ==== End of ZLAQR5 ==== ','line_number':18143,'multiline':True]['text':' zlaqr5_ ','line_number':18146,'multiline':True]['text':' Subroutine ','line_number':18148,'multiline':True]['text':' System generated locals ','line_number':18152,'multiline':True]['text':' Local variables ','line_number':18158,'multiline':True]['text':' Subroutine ','line_number':18160,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLARCM performs a very simple matrix-matrix multiplication:
             C := A * B,
    where A is M by M and real; B is M by N and complex;
    C is M by N and complex.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix A and of the matrix C.
            M >= 0.

    N       (input) INTEGER
            The number of columns and rows of the matrix B and
            the number of columns of the matrix C.
            N >= 0.

    A       (input) DOUBLE PRECISION array, dimension (LDA, M)
            A contains the M by M matrix A.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >=max(1,M).

    B       (input) DOUBLE PRECISION array, dimension (LDB, N)
            B contains the M by N matrix B.

    LDB     (input) INTEGER
            The leading dimension of the array B. LDB >=max(1,M).

    C       (input) COMPLEX*16 array, dimension (LDC, N)
            C contains the M by N matrix C.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >=max(1,M).

    RWORK   (workspace) DOUBLE PRECISION array, dimension (2*M*N)

    =====================================================================


       Quick return if possible.
','line_number':18165,'multiline':True]['text':' Parameter adjustments ','line_number':18218,'multiline':True]['text':' Function Body ','line_number':18230,'multiline':True]['text':' L10: ','line_number':18241,'multiline':True]['text':' L20: ','line_number':18243,'multiline':True]['text':' L30: ','line_number':18256,'multiline':True]['text':' L40: ','line_number':18258,'multiline':True]['text':' L50: ','line_number':18266,'multiline':True]['text':' L60: ','line_number':18268,'multiline':True]['text':' L70: ','line_number':18282,'multiline':True]['text':' L80: ','line_number':18284,'multiline':True]['text':'     End of ZLARCM ','line_number':18289,'multiline':True]['text':' zlarcm_ ','line_number':18291,'multiline':True]['text':' Subroutine ','line_number':18293,'multiline':True]['text':' System generated locals ','line_number':18297,'multiline':True]['text':' Local variables ','line_number':18301,'multiline':True]['text':' Subroutine ','line_number':18306,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLARF applies a complex elementary reflector H to a complex M-by-N
    matrix C, from either the left or the right. H is represented in the
    form

          H = I - tau * v * v'

    where tau is a complex scalar and v is a complex vector.

    If tau = 0, then H is taken to be the unit matrix.

    To apply H' (the conjugate transpose of H), supply conjg(tau) instead
    tau.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': form  H * C
            = 'R': form  C * H

    M       (input) INTEGER
            The number of rows of the matrix C.

    N       (input) INTEGER
            The number of columns of the matrix C.

    V       (input) COMPLEX*16 array, dimension
                       (1 + (M-1)*abs(INCV)) if SIDE = 'L'
                    or (1 + (N-1)*abs(INCV)) if SIDE = 'R'
            The vector v in the representation of H. V is not used if
            TAU = 0.

    INCV    (input) INTEGER
            The increment between elements of v. INCV <> 0.

    TAU     (input) COMPLEX*16
            The value tau in the representation of H.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by the matrix H * C if SIDE = 'L',
            or C * H if SIDE = 'R'.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX*16 array, dimension
                           (N) if SIDE = 'L'
                        or (M) if SIDE = 'R'

    =====================================================================
','line_number':18316,'multiline':True]['text':' Parameter adjustments ','line_number':18380,'multiline':True]['text':' Function Body ','line_number':18387,'multiline':True]['text':'
       Set up variables for scanning V.  LASTV begins pointing to the end
       of V.
','line_number':18392,'multiline':True]['text':'     Look for the last non-zero row in V. ','line_number':18406,'multiline':True]['text':' while(complicated condition) ','line_number':18407,'multiline':True]['text':'     Scan for the last non-zero column in C(1:lastv,:). ','line_number':18415,'multiline':True]['text':'     Scan for the last non-zero row in C(:,1:lastv). ','line_number':18418,'multiline':True]['text':'
       Note that lastc.eq.0 renders the BLAS operations null; no special
       case is needed at this level.
','line_number':18422,'multiline':True]['text':'        Form  H * C ','line_number':18428,'multiline':True]['text':'           w(1:lastc,1) := C(1:lastv,1:lastc)' * v(1:lastv,1) ','line_number':18432,'multiline':True]['text':'           C(1:lastv,1:lastc) := C(...) - v(1:lastv,1) * w(1:lastc,1)' ','line_number':18437,'multiline':True]['text':'        Form  C * H ','line_number':18445,'multiline':True]['text':'           w(1:lastc,1) := C(1:lastc,1:lastv) * v(1:lastv,1) ','line_number':18449,'multiline':True]['text':'           C(1:lastc,1:lastv) := C(...) - w(1:lastc,1) * v(1:lastv,1)' ','line_number':18454,'multiline':True]['text':'     End of ZLARF ','line_number':18463,'multiline':True]['text':' zlarf_ ','line_number':18465,'multiline':True]['text':' Subroutine ','line_number':18467,'multiline':True]['text':' System generated locals ','line_number':18472,'multiline':True]['text':' Local variables ','line_number':18477,'multiline':True]['text':' Subroutine ','line_number':18481,'multiline':True]['text':' Subroutine ','line_number':18486,'multiline':True]['text':' Subroutine ','line_number':18491,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLARFB applies a complex block reflector H or its transpose H' to a
    complex M-by-N matrix C, from either the left or the right.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply H or H' from the Left
            = 'R': apply H or H' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply H (No transpose)
            = 'C': apply H' (Conjugate transpose)

    DIRECT  (input) CHARACTER*1
            Indicates how H is formed from a product of elementary
            reflectors
            = 'F': H = H(1) H(2) . . . H(k) (Forward)
            = 'B': H = H(k) . . . H(2) H(1) (Backward)

    STOREV  (input) CHARACTER*1
            Indicates how the vectors which define the elementary
            reflectors are stored:
            = 'C': Columnwise
            = 'R': Rowwise

    M       (input) INTEGER
            The number of rows of the matrix C.

    N       (input) INTEGER
            The number of columns of the matrix C.

    K       (input) INTEGER
            The order of the matrix T (= the number of elementary
            reflectors whose product defines the block reflector).

    V       (input) COMPLEX*16 array, dimension
                                  (LDV,K) if STOREV = 'C'
                                  (LDV,M) if STOREV = 'R' and SIDE = 'L'
                                  (LDV,N) if STOREV = 'R' and SIDE = 'R'
            The matrix V. See further details.

    LDV     (input) INTEGER
            The leading dimension of the array V.
            If STOREV = 'C' and SIDE = 'L', LDV >= max(1,M);
            if STOREV = 'C' and SIDE = 'R', LDV >= max(1,N);
            if STOREV = 'R', LDV >= K.

    T       (input) COMPLEX*16 array, dimension (LDT,K)
            The triangular K-by-K matrix T in the representation of the
            block reflector.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= K.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by H*C or H'*C or C*H or C*H'.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX*16 array, dimension (LDWORK,K)

    LDWORK  (input) INTEGER
            The leading dimension of the array WORK.
            If SIDE = 'L', LDWORK >= max(1,N);
            if SIDE = 'R', LDWORK >= max(1,M).

    =====================================================================


       Quick return if possible
','line_number':18497,'multiline':True]['text':' Parameter adjustments ','line_number':18582,'multiline':True]['text':' Function Body ','line_number':18596,'multiline':True]['text':'
             Let  V =  ( V1 )    (first K rows)
                       ( V2 )
             where  V1  is unit lower triangular.
','line_number':18611,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':18619,'multiline':True]['text':'
                W := C' * V  =  (C1'*V1 + C2'*V2)  (stored in WORK)

                W := C1'
','line_number':18629,'multiline':True]['text':' L10: ','line_number':18640,'multiline':True]['text':'              W := W * V1 ','line_number':18643,'multiline':True]['text':'                 W := W + C2'*V2 ','line_number':18649,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':18658,'multiline':True]['text':'              C := C - V * W' ','line_number':18663,'multiline':True]['text':'                 C2 := C2 - V2 * W' ','line_number':18667,'multiline':True]['text':'              W := W * V1' ','line_number':18677,'multiline':True]['text':'              C1 := C1 - W' ','line_number':18683,'multiline':True]['text':' L20: ','line_number':18695,'multiline':True]['text':' L30: ','line_number':18697,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':18702,'multiline':True]['text':'
                W := C * V  =  (C1*V1 + C2*V2)  (stored in WORK)

                W := C1
','line_number':18711,'multiline':True]['text':' L40: ','line_number':18721,'multiline':True]['text':'              W := W * V1 ','line_number':18724,'multiline':True]['text':'                 W := W + C2 * V2 ','line_number':18730,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':18739,'multiline':True]['text':'              C := C - W * V' ','line_number':18744,'multiline':True]['text':'                 C2 := C2 - W * V2' ','line_number':18748,'multiline':True]['text':'              W := W * V1' ','line_number':18758,'multiline':True]['text':'              C1 := C1 - W ','line_number':18764,'multiline':True]['text':' L50: ','line_number':18776,'multiline':True]['text':' L60: ','line_number':18778,'multiline':True]['text':'
             Let  V =  ( V1 )
                       ( V2 )    (last K rows)
             where  V2  is unit upper triangular.
','line_number':18784,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':18792,'multiline':True]['text':'
                W := C' * V  =  (C1'*V1 + C2'*V2)  (stored in WORK)

                W := C2'
','line_number':18802,'multiline':True]['text':' L70: ','line_number':18813,'multiline':True]['text':'              W := W * V2 ','line_number':18816,'multiline':True]['text':'                 W := W + C1'*V1 ','line_number':18823,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':18831,'multiline':True]['text':'              C := C - V * W' ','line_number':18836,'multiline':True]['text':'                 C1 := C1 - V1 * W' ','line_number':18840,'multiline':True]['text':'              W := W * V2' ','line_number':18849,'multiline':True]['text':'              C2 := C2 - W' ','line_number':18855,'multiline':True]['text':' L80: ','line_number':18867,'multiline':True]['text':' L90: ','line_number':18869,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':18874,'multiline':True]['text':'
                W := C * V  =  (C1*V1 + C2*V2)  (stored in WORK)

                W := C2
','line_number':18883,'multiline':True]['text':' L100: ','line_number':18893,'multiline':True]['text':'              W := W * V2 ','line_number':18896,'multiline':True]['text':'                 W := W + C1 * V1 ','line_number':18903,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':18911,'multiline':True]['text':'              C := C - W * V' ','line_number':18916,'multiline':True]['text':'                 C1 := C1 - W * V1' ','line_number':18920,'multiline':True]['text':'              W := W * V2' ','line_number':18929,'multiline':True]['text':'              C2 := C2 - W ','line_number':18935,'multiline':True]['text':' L110: ','line_number':18947,'multiline':True]['text':' L120: ','line_number':18949,'multiline':True]['text':'
             Let  V =  ( V1  V2 )    (V1: first K columns)
             where  V1  is unit upper triangular.
','line_number':18958,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':18965,'multiline':True]['text':'
                W := C' * V'  =  (C1'*V1' + C2'*V2') (stored in WORK)

                W := C1'
','line_number':18975,'multiline':True]['text':' L130: ','line_number':18986,'multiline':True]['text':'              W := W * V1' ','line_number':18989,'multiline':True]['text':'                 W := W + C2'*V2' ','line_number':18996,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':19006,'multiline':True]['text':'              C := C - V' * W' ','line_number':19011,'multiline':True]['text':'                 C2 := C2 - V2' * W' ','line_number':19015,'multiline':True]['text':'              W := W * V1 ','line_number':19025,'multiline':True]['text':'              C1 := C1 - W' ','line_number':19030,'multiline':True]['text':' L140: ','line_number':19042,'multiline':True]['text':' L150: ','line_number':19044,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':19049,'multiline':True]['text':'
                W := C * V'  =  (C1*V1' + C2*V2')  (stored in WORK)

                W := C1
','line_number':19058,'multiline':True]['text':' L160: ','line_number':19068,'multiline':True]['text':'              W := W * V1' ','line_number':19071,'multiline':True]['text':'                 W := W + C2 * V2' ','line_number':19078,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':19087,'multiline':True]['text':'              C := C - W * V ','line_number':19092,'multiline':True]['text':'                 C2 := C2 - W * V2 ','line_number':19096,'multiline':True]['text':'              W := W * V1 ','line_number':19106,'multiline':True]['text':'              C1 := C1 - W ','line_number':19111,'multiline':True]['text':' L170: ','line_number':19123,'multiline':True]['text':' L180: ','line_number':19125,'multiline':True]['text':'
             Let  V =  ( V1  V2 )    (V2: last K columns)
             where  V2  is unit lower triangular.
','line_number':19132,'multiline':True]['text':'
                Form  H * C  or  H' * C  where  C = ( C1 )
                                                    ( C2 )

   Computing MAX
','line_number':19139,'multiline':True]['text':'
                W := C' * V'  =  (C1'*V1' + C2'*V2') (stored in WORK)

                W := C2'
','line_number':19149,'multiline':True]['text':' L190: ','line_number':19160,'multiline':True]['text':'              W := W * V2' ','line_number':19163,'multiline':True]['text':'                 W := W + C1'*V1' ','line_number':19170,'multiline':True]['text':'              W := W * T'  or  W * T ','line_number':19179,'multiline':True]['text':'              C := C - V' * W' ','line_number':19184,'multiline':True]['text':'                 C1 := C1 - V1' * W' ','line_number':19188,'multiline':True]['text':'              W := W * V2 ','line_number':19197,'multiline':True]['text':'              C2 := C2 - W' ','line_number':19203,'multiline':True]['text':' L200: ','line_number':19215,'multiline':True]['text':' L210: ','line_number':19217,'multiline':True]['text':'
                Form  C * H  or  C * H'  where  C = ( C1  C2 )

   Computing MAX
','line_number':19222,'multiline':True]['text':'
                W := C * V'  =  (C1*V1' + C2*V2')  (stored in WORK)

                W := C2
','line_number':19231,'multiline':True]['text':' L220: ','line_number':19241,'multiline':True]['text':'              W := W * V2' ','line_number':19244,'multiline':True]['text':'                 W := W + C1 * V1' ','line_number':19251,'multiline':True]['text':'              W := W * T  or  W * T' ','line_number':19259,'multiline':True]['text':'              C := C - W * V ','line_number':19264,'multiline':True]['text':'                 C1 := C1 - W * V1 ','line_number':19268,'multiline':True]['text':'              W := W * V2 ','line_number':19277,'multiline':True]['text':'              C1 := C1 - W ','line_number':19283,'multiline':True]['text':' L230: ','line_number':19295,'multiline':True]['text':' L240: ','line_number':19297,'multiline':True]['text':'     End of ZLARFB ','line_number':19307,'multiline':True]['text':' zlarfb_ ','line_number':19309,'multiline':True]['text':' Subroutine ','line_number':19311,'multiline':True]['text':' System generated locals ','line_number':19314,'multiline':True]['text':' Local variables ','line_number':19319,'multiline':True]['text':' Subroutine ','line_number':19322,'multiline':True]['text':' Subroutine ','line_number':19328,'multiline':True]['text':' Double Complex ','line_number':19331,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLARFG generates a complex elementary reflector H of order n, such
    that

          H' * ( alpha ) = ( beta ),   H' * H = I.
               (   x   )   (   0  )

    where alpha and beta are scalars, with beta real, and x is an
    (n-1)-element complex vector. H is represented in the form

          H = I - tau * ( 1 ) * ( 1 v' ) ,
                        ( v )

    where tau is a complex scalar and v is a complex (n-1)-element
    vector. Note that H is not hermitian.

    If the elements of x are all zero and alpha is real, then tau = 0
    and H is taken to be the unit matrix.

    Otherwise  1 <= real(tau) <= 2  and  abs(tau-1) <= 1 .

    Arguments
    =========

    N       (input) INTEGER
            The order of the elementary reflector.

    ALPHA   (input/output) COMPLEX*16
            On entry, the value alpha.
            On exit, it is overwritten with the value beta.

    X       (input/output) COMPLEX*16 array, dimension
                           (1+(N-2)*abs(INCX))
            On entry, the vector x.
            On exit, it is overwritten with the vector v.

    INCX    (input) INTEGER
            The increment between elements of X. INCX > 0.

    TAU     (output) COMPLEX*16
            The value tau.

    =====================================================================
','line_number':19335,'multiline':True]['text':' Parameter adjustments ','line_number':19390,'multiline':True]['text':' Function Body ','line_number':19393,'multiline':True]['text':'        H  =  I ','line_number':19406,'multiline':True]['text':'        general case ','line_number':19411,'multiline':True]['text':'           XNORM, BETA may be inaccurate; scale X and recompute them ','line_number':19421,'multiline':True]['text':'           New BETA is at most 1, at least SAFMIN ','line_number':19434,'multiline':True]['text':'        If ALPHA is subnormal, it may lose relative accuracy ','line_number':19453,'multiline':True]['text':' L20: ','line_number':19458,'multiline':True]['text':'     End of ZLARFG ','line_number':19465,'multiline':True]['text':' zlarfg_ ','line_number':19467,'multiline':True]['text':' Subroutine ','line_number':19469,'multiline':True]['text':' System generated locals ','line_number':19473,'multiline':True]['text':' Local variables ','line_number':19477,'multiline':True]['text':' Subroutine ','line_number':19481,'multiline':True]['text':' Subroutine ','line_number':19485,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLARFT forms the triangular factor T of a complex block reflector H
    of order n, which is defined as a product of k elementary reflectors.

    If DIRECT = 'F', H = H(1) H(2) . . . H(k) and T is upper triangular;

    If DIRECT = 'B', H = H(k) . . . H(2) H(1) and T is lower triangular.

    If STOREV = 'C', the vector which defines the elementary reflector
    H(i) is stored in the i-th column of the array V, and

       H  =  I - V * T * V'

    If STOREV = 'R', the vector which defines the elementary reflector
    H(i) is stored in the i-th row of the array V, and

       H  =  I - V' * T * V

    Arguments
    =========

    DIRECT  (input) CHARACTER*1
            Specifies the order in which the elementary reflectors are
            multiplied to form the block reflector:
            = 'F': H = H(1) H(2) . . . H(k) (Forward)
            = 'B': H = H(k) . . . H(2) H(1) (Backward)

    STOREV  (input) CHARACTER*1
            Specifies how the vectors which define the elementary
            reflectors are stored (see also Further Details):
            = 'C': columnwise
            = 'R': rowwise

    N       (input) INTEGER
            The order of the block reflector H. N >= 0.

    K       (input) INTEGER
            The order of the triangular factor T (= the number of
            elementary reflectors). K >= 1.

    V       (input/output) COMPLEX*16 array, dimension
                                 (LDV,K) if STOREV = 'C'
                                 (LDV,N) if STOREV = 'R'
            The matrix V. See further details.

    LDV     (input) INTEGER
            The leading dimension of the array V.
            If STOREV = 'C', LDV >= max(1,N); if STOREV = 'R', LDV >= K.

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i).

    T       (output) COMPLEX*16 array, dimension (LDT,K)
            The k by k triangular factor T of the block reflector.
            If DIRECT = 'F', T is upper triangular; if DIRECT = 'B', T is
            lower triangular. The rest of the array is not used.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= K.

    Further Details
    ===============

    The shape of the matrix V and the storage of the vectors which define
    the H(i) is best illustrated by the following example with n = 5 and
    k = 3. The elements equal to 1 are not stored; the corresponding
    array elements are modified but restored on exit. The rest of the
    array is not used.

    DIRECT = 'F' and STOREV = 'C':         DIRECT = 'F' and STOREV = 'R':

                 V = (  1       )                 V = (  1 v1 v1 v1 v1 )
                     ( v1  1    )                     (     1 v2 v2 v2 )
                     ( v1 v2  1 )                     (        1 v3 v3 )
                     ( v1 v2 v3 )
                     ( v1 v2 v3 )

    DIRECT = 'B' and STOREV = 'C':         DIRECT = 'B' and STOREV = 'R':

                 V = ( v1 v2 v3 )                 V = ( v1 v1  1       )
                     ( v1 v2 v3 )                     ( v2 v2 v2  1    )
                     (  1 v2 v3 )                     ( v3 v3 v3 v3  1 )
                     (     1 v3 )
                     (        1 )

    =====================================================================


       Quick return if possible
','line_number':19489,'multiline':True]['text':' Parameter adjustments ','line_number':19590,'multiline':True]['text':' Function Body ','line_number':19599,'multiline':True]['text':'              H(i)  =  I ','line_number':19612,'multiline':True]['text':' L10: ','line_number':19618,'multiline':True]['text':'              general case ','line_number':19622,'multiline':True]['text':'                 Skip any trailing zeros. ','line_number':19629,'multiline':True]['text':'                 T(1:i-1,i) := - tau(i) * V(i:j,1:i-1)' * V(i:j,i) ','line_number':19640,'multiline':True]['text':'                 Skip any trailing zeros. ','line_number':19650,'multiline':True]['text':'                 T(1:i-1,i) := - tau(i) * V(1:i-1,i:j) * V(i,i:j)' ','line_number':19661,'multiline':True]['text':'              T(1:i-1,i) := T(1:i-1,1:i-1) * T(1:i-1,i) ','line_number':19682,'multiline':True]['text':' L20: ','line_number':19696,'multiline':True]['text':'              H(i)  =  I ','line_number':19704,'multiline':True]['text':' L30: ','line_number':19710,'multiline':True]['text':'              general case ','line_number':19714,'multiline':True]['text':'                    Skip any leading zeros. ','line_number':19722,'multiline':True]['text':'
                      T(i+1:k,i) :=
                              - tau(i) * V(j:n-k+i,i+1:k)' * V(j:n-k+i,i)
','line_number':19733,'multiline':True]['text':'                    Skip any leading zeros. ','line_number':19753,'multiline':True]['text':'
                      T(i+1:k,i) :=
                              - tau(i) * V(i+1:k,j:n-k+i) * V(i,j:n-k+i)'
','line_number':19764,'multiline':True]['text':'                 T(i+1:k,i) := T(i+1:k,i+1:k) * T(i+1:k,i) ','line_number':19785,'multiline':True]['text':' L40: ','line_number':19802,'multiline':True]['text':'     End of ZLARFT ','line_number':19807,'multiline':True]['text':' zlarft_ ','line_number':19809,'multiline':True]['text':' Subroutine ','line_number':19811,'multiline':True]['text':' System generated locals ','line_number':19814,'multiline':True]['text':' Local variables ','line_number':19819,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLARTG generates a plane rotation so that

       [  CS  SN  ]     [ F ]     [ R ]
       [  __      ]  .  [   ]  =  [   ]   where CS**2 + |SN|**2 = 1.
       [ -SN  CS  ]     [ G ]     [ 0 ]

    This is a faster version of the BLAS1 routine ZROTG, except for
    the following differences:
       F and G are unchanged on return.
       If G=0, then CS=1 and SN=0.
       If F=0, then CS=0 and SN is chosen so that R is real.

    Arguments
    =========

    F       (input) COMPLEX*16
            The first component of vector to be rotated.

    G       (input) COMPLEX*16
            The second component of vector to be rotated.

    CS      (output) DOUBLE PRECISION
            The cosine of the rotation.

    SN      (output) COMPLEX*16
            The sine of the rotation.

    R       (output) COMPLEX*16
            The nonzero component of the rotated vector.

    Further Details
    ======= =======

    3-5-96 - Modified with a new algorithm by W. Kahan and J. Demmel

    This version has a few statements commented out for thread safety
    (machine parameters are computed on each entry). 10 feb 03, SJH.

    =====================================================================

       LOGICAL            FIRST
       SAVE               FIRST, SAFMX2, SAFMIN, SAFMN2
       DATA               FIRST / .TRUE. /

       IF( FIRST ) THEN
','line_number':19835,'multiline':True]['text':'
          FIRST = .FALSE.
       END IF
   Computing MAX
   Computing MAX
','line_number':19897,'multiline':True]['text':' Computing MAX ','line_number':19904,'multiline':True]['text':' Computing 2nd power ','line_number':19940,'multiline':True]['text':' Computing 2nd power ','line_number':19942,'multiline':True]['text':' Computing 2nd power ','line_number':19945,'multiline':True]['text':' Computing 2nd power ','line_number':19947,'multiline':True]['text':'        This is a rare case: F is very small. ','line_number':19952,'multiline':True]['text':'           Do complex/real division explicitly with two real divisions ','line_number':19960,'multiline':True]['text':'
          G2 and G2S are accurate
          G2 is at least SAFMIN, and G2S is at least SAFMN2
','line_number':19973,'multiline':True]['text':'
          Error in CS from underflow in F2S is at most
          UNFL / SAFMN2 .lt. sqrt(UNFL*EPS) .lt. EPS
          If MAX(G2,ONE)=G2, then F2 .lt. G2*SAFMIN,
          and so CS .lt. sqrt(SAFMIN)
          If MAX(G2,ONE)=ONE, then F2 .lt. SAFMIN
          and so CS .lt. sqrt(SAFMIN)/SAFMN2 = sqrt(EPS)
          Therefore, CS = F2S/G2S / sqrt( 1 + (F2S/G2S)**2 ) = F2S/G2S
','line_number':19978,'multiline':True]['text':'
          Make sure abs(FF) = 1
          Do complex/real division explicitly with 2 real divisions
   Computing MAX
','line_number':19988,'multiline':True]['text':'
          This is the most common case.
          Neither F2 nor F2/G2 are less than SAFMIN
          F2S cannot overflow, and it is accurate
','line_number':20024,'multiline':True]['text':'        Do the F2S(real)*FS(complex) multiply with two real multiplies ','line_number':20031,'multiline':True]['text':'        Do complex/real division explicitly with two real divisions ','line_number':20038,'multiline':True]['text':' L30: ','line_number':20053,'multiline':True]['text':' L40: ','line_number':20060,'multiline':True]['text':'     End of ZLARTG ','line_number':20067,'multiline':True]['text':' zlartg_ ','line_number':20069,'multiline':True]['text':' Subroutine ','line_number':20071,'multiline':True]['text':' System generated locals ','line_number':20075,'multiline':True]['text':' Local variables ','line_number':20079,'multiline':True]['text':' Subroutine ','line_number':20090,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLASCL multiplies the M by N complex matrix A by the real scalar
    CTO/CFROM.  This is done without over/underflow as long as the final
    result CTO*A(I,J)/CFROM does not over/underflow. TYPE specifies that
    A may be full, upper triangular, lower triangular, upper Hessenberg,
    or banded.

    Arguments
    =========

    TYPE    (input) CHARACTER*1
            TYPE indices the storage type of the input matrix.
            = 'G':  A is a full matrix.
            = 'L':  A is a lower triangular matrix.
            = 'U':  A is an upper triangular matrix.
            = 'H':  A is an upper Hessenberg matrix.
            = 'B':  A is a symmetric band matrix with lower bandwidth KL
                    and upper bandwidth KU and with the only the lower
                    half stored.
            = 'Q':  A is a symmetric band matrix with lower bandwidth KL
                    and upper bandwidth KU and with the only the upper
                    half stored.
            = 'Z':  A is a band matrix with lower bandwidth KL and upper
                    bandwidth KU.

    KL      (input) INTEGER
            The lower bandwidth of A.  Referenced only if TYPE = 'B',
            'Q' or 'Z'.

    KU      (input) INTEGER
            The upper bandwidth of A.  Referenced only if TYPE = 'B',
            'Q' or 'Z'.

    CFROM   (input) DOUBLE PRECISION
    CTO     (input) DOUBLE PRECISION
            The matrix A is multiplied by CTO/CFROM. A(I,J) is computed
            without over/underflow if the final result CTO*A(I,J)/CFROM
            can be represented without over/underflow.  CFROM must be
            nonzero.

    M       (input) INTEGER
            The number of rows of the matrix A.  M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            The matrix to be multiplied by CTO/CFROM.  See TYPE for the
            storage type.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    INFO    (output) INTEGER
            0  - successful exit
            <0 - if INFO = -i, the i-th argument had an illegal value.

    =====================================================================


       Test the input arguments
','line_number':20094,'multiline':True]['text':' Parameter adjustments ','line_number':20166,'multiline':True]['text':' Function Body ','line_number':20171,'multiline':True]['text':' Computing MAX ','line_number':20205,'multiline':True]['text':' if(complicated condition) ','line_number':20209,'multiline':True]['text':' Computing MAX ','line_number':20210,'multiline':True]['text':'     Quick return if possible ','line_number':20228,'multiline':True]['text':'     Get machine parameters ','line_number':20234,'multiline':True]['text':'
          CFROMC is an inf.  Multiply by a correctly signed zero for
          finite CTOC, or a NaN if CTOC is infinite.
','line_number':20245,'multiline':True]['text':'
             CTOC is either 0 or an inf.  In both cases, CTOC itself
             serves as the correct multiplication factor.
','line_number':20255,'multiline':True]['text':'        Full matrix ','line_number':20278,'multiline':True]['text':' L20: ','line_number':20288,'multiline':True]['text':' L30: ','line_number':20290,'multiline':True]['text':'        Lower triangular matrix ','line_number':20295,'multiline':True]['text':' L40: ','line_number':20305,'multiline':True]['text':' L50: ','line_number':20307,'multiline':True]['text':'        Upper triangular matrix ','line_number':20312,'multiline':True]['text':' L60: ','line_number':20322,'multiline':True]['text':' L70: ','line_number':20324,'multiline':True]['text':'        Upper Hessenberg matrix ','line_number':20329,'multiline':True]['text':' Computing MIN ','line_number':20333,'multiline':True]['text':' L80: ','line_number':20341,'multiline':True]['text':' L90: ','line_number':20343,'multiline':True]['text':'        Lower half of a symmetric band matrix ','line_number':20348,'multiline':True]['text':' Computing MIN ','line_number':20354,'multiline':True]['text':' L100: ','line_number':20362,'multiline':True]['text':' L110: ','line_number':20364,'multiline':True]['text':'        Upper half of a symmetric band matrix ','line_number':20369,'multiline':True]['text':' Computing MAX ','line_number':20375,'multiline':True]['text':' L120: ','line_number':20383,'multiline':True]['text':' L130: ','line_number':20385,'multiline':True]['text':'        Band matrix ','line_number':20390,'multiline':True]['text':' Computing MAX ','line_number':20398,'multiline':True]['text':' Computing MIN ','line_number':20400,'multiline':True]['text':' L140: ','line_number':20408,'multiline':True]['text':' L150: ','line_number':20410,'multiline':True]['text':'     End of ZLASCL ','line_number':20421,'multiline':True]['text':' zlascl_ ','line_number':20423,'multiline':True]['text':' Subroutine ','line_number':20425,'multiline':True]['text':' System generated locals ','line_number':20429,'multiline':True]['text':' Local variables ','line_number':20432,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLASET initializes a 2-D array A to BETA on the diagonal and
    ALPHA on the offdiagonals.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies the part of the matrix A to be set.
            = 'U':      Upper triangular part is set. The lower triangle
                        is unchanged.
            = 'L':      Lower triangular part is set. The upper triangle
                        is unchanged.
            Otherwise:  All of the matrix A is set.

    M       (input) INTEGER
            On entry, M specifies the number of rows of A.

    N       (input) INTEGER
            On entry, N specifies the number of columns of A.

    ALPHA   (input) COMPLEX*16
            All the offdiagonal array elements are set to ALPHA.

    BETA    (input) COMPLEX*16
            All the diagonal array elements are set to BETA.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the m by n matrix A.
            On exit, A(i,j) = ALPHA, 1 <= i <= m, 1 <= j <= n, i.ne.j;
                     A(i,i) = BETA , 1 <= i <= min(m,n)

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    =====================================================================
','line_number':20437,'multiline':True]['text':' Parameter adjustments ','line_number':20485,'multiline':True]['text':' Function Body ','line_number':20490,'multiline':True]['text':'
          Set the diagonal to BETA and the strictly upper triangular
          part of the array to ALPHA.
','line_number':20493,'multiline':True]['text':' Computing MIN ','line_number':20500,'multiline':True]['text':' L10: ','line_number':20506,'multiline':True]['text':' L20: ','line_number':20508,'multiline':True]['text':' L30: ','line_number':20514,'multiline':True]['text':'
          Set the diagonal to BETA and the strictly lower triangular
          part of the array to ALPHA.
','line_number':20519,'multiline':True]['text':' L40: ','line_number':20530,'multiline':True]['text':' L50: ','line_number':20532,'multiline':True]['text':' L60: ','line_number':20538,'multiline':True]['text':'
          Set the array to BETA on the diagonal and ALPHA on the
          offdiagonal.
','line_number':20543,'multiline':True]['text':' L70: ','line_number':20554,'multiline':True]['text':' L80: ','line_number':20556,'multiline':True]['text':' L90: ','line_number':20562,'multiline':True]['text':'     End of ZLASET ','line_number':20568,'multiline':True]['text':' zlaset_ ','line_number':20570,'multiline':True]['text':' Subroutine ','line_number':20572,'multiline':True]['text':' System generated locals ','line_number':20576,'multiline':True]['text':' Local variables ','line_number':20580,'multiline':True]['text':' Subroutine ','line_number':20585,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLASR applies a sequence of real plane rotations to a complex matrix
    A, from either the left or the right.

    When SIDE = 'L', the transformation takes the form

       A := P*A

    and when SIDE = 'R', the transformation takes the form

       A := A*P**T

    where P is an orthogonal matrix consisting of a sequence of z plane
    rotations, with z = M when SIDE = 'L' and z = N when SIDE = 'R',
    and P**T is the transpose of P.

    When DIRECT = 'F' (Forward sequence), then

       P = P(z-1) * ... * P(2) * P(1)

    and when DIRECT = 'B' (Backward sequence), then

       P = P(1) * P(2) * ... * P(z-1)

    where P(k) is a plane rotation matrix defined by the 2-by-2 rotation

       R(k) = (  c(k)  s(k) )
            = ( -s(k)  c(k) ).

    When PIVOT = 'V' (Variable pivot), the rotation is performed
    for the plane (k,k+1), i.e., P(k) has the form

       P(k) = (  1                                            )
              (       ...                                     )
              (              1                                )
              (                   c(k)  s(k)                  )
              (                  -s(k)  c(k)                  )
              (                                1              )
              (                                     ...       )
              (                                            1  )

    where R(k) appears as a rank-2 modification to the identity matrix in
    rows and columns k and k+1.

    When PIVOT = 'T' (Top pivot), the rotation is performed for the
    plane (1,k+1), so P(k) has the form

       P(k) = (  c(k)                    s(k)                 )
              (         1                                     )
              (              ...                              )
              (                     1                         )
              ( -s(k)                    c(k)                 )
              (                                 1             )
              (                                      ...      )
              (                                             1 )

    where R(k) appears in rows and columns 1 and k+1.

    Similarly, when PIVOT = 'B' (Bottom pivot), the rotation is
    performed for the plane (k,z), giving P(k) the form

       P(k) = ( 1                                             )
              (      ...                                      )
              (             1                                 )
              (                  c(k)                    s(k) )
              (                         1                     )
              (                              ...              )
              (                                     1         )
              (                 -s(k)                    c(k) )

    where R(k) appears in rows and columns k and z.  The rotations are
    performed without ever forming P(k) explicitly.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            Specifies whether the plane rotation matrix P is applied to
            A on the left or the right.
            = 'L':  Left, compute A := P*A
            = 'R':  Right, compute A:= A*P**T

    PIVOT   (input) CHARACTER*1
            Specifies the plane for which P(k) is a plane rotation
            matrix.
            = 'V':  Variable pivot, the plane (k,k+1)
            = 'T':  Top pivot, the plane (1,k+1)
            = 'B':  Bottom pivot, the plane (k,z)

    DIRECT  (input) CHARACTER*1
            Specifies whether P is a forward or backward sequence of
            plane rotations.
            = 'F':  Forward, P = P(z-1)*...*P(2)*P(1)
            = 'B':  Backward, P = P(1)*P(2)*...*P(z-1)

    M       (input) INTEGER
            The number of rows of the matrix A.  If m <= 1, an immediate
            return is effected.

    N       (input) INTEGER
            The number of columns of the matrix A.  If n <= 1, an
            immediate return is effected.

    C       (input) DOUBLE PRECISION array, dimension
                    (M-1) if SIDE = 'L'
                    (N-1) if SIDE = 'R'
            The cosines c(k) of the plane rotations.

    S       (input) DOUBLE PRECISION array, dimension
                    (M-1) if SIDE = 'L'
                    (N-1) if SIDE = 'R'
            The sines s(k) of the plane rotations.  The 2-by-2 plane
            rotation part of the matrix P(k), R(k), has the form
            R(k) = (  c(k)  s(k) )
                   ( -s(k)  c(k) ).

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            The M-by-N matrix A.  On exit, A is overwritten by P*A if
            SIDE = 'R' or by A*P**T if SIDE = 'L'.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,M).

    =====================================================================


       Test the input parameters
','line_number':20588,'multiline':True]['text':' Parameter adjustments ','line_number':20726,'multiline':True]['text':' Function Body ','line_number':20733,'multiline':True]['text':'     Quick return if possible ','line_number':20755,'multiline':True]['text':'        Form  P * A ','line_number':20762,'multiline':True]['text':' L10: ','line_number':20791,'multiline':True]['text':' L20: ','line_number':20794,'multiline':True]['text':' L30: ','line_number':20821,'multiline':True]['text':' L40: ','line_number':20824,'multiline':True]['text':' L50: ','line_number':20854,'multiline':True]['text':' L60: ','line_number':20857,'multiline':True]['text':' L70: ','line_number':20884,'multiline':True]['text':' L80: ','line_number':20887,'multiline':True]['text':' L90: ','line_number':20917,'multiline':True]['text':' L100: ','line_number':20920,'multiline':True]['text':' L110: ','line_number':20947,'multiline':True]['text':' L120: ','line_number':20950,'multiline':True]['text':'        Form A * P' ','line_number':20956,'multiline':True]['text':' L130: ','line_number':20985,'multiline':True]['text':' L140: ','line_number':20988,'multiline':True]['text':' L150: ','line_number':21015,'multiline':True]['text':' L160: ','line_number':21018,'multiline':True]['text':' L170: ','line_number':21048,'multiline':True]['text':' L180: ','line_number':21051,'multiline':True]['text':' L190: ','line_number':21078,'multiline':True]['text':' L200: ','line_number':21081,'multiline':True]['text':' L210: ','line_number':21111,'multiline':True]['text':' L220: ','line_number':21114,'multiline':True]['text':' L230: ','line_number':21141,'multiline':True]['text':' L240: ','line_number':21144,'multiline':True]['text':'     End of ZLASR ','line_number':21152,'multiline':True]['text':' zlasr_ ','line_number':21154,'multiline':True]['text':' Subroutine ','line_number':21156,'multiline':True]['text':' System generated locals ','line_number':21159,'multiline':True]['text':' Local variables ','line_number':21163,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLASSQ returns the values scl and ssq such that

       ( scl**2 )*ssq = x( 1 )**2 +...+ x( n )**2 + ( scale**2 )*sumsq,

    where x( i ) = abs( X( 1 + ( i - 1 )*INCX ) ). The value of sumsq is
    assumed to be at least unity and the value of ssq will then satisfy

       1.0 .le. ssq .le. ( sumsq + 2*n ).

    scale is assumed to be non-negative and scl returns the value

       scl = max( scale, abs( real( x( i ) ) ), abs( aimag( x( i ) ) ) ),
              i

    scale and sumsq must be supplied in SCALE and SUMSQ respectively.
    SCALE and SUMSQ are overwritten by scl and ssq respectively.

    The routine makes only one pass through the vector X.

    Arguments
    =========

    N       (input) INTEGER
            The number of elements to be used from the vector X.

    X       (input) COMPLEX*16 array, dimension (N)
            The vector x as described above.
               x( i )  = X( 1 + ( i - 1 )*INCX ), 1 <= i <= n.

    INCX    (input) INTEGER
            The increment between successive values of the vector X.
            INCX > 0.

    SCALE   (input/output) DOUBLE PRECISION
            On entry, the value  scale  in the equation above.
            On exit, SCALE is overwritten with the value  scl .

    SUMSQ   (input/output) DOUBLE PRECISION
            On entry, the value  sumsq  in the equation above.
            On exit, SUMSQ is overwritten with the value  ssq .

   =====================================================================
','line_number':21168,'multiline':True]['text':' Parameter adjustments ','line_number':21223,'multiline':True]['text':' Function Body ','line_number':21226,'multiline':True]['text':' Computing 2nd power ','line_number':21236,'multiline':True]['text':' Computing 2nd power ','line_number':21241,'multiline':True]['text':' Computing 2nd power ','line_number':21249,'multiline':True]['text':' Computing 2nd power ','line_number':21254,'multiline':True]['text':' L10: ','line_number':21259,'multiline':True]['text':'     End of ZLASSQ ','line_number':21265,'multiline':True]['text':' zlassq_ ','line_number':21267,'multiline':True]['text':' Subroutine ','line_number':21269,'multiline':True]['text':' System generated locals ','line_number':21272,'multiline':True]['text':' Local variables ','line_number':21275,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLASWP performs a series of row interchanges on the matrix A.
    One row interchange is initiated for each of rows K1 through K2 of A.

    Arguments
    =========

    N       (input) INTEGER
            The number of columns of the matrix A.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the matrix of column dimension N to which the row
            interchanges will be applied.
            On exit, the permuted matrix.

    LDA     (input) INTEGER
            The leading dimension of the array A.

    K1      (input) INTEGER
            The first element of IPIV for which a row interchange will
            be done.

    K2      (input) INTEGER
            The last element of IPIV for which a row interchange will
            be done.

    IPIV    (input) INTEGER array, dimension (K2*abs(INCX))
            The vector of pivot indices.  Only the elements in positions
            K1 through K2 of IPIV are accessed.
            IPIV(K) = L implies rows K and L are to be interchanged.

    INCX    (input) INTEGER
            The increment between successive values of IPIV.  If IPIV
            is negative, the pivots are applied in reverse order.

    Further Details
    ===============

    Modified by
     R. C. Whaley, Computer Science Dept., Univ. of Tenn., Knoxville, USA

   =====================================================================


       Interchange row I with row IPIV(I) for each of rows K1 through K2.
','line_number':21280,'multiline':True]['text':' Parameter adjustments ','line_number':21336,'multiline':True]['text':' Function Body ','line_number':21342,'multiline':True]['text':' L10: ','line_number':21377,'multiline':True]['text':' L20: ','line_number':21381,'multiline':True]['text':' L30: ','line_number':21383,'multiline':True]['text':' L40: ','line_number':21403,'multiline':True]['text':' L50: ','line_number':21407,'multiline':True]['text':'     End of ZLASWP ','line_number':21413,'multiline':True]['text':' zlaswp_ ','line_number':21415,'multiline':True]['text':' Subroutine ','line_number':21417,'multiline':True]['text':' System generated locals ','line_number':21421,'multiline':True]['text':' Local variables ','line_number':21426,'multiline':True]['text':' Subroutine ','line_number':21430,'multiline':True]['text':' Double Complex ','line_number':21432,'multiline':True]['text':' Subroutine ','line_number':21434,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLATRD reduces NB rows and columns of a complex Hermitian matrix A to
    Hermitian tridiagonal form by a unitary similarity
    transformation Q' * A * Q, and returns the matrices V and W which are
    needed to apply the transformation to the unreduced part of A.

    If UPLO = 'U', ZLATRD reduces the last NB rows and columns of a
    matrix, of which the upper triangle is supplied;
    if UPLO = 'L', ZLATRD reduces the first NB rows and columns of a
    matrix, of which the lower triangle is supplied.

    This is an auxiliary routine called by ZHETRD.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            Hermitian matrix A is stored:
            = 'U': Upper triangular
            = 'L': Lower triangular

    N       (input) INTEGER
            The order of the matrix A.

    NB      (input) INTEGER
            The number of rows and columns to be reduced.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            n-by-n upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n-by-n lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.
            On exit:
            if UPLO = 'U', the last NB columns have been reduced to
              tridiagonal form, with the diagonal elements overwriting
              the diagonal elements of A; the elements above the diagonal
              with the array TAU, represent the unitary matrix Q as a
              product of elementary reflectors;
            if UPLO = 'L', the first NB columns have been reduced to
              tridiagonal form, with the diagonal elements overwriting
              the diagonal elements of A; the elements below the diagonal
              with the array TAU, represent the  unitary matrix Q as a
              product of elementary reflectors.
            See Further Details.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    E       (output) DOUBLE PRECISION array, dimension (N-1)
            If UPLO = 'U', E(n-nb:n-1) contains the superdiagonal
            elements of the last NB columns of the reduced matrix;
            if UPLO = 'L', E(1:nb) contains the subdiagonal elements of
            the first NB columns of the reduced matrix.

    TAU     (output) COMPLEX*16 array, dimension (N-1)
            The scalar factors of the elementary reflectors, stored in
            TAU(n-nb:n-1) if UPLO = 'U', and in TAU(1:nb) if UPLO = 'L'.
            See Further Details.

    W       (output) COMPLEX*16 array, dimension (LDW,NB)
            The n-by-nb matrix W required to update the unreduced part
            of A.

    LDW     (input) INTEGER
            The leading dimension of the array W. LDW >= max(1,N).

    Further Details
    ===============

    If UPLO = 'U', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(n) H(n-1) . . . H(n-nb+1).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(i:n) = 0 and v(i-1) = 1; v(1:i-1) is stored on exit in A(1:i-1,i),
    and tau in TAU(i-1).

    If UPLO = 'L', the matrix Q is represented as a product of elementary
    reflectors

       Q = H(1) H(2) . . . H(nb).

    Each H(i) has the form

       H(i) = I - tau * v * v'

    where tau is a complex scalar, and v is a complex vector with
    v(1:i) = 0 and v(i+1) = 1; v(i+1:n) is stored on exit in A(i+1:n,i),
    and tau in TAU(i).

    The elements of the vectors v together form the n-by-nb matrix V
    which is needed, with W, to apply the transformation to the unreduced
    part of the matrix, using a Hermitian rank-2k update of the form:
    A := A - V*W' - W*V'.

    The contents of A on exit are illustrated by the following examples
    with n = 5 and nb = 2:

    if UPLO = 'U':                       if UPLO = 'L':

      (  a   a   a   v4  v5 )              (  d                  )
      (      a   a   v4  v5 )              (  1   d              )
      (          a   1   v5 )              (  v1  1   a          )
      (              d   1  )              (  v1  v2  a   a      )
      (                  d  )              (  v1  v2  a   a   a  )

    where d denotes a diagonal element of the reduced matrix, a denotes
    an element of the original matrix that is unchanged, and vi denotes
    an element of the vector defining H(i).

    =====================================================================


       Quick return if possible
','line_number':21446,'multiline':True]['text':' Parameter adjustments ','line_number':21580,'multiline':True]['text':' Function Body ','line_number':21590,'multiline':True]['text':'        Reduce last NB columns of upper triangle ','line_number':21597,'multiline':True]['text':'              Update A(1:i,i) ','line_number':21604,'multiline':True]['text':'
                Generate elementary reflector H(i) to annihilate
                A(1:i-2,i)
','line_number':21635,'multiline':True]['text':'              Compute W(1:i-1,i) ','line_number':21650,'multiline':True]['text':' L10: ','line_number':21697,'multiline':True]['text':'        Reduce first NB columns of lower triangle ','line_number':21701,'multiline':True]['text':'           Update A(i:n,i) ','line_number':21706,'multiline':True]['text':'
                Generate elementary reflector H(i) to annihilate
                A(i+2:n,i)
','line_number':21738,'multiline':True]['text':' Computing MIN ','line_number':21746,'multiline':True]['text':'              Compute W(i+1:n,i) ','line_number':21755,'multiline':True]['text':' L20: ','line_number':21800,'multiline':True]['text':'     End of ZLATRD ','line_number':21806,'multiline':True]['text':' zlatrd_ ','line_number':21808,'multiline':True]['text':' Subroutine ','line_number':21810,'multiline':True]['text':' System generated locals ','line_number':21814,'multiline':True]['text':' Local variables ','line_number':21819,'multiline':True]['text':' Subroutine ','line_number':21828,'multiline':True]['text':' Double Complex ','line_number':21835,'multiline':True]['text':' Double Complex ','line_number':21838,'multiline':True]['text':' Subroutine ','line_number':21840,'multiline':True]['text':' Subroutine ','line_number':21847,'multiline':True]['text':' Double Complex ','line_number':21851,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLATRS solves one of the triangular systems

       A * x = s*b,  A**T * x = s*b,  or  A**H * x = s*b,

    with scaling to prevent overflow.  Here A is an upper or lower
    triangular matrix, A**T denotes the transpose of A, A**H denotes the
    conjugate transpose of A, x and b are n-element vectors, and s is a
    scaling factor, usually less than or equal to 1, chosen so that the
    components of x will be less than the overflow threshold.  If the
    unscaled problem will not cause overflow, the Level 2 BLAS routine
    ZTRSV is called. If the matrix A is singular (A(j,j) = 0 for some j),
    then s is set to 0 and a non-trivial solution to A*x = 0 is returned.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the matrix A is upper or lower triangular.
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    TRANS   (input) CHARACTER*1
            Specifies the operation applied to A.
            = 'N':  Solve A * x = s*b     (No transpose)
            = 'T':  Solve A**T * x = s*b  (Transpose)
            = 'C':  Solve A**H * x = s*b  (Conjugate transpose)

    DIAG    (input) CHARACTER*1
            Specifies whether or not the matrix A is unit triangular.
            = 'N':  Non-unit triangular
            = 'U':  Unit triangular

    NORMIN  (input) CHARACTER*1
            Specifies whether CNORM has been set or not.
            = 'Y':  CNORM contains the column norms on entry
            = 'N':  CNORM is not set on entry.  On exit, the norms will
                    be computed and stored in CNORM.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,N)
            The triangular matrix A.  If UPLO = 'U', the leading n by n
            upper triangular part of the array A contains the upper
            triangular matrix, and the strictly lower triangular part of
            A is not referenced.  If UPLO = 'L', the leading n by n lower
            triangular part of the array A contains the lower triangular
            matrix, and the strictly upper triangular part of A is not
            referenced.  If DIAG = 'U', the diagonal elements of A are
            also not referenced and are assumed to be 1.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max (1,N).

    X       (input/output) COMPLEX*16 array, dimension (N)
            On entry, the right hand side b of the triangular system.
            On exit, X is overwritten by the solution vector x.

    SCALE   (output) DOUBLE PRECISION
            The scaling factor s for the triangular system
               A * x = s*b,  A**T * x = s*b,  or  A**H * x = s*b.
            If SCALE = 0, the matrix A is singular or badly scaled, and
            the vector x is an exact or approximate solution to A*x = 0.

    CNORM   (input or output) DOUBLE PRECISION array, dimension (N)

            If NORMIN = 'Y', CNORM is an input argument and CNORM(j)
            contains the norm of the off-diagonal part of the j-th column
            of A.  If TRANS = 'N', CNORM(j) must be greater than or equal
            to the infinity-norm, and if TRANS = 'T' or 'C', CNORM(j)
            must be greater than or equal to the 1-norm.

            If NORMIN = 'N', CNORM is an output argument and CNORM(j)
            returns the 1-norm of the offdiagonal part of the j-th column
            of A.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -k, the k-th argument had an illegal value

    Further Details
    ======= =======

    A rough bound on x is computed; if that is less than overflow, ZTRSV
    is called, otherwise, specific code is used which checks for possible
    overflow or divide-by-zero at every operation.

    A columnwise scheme is used for solving A*x = b.  The basic algorithm
    if A is lower triangular is

         x[1:n] := b[1:n]
         for j = 1, ..., n
              x(j) := x(j) / A(j,j)
              x[j+1:n] := x[j+1:n] - x(j) * A[j+1:n,j]
         end

    Define bounds on the components of x after j iterations of the loop:
       M(j) = bound on x[1:j]
       G(j) = bound on x[j+1:n]
    Initially, let M(0) = 0 and G(0) = max{x(i), i=1,...,n}.

    Then for iteration j+1 we have
       M(j+1) <= G(j) / | A(j+1,j+1) |
       G(j+1) <= G(j) + M(j+1) * | A[j+2:n,j+1] |
              <= G(j) ( 1 + CNORM(j+1) / | A(j+1,j+1) | )

    where CNORM(j+1) is greater than or equal to the infinity-norm of
    column j+1 of A, not counting the diagonal.  Hence

       G(j) <= G(0) product ( 1 + CNORM(i) / | A(i,i) | )
                    1<=i<=j
    and

       |x(j)| <= ( G(0) / |A(j,j)| ) product ( 1 + CNORM(i) / |A(i,i)| )
                                     1<=i< j

    Since |x(j)| <= M(j), we use the Level 2 BLAS routine ZTRSV if the
    reciprocal of the largest M(j), j=1,..,n, is larger than
    max(underflow, 1/overflow).

    The bound on x(j) is also used to determine when a step in the
    columnwise method can be performed without fear of overflow.  If
    the computed bound is greater than a large constant, x is scaled to
    prevent overflow, but if the bound overflows, x is set to 0, x(j) to
    1, and scale to 0, and a non-trivial solution to A*x = 0 is found.

    Similarly, a row-wise scheme is used to solve A**T *x = b  or
    A**H *x = b.  The basic algorithm for A upper triangular is

         for j = 1, ..., n
              x(j) := ( b(j) - A[1:j-1,j]' * x[1:j-1] ) / A(j,j)
         end

    We simultaneously compute two bounds
         G(j) = bound on ( b(i) - A[1:i-1,i]' * x[1:i-1] ), 1<=i<=j
         M(j) = bound on x(i), 1<=i<=j

    The initial values are G(0) = 0, M(0) = max{b(i), i=1,..,n}, and we
    add the constraint G(j) >= G(j-1) and M(j) >= M(j-1) for j >= 1.
    Then the bound on x(j) is

         M(j) <= M(j-1) * ( 1 + CNORM(j) ) / | A(j,j) |

              <= M(0) * product ( ( 1 + CNORM(i) ) / |A(i,i)| )
                        1<=i<=j

    and we can safely call ZTRSV if 1/M(n) and 1/G(n) are both greater
    than max(underflow, 1/overflow).

    =====================================================================
','line_number':21860,'multiline':True]['text':' Parameter adjustments ','line_number':22023,'multiline':True]['text':' Function Body ','line_number':22030,'multiline':True]['text':'     Test the input parameters. ','line_number':22036,'multiline':True]['text':'     Quick return if possible ','line_number':22059,'multiline':True]['text':'     Determine machine dependent parameters to control overflow. ','line_number':22065,'multiline':True]['text':'        Compute the 1-norm of each column, not including the diagonal. ','line_number':22076,'multiline':True]['text':'           A is upper triangular. ','line_number':22080,'multiline':True]['text':' L10: ','line_number':22086,'multiline':True]['text':'           A is lower triangular. ','line_number':22090,'multiline':True]['text':' L20: ','line_number':22096,'multiline':True]['text':'
       Scale the column norms by TSCAL if the maximum element in CNORM is
       greater than BIGNUM/2.
','line_number':22102,'multiline':True]['text':'
       Compute a bound on the computed solution vector to see if the
       Level 2 BLAS routine ZTRSV can be used.
','line_number':22116,'multiline':True]['text':' Computing MAX ','line_number':22124,'multiline':True]['text':' L30: ','line_number':22129,'multiline':True]['text':'        Compute the growth in A * x = b. ','line_number':22135,'multiline':True]['text':'
             A is non-unit triangular.

             Compute GROW = 1/G(j) and XBND = 1/M(j).
             Initially, G(0) = max{x(i), i=1,...,n}.
','line_number':22154,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22167,'multiline':True]['text':'
                   M(j) = G(j-1) / abs(A(j,j))

   Computing MIN
','line_number':22180,'multiline':True]['text':'                 M(j) could overflow, set XBND to 0. ','line_number':22189,'multiline':True]['text':'                 G(j) = G(j-1)*( 1 + CNORM(j) / abs(A(j,j)) ) ','line_number':22196,'multiline':True]['text':'                 G(j) could overflow, set GROW to 0. ','line_number':22201,'multiline':True]['text':' L40: ','line_number':22205,'multiline':True]['text':'
             A is unit triangular.

             Compute GROW = 1/G(j), where G(0) = max{x(i), i=1,...,n}.

   Computing MIN
','line_number':22210,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22223,'multiline':True]['text':'              G(j) = G(j-1)*( 1 + CNORM(j) ) ','line_number':22229,'multiline':True]['text':' L50: ','line_number':22232,'multiline':True]['text':'        Compute the growth in A**T * x = b  or  A**H * x = b. ','line_number':22240,'multiline':True]['text':'
             A is non-unit triangular.

             Compute GROW = 1/G(j) and XBND = 1/M(j).
             Initially, M(0) = max{x(i), i=1,...,n}.
','line_number':22259,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22272,'multiline':True]['text':'              G(j) = max( G(j-1), M(j-1)*( 1 + CNORM(j) ) ) ','line_number':22278,'multiline':True]['text':' Computing MIN ','line_number':22281,'multiline':True]['text':'                 M(j) = M(j-1)*( 1 + CNORM(j) ) / abs(A(j,j)) ','line_number':22292,'multiline':True]['text':'                 M(j) could overflow, set XBND to 0. ','line_number':22299,'multiline':True]['text':' L70: ','line_number':22303,'multiline':True]['text':'
             A is unit triangular.

             Compute GROW = 1/G(j), where G(0) = max{x(i), i=1,...,n}.

   Computing MIN
','line_number':22308,'multiline':True]['text':'              Exit the loop if the growth factor is too small. ','line_number':22321,'multiline':True]['text':'              G(j) = ( 1 + CNORM(j) )*G(j-1) ','line_number':22327,'multiline':True]['text':' L80: ','line_number':22331,'multiline':True]['text':'
          Use the Level 2 BLAS solve if the reciprocal of the bound on
          elements of X is not too small.
','line_number':22340,'multiline':True]['text':'        Use a Level 1 BLAS solve, scaling intermediate results. ','line_number':22348,'multiline':True]['text':'
             Scale X so that its components are less than or equal to
             BIGNUM in absolute value.
','line_number':22352,'multiline':True]['text':'           Solve A * x = b ','line_number':22366,'multiline':True]['text':'              Compute x(j) = b(j) / A(j,j), scaling x if necessary. ','line_number':22372,'multiline':True]['text':'                    abs(A(j,j)) > SMLNUM: ','line_number':22391,'multiline':True]['text':'                          Scale x by 1/b(j). ','line_number':22396,'multiline':True]['text':'                    0 < abs(A(j,j)) <= SMLNUM: ','line_number':22412,'multiline':True]['text':'
                         Scale x by (1/abs(x(j)))*abs(A(j,j))*BIGNUM
                         to avoid overflow when dividing by A(j,j).
','line_number':22416,'multiline':True]['text':'
                            Scale by 1/CNORM(j) to avoid overflow when
                            multiplying x(j) times column j.
','line_number':22424,'multiline':True]['text':'
                      A(j,j) = 0:  Set x(1:n) = 0, x(j) = 1, and
                      scale = 0, and compute a solution to A*x = 0.
','line_number':22443,'multiline':True]['text':' L100: ','line_number':22452,'multiline':True]['text':'
                Scale x if necessary to avoid overflow when adding a
                multiple of column j of A.
','line_number':22462,'multiline':True]['text':'                    Scale x by 1/(2*abs(x(j))). ','line_number':22471,'multiline':True]['text':'                 Scale x by 1/2. ','line_number':22479,'multiline':True]['text':'
                      Compute the update
                         x(1:j-1) := x(1:j-1) - x(j) * A(1:j-1,j)
','line_number':22488,'multiline':True]['text':'
                      Compute the update
                         x(j+1:n) := x(j+1:n) - x(j) * A(j+1:n,j)
','line_number':22508,'multiline':True]['text':' L120: ','line_number':22526,'multiline':True]['text':'           Solve A**T * x = b ','line_number':22531,'multiline':True]['text':'
                Compute x(j) = b(j) - sum A(k,j)*x(k).
                                      k<>j
','line_number':22537,'multiline':True]['text':'                 If x(j) could overflow, scale x by 1/(2*XMAX). ','line_number':22549,'multiline':True]['text':'
                         Divide by A(j,j) when scaling x if A(j,j) > 1.

   Computing MIN
','line_number':22564,'multiline':True]['text':'
                   If the scaling needed for A in the dot product is 1,
                   call ZDOTU to perform the dot product.
','line_number':22584,'multiline':True]['text':'                 Otherwise, use in-line code for the dot product. ','line_number':22602,'multiline':True]['text':' L130: ','line_number':22618,'multiline':True]['text':' L140: ','line_number':22634,'multiline':True]['text':'
                   Compute x(j) := ( x(j) - CSUMJ ) / A(j,j) if 1/A(j,j)
                   was not used to scale the dotproduct.
','line_number':22642,'multiline':True]['text':'                    Compute x(j) = x(j) / A(j,j), scaling if necessary. ','line_number':22667,'multiline':True]['text':'                       abs(A(j,j)) > SMLNUM: ','line_number':22673,'multiline':True]['text':'                             Scale X by 1/abs(x(j)). ','line_number':22678,'multiline':True]['text':'                       0 < abs(A(j,j)) <= SMLNUM: ','line_number':22691,'multiline':True]['text':'                          Scale x by (1/abs(x(j)))*abs(A(j,j))*BIGNUM. ','line_number':22695,'multiline':True]['text':'
                         A(j,j) = 0:  Set x(1:n) = 0, x(j) = 1, and
                         scale = 0 and compute a solution to A**T *x = 0.
','line_number':22707,'multiline':True]['text':' L150: ','line_number':22716,'multiline':True]['text':'
                   Compute x(j) := x(j) / A(j,j) - CSUMJ if the dot
                   product has already been divided by 1/A(j,j).
','line_number':22727,'multiline':True]['text':' Computing MAX ','line_number':22737,'multiline':True]['text':' L170: ','line_number':22742,'multiline':True]['text':'           Solve A**H * x = b ','line_number':22747,'multiline':True]['text':'
                Compute x(j) = b(j) - sum A(k,j)*x(k).
                                      k<>j
','line_number':22753,'multiline':True]['text':'                 If x(j) could overflow, scale x by 1/(2*XMAX). ','line_number':22765,'multiline':True]['text':'
                         Divide by A(j,j) when scaling x if A(j,j) > 1.

   Computing MIN
','line_number':22779,'multiline':True]['text':'
                   If the scaling needed for A in the dot product is 1,
                   call ZDOTC to perform the dot product.
','line_number':22799,'multiline':True]['text':'                 Otherwise, use in-line code for the dot product. ','line_number':22817,'multiline':True]['text':' L180: ','line_number':22833,'multiline':True]['text':' L190: ','line_number':22849,'multiline':True]['text':'
                   Compute x(j) := ( x(j) - CSUMJ ) / A(j,j) if 1/A(j,j)
                   was not used to scale the dotproduct.
','line_number':22857,'multiline':True]['text':'                    Compute x(j) = x(j) / A(j,j), scaling if necessary. ','line_number':22881,'multiline':True]['text':'                       abs(A(j,j)) > SMLNUM: ','line_number':22887,'multiline':True]['text':'                             Scale X by 1/abs(x(j)). ','line_number':22892,'multiline':True]['text':'                       0 < abs(A(j,j)) <= SMLNUM: ','line_number':22905,'multiline':True]['text':'                          Scale x by (1/abs(x(j)))*abs(A(j,j))*BIGNUM. ','line_number':22909,'multiline':True]['text':'
                         A(j,j) = 0:  Set x(1:n) = 0, x(j) = 1, and
                         scale = 0 and compute a solution to A**H *x = 0.
','line_number':22921,'multiline':True]['text':' L200: ','line_number':22930,'multiline':True]['text':'
                   Compute x(j) := x(j) / A(j,j) - CSUMJ if the dot
                   product has already been divided by 1/A(j,j).
','line_number':22941,'multiline':True]['text':' Computing MAX ','line_number':22951,'multiline':True]['text':' L220: ','line_number':22956,'multiline':True]['text':'     Scale the column norms by 1/TSCAL for return. ','line_number':22962,'multiline':True]['text':'     End of ZLATRS ','line_number':22971,'multiline':True]['text':' zlatrs_ ','line_number':22973,'multiline':True]['text':' Subroutine ','line_number':22975,'multiline':True]['text':' System generated locals ','line_number':22978,'multiline':True]['text':' Local variables ','line_number':22983,'multiline':True]['text':' Double Complex ','line_number':22987,'multiline':True]['text':' Subroutine ','line_number':22989,'multiline':True]['text':' Subroutine ','line_number':22993,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLAUU2 computes the product U * U' or L' * L, where the triangular
    factor U or L is stored in the upper or lower triangular part of
    the array A.

    If UPLO = 'U' or 'u' then the upper triangle of the result is stored,
    overwriting the factor U in A.
    If UPLO = 'L' or 'l' then the lower triangle of the result is stored,
    overwriting the factor L in A.

    This is the unblocked form of the algorithm, calling Level 2 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the triangular factor stored in the array A
            is upper or lower triangular:
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the triangular factor U or L.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the triangular factor U or L.
            On exit, if UPLO = 'U', the upper triangle of A is
            overwritten with the upper triangle of the product U * U';
            if UPLO = 'L', the lower triangle of A is overwritten with
            the lower triangle of the product L' * L.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':22998,'multiline':True]['text':' Parameter adjustments ','line_number':23051,'multiline':True]['text':' Function Body ','line_number':23056,'multiline':True]['text':'     Quick return if possible ','line_number':23072,'multiline':True]['text':'        Compute the product U * U'. ','line_number':23080,'multiline':True]['text':' L10: ','line_number':23106,'multiline':True]['text':'        Compute the product L' * L. ','line_number':23111,'multiline':True]['text':' L20: ','line_number':23137,'multiline':True]['text':'     End of ZLAUU2 ','line_number':23143,'multiline':True]['text':' zlauu2_ ','line_number':23145,'multiline':True]['text':' Subroutine ','line_number':23147,'multiline':True]['text':' System generated locals ','line_number':23150,'multiline':True]['text':' Local variables ','line_number':23153,'multiline':True]['text':' Subroutine ','line_number':23156,'multiline':True]['text':' Subroutine ','line_number':23163,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZLAUUM computes the product U * U' or L' * L, where the triangular
    factor U or L is stored in the upper or lower triangular part of
    the array A.

    If UPLO = 'U' or 'u' then the upper triangle of the result is stored,
    overwriting the factor U in A.
    If UPLO = 'L' or 'l' then the lower triangle of the result is stored,
    overwriting the factor L in A.

    This is the blocked form of the algorithm, calling Level 3 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the triangular factor stored in the array A
            is upper or lower triangular:
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the triangular factor U or L.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the triangular factor U or L.
            On exit, if UPLO = 'U', the upper triangle of A is
            overwritten with the upper triangle of the product U * U';
            if UPLO = 'L', the lower triangle of A is overwritten with
            the lower triangle of the product L' * L.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':23171,'multiline':True]['text':' Parameter adjustments ','line_number':23224,'multiline':True]['text':' Function Body ','line_number':23229,'multiline':True]['text':'     Quick return if possible ','line_number':23245,'multiline':True]['text':'     Determine the block size for this environment. ','line_number':23251,'multiline':True]['text':'        Use unblocked code ','line_number':23258,'multiline':True]['text':'        Use blocked code ','line_number':23263,'multiline':True]['text':'           Compute the product U * U'. ','line_number':23267,'multiline':True]['text':' Computing MIN ','line_number':23272,'multiline':True]['text':' L10: ','line_number':23292,'multiline':True]['text':'           Compute the product L' * L. ','line_number':23296,'multiline':True]['text':' Computing MIN ','line_number':23301,'multiline':True]['text':' L20: ','line_number':23321,'multiline':True]['text':'     End of ZLAUUM ','line_number':23328,'multiline':True]['text':' zlauum_ ','line_number':23330,'multiline':True]['text':' Subroutine ','line_number':23332,'multiline':True]['text':' System generated locals ','line_number':23335,'multiline':True]['text':' Local variables ','line_number':23340,'multiline':True]['text':' Double Complex ','line_number':23344,'multiline':True]['text':' Subroutine ','line_number':23346,'multiline':True]['text':' Subroutine ','line_number':23351,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZPOTF2 computes the Cholesky factorization of a complex Hermitian
    positive definite matrix A.

    The factorization has the form
       A = U' * U ,  if UPLO = 'U', or
       A = L  * L',  if UPLO = 'L',
    where U is an upper triangular matrix and L is lower triangular.

    This is the unblocked version of the algorithm, calling Level 2 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the upper or lower triangular part of the
            Hermitian matrix A is stored.
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            n by n upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n by n lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.

            On exit, if INFO = 0, the factor U or L from the Cholesky
            factorization A = U'*U  or A = L*L'.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value
            > 0: if INFO = k, the leading minor of order k is not
                 positive definite, and the factorization could not be
                 completed.

    =====================================================================


       Test the input parameters.
','line_number':23356,'multiline':True]['text':' Parameter adjustments ','line_number':23416,'multiline':True]['text':' Function Body ','line_number':23421,'multiline':True]['text':'     Quick return if possible ','line_number':23437,'multiline':True]['text':'        Compute the Cholesky factorization A = U'*U. ','line_number':23445,'multiline':True]['text':'           Compute U(J,J) and test for non-positive-definiteness. ','line_number':23450,'multiline':True]['text':'           Compute elements J+1:N of row J. ','line_number':23468,'multiline':True]['text':' L10: ','line_number':23485,'multiline':True]['text':'        Compute the Cholesky factorization A = L*L'. ','line_number':23489,'multiline':True]['text':'           Compute L(J,J) and test for non-positive-definiteness. ','line_number':23494,'multiline':True]['text':'           Compute elements J+1:N of column J. ','line_number':23511,'multiline':True]['text':' L20: ','line_number':23528,'multiline':True]['text':'     End of ZPOTF2 ','line_number':23539,'multiline':True]['text':' zpotf2_ ','line_number':23541,'multiline':True]['text':' Subroutine ','line_number':23543,'multiline':True]['text':' System generated locals ','line_number':23546,'multiline':True]['text':' Local variables ','line_number':23550,'multiline':True]['text':' Subroutine ','line_number':23553,'multiline':True]['text':' Subroutine ','line_number':23560,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZPOTRF computes the Cholesky factorization of a complex Hermitian
    positive definite matrix A.

    The factorization has the form
       A = U**H * U,  if UPLO = 'U', or
       A = L  * L**H,  if UPLO = 'L',
    where U is an upper triangular matrix and L is lower triangular.

    This is the block version of the algorithm, calling Level 3 BLAS.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the Hermitian matrix A.  If UPLO = 'U', the leading
            N-by-N upper triangular part of A contains the upper
            triangular part of the matrix A, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading N-by-N lower triangular part of A contains the lower
            triangular part of the matrix A, and the strictly upper
            triangular part of A is not referenced.

            On exit, if INFO = 0, the factor U or L from the Cholesky
            factorization A = U**H*U or A = L*L**H.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, the leading minor of order i is not
                  positive definite, and the factorization could not be
                  completed.

    =====================================================================


       Test the input parameters.
','line_number':23568,'multiline':True]['text':' Parameter adjustments ','line_number':23626,'multiline':True]['text':' Function Body ','line_number':23631,'multiline':True]['text':'     Quick return if possible ','line_number':23647,'multiline':True]['text':'     Determine the block size for this environment. ','line_number':23653,'multiline':True]['text':'        Use unblocked code. ','line_number':23659,'multiline':True]['text':'        Use blocked code. ','line_number':23664,'multiline':True]['text':'           Compute the Cholesky factorization A = U'*U. ','line_number':23668,'multiline':True]['text':'
                Update and factorize the current diagonal block and test
                for non-positive-definiteness.

   Computing MIN
','line_number':23674,'multiline':True]['text':'                 Compute the current block row. ','line_number':23692,'multiline':True]['text':' L10: ','line_number':23706,'multiline':True]['text':'           Compute the Cholesky factorization A = L*L'. ','line_number':23711,'multiline':True]['text':'
                Update and factorize the current diagonal block and test
                for non-positive-definiteness.

   Computing MIN
','line_number':23717,'multiline':True]['text':'                 Compute the current block column. ','line_number':23734,'multiline':True]['text':' L20: ','line_number':23748,'multiline':True]['text':'     End of ZPOTRF ','line_number':23760,'multiline':True]['text':' zpotrf_ ','line_number':23762,'multiline':True]['text':' Subroutine ','line_number':23764,'multiline':True]['text':' System generated locals ','line_number':23767,'multiline':True]['text':' Local variables ','line_number':23770,'multiline':True]['text':' Subroutine ','line_number':23772,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZPOTRI computes the inverse of a complex Hermitian positive definite
    matrix A using the Cholesky factorization A = U**H*U or A = L*L**H
    computed by ZPOTRF.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the triangular factor U or L from the Cholesky
            factorization A = U**H*U or A = L*L**H, as computed by
            ZPOTRF.
            On exit, the upper or lower triangle of the (Hermitian)
            inverse of A, overwriting the input factor U or L.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  if INFO = i, the (i,i) element of the factor U or L is
                  zero, and the inverse could not be computed.

    =====================================================================


       Test the input parameters.
','line_number':23778,'multiline':True]['text':' Parameter adjustments ','line_number':23824,'multiline':True]['text':' Function Body ','line_number':23829,'multiline':True]['text':'     Quick return if possible ','line_number':23844,'multiline':True]['text':'     Invert the triangular Cholesky factor U or L. ','line_number':23850,'multiline':True]['text':'     Form inv(U)*inv(U)' or inv(L)'*inv(L). ','line_number':23857,'multiline':True]['text':'     End of ZPOTRI ','line_number':23863,'multiline':True]['text':' zpotri_ ','line_number':23865,'multiline':True]['text':' Subroutine ','line_number':23867,'multiline':True]['text':' System generated locals ','line_number':23871,'multiline':True]['text':' Local variables ','line_number':23874,'multiline':True]['text':' Subroutine ','line_number':23877,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZPOTRS solves a system of linear equations A*X = B with a Hermitian
    positive definite matrix A using the Cholesky factorization
    A = U**H*U or A = L*L**H computed by ZPOTRF.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  Upper triangle of A is stored;
            = 'L':  Lower triangle of A is stored.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    NRHS    (input) INTEGER
            The number of right hand sides, i.e., the number of columns
            of the matrix B.  NRHS >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,N)
            The triangular factor U or L from the Cholesky factorization
            A = U**H*U or A = L*L**H, as computed by ZPOTRF.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    B       (input/output) COMPLEX*16 array, dimension (LDB,NRHS)
            On entry, the right hand side matrix B.
            On exit, the solution matrix X.

    LDB     (input) INTEGER
            The leading dimension of the array B.  LDB >= max(1,N).

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':23883,'multiline':True]['text':' Parameter adjustments ','line_number':23935,'multiline':True]['text':' Function Body ','line_number':23943,'multiline':True]['text':'     Quick return if possible ','line_number':23963,'multiline':True]['text':'
          Solve A*X = B where A = U'*U.

          Solve U'*X = B, overwriting B with X.
','line_number':23971,'multiline':True]['text':'        Solve U*X = B, overwriting B with X. ','line_number':23980,'multiline':True]['text':'
          Solve A*X = B where A = L*L'.

          Solve L*X = B, overwriting B with X.
','line_number':23986,'multiline':True]['text':'        Solve L'*X = B, overwriting B with X. ','line_number':23995,'multiline':True]['text':'     End of ZPOTRS ','line_number':24003,'multiline':True]['text':' zpotrs_ ','line_number':24005,'multiline':True]['text':' Subroutine ','line_number':24007,'multiline':True]['text':' System generated locals ','line_number':24010,'multiline':True]['text':' Local variables ','line_number':24014,'multiline':True]['text':'
    -- LAPACK auxiliary routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZROT   applies a plane rotation, where the cos (C) is real and the
    sin (S) is complex, and the vectors CX and CY are complex.

    Arguments
    =========

    N       (input) INTEGER
            The number of elements in the vectors CX and CY.

    CX      (input/output) COMPLEX*16 array, dimension (N)
            On input, the vector X.
            On output, CX is overwritten with C*X + S*Y.

    INCX    (input) INTEGER
            The increment between successive values of CY.  INCX <> 0.

    CY      (input/output) COMPLEX*16 array, dimension (N)
            On input, the vector Y.
            On output, CY is overwritten with -CONJG(S)*X + C*Y.

    INCY    (input) INTEGER
            The increment between successive values of CY.  INCX <> 0.

    C       (input) DOUBLE PRECISION
    S       (input) COMPLEX*16
            C and S define a rotation
               [  C          S  ]
               [ -conjg(S)   C  ]
            where C*C + S*CONJG(S) = 1.0.

   =====================================================================
','line_number':24019,'multiline':True]['text':' Parameter adjustments ','line_number':24063,'multiline':True]['text':' Function Body ','line_number':24067,'multiline':True]['text':'     Code for unequal increments or equal increments not equal to 1 ','line_number':24075,'multiline':True]['text':' L10: ','line_number':24107,'multiline':True]['text':'     Code for both increments equal to 1 ','line_number':24111,'multiline':True]['text':' L30: ','line_number':24134,'multiline':True]['text':' zrot_ ','line_number':24137,'multiline':True]['text':' Subroutine ','line_number':24139,'multiline':True]['text':' System generated locals ','line_number':24144,'multiline':True]['text':' Local variables ','line_number':24148,'multiline':True]['text':' Subroutine ','line_number':24155,'multiline':True]['text':' Subroutine ','line_number':24160,'multiline':True]['text':' Subroutine ','line_number':24171,'multiline':True]['text':' Subroutine ','line_number':24176,'multiline':True]['text':' Subroutine ','line_number':24183,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZSTEDC computes all eigenvalues and, optionally, eigenvectors of a
    symmetric tridiagonal matrix using the divide and conquer method.
    The eigenvectors of a full or band complex Hermitian matrix can also
    be found if ZHETRD or ZHPTRD or ZHBTRD has been used to reduce this
    matrix to tridiagonal form.

    This code makes very mild assumptions about floating point
    arithmetic. It will work on machines with a guard digit in
    add/subtract, or on those binary machines without guard digits
    which subtract like the Cray X-MP, Cray Y-MP, Cray C-90, or Cray-2.
    It could conceivably fail on hexadecimal or decimal machines
    without guard digits, but we know of none.  See DLAED3 for details.

    Arguments
    =========

    COMPZ   (input) CHARACTER*1
            = 'N':  Compute eigenvalues only.
            = 'I':  Compute eigenvectors of tridiagonal matrix also.
            = 'V':  Compute eigenvectors of original Hermitian matrix
                    also.  On entry, Z contains the unitary matrix used
                    to reduce the original matrix to tridiagonal form.

    N       (input) INTEGER
            The dimension of the symmetric tridiagonal matrix.  N >= 0.

    D       (input/output) DOUBLE PRECISION array, dimension (N)
            On entry, the diagonal elements of the tridiagonal matrix.
            On exit, if INFO = 0, the eigenvalues in ascending order.

    E       (input/output) DOUBLE PRECISION array, dimension (N-1)
            On entry, the subdiagonal elements of the tridiagonal matrix.
            On exit, E has been destroyed.

    Z       (input/output) COMPLEX*16 array, dimension (LDZ,N)
            On entry, if COMPZ = 'V', then Z contains the unitary
            matrix used in the reduction to tridiagonal form.
            On exit, if INFO = 0, then if COMPZ = 'V', Z contains the
            orthonormal eigenvectors of the original Hermitian matrix,
            and if COMPZ = 'I', Z contains the orthonormal eigenvectors
            of the symmetric tridiagonal matrix.
            If  COMPZ = 'N', then Z is not referenced.

    LDZ     (input) INTEGER
            The leading dimension of the array Z.  LDZ >= 1.
            If eigenvectors are desired, then LDZ >= max(1,N).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If COMPZ = 'N' or 'I', or N <= 1, LWORK must be at least 1.
            If COMPZ = 'V' and N > 1, LWORK must be at least N*N.
            Note that for COMPZ = 'V', then if N is less than or
            equal to the minimum divide size, usually 25, then LWORK need
            only be 1.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal sizes of the WORK, RWORK and
            IWORK arrays, returns these values as the first entries of
            the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    RWORK   (workspace/output) DOUBLE PRECISION array,
                                           dimension (LRWORK)
            On exit, if INFO = 0, RWORK(1) returns the optimal LRWORK.

    LRWORK  (input) INTEGER
            The dimension of the array RWORK.
            If COMPZ = 'N' or N <= 1, LRWORK must be at least 1.
            If COMPZ = 'V' and N > 1, LRWORK must be at least
                           1 + 3*N + 2*N*lg N + 3*N**2 ,
                           where lg( N ) = smallest integer k such
                           that 2**k >= N.
            If COMPZ = 'I' and N > 1, LRWORK must be at least
                           1 + 4*N + 2*N**2 .
            Note that for COMPZ = 'I' or 'V', then if N is less than or
            equal to the minimum divide size, usually 25, then LRWORK
            need only be max(1,2*(N-1)).

            If LRWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    IWORK   (workspace/output) INTEGER array, dimension (MAX(1,LIWORK))
            On exit, if INFO = 0, IWORK(1) returns the optimal LIWORK.

    LIWORK  (input) INTEGER
            The dimension of the array IWORK.
            If COMPZ = 'N' or N <= 1, LIWORK must be at least 1.
            If COMPZ = 'V' or N > 1,  LIWORK must be at least
                                      6 + 6*N + 5*N*lg N.
            If COMPZ = 'I' or N > 1,  LIWORK must be at least
                                      3 + 5*N .
            Note that for COMPZ = 'I' or 'V', then if N is less than or
            equal to the minimum divide size, usually 25, then LIWORK
            need only be 1.

            If LIWORK = -1, then a workspace query is assumed; the
            routine only calculates the optimal sizes of the WORK, RWORK
            and IWORK arrays, returns these values as the first entries
            of the WORK, RWORK and IWORK arrays, and no error message
            related to LWORK or LRWORK or LIWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit.
            < 0:  if INFO = -i, the i-th argument had an illegal value.
            > 0:  The algorithm failed to compute an eigenvalue while
                  working on the submatrix lying in rows and columns
                  INFO/(N+1) through mod(INFO,N+1).

    Further Details
    ===============

    Based on contributions by
       Jeff Rutter, Computer Science Division, University of California
       at Berkeley, USA

    =====================================================================


       Test the input parameters.
','line_number':24187,'multiline':True]['text':' Parameter adjustments ','line_number':24324,'multiline':True]['text':' Function Body ','line_number':24334,'multiline':True]['text':'        Compute the workspace requirements ','line_number':24357,'multiline':True]['text':' Computing 2nd power ','line_number':24378,'multiline':True]['text':' Computing 2nd power ','line_number':24384,'multiline':True]['text':'     Quick return if possible ','line_number':24410,'multiline':True]['text':'
       If the following conditional clause is removed, then the routine
       will use the Divide and Conquer routine to compute only the
       eigenvalues, which requires (3N + 3N**2) real workspace and
       (2 + 5N + 2N lg(N)) integer workspace.
       Since on many architectures DSTERF is much faster than any other
       algorithm for finding eigenvalues only, it is used here
       as the default. If the conditional clause is removed, then
       information on the size of workspace needs to be changed.

       If COMPZ = 'N', use DSTERF to compute the eigenvalues.
','line_number':24423,'multiline':True]['text':'
       If N is smaller than the minimum divide size (SMLSIZ+1), then
       solve the problem with another solver.
','line_number':24441,'multiline':True]['text':'        If COMPZ = 'I', we simply call DSTEDC instead. ','line_number':24453,'multiline':True]['text':' L10: ','line_number':24468,'multiline':True]['text':' L20: ','line_number':24470,'multiline':True]['text':'
          From now on, only option left to be handled is COMPZ = 'V',
          i.e. ICOMPZ = 1.

          Scale.
','line_number':24475,'multiline':True]['text':'        while ( START <= N ) ','line_number':24491,'multiline':True]['text':'
             Let FINISH be the position of the next subdiagonal entry
             such that E( FINISH ) <= TINY or FINISH = N if no such
             subdiagonal exists.  The matrix identified by the elements
             between START and FINISH constitutes an independent
             sub-problem.
','line_number':24496,'multiline':True]['text':'           (Sub) Problem determined.  Compute its size and solve it. ','line_number':24515,'multiline':True]['text':'              Scale. ','line_number':24520,'multiline':True]['text':'              Scale back. ','line_number':24538,'multiline':True]['text':'
          endwhile

          If the problem split any number of times, then the eigenvalues
          will not be properly ordered.  Here we permute the eigenvalues
          (and the associated eigenvectors) into ascending order.
','line_number':24560,'multiline':True]['text':'           Use Selection Sort to minimize swaps of eigenvectors ','line_number':24570,'multiline':True]['text':' L50: ','line_number':24583,'multiline':True]['text':' L60: ','line_number':24591,'multiline':True]['text':'     End of ZSTEDC ','line_number':24603,'multiline':True]['text':' zstedc_ ','line_number':24605,'multiline':True]['text':' Subroutine ','line_number':24607,'multiline':True]['text':' System generated locals ','line_number':24611,'multiline':True]['text':' Local variables ','line_number':24615,'multiline':True]['text':' Subroutine ','line_number':24624,'multiline':True]['text':' Subroutine ','line_number':24628,'multiline':True]['text':' Subroutine ','line_number':24636,'multiline':True]['text':' Subroutine ','line_number':24640,'multiline':True]['text':' Subroutine ','line_number':24643,'multiline':True]['text':' Subroutine ','line_number':24645,'multiline':True]['text':' Subroutine ','line_number':24651,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZSTEQR computes all eigenvalues and, optionally, eigenvectors of a
    symmetric tridiagonal matrix using the implicit QL or QR method.
    The eigenvectors of a full or band complex Hermitian matrix can also
    be found if ZHETRD or ZHPTRD or ZHBTRD has been used to reduce this
    matrix to tridiagonal form.

    Arguments
    =========

    COMPZ   (input) CHARACTER*1
            = 'N':  Compute eigenvalues only.
            = 'V':  Compute eigenvalues and eigenvectors of the original
                    Hermitian matrix.  On entry, Z must contain the
                    unitary matrix used to reduce the original matrix
                    to tridiagonal form.
            = 'I':  Compute eigenvalues and eigenvectors of the
                    tridiagonal matrix.  Z is initialized to the identity
                    matrix.

    N       (input) INTEGER
            The order of the matrix.  N >= 0.

    D       (input/output) DOUBLE PRECISION array, dimension (N)
            On entry, the diagonal elements of the tridiagonal matrix.
            On exit, if INFO = 0, the eigenvalues in ascending order.

    E       (input/output) DOUBLE PRECISION array, dimension (N-1)
            On entry, the (n-1) subdiagonal elements of the tridiagonal
            matrix.
            On exit, E has been destroyed.

    Z       (input/output) COMPLEX*16 array, dimension (LDZ, N)
            On entry, if  COMPZ = 'V', then Z contains the unitary
            matrix used in the reduction to tridiagonal form.
            On exit, if INFO = 0, then if COMPZ = 'V', Z contains the
            orthonormal eigenvectors of the original Hermitian matrix,
            and if COMPZ = 'I', Z contains the orthonormal eigenvectors
            of the symmetric tridiagonal matrix.
            If COMPZ = 'N', then Z is not referenced.

    LDZ     (input) INTEGER
            The leading dimension of the array Z.  LDZ >= 1, and if
            eigenvectors are desired, then  LDZ >= max(1,N).

    WORK    (workspace) DOUBLE PRECISION array, dimension (max(1,2*N-2))
            If COMPZ = 'N', then WORK is not referenced.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value
            > 0:  the algorithm has failed to find all the eigenvalues in
                  a total of 30*N iterations; if INFO = i, then i
                  elements of E have not converged to zero; on exit, D
                  and E contain the elements of a symmetric tridiagonal
                  matrix which is unitarily similar to the original
                  matrix.

    =====================================================================


       Test the input parameters.
','line_number':24655,'multiline':True]['text':' Parameter adjustments ','line_number':24728,'multiline':True]['text':' Function Body ','line_number':24736,'multiline':True]['text':'     Quick return if possible ','line_number':24761,'multiline':True]['text':'     Determine the unit roundoff and over/underflow thresholds. ','line_number':24775,'multiline':True]['text':' Computing 2nd power ','line_number':24778,'multiline':True]['text':'
       Compute the eigenvalues and eigenvectors of the tridiagonal
       matrix.
','line_number':24786,'multiline':True]['text':'
       Determine where the matrix splits and choose QL or QR iteration
       for each block, according to whether top or bottom diagonal
       element is smaller.
','line_number':24798,'multiline':True]['text':' L20: ','line_number':24826,'multiline':True]['text':'     Scale submatrix in rows and columns L to LEND ','line_number':24841,'multiline':True]['text':'     Choose between QL and QR iteration ','line_number':24867,'multiline':True]['text':'
          QL Iteration

          Look for small subdiagonal element.
','line_number':24876,'multiline':True]['text':' Computing 2nd power ','line_number':24887,'multiline':True]['text':' L50: ','line_number':24894,'multiline':True]['text':'
          If remaining matrix is 2-by-2, use DLAE2 or SLAEV2
          to compute its eigensystem.
','line_number':24909,'multiline':True]['text':'        Form shift. ','line_number':24939,'multiline':True]['text':'        Inner loop ','line_number':24949,'multiline':True]['text':'           If eigenvectors are desired, then save rotations. ','line_number':24966,'multiline':True]['text':' L70: ','line_number':24973,'multiline':True]['text':'        If eigenvectors are desired, then apply saved rotations. ','line_number':24976,'multiline':True]['text':'        Eigenvalue found. ','line_number':24988,'multiline':True]['text':'
          QR Iteration

          Look for small superdiagonal element.
','line_number':25001,'multiline':True]['text':' Computing 2nd power ','line_number':25012,'multiline':True]['text':' L100: ','line_number':25019,'multiline':True]['text':'
          If remaining matrix is 2-by-2, use DLAE2 or SLAEV2
          to compute its eigensystem.
','line_number':25034,'multiline':True]['text':'        Form shift. ','line_number':25065,'multiline':True]['text':'        Inner loop ','line_number':25075,'multiline':True]['text':'           If eigenvectors are desired, then save rotations. ','line_number':25092,'multiline':True]['text':' L120: ','line_number':25099,'multiline':True]['text':'        If eigenvectors are desired, then apply saved rotations. ','line_number':25102,'multiline':True]['text':'        Eigenvalue found. ','line_number':25114,'multiline':True]['text':'     Undo scaling if necessary ','line_number':25127,'multiline':True]['text':'
       Check for no convergence to an eigenvalue after a total
       of N*MAXIT iterations.
','line_number':25146,'multiline':True]['text':' L150: ','line_number':25157,'multiline':True]['text':'     Order eigenvalues and eigenvectors. ','line_number':25163,'multiline':True]['text':'        Use Quick Sort ','line_number':25168,'multiline':True]['text':'        Use Selection Sort to minimize swaps of eigenvectors ','line_number':25174,'multiline':True]['text':' L170: ','line_number':25187,'multiline':True]['text':' L180: ','line_number':25195,'multiline':True]['text':'     End of ZSTEQR ','line_number':25200,'multiline':True]['text':' zsteqr_ ','line_number':25202,'multiline':True]['text':' Subroutine ','line_number':25204,'multiline':True]['text':' System generated locals ','line_number':25209,'multiline':True]['text':' Local variables ','line_number':25215,'multiline':True]['text':' Subroutine ','line_number':25225,'multiline':True]['text':' Subroutine ','line_number':25229,'multiline':True]['text':' Subroutine ','line_number':25232,'multiline':True]['text':' Subroutine ','line_number':25238,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZTREVC computes some or all of the right and/or left eigenvectors of
    a complex upper triangular matrix T.
    Matrices of this type are produced by the Schur factorization of
    a complex general matrix:  A = Q*T*Q**H, as computed by ZHSEQR.

    The right eigenvector x and the left eigenvector y of T corresponding
    to an eigenvalue w are defined by:

                 T*x = w*x,     (y**H)*T = w*(y**H)

    where y**H denotes the conjugate transpose of the vector y.
    The eigenvalues are not input to this routine, but are read directly
    from the diagonal of T.

    This routine returns the matrices X and/or Y of right and left
    eigenvectors of T, or the products Q*X and/or Q*Y, where Q is an
    input matrix.  If Q is the unitary factor that reduces a matrix A to
    Schur form T, then Q*X and Q*Y are the matrices of right and left
    eigenvectors of A.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'R':  compute right eigenvectors only;
            = 'L':  compute left eigenvectors only;
            = 'B':  compute both right and left eigenvectors.

    HOWMNY  (input) CHARACTER*1
            = 'A':  compute all right and/or left eigenvectors;
            = 'B':  compute all right and/or left eigenvectors,
                    backtransformed using the matrices supplied in
                    VR and/or VL;
            = 'S':  compute selected right and/or left eigenvectors,
                    as indicated by the logical array SELECT.

    SELECT  (input) LOGICAL array, dimension (N)
            If HOWMNY = 'S', SELECT specifies the eigenvectors to be
            computed.
            The eigenvector corresponding to the j-th eigenvalue is
            computed if SELECT(j) = .TRUE..
            Not referenced if HOWMNY = 'A' or 'B'.

    N       (input) INTEGER
            The order of the matrix T. N >= 0.

    T       (input/output) COMPLEX*16 array, dimension (LDT,N)
            The upper triangular matrix T.  T is modified, but restored
            on exit.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= max(1,N).

    VL      (input/output) COMPLEX*16 array, dimension (LDVL,MM)
            On entry, if SIDE = 'L' or 'B' and HOWMNY = 'B', VL must
            contain an N-by-N matrix Q (usually the unitary matrix Q of
            Schur vectors returned by ZHSEQR).
            On exit, if SIDE = 'L' or 'B', VL contains:
            if HOWMNY = 'A', the matrix Y of left eigenvectors of T;
            if HOWMNY = 'B', the matrix Q*Y;
            if HOWMNY = 'S', the left eigenvectors of T specified by
                             SELECT, stored consecutively in the columns
                             of VL, in the same order as their
                             eigenvalues.
            Not referenced if SIDE = 'R'.

    LDVL    (input) INTEGER
            The leading dimension of the array VL.  LDVL >= 1, and if
            SIDE = 'L' or 'B', LDVL >= N.

    VR      (input/output) COMPLEX*16 array, dimension (LDVR,MM)
            On entry, if SIDE = 'R' or 'B' and HOWMNY = 'B', VR must
            contain an N-by-N matrix Q (usually the unitary matrix Q of
            Schur vectors returned by ZHSEQR).
            On exit, if SIDE = 'R' or 'B', VR contains:
            if HOWMNY = 'A', the matrix X of right eigenvectors of T;
            if HOWMNY = 'B', the matrix Q*X;
            if HOWMNY = 'S', the right eigenvectors of T specified by
                             SELECT, stored consecutively in the columns
                             of VR, in the same order as their
                             eigenvalues.
            Not referenced if SIDE = 'L'.

    LDVR    (input) INTEGER
            The leading dimension of the array VR.  LDVR >= 1, and if
            SIDE = 'R' or 'B'; LDVR >= N.

    MM      (input) INTEGER
            The number of columns in the arrays VL and/or VR. MM >= M.

    M       (output) INTEGER
            The number of columns in the arrays VL and/or VR actually
            used to store the eigenvectors.  If HOWMNY = 'A' or 'B', M
            is set to N.  Each selected eigenvector occupies one
            column.

    WORK    (workspace) COMPLEX*16 array, dimension (2*N)

    RWORK   (workspace) DOUBLE PRECISION array, dimension (N)

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    Further Details
    ===============

    The algorithm used in this program is basically backward (forward)
    substitution, with scaling to make the code robust against
    possible overflow.

    Each eigenvector is normalized so that the element of largest
    magnitude has magnitude 1; here the magnitude of a complex number
    (x,y) is taken to be |x| + |y|.

    =====================================================================


       Decode and test the input parameters
','line_number':25243,'multiline':True]['text':' Parameter adjustments ','line_number':25374,'multiline':True]['text':' Function Body ','line_number':25388,'multiline':True]['text':'
       Set M to the number of columns required to store the selected
       eigenvectors.
','line_number':25397,'multiline':True]['text':' L10: ','line_number':25409,'multiline':True]['text':'     Quick return if possible. ','line_number':25437,'multiline':True]['text':'     Set the constants to control overflow. ','line_number':25443,'multiline':True]['text':'     Store the diagonal elements of T in working array WORK. ','line_number':25451,'multiline':True]['text':' L20: ','line_number':25458,'multiline':True]['text':'
       Compute 1-norm of each column of strictly upper triangular
       part of T to control overflow in triangular solver.
','line_number':25461,'multiline':True]['text':' L30: ','line_number':25471,'multiline':True]['text':'        Compute right eigenvectors. ','line_number':25476,'multiline':True]['text':' Computing MAX ','line_number':25486,'multiline':True]['text':'           Form right-hand side. ','line_number':25494,'multiline':True]['text':' L40: ','line_number':25502,'multiline':True]['text':'
             Solve the triangular system:
                (T(1:KI-1,1:KI-1) - T(KI,KI))*X = SCALE*WORK.
','line_number':25505,'multiline':True]['text':' L50: ','line_number':25524,'multiline':True]['text':'           Copy the vector x or Q*x to VR and normalize. ','line_number':25535,'multiline':True]['text':' L60: ','line_number':25550,'multiline':True]['text':'           Set back the original diagonal elements of T. ','line_number':25567,'multiline':True]['text':' L70: ','line_number':25574,'multiline':True]['text':'        Compute left eigenvectors. ','line_number':25585,'multiline':True]['text':' Computing MAX ','line_number':25596,'multiline':True]['text':'           Form right-hand side. ','line_number':25605,'multiline':True]['text':' L90: ','line_number':25613,'multiline':True]['text':'
             Solve the triangular system:
                (T(KI+1:N,KI+1:N) - T(KI,KI))'*X = SCALE*WORK.
','line_number':25616,'multiline':True]['text':' L100: ','line_number':25635,'multiline':True]['text':'           Copy the vector x or Q*x to VL and normalize. ','line_number':25647,'multiline':True]['text':' L110: ','line_number':25666,'multiline':True]['text':'           Set back the original diagonal elements of T. ','line_number':25684,'multiline':True]['text':' L120: ','line_number':25691,'multiline':True]['text':'     End of ZTREVC ','line_number':25702,'multiline':True]['text':' ztrevc_ ','line_number':25704,'multiline':True]['text':' Subroutine ','line_number':25706,'multiline':True]['text':' System generated locals ','line_number':25710,'multiline':True]['text':' Local variables ','line_number':25714,'multiline':True]['text':' Subroutine ','line_number':25718,'multiline':True]['text':' Subroutine ','line_number':25722,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZTREXC reorders the Schur factorization of a complex matrix
    A = Q*T*Q**H, so that the diagonal element of T with row index IFST
    is moved to row ILST.

    The Schur form T is reordered by a unitary similarity transformation
    Z**H*T*Z, and optionally the matrix Q of Schur vectors is updated by
    postmultplying it with Z.

    Arguments
    =========

    COMPQ   (input) CHARACTER*1
            = 'V':  update the matrix Q of Schur vectors;
            = 'N':  do not update Q.

    N       (input) INTEGER
            The order of the matrix T. N >= 0.

    T       (input/output) COMPLEX*16 array, dimension (LDT,N)
            On entry, the upper triangular matrix T.
            On exit, the reordered upper triangular matrix.

    LDT     (input) INTEGER
            The leading dimension of the array T. LDT >= max(1,N).

    Q       (input/output) COMPLEX*16 array, dimension (LDQ,N)
            On entry, if COMPQ = 'V', the matrix Q of Schur vectors.
            On exit, if COMPQ = 'V', Q has been postmultiplied by the
            unitary transformation matrix Z which reorders T.
            If COMPQ = 'N', Q is not referenced.

    LDQ     (input) INTEGER
            The leading dimension of the array Q.  LDQ >= max(1,N).

    IFST    (input) INTEGER
    ILST    (input) INTEGER
            Specify the reordering of the diagonal elements of T:
            The element with row index IFST is moved to row ILST by a
            sequence of transpositions between adjacent elements.
            1 <= IFST <= N; 1 <= ILST <= N.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Decode and test the input parameters.
','line_number':25727,'multiline':True]['text':' Parameter adjustments ','line_number':25788,'multiline':True]['text':' Function Body ','line_number':25796,'multiline':True]['text':'     Quick return if possible ','line_number':25818,'multiline':True]['text':'        Move the IFST-th diagonal element forward down the diagonal. ','line_number':25826,'multiline':True]['text':'        Move the IFST-th diagonal element backward up the diagonal. ','line_number':25833,'multiline':True]['text':'        Interchange the k-th and (k+1)-th diagonal elements. ','line_number':25844,'multiline':True]['text':'        Determine the transformation to perform the interchange. ','line_number':25851,'multiline':True]['text':'        Apply transformation to the matrix T. ','line_number':25856,'multiline':True]['text':'           Accumulate transformation in the matrix Q. ','line_number':25875,'multiline':True]['text':' L10: ','line_number':25882,'multiline':True]['text':'     End of ZTREXC ','line_number':25887,'multiline':True]['text':' ztrexc_ ','line_number':25889,'multiline':True]['text':' Subroutine ','line_number':25891,'multiline':True]['text':' System generated locals ','line_number':25894,'multiline':True]['text':' Local variables ','line_number':25898,'multiline':True]['text':' Subroutine ','line_number':25902,'multiline':True]['text':' Subroutine ','line_number':25905,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZTRTI2 computes the inverse of a complex upper or lower triangular
    matrix.

    This is the Level 2 BLAS version of the algorithm.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            Specifies whether the matrix A is upper or lower triangular.
            = 'U':  Upper triangular
            = 'L':  Lower triangular

    DIAG    (input) CHARACTER*1
            Specifies whether or not the matrix A is unit triangular.
            = 'N':  Non-unit triangular
            = 'U':  Unit triangular

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the triangular matrix A.  If UPLO = 'U', the
            leading n by n upper triangular part of the array A contains
            the upper triangular matrix, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading n by n lower triangular part of the array A contains
            the lower triangular matrix, and the strictly upper
            triangular part of A is not referenced.  If DIAG = 'U', the
            diagonal elements of A are also not referenced and are
            assumed to be 1.

            On exit, the (triangular) inverse of the original matrix, in
            the same storage format.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -k, the k-th argument had an illegal value

    =====================================================================


       Test the input parameters.
','line_number':25910,'multiline':True]['text':' Parameter adjustments ','line_number':25968,'multiline':True]['text':' Function Body ','line_number':25973,'multiline':True]['text':'        Compute inverse of upper triangular matrix. ','line_number':25994,'multiline':True]['text':'           Compute elements 1:j-1 of j-th column. ','line_number':26010,'multiline':True]['text':' L10: ','line_number':26017,'multiline':True]['text':'        Compute inverse of lower triangular matrix. ','line_number':26021,'multiline':True]['text':'              Compute elements j+1:n of j-th column. ','line_number':26037,'multiline':True]['text':' L20: ','line_number':26045,'multiline':True]['text':'     End of ZTRTI2 ','line_number':26051,'multiline':True]['text':' ztrti2_ ','line_number':26053,'multiline':True]['text':' Subroutine ','line_number':26055,'multiline':True]['text':' System generated locals ','line_number':26058,'multiline':True]['text':' Local variables ','line_number':26064,'multiline':True]['text':' Subroutine ','line_number':26068,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZTRTRI computes the inverse of a complex upper or lower triangular
    matrix A.

    This is the Level 3 BLAS version of the algorithm.

    Arguments
    =========

    UPLO    (input) CHARACTER*1
            = 'U':  A is upper triangular;
            = 'L':  A is lower triangular.

    DIAG    (input) CHARACTER*1
            = 'N':  A is non-unit triangular;
            = 'U':  A is unit triangular.

    N       (input) INTEGER
            The order of the matrix A.  N >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the triangular matrix A.  If UPLO = 'U', the
            leading N-by-N upper triangular part of the array A contains
            the upper triangular matrix, and the strictly lower
            triangular part of A is not referenced.  If UPLO = 'L', the
            leading N-by-N lower triangular part of the array A contains
            the lower triangular matrix, and the strictly upper
            triangular part of A is not referenced.  If DIAG = 'U', the
            diagonal elements of A are also not referenced and are
            assumed to be 1.
            On exit, the (triangular) inverse of the original matrix, in
            the same storage format.

    LDA     (input) INTEGER
            The leading dimension of the array A.  LDA >= max(1,N).

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value
            > 0: if INFO = i, A(i,i) is exactly zero.  The triangular
                 matrix is singular and its inverse can not be computed.

    =====================================================================


       Test the input parameters.
','line_number':26080,'multiline':True]['text':' Parameter adjustments ','line_number':26137,'multiline':True]['text':' Function Body ','line_number':26142,'multiline':True]['text':'     Quick return if possible ','line_number':26161,'multiline':True]['text':'     Check for singularity if non-unit. ','line_number':26167,'multiline':True]['text':' L10: ','line_number':26176,'multiline':True]['text':'
       Determine the block size for this environment.

   Writing concatenation
','line_number':26181,'multiline':True]['text':'        Use unblocked code ','line_number':26193,'multiline':True]['text':'        Use blocked code ','line_number':26198,'multiline':True]['text':'           Compute inverse of upper triangular matrix ','line_number':26202,'multiline':True]['text':' Computing MIN ','line_number':26207,'multiline':True]['text':'              Compute rows 1:j-1 of current block column ','line_number':26211,'multiline':True]['text':'              Compute inverse of current diagonal block ','line_number':26222,'multiline':True]['text':' L20: ','line_number':26225,'multiline':True]['text':'           Compute inverse of lower triangular matrix ','line_number':26229,'multiline':True]['text':' Computing MIN ','line_number':26234,'multiline':True]['text':'                 Compute rows j+jb:n of current block column ','line_number':26239,'multiline':True]['text':'              Compute inverse of current diagonal block ','line_number':26252,'multiline':True]['text':' L30: ','line_number':26255,'multiline':True]['text':'     End of ZTRTRI ','line_number':26262,'multiline':True]['text':' ztrtri_ ','line_number':26264,'multiline':True]['text':' Subroutine ','line_number':26266,'multiline':True]['text':' System generated locals ','line_number':26270,'multiline':True]['text':' Local variables ','line_number':26274,'multiline':True]['text':' Subroutine ','line_number':26276,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNG2R generates an m by n complex matrix Q with orthonormal columns,
    which is defined as the first n columns of a product of k elementary
    reflectors of order m

          Q  =  H(1) H(2) . . . H(k)

    as returned by ZGEQRF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. M >= N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. N >= K >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the i-th column must contain the vector which
            defines the elementary reflector H(i), for i = 1,2,...,k, as
            returned by ZGEQRF in the first k columns of its array
            argument A.
            On exit, the m by n matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEQRF.

    WORK    (workspace) COMPLEX*16 array, dimension (N)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':26282,'multiline':True]['text':' Parameter adjustments ','line_number':26339,'multiline':True]['text':' Function Body ','line_number':26346,'multiline':True]['text':'     Quick return if possible ','line_number':26363,'multiline':True]['text':'     Initialise columns k+1:n to columns of the unit matrix ','line_number':26369,'multiline':True]['text':' L10: ','line_number':26377,'multiline':True]['text':' L20: ','line_number':26381,'multiline':True]['text':'        Apply H(i) to A(i:m,i:n) from the left ','line_number':26386,'multiline':True]['text':'        Set A(1:i-1,i) to zero ','line_number':26407,'multiline':True]['text':' L30: ','line_number':26413,'multiline':True]['text':' L40: ','line_number':26415,'multiline':True]['text':'     End of ZUNG2R ','line_number':26419,'multiline':True]['text':' zung2r_ ','line_number':26421,'multiline':True]['text':' Subroutine ','line_number':26423,'multiline':True]['text':' System generated locals ','line_number':26427,'multiline':True]['text':' Local variables ','line_number':26430,'multiline':True]['text':' Subroutine ','line_number':26435,'multiline':True]['text':' Subroutine ','line_number':26440,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNGBR generates one of the complex unitary matrices Q or P**H
    determined by ZGEBRD when reducing a complex matrix A to bidiagonal
    form: A = Q * B * P**H.  Q and P**H are defined as products of
    elementary reflectors H(i) or G(i) respectively.

    If VECT = 'Q', A is assumed to have been an M-by-K matrix, and Q
    is of order M:
    if m >= k, Q = H(1) H(2) . . . H(k) and ZUNGBR returns the first n
    columns of Q, where m >= n >= k;
    if m < k, Q = H(1) H(2) . . . H(m-1) and ZUNGBR returns Q as an
    M-by-M matrix.

    If VECT = 'P', A is assumed to have been a K-by-N matrix, and P**H
    is of order N:
    if k < n, P**H = G(k) . . . G(2) G(1) and ZUNGBR returns the first m
    rows of P**H, where n >= m >= k;
    if k >= n, P**H = G(n-1) . . . G(2) G(1) and ZUNGBR returns P**H as
    an N-by-N matrix.

    Arguments
    =========

    VECT    (input) CHARACTER*1
            Specifies whether the matrix Q or the matrix P**H is
            required, as defined in the transformation applied by ZGEBRD:
            = 'Q':  generate Q;
            = 'P':  generate P**H.

    M       (input) INTEGER
            The number of rows of the matrix Q or P**H to be returned.
            M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q or P**H to be returned.
            N >= 0.
            If VECT = 'Q', M >= N >= min(M,K);
            if VECT = 'P', N >= M >= min(N,K).

    K       (input) INTEGER
            If VECT = 'Q', the number of columns in the original M-by-K
            matrix reduced by ZGEBRD.
            If VECT = 'P', the number of rows in the original K-by-N
            matrix reduced by ZGEBRD.
            K >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the vectors which define the elementary reflectors,
            as returned by ZGEBRD.
            On exit, the M-by-N matrix Q or P**H.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= M.

    TAU     (input) COMPLEX*16 array, dimension
                                  (min(M,K)) if VECT = 'Q'
                                  (min(N,K)) if VECT = 'P'
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i) or G(i), which determines Q or P**H, as
            returned by ZGEBRD in its array argument TAUQ or TAUP.

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= max(1,min(M,N)).
            For optimum performance LWORK >= min(M,N)*NB, where NB
            is the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':26447,'multiline':True]['text':' Parameter adjustments ','line_number':26540,'multiline':True]['text':' Function Body ','line_number':26547,'multiline':True]['text':'     Quick return if possible ','line_number':26587,'multiline':True]['text':'
          Form Q, determined by a call to ZGEBRD to reduce an m-by-k
          matrix
','line_number':26596,'multiline':True]['text':'           If m >= k, assume m >= n >= k ','line_number':26603,'multiline':True]['text':'
             If m < k, assume m = n

             Shift the vectors which define the elementary reflectors one
             column to the right, and set the first row and column of Q
             to those of the unit matrix
','line_number':26610,'multiline':True]['text':' L10: ','line_number':26626,'multiline':True]['text':' L20: ','line_number':26628,'multiline':True]['text':' L30: ','line_number':26636,'multiline':True]['text':'              Form Q(2:m,2:m) ','line_number':26640,'multiline':True]['text':'
          Form P', determined by a call to ZGEBRD to reduce a k-by-n
          matrix
','line_number':26651,'multiline':True]['text':'           If k < n, assume k <= m <= n ','line_number':26658,'multiline':True]['text':'
             If k >= n, assume m = n

             Shift the vectors which define the elementary reflectors one
             row downward, and set the first row and column of P' to
             those of the unit matrix
','line_number':26665,'multiline':True]['text':' L40: ','line_number':26679,'multiline':True]['text':' L50: ','line_number':26687,'multiline':True]['text':' L60: ','line_number':26691,'multiline':True]['text':'              Form P'(2:n,2:n) ','line_number':26695,'multiline':True]['text':'     End of ZUNGBR ','line_number':26708,'multiline':True]['text':' zungbr_ ','line_number':26710,'multiline':True]['text':' Subroutine ','line_number':26712,'multiline':True]['text':' System generated locals ','line_number':26716,'multiline':True]['text':' Local variables ','line_number':26719,'multiline':True]['text':' Subroutine ','line_number':26721,'multiline':True]['text':' Subroutine ','line_number':26726,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNGHR generates a complex unitary matrix Q which is defined as the
    product of IHI-ILO elementary reflectors of order N, as returned by
    ZGEHRD:

    Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Arguments
    =========

    N       (input) INTEGER
            The order of the matrix Q. N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            ILO and IHI must have the same values as in the previous call
            of ZGEHRD. Q is equal to the unit matrix except in the
            submatrix Q(ilo+1:ihi,ilo+1:ihi).
            1 <= ILO <= IHI <= N, if N > 0; ILO=1 and IHI=0, if N=0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the vectors which define the elementary reflectors,
            as returned by ZGEHRD.
            On exit, the N-by-N unitary matrix Q.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,N).

    TAU     (input) COMPLEX*16 array, dimension (N-1)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEHRD.

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= IHI-ILO.
            For optimum performance LWORK >= (IHI-ILO)*NB, where NB is
            the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':26731,'multiline':True]['text':' Parameter adjustments ','line_number':26795,'multiline':True]['text':' Function Body ','line_number':26802,'multiline':True]['text':'     Quick return if possible ','line_number':26833,'multiline':True]['text':'
       Shift the vectors which define the elementary reflectors one
       column to the right, and set the first ilo and the last n-ihi
       rows and columns to those of the unit matrix
','line_number':26840,'multiline':True]['text':' L10: ','line_number':26852,'multiline':True]['text':' L20: ','line_number':26859,'multiline':True]['text':' L30: ','line_number':26865,'multiline':True]['text':' L40: ','line_number':26867,'multiline':True]['text':' L50: ','line_number':26875,'multiline':True]['text':' L60: ','line_number':26879,'multiline':True]['text':' L70: ','line_number':26887,'multiline':True]['text':' L80: ','line_number':26891,'multiline':True]['text':'        Generate Q(ilo+1:ihi,ilo+1:ihi) ','line_number':26896,'multiline':True]['text':'     End of ZUNGHR ','line_number':26904,'multiline':True]['text':' zunghr_ ','line_number':26906,'multiline':True]['text':' Subroutine ','line_number':26908,'multiline':True]['text':' System generated locals ','line_number':26912,'multiline':True]['text':' Local variables ','line_number':26916,'multiline':True]['text':' Subroutine ','line_number':26918,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNGL2 generates an m-by-n complex matrix Q with orthonormal rows,
    which is defined as the first m rows of a product of k elementary
    reflectors of order n

          Q  =  H(k)' . . . H(2)' H(1)'

    as returned by ZGELQF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. N >= M.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. M >= K >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the i-th row must contain the vector which defines
            the elementary reflector H(i), for i = 1,2,...,k, as returned
            by ZGELQF in the first k rows of its array argument A.
            On exit, the m by n matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGELQF.

    WORK    (workspace) COMPLEX*16 array, dimension (M)

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':26924,'multiline':True]['text':' Parameter adjustments ','line_number':26980,'multiline':True]['text':' Function Body ','line_number':26987,'multiline':True]['text':'     Quick return if possible ','line_number':27004,'multiline':True]['text':'        Initialise rows k+1:m to rows of the unit matrix ','line_number':27012,'multiline':True]['text':' L10: ','line_number':27020,'multiline':True]['text':' L20: ','line_number':27026,'multiline':True]['text':'        Apply H(i)' to A(i:m,i:n) from the right ','line_number':27032,'multiline':True]['text':'        Set A(i,1:i-1) to zero ','line_number':27058,'multiline':True]['text':' L30: ','line_number':27064,'multiline':True]['text':' L40: ','line_number':27066,'multiline':True]['text':'     End of ZUNGL2 ','line_number':27070,'multiline':True]['text':' zungl2_ ','line_number':27072,'multiline':True]['text':' Subroutine ','line_number':27074,'multiline':True]['text':' System generated locals ','line_number':27078,'multiline':True]['text':' Local variables ','line_number':27081,'multiline':True]['text':' Subroutine ','line_number':27083,'multiline':True]['text':' Subroutine ','line_number':27088,'multiline':True]['text':' Subroutine ','line_number':27093,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNGLQ generates an M-by-N complex matrix Q with orthonormal rows,
    which is defined as the first M rows of a product of K elementary
    reflectors of order N

          Q  =  H(k)' . . . H(2)' H(1)'

    as returned by ZGELQF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. N >= M.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. M >= K >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the i-th row must contain the vector which defines
            the elementary reflector H(i), for i = 1,2,...,k, as returned
            by ZGELQF in the first k rows of its array argument A.
            On exit, the M-by-N matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGELQF.

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= max(1,M).
            For optimum performance LWORK >= M*NB, where NB is
            the optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit;
            < 0:  if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':27100,'multiline':True]['text':' Parameter adjustments ','line_number':27167,'multiline':True]['text':' Function Body ','line_number':27174,'multiline':True]['text':'     Quick return if possible ','line_number':27199,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':27211,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':27221,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':27227,'multiline':True]['text':' Computing MAX ','line_number':27233,'multiline':True]['text':'
          Use blocked code after the last block.
          The first kk rows are handled by the block method.
','line_number':27243,'multiline':True]['text':' Computing MIN ','line_number':27249,'multiline':True]['text':'        Set A(kk+1:m,1:kk) to zero. ','line_number':27253,'multiline':True]['text':' L10: ','line_number':27261,'multiline':True]['text':' L20: ','line_number':27263,'multiline':True]['text':'     Use unblocked code for the last or only block. ','line_number':27269,'multiline':True]['text':'        Use blocked code ','line_number':27281,'multiline':True]['text':' Computing MIN ','line_number':27285,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':27290,'multiline':True]['text':'              Apply H' to A(i+ib:m,i:n) from the right ','line_number':27299,'multiline':True]['text':'           Apply H' to columns i:n of current block ','line_number':27309,'multiline':True]['text':'           Set columns 1:i-1 of current block to zero ','line_number':27315,'multiline':True]['text':' L30: ','line_number':27323,'multiline':True]['text':' L40: ','line_number':27325,'multiline':True]['text':' L50: ','line_number':27327,'multiline':True]['text':'     End of ZUNGLQ ','line_number':27334,'multiline':True]['text':' zunglq_ ','line_number':27336,'multiline':True]['text':' Subroutine ','line_number':27338,'multiline':True]['text':' System generated locals ','line_number':27342,'multiline':True]['text':' Local variables ','line_number':27345,'multiline':True]['text':' Subroutine ','line_number':27347,'multiline':True]['text':' Subroutine ','line_number':27352,'multiline':True]['text':' Subroutine ','line_number':27357,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNGQR generates an M-by-N complex matrix Q with orthonormal columns,
    which is defined as the first N columns of a product of K elementary
    reflectors of order M

          Q  =  H(1) H(2) . . . H(k)

    as returned by ZGEQRF.

    Arguments
    =========

    M       (input) INTEGER
            The number of rows of the matrix Q. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix Q. M >= N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines the
            matrix Q. N >= K >= 0.

    A       (input/output) COMPLEX*16 array, dimension (LDA,N)
            On entry, the i-th column must contain the vector which
            defines the elementary reflector H(i), for i = 1,2,...,k, as
            returned by ZGEQRF in the first k columns of its array
            argument A.
            On exit, the M-by-N matrix Q.

    LDA     (input) INTEGER
            The first dimension of the array A. LDA >= max(1,M).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEQRF.

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK. LWORK >= max(1,N).
            For optimum performance LWORK >= N*NB, where NB is the
            optimal blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument has an illegal value

    =====================================================================


       Test the input arguments
','line_number':27364,'multiline':True]['text':' Parameter adjustments ','line_number':27432,'multiline':True]['text':' Function Body ','line_number':27439,'multiline':True]['text':'     Quick return if possible ','line_number':27464,'multiline':True]['text':'
          Determine when to cross over from blocked to unblocked code.

   Computing MAX
','line_number':27476,'multiline':True]['text':'           Determine if workspace is large enough for blocked code. ','line_number':27486,'multiline':True]['text':'
                Not enough workspace to use optimal NB:  reduce NB and
                determine the minimum value of NB.
','line_number':27492,'multiline':True]['text':' Computing MAX ','line_number':27498,'multiline':True]['text':'
          Use blocked code after the last block.
          The first kk columns are handled by the block method.
','line_number':27508,'multiline':True]['text':' Computing MIN ','line_number':27514,'multiline':True]['text':'        Set A(1:kk,kk+1:n) to zero. ','line_number':27518,'multiline':True]['text':' L10: ','line_number':27526,'multiline':True]['text':' L20: ','line_number':27528,'multiline':True]['text':'     Use unblocked code for the last or only block. ','line_number':27534,'multiline':True]['text':'        Use blocked code ','line_number':27546,'multiline':True]['text':' Computing MIN ','line_number':27550,'multiline':True]['text':'
                Form the triangular factor of the block reflector
                H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':27555,'multiline':True]['text':'              Apply H to A(i:m,i+ib:n) from the left ','line_number':27564,'multiline':True]['text':'           Apply H to rows i:m of current block ','line_number':27574,'multiline':True]['text':'           Set rows 1:i-1 of current block to zero ','line_number':27580,'multiline':True]['text':' L30: ','line_number':27588,'multiline':True]['text':' L40: ','line_number':27590,'multiline':True]['text':' L50: ','line_number':27592,'multiline':True]['text':'     End of ZUNGQR ','line_number':27599,'multiline':True]['text':' zungqr_ ','line_number':27601,'multiline':True]['text':' Subroutine ','line_number':27603,'multiline':True]['text':' System generated locals ','line_number':27607,'multiline':True]['text':' Local variables ','line_number':27611,'multiline':True]['text':' Subroutine ','line_number':27617,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNM2L overwrites the general complex m-by-n matrix C with

          Q * C  if SIDE = 'L' and TRANS = 'N', or

          Q'* C  if SIDE = 'L' and TRANS = 'C', or

          C * Q  if SIDE = 'R' and TRANS = 'N', or

          C * Q' if SIDE = 'R' and TRANS = 'C',

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k) . . . H(2) H(1)

    as returned by ZGEQLF. Q is of order m if SIDE = 'L' and of order n
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q' from the Left
            = 'R': apply Q or Q' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q' (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            ZGEQLF in the last k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEQLF.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the m-by-n matrix C.
            On exit, C is overwritten by Q*C or Q'*C or C*Q' or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX*16 array, dimension
                                     (N) if SIDE = 'L',
                                     (M) if SIDE = 'R'

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':27623,'multiline':True]['text':' Parameter adjustments ','line_number':27710,'multiline':True]['text':' Function Body ','line_number':27720,'multiline':True]['text':'     NQ is the order of Q ','line_number':27725,'multiline':True]['text':'     Quick return if possible ','line_number':27753,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m-k+i,1:n) ','line_number':27780,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m,1:n-k+i) ','line_number':27785,'multiline':True]['text':'        Apply H(i) or H(i)' ','line_number':27790,'multiline':True]['text':' L10: ','line_number':27807,'multiline':True]['text':'     End of ZUNM2L ','line_number':27811,'multiline':True]['text':' zunm2l_ ','line_number':27813,'multiline':True]['text':' Subroutine ','line_number':27815,'multiline':True]['text':' System generated locals ','line_number':27819,'multiline':True]['text':' Local variables ','line_number':27823,'multiline':True]['text':' Subroutine ','line_number':27829,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNM2R overwrites the general complex m-by-n matrix C with

          Q * C  if SIDE = 'L' and TRANS = 'N', or

          Q'* C  if SIDE = 'L' and TRANS = 'C', or

          C * Q  if SIDE = 'R' and TRANS = 'N', or

          C * Q' if SIDE = 'R' and TRANS = 'C',

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(1) H(2) . . . H(k)

    as returned by ZGEQRF. Q is of order m if SIDE = 'L' and of order n
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q' from the Left
            = 'R': apply Q or Q' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q' (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            ZGEQRF in the first k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEQRF.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the m-by-n matrix C.
            On exit, C is overwritten by Q*C or Q'*C or C*Q' or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX*16 array, dimension
                                     (N) if SIDE = 'L',
                                     (M) if SIDE = 'R'

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':27835,'multiline':True]['text':' Parameter adjustments ','line_number':27922,'multiline':True]['text':' Function Body ','line_number':27932,'multiline':True]['text':'     NQ is the order of Q ','line_number':27937,'multiline':True]['text':'     Quick return if possible ','line_number':27965,'multiline':True]['text':'           H(i) or H(i)' is applied to C(i:m,1:n) ','line_number':27994,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m,i:n) ','line_number':28000,'multiline':True]['text':'        Apply H(i) or H(i)' ','line_number':28006,'multiline':True]['text':' L10: ','line_number':28023,'multiline':True]['text':'     End of ZUNM2R ','line_number':28027,'multiline':True]['text':' zunm2r_ ','line_number':28029,'multiline':True]['text':' Subroutine ','line_number':28031,'multiline':True]['text':' System generated locals ','line_number':28036,'multiline':True]['text':' Local variables ','line_number':28041,'multiline':True]['text':' Subroutine ','line_number':28046,'multiline':True]['text':' Subroutine ','line_number':28053,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    If VECT = 'Q', ZUNMBR overwrites the general complex M-by-N matrix C
    with
                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    If VECT = 'P', ZUNMBR overwrites the general complex M-by-N matrix C
    with
                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      P * C          C * P
    TRANS = 'C':      P**H * C       C * P**H

    Here Q and P**H are the unitary matrices determined by ZGEBRD when
    reducing a complex matrix A to bidiagonal form: A = Q * B * P**H. Q
    and P**H are defined as products of elementary reflectors H(i) and
    G(i) respectively.

    Let nq = m if SIDE = 'L' and nq = n if SIDE = 'R'. Thus nq is the
    order of the unitary matrix Q or P**H that is applied.

    If VECT = 'Q', A is assumed to have been an NQ-by-K matrix:
    if nq >= k, Q = H(1) H(2) . . . H(k);
    if nq < k, Q = H(1) H(2) . . . H(nq-1).

    If VECT = 'P', A is assumed to have been a K-by-NQ matrix:
    if k < nq, P = G(1) G(2) . . . G(k);
    if k >= nq, P = G(1) G(2) . . . G(nq-1).

    Arguments
    =========

    VECT    (input) CHARACTER*1
            = 'Q': apply Q or Q**H;
            = 'P': apply P or P**H.

    SIDE    (input) CHARACTER*1
            = 'L': apply Q, Q**H, P or P**H from the Left;
            = 'R': apply Q, Q**H, P or P**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q or P;
            = 'C':  Conjugate transpose, apply Q**H or P**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            If VECT = 'Q', the number of columns in the original
            matrix reduced by ZGEBRD.
            If VECT = 'P', the number of rows in the original
            matrix reduced by ZGEBRD.
            K >= 0.

    A       (input) COMPLEX*16 array, dimension
                                  (LDA,min(nq,K)) if VECT = 'Q'
                                  (LDA,nq)        if VECT = 'P'
            The vectors which define the elementary reflectors H(i) and
            G(i), whose products determine the matrices Q and P, as
            returned by ZGEBRD.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If VECT = 'Q', LDA >= max(1,nq);
            if VECT = 'P', LDA >= max(1,min(nq,K)).

    TAU     (input) COMPLEX*16 array, dimension (min(nq,K))
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i) or G(i) which determines Q or P, as returned
            by ZGEBRD in the array argument TAUQ or TAUP.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q
            or P*C or P**H*C or C*P or C*P**H.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M);
            if N = 0 or M = 0, LWORK >= 1.
            For optimum performance LWORK >= max(1,N*NB) if SIDE = 'L',
            and LWORK >= max(1,M*NB) if SIDE = 'R', where NB is the
            optimal blocksize. (NB = 0 if M = 0 or N = 0.)

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':28060,'multiline':True]['text':' Parameter adjustments ','line_number':28178,'multiline':True]['text':' Function Body ','line_number':28188,'multiline':True]['text':'     NQ is the order of Q or P and NW is the minimum dimension of WORK ','line_number':28195,'multiline':True]['text':' if(complicated condition) ','line_number':28219,'multiline':True]['text':' Computing MAX ','line_number':28220,'multiline':True]['text':' Writing concatenation ','line_number':28235,'multiline':True]['text':' Writing concatenation ','line_number':28244,'multiline':True]['text':' Writing concatenation ','line_number':28255,'multiline':True]['text':' Writing concatenation ','line_number':28264,'multiline':True]['text':' Computing MAX ','line_number':28274,'multiline':True]['text':'     Quick return if possible ','line_number':28291,'multiline':True]['text':'        Apply Q ','line_number':28299,'multiline':True]['text':'           Q was determined by a call to ZGEBRD with nq >= k ','line_number':28303,'multiline':True]['text':'           Q was determined by a call to ZGEBRD with nq < k ','line_number':28309,'multiline':True]['text':'        Apply P ','line_number':28328,'multiline':True]['text':'           P was determined by a call to ZGEBRD with nq > k ','line_number':28337,'multiline':True]['text':'           P was determined by a call to ZGEBRD with nq <= k ','line_number':28343,'multiline':True]['text':'     End of ZUNMBR ','line_number':28365,'multiline':True]['text':' zunmbr_ ','line_number':28367,'multiline':True]['text':' Subroutine ','line_number':28369,'multiline':True]['text':' System generated locals ','line_number':28374,'multiline':True]['text':' Local variables ','line_number':28379,'multiline':True]['text':' Subroutine ','line_number':28384,'multiline':True]['text':' Subroutine ','line_number':28389,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNMHR overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix of order nq, with nq = m if
    SIDE = 'L' and nq = n if SIDE = 'R'. Q is defined as the product of
    IHI-ILO elementary reflectors, as returned by ZGEHRD:

    Q = H(ilo) H(ilo+1) . . . H(ihi-1).

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q**H (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    ILO     (input) INTEGER
    IHI     (input) INTEGER
            ILO and IHI must have the same values as in the previous call
            of ZGEHRD. Q is equal to the unit matrix except in the
            submatrix Q(ilo+1:ihi,ilo+1:ihi).
            If SIDE = 'L', then 1 <= ILO <= IHI <= M, if M > 0, and
            ILO = 1 and IHI = 0, if M = 0;
            if SIDE = 'R', then 1 <= ILO <= IHI <= N, if N > 0, and
            ILO = 1 and IHI = 0, if N = 0.

    A       (input) COMPLEX*16 array, dimension
                                 (LDA,M) if SIDE = 'L'
                                 (LDA,N) if SIDE = 'R'
            The vectors which define the elementary reflectors, as
            returned by ZGEHRD.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            LDA >= max(1,M) if SIDE = 'L'; LDA >= max(1,N) if SIDE = 'R'.

    TAU     (input) COMPLEX*16 array, dimension
                                 (M-1) if SIDE = 'L'
                                 (N-1) if SIDE = 'R'
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEHRD.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':28394,'multiline':True]['text':' Parameter adjustments ','line_number':28492,'multiline':True]['text':' Function Body ','line_number':28502,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':28508,'multiline':True]['text':' Writing concatenation ','line_number':28540,'multiline':True]['text':' Writing concatenation ','line_number':28547,'multiline':True]['text':'     Quick return if possible ','line_number':28566,'multiline':True]['text':'     End of ZUNMHR ','line_number':28591,'multiline':True]['text':' zunmhr_ ','line_number':28593,'multiline':True]['text':' Subroutine ','line_number':28595,'multiline':True]['text':' System generated locals ','line_number':28599,'multiline':True]['text':' Local variables ','line_number':28603,'multiline':True]['text':' Subroutine ','line_number':28609,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNML2 overwrites the general complex m-by-n matrix C with

          Q * C  if SIDE = 'L' and TRANS = 'N', or

          Q'* C  if SIDE = 'L' and TRANS = 'C', or

          C * Q  if SIDE = 'R' and TRANS = 'N', or

          C * Q' if SIDE = 'R' and TRANS = 'C',

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k)' . . . H(2)' H(1)'

    as returned by ZGELQF. Q is of order m if SIDE = 'L' and of order n
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q' from the Left
            = 'R': apply Q or Q' from the Right

    TRANS   (input) CHARACTER*1
            = 'N': apply Q  (No transpose)
            = 'C': apply Q' (Conjugate transpose)

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX*16 array, dimension
                                 (LDA,M) if SIDE = 'L',
                                 (LDA,N) if SIDE = 'R'
            The i-th row must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            ZGELQF in the first k rows of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,K).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGELQF.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the m-by-n matrix C.
            On exit, C is overwritten by Q*C or Q'*C or C*Q' or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace) COMPLEX*16 array, dimension
                                     (N) if SIDE = 'L',
                                     (M) if SIDE = 'R'

    INFO    (output) INTEGER
            = 0: successful exit
            < 0: if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':28615,'multiline':True]['text':' Parameter adjustments ','line_number':28702,'multiline':True]['text':' Function Body ','line_number':28712,'multiline':True]['text':'     NQ is the order of Q ','line_number':28717,'multiline':True]['text':'     Quick return if possible ','line_number':28745,'multiline':True]['text':'           H(i) or H(i)' is applied to C(i:m,1:n) ','line_number':28774,'multiline':True]['text':'           H(i) or H(i)' is applied to C(1:m,i:n) ','line_number':28780,'multiline':True]['text':'        Apply H(i) or H(i)' ','line_number':28786,'multiline':True]['text':' L10: ','line_number':28811,'multiline':True]['text':'     End of ZUNML2 ','line_number':28815,'multiline':True]['text':' zunml2_ ','line_number':28817,'multiline':True]['text':' Subroutine ','line_number':28819,'multiline':True]['text':' System generated locals ','line_number':28824,'multiline':True]['text':' Local variables ','line_number':28830,'multiline':True]['text':' was [65][64] ','line_number':28832,'multiline':True]['text':' Subroutine ','line_number':28837,'multiline':True]['text':' Subroutine ','line_number':28842,'multiline':True]['text':' Subroutine ','line_number':28848,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNMLQ overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k)' . . . H(2)' H(1)'

    as returned by ZGELQF. Q is of order M if SIDE = 'L' and of order N
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Conjugate transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX*16 array, dimension
                                 (LDA,M) if SIDE = 'L',
                                 (LDA,N) if SIDE = 'R'
            The i-th row must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            ZGELQF in the first k rows of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A. LDA >= max(1,K).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGELQF.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':28856,'multiline':True]['text':' Parameter adjustments ','line_number':28951,'multiline':True]['text':' Function Body ','line_number':28961,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':28967,'multiline':True]['text':'
          Determine the block size.  NB may be at most NBMAX, where NBMAX
          is used to define the local array T.

   Computing MIN
   Writing concatenation
','line_number':28996,'multiline':True]['text':'     Quick return if possible ','line_number':29021,'multiline':True]['text':'
   Computing MAX
   Writing concatenation
','line_number':29034,'multiline':True]['text':'        Use unblocked code ','line_number':29051,'multiline':True]['text':'        Use blocked code ','line_number':29057,'multiline':True]['text':' Computing MIN ','line_number':29086,'multiline':True]['text':'
             Form the triangular factor of the block reflector
             H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':29090,'multiline':True]['text':'              H or H' is applied to C(i:m,1:n) ','line_number':29100,'multiline':True]['text':'              H or H' is applied to C(1:m,i:n) ','line_number':29106,'multiline':True]['text':'           Apply H or H' ','line_number':29112,'multiline':True]['text':' L10: ','line_number':29117,'multiline':True]['text':'     End of ZUNMLQ ','line_number':29123,'multiline':True]['text':' zunmlq_ ','line_number':29125,'multiline':True]['text':' Subroutine ','line_number':29127,'multiline':True]['text':' System generated locals ','line_number':29132,'multiline':True]['text':' Local variables ','line_number':29138,'multiline':True]['text':' was [65][64] ','line_number':29140,'multiline':True]['text':' Subroutine ','line_number':29145,'multiline':True]['text':' Subroutine ','line_number':29150,'multiline':True]['text':' Subroutine ','line_number':29156,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNMQL overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(k) . . . H(2) H(1)

    as returned by ZGEQLF. Q is of order M if SIDE = 'L' and of order N
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            ZGEQLF in the last k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEQLF.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':29163,'multiline':True]['text':' Parameter adjustments ','line_number':29258,'multiline':True]['text':' Function Body ','line_number':29268,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':29274,'multiline':True]['text':'
             Determine the block size.  NB may be at most NBMAX, where
             NBMAX is used to define the local array T.

   Computing MIN
   Writing concatenation
','line_number':29304,'multiline':True]['text':'     Quick return if possible ','line_number':29334,'multiline':True]['text':'
   Computing MAX
   Writing concatenation
','line_number':29346,'multiline':True]['text':'        Use unblocked code ','line_number':29363,'multiline':True]['text':'        Use blocked code ','line_number':29369,'multiline':True]['text':' Computing MIN ','line_number':29390,'multiline':True]['text':'
             Form the triangular factor of the block reflector
             H = H(i+ib-1) . . . H(i+1) H(i)
','line_number':29394,'multiline':True]['text':'              H or H' is applied to C(1:m-k+i+ib-1,1:n) ','line_number':29404,'multiline':True]['text':'              H or H' is applied to C(1:m,1:n-k+i+ib-1) ','line_number':29409,'multiline':True]['text':'           Apply H or H' ','line_number':29414,'multiline':True]['text':' L10: ','line_number':29419,'multiline':True]['text':'     End of ZUNMQL ','line_number':29425,'multiline':True]['text':' zunmql_ ','line_number':29427,'multiline':True]['text':' Subroutine ','line_number':29429,'multiline':True]['text':' System generated locals ','line_number':29434,'multiline':True]['text':' Local variables ','line_number':29440,'multiline':True]['text':' was [65][64] ','line_number':29442,'multiline':True]['text':' Subroutine ','line_number':29447,'multiline':True]['text':' Subroutine ','line_number':29452,'multiline':True]['text':' Subroutine ','line_number':29458,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNMQR overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix defined as the product of k
    elementary reflectors

          Q = H(1) H(2) . . . H(k)

    as returned by ZGEQRF. Q is of order M if SIDE = 'L' and of order N
    if SIDE = 'R'.

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Conjugate transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    K       (input) INTEGER
            The number of elementary reflectors whose product defines
            the matrix Q.
            If SIDE = 'L', M >= K >= 0;
            if SIDE = 'R', N >= K >= 0.

    A       (input) COMPLEX*16 array, dimension (LDA,K)
            The i-th column must contain the vector which defines the
            elementary reflector H(i), for i = 1,2,...,k, as returned by
            ZGEQRF in the first k columns of its array argument A.
            A is modified by the routine but restored on exit.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            If SIDE = 'L', LDA >= max(1,M);
            if SIDE = 'R', LDA >= max(1,N).

    TAU     (input) COMPLEX*16 array, dimension (K)
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZGEQRF.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >= M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':29465,'multiline':True]['text':' Parameter adjustments ','line_number':29560,'multiline':True]['text':' Function Body ','line_number':29570,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':29576,'multiline':True]['text':'
          Determine the block size.  NB may be at most NBMAX, where NBMAX
          is used to define the local array T.

   Computing MIN
   Writing concatenation
','line_number':29605,'multiline':True]['text':'     Quick return if possible ','line_number':29630,'multiline':True]['text':'
   Computing MAX
   Writing concatenation
','line_number':29643,'multiline':True]['text':'        Use unblocked code ','line_number':29660,'multiline':True]['text':'        Use blocked code ','line_number':29666,'multiline':True]['text':' Computing MIN ','line_number':29689,'multiline':True]['text':'
             Form the triangular factor of the block reflector
             H = H(i) H(i+1) . . . H(i+ib-1)
','line_number':29693,'multiline':True]['text':'              H or H' is applied to C(i:m,1:n) ','line_number':29704,'multiline':True]['text':'              H or H' is applied to C(1:m,i:n) ','line_number':29710,'multiline':True]['text':'           Apply H or H' ','line_number':29716,'multiline':True]['text':' L10: ','line_number':29721,'multiline':True]['text':'     End of ZUNMQR ','line_number':29727,'multiline':True]['text':' zunmqr_ ','line_number':29729,'multiline':True]['text':' Subroutine ','line_number':29731,'multiline':True]['text':' System generated locals ','line_number':29736,'multiline':True]['text':' Local variables ','line_number':29741,'multiline':True]['text':' Subroutine ','line_number':29747,'multiline':True]['text':' Subroutine ','line_number':29752,'multiline':True]['text':'
    -- LAPACK routine (version 3.2) --
    -- LAPACK is a software package provided by Univ. of Tennessee,    --
    -- Univ. of California Berkeley, Univ. of Colorado Denver and NAG Ltd..--
       November 2006


    Purpose
    =======

    ZUNMTR overwrites the general complex M-by-N matrix C with

                    SIDE = 'L'     SIDE = 'R'
    TRANS = 'N':      Q * C          C * Q
    TRANS = 'C':      Q**H * C       C * Q**H

    where Q is a complex unitary matrix of order nq, with nq = m if
    SIDE = 'L' and nq = n if SIDE = 'R'. Q is defined as the product of
    nq-1 elementary reflectors, as returned by ZHETRD:

    if UPLO = 'U', Q = H(nq-1) . . . H(2) H(1);

    if UPLO = 'L', Q = H(1) H(2) . . . H(nq-1).

    Arguments
    =========

    SIDE    (input) CHARACTER*1
            = 'L': apply Q or Q**H from the Left;
            = 'R': apply Q or Q**H from the Right.

    UPLO    (input) CHARACTER*1
            = 'U': Upper triangle of A contains elementary reflectors
                   from ZHETRD;
            = 'L': Lower triangle of A contains elementary reflectors
                   from ZHETRD.

    TRANS   (input) CHARACTER*1
            = 'N':  No transpose, apply Q;
            = 'C':  Conjugate transpose, apply Q**H.

    M       (input) INTEGER
            The number of rows of the matrix C. M >= 0.

    N       (input) INTEGER
            The number of columns of the matrix C. N >= 0.

    A       (input) COMPLEX*16 array, dimension
                                 (LDA,M) if SIDE = 'L'
                                 (LDA,N) if SIDE = 'R'
            The vectors which define the elementary reflectors, as
            returned by ZHETRD.

    LDA     (input) INTEGER
            The leading dimension of the array A.
            LDA >= max(1,M) if SIDE = 'L'; LDA >= max(1,N) if SIDE = 'R'.

    TAU     (input) COMPLEX*16 array, dimension
                                 (M-1) if SIDE = 'L'
                                 (N-1) if SIDE = 'R'
            TAU(i) must contain the scalar factor of the elementary
            reflector H(i), as returned by ZHETRD.

    C       (input/output) COMPLEX*16 array, dimension (LDC,N)
            On entry, the M-by-N matrix C.
            On exit, C is overwritten by Q*C or Q**H*C or C*Q**H or C*Q.

    LDC     (input) INTEGER
            The leading dimension of the array C. LDC >= max(1,M).

    WORK    (workspace/output) COMPLEX*16 array, dimension (MAX(1,LWORK))
            On exit, if INFO = 0, WORK(1) returns the optimal LWORK.

    LWORK   (input) INTEGER
            The dimension of the array WORK.
            If SIDE = 'L', LWORK >= max(1,N);
            if SIDE = 'R', LWORK >= max(1,M).
            For optimum performance LWORK >= N*NB if SIDE = 'L', and
            LWORK >=M*NB if SIDE = 'R', where NB is the optimal
            blocksize.

            If LWORK = -1, then a workspace query is assumed; the routine
            only calculates the optimal size of the WORK array, returns
            this value as the first entry of the WORK array, and no error
            message related to LWORK is issued by XERBLA.

    INFO    (output) INTEGER
            = 0:  successful exit
            < 0:  if INFO = -i, the i-th argument had an illegal value

    =====================================================================


       Test the input arguments
','line_number':29759,'multiline':True]['text':' Parameter adjustments ','line_number':29855,'multiline':True]['text':' Function Body ','line_number':29865,'multiline':True]['text':'     NQ is the order of Q and NW is the minimum dimension of WORK ','line_number':29871,'multiline':True]['text':' Writing concatenation ','line_number':29902,'multiline':True]['text':' Writing concatenation ','line_number':29911,'multiline':True]['text':' Writing concatenation ','line_number':29922,'multiline':True]['text':' Writing concatenation ','line_number':29931,'multiline':True]['text':'     Quick return if possible ','line_number':29953,'multiline':True]['text':'        Q was determined by a call to ZHETRD with UPLO = 'U' ','line_number':29970,'multiline':True]['text':'        Q was determined by a call to ZHETRD with UPLO = 'L' ','line_number':29977,'multiline':True]['text':'     End of ZUNMTR ','line_number':29993,'multiline':True]['text':' zunmtr_ ','line_number':29995,'multiline':True]