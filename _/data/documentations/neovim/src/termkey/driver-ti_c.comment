['text':' we want strdup()','line_number':1,'multiline':False]['text':' curses.h has just polluted our namespace. We want this back ','line_number':13,'multiline':True]['text':' THIS LIST MUST REMAIN SORTED! ','line_number':41,'multiline':True]['text':' Not quite, but it's the best we can do','line_number':64,'multiline':False]['text':' Not quite, but it's the best we can do','line_number':69,'multiline':False]['text':' To be efficient at lookups, we store the byte sequence => keyinfo mapping
 * in a trie. This avoids a slow linear search through a flat list of
 * sequences. Because it is likely most nodes will be very sparse, we optimise
 * vector to store an extent map after the database is loaded.
 ','line_number':106,'multiline':True]['text':' INCLUSIVE endpoints of the extent range ','line_number':128,'multiline':True]['text':' dynamic size at allocation time ','line_number':129,'multiline':True]['text':' only valid until first 'start' call ','line_number':136,'multiline':True]['text':' only valid until first 'start' call ','line_number':138,'multiline':True]['text':' Never reached but keeps compiler happy','line_number':196,'multiline':False]['text':' Find the real bounds','line_number':230,'multiline':False]['text':' Have to cast away the const. But it's OK - we know terminfo won't really
    * modify term ','line_number':287,'multiline':True]['text':' First the regular key strings
   ','line_number':298,'multiline':True]['text':' Maybe it has a shifted version ','line_number':312,'multiline':True]['text':' Now the F<digit> keys
   ','line_number':322,'multiline':True]['text':' Finally mouse mode ','line_number':336,'multiline':True]['text':' Take copies of these terminfo strings, in case we build multiple termkey
   * instances for multiple different termtypes, and it's different by the
   * time we want to use it
   ','line_number':341,'multiline':True]['text':' ti->unibi may be NULL if errno == ENOENT. That means the terminal wasn't
   * known. Lets keep going because if we get getstr hook that might invent
   * new strings for us
   ','line_number':402,'multiline':True]['text':' Have to cast away the const. But it's OK - we know terminfo won't really
    * modify term ','line_number':412,'multiline':True]['text':' The terminfo database will contain keys in application cursor key mode.
   * We may need to enable that mode
   ','line_number':437,'multiline':True]['text':' There's no point trying to write() to a pipe ','line_number':441,'multiline':True]['text':' Can't call putp or tputs because they suck and don't give us fd control','line_number':450,'multiline':False]['text':' There's no point trying to write() to a pipe ','line_number':472,'multiline':True]['text':' The terminfo database will contain keys in application cursor key mode.
   * We may need to enable that mode
   ','line_number':481,'multiline':True]['text':' Can't call putp or tputs because they suck and don't give us fd control','line_number':485,'multiline':False]['text':' If p is not NULL then we hadn't walked off the end yet, so we have a','line_number':565,'multiline':False]['text':' partial match','line_number':566,'multiline':False]['text':' Unsigned because we'll be using it as an array subscript','line_number':578,'multiline':False]['text':' Intermediate node','line_number':592,'multiline':False]['text':' Final key node','line_number':595,'multiline':False]