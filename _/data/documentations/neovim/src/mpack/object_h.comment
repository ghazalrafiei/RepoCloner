['text':' Storing integer in pointers in undefined behavior according to the C
 * standard. Define a union type to accommodate arbitrary user data associated
 * with nodes(and with requests in rpc.h). ','line_number':24,'multiline':True]['text':' flag to determine if the key was visited when traversing a map ','line_number':37,'multiline':True]['text':' allow 2 instances mpack_data_t per node. the reason is that when
   * serializing, the user may need to keep track of traversal state besides the
   * parent node reference ','line_number':39,'multiline':True]['text':' Some compilers warn against anonymous structs:
 * https://github.com/libmpack/libmpack/issues/6 ','line_number':55,'multiline':True]['text':' MPACK_OBJECT_H ','line_number':86,'multiline':True]