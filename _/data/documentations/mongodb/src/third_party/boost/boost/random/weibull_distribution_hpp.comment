['text':' boost random/weibull_distribution.hpp header file
 *
 * Copyright Steven Watanabe 2010
 * Distributed under the Boost Software License, Version 1.0. (See
 * accompanying file LICENSE_1_0.txt or copy at
 * http://www.boost.org/LICENSE_1_0.txt)
 *
 * See http://www.boost.org for most recent version including documentation.
 *
 * $Id$
 ','line_number':1,'multiline':True]['text':'*
 * The Weibull distribution is a real valued distribution with two
 * parameters a and b, producing values >= 0.
 *
 * It has \f$\displaystyle p(x) = \frac{a}{b}\left(\frac{x}{b}\right)^{a-1}e^{-\left(\frac{x}{b}\right)^a}\f$.
 ','line_number':27,'multiline':True]['text':'*
         * Constructs a @c param_type from the "a" and "b" parameters
         * of the distribution.
         *
         * Requires: a > 0 && b > 0
         ','line_number':43,'multiline':True]['text':'* Returns the "a" parameter of the distribtuion. ','line_number':53,'multiline':True]['text':'* Returns the "b" parameter of the distribution. ','line_number':55,'multiline':True]['text':'* Writes a @c param_type to a @c std::ostream. ','line_number':58,'multiline':True]['text':'* Reads a @c param_type from a @c std::istream. ','line_number':62,'multiline':True]['text':'* Returns true if the two sets of parameters are the same. ','line_number':66,'multiline':True]['text':'* Returns true if the two sets of parameters are the different. ','line_number':70,'multiline':True]['text':'*
     * Constructs a @c weibull_distribution from its "a" and "b" parameters.
     *
     * Requires: a > 0 && b > 0
     ','line_number':78,'multiline':True]['text':'* Constructs a @c weibull_distribution from its parameters. ','line_number':86,'multiline':True]['text':'*
     * Returns a random variate distributed according to the
     * @c weibull_distribution.
     ','line_number':91,'multiline':True]['text':'*
     * Returns a random variate distributed accordint to the Weibull
     * distribution with parameters specified by @c param.
     ','line_number':103,'multiline':True]['text':'* Returns the "a" parameter of the distribution. ','line_number':113,'multiline':True]['text':'* Returns the "b" parameter of the distribution. ','line_number':115,'multiline':True]['text':'* Returns the smallest value that the distribution can produce. ','line_number':118,'multiline':True]['text':'* Returns the largest value that the distribution can produce. ','line_number':120,'multiline':True]['text':'* Returns the parameters of the distribution. ','line_number':124,'multiline':True]['text':'* Sets the parameters of the distribution. ','line_number':126,'multiline':True]['text':'*
     * Effects: Subsequent uses of the distribution do not depend
     * on values produced by any engine prior to invoking reset.
     ','line_number':133,'multiline':True]['text':'* Writes a @c weibull_distribution to a @c std::ostream. ','line_number':139,'multiline':True]['text':'* Reads a @c weibull_distribution from a @c std::istream. ','line_number':146,'multiline':True]['text':'*
     * Returns true if the two instances of @c weibull_distribution will
     * return identical sequences of values given equal generators.
     ','line_number':156,'multiline':True]['text':'*
     * Returns true if the two instances of @c weibull_distribution will
     * return different sequences of values given equal generators.
     ','line_number':163,'multiline':True]['text':' namespace random','line_number':174,'multiline':False]['text':' namespace boost','line_number':175,'multiline':False]['text':' BOOST_RANDOM_WEIBULL_DISTRIBUTION_HPP','line_number':177,'multiline':False]