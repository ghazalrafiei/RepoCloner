['text':' boost random/uniform_on_sphere.hpp header file
 *
 * Copyright Jens Maurer 2000-2001
 * Copyright Steven Watanabe 2011
 * Distributed under the Boost Software License, Version 1.0. (See
 * accompanying file LICENSE_1_0.txt or copy at
 * http://www.boost.org/LICENSE_1_0.txt)
 *
 * See http://www.boost.org for most recent version including documentation.
 *
 * $Id$
 *
 * Revision history
 *  2001-02-18  moved to individual header files
 ','line_number':1,'multiline':True]['text':' std::transform','line_number':21,'multiline':False]['text':' std::bind2nd, std::divides','line_number':22,'multiline':False]['text':'*
 * Instantiations of class template uniform_on_sphere model a
 * \random_distribution. Such a distribution produces random
 * numbers uniformly distributed on the unit sphere of arbitrary
 * dimension @c dim. The @c Cont template parameter must be a STL-like
 * container type with begin and end operations returning non-const
 * ForwardIterators of type @c Cont::iterator. 
 ','line_number':31,'multiline':True]['text':'*
         * Constructs the parameters of a uniform_on_sphere
         * distribution, given the dimension of the sphere.
         ','line_number':52,'multiline':True]['text':'* Returns the dimension of the sphere. ','line_number':61,'multiline':True]['text':'* Writes the parameters to a @c std::ostream. ','line_number':64,'multiline':True]['text':'* Reads the parameters from a @c std::istream. ','line_number':71,'multiline':True]['text':'* Returns true if the two sets of parameters are equal. ','line_number':78,'multiline':True]['text':'* Returns true if the two sets of parameters are different. ','line_number':82,'multiline':True]['text':'*
     * Constructs a @c uniform_on_sphere distribution.
     * @c dim is the dimension of the sphere.
     *
     * Requires: dim >= 0
     ','line_number':89,'multiline':True]['text':'*
     * Constructs a @c uniform_on_sphere distribution from its parameters.
     ','line_number':98,'multiline':True]['text':' compiler-generated copy ctor and assignment operator are fine','line_number':104,'multiline':False]['text':'* Returns the dimension of the sphere. ','line_number':106,'multiline':True]['text':'* Returns the parameters of the distribution. ','line_number':109,'multiline':True]['text':'* Sets the parameters of the distribution. ','line_number':111,'multiline':True]['text':'*
     * Returns the smallest value that the distribution can produce.
     * Note that this is required to approximate the standard library's
     * requirements.  The behavior is defined according to lexicographical
     * comparison so that for a container type of std::vector,
     * dist.min() <= x <= dist.max() where x is any value produced
     * by the distribution.
     ','line_number':118,'multiline':True]['text':'*
     * Returns the largest value that the distribution can produce.
     * Note that this is required to approximate the standard library's
     * requirements.  The behavior is defined according to lexicographical
     * comparison so that for a container type of std::vector,
     * dist.min() <= x <= dist.max() where x is any value produced
     * by the distribution.
     ','line_number':134,'multiline':True]['text':'*
     * Effects: Subsequent uses of the distribution do not depend
     * on values produced by any engine prior to invoking reset.
     ','line_number':151,'multiline':True]['text':'*
     * Returns a point uniformly distributed over the surface of
     * a sphere of dimension dim().
     ','line_number':157,'multiline':True]['text':' for all i: result[i] /= sqrt(sqsum)','line_number':227,'multiline':False]['text':'*
     * Returns a point uniformly distributed over the surface of
     * a sphere of dimension param.dim().
     ','line_number':239,'multiline':True]['text':'* Writes the distribution to a @c std::ostream. ','line_number':249,'multiline':True]['text':'* Reads the distribution from a @c std::istream. ','line_number':256,'multiline':True]['text':'*
     * Returns true if the two distributions will produce identical
     * sequences of values, given equal generators.
     ','line_number':264,'multiline':True]['text':'*
     * Returns true if the two distributions may produce different
     * sequences of values, given equal generators.
     ','line_number':271,'multiline':True]['text':' namespace random','line_number':282,'multiline':False]['text':' namespace boost','line_number':286,'multiline':False]['text':' BOOST_RANDOM_UNIFORM_ON_SPHERE_HPP','line_number':288,'multiline':False]