['text':'-
 * Copyright (c) 2014-present MongoDB, Inc.
 * Copyright (c) 2008-2014 WiredTiger, Inc.
 *	All rights reserved.
 *
 * See the file LICENSE for redistribution information.
 ','line_number':1,'multiline':True]['text':'
 * __wt_thread_create --
 *     Create a new thread of control.
 ','line_number':11,'multiline':True]['text':'
     * Creating a thread isn't a memory barrier, but WiredTiger commonly sets flags and or state and
     * then expects worker threads to start. Include a barrier to ensure safety in those cases.
     ','line_number':19,'multiline':True]['text':' Spawn a new thread of control. ','line_number':25,'multiline':True]['text':'
 * __wt_thread_join --
 *     Wait for a thread of control to exit.
 ','line_number':35,'multiline':True]['text':' Only attempt to join if thread was created successfully ','line_number':45,'multiline':True]['text':'
     * Joining a thread isn't a memory barrier, but WiredTiger commonly sets flags and or state and
     * then expects worker threads to halt. Include a barrier to ensure safety in those cases.
     ','line_number':50,'multiline':True]['text':' If we fail to wait, we will leak handles, do not continue. ','line_number':60,'multiline':True]['text':'
 * __wt_thread_id --
 *     Return an arithmetic representation of a thread ID on POSIX.
 ','line_number':76,'multiline':True]['text':'
 * __wt_thread_str --
 *     Fill in a printable version of the process and thread IDs.
 ','line_number':86,'multiline':True]['text':'
 * __wt_process_id --
 *     Return the process ID assigned by the operating system.
 ','line_number':97,'multiline':True]