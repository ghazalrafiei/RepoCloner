['text':'-
 * Copyright (c) 2014-present MongoDB, Inc.
 * Copyright (c) 2008-2014 WiredTiger, Inc.
 *	All rights reserved.
 *
 * See the file LICENSE for redistribution information.
 ','line_number':1,'multiline':True]['text':'
 * __hs_start_internal_session --
 *     Create a temporary internal session to retrieve history store.
 ','line_number':11,'multiline':True]['text':'
 * __hs_release_internal_session --
 *     Release the temporary internal session started to retrieve history store.
 ','line_number':21,'multiline':True]['text':'
 * __hs_cleanup_las --
 *     Drop the lookaside file if it exists.
 ','line_number':31,'multiline':True]['text':' Read-only and in-memory configurations won't drop the lookaside. ','line_number':44,'multiline':True]['text':' The LAS table may exist on upgrade. Discard it. ','line_number':48,'multiline':True]['text':'
 * __wt_hs_get_btree --
 *     Get the history store btree by opening a history store cursor.
 ','line_number':55,'multiline':True]['text':'
 * __wt_hs_config --
 *     Configure the history store table.
 ','line_number':74,'multiline':True]['text':' The history store is not available for in-memory configurations. ','line_number':95,'multiline':True]['text':'
     * Retrieve the btree from the history store cursor.
     ','line_number':101,'multiline':True]['text':' Track the history store file ID. ','line_number':106,'multiline':True]['text':'
     * We need to set file_max on the btree associated with one of the history store sessions.
     ','line_number':110,'multiline':True]['text':'
     * Now that we have the history store's handle, we may set the flag because we know the file is
     * open.
     ','line_number':116,'multiline':True]['text':'
 * __wt_hs_open --
 *     Initialize the database's history store.
 ','line_number':128,'multiline':True]['text':' Read-only and in-memory configurations don't need the history store table. ','line_number':139,'multiline':True]['text':' Drop the lookaside file if it still exists. ','line_number':143,'multiline':True]['text':' Create the table. ','line_number':146,'multiline':True]['text':'
 * __wt_hs_close --
 *     Destroy the database's history store.
 ','line_number':154,'multiline':True]