['text':'-
 * Copyright (c) 2014-present MongoDB, Inc.
 * Copyright (c) 2008-2014 WiredTiger, Inc.
 *	All rights reserved.
 *
 * See the file LICENSE for redistribution information.
 ','line_number':1,'multiline':True]['text':'
 * Per session handle cached block manager information.
 ','line_number':11,'multiline':True]['text':' List of WT_EXT handles ','line_number':15,'multiline':True]['text':' Count ','line_number':16,'multiline':True]['text':' List of WT_SIZE handles ','line_number':18,'multiline':True]['text':' Count ','line_number':19,'multiline':True]['text':'
 * __block_ext_alloc --
 *     Allocate a new WT_EXT structure.
 ','line_number':22,'multiline':True]['text':'
 * __wt_block_ext_alloc --
 *     Return a WT_EXT structure for use.
 ','line_number':41,'multiline':True]['text':' Return a WT_EXT structure for use from a cached list. ','line_number':54,'multiline':True]['text':' Clear any left-over references. ','line_number':59,'multiline':True]['text':'
         * The count is advisory to minimize our exposure to bugs, but don't let it go negative.
         ','line_number':63,'multiline':True]['text':'
 * __block_ext_prealloc --
 *     Pre-allocate WT_EXT structures.
 ','line_number':76,'multiline':True]['text':'
 * __wt_block_ext_free --
 *     Add a WT_EXT structure to the cached list.
 ','line_number':97,'multiline':True]['text':'
 * __block_ext_discard --
 *     Discard some or all of the WT_EXT structure cache.
 ','line_number':116,'multiline':True]['text':'
 * __block_size_alloc --
 *     Allocate a new WT_SIZE structure.
 ','line_number':146,'multiline':True]['text':'
 * __wt_block_size_alloc --
 *     Return a WT_SIZE structure for use.
 ','line_number':156,'multiline':True]['text':' Return a WT_SIZE structure for use from a cached list. ','line_number':167,'multiline':True]['text':'
         * The count is advisory to minimize our exposure to bugs, but don't let it go negative.
         ','line_number':172,'multiline':True]['text':'
 * __block_size_prealloc --
 *     Pre-allocate WT_SIZE structures.
 ','line_number':183,'multiline':True]['text':'
 * __wt_block_size_free --
 *     Add a WT_SIZE structure to the cached list.
 ','line_number':204,'multiline':True]['text':'
 * __block_size_discard --
 *     Discard some or all of the WT_SIZE structure cache.
 ','line_number':223,'multiline':True]['text':'
 * __block_manager_session_cleanup --
 *     Clean up the session handle's block manager information.
 ','line_number':253,'multiline':True]['text':'
 * __wt_block_ext_prealloc --
 *     Pre-allocate WT_EXT and WT_SIZE structures.
 ','line_number':273,'multiline':True]['text':'
 * __wt_block_ext_discard --
 *     Discard WT_EXT and WT_SIZE structures after checkpoint runs.
 ','line_number':289,'multiline':True]