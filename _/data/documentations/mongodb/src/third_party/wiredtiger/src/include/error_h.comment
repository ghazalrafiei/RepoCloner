['text':'-
 * Copyright (c) 2014-present MongoDB, Inc.
 * Copyright (c) 2008-2014 WiredTiger, Inc.
 *	All rights reserved.
 *
 * See the file LICENSE for redistribution information.
 ','line_number':1,'multiline':True]['text':' In DIAGNOSTIC mode, yield in places where we want to encourage races (except for with
 * antithesis). ','line_number':13,'multiline':True]['text':' Set "ret" and branch-to-err-label tests. ','line_number':41,'multiline':True]['text':' Return WT_PANIC regardless of earlier return codes. ','line_number':64,'multiline':True]['text':' Return tests. ','line_number':67,'multiline':True]['text':' Set "ret" if not already set. ','line_number':100,'multiline':True]['text':' Return WT_PANIC regardless of earlier return codes. ','line_number':120,'multiline':True]['text':' Called on unexpected code path: locate the failure. ','line_number':123,'multiline':True]['text':'
 * Branch prediction hints. If an expression is likely to return true/false we can use this
 * information to improve performance at runtime. This is not supported for MSVC compilers.
 ','line_number':128,'multiline':True]['text':'
 * BUILD_ASSERTION_STRING --
 *  Append a common prefix to an assertion message and save into the provided buffer.
 ','line_number':142,'multiline':True]['text':' If we would overflow, finish with what we have. ','line_number':152,'multiline':True]['text':'
 * TRIGGER_ABORT --
 *  Abort the program.
 *
 * When unit testing assertions we don't want to call __wt_abort, but we do want to track that we
 * should have done so.
 ','line_number':157,'multiline':True]['text':'
 * EXTRA_DIAGNOSTICS_ENABLED --
 *  Fetch whether diagnostic asserts for the provided category are runtime enabled.
 *  When compiled with HAVE_DIAGNOSTIC=1, the WT_DIAGNOSTIC_ALL category is always set on
 *  the connection and this function will always return true for non-null sessions.
 ','line_number':186,'multiline':True]['text':'
 * WT_ASSERT --
 *  Assert an expression and abort if it fails.
 *  Only enabled when compiled with HAVE_DIAGNOSTIC=1.
 ','line_number':196,'multiline':True]['text':'
 * WT_ASSERT_OPTIONAL --
 *  Assert an expression if the relevant assertion category is enabled.
 ','line_number':211,'multiline':True]['text':'
 * WT_ASSERT_ALWAYS --
 *  Assert an expression. This is enabled regardless of configuration.
 ','line_number':222,'multiline':True]['text':'
 * WT_ERR_ASSERT --
 *  Assert an expression. If the relevant assertion category is
 *  enabled abort the program, otherwise print a message and return WT_ERR.
 ','line_number':232,'multiline':True]['text':'
 * WT_RET_ASSERT --
 *  Assert an expression. If the relevant assertion category is enabled
 *  abort the program, otherwise print a message and early return from the function.
 ','line_number':247,'multiline':True]['text':'
 * WT_RET_PANIC_ASSERT --
 *  Assert an expression. If the relevant assertion category is enabled
 *  abort the program, otherwise return WT_PANIC.
 ','line_number':262,'multiline':True]