['text':'-
 * Copyright (c) 2014-present MongoDB, Inc.
 * Copyright (c) 2008-2014 WiredTiger, Inc.
 *	All rights reserved.
 *
 * See the file LICENSE for redistribution information.
 ','line_number':1,'multiline':True]['text':'
 * __truncate_table --
 *     WT_SESSION::truncate for a table.
 ','line_number':11,'multiline':True]['text':' Truncate the column groups. ','line_number':25,'multiline':True]['text':' Truncate the indices. ','line_number':29,'multiline':True]['text':'
 * __truncate_tiered --
 *     Truncate for a tiered data source.
 ','line_number':39,'multiline':True]['text':'
 * __truncate_dsrc --
 *     WT_SESSION::truncate for a data-source without a truncate operation.
 ','line_number':60,'multiline':True]['text':' Open a cursor and traverse the object, removing every entry. ','line_number':71,'multiline':True]['text':'
 * __wt_schema_truncate --
 *     WT_SESSION::truncate without a range.
 ','line_number':85,'multiline':True]['text':'
         * File truncate translates into a range truncate.
         ','line_number':102,'multiline':True]['text':' If we didn't find a metadata entry, map that error to ENOENT. ','line_number':119,'multiline':True]['text':'
 * __wt_range_truncate --
 *     Truncate of a cursor range, default implementation. This truncate takes explicit cursors
 *     rather than a truncate information structure since it is used to implement truncate for
 *     column groups within a complex table, and those use different cursors than the API level
 *     truncate tracks.
 ','line_number':123,'multiline':True]['text':'
 * __wt_schema_range_truncate --
 *     WT_SESSION::truncate with a range.
 ','line_number':153,'multiline':True]