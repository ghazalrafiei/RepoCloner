['text':'','line_number':1,'multiline':False]['text':' A DBQuery which is explained rather than executed normally. Also could be thought of as','line_number':2,'multiline':False]['text':' an "explainable cursor". Explains of .find() operations run through this abstraction.','line_number':3,'multiline':False]['text':'','line_number':4,'multiline':False]['text':'','line_number':7,'multiline':False]['text':' Private methods.','line_number':8,'multiline':False]['text':'','line_number':9,'multiline':False]['text':'*
     * Many of the methods of an explain query just pass through to the underlying
     * non-explained DBQuery. Use this to generate a function which calls function 'name' on
     * 'destObj' and then returns this.
     ','line_number':11,'multiline':True]['text':'','line_number':24,'multiline':False]['text':' Private vars.','line_number':25,'multiline':False]['text':'','line_number':26,'multiline':False]['text':' Used if this query is a count, not a find.','line_number':33,'multiline':False]['text':'','line_number':37,'multiline':False]['text':' Public delegation methods. These just pass through to the underlying','line_number':38,'multiline':False]['text':' DBQuery.','line_number':39,'multiline':False]['text':'','line_number':40,'multiline':False]['text':' Generate the delegation methods from the list of their names.','line_number':59,'multiline':False]['text':'','line_number':65,'multiline':False]['text':' Core public methods.','line_number':66,'multiline':False]['text':'','line_number':67,'multiline':False]['text':'*
         * Indicates that we are done building the query to explain, and sends the explain
         * command or query to the server.
         *
         * Returns the result of running the explain.
         ','line_number':69,'multiline':True]['text':' Mark this query as finished. Shouldn't be used for another explain.','line_number':80,'multiline':False]['text':' Explain always gets pretty printed.','line_number':83,'multiline':False]['text':' Convert this explain query into an explain command, and send the command to','line_number':86,'multiline':False]['text':' the server.','line_number':87,'multiline':False]['text':' True means to always apply the skip and limit values.','line_number':90,'multiline':False]['text':' If "maxTimeMS" or "$readPreference" are  set on 'innerCmd', they need to be','line_number':99,'multiline':False]['text':' propagated to the top-level of 'explainCmd' in order to have the intended effect.','line_number':100,'multiline':False]['text':'*
         * Returns the explain resulting from running this query as a count operation.
         *
         * If 'applySkipLimit' is true, then the skip and limit values set on this query values are
         * passed to the server; otherwise they are ignored.
         ','line_number':128,'multiline':True]['text':'*
         * This gets called automatically by the shell in interactive mode. It should
         * print the result of running the explain.
         ','line_number':142,'multiline':True]['text':'*
         * Display help text.
         ','line_number':151,'multiline':True]