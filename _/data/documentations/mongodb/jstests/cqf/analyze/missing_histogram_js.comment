['text':'*
 * This test verifies that we gracefully handle the case where we do not have statistics or a
 * histogram available for a given path. It also tests empty collections are handled appropriately.
 ','line_number':1,'multiline':True]['text':' Ensure we have no histogram.','line_number':31,'multiline':False]['text':' We actually use heuristics because we don't generate histograms for empty collections;','line_number':36,'multiline':False]['text':' however, our estimate should still be 0, because the collection is empty.','line_number':37,'multiline':False]['text':' We should use heuristics in this case since we have no histogram. Our estimate happens to be','line_number':41,'multiline':False]['text':' correct because we only have one document.','line_number':42,'multiline':False]