['text':'*
 * Tests that the $group stage will be replaced with $_internalStreamingGroup when group id is
 * monotonic on time and documents are sorted on time.
 *
 * @tags: [
 *   # Explain of a resolved view must be executed by mongos.
 *   directly_against_shardsvrs_incompatible,
 *   does_not_support_stepdowns,
 *   requires_fcv_63,
 *   requires_timeseries,
 * ]
 ','line_number':1,'multiline':True]['text':' Incorrect use of $_internalStreamingGroup should return error','line_number':63,'multiline':False]