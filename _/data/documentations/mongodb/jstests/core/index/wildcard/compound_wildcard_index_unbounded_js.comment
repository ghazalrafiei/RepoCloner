['text':'*
 * Tests compound wildcard index with unbounded scans including multikey metadata entries doesn't
 * cause any errors.
 *
 * @tags: [
 *   requires_fcv_70,
 *   # explain does not support majority read concern
 *   assumes_read_concern_local,
 * ]
 ','line_number':1,'multiline':True]['text':' Add an array field in order to make wildcard insert a multikey path metadata entry.','line_number':24,'multiline':False]['text':' Asserting that we have unbounded index scans on $_path so that multikey metadata will also be','line_number':33,'multiline':False]['text':' included in the scan.','line_number':34,'multiline':False]['text':' The execution should succeed rather than spot any index corruption.','line_number':46,'multiline':False]