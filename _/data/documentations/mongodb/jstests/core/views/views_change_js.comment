['text':'*
 * Tests the behavior of views when the backing view or collection is changed.
 * @tags: [
 *   assumes_superuser_permissions,
 *   assumes_unsharded_collection,
 *   requires_non_retryable_commands,
 * ]
 ','line_number':1,'multiline':True]['text':' Convenience functions.','line_number':16,'multiline':False]['text':' A view is updated when its viewOn is modified. When auth is enabled, we expect collMod to','line_number':39,'multiline':False]['text':' fail when specifying "viewOn" but not "pipeline".','line_number':40,'multiline':False]['text':' A view is updated when its pipeline is modified. When auth is enabled, we expect collMod to','line_number':53,'multiline':False]['text':' fail when specifying "pipeline" but not "viewOn".','line_number':54,'multiline':False]['text':' A view is updated when the backing collection is updated.','line_number':68,'multiline':False]['text':' A view is updated when a backing view is updated.','line_number':76,'multiline':False]['text':' A view appears empty if the backing collection is dropped.','line_number':85,'multiline':False]['text':' A view appears empty if a backing view is dropped.','line_number':93,'multiline':False]