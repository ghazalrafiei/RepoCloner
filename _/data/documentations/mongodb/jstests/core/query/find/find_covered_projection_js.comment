['text':'*
 * Tests queries that are covered by an index.
 *
 * This test cannot implicitly shard accessed collections because queries on a sharded collection
 * cannot be covered when they aren't on the shard key since the document needs to be fetched in
 * order to apply the SHARDING_FILTER stage.
 * @tags: [
 *   assumes_unsharded_collection,
 * ]
 ','line_number':1,'multiline':True]['text':' Create an index on one field.','line_number':29,'multiline':False]['text':' Create a compound index.','line_number':34,'multiline':False]['text':' Repeat the above test, but with a compound index involving _id.','line_number':44,'multiline':False]['text':' Create an index on an embedded object.','line_number':52,'multiline':False]