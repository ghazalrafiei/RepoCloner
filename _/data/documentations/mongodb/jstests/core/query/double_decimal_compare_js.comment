['text':'*
 * Verifies correctness of double/decimal comparisons depending on the engine being used. This
 * is intended to reproduce SERVER-58155.
 * @tags: [
 *   assumes_read_concern_local,
 *   no_selinux,
 * ]
 ','line_number':1,'multiline':True]['text':' The double query will only match the single double value, and the decimal query will only match','line_number':19,'multiline':False]['text':' the decimal values.','line_number':20,'multiline':False]['text':' The double query will only match the single double value, and the decimal query will only match','line_number':29,'multiline':False]['text':' the decimal values.','line_number':30,'multiline':False]