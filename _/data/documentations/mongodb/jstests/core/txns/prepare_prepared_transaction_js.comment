['text':'*
 * Tests that we can successfully prepare a prepared transaction.
 *
 * @tags: [
 *   # The test runs commands that are not allowed with security token: endSession,
 *   # prepareTransaction.
 *   not_allowed_with_signed_security_token,
 *   uses_transactions,
 *   uses_prepare_transaction
 * ]
 ','line_number':1,'multiline':True]['text':' Attempting to prepare an already prepared transaction should return successfully with a','line_number':31,'multiline':False]['text':' prepareTimestamp.','line_number':32,'multiline':False]['text':' Client's opTime is later than the prepareOpTime, so just return the prepareTimestamp.','line_number':34,'multiline':False]['text':' Both prepareTimestamps should be equal.','line_number':39,'multiline':False]