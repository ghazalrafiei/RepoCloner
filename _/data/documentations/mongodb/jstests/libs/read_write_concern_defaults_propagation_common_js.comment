['text':' Check that setting the defaults on setConn propagates correctly across checkConns.','line_number':17,'multiline':False]['text':' Get the current defaults from setConn.','line_number':19,'multiline':False]['text':' Ensure that all checkConns agree with this. Use a loop in case the initial defaults were','line_number':23,'multiline':False]['text':' recently set and have not yet propagated to all nodes.','line_number':24,'multiline':False]['text':' Set new defaults on setConn.','line_number':43,'multiline':False]['text':' If these happen to match what's already there, adjust them.','line_number':48,'multiline':False]['text':' Prevent any existing default WC (which may be unsatisfiable) from being applied.','line_number':63,'multiline':False]['text':' Check that updateOpTime has increased.  Since everything is running on one host, we can','line_number':71,'multiline':False]['text':' also check that each of kUpdateOpTime and localUpdateWallClockTime have increased.','line_number':72,'multiline':False]['text':' Ensure that all checkConns agree with this.','line_number':81,'multiline':False]['text':' Get the defaults from all the connections.','line_number':84,'multiline':False]['text':' Check if they all match the recently-set values.','line_number':88,'multiline':False]['text':'*
     * Tests propagation of RWC defaults across an environment.  Updated RWC defaults are set using
     * setConn, and then each connection in the checkConns array is checked to ensure that it
     * becomes aware of the new defaults (within the acceptable window of 2 minutes).
     ','line_number':113,'multiline':True]['text':' Since these connections are on a brand new replset/cluster, this checks the propagation','line_number':119,'multiline':False]['text':' of the initial setting of defaults.','line_number':120,'multiline':False]['text':' Do it again to check that updating the defaults also propagates correctly.','line_number':123,'multiline':False]['text':'*
     * Asserts eventually all given nodes have no default RWC in their in-memory cache.
     ','line_number':127,'multiline':True]['text':' Note localUpdateWallClockTime is generated by the in-memory cache, so it will be','line_number':135,'multiline':False]['text':' present even if there are no defaults.','line_number':136,'multiline':False]['text':'*
     * Tests that when the RWC defaults document is removed, either through a delete or drop of
     * config.settings, the RWC defaults cache is invalidated.
     ','line_number':149,'multiline':True]['text':' Set the defaults to some value other than the implicit server defaults. Then remove the','line_number':154,'multiline':False]['text':' defaults document and verify the cache is invalidated on all nodes.','line_number':155,'multiline':False]