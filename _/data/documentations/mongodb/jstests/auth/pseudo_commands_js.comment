['text':'*
 * This tests the access control for the pseudo-commands inprog, curop, and killop.  Once
 * SERVER-5466 is resolved this test should be removed and its functionality merged into
 * commands_builtin_roles.js and commands_user_defined_roles.js.
 * @tags: [requires_sharding]
 ','line_number':1,'multiline':True]['text':'*
     * Tests that a single operation has the proper authorization.  The operation is run by invoking
     * "testFunc".  "testFunc" must take a single argument indicating whether we expect the test to
     * pass or fail due to being unauthorized.  Roles in an object that described which roles are
     * expected to be able to run the operation, and privilege is the privilege that must be granted
     * a user-defined-role in order to run the operation.
     ','line_number':22,'multiline':True]['text':' Test built-in roles first','line_number':30,'multiline':False]['text':' Now test user-defined role','line_number':36,'multiline':False]['text':'*
     * Returns true if conn is a connection to mongos,
     * and false otherwise.
     ','line_number':44,'multiline':True]['text':' Operation id cannot exceed INT_MAX.','line_number':117,'multiline':False]['text':' opid format different between mongos and mongod','line_number':118,'multiline':False]['text':' unlock doesn't work on mongos','line_number':137,'multiline':False]['text':' must be locked first','line_number':163,'multiline':False]['text':' If the storage engine doesnt support fsync lock, we can't proceed','line_number':164,'multiline':False]['text':' If we get to the storage engine, we better be authorized.','line_number':168,'multiline':False]['text':' convert undefined to false','line_number':174,'multiline':False]