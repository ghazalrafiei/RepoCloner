['text':'*
 * Starts a shard split through `abortShardSplit` and assert that no tenant access blockers are
 * recovered since we do not recover access blockers for aborted split marked garbage collectable.
 * Also verifies the serverless operation lock is not acquired when starting a split in aborted
 * state.
 * @tags: [requires_fcv_63, serverless]
 ','line_number':1,'multiline':True]['text':' Skip db hash check because secondary is left with a different config.','line_number':20,'multiline':False]['text':' Pause the shard split before waiting to mark the doc for garbage collection.','line_number':36,'multiline':False]['text':' we do not recover access blockers for kAborted marked for garbage collection','line_number':56,'multiline':False]['text':' We do not acquire the lock for document marked for garbage collection','line_number':62,'multiline':False]