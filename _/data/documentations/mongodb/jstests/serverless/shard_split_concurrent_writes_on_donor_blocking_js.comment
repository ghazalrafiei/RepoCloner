['text':'*
 * Tests that the donor blocks writes that are executed while the shard split in the blocking state,
 * then rejects the writes when the migration completes.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_63
 * ]
 ','line_number':1,'multiline':True]['text':' Increase timeout because blocking in the critical section contributes to operation latency.','line_number':28,'multiline':False]['text':'*
 * Tests that the donor blocks writes that are executed in the blocking state and increase the
 * countBlockedWrites count.
 ','line_number':44,'multiline':True]['text':'*
 * run the setup for each cases before the migration starts
 ','line_number':66,'multiline':True]['text':'*
 * Run the test cases after the migration has committed
 ','line_number':102,'multiline':True]['text':'*
 * Run the test cases after the migration has committed
 ','line_number':130,'multiline':True]['text':' start a shard split operation asynchronously.','line_number':166,'multiline':False]['text':' Run the command after the migration enters the blocking state.','line_number':169,'multiline':False]['text':' Run test cases while the migration is in blocking state.','line_number':172,'multiline':False]['text':' Allow the migration to complete.','line_number':175,'multiline':False]['text':' run test after blocking is over and the migration committed.','line_number':181,'multiline':False]