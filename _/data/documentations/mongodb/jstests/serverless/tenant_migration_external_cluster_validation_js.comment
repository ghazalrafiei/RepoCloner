['text':'*
 * Verify that after a tenant migration the donor and recipient can validate each other's
 * cluster times.
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_71,
 * ]
 ','line_number':1,'multiline':True]['text':' Multiple users cannot be authenticated on one connection within a session.','line_number':19,'multiline':False]['text':' User that runs the tenant migration.','line_number':22,'multiline':False]['text':' User that runs commands against the tenant database.','line_number':28,'multiline':False]['text':' Allow non-timestamped reads on donor after migration completes for testing.','line_number':62,'multiline':False]