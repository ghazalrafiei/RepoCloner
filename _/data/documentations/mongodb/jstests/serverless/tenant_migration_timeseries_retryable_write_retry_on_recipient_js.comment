['text':'*
 * Tests that retryable writes made on the donor during a tenant migration can be properly retried
 * on the recipient for time-series collections.
 *
 * This test is based on "tenant_migration_retryable_write_retry_on_recipient.js".
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   # TODO (SERVER-70605): Remove this tag once the time-series always compressed buckets feature
 *   # flag can be removed.
 *   multiversion_incompatible,
 * ]
 ','line_number':1,'multiline':True]['text':' Batched inserts resulting in "inserts".','line_number':72,'multiline':False]['text':' Batched inserts resulting in "updates".','line_number':76,'multiline':False]['text':' Wait for the migration to complete.','line_number':103,'multiline':False]['text':' Print the no-op oplog entries for debugging purposes.','line_number':109,'multiline':False]['text':' If retryable inserts don't work, we will see 6 here.','line_number':120,'multiline':False]['text':' Print the no-op oplog entries for debugging purposes.','line_number':147,'multiline':False]