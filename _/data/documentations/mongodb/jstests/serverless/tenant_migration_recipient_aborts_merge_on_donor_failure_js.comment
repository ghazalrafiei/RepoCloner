['text':'*
 * Tests that the recipient will abort a shard merge on donor failure
 *
 * @tags: [
 *   incompatible_with_macos,
 *   incompatible_with_windows_tls,
 *   requires_shard_merge,
 *   requires_majority_read_concern,
 *   requires_persistence,
 *   serverless,
 *   requires_fcv_71,
 * ]
 ','line_number':1,'multiline':True]['text':' wait until the completion path has started after the abort','line_number':54,'multiline':False]['text':' step up a secondary so that the migration will complete and the','line_number':61,'multiline':False]['text':' waitForMigrationToComplete call to the donor primary succeeds','line_number':62,'multiline':False]