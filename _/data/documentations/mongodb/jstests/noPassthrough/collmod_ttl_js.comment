['text':'*
 * Tests that the expired data is deleted correctly after an index is converted to TTL using the
 * collMod command.
 *
 * @tags: [
 *   assumes_no_implicit_collection_creation_after_drop,
 *   multiversion_incompatible,
 *   requires_ttl_index,
 * ]
 ','line_number':1,'multiline':True]['text':' Runs TTL monitor constantly to speed up this test.','line_number':13,'multiline':False]['text':' Inserts a measurement older than the TTL expiry. The data should be found.','line_number':23,'multiline':False]['text':' Converts to a TTL index and checks the data is deleted.','line_number':30,'multiline':False]