['text':'*
 * Tests that the collMod command disallows writes that introduce new duplicate keys and the option
 * is persisted over restarts.
 *
 * @tags: [
 *  requires_fcv_60,
 *  requires_persistence,
 *  requires_replication,
 * ]
 ','line_number':1,'multiline':True]['text':' Sets 'prepareUnique' and checks that the index rejects duplicates.','line_number':21,'multiline':False]['text':' Restarts the primary and checks the index spec is persisted.','line_number':29,'multiline':False]['text':' Converts the index to unique.','line_number':37,'multiline':False]