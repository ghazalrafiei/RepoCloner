['text':'*
 * Verifies that the data consistency checks work against the variety of cluster types we use in our
 * testing.
 *
 * @tags: [
 *   requires_replication,
 *   requires_sharding,
 * ]
 ','line_number':1,'multiline':True]['text':' We skip doing the data consistency checks while terminating the cluster because they conflict','line_number':11,'multiline':False]['text':' with the counts of the number of times the "dbhash" and "validate" commands are run.','line_number':12,'multiline':False]