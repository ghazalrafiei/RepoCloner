['text':'*
 * Tests that an in-progress background validation succeeds when DDL operations are executed against
 * the collection.
 *
 * @tags: [requires_persistence]
 ','line_number':1,'multiline':True]['text':' Clear the log to get rid of any existing fail point logging that will be used to hang on.','line_number':35,'multiline':False]['text':' Insert 10,000 documents because validation will yield every 4096 entries fetched.','line_number':45,'multiline':False]['text':'*
 * Collection validation succeeds when dropping an index being validated.
 ','line_number':57,'multiline':True]['text':'*
 * Collection validation succeeds when dropping the collection being validated.
 ','line_number':79,'multiline':True]['text':'*
 * Collection validation succeeds when the collection being validated is renamed.
 ','line_number':100,'multiline':True]['text':'*
 * Collection validation succeeds when dropping the database the collection being validated is part
 * of.
 ','line_number':125,'multiline':True]['text':'*
 * Collection validation succeeds when running operations that do not affect ongoing background
 * validation.
 ','line_number':147,'multiline':True]['text':' Run a bunch of operations that shouldn't make background validation fail.','line_number':162,'multiline':False]['text':' Rename the collection being validated across the same database.','line_number':174,'multiline':False]