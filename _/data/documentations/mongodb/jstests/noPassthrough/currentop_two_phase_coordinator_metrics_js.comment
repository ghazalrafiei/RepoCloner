['text':'*
 * Tests that the transaction items in the 'twoPhaseCommitCoordinator' object in currentOp() are
 * being tracked correctly.
 * @tags: [
 *   uses_prepare_transaction,
 *   uses_transactions,
 * ]
 ','line_number':1,'multiline':True]['text':' insert into both shards','line_number':48,'multiline':False]['text':' mongos broadcasts currentOp to all the shards and puts each shard’s','line_number':101,'multiline':False]['text':' response in a subobject under the shard’s name','line_number':102,'multiline':False]['text':' make sure totalCommitDuration is at least as big as all the other durations.','line_number':128,'multiline':False]['text':' this find is to ensure all the shards' filtering metadata are up to date','line_number':171,'multiline':False]['text':' Not using 'Object.keys(failPointStates)' since lexical order is not guaranteed','line_number':260,'multiline':False]