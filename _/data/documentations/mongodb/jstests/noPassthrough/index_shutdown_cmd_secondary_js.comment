['text':'*
 * If a user attempts to shut down the server using the shutdown command without the force: true
 * option while there is an index build in progress, we should reject the shutdown request.
 * @tags: [
 *   requires_replication,
 * ]
 ','line_number':1,'multiline':True]['text':' Disallow elections on secondary.','line_number':14,'multiline':False]['text':' Stop the secondary using the shutdown command without {force: true}.','line_number':40,'multiline':False]['text':' This runs the shutdown command without {force: true} with additional handling for expected','line_number':54,'multiline':False]['text':' network errors when the command succeeds.','line_number':55,'multiline':False]