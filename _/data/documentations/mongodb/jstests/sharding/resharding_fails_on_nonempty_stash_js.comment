['text':'*
 * Tests that unrecoverable errors during resharding's oplog application are handled by the
 * recipient.
 *
 * @tags: [
 *   uses_atclustertime
 * ]
 ','line_number':1,'multiline':True]['text':' The following documents violate the global _id uniqueness assumption of sharded','line_number':43,'multiline':False]['text':' collections. It is possible to construct such a sharded collection due to how each shard','line_number':44,'multiline':False]['text':' independently enforces the uniqueness of _id values for only the documents it owns. The','line_number':45,'multiline':False]['text':' resharding operation is expected to abort upon discovering this violation.','line_number':46,'multiline':False]