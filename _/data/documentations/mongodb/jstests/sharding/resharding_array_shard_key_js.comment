['text':'*
 * Tests that during resharding, inserts and updates that specify an array for the new shard key
 * fail.
 *
 * @tags: [
 *   uses_atclustertime,
 * ]
 ','line_number':1,'multiline':True]['text':' Once the resharding operation begins, the donor shard will require that the shard key','line_number':51,'multiline':False]['text':' value under both the current and new key patterns is valid.','line_number':52,'multiline':False]