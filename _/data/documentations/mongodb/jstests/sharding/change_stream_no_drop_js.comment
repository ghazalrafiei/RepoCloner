['text':'*
 * DDL coordinator are responsible for dropping temporary collections, especially after failures.
 * However, the change stream should not be aware of those events.
 * @tags: [
 *  # Requires all nodes to be running the latest binary.
 *  multiversion_incompatible,
 * ]
 ','line_number':1,'multiline':True]['text':' Enable explicitly the periodic no-op writer to allow the router to process change stream events','line_number':25,'multiline':False]['text':' coming from all shards. This is enabled for production clusters by default.','line_number':26,'multiline':False]['text':' create a database and a change stream on it','line_number':35,'multiline':False]['text':' setFeatureCompatibilityVersion might cause dropping of deprecated collections','line_number':41,'multiline':False]