['text':' Sort collections by name.','line_number':17,'multiline':False]['text':' For each unsharded collection, there should be a total of 2 indexes - one for the _id','line_number':61,'multiline':False]['text':' field and the other we have created. However, in the case of sharded collections, only','line_number':62,'multiline':False]['text':' the _id index is present. When running movePrimary, indexes of sharded collections are','line_number':63,'multiline':False]['text':' not copied.','line_number':64,'multiline':False]['text':' UUIDs should be the same as the original','line_number':109,'multiline':False]['text':' UUIDs should not be the same as the original','line_number':113,'multiline':False]['text':' Failpoint will cause movePrimary to fail after the first collection has been copied over','line_number':164,'multiline':False]['text':' If the collections are sharded, the UUID of the collection on the donor should be','line_number':171,'multiline':False]['text':' copied over and the options should be the same so retrying the move should succeed.','line_number':172,'multiline':False]['text':' Now change an option on the toShard, and verify that calling clone again succeeds','line_number':176,'multiline':False]['text':' when the options don't match.','line_number':177,'multiline':False]['text':' Assert that the fromShard does not have the new options, but the toShard does','line_number':182,'multiline':False]['text':' The docs should still be on the original primary shard (fromShard).','line_number':191,'multiline':False]['text':' Now drop and recreate the collection on the toShard. The collection should now have','line_number':194,'multiline':False]['text':' a different UUID, so we should fail on movePrimary.','line_number':195,'multiline':False]['text':' The failure of the previous attempt caused the dirty data on the recipient to be dropped,','line_number':202,'multiline':False]['text':' so the data cloning shouldn't find any impediments.','line_number':203,'multiline':False]