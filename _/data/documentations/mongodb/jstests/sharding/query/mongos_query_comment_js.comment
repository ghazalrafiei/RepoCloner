['text':'*
 * Test that a legacy query via mongos retains the $comment query meta-operator when transformed
 * into a find command for the shards. In addition, verify that the find command comment parameter
 * and query operator are passed to the shards correctly, and that an attempt to attach a non-string
 * comment to the find command fails.
 ','line_number':1,'multiline':True]['text':' Need references to the database via both mongos and mongod so that we can enable profiling &','line_number':14,'multiline':False]['text':' test queries on the shard.','line_number':15,'multiline':False]['text':' The profiler will be used to verify that comments are present on the shard.','line_number':29,'multiline':False]['text':' TEST CASE: Verify that find.comment and non-string find.filter.$comment propagate.','line_number':32,'multiline':False]['text':' TEST CASE: Verify that find command with a non-string comment parameter gets propagated.','line_number':40,'multiline':False]