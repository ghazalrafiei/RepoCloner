['text':'*
 * Tests that write operations on the collection being resharded succeed even when the routing
 * information for the associated temporary resharding collection isn't currently known.
 *
 * @tags: [
 *   uses_atclustertime,
 *   uses_transactions,
 * ]
 ','line_number':1,'multiline':True]['text':'','line_number':86,'multiline':False]['text':' Wait for the recipients to have finished cloning so the temporary resharding','line_number':93,'multiline':False]['text':' collection is known to exist.','line_number':94,'multiline':False]['text':' Make the routing info for the temporary sharding collection unknown','line_number':103,'multiline':False]