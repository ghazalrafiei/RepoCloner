['text':'*
 * Test for the ReshardingTest fixture itself.
 *
 * Verifies that the reshardCollection command is run and kept suspended in the "applying" state.
 *
 * @tags: [
 *   uses_atclustertime
 * ]
 ','line_number':1,'multiline':True]['text':' Perform some inserts before resharding starts so there's data to clone.','line_number':26,'multiline':False]['text':' Wait for the recipients to have finished cloning and then perform some updates so there's oplog','line_number':43,'multiline':False]['text':' entries to fetch and apply.','line_number':44,'multiline':False]['text':' The reshardCollection command should still be actively running on mongos.','line_number':53,'multiline':False]