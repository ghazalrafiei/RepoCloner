['text':'
 * Tests that a transaction that failed with a transient transaction error
 * can be retried on a replica set and that its txnRetryCounter is persisted correctly
 * on all nodes.
 *
 * @tags: [requires_fcv_60, uses_transactions, requires_persistence]
 ','line_number':1,'multiline':True]['text':' txnRetryCounter defaults to 0.','line_number':70,'multiline':False]['text':' Preload the collection metadata to avoid repeating the insert command if it fails due to','line_number':145,'multiline':False]['text':' StaleConfig error.','line_number':146,'multiline':False]['text':' signal ','line_number':183,'multiline':True]['text':'forRestart ','line_number':183,'multiline':True]