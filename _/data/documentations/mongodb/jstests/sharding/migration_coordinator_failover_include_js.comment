['text':' Wait for mongos to see a primary node on the primary shard, because mongos does not retry','line_number':25,'multiline':False]['text':' writes on NotPrimary errors, and we are about to insert docs through mongos.','line_number':26,'multiline':False]['text':' Insert some docs into the collection so that the migration leaves orphans on either the','line_number':29,'multiline':False]['text':' donor or recipient, depending on the decision.','line_number':30,'multiline':False]['text':' Shard the collection.','line_number':38,'multiline':False]['text':' Turn on a failpoint to make the migration commit fail on the config server.','line_number':42,'multiline':False]['text':' stepDownSecs ','line_number':65,'multiline':True]['text':' Wait for mongos to see a new primary of rs0 before running the count command, because mongos','line_number':85,'multiline':False]['text':' will only wait 20 seconds to see a new primary from within the count command, and it may take','line_number':86,'multiline':False]['text':' longer for a new primary to be elected if both replica set nodes run for election at the same','line_number':87,'multiline':False]['text':' time (and therefore both lose the first election).','line_number':88,'multiline':False]['text':' The data should still be present on the shard that owns the chunk.','line_number':91,'multiline':False]['text':' Turn off the failpoint on the config server before returning.','line_number':101,'multiline':False]