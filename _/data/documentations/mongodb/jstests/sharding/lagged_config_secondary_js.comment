['text':'*
 * Test that mongos times out when the config server replica set only contains nodes that
 * are behind the majority opTime.
 * @tags: [config_shard_incompatible]
 ','line_number':1,'multiline':True]['text':' Checking UUID and index consistency involves mongos being able to do a read from the config','line_number':9,'multiline':False]['text':' server, but this test is designed to make mongos time out when reading from the config server.','line_number':10,'multiline':False]['text':' Ensures that all metadata writes thus far have been replicated to all nodes','line_number':38,'multiline':False]['text':' Do one metadata write in order to bump the optime on mongos','line_number':43,'multiline':False]['text':' Clears all cached info so mongos will be forced to query from the config.','line_number':49,'multiline':False]['text':' Can't do clean shutdown with this failpoint on.','line_number':80,'multiline':False]