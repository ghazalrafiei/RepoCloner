['text':'*
 * Tests the behavior of resuming a change stream with the resume token generated for newly added
 * events.
 *
 *  @tags: [
 *   requires_fcv_60,
 *   # The test assumes certain ordering of the events. The chunk migrations on a sharded collection
 *   # could break the test.
 *   assumes_unsharded_collection,
 *   assumes_against_mongod_not_mongos,
 * ]
 ','line_number':1,'multiline':True]['text':' Test the 'create' event.','line_number':32,'multiline':False]['text':' Test the 'createIndexes' event on an empty collection.','line_number':43,'multiline':False]['text':' Insert a document so that the collection is not empty so that we can get coverage for','line_number':54,'multiline':False]['text':' 'commitIndexBuild' when creating an index on field "b" below.','line_number':55,'multiline':False]['text':' Test the 'createIndexes' event on a non-empty collection.','line_number':67,'multiline':False]['text':' Test the 'dropIndexes' event.','line_number':78,'multiline':False]['text':' Insert another document so that we can validate the resuming behavior for the','line_number':89,'multiline':False]['text':' dropIndexes event.','line_number':90,'multiline':False]['text':' Testing resuming with 'resumeAfter' and 'startAfter'.','line_number':115,'multiline':False]['text':' Runs the test using a whole-db change stream','line_number':122,'multiline':False]