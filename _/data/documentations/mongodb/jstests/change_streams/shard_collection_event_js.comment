['text':'*
 * Test that change streams returns shardCollection events with the options specified on the
 * original user command.
 *  @tags: [
 *    requires_fcv_60,
 *    requires_sharding,
 *    uses_change_streams,
 *    change_stream_does_not_expect_txns,
 *    assumes_unsharded_collection,
 *    assumes_read_preference_unchanged,
 * ]
 ','line_number':1,'multiline':True]['text':' SERVER-83104: Remove 'numInitialChunks' check once 8.0 becomes last LTS.','line_number':44,'multiline':False]['text':' SERVER-83104: Remove 'capped' check once 8.0 becomes last LTS.','line_number':49,'multiline':False]['text':' The only possible other events are create collection or index.','line_number':63,'multiline':False]['text':' Check the presence and the type of 'wallTime' field. We have no way to check the correctness','line_number':69,'multiline':False]['text':' of 'wallTime' value, so we delete it afterwards.','line_number':70,'multiline':False]['text':' The only possible other events are create collection or index.','line_number':99,'multiline':False]['text':' Insert a document before starting the next change stream so that we can validate the','line_number':108,'multiline':False]['text':' resuming behavior.','line_number':109,'multiline':False]['text':' Resume with 'resumeAfter'.','line_number':112,'multiline':False]['text':' This test verifies simple key parameter passing .','line_number':145,'multiline':True]['text':' This test verifies simple hashed key parameter passing .','line_number':153,'multiline':True]['text':' This test verifies compound hashed key parameter passing .','line_number':165,'multiline':True]['text':' This test verifies unique parameter passing .','line_number':177,'multiline':True]['text':' This test verifies collation parameter passing .','line_number':185,'multiline':True]