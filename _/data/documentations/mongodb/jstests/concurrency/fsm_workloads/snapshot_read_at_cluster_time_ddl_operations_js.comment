['text':'*
 * Perform point-in-time snapshot reads that span a 'find' and multiple 'getmore's concurrently with
 * CRUD operations.
 *
 * @tags: [
 *   creates_background_indexes,
 *   does_not_support_causal_consistency,
 *   requires_majority_read_concern,
 *   requires_replication,
 * ]
 ','line_number':1,'multiline':True]['text':' dropIndex can cause queries to throw if these queries yield.','line_number':45,'multiline':False]['text':' dropIndex can cause queries to throw if these queries yield.','line_number':58,'multiline':False]['text':' dropIndex can cause queries to throw if these queries yield.','line_number':71,'multiline':False]['text':' We temporarily increase the minimum snapshot history window to ensure point-in-time reads','line_number':108,'multiline':False]['text':' at the initial insert timestamp are valid throughout the duration of this test.','line_number':109,'multiline':False]['text':' We modify chunk history to be larger on config nodes to ensure snapshot reads succeed for','line_number':115,'multiline':False]['text':' sharded clusters.','line_number':116,'multiline':False]