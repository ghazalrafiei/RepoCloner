['text':'*
 * Same as the base workload, but refines to a nested shard key.
 *
 * @tags: [requires_persistence, requires_sharding]
 ','line_number':1,'multiline':True]['text':' Note the base workload assumes this is the nested key when constructing the crud ops.','line_number':13,'multiline':False]