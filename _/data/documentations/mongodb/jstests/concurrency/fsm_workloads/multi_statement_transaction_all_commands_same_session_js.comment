['text':'*
 * Runs update, findAndModify, delete, find, and getMore in a transaction with all threads using the
 * same session.
 *
 * @tags: [uses_transactions, state_functions_share_transaction, assumes_snapshot_transactions]
 ','line_number':1,'multiline':True]['text':' Force the session to use `lsid` for its session id. This way all threads will use','line_number':22,'multiline':False]['text':' the same session.','line_number':23,'multiline':False]