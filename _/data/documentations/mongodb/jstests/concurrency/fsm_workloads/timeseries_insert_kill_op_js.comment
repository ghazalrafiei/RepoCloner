['text':'*
 * Tests killing time-series inserts.
 *
 * @tags: [
 *   requires_timeseries,
 *   # killOp does not support stepdowns.
 *   does_not_support_stepdowns,
 *   # Timeseries do not support multi-document transactions with inserts.
 *   does_not_support_transactions,
 *   # Kill operations do not propagate for writes on mongos.
 *   assumes_unsharded_collection
 * ]
 ','line_number':1,'multiline':True]['text':' TODO (SERVER-44673): Use assert.commandWorkedOrFailedWithCode without special handling.','line_number':28,'multiline':False]['text':' TODO SERVER-80143: Re-enable with 100 iterations.','line_number':87,'multiline':False]