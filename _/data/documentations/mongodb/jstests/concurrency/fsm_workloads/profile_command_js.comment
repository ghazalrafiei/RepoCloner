['text':'*
 * profile_command.js
 *
 * Sets the profile level and filter to different values, and asserts that they take effect
 * simultaneously:
 *   1. The profile command never returns an inconsistent combination of {level, filter}.
 *   2. We never log or profile based on an inconsistent combination of {level, filter}.
 *
 * @tags: [
 *   requires_profiling,
 * ]
 ','line_number':1,'multiline':True]['text':' After init(), these are the only profile settings we set.','line_number':18,'multiline':False]['text':' For example, we never set { level: 1, filter {millis: {$gt: 1}} }.','line_number':19,'multiline':False]['text':' We never profiled a read.','line_number':63,'multiline':False]['text':' Just let every state go to every other state with equal probability.','line_number':67,'multiline':False]