['text':'*
 * Concurrently performs DDL commands and FCV changes and verifies guarantees are
 * not broken.
 *
 * @tags: [
 *   requires_sharding,
 *   # TODO (SERVER-56879) Support add/remove shards in new DDL paths
 *   does_not_support_add_remove_shards,
 *   # Requires all nodes to be running the latest binary.
 *   multiversion_incompatible,
 *  ]
 ','line_number':1,'multiline':True]['text':' Invalid fcv transition (e.g lastContinuous -> lastLTS)','line_number':30,'multiline':False]['text':' Cannot upgrade FCV if a previous FCV downgrade stopped in the middle of cleaning','line_number':35,'multiline':False]['text':' up internal server metadata.','line_number':36,'multiline':False]['text':' Cannot downgrade FCV that requires a collMod command when index builds are','line_number':44,'multiline':False]['text':' concurrently taking place.','line_number':45,'multiline':False]