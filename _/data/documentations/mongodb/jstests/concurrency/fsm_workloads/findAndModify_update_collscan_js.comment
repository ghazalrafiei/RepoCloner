['text':'*
 * findAndModify_update_collscan.js
 *
 * Each thread inserts multiple documents into a collection, and then
 * repeatedly performs the findAndModify command. A single document is
 * selected based on 'query' and 'sort' specifications, and updated
 * using either the $min or $max operator.
 *
 * Attempts to force a collection scan by not creating an index.
 ','line_number':1,'multiline':True]['text':' Do not create the { tid: 1, value: 1 } index so that a','line_number':15,'multiline':False]['text':' collection','line_number':16,'multiline':False]['text':' scan is performed for the query and sort operations.','line_number':17,'multiline':False]['text':' Remove the shardKey so that a collection scan is performed','line_number':20,'multiline':False]