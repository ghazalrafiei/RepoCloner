['text':'*
 * update_rename.js
 *
 * Each thread does a $rename to cause documents to jump between indexes.
 ','line_number':1,'multiline':True]['text':' Create an index on all but one fieldName key to make it possible to test renames','line_number':37,'multiline':False]['text':' between indexed fields and non-indexed fields','line_number':38,'multiline':False]['text':' numDocs should be much less than threadCount, to make more threads use the same docs.','line_number':45,'multiline':False]