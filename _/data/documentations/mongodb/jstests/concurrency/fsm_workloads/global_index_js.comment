['text':'*
 * Perform global index CRUD operations, with create and drop commands.
 *
 * @tags: [
 *     featureFlagGlobalIndexes,
 *     requires_fcv_62,
 *     requires_replication,
 *     requires_sharding,
 * ]
 ','line_number':1,'multiline':True]['text':'
            6924201 - Container does not exist (delete).
            6789402 - Container does not exist (insert).
        ','line_number':18,'multiline':True]['text':' _shardsvrCreateGlobalIndex is idempotent, we don't expect any failures.','line_number':43,'multiline':False]['text':' _shardsvrDropGlobalIndex is idempotent, we don't expect any failures.','line_number':49,'multiline':False]