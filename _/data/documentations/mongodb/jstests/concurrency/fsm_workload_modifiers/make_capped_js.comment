['text':'*
 * make_capped.js
 *
 * Defines a modifier for workloads that drops the collection and re-creates it
 * as capped at the start of setup.
 *
 * However, it only does this when it owns the collection, to avoid surprising
 * other workloads.
 ','line_number':1,'multiline':True]['text':' bytes','line_number':16,'multiline':False]['text':' Ignore errors when a cursor's position in the capped collection is deleted.','line_number':29,'multiline':False]['text':' Reads from the beginning of a capped collection are not guaranteed to succeed','line_number':30,'multiline':False]['text':' when there are concurrent inserts that cause a truncation.','line_number':31,'multiline':False]