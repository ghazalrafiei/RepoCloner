['text':' these tests are specifically for linux','line_number':5,'multiline':False]['text':' Use localhost so that SAN matches.','line_number':12,'multiline':False]['text':' By default, rs.initiate takes a very long time to timeout. We should shorten this','line_number':26,'multiline':False]['text':' period, because we expect it to fail. ReplSetTest has both a static and local copy','line_number':27,'multiline':False]['text':' of kDefaultTimeOutMS, so we must override both.','line_number':28,'multiline':False]['text':' The rs.initiate will fail in an assert.soon, which would ordinarily trigger the hang','line_number':33,'multiline':False]['text':' analyzer.  We don't want that to happen, so we disable it here.','line_number':34,'multiline':False]['text':' ca.pem signed client.pem and server.pem','line_number':50,'multiline':False]['text':' trusted-ca.pem signed trusted-client.pem and trusted-server.pem','line_number':51,'multiline':False]