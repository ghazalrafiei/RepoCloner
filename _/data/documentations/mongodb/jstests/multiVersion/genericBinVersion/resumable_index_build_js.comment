['text':'*
 * Tests that resumable index builds can resume after restart with a different bin version
 * @tags: [
 *   requires_persistence,
 *   requires_replication
 * ]
 ','line_number':1,'multiline':True]['text':' Create large enough docs so the index build will spill to disk.','line_number':13,'multiline':False]