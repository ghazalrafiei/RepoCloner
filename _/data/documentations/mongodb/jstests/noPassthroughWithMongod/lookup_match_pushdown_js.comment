['text':'*
 * Tests that the $match stage is pushed before $lookup stage.
 ','line_number':1,'multiline':True]['text':' Checks that the order of the pipeline stages matches the expected ordering.','line_number':19,'multiline':False]['text':' Check pipeline is as expected.','line_number':21,'multiline':False]['text':' Check results are as expected.','line_number':33,'multiline':False]['text':' For $eq and $expr:$eq, we should see the same results (in this particular case).','line_number':40,'multiline':False]['text':' Ensure $match gets pushed down into $lookup when $eq is used.','line_number':43,'multiline':False]['text':' Ensure $match gets pushed down into $lookup when $expr:$eq is used.','line_number':51,'multiline':False]['text':' For $eq and $expr:$gt, we should see the same results (in this particular case).','line_number':59,'multiline':False]['text':' Ensure $match gets pushed down into $lookup when $eq is used.','line_number':62,'multiline':False]['text':' Ensure $match gets pushed down into $lookup when $expr:$eq is used.','line_number':70,'multiline':False]