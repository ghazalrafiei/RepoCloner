['text':'*
 * Test that CollectionCloner completes without error when a collection is dropped during cloning.
 ','line_number':1,'multiline':True]['text':' Set up replica set. Disallow chaining so nodes always sync from primary.','line_number':9,'multiline':False]['text':' The default WC is majority and this test can't satisfy majority writes.','line_number':26,'multiline':False]['text':' This function adds data to the collection, restarts the secondary node with the given','line_number':30,'multiline':False]['text':' parameters and setting the given failpoint, waits for the failpoint to be hit,','line_number':31,'multiline':False]['text':' drops the collection, then disables the failpoint.  It then optionally waits for the','line_number':32,'multiline':False]['text':' expectedLog message and waits for the secondary to complete initial sync, then ensures','line_number':33,'multiline':False]['text':' the collection on the secondary is empty.','line_number':34,'multiline':False]['text':' Skip clearing initial sync progress after a successful initial sync attempt so that we','line_number':43,'multiline':False]['text':' can check initialSyncStatus fields after initial sync is complete.','line_number':44,'multiline':False]['text':' Restarting the secondary may have resulted in an election.  Wait until the system','line_number':59,'multiline':False]['text':' stabilizes and reaches RS_STARTUP2 state.','line_number':60,'multiline':False]['text':' Get the uuid for use in checking the log line.','line_number':66,'multiline':False]['text':' Only set for test cases that use 'system.drop' namespaces when dropping collections.','line_number':78,'multiline':False]['text':' In those tests the variable 'rnss' represents such a namespace. Used for expectedLog.','line_number':79,'multiline':False]['text':' See test cases 3 and 4 below.','line_number':80,'multiline':False]['text':' We don't support 4.2 style two-phase drops with EMRC=false - in that configuration, the','line_number':139,'multiline':False]['text':' collection will instead be renamed to a <db>.system.drop.* namespace before being dropped. Since','line_number':140,'multiline':False]['text':' the cloner queries collection by UUID, it may observe the first drop phase as a rename.','line_number':141,'multiline':False]['text':' We still want to check that initial sync succeeds in such a case.','line_number':142,'multiline':False]['text':' Add another node to the set, so when we drop the collection it can commit.  This other','line_number':163,'multiline':False]['text':' secondary will be finished with initial sync when the drop happens.','line_number':164,'multiline':False]