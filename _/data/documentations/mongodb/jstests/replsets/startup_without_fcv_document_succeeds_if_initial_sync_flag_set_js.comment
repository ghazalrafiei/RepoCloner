['text':'*
 * Tests that when a node restarts during initial sync before it can clone the FCV document, it is
 * still able to start up successfully and restart initial sync.
 *
 * This test asserts a certain FCV is cloned through initial sync.
 * @tags: [multiversion_incompatible]
 ','line_number':1,'multiline':True]['text':' Hang initial sync before cloning the FCV document.','line_number':21,'multiline':False]['text':' Get the new secondary connection.','line_number':34,'multiline':False]['text':' Assert that the FCV document was cloned through initial sync on the secondary.','line_number':39,'multiline':False]