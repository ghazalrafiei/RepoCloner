['text':'*
 * Test that a dropIndex on a collection that was renamed is rolled back successfully.
 *
 * This test works by creating a collection and an index in that collection,
 * then renaming that collection and rolling back a drop on that index.
 ','line_number':1,'multiline':True]['text':' Operations that will be present on both nodes, before the common point.','line_number':17,'multiline':False]['text':' This creates the collection implicitly and then creates the index.','line_number':20,'multiline':False]['text':' Operations that will be performed on the rollback node past the common point.','line_number':32,'multiline':False]['text':' Set up Rollback Test.','line_number':42,'multiline':False]['text':' Wait for rollback to finish.','line_number':49,'multiline':False]