['text':'*
 * Tests that the queryExecutor stats are correctly returned when $unionWith is performed on
 * collections.
 *
 * @tags: [
 *     assumes_unsharded_collection,
 *     do_not_wrap_aggregations_in_facets,
 *     assumes_read_preference_unchanged,
 *     assumes_read_concern_unchanged,
 *     assumes_against_mongod_not_mongos,
 *     does_not_support_repeated_reads,
 * ]
 ','line_number':1,'multiline':True]['text':' Collect the previous scannedObjects before running aggregation. This value will be','line_number':33,'multiline':False]['text':' subtracted from the current scannedObjects and will prevent test case from failing if it is','line_number':34,'multiline':False]['text':' run on existing mongoD instance.','line_number':35,'multiline':False]['text':' Concatenate and sort arrays by '_id'.','line_number':46,'multiline':False]