['text':'*
 * Ensure that path expressions over the result of a $replaceRoot stage start from the new root and
 * not from the root of the original document.
 ','line_number':1,'multiline':True]['text':' Simple case where the old and new root both have a document with the same name.','line_number':18,'multiline':False]['text':' This case also exercises an SBE edge case, where optimizing a $replaceRoot can introduce','line_number':26,'multiline':False]['text':' SBE-incompatible operators. If the planner does not observe the change in compatibility and tries','line_number':27,'multiline':False]['text':' to lower the expression to SBE anyway, it will crash the server.','line_number':28,'multiline':False]