['text':'*
 * Tests that using a $graphLookup stage inside of a $facet stage will yield the same results as
 * using the $graphLookup stage outside of the $facet stage.
 ','line_number':1,'multiline':True]['text':' We will only use one collection, the $graphLookup will look up from the same collection.','line_number':7,'multiline':False]['text':' The graph in ASCII form: 0 --- 1 --- 2    3','line_number':10,'multiline':False]['text':' For each document in the collection, this will compute all the other documents that are','line_number':17,'multiline':False]['text':' reachable from this one.','line_number':18,'multiline':False]