['text':'*
 * Test that densify fails if the partition table exceeds the memory limit.
 * @tags: [
 *   # Needed as $densify is a 51 feature.
 *   requires_fcv_51,
 *   not_allowed_with_signed_security_token,
 * ]
 ','line_number':1,'multiline':True]['text':' Lower limit for testing.','line_number':18,'multiline':False]['text':' Create a string entirely of comma characters.','line_number':25,'multiline':False]['text':' Test that densify succeeds when the memory limit would be exceeded, but documents don't need to','line_number':40,'multiline':False]['text':' be densified.','line_number':41,'multiline':False]['text':' Reset limit for other tests.','line_number':50,'multiline':False]