['text':'*
 * Collection of helper functions for testing the $merge aggregation stage.
 ','line_number':1,'multiline':True]['text':'*
 * Executes the callback function with each valid combination of 'whenMatched' and 'whenNotMatched'
 * modes (as named arguments). Note that one mode is a pipeline.
 ','line_number':10,'multiline':True]['text':' In sharded passthrough suites, the error code may be different depending on where we','line_number':45,'multiline':False]['text':' extract the "on" fields.','line_number':46,'multiline':False]['text':' Skip the combination of merge modes which will fail depending on the contents of the','line_number':62,'multiline':False]['text':' source and target collection, as this will cause the assertion below to trip.','line_number':63,'multiline':False]['text':' Do not include the "on" fields in the command if the caller did not specify it.','line_number':73,'multiline':False]['text':' Helper to drop a collection without using the shell helper, and thus avoiding the implicit','line_number':84,'multiline':False]['text':' recreation in the sharded collections passthrough suites.','line_number':85,'multiline':False]